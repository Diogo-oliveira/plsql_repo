-- CHANGED BY: Telmo Castro
-- CHANGE DATE: 31-03-2010
-- CHANGE REASON: SCH-496
create unique index APO_CNT_UK
on APPOINTMENT (CASE  WHEN (ID_CONTENT IS NOT NULL AND NVL(FLG_AVAILABLE,'Y')<>'N') THEN ID_CONTENT END, 
              CASE  WHEN (ID_CONTENT IS NOT NULL AND NVL(FLG_AVAILABLE,'Y')<>'N') THEN FLG_AVAILABLE END);
-- CHANGE END: Telmo Castro