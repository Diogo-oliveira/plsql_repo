/*-- Last Change Revision: $Rev: 1589205 $*/
/*-- Last Change by: $Author: rui.gomes $*/
/*-- Date of last change: $Date: 2014-05-13 12:47:15 +0100 (ter, 13 mai 2014) $*/
CREATE OR REPLACE PACKAGE ts_cda_req_det
/*
| Generated by or retrieved from QCGU - DO NOT MODIFY!
| QCGU - "Get it right, do it fast" - www.ToadWorld.com
| QCGU Universal ID: {1BD37A66-EA60-4927-9A64-F6DD89237236}
| Created On: Maio 8, 2014 11:7:18
| Created By: ALERT
*/
 IS

    -- Collection of %ROWTYPE records based on "CDA_REQ_DET"
    TYPE cda_req_det_tc IS TABLE OF cda_req_det%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE cda_req_det_ntt IS TABLE OF cda_req_det%ROWTYPE;
    TYPE cda_req_det_vat IS VARRAY(100) OF cda_req_det%ROWTYPE;

    -- Same type structure, with a static name.
    TYPE aat IS TABLE OF cda_req_det%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE ntt IS TABLE OF cda_req_det%ROWTYPE;
    TYPE vat IS VARRAY(100) OF cda_req_det%ROWTYPE;

    -- Column Collection based on column "ID_CDA_REQ_DET"
    TYPE id_cda_req_det_cc IS TABLE OF cda_req_det.id_cda_req_det%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_CDA_REQ"
    TYPE id_cda_req_cc IS TABLE OF cda_req_det.id_cda_req%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "FLG_STATUS"
    TYPE flg_status_cc IS TABLE OF cda_req_det.flg_status%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_REPORT"
    TYPE id_report_cc IS TABLE OF cda_req_det.id_report%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "QRDA_TYPE"
    TYPE qrda_type_cc IS TABLE OF cda_req_det.qrda_type%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "DT_STATUS_START"
    TYPE dt_status_start_cc IS TABLE OF cda_req_det.dt_status_start%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "DT_STATUS_END"
    TYPE dt_status_end_cc IS TABLE OF cda_req_det.dt_status_end%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_USER"
    TYPE create_user_cc IS TABLE OF cda_req_det.create_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_TIME"
    TYPE create_time_cc IS TABLE OF cda_req_det.create_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_INSTITUTION"
    TYPE create_institution_cc IS TABLE OF cda_req_det.create_institution%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_USER"
    TYPE update_user_cc IS TABLE OF cda_req_det.update_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_TIME"
    TYPE update_time_cc IS TABLE OF cda_req_det.update_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_INSTITUTION"
    TYPE update_institution_cc IS TABLE OF cda_req_det.update_institution%TYPE INDEX BY BINARY_INTEGER;

    -- Insert one row, providing primary key if present
    PROCEDURE ins
    (
        id_cda_req_det_in     IN cda_req_det.id_cda_req_det%TYPE,
        id_cda_req_in         IN cda_req_det.id_cda_req%TYPE DEFAULT NULL,
        flg_status_in         IN cda_req_det.flg_status%TYPE DEFAULT NULL,
        id_report_in          IN cda_req_det.id_report%TYPE DEFAULT NULL,
        qrda_type_in          IN cda_req_det.qrda_type%TYPE DEFAULT NULL,
        dt_status_start_in    IN cda_req_det.dt_status_start%TYPE DEFAULT current_timestamp,
        dt_status_end_in      IN cda_req_det.dt_status_end%TYPE DEFAULT NULL,
        create_user_in        IN cda_req_det.create_user%TYPE DEFAULT NULL,
        create_time_in        IN cda_req_det.create_time%TYPE DEFAULT NULL,
        create_institution_in IN cda_req_det.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN cda_req_det.update_user%TYPE DEFAULT NULL,
        update_time_in        IN cda_req_det.update_time%TYPE DEFAULT NULL,
        update_institution_in IN cda_req_det.update_institution%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_cda_req_det_in     IN cda_req_det.id_cda_req_det%TYPE,
        id_cda_req_in         IN cda_req_det.id_cda_req%TYPE DEFAULT NULL,
        flg_status_in         IN cda_req_det.flg_status%TYPE DEFAULT NULL,
        id_report_in          IN cda_req_det.id_report%TYPE DEFAULT NULL,
        qrda_type_in          IN cda_req_det.qrda_type%TYPE DEFAULT NULL,
        dt_status_start_in    IN cda_req_det.dt_status_start%TYPE DEFAULT current_timestamp,
        dt_status_end_in      IN cda_req_det.dt_status_end%TYPE DEFAULT NULL,
        create_user_in        IN cda_req_det.create_user%TYPE DEFAULT NULL,
        create_time_in        IN cda_req_det.create_time%TYPE DEFAULT NULL,
        create_institution_in IN cda_req_det.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN cda_req_det.update_user%TYPE DEFAULT NULL,
        update_time_in        IN cda_req_det.update_time%TYPE DEFAULT NULL,
        update_institution_in IN cda_req_det.update_institution%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a row based on a record.
    -- Specify whether or not a primary key value should be generated.
    PROCEDURE ins
    (
        rec_in          IN cda_req_det%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rec_in          IN cda_req_det%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a collection of rows using FORALL; all primary key values
    -- must have already been generated, or are handled in triggers.
    PROCEDURE ins
    (
        rows_in         IN cda_req_det_tc,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rows_in         IN cda_req_det_tc,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Return next primary key value using the named sequence.
    FUNCTION next_key(sequence_in IN VARCHAR2 := NULL) RETURN cda_req_det.id_cda_req_det%TYPE;

    -- Insert one row, generating hidden primary key using a sequence
    PROCEDURE ins
    (
        id_cda_req_in         IN cda_req_det.id_cda_req%TYPE DEFAULT NULL,
        flg_status_in         IN cda_req_det.flg_status%TYPE DEFAULT NULL,
        id_report_in          IN cda_req_det.id_report%TYPE DEFAULT NULL,
        qrda_type_in          IN cda_req_det.qrda_type%TYPE DEFAULT NULL,
        dt_status_start_in    IN cda_req_det.dt_status_start%TYPE DEFAULT current_timestamp,
        dt_status_end_in      IN cda_req_det.dt_status_end%TYPE DEFAULT NULL,
        create_user_in        IN cda_req_det.create_user%TYPE DEFAULT NULL,
        create_time_in        IN cda_req_det.create_time%TYPE DEFAULT NULL,
        create_institution_in IN cda_req_det.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN cda_req_det.update_user%TYPE DEFAULT NULL,
        update_time_in        IN cda_req_det.update_time%TYPE DEFAULT NULL,
        update_institution_in IN cda_req_det.update_institution%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_cda_req_in         IN cda_req_det.id_cda_req%TYPE DEFAULT NULL,
        flg_status_in         IN cda_req_det.flg_status%TYPE DEFAULT NULL,
        id_report_in          IN cda_req_det.id_report%TYPE DEFAULT NULL,
        qrda_type_in          IN cda_req_det.qrda_type%TYPE DEFAULT NULL,
        dt_status_start_in    IN cda_req_det.dt_status_start%TYPE DEFAULT current_timestamp,
        dt_status_end_in      IN cda_req_det.dt_status_end%TYPE DEFAULT NULL,
        create_user_in        IN cda_req_det.create_user%TYPE DEFAULT NULL,
        create_time_in        IN cda_req_det.create_time%TYPE DEFAULT NULL,
        create_institution_in IN cda_req_det.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN cda_req_det.update_user%TYPE DEFAULT NULL,
        update_time_in        IN cda_req_det.update_time%TYPE DEFAULT NULL,
        update_institution_in IN cda_req_det.update_institution%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert one row, returning primary key generated by sequence
    PROCEDURE ins
    (
        id_cda_req_in         IN cda_req_det.id_cda_req%TYPE DEFAULT NULL,
        flg_status_in         IN cda_req_det.flg_status%TYPE DEFAULT NULL,
        id_report_in          IN cda_req_det.id_report%TYPE DEFAULT NULL,
        qrda_type_in          IN cda_req_det.qrda_type%TYPE DEFAULT NULL,
        dt_status_start_in    IN cda_req_det.dt_status_start%TYPE DEFAULT current_timestamp,
        dt_status_end_in      IN cda_req_det.dt_status_end%TYPE DEFAULT NULL,
        create_user_in        IN cda_req_det.create_user%TYPE DEFAULT NULL,
        create_time_in        IN cda_req_det.create_time%TYPE DEFAULT NULL,
        create_institution_in IN cda_req_det.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN cda_req_det.update_user%TYPE DEFAULT NULL,
        update_time_in        IN cda_req_det.update_time%TYPE DEFAULT NULL,
        update_institution_in IN cda_req_det.update_institution%TYPE DEFAULT NULL,
        id_cda_req_det_out    IN OUT cda_req_det.id_cda_req_det%TYPE
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_cda_req_in         IN cda_req_det.id_cda_req%TYPE DEFAULT NULL,
        flg_status_in         IN cda_req_det.flg_status%TYPE DEFAULT NULL,
        id_report_in          IN cda_req_det.id_report%TYPE DEFAULT NULL,
        qrda_type_in          IN cda_req_det.qrda_type%TYPE DEFAULT NULL,
        dt_status_start_in    IN cda_req_det.dt_status_start%TYPE DEFAULT current_timestamp,
        dt_status_end_in      IN cda_req_det.dt_status_end%TYPE DEFAULT NULL,
        create_user_in        IN cda_req_det.create_user%TYPE DEFAULT NULL,
        create_time_in        IN cda_req_det.create_time%TYPE DEFAULT NULL,
        create_institution_in IN cda_req_det.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN cda_req_det.update_user%TYPE DEFAULT NULL,
        update_time_in        IN cda_req_det.update_time%TYPE DEFAULT NULL,
        update_institution_in IN cda_req_det.update_institution%TYPE DEFAULT NULL,
        id_cda_req_det_out    IN OUT cda_req_det.id_cda_req_det%TYPE
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert one row with function, return generated primary key
    FUNCTION ins
    (
        id_cda_req_in         IN cda_req_det.id_cda_req%TYPE DEFAULT NULL,
        flg_status_in         IN cda_req_det.flg_status%TYPE DEFAULT NULL,
        id_report_in          IN cda_req_det.id_report%TYPE DEFAULT NULL,
        qrda_type_in          IN cda_req_det.qrda_type%TYPE DEFAULT NULL,
        dt_status_start_in    IN cda_req_det.dt_status_start%TYPE DEFAULT current_timestamp,
        dt_status_end_in      IN cda_req_det.dt_status_end%TYPE DEFAULT NULL,
        create_user_in        IN cda_req_det.create_user%TYPE DEFAULT NULL,
        create_time_in        IN cda_req_det.create_time%TYPE DEFAULT NULL,
        create_institution_in IN cda_req_det.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN cda_req_det.update_user%TYPE DEFAULT NULL,
        update_time_in        IN cda_req_det.update_time%TYPE DEFAULT NULL,
        update_institution_in IN cda_req_det.update_institution%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) RETURN cda_req_det.id_cda_req_det%TYPE;

    FUNCTION ins
    (
        id_cda_req_in         IN cda_req_det.id_cda_req%TYPE DEFAULT NULL,
        flg_status_in         IN cda_req_det.flg_status%TYPE DEFAULT NULL,
        id_report_in          IN cda_req_det.id_report%TYPE DEFAULT NULL,
        qrda_type_in          IN cda_req_det.qrda_type%TYPE DEFAULT NULL,
        dt_status_start_in    IN cda_req_det.dt_status_start%TYPE DEFAULT current_timestamp,
        dt_status_end_in      IN cda_req_det.dt_status_end%TYPE DEFAULT NULL,
        create_user_in        IN cda_req_det.create_user%TYPE DEFAULT NULL,
        create_time_in        IN cda_req_det.create_time%TYPE DEFAULT NULL,
        create_institution_in IN cda_req_det.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN cda_req_det.update_user%TYPE DEFAULT NULL,
        update_time_in        IN cda_req_det.update_time%TYPE DEFAULT NULL,
        update_institution_in IN cda_req_det.update_institution%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    ) RETURN cda_req_det.id_cda_req_det%TYPE;

    -- Update any/all columns by primary key. If you pass NULL, then
    -- the current column value is set to itself. If you need a more
    -- selected UPDATE then use one of the onecol procedures below.

    PROCEDURE upd
    (
        id_cda_req_det_in      IN cda_req_det.id_cda_req_det%TYPE,
        id_cda_req_in          IN cda_req_det.id_cda_req%TYPE DEFAULT NULL,
        id_cda_req_nin         IN BOOLEAN := TRUE,
        flg_status_in          IN cda_req_det.flg_status%TYPE DEFAULT NULL,
        flg_status_nin         IN BOOLEAN := TRUE,
        id_report_in           IN cda_req_det.id_report%TYPE DEFAULT NULL,
        id_report_nin          IN BOOLEAN := TRUE,
        qrda_type_in           IN cda_req_det.qrda_type%TYPE DEFAULT NULL,
        qrda_type_nin          IN BOOLEAN := TRUE,
        dt_status_start_in     IN cda_req_det.dt_status_start%TYPE DEFAULT NULL,
        dt_status_start_nin    IN BOOLEAN := TRUE,
        dt_status_end_in       IN cda_req_det.dt_status_end%TYPE DEFAULT NULL,
        dt_status_end_nin      IN BOOLEAN := TRUE,
        create_user_in         IN cda_req_det.create_user%TYPE DEFAULT NULL,
        create_user_nin        IN BOOLEAN := TRUE,
        create_time_in         IN cda_req_det.create_time%TYPE DEFAULT NULL,
        create_time_nin        IN BOOLEAN := TRUE,
        create_institution_in  IN cda_req_det.create_institution%TYPE DEFAULT NULL,
        create_institution_nin IN BOOLEAN := TRUE,
        update_user_in         IN cda_req_det.update_user%TYPE DEFAULT NULL,
        update_user_nin        IN BOOLEAN := TRUE,
        update_time_in         IN cda_req_det.update_time%TYPE DEFAULT NULL,
        update_time_nin        IN BOOLEAN := TRUE,
        update_institution_in  IN cda_req_det.update_institution%TYPE DEFAULT NULL,
        update_institution_nin IN BOOLEAN := TRUE,
        handle_error_in        IN BOOLEAN := TRUE,
        rows_out               IN OUT table_varchar
    );

    PROCEDURE upd
    (
        id_cda_req_det_in      IN cda_req_det.id_cda_req_det%TYPE,
        id_cda_req_in          IN cda_req_det.id_cda_req%TYPE DEFAULT NULL,
        id_cda_req_nin         IN BOOLEAN := TRUE,
        flg_status_in          IN cda_req_det.flg_status%TYPE DEFAULT NULL,
        flg_status_nin         IN BOOLEAN := TRUE,
        id_report_in           IN cda_req_det.id_report%TYPE DEFAULT NULL,
        id_report_nin          IN BOOLEAN := TRUE,
        qrda_type_in           IN cda_req_det.qrda_type%TYPE DEFAULT NULL,
        qrda_type_nin          IN BOOLEAN := TRUE,
        dt_status_start_in     IN cda_req_det.dt_status_start%TYPE DEFAULT NULL,
        dt_status_start_nin    IN BOOLEAN := TRUE,
        dt_status_end_in       IN cda_req_det.dt_status_end%TYPE DEFAULT NULL,
        dt_status_end_nin      IN BOOLEAN := TRUE,
        create_user_in         IN cda_req_det.create_user%TYPE DEFAULT NULL,
        create_user_nin        IN BOOLEAN := TRUE,
        create_time_in         IN cda_req_det.create_time%TYPE DEFAULT NULL,
        create_time_nin        IN BOOLEAN := TRUE,
        create_institution_in  IN cda_req_det.create_institution%TYPE DEFAULT NULL,
        create_institution_nin IN BOOLEAN := TRUE,
        update_user_in         IN cda_req_det.update_user%TYPE DEFAULT NULL,
        update_user_nin        IN BOOLEAN := TRUE,
        update_time_in         IN cda_req_det.update_time%TYPE DEFAULT NULL,
        update_time_nin        IN BOOLEAN := TRUE,
        update_institution_in  IN cda_req_det.update_institution%TYPE DEFAULT NULL,
        update_institution_nin IN BOOLEAN := TRUE,
        handle_error_in        IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        id_cda_req_in          IN cda_req_det.id_cda_req%TYPE DEFAULT NULL,
        id_cda_req_nin         IN BOOLEAN := TRUE,
        flg_status_in          IN cda_req_det.flg_status%TYPE DEFAULT NULL,
        flg_status_nin         IN BOOLEAN := TRUE,
        id_report_in           IN cda_req_det.id_report%TYPE DEFAULT NULL,
        id_report_nin          IN BOOLEAN := TRUE,
        qrda_type_in           IN cda_req_det.qrda_type%TYPE DEFAULT NULL,
        qrda_type_nin          IN BOOLEAN := TRUE,
        dt_status_start_in     IN cda_req_det.dt_status_start%TYPE DEFAULT NULL,
        dt_status_start_nin    IN BOOLEAN := TRUE,
        dt_status_end_in       IN cda_req_det.dt_status_end%TYPE DEFAULT NULL,
        dt_status_end_nin      IN BOOLEAN := TRUE,
        create_user_in         IN cda_req_det.create_user%TYPE DEFAULT NULL,
        create_user_nin        IN BOOLEAN := TRUE,
        create_time_in         IN cda_req_det.create_time%TYPE DEFAULT NULL,
        create_time_nin        IN BOOLEAN := TRUE,
        create_institution_in  IN cda_req_det.create_institution%TYPE DEFAULT NULL,
        create_institution_nin IN BOOLEAN := TRUE,
        update_user_in         IN cda_req_det.update_user%TYPE DEFAULT NULL,
        update_user_nin        IN BOOLEAN := TRUE,
        update_time_in         IN cda_req_det.update_time%TYPE DEFAULT NULL,
        update_time_nin        IN BOOLEAN := TRUE,
        update_institution_in  IN cda_req_det.update_institution%TYPE DEFAULT NULL,
        update_institution_nin IN BOOLEAN := TRUE,
        where_in               VARCHAR2,
        handle_error_in        IN BOOLEAN := TRUE,
        rows_out               IN OUT table_varchar
    );

    PROCEDURE upd
    (
        id_cda_req_in          IN cda_req_det.id_cda_req%TYPE DEFAULT NULL,
        id_cda_req_nin         IN BOOLEAN := TRUE,
        flg_status_in          IN cda_req_det.flg_status%TYPE DEFAULT NULL,
        flg_status_nin         IN BOOLEAN := TRUE,
        id_report_in           IN cda_req_det.id_report%TYPE DEFAULT NULL,
        id_report_nin          IN BOOLEAN := TRUE,
        qrda_type_in           IN cda_req_det.qrda_type%TYPE DEFAULT NULL,
        qrda_type_nin          IN BOOLEAN := TRUE,
        dt_status_start_in     IN cda_req_det.dt_status_start%TYPE DEFAULT NULL,
        dt_status_start_nin    IN BOOLEAN := TRUE,
        dt_status_end_in       IN cda_req_det.dt_status_end%TYPE DEFAULT NULL,
        dt_status_end_nin      IN BOOLEAN := TRUE,
        create_user_in         IN cda_req_det.create_user%TYPE DEFAULT NULL,
        create_user_nin        IN BOOLEAN := TRUE,
        create_time_in         IN cda_req_det.create_time%TYPE DEFAULT NULL,
        create_time_nin        IN BOOLEAN := TRUE,
        create_institution_in  IN cda_req_det.create_institution%TYPE DEFAULT NULL,
        create_institution_nin IN BOOLEAN := TRUE,
        update_user_in         IN cda_req_det.update_user%TYPE DEFAULT NULL,
        update_user_nin        IN BOOLEAN := TRUE,
        update_time_in         IN cda_req_det.update_time%TYPE DEFAULT NULL,
        update_time_nin        IN BOOLEAN := TRUE,
        update_institution_in  IN cda_req_det.update_institution%TYPE DEFAULT NULL,
        update_institution_nin IN BOOLEAN := TRUE,
        where_in               VARCHAR2,
        handle_error_in        IN BOOLEAN := TRUE
    );

    PROCEDURE upd_ins
    (
        id_cda_req_det_in     IN cda_req_det.id_cda_req_det%TYPE,
        id_cda_req_in         IN cda_req_det.id_cda_req%TYPE DEFAULT NULL,
        flg_status_in         IN cda_req_det.flg_status%TYPE DEFAULT NULL,
        id_report_in          IN cda_req_det.id_report%TYPE DEFAULT NULL,
        qrda_type_in          IN cda_req_det.qrda_type%TYPE DEFAULT NULL,
        dt_status_start_in    IN cda_req_det.dt_status_start%TYPE DEFAULT NULL,
        dt_status_end_in      IN cda_req_det.dt_status_end%TYPE DEFAULT NULL,
        create_user_in        IN cda_req_det.create_user%TYPE DEFAULT NULL,
        create_time_in        IN cda_req_det.create_time%TYPE DEFAULT NULL,
        create_institution_in IN cda_req_det.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN cda_req_det.update_user%TYPE DEFAULT NULL,
        update_time_in        IN cda_req_det.update_time%TYPE DEFAULT NULL,
        update_institution_in IN cda_req_det.update_institution%TYPE DEFAULT NULL,
        handle_error_in       IN BOOLEAN := TRUE,
        rows_out              OUT table_varchar
    );

    PROCEDURE upd_ins
    (
        id_cda_req_det_in     IN cda_req_det.id_cda_req_det%TYPE,
        id_cda_req_in         IN cda_req_det.id_cda_req%TYPE DEFAULT NULL,
        flg_status_in         IN cda_req_det.flg_status%TYPE DEFAULT NULL,
        id_report_in          IN cda_req_det.id_report%TYPE DEFAULT NULL,
        qrda_type_in          IN cda_req_det.qrda_type%TYPE DEFAULT NULL,
        dt_status_start_in    IN cda_req_det.dt_status_start%TYPE DEFAULT NULL,
        dt_status_end_in      IN cda_req_det.dt_status_end%TYPE DEFAULT NULL,
        create_user_in        IN cda_req_det.create_user%TYPE DEFAULT NULL,
        create_time_in        IN cda_req_det.create_time%TYPE DEFAULT NULL,
        create_institution_in IN cda_req_det.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN cda_req_det.update_user%TYPE DEFAULT NULL,
        update_time_in        IN cda_req_det.update_time%TYPE DEFAULT NULL,
        update_institution_in IN cda_req_det.update_institution%TYPE DEFAULT NULL,
        handle_error_in       IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        rec_in          IN cda_req_det%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        IN OUT table_varchar
    );

    PROCEDURE upd
    (
        rec_in          IN cda_req_det%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        col_in            IN cda_req_det_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          IN OUT table_varchar
    );

    PROCEDURE upd
    (
        col_in            IN cda_req_det_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE
    );

    -- Use Native Dynamic SQL increment a single NUMBER column
    -- for all rows specified by the dynamic WHERE clause
    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_cda_req_det_in IN cda_req_det.id_cda_req_det%TYPE,
        handle_error_in   IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_cda_req_det_in IN cda_req_det.id_cda_req_det%TYPE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          OUT table_varchar
    );

    -- Delete all rows for primary key column ID_CDA_REQ_DET
    PROCEDURE del_id_cda_req_det
    (
        id_cda_req_det_in IN cda_req_det.id_cda_req_det%TYPE,
        handle_error_in   IN BOOLEAN := TRUE
    );

    -- Delete all rows for primary key column ID_CDA_REQ_DET
    PROCEDURE del_id_cda_req_det
    (
        id_cda_req_det_in IN cda_req_det.id_cda_req_det%TYPE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          OUT table_varchar
    );

    -- Delete for unique value of CDARD_UK
    PROCEDURE del_cdard_uk
    (
        id_cda_req_in   IN cda_req_det.id_cda_req%TYPE,
        flg_status_in   IN cda_req_det.flg_status%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete for unique value of CDARD_UK
    PROCEDURE del_cdard_uk
    (
        id_cda_req_in   IN cda_req_det.id_cda_req%TYPE,
        flg_status_in   IN cda_req_det.flg_status%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for this CDARD_CDAR_FK foreign key value
    PROCEDURE del_cdard_cdar_fk
    (
        id_cda_req_in   IN cda_req_det.id_cda_req%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this CDARD_CDAR_FK foreign key value
    PROCEDURE del_cdard_cdar_fk
    (
        id_cda_req_in   IN cda_req_det.id_cda_req%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for this CDARD_REP_FK foreign key value
    PROCEDURE del_cdard_rep_fk
    (
        id_report_in    IN cda_req_det.id_report%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this CDARD_REP_FK foreign key value
    PROCEDURE del_cdard_rep_fk
    (
        id_report_in    IN cda_req_det.id_report%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Initialize a record with default values for columns in the table.
    PROCEDURE initrec(cda_req_det_inout IN OUT cda_req_det%ROWTYPE);

    FUNCTION initrec RETURN cda_req_det%ROWTYPE;

    FUNCTION get_data_rowid(rows_in IN table_varchar) RETURN cda_req_det_tc;

    FUNCTION get_data_rowid_pat(rows_in IN table_varchar) RETURN cda_req_det_tc;

END ts_cda_req_det;
/
