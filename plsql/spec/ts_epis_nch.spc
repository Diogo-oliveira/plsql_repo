/*-- Last Change Revision: $Rev: 2029166 $*/
/*-- Last Change by: $Author: mario.fernandes $*/
/*-- Date of last change: $Date: 2022-08-02 18:50:10 +0100 (ter, 02 ago 2022) $*/


CREATE OR REPLACE PACKAGE ts_epis_nch
/*
| Generated by or retrieved from QCGU - DO NOT MODIFY!
| QCGU - "Get it right, do it fast" - www.ToadWorld.com
| QCGU Universal ID: {1BD37A66-EA60-4927-9A64-F6DD89237236}
| Created On: Março 19, 2010 12:32:28
| Created By: ALERT
*/
 IS

    -- Collection of %ROWTYPE records based on "EPIS_NCH"
    TYPE epis_nch_tc IS TABLE OF epis_nch%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE epis_nch_ntt IS TABLE OF epis_nch%ROWTYPE;
    TYPE epis_nch_vat IS VARRAY(100) OF epis_nch%ROWTYPE;

    -- Same type structure, with a static name.
    TYPE aat IS TABLE OF epis_nch%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE ntt IS TABLE OF epis_nch%ROWTYPE;
    TYPE vat IS VARRAY(100) OF epis_nch%ROWTYPE;

    -- Column Collection based on column "ID_EPIS_NCH"
    TYPE id_epis_nch_cc IS TABLE OF epis_nch.id_epis_nch%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_EPISODE"
    TYPE id_episode_cc IS TABLE OF epis_nch.id_episode%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_PATIENT"
    TYPE id_patient_cc IS TABLE OF epis_nch.id_patient%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "NCH_VALUE"
    TYPE nch_value_cc IS TABLE OF epis_nch.nch_value%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "DT_BEGIN"
    TYPE dt_begin_cc IS TABLE OF epis_nch.dt_begin%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "DT_END"
    TYPE dt_end_cc IS TABLE OF epis_nch.dt_end%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "FLG_STATUS"
    TYPE flg_status_cc IS TABLE OF epis_nch.flg_status%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "FLG_TYPE"
    TYPE flg_type_cc IS TABLE OF epis_nch.flg_type%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "DT_CREATION"
    TYPE dt_creation_cc IS TABLE OF epis_nch.dt_creation%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_PROF_CREATION"
    TYPE id_prof_creation_cc IS TABLE OF epis_nch.id_prof_creation%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_NCH_LEVEL"
    TYPE id_nch_level_cc IS TABLE OF epis_nch.id_nch_level%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_USER"
    TYPE create_user_cc IS TABLE OF epis_nch.create_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_TIME"
    TYPE create_time_cc IS TABLE OF epis_nch.create_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_INSTITUTION"
    TYPE create_institution_cc IS TABLE OF epis_nch.create_institution%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_USER"
    TYPE update_user_cc IS TABLE OF epis_nch.update_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_TIME"
    TYPE update_time_cc IS TABLE OF epis_nch.update_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_INSTITUTION"
    TYPE update_institution_cc IS TABLE OF epis_nch.update_institution%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "REASON_NOTES"
    TYPE reason_notes_cc IS TABLE OF epis_nch.reason_notes%TYPE INDEX BY BINARY_INTEGER;

    -- Insert one row, providing primary key if present
    PROCEDURE ins
    (
        id_epis_nch_in        IN epis_nch.id_epis_nch%TYPE,
        id_episode_in         IN epis_nch.id_episode%TYPE DEFAULT NULL,
        id_patient_in         IN epis_nch.id_patient%TYPE DEFAULT NULL,
        nch_value_in          IN epis_nch.nch_value%TYPE DEFAULT NULL,
        dt_begin_in           IN epis_nch.dt_begin%TYPE DEFAULT NULL,
        dt_end_in             IN epis_nch.dt_end%TYPE DEFAULT NULL,
        flg_status_in         IN epis_nch.flg_status%TYPE DEFAULT 'A',
        flg_type_in           IN epis_nch.flg_type%TYPE DEFAULT 'D',
        dt_creation_in        IN epis_nch.dt_creation%TYPE DEFAULT current_timestamp,
        id_prof_creation_in   IN epis_nch.id_prof_creation%TYPE DEFAULT NULL,
        id_nch_level_in       IN epis_nch.id_nch_level%TYPE DEFAULT NULL,
        create_user_in        IN epis_nch.create_user%TYPE DEFAULT NULL,
        create_time_in        IN epis_nch.create_time%TYPE DEFAULT NULL,
        create_institution_in IN epis_nch.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN epis_nch.update_user%TYPE DEFAULT NULL,
        update_time_in        IN epis_nch.update_time%TYPE DEFAULT NULL,
        update_institution_in IN epis_nch.update_institution%TYPE DEFAULT NULL,
        reason_notes_in       IN epis_nch.reason_notes%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_epis_nch_in        IN epis_nch.id_epis_nch%TYPE,
        id_episode_in         IN epis_nch.id_episode%TYPE DEFAULT NULL,
        id_patient_in         IN epis_nch.id_patient%TYPE DEFAULT NULL,
        nch_value_in          IN epis_nch.nch_value%TYPE DEFAULT NULL,
        dt_begin_in           IN epis_nch.dt_begin%TYPE DEFAULT NULL,
        dt_end_in             IN epis_nch.dt_end%TYPE DEFAULT NULL,
        flg_status_in         IN epis_nch.flg_status%TYPE DEFAULT 'A',
        flg_type_in           IN epis_nch.flg_type%TYPE DEFAULT 'D',
        dt_creation_in        IN epis_nch.dt_creation%TYPE DEFAULT current_timestamp,
        id_prof_creation_in   IN epis_nch.id_prof_creation%TYPE DEFAULT NULL,
        id_nch_level_in       IN epis_nch.id_nch_level%TYPE DEFAULT NULL,
        create_user_in        IN epis_nch.create_user%TYPE DEFAULT NULL,
        create_time_in        IN epis_nch.create_time%TYPE DEFAULT NULL,
        create_institution_in IN epis_nch.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN epis_nch.update_user%TYPE DEFAULT NULL,
        update_time_in        IN epis_nch.update_time%TYPE DEFAULT NULL,
        update_institution_in IN epis_nch.update_institution%TYPE DEFAULT NULL,
        reason_notes_in       IN epis_nch.reason_notes%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a row based on a record.
    -- Specify whether or not a primary key value should be generated.
    PROCEDURE ins
    (
        rec_in          IN epis_nch%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rec_in          IN epis_nch%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a collection of rows using FORALL; all primary key values
    -- must have already been generated, or are handled in triggers.
    PROCEDURE ins
    (
        rows_in         IN epis_nch_tc,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rows_in         IN epis_nch_tc,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Return next primary key value using the named sequence.
    FUNCTION next_key(sequence_in IN VARCHAR2 := NULL) RETURN epis_nch.id_epis_nch%TYPE;

    -- Insert one row, generating hidden primary key using a sequence
    PROCEDURE ins
    (
        id_episode_in         IN epis_nch.id_episode%TYPE DEFAULT NULL,
        id_patient_in         IN epis_nch.id_patient%TYPE DEFAULT NULL,
        nch_value_in          IN epis_nch.nch_value%TYPE DEFAULT NULL,
        dt_begin_in           IN epis_nch.dt_begin%TYPE DEFAULT NULL,
        dt_end_in             IN epis_nch.dt_end%TYPE DEFAULT NULL,
        flg_status_in         IN epis_nch.flg_status%TYPE DEFAULT 'A',
        flg_type_in           IN epis_nch.flg_type%TYPE DEFAULT 'D',
        dt_creation_in        IN epis_nch.dt_creation%TYPE DEFAULT current_timestamp,
        id_prof_creation_in   IN epis_nch.id_prof_creation%TYPE DEFAULT NULL,
        id_nch_level_in       IN epis_nch.id_nch_level%TYPE DEFAULT NULL,
        create_user_in        IN epis_nch.create_user%TYPE DEFAULT NULL,
        create_time_in        IN epis_nch.create_time%TYPE DEFAULT NULL,
        create_institution_in IN epis_nch.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN epis_nch.update_user%TYPE DEFAULT NULL,
        update_time_in        IN epis_nch.update_time%TYPE DEFAULT NULL,
        update_institution_in IN epis_nch.update_institution%TYPE DEFAULT NULL,
        reason_notes_in       IN epis_nch.reason_notes%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_episode_in         IN epis_nch.id_episode%TYPE DEFAULT NULL,
        id_patient_in         IN epis_nch.id_patient%TYPE DEFAULT NULL,
        nch_value_in          IN epis_nch.nch_value%TYPE DEFAULT NULL,
        dt_begin_in           IN epis_nch.dt_begin%TYPE DEFAULT NULL,
        dt_end_in             IN epis_nch.dt_end%TYPE DEFAULT NULL,
        flg_status_in         IN epis_nch.flg_status%TYPE DEFAULT 'A',
        flg_type_in           IN epis_nch.flg_type%TYPE DEFAULT 'D',
        dt_creation_in        IN epis_nch.dt_creation%TYPE DEFAULT current_timestamp,
        id_prof_creation_in   IN epis_nch.id_prof_creation%TYPE DEFAULT NULL,
        id_nch_level_in       IN epis_nch.id_nch_level%TYPE DEFAULT NULL,
        create_user_in        IN epis_nch.create_user%TYPE DEFAULT NULL,
        create_time_in        IN epis_nch.create_time%TYPE DEFAULT NULL,
        create_institution_in IN epis_nch.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN epis_nch.update_user%TYPE DEFAULT NULL,
        update_time_in        IN epis_nch.update_time%TYPE DEFAULT NULL,
        update_institution_in IN epis_nch.update_institution%TYPE DEFAULT NULL,
        reason_notes_in       IN epis_nch.reason_notes%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert one row, returning primary key generated by sequence
    PROCEDURE ins
    (
        id_episode_in         IN epis_nch.id_episode%TYPE DEFAULT NULL,
        id_patient_in         IN epis_nch.id_patient%TYPE DEFAULT NULL,
        nch_value_in          IN epis_nch.nch_value%TYPE DEFAULT NULL,
        dt_begin_in           IN epis_nch.dt_begin%TYPE DEFAULT NULL,
        dt_end_in             IN epis_nch.dt_end%TYPE DEFAULT NULL,
        flg_status_in         IN epis_nch.flg_status%TYPE DEFAULT 'A',
        flg_type_in           IN epis_nch.flg_type%TYPE DEFAULT 'D',
        dt_creation_in        IN epis_nch.dt_creation%TYPE DEFAULT current_timestamp,
        id_prof_creation_in   IN epis_nch.id_prof_creation%TYPE DEFAULT NULL,
        id_nch_level_in       IN epis_nch.id_nch_level%TYPE DEFAULT NULL,
        create_user_in        IN epis_nch.create_user%TYPE DEFAULT NULL,
        create_time_in        IN epis_nch.create_time%TYPE DEFAULT NULL,
        create_institution_in IN epis_nch.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN epis_nch.update_user%TYPE DEFAULT NULL,
        update_time_in        IN epis_nch.update_time%TYPE DEFAULT NULL,
        update_institution_in IN epis_nch.update_institution%TYPE DEFAULT NULL,
        reason_notes_in       IN epis_nch.reason_notes%TYPE DEFAULT NULL,
        id_epis_nch_out       IN OUT epis_nch.id_epis_nch%TYPE
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_episode_in         IN epis_nch.id_episode%TYPE DEFAULT NULL,
        id_patient_in         IN epis_nch.id_patient%TYPE DEFAULT NULL,
        nch_value_in          IN epis_nch.nch_value%TYPE DEFAULT NULL,
        dt_begin_in           IN epis_nch.dt_begin%TYPE DEFAULT NULL,
        dt_end_in             IN epis_nch.dt_end%TYPE DEFAULT NULL,
        flg_status_in         IN epis_nch.flg_status%TYPE DEFAULT 'A',
        flg_type_in           IN epis_nch.flg_type%TYPE DEFAULT 'D',
        dt_creation_in        IN epis_nch.dt_creation%TYPE DEFAULT current_timestamp,
        id_prof_creation_in   IN epis_nch.id_prof_creation%TYPE DEFAULT NULL,
        id_nch_level_in       IN epis_nch.id_nch_level%TYPE DEFAULT NULL,
        create_user_in        IN epis_nch.create_user%TYPE DEFAULT NULL,
        create_time_in        IN epis_nch.create_time%TYPE DEFAULT NULL,
        create_institution_in IN epis_nch.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN epis_nch.update_user%TYPE DEFAULT NULL,
        update_time_in        IN epis_nch.update_time%TYPE DEFAULT NULL,
        update_institution_in IN epis_nch.update_institution%TYPE DEFAULT NULL,
        reason_notes_in       IN epis_nch.reason_notes%TYPE DEFAULT NULL,
        id_epis_nch_out       IN OUT epis_nch.id_epis_nch%TYPE
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert one row with function, return generated primary key
    FUNCTION ins
    (
        id_episode_in         IN epis_nch.id_episode%TYPE DEFAULT NULL,
        id_patient_in         IN epis_nch.id_patient%TYPE DEFAULT NULL,
        nch_value_in          IN epis_nch.nch_value%TYPE DEFAULT NULL,
        dt_begin_in           IN epis_nch.dt_begin%TYPE DEFAULT NULL,
        dt_end_in             IN epis_nch.dt_end%TYPE DEFAULT NULL,
        flg_status_in         IN epis_nch.flg_status%TYPE DEFAULT 'A',
        flg_type_in           IN epis_nch.flg_type%TYPE DEFAULT 'D',
        dt_creation_in        IN epis_nch.dt_creation%TYPE DEFAULT current_timestamp,
        id_prof_creation_in   IN epis_nch.id_prof_creation%TYPE DEFAULT NULL,
        id_nch_level_in       IN epis_nch.id_nch_level%TYPE DEFAULT NULL,
        create_user_in        IN epis_nch.create_user%TYPE DEFAULT NULL,
        create_time_in        IN epis_nch.create_time%TYPE DEFAULT NULL,
        create_institution_in IN epis_nch.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN epis_nch.update_user%TYPE DEFAULT NULL,
        update_time_in        IN epis_nch.update_time%TYPE DEFAULT NULL,
        update_institution_in IN epis_nch.update_institution%TYPE DEFAULT NULL,
        reason_notes_in       IN epis_nch.reason_notes%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) RETURN epis_nch.id_epis_nch%TYPE;

    FUNCTION ins
    (
        id_episode_in         IN epis_nch.id_episode%TYPE DEFAULT NULL,
        id_patient_in         IN epis_nch.id_patient%TYPE DEFAULT NULL,
        nch_value_in          IN epis_nch.nch_value%TYPE DEFAULT NULL,
        dt_begin_in           IN epis_nch.dt_begin%TYPE DEFAULT NULL,
        dt_end_in             IN epis_nch.dt_end%TYPE DEFAULT NULL,
        flg_status_in         IN epis_nch.flg_status%TYPE DEFAULT 'A',
        flg_type_in           IN epis_nch.flg_type%TYPE DEFAULT 'D',
        dt_creation_in        IN epis_nch.dt_creation%TYPE DEFAULT current_timestamp,
        id_prof_creation_in   IN epis_nch.id_prof_creation%TYPE DEFAULT NULL,
        id_nch_level_in       IN epis_nch.id_nch_level%TYPE DEFAULT NULL,
        create_user_in        IN epis_nch.create_user%TYPE DEFAULT NULL,
        create_time_in        IN epis_nch.create_time%TYPE DEFAULT NULL,
        create_institution_in IN epis_nch.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN epis_nch.update_user%TYPE DEFAULT NULL,
        update_time_in        IN epis_nch.update_time%TYPE DEFAULT NULL,
        update_institution_in IN epis_nch.update_institution%TYPE DEFAULT NULL,
        reason_notes_in       IN epis_nch.reason_notes%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    ) RETURN epis_nch.id_epis_nch%TYPE;

    -- Update any/all columns by primary key. If you pass NULL, then
    -- the current column value is set to itself. If you need a more
    -- selected UPDATE then use one of the onecol procedures below.

    PROCEDURE upd
    (
        id_epis_nch_in         IN epis_nch.id_epis_nch%TYPE,
        id_episode_in          IN epis_nch.id_episode%TYPE DEFAULT NULL,
        id_episode_nin         IN BOOLEAN := TRUE,
        id_patient_in          IN epis_nch.id_patient%TYPE DEFAULT NULL,
        id_patient_nin         IN BOOLEAN := TRUE,
        nch_value_in           IN epis_nch.nch_value%TYPE DEFAULT NULL,
        nch_value_nin          IN BOOLEAN := TRUE,
        dt_begin_in            IN epis_nch.dt_begin%TYPE DEFAULT NULL,
        dt_begin_nin           IN BOOLEAN := TRUE,
        dt_end_in              IN epis_nch.dt_end%TYPE DEFAULT NULL,
        dt_end_nin             IN BOOLEAN := TRUE,
        flg_status_in          IN epis_nch.flg_status%TYPE DEFAULT NULL,
        flg_status_nin         IN BOOLEAN := TRUE,
        flg_type_in            IN epis_nch.flg_type%TYPE DEFAULT NULL,
        flg_type_nin           IN BOOLEAN := TRUE,
        dt_creation_in         IN epis_nch.dt_creation%TYPE DEFAULT NULL,
        dt_creation_nin        IN BOOLEAN := TRUE,
        id_prof_creation_in    IN epis_nch.id_prof_creation%TYPE DEFAULT NULL,
        id_prof_creation_nin   IN BOOLEAN := TRUE,
        id_nch_level_in        IN epis_nch.id_nch_level%TYPE DEFAULT NULL,
        id_nch_level_nin       IN BOOLEAN := TRUE,
        create_user_in         IN epis_nch.create_user%TYPE DEFAULT NULL,
        create_user_nin        IN BOOLEAN := TRUE,
        create_time_in         IN epis_nch.create_time%TYPE DEFAULT NULL,
        create_time_nin        IN BOOLEAN := TRUE,
        create_institution_in  IN epis_nch.create_institution%TYPE DEFAULT NULL,
        create_institution_nin IN BOOLEAN := TRUE,
        update_user_in         IN epis_nch.update_user%TYPE DEFAULT NULL,
        update_user_nin        IN BOOLEAN := TRUE,
        update_time_in         IN epis_nch.update_time%TYPE DEFAULT NULL,
        update_time_nin        IN BOOLEAN := TRUE,
        update_institution_in  IN epis_nch.update_institution%TYPE DEFAULT NULL,
        update_institution_nin IN BOOLEAN := TRUE,
        reason_notes_in        IN epis_nch.reason_notes%TYPE DEFAULT NULL,
        reason_notes_nin       IN BOOLEAN := TRUE,
        handle_error_in        IN BOOLEAN := TRUE,
        rows_out               IN OUT table_varchar
    );

    PROCEDURE upd
    (
        id_epis_nch_in         IN epis_nch.id_epis_nch%TYPE,
        id_episode_in          IN epis_nch.id_episode%TYPE DEFAULT NULL,
        id_episode_nin         IN BOOLEAN := TRUE,
        id_patient_in          IN epis_nch.id_patient%TYPE DEFAULT NULL,
        id_patient_nin         IN BOOLEAN := TRUE,
        nch_value_in           IN epis_nch.nch_value%TYPE DEFAULT NULL,
        nch_value_nin          IN BOOLEAN := TRUE,
        dt_begin_in            IN epis_nch.dt_begin%TYPE DEFAULT NULL,
        dt_begin_nin           IN BOOLEAN := TRUE,
        dt_end_in              IN epis_nch.dt_end%TYPE DEFAULT NULL,
        dt_end_nin             IN BOOLEAN := TRUE,
        flg_status_in          IN epis_nch.flg_status%TYPE DEFAULT NULL,
        flg_status_nin         IN BOOLEAN := TRUE,
        flg_type_in            IN epis_nch.flg_type%TYPE DEFAULT NULL,
        flg_type_nin           IN BOOLEAN := TRUE,
        dt_creation_in         IN epis_nch.dt_creation%TYPE DEFAULT NULL,
        dt_creation_nin        IN BOOLEAN := TRUE,
        id_prof_creation_in    IN epis_nch.id_prof_creation%TYPE DEFAULT NULL,
        id_prof_creation_nin   IN BOOLEAN := TRUE,
        id_nch_level_in        IN epis_nch.id_nch_level%TYPE DEFAULT NULL,
        id_nch_level_nin       IN BOOLEAN := TRUE,
        create_user_in         IN epis_nch.create_user%TYPE DEFAULT NULL,
        create_user_nin        IN BOOLEAN := TRUE,
        create_time_in         IN epis_nch.create_time%TYPE DEFAULT NULL,
        create_time_nin        IN BOOLEAN := TRUE,
        create_institution_in  IN epis_nch.create_institution%TYPE DEFAULT NULL,
        create_institution_nin IN BOOLEAN := TRUE,
        update_user_in         IN epis_nch.update_user%TYPE DEFAULT NULL,
        update_user_nin        IN BOOLEAN := TRUE,
        update_time_in         IN epis_nch.update_time%TYPE DEFAULT NULL,
        update_time_nin        IN BOOLEAN := TRUE,
        update_institution_in  IN epis_nch.update_institution%TYPE DEFAULT NULL,
        update_institution_nin IN BOOLEAN := TRUE,
        reason_notes_in        IN epis_nch.reason_notes%TYPE DEFAULT NULL,
        reason_notes_nin       IN BOOLEAN := TRUE,
        handle_error_in        IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        id_episode_in          IN epis_nch.id_episode%TYPE DEFAULT NULL,
        id_episode_nin         IN BOOLEAN := TRUE,
        id_patient_in          IN epis_nch.id_patient%TYPE DEFAULT NULL,
        id_patient_nin         IN BOOLEAN := TRUE,
        nch_value_in           IN epis_nch.nch_value%TYPE DEFAULT NULL,
        nch_value_nin          IN BOOLEAN := TRUE,
        dt_begin_in            IN epis_nch.dt_begin%TYPE DEFAULT NULL,
        dt_begin_nin           IN BOOLEAN := TRUE,
        dt_end_in              IN epis_nch.dt_end%TYPE DEFAULT NULL,
        dt_end_nin             IN BOOLEAN := TRUE,
        flg_status_in          IN epis_nch.flg_status%TYPE DEFAULT NULL,
        flg_status_nin         IN BOOLEAN := TRUE,
        flg_type_in            IN epis_nch.flg_type%TYPE DEFAULT NULL,
        flg_type_nin           IN BOOLEAN := TRUE,
        dt_creation_in         IN epis_nch.dt_creation%TYPE DEFAULT NULL,
        dt_creation_nin        IN BOOLEAN := TRUE,
        id_prof_creation_in    IN epis_nch.id_prof_creation%TYPE DEFAULT NULL,
        id_prof_creation_nin   IN BOOLEAN := TRUE,
        id_nch_level_in        IN epis_nch.id_nch_level%TYPE DEFAULT NULL,
        id_nch_level_nin       IN BOOLEAN := TRUE,
        create_user_in         IN epis_nch.create_user%TYPE DEFAULT NULL,
        create_user_nin        IN BOOLEAN := TRUE,
        create_time_in         IN epis_nch.create_time%TYPE DEFAULT NULL,
        create_time_nin        IN BOOLEAN := TRUE,
        create_institution_in  IN epis_nch.create_institution%TYPE DEFAULT NULL,
        create_institution_nin IN BOOLEAN := TRUE,
        update_user_in         IN epis_nch.update_user%TYPE DEFAULT NULL,
        update_user_nin        IN BOOLEAN := TRUE,
        update_time_in         IN epis_nch.update_time%TYPE DEFAULT NULL,
        update_time_nin        IN BOOLEAN := TRUE,
        update_institution_in  IN epis_nch.update_institution%TYPE DEFAULT NULL,
        update_institution_nin IN BOOLEAN := TRUE,
        reason_notes_in        IN epis_nch.reason_notes%TYPE DEFAULT NULL,
        reason_notes_nin       IN BOOLEAN := TRUE,
        where_in               VARCHAR2 DEFAULT NULL,
        handle_error_in        IN BOOLEAN := TRUE,
        rows_out               IN OUT table_varchar
    );

    PROCEDURE upd
    (
        id_episode_in          IN epis_nch.id_episode%TYPE DEFAULT NULL,
        id_episode_nin         IN BOOLEAN := TRUE,
        id_patient_in          IN epis_nch.id_patient%TYPE DEFAULT NULL,
        id_patient_nin         IN BOOLEAN := TRUE,
        nch_value_in           IN epis_nch.nch_value%TYPE DEFAULT NULL,
        nch_value_nin          IN BOOLEAN := TRUE,
        dt_begin_in            IN epis_nch.dt_begin%TYPE DEFAULT NULL,
        dt_begin_nin           IN BOOLEAN := TRUE,
        dt_end_in              IN epis_nch.dt_end%TYPE DEFAULT NULL,
        dt_end_nin             IN BOOLEAN := TRUE,
        flg_status_in          IN epis_nch.flg_status%TYPE DEFAULT NULL,
        flg_status_nin         IN BOOLEAN := TRUE,
        flg_type_in            IN epis_nch.flg_type%TYPE DEFAULT NULL,
        flg_type_nin           IN BOOLEAN := TRUE,
        dt_creation_in         IN epis_nch.dt_creation%TYPE DEFAULT NULL,
        dt_creation_nin        IN BOOLEAN := TRUE,
        id_prof_creation_in    IN epis_nch.id_prof_creation%TYPE DEFAULT NULL,
        id_prof_creation_nin   IN BOOLEAN := TRUE,
        id_nch_level_in        IN epis_nch.id_nch_level%TYPE DEFAULT NULL,
        id_nch_level_nin       IN BOOLEAN := TRUE,
        create_user_in         IN epis_nch.create_user%TYPE DEFAULT NULL,
        create_user_nin        IN BOOLEAN := TRUE,
        create_time_in         IN epis_nch.create_time%TYPE DEFAULT NULL,
        create_time_nin        IN BOOLEAN := TRUE,
        create_institution_in  IN epis_nch.create_institution%TYPE DEFAULT NULL,
        create_institution_nin IN BOOLEAN := TRUE,
        update_user_in         IN epis_nch.update_user%TYPE DEFAULT NULL,
        update_user_nin        IN BOOLEAN := TRUE,
        update_time_in         IN epis_nch.update_time%TYPE DEFAULT NULL,
        update_time_nin        IN BOOLEAN := TRUE,
        update_institution_in  IN epis_nch.update_institution%TYPE DEFAULT NULL,
        update_institution_nin IN BOOLEAN := TRUE,
        reason_notes_in        IN epis_nch.reason_notes%TYPE DEFAULT NULL,
        reason_notes_nin       IN BOOLEAN := TRUE,
        where_in               VARCHAR2 DEFAULT NULL,
        handle_error_in        IN BOOLEAN := TRUE
    );

    PROCEDURE upd_ins
    (
        id_epis_nch_in        IN epis_nch.id_epis_nch%TYPE,
        id_episode_in         IN epis_nch.id_episode%TYPE DEFAULT NULL,
        id_patient_in         IN epis_nch.id_patient%TYPE DEFAULT NULL,
        nch_value_in          IN epis_nch.nch_value%TYPE DEFAULT NULL,
        dt_begin_in           IN epis_nch.dt_begin%TYPE DEFAULT NULL,
        dt_end_in             IN epis_nch.dt_end%TYPE DEFAULT NULL,
        flg_status_in         IN epis_nch.flg_status%TYPE DEFAULT NULL,
        flg_type_in           IN epis_nch.flg_type%TYPE DEFAULT NULL,
        dt_creation_in        IN epis_nch.dt_creation%TYPE DEFAULT NULL,
        id_prof_creation_in   IN epis_nch.id_prof_creation%TYPE DEFAULT NULL,
        id_nch_level_in       IN epis_nch.id_nch_level%TYPE DEFAULT NULL,
        create_user_in        IN epis_nch.create_user%TYPE DEFAULT NULL,
        create_time_in        IN epis_nch.create_time%TYPE DEFAULT NULL,
        create_institution_in IN epis_nch.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN epis_nch.update_user%TYPE DEFAULT NULL,
        update_time_in        IN epis_nch.update_time%TYPE DEFAULT NULL,
        update_institution_in IN epis_nch.update_institution%TYPE DEFAULT NULL,
        reason_notes_in       IN epis_nch.reason_notes%TYPE DEFAULT NULL,
        handle_error_in       IN BOOLEAN := TRUE,
        rows_out              OUT table_varchar
    );

    PROCEDURE upd_ins
    (
        id_epis_nch_in        IN epis_nch.id_epis_nch%TYPE,
        id_episode_in         IN epis_nch.id_episode%TYPE DEFAULT NULL,
        id_patient_in         IN epis_nch.id_patient%TYPE DEFAULT NULL,
        nch_value_in          IN epis_nch.nch_value%TYPE DEFAULT NULL,
        dt_begin_in           IN epis_nch.dt_begin%TYPE DEFAULT NULL,
        dt_end_in             IN epis_nch.dt_end%TYPE DEFAULT NULL,
        flg_status_in         IN epis_nch.flg_status%TYPE DEFAULT NULL,
        flg_type_in           IN epis_nch.flg_type%TYPE DEFAULT NULL,
        dt_creation_in        IN epis_nch.dt_creation%TYPE DEFAULT NULL,
        id_prof_creation_in   IN epis_nch.id_prof_creation%TYPE DEFAULT NULL,
        id_nch_level_in       IN epis_nch.id_nch_level%TYPE DEFAULT NULL,
        create_user_in        IN epis_nch.create_user%TYPE DEFAULT NULL,
        create_time_in        IN epis_nch.create_time%TYPE DEFAULT NULL,
        create_institution_in IN epis_nch.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN epis_nch.update_user%TYPE DEFAULT NULL,
        update_time_in        IN epis_nch.update_time%TYPE DEFAULT NULL,
        update_institution_in IN epis_nch.update_institution%TYPE DEFAULT NULL,
        reason_notes_in       IN epis_nch.reason_notes%TYPE DEFAULT NULL,
        handle_error_in       IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        rec_in          IN epis_nch%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        IN OUT table_varchar
    );

    PROCEDURE upd
    (
        rec_in          IN epis_nch%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        col_in            IN epis_nch_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          IN OUT table_varchar
    );

    PROCEDURE upd
    (
        col_in            IN epis_nch_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE
    );

    -- Use Native Dynamic SQL increment a single NUMBER column
    -- for all rows specified by the dynamic WHERE clause
    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_epis_nch_in  IN epis_nch.id_epis_nch%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_epis_nch_in  IN epis_nch.id_epis_nch%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for primary key column ID_EPIS_NCH
    PROCEDURE del_id_epis_nch
    (
        id_epis_nch_in  IN epis_nch.id_epis_nch%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for primary key column ID_EPIS_NCH
    PROCEDURE del_id_epis_nch
    (
        id_epis_nch_in  IN epis_nch.id_epis_nch%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for this ENH_EPI_FK foreign key value
    PROCEDURE del_enh_epi_fk
    (
        id_episode_in   IN epis_nch.id_episode%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this ENH_EPI_FK foreign key value
    PROCEDURE del_enh_epi_fk
    (
        id_episode_in   IN epis_nch.id_episode%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for this ENH_NCHL_FK foreign key value
    PROCEDURE del_enh_nchl_fk
    (
        id_nch_level_in IN epis_nch.id_nch_level%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this ENH_NCHL_FK foreign key value
    PROCEDURE del_enh_nchl_fk
    (
        id_nch_level_in IN epis_nch.id_nch_level%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for this ENH_PAT_FK foreign key value
    PROCEDURE del_enh_pat_fk
    (
        id_patient_in   IN epis_nch.id_patient%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this ENH_PAT_FK foreign key value
    PROCEDURE del_enh_pat_fk
    (
        id_patient_in   IN epis_nch.id_patient%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for this ENH_PROF_CREATE_FK foreign key value
    PROCEDURE del_enh_prof_create_fk
    (
        id_prof_creation_in IN epis_nch.id_prof_creation%TYPE,
        handle_error_in     IN BOOLEAN := TRUE
    );

    -- Delete all rows for this ENH_PROF_CREATE_FK foreign key value
    PROCEDURE del_enh_prof_create_fk
    (
        id_prof_creation_in IN epis_nch.id_prof_creation%TYPE,
        handle_error_in     IN BOOLEAN := TRUE,
        rows_out            OUT table_varchar
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Initialize a record with default values for columns in the table.
    PROCEDURE initrec(epis_nch_inout IN OUT epis_nch%ROWTYPE);

    FUNCTION initrec RETURN epis_nch%ROWTYPE;

    FUNCTION get_data_rowid(rows_in IN table_varchar) RETURN epis_nch_tc;

    FUNCTION get_data_rowid_pat(rows_in IN table_varchar) RETURN epis_nch_tc;

END ts_epis_nch;
/