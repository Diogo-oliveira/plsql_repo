/*-- Last Change Revision: $Rev: 2029394 $*/
/*-- Last Change by: $Author: mario.fernandes $*/
/*-- Date of last change: $Date: 2022-08-02 18:51:25 +0100 (ter, 02 ago 2022) $*/
CREATE OR REPLACE PACKAGE ts_supply_workflow_hist
/*
| Generated by or retrieved - DO NOT MODIFY!
| Created On: 2022-07-29 08:18:13
| Created By: ALERT
*/
 IS

    -- Collection of %ROWTYPE records based on supply_workflow_hist
    TYPE supply_workflow_hist_tc IS TABLE OF supply_workflow_hist%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE supply_workflow_hist_ntt IS TABLE OF supply_workflow_hist%ROWTYPE;
    TYPE supply_workflow_hist_vat IS VARRAY(100) OF supply_workflow_hist%ROWTYPE;

    -- Column Collection based on column ID_SUPPLY_WORKFLOW_HIST
    TYPE id_supply_workflow_hist_cc IS TABLE OF supply_workflow_hist.id_supply_workflow_hist%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_SUPPLY_WORKFLOW
    TYPE id_supply_workflow_cc IS TABLE OF supply_workflow_hist.id_supply_workflow%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_PROFESSIONAL
    TYPE id_professional_cc IS TABLE OF supply_workflow_hist.id_professional%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_EPISODE
    TYPE id_episode_cc IS TABLE OF supply_workflow_hist.id_episode%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_SUPPLY_REQUEST
    TYPE id_supply_request_cc IS TABLE OF supply_workflow_hist.id_supply_request%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_SUPPLY
    TYPE id_supply_cc IS TABLE OF supply_workflow_hist.id_supply%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_SUPPLY_LOCATION
    TYPE id_supply_location_cc IS TABLE OF supply_workflow_hist.id_supply_location%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column BARCODE_REQ
    TYPE barcode_req_cc IS TABLE OF supply_workflow_hist.barcode_req%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column BARCODE_SCANNED
    TYPE barcode_scanned_cc IS TABLE OF supply_workflow_hist.barcode_scanned%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column QUANTITY
    TYPE quantity_cc IS TABLE OF supply_workflow_hist.quantity%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_UNIT_MEASURE
    TYPE id_unit_measure_cc IS TABLE OF supply_workflow_hist.id_unit_measure%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_CONTEXT
    TYPE id_context_cc IS TABLE OF supply_workflow_hist.id_context%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column FLG_CONTEXT
    TYPE flg_context_cc IS TABLE OF supply_workflow_hist.flg_context%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column FLG_STATUS
    TYPE flg_status_cc IS TABLE OF supply_workflow_hist.flg_status%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column FLG_REASON
    TYPE flg_reason_cc IS TABLE OF supply_workflow_hist.flg_reason%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column DT_REQUEST
    TYPE dt_request_cc IS TABLE OF supply_workflow_hist.dt_request%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column DT_RETURNED
    TYPE dt_returned_cc IS TABLE OF supply_workflow_hist.dt_returned%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column NOTES
    TYPE notes_cc IS TABLE OF supply_workflow_hist.notes%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_PROF_CANCEL
    TYPE id_prof_cancel_cc IS TABLE OF supply_workflow_hist.id_prof_cancel%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column DT_CANCEL
    TYPE dt_cancel_cc IS TABLE OF supply_workflow_hist.dt_cancel%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column NOTES_CANCEL
    TYPE notes_cancel_cc IS TABLE OF supply_workflow_hist.notes_cancel%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_CANCEL_REASON
    TYPE id_cancel_reason_cc IS TABLE OF supply_workflow_hist.id_cancel_reason%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column NOTES_REJECT
    TYPE notes_reject_cc IS TABLE OF supply_workflow_hist.notes_reject%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column DT_REJECT
    TYPE dt_reject_cc IS TABLE OF supply_workflow_hist.dt_reject%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_PROF_REJECT
    TYPE id_prof_reject_cc IS TABLE OF supply_workflow_hist.id_prof_reject%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column DT_SUPPLY_WORKFLOW
    TYPE dt_supply_workflow_cc IS TABLE OF supply_workflow_hist.dt_supply_workflow%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column CREATE_USER
    TYPE create_user_cc IS TABLE OF supply_workflow_hist.create_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column CREATE_TIME
    TYPE create_time_cc IS TABLE OF supply_workflow_hist.create_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column CREATE_INSTITUTION
    TYPE create_institution_cc IS TABLE OF supply_workflow_hist.create_institution%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column UPDATE_USER
    TYPE update_user_cc IS TABLE OF supply_workflow_hist.update_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column UPDATE_TIME
    TYPE update_time_cc IS TABLE OF supply_workflow_hist.update_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column UPDATE_INSTITUTION
    TYPE update_institution_cc IS TABLE OF supply_workflow_hist.update_institution%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_REQ_REASON
    TYPE id_req_reason_cc IS TABLE OF supply_workflow_hist.id_req_reason%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_DEL_REASON
    TYPE id_del_reason_cc IS TABLE OF supply_workflow_hist.id_del_reason%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_SUPPLY_SET
    TYPE id_supply_set_cc IS TABLE OF supply_workflow_hist.id_supply_set%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_SUP_WORKFLOW_PARENT
    TYPE id_sup_workflow_parent_cc IS TABLE OF supply_workflow_hist.id_sup_workflow_parent%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ASSET_NUMBER
    TYPE asset_number_cc IS TABLE OF supply_workflow_hist.asset_number%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column FLG_OUTDATED
    TYPE flg_outdated_cc IS TABLE OF supply_workflow_hist.flg_outdated%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column TOTAL_QUANTITY
    TYPE total_quantity_cc IS TABLE OF supply_workflow_hist.total_quantity%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column FLG_CONS_TYPE
    TYPE flg_cons_type_cc IS TABLE OF supply_workflow_hist.flg_cons_type%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column FLG_REUSABLE
    TYPE flg_reusable_cc IS TABLE OF supply_workflow_hist.flg_reusable%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column FLG_EDITABLE
    TYPE flg_editable_cc IS TABLE OF supply_workflow_hist.flg_editable%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column TOTAL_AVAIL_QUANTITY
    TYPE total_avail_quantity_cc IS TABLE OF supply_workflow_hist.total_avail_quantity%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column COD_TABLE
    TYPE cod_table_cc IS TABLE OF supply_workflow_hist.cod_table%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column FLG_PREPARING
    TYPE flg_preparing_cc IS TABLE OF supply_workflow_hist.flg_preparing%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column FLG_COUNTABLE
    TYPE flg_countable_cc IS TABLE OF supply_workflow_hist.flg_countable%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_SUPPLY_AREA
    TYPE id_supply_area_cc IS TABLE OF supply_workflow_hist.id_supply_area%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column LOT
    TYPE lot_cc IS TABLE OF supply_workflow_hist.lot%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column DT_EXPIRATION
    TYPE dt_expiration_cc IS TABLE OF supply_workflow_hist.dt_expiration%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column FLG_VALIDATION
    TYPE flg_validation_cc IS TABLE OF supply_workflow_hist.flg_validation%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column SUPPLY_MIGRATION
    TYPE supply_migration_cc IS TABLE OF supply_workflow_hist.supply_migration%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_CONSUMPTION_PARENT
    TYPE id_consumption_parent_cc IS TABLE OF supply_workflow_hist.id_consumption_parent%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_DISPENSE_PARENT
    TYPE id_dispense_parent_cc IS TABLE OF supply_workflow_hist.id_dispense_parent%TYPE INDEX BY BINARY_INTEGER;

    -- Insert one row, providing primary key if present (with rows_out)
    PROCEDURE ins
    (
        id_supply_workflow_hist_in IN supply_workflow_hist.id_supply_workflow_hist%TYPE,
        id_supply_workflow_in      IN supply_workflow_hist.id_supply_workflow%TYPE DEFAULT NULL,
        id_professional_in         IN supply_workflow_hist.id_professional%TYPE DEFAULT NULL,
        id_episode_in              IN supply_workflow_hist.id_episode%TYPE DEFAULT NULL,
        id_supply_request_in       IN supply_workflow_hist.id_supply_request%TYPE DEFAULT NULL,
        id_supply_in               IN supply_workflow_hist.id_supply%TYPE DEFAULT NULL,
        id_supply_location_in      IN supply_workflow_hist.id_supply_location%TYPE DEFAULT NULL,
        barcode_req_in             IN supply_workflow_hist.barcode_req%TYPE DEFAULT NULL,
        barcode_scanned_in         IN supply_workflow_hist.barcode_scanned%TYPE DEFAULT NULL,
        quantity_in                IN supply_workflow_hist.quantity%TYPE DEFAULT NULL,
        id_unit_measure_in         IN supply_workflow_hist.id_unit_measure%TYPE DEFAULT NULL,
        id_context_in              IN supply_workflow_hist.id_context%TYPE DEFAULT NULL,
        flg_context_in             IN supply_workflow_hist.flg_context%TYPE DEFAULT NULL,
        flg_status_in              IN supply_workflow_hist.flg_status%TYPE DEFAULT NULL,
        flg_reason_in              IN supply_workflow_hist.flg_reason%TYPE DEFAULT NULL,
        dt_request_in              IN supply_workflow_hist.dt_request%TYPE DEFAULT NULL,
        dt_returned_in             IN supply_workflow_hist.dt_returned%TYPE DEFAULT NULL,
        notes_in                   IN supply_workflow_hist.notes%TYPE DEFAULT NULL,
        id_prof_cancel_in          IN supply_workflow_hist.id_prof_cancel%TYPE DEFAULT NULL,
        dt_cancel_in               IN supply_workflow_hist.dt_cancel%TYPE DEFAULT NULL,
        notes_cancel_in            IN supply_workflow_hist.notes_cancel%TYPE DEFAULT NULL,
        id_cancel_reason_in        IN supply_workflow_hist.id_cancel_reason%TYPE DEFAULT NULL,
        notes_reject_in            IN supply_workflow_hist.notes_reject%TYPE DEFAULT NULL,
        dt_reject_in               IN supply_workflow_hist.dt_reject%TYPE DEFAULT NULL,
        id_prof_reject_in          IN supply_workflow_hist.id_prof_reject%TYPE DEFAULT NULL,
        dt_supply_workflow_in      IN supply_workflow_hist.dt_supply_workflow%TYPE DEFAULT NULL,
        create_user_in             IN supply_workflow_hist.create_user%TYPE DEFAULT NULL,
        create_time_in             IN supply_workflow_hist.create_time%TYPE DEFAULT NULL,
        create_institution_in      IN supply_workflow_hist.create_institution%TYPE DEFAULT NULL,
        update_user_in             IN supply_workflow_hist.update_user%TYPE DEFAULT NULL,
        update_time_in             IN supply_workflow_hist.update_time%TYPE DEFAULT NULL,
        update_institution_in      IN supply_workflow_hist.update_institution%TYPE DEFAULT NULL,
        id_req_reason_in           IN supply_workflow_hist.id_req_reason%TYPE DEFAULT NULL,
        id_del_reason_in           IN supply_workflow_hist.id_del_reason%TYPE DEFAULT NULL,
        id_supply_set_in           IN supply_workflow_hist.id_supply_set%TYPE DEFAULT NULL,
        id_sup_workflow_parent_in  IN supply_workflow_hist.id_sup_workflow_parent%TYPE DEFAULT NULL,
        asset_number_in            IN supply_workflow_hist.asset_number%TYPE DEFAULT NULL,
        flg_outdated_in            IN supply_workflow_hist.flg_outdated%TYPE DEFAULT 'A',
        total_quantity_in          IN supply_workflow_hist.total_quantity%TYPE DEFAULT NULL,
        flg_cons_type_in           IN supply_workflow_hist.flg_cons_type%TYPE DEFAULT NULL,
        flg_reusable_in            IN supply_workflow_hist.flg_reusable%TYPE DEFAULT NULL,
        flg_editable_in            IN supply_workflow_hist.flg_editable%TYPE DEFAULT NULL,
        total_avail_quantity_in    IN supply_workflow_hist.total_avail_quantity%TYPE DEFAULT NULL,
        cod_table_in               IN supply_workflow_hist.cod_table%TYPE DEFAULT NULL,
        flg_preparing_in           IN supply_workflow_hist.flg_preparing%TYPE DEFAULT NULL,
        flg_countable_in           IN supply_workflow_hist.flg_countable%TYPE DEFAULT NULL,
        id_supply_area_in          IN supply_workflow_hist.id_supply_area%TYPE DEFAULT NULL,
        lot_in                     IN supply_workflow_hist.lot%TYPE DEFAULT NULL,
        dt_expiration_in           IN supply_workflow_hist.dt_expiration%TYPE DEFAULT NULL,
        flg_validation_in          IN supply_workflow_hist.flg_validation%TYPE DEFAULT NULL,
        supply_migration_in        IN supply_workflow_hist.supply_migration%TYPE DEFAULT NULL,
        id_consumption_parent_in   IN supply_workflow_hist.id_consumption_parent%TYPE DEFAULT NULL,
        id_dispense_parent_in      IN supply_workflow_hist.id_dispense_parent%TYPE DEFAULT NULL,
        handle_error_in            IN BOOLEAN := FALSE,
        rows_out                   OUT table_varchar
    );

    -- Insert one row, providing primary key if present (without rows_out)
    PROCEDURE ins
    (
        id_supply_workflow_hist_in IN supply_workflow_hist.id_supply_workflow_hist%TYPE,
        id_supply_workflow_in      IN supply_workflow_hist.id_supply_workflow%TYPE DEFAULT NULL,
        id_professional_in         IN supply_workflow_hist.id_professional%TYPE DEFAULT NULL,
        id_episode_in              IN supply_workflow_hist.id_episode%TYPE DEFAULT NULL,
        id_supply_request_in       IN supply_workflow_hist.id_supply_request%TYPE DEFAULT NULL,
        id_supply_in               IN supply_workflow_hist.id_supply%TYPE DEFAULT NULL,
        id_supply_location_in      IN supply_workflow_hist.id_supply_location%TYPE DEFAULT NULL,
        barcode_req_in             IN supply_workflow_hist.barcode_req%TYPE DEFAULT NULL,
        barcode_scanned_in         IN supply_workflow_hist.barcode_scanned%TYPE DEFAULT NULL,
        quantity_in                IN supply_workflow_hist.quantity%TYPE DEFAULT NULL,
        id_unit_measure_in         IN supply_workflow_hist.id_unit_measure%TYPE DEFAULT NULL,
        id_context_in              IN supply_workflow_hist.id_context%TYPE DEFAULT NULL,
        flg_context_in             IN supply_workflow_hist.flg_context%TYPE DEFAULT NULL,
        flg_status_in              IN supply_workflow_hist.flg_status%TYPE DEFAULT NULL,
        flg_reason_in              IN supply_workflow_hist.flg_reason%TYPE DEFAULT NULL,
        dt_request_in              IN supply_workflow_hist.dt_request%TYPE DEFAULT NULL,
        dt_returned_in             IN supply_workflow_hist.dt_returned%TYPE DEFAULT NULL,
        notes_in                   IN supply_workflow_hist.notes%TYPE DEFAULT NULL,
        id_prof_cancel_in          IN supply_workflow_hist.id_prof_cancel%TYPE DEFAULT NULL,
        dt_cancel_in               IN supply_workflow_hist.dt_cancel%TYPE DEFAULT NULL,
        notes_cancel_in            IN supply_workflow_hist.notes_cancel%TYPE DEFAULT NULL,
        id_cancel_reason_in        IN supply_workflow_hist.id_cancel_reason%TYPE DEFAULT NULL,
        notes_reject_in            IN supply_workflow_hist.notes_reject%TYPE DEFAULT NULL,
        dt_reject_in               IN supply_workflow_hist.dt_reject%TYPE DEFAULT NULL,
        id_prof_reject_in          IN supply_workflow_hist.id_prof_reject%TYPE DEFAULT NULL,
        dt_supply_workflow_in      IN supply_workflow_hist.dt_supply_workflow%TYPE DEFAULT NULL,
        create_user_in             IN supply_workflow_hist.create_user%TYPE DEFAULT NULL,
        create_time_in             IN supply_workflow_hist.create_time%TYPE DEFAULT NULL,
        create_institution_in      IN supply_workflow_hist.create_institution%TYPE DEFAULT NULL,
        update_user_in             IN supply_workflow_hist.update_user%TYPE DEFAULT NULL,
        update_time_in             IN supply_workflow_hist.update_time%TYPE DEFAULT NULL,
        update_institution_in      IN supply_workflow_hist.update_institution%TYPE DEFAULT NULL,
        id_req_reason_in           IN supply_workflow_hist.id_req_reason%TYPE DEFAULT NULL,
        id_del_reason_in           IN supply_workflow_hist.id_del_reason%TYPE DEFAULT NULL,
        id_supply_set_in           IN supply_workflow_hist.id_supply_set%TYPE DEFAULT NULL,
        id_sup_workflow_parent_in  IN supply_workflow_hist.id_sup_workflow_parent%TYPE DEFAULT NULL,
        asset_number_in            IN supply_workflow_hist.asset_number%TYPE DEFAULT NULL,
        flg_outdated_in            IN supply_workflow_hist.flg_outdated%TYPE DEFAULT 'A',
        total_quantity_in          IN supply_workflow_hist.total_quantity%TYPE DEFAULT NULL,
        flg_cons_type_in           IN supply_workflow_hist.flg_cons_type%TYPE DEFAULT NULL,
        flg_reusable_in            IN supply_workflow_hist.flg_reusable%TYPE DEFAULT NULL,
        flg_editable_in            IN supply_workflow_hist.flg_editable%TYPE DEFAULT NULL,
        total_avail_quantity_in    IN supply_workflow_hist.total_avail_quantity%TYPE DEFAULT NULL,
        cod_table_in               IN supply_workflow_hist.cod_table%TYPE DEFAULT NULL,
        flg_preparing_in           IN supply_workflow_hist.flg_preparing%TYPE DEFAULT NULL,
        flg_countable_in           IN supply_workflow_hist.flg_countable%TYPE DEFAULT NULL,
        id_supply_area_in          IN supply_workflow_hist.id_supply_area%TYPE DEFAULT NULL,
        lot_in                     IN supply_workflow_hist.lot%TYPE DEFAULT NULL,
        dt_expiration_in           IN supply_workflow_hist.dt_expiration%TYPE DEFAULT NULL,
        flg_validation_in          IN supply_workflow_hist.flg_validation%TYPE DEFAULT NULL,
        supply_migration_in        IN supply_workflow_hist.supply_migration%TYPE DEFAULT NULL,
        id_consumption_parent_in   IN supply_workflow_hist.id_consumption_parent%TYPE DEFAULT NULL,
        id_dispense_parent_in      IN supply_workflow_hist.id_dispense_parent%TYPE DEFAULT NULL,
        handle_error_in            IN BOOLEAN := FALSE
    );

    -- Insert a row based on a record
    -- Specify whether or not a primary key value should be generated
    PROCEDURE ins
    (
        rec_in          IN supply_workflow_hist%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := FALSE,
        rows_out        OUT table_varchar
    );

    -- Insert a row based on a record
    -- Specify whether or not a primary key value should be generated
    PROCEDURE ins
    (
        rec_in          IN supply_workflow_hist%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := FALSE
    );

    -- Insert a collection of rows using FORALL; all primary key values
    -- must have already been generated, or are handled in triggers
    PROCEDURE ins
    (
        rows_in         IN supply_workflow_hist_tc,
        handle_error_in IN BOOLEAN := FALSE,
        rows_out        OUT table_varchar
    );

    -- Insert a collection of rows using FORALL; all primary key values
    -- must have already been generated, or are handled in triggers
    PROCEDURE ins
    (
        rows_in         IN supply_workflow_hist_tc,
        handle_error_in IN BOOLEAN := FALSE
    );

    -- Return next primary key value using the named sequence
    FUNCTION next_key(sequence_in IN VARCHAR2 := NULL) RETURN supply_workflow_hist.id_supply_workflow_hist%TYPE;

    -- Insert one row, generating hidden primary key using a sequence
    PROCEDURE ins
    (
        id_supply_workflow_in     IN supply_workflow_hist.id_supply_workflow%TYPE DEFAULT NULL,
        id_professional_in        IN supply_workflow_hist.id_professional%TYPE DEFAULT NULL,
        id_episode_in             IN supply_workflow_hist.id_episode%TYPE DEFAULT NULL,
        id_supply_request_in      IN supply_workflow_hist.id_supply_request%TYPE DEFAULT NULL,
        id_supply_in              IN supply_workflow_hist.id_supply%TYPE DEFAULT NULL,
        id_supply_location_in     IN supply_workflow_hist.id_supply_location%TYPE DEFAULT NULL,
        barcode_req_in            IN supply_workflow_hist.barcode_req%TYPE DEFAULT NULL,
        barcode_scanned_in        IN supply_workflow_hist.barcode_scanned%TYPE DEFAULT NULL,
        quantity_in               IN supply_workflow_hist.quantity%TYPE DEFAULT NULL,
        id_unit_measure_in        IN supply_workflow_hist.id_unit_measure%TYPE DEFAULT NULL,
        id_context_in             IN supply_workflow_hist.id_context%TYPE DEFAULT NULL,
        flg_context_in            IN supply_workflow_hist.flg_context%TYPE DEFAULT NULL,
        flg_status_in             IN supply_workflow_hist.flg_status%TYPE DEFAULT NULL,
        flg_reason_in             IN supply_workflow_hist.flg_reason%TYPE DEFAULT NULL,
        dt_request_in             IN supply_workflow_hist.dt_request%TYPE DEFAULT NULL,
        dt_returned_in            IN supply_workflow_hist.dt_returned%TYPE DEFAULT NULL,
        notes_in                  IN supply_workflow_hist.notes%TYPE DEFAULT NULL,
        id_prof_cancel_in         IN supply_workflow_hist.id_prof_cancel%TYPE DEFAULT NULL,
        dt_cancel_in              IN supply_workflow_hist.dt_cancel%TYPE DEFAULT NULL,
        notes_cancel_in           IN supply_workflow_hist.notes_cancel%TYPE DEFAULT NULL,
        id_cancel_reason_in       IN supply_workflow_hist.id_cancel_reason%TYPE DEFAULT NULL,
        notes_reject_in           IN supply_workflow_hist.notes_reject%TYPE DEFAULT NULL,
        dt_reject_in              IN supply_workflow_hist.dt_reject%TYPE DEFAULT NULL,
        id_prof_reject_in         IN supply_workflow_hist.id_prof_reject%TYPE DEFAULT NULL,
        dt_supply_workflow_in     IN supply_workflow_hist.dt_supply_workflow%TYPE DEFAULT NULL,
        create_user_in            IN supply_workflow_hist.create_user%TYPE DEFAULT NULL,
        create_time_in            IN supply_workflow_hist.create_time%TYPE DEFAULT NULL,
        create_institution_in     IN supply_workflow_hist.create_institution%TYPE DEFAULT NULL,
        update_user_in            IN supply_workflow_hist.update_user%TYPE DEFAULT NULL,
        update_time_in            IN supply_workflow_hist.update_time%TYPE DEFAULT NULL,
        update_institution_in     IN supply_workflow_hist.update_institution%TYPE DEFAULT NULL,
        id_req_reason_in          IN supply_workflow_hist.id_req_reason%TYPE DEFAULT NULL,
        id_del_reason_in          IN supply_workflow_hist.id_del_reason%TYPE DEFAULT NULL,
        id_supply_set_in          IN supply_workflow_hist.id_supply_set%TYPE DEFAULT NULL,
        id_sup_workflow_parent_in IN supply_workflow_hist.id_sup_workflow_parent%TYPE DEFAULT NULL,
        asset_number_in           IN supply_workflow_hist.asset_number%TYPE DEFAULT NULL,
        flg_outdated_in           IN supply_workflow_hist.flg_outdated%TYPE DEFAULT 'A',
        total_quantity_in         IN supply_workflow_hist.total_quantity%TYPE DEFAULT NULL,
        flg_cons_type_in          IN supply_workflow_hist.flg_cons_type%TYPE DEFAULT NULL,
        flg_reusable_in           IN supply_workflow_hist.flg_reusable%TYPE DEFAULT NULL,
        flg_editable_in           IN supply_workflow_hist.flg_editable%TYPE DEFAULT NULL,
        total_avail_quantity_in   IN supply_workflow_hist.total_avail_quantity%TYPE DEFAULT NULL,
        cod_table_in              IN supply_workflow_hist.cod_table%TYPE DEFAULT NULL,
        flg_preparing_in          IN supply_workflow_hist.flg_preparing%TYPE DEFAULT NULL,
        flg_countable_in          IN supply_workflow_hist.flg_countable%TYPE DEFAULT NULL,
        id_supply_area_in         IN supply_workflow_hist.id_supply_area%TYPE DEFAULT NULL,
        lot_in                    IN supply_workflow_hist.lot%TYPE DEFAULT NULL,
        dt_expiration_in          IN supply_workflow_hist.dt_expiration%TYPE DEFAULT NULL,
        flg_validation_in         IN supply_workflow_hist.flg_validation%TYPE DEFAULT NULL,
        supply_migration_in       IN supply_workflow_hist.supply_migration%TYPE DEFAULT NULL,
        id_consumption_parent_in  IN supply_workflow_hist.id_consumption_parent%TYPE DEFAULT NULL,
        id_dispense_parent_in     IN supply_workflow_hist.id_dispense_parent%TYPE DEFAULT NULL,
        -- Pass false if you want errors to propagate out unhandled
        handle_error_in IN BOOLEAN := FALSE,
        rows_out        OUT table_varchar
    );

    -- Insert one row, generating hidden primary key using a sequence
    PROCEDURE ins
    (
        id_supply_workflow_in     IN supply_workflow_hist.id_supply_workflow%TYPE DEFAULT NULL,
        id_professional_in        IN supply_workflow_hist.id_professional%TYPE DEFAULT NULL,
        id_episode_in             IN supply_workflow_hist.id_episode%TYPE DEFAULT NULL,
        id_supply_request_in      IN supply_workflow_hist.id_supply_request%TYPE DEFAULT NULL,
        id_supply_in              IN supply_workflow_hist.id_supply%TYPE DEFAULT NULL,
        id_supply_location_in     IN supply_workflow_hist.id_supply_location%TYPE DEFAULT NULL,
        barcode_req_in            IN supply_workflow_hist.barcode_req%TYPE DEFAULT NULL,
        barcode_scanned_in        IN supply_workflow_hist.barcode_scanned%TYPE DEFAULT NULL,
        quantity_in               IN supply_workflow_hist.quantity%TYPE DEFAULT NULL,
        id_unit_measure_in        IN supply_workflow_hist.id_unit_measure%TYPE DEFAULT NULL,
        id_context_in             IN supply_workflow_hist.id_context%TYPE DEFAULT NULL,
        flg_context_in            IN supply_workflow_hist.flg_context%TYPE DEFAULT NULL,
        flg_status_in             IN supply_workflow_hist.flg_status%TYPE DEFAULT NULL,
        flg_reason_in             IN supply_workflow_hist.flg_reason%TYPE DEFAULT NULL,
        dt_request_in             IN supply_workflow_hist.dt_request%TYPE DEFAULT NULL,
        dt_returned_in            IN supply_workflow_hist.dt_returned%TYPE DEFAULT NULL,
        notes_in                  IN supply_workflow_hist.notes%TYPE DEFAULT NULL,
        id_prof_cancel_in         IN supply_workflow_hist.id_prof_cancel%TYPE DEFAULT NULL,
        dt_cancel_in              IN supply_workflow_hist.dt_cancel%TYPE DEFAULT NULL,
        notes_cancel_in           IN supply_workflow_hist.notes_cancel%TYPE DEFAULT NULL,
        id_cancel_reason_in       IN supply_workflow_hist.id_cancel_reason%TYPE DEFAULT NULL,
        notes_reject_in           IN supply_workflow_hist.notes_reject%TYPE DEFAULT NULL,
        dt_reject_in              IN supply_workflow_hist.dt_reject%TYPE DEFAULT NULL,
        id_prof_reject_in         IN supply_workflow_hist.id_prof_reject%TYPE DEFAULT NULL,
        dt_supply_workflow_in     IN supply_workflow_hist.dt_supply_workflow%TYPE DEFAULT NULL,
        create_user_in            IN supply_workflow_hist.create_user%TYPE DEFAULT NULL,
        create_time_in            IN supply_workflow_hist.create_time%TYPE DEFAULT NULL,
        create_institution_in     IN supply_workflow_hist.create_institution%TYPE DEFAULT NULL,
        update_user_in            IN supply_workflow_hist.update_user%TYPE DEFAULT NULL,
        update_time_in            IN supply_workflow_hist.update_time%TYPE DEFAULT NULL,
        update_institution_in     IN supply_workflow_hist.update_institution%TYPE DEFAULT NULL,
        id_req_reason_in          IN supply_workflow_hist.id_req_reason%TYPE DEFAULT NULL,
        id_del_reason_in          IN supply_workflow_hist.id_del_reason%TYPE DEFAULT NULL,
        id_supply_set_in          IN supply_workflow_hist.id_supply_set%TYPE DEFAULT NULL,
        id_sup_workflow_parent_in IN supply_workflow_hist.id_sup_workflow_parent%TYPE DEFAULT NULL,
        asset_number_in           IN supply_workflow_hist.asset_number%TYPE DEFAULT NULL,
        flg_outdated_in           IN supply_workflow_hist.flg_outdated%TYPE DEFAULT 'A',
        total_quantity_in         IN supply_workflow_hist.total_quantity%TYPE DEFAULT NULL,
        flg_cons_type_in          IN supply_workflow_hist.flg_cons_type%TYPE DEFAULT NULL,
        flg_reusable_in           IN supply_workflow_hist.flg_reusable%TYPE DEFAULT NULL,
        flg_editable_in           IN supply_workflow_hist.flg_editable%TYPE DEFAULT NULL,
        total_avail_quantity_in   IN supply_workflow_hist.total_avail_quantity%TYPE DEFAULT NULL,
        cod_table_in              IN supply_workflow_hist.cod_table%TYPE DEFAULT NULL,
        flg_preparing_in          IN supply_workflow_hist.flg_preparing%TYPE DEFAULT NULL,
        flg_countable_in          IN supply_workflow_hist.flg_countable%TYPE DEFAULT NULL,
        id_supply_area_in         IN supply_workflow_hist.id_supply_area%TYPE DEFAULT NULL,
        lot_in                    IN supply_workflow_hist.lot%TYPE DEFAULT NULL,
        dt_expiration_in          IN supply_workflow_hist.dt_expiration%TYPE DEFAULT NULL,
        flg_validation_in         IN supply_workflow_hist.flg_validation%TYPE DEFAULT NULL,
        supply_migration_in       IN supply_workflow_hist.supply_migration%TYPE DEFAULT NULL,
        id_consumption_parent_in  IN supply_workflow_hist.id_consumption_parent%TYPE DEFAULT NULL,
        id_dispense_parent_in     IN supply_workflow_hist.id_dispense_parent%TYPE DEFAULT NULL,
        -- Pass false if you want errors to propagate out unhandled
        handle_error_in IN BOOLEAN := FALSE
    );

    -- Insert one row, returning primary key generated by sequence
    PROCEDURE ins
    (
        id_supply_workflow_in       IN supply_workflow_hist.id_supply_workflow%TYPE DEFAULT NULL,
        id_professional_in          IN supply_workflow_hist.id_professional%TYPE DEFAULT NULL,
        id_episode_in               IN supply_workflow_hist.id_episode%TYPE DEFAULT NULL,
        id_supply_request_in        IN supply_workflow_hist.id_supply_request%TYPE DEFAULT NULL,
        id_supply_in                IN supply_workflow_hist.id_supply%TYPE DEFAULT NULL,
        id_supply_location_in       IN supply_workflow_hist.id_supply_location%TYPE DEFAULT NULL,
        barcode_req_in              IN supply_workflow_hist.barcode_req%TYPE DEFAULT NULL,
        barcode_scanned_in          IN supply_workflow_hist.barcode_scanned%TYPE DEFAULT NULL,
        quantity_in                 IN supply_workflow_hist.quantity%TYPE DEFAULT NULL,
        id_unit_measure_in          IN supply_workflow_hist.id_unit_measure%TYPE DEFAULT NULL,
        id_context_in               IN supply_workflow_hist.id_context%TYPE DEFAULT NULL,
        flg_context_in              IN supply_workflow_hist.flg_context%TYPE DEFAULT NULL,
        flg_status_in               IN supply_workflow_hist.flg_status%TYPE DEFAULT NULL,
        flg_reason_in               IN supply_workflow_hist.flg_reason%TYPE DEFAULT NULL,
        dt_request_in               IN supply_workflow_hist.dt_request%TYPE DEFAULT NULL,
        dt_returned_in              IN supply_workflow_hist.dt_returned%TYPE DEFAULT NULL,
        notes_in                    IN supply_workflow_hist.notes%TYPE DEFAULT NULL,
        id_prof_cancel_in           IN supply_workflow_hist.id_prof_cancel%TYPE DEFAULT NULL,
        dt_cancel_in                IN supply_workflow_hist.dt_cancel%TYPE DEFAULT NULL,
        notes_cancel_in             IN supply_workflow_hist.notes_cancel%TYPE DEFAULT NULL,
        id_cancel_reason_in         IN supply_workflow_hist.id_cancel_reason%TYPE DEFAULT NULL,
        notes_reject_in             IN supply_workflow_hist.notes_reject%TYPE DEFAULT NULL,
        dt_reject_in                IN supply_workflow_hist.dt_reject%TYPE DEFAULT NULL,
        id_prof_reject_in           IN supply_workflow_hist.id_prof_reject%TYPE DEFAULT NULL,
        dt_supply_workflow_in       IN supply_workflow_hist.dt_supply_workflow%TYPE DEFAULT NULL,
        create_user_in              IN supply_workflow_hist.create_user%TYPE DEFAULT NULL,
        create_time_in              IN supply_workflow_hist.create_time%TYPE DEFAULT NULL,
        create_institution_in       IN supply_workflow_hist.create_institution%TYPE DEFAULT NULL,
        update_user_in              IN supply_workflow_hist.update_user%TYPE DEFAULT NULL,
        update_time_in              IN supply_workflow_hist.update_time%TYPE DEFAULT NULL,
        update_institution_in       IN supply_workflow_hist.update_institution%TYPE DEFAULT NULL,
        id_req_reason_in            IN supply_workflow_hist.id_req_reason%TYPE DEFAULT NULL,
        id_del_reason_in            IN supply_workflow_hist.id_del_reason%TYPE DEFAULT NULL,
        id_supply_set_in            IN supply_workflow_hist.id_supply_set%TYPE DEFAULT NULL,
        id_sup_workflow_parent_in   IN supply_workflow_hist.id_sup_workflow_parent%TYPE DEFAULT NULL,
        asset_number_in             IN supply_workflow_hist.asset_number%TYPE DEFAULT NULL,
        flg_outdated_in             IN supply_workflow_hist.flg_outdated%TYPE DEFAULT 'A',
        total_quantity_in           IN supply_workflow_hist.total_quantity%TYPE DEFAULT NULL,
        flg_cons_type_in            IN supply_workflow_hist.flg_cons_type%TYPE DEFAULT NULL,
        flg_reusable_in             IN supply_workflow_hist.flg_reusable%TYPE DEFAULT NULL,
        flg_editable_in             IN supply_workflow_hist.flg_editable%TYPE DEFAULT NULL,
        total_avail_quantity_in     IN supply_workflow_hist.total_avail_quantity%TYPE DEFAULT NULL,
        cod_table_in                IN supply_workflow_hist.cod_table%TYPE DEFAULT NULL,
        flg_preparing_in            IN supply_workflow_hist.flg_preparing%TYPE DEFAULT NULL,
        flg_countable_in            IN supply_workflow_hist.flg_countable%TYPE DEFAULT NULL,
        id_supply_area_in           IN supply_workflow_hist.id_supply_area%TYPE DEFAULT NULL,
        lot_in                      IN supply_workflow_hist.lot%TYPE DEFAULT NULL,
        dt_expiration_in            IN supply_workflow_hist.dt_expiration%TYPE DEFAULT NULL,
        flg_validation_in           IN supply_workflow_hist.flg_validation%TYPE DEFAULT NULL,
        supply_migration_in         IN supply_workflow_hist.supply_migration%TYPE DEFAULT NULL,
        id_consumption_parent_in    IN supply_workflow_hist.id_consumption_parent%TYPE DEFAULT NULL,
        id_dispense_parent_in       IN supply_workflow_hist.id_dispense_parent%TYPE DEFAULT NULL,
        id_supply_workflow_hist_out IN OUT supply_workflow_hist.id_supply_workflow_hist%TYPE,
        -- Pass false if you want errors to propagate out unhandled
        handle_error_in IN BOOLEAN := FALSE,
        rows_out        OUT table_varchar
    );

    -- Insert one row, returning primary key generated by sequence
    PROCEDURE ins
    (
        id_supply_workflow_in       IN supply_workflow_hist.id_supply_workflow%TYPE DEFAULT NULL,
        id_professional_in          IN supply_workflow_hist.id_professional%TYPE DEFAULT NULL,
        id_episode_in               IN supply_workflow_hist.id_episode%TYPE DEFAULT NULL,
        id_supply_request_in        IN supply_workflow_hist.id_supply_request%TYPE DEFAULT NULL,
        id_supply_in                IN supply_workflow_hist.id_supply%TYPE DEFAULT NULL,
        id_supply_location_in       IN supply_workflow_hist.id_supply_location%TYPE DEFAULT NULL,
        barcode_req_in              IN supply_workflow_hist.barcode_req%TYPE DEFAULT NULL,
        barcode_scanned_in          IN supply_workflow_hist.barcode_scanned%TYPE DEFAULT NULL,
        quantity_in                 IN supply_workflow_hist.quantity%TYPE DEFAULT NULL,
        id_unit_measure_in          IN supply_workflow_hist.id_unit_measure%TYPE DEFAULT NULL,
        id_context_in               IN supply_workflow_hist.id_context%TYPE DEFAULT NULL,
        flg_context_in              IN supply_workflow_hist.flg_context%TYPE DEFAULT NULL,
        flg_status_in               IN supply_workflow_hist.flg_status%TYPE DEFAULT NULL,
        flg_reason_in               IN supply_workflow_hist.flg_reason%TYPE DEFAULT NULL,
        dt_request_in               IN supply_workflow_hist.dt_request%TYPE DEFAULT NULL,
        dt_returned_in              IN supply_workflow_hist.dt_returned%TYPE DEFAULT NULL,
        notes_in                    IN supply_workflow_hist.notes%TYPE DEFAULT NULL,
        id_prof_cancel_in           IN supply_workflow_hist.id_prof_cancel%TYPE DEFAULT NULL,
        dt_cancel_in                IN supply_workflow_hist.dt_cancel%TYPE DEFAULT NULL,
        notes_cancel_in             IN supply_workflow_hist.notes_cancel%TYPE DEFAULT NULL,
        id_cancel_reason_in         IN supply_workflow_hist.id_cancel_reason%TYPE DEFAULT NULL,
        notes_reject_in             IN supply_workflow_hist.notes_reject%TYPE DEFAULT NULL,
        dt_reject_in                IN supply_workflow_hist.dt_reject%TYPE DEFAULT NULL,
        id_prof_reject_in           IN supply_workflow_hist.id_prof_reject%TYPE DEFAULT NULL,
        dt_supply_workflow_in       IN supply_workflow_hist.dt_supply_workflow%TYPE DEFAULT NULL,
        create_user_in              IN supply_workflow_hist.create_user%TYPE DEFAULT NULL,
        create_time_in              IN supply_workflow_hist.create_time%TYPE DEFAULT NULL,
        create_institution_in       IN supply_workflow_hist.create_institution%TYPE DEFAULT NULL,
        update_user_in              IN supply_workflow_hist.update_user%TYPE DEFAULT NULL,
        update_time_in              IN supply_workflow_hist.update_time%TYPE DEFAULT NULL,
        update_institution_in       IN supply_workflow_hist.update_institution%TYPE DEFAULT NULL,
        id_req_reason_in            IN supply_workflow_hist.id_req_reason%TYPE DEFAULT NULL,
        id_del_reason_in            IN supply_workflow_hist.id_del_reason%TYPE DEFAULT NULL,
        id_supply_set_in            IN supply_workflow_hist.id_supply_set%TYPE DEFAULT NULL,
        id_sup_workflow_parent_in   IN supply_workflow_hist.id_sup_workflow_parent%TYPE DEFAULT NULL,
        asset_number_in             IN supply_workflow_hist.asset_number%TYPE DEFAULT NULL,
        flg_outdated_in             IN supply_workflow_hist.flg_outdated%TYPE DEFAULT 'A',
        total_quantity_in           IN supply_workflow_hist.total_quantity%TYPE DEFAULT NULL,
        flg_cons_type_in            IN supply_workflow_hist.flg_cons_type%TYPE DEFAULT NULL,
        flg_reusable_in             IN supply_workflow_hist.flg_reusable%TYPE DEFAULT NULL,
        flg_editable_in             IN supply_workflow_hist.flg_editable%TYPE DEFAULT NULL,
        total_avail_quantity_in     IN supply_workflow_hist.total_avail_quantity%TYPE DEFAULT NULL,
        cod_table_in                IN supply_workflow_hist.cod_table%TYPE DEFAULT NULL,
        flg_preparing_in            IN supply_workflow_hist.flg_preparing%TYPE DEFAULT NULL,
        flg_countable_in            IN supply_workflow_hist.flg_countable%TYPE DEFAULT NULL,
        id_supply_area_in           IN supply_workflow_hist.id_supply_area%TYPE DEFAULT NULL,
        lot_in                      IN supply_workflow_hist.lot%TYPE DEFAULT NULL,
        dt_expiration_in            IN supply_workflow_hist.dt_expiration%TYPE DEFAULT NULL,
        flg_validation_in           IN supply_workflow_hist.flg_validation%TYPE DEFAULT NULL,
        supply_migration_in         IN supply_workflow_hist.supply_migration%TYPE DEFAULT NULL,
        id_consumption_parent_in    IN supply_workflow_hist.id_consumption_parent%TYPE DEFAULT NULL,
        id_dispense_parent_in       IN supply_workflow_hist.id_dispense_parent%TYPE DEFAULT NULL,
        id_supply_workflow_hist_out IN OUT supply_workflow_hist.id_supply_workflow_hist%TYPE,
        -- Pass false if you want errors to propagate out unhandled
        handle_error_in IN BOOLEAN := FALSE
    );

    -- Insert one row with function, return generated primary key
    FUNCTION ins
    (
        id_supply_workflow_in     IN supply_workflow_hist.id_supply_workflow%TYPE DEFAULT NULL,
        id_professional_in        IN supply_workflow_hist.id_professional%TYPE DEFAULT NULL,
        id_episode_in             IN supply_workflow_hist.id_episode%TYPE DEFAULT NULL,
        id_supply_request_in      IN supply_workflow_hist.id_supply_request%TYPE DEFAULT NULL,
        id_supply_in              IN supply_workflow_hist.id_supply%TYPE DEFAULT NULL,
        id_supply_location_in     IN supply_workflow_hist.id_supply_location%TYPE DEFAULT NULL,
        barcode_req_in            IN supply_workflow_hist.barcode_req%TYPE DEFAULT NULL,
        barcode_scanned_in        IN supply_workflow_hist.barcode_scanned%TYPE DEFAULT NULL,
        quantity_in               IN supply_workflow_hist.quantity%TYPE DEFAULT NULL,
        id_unit_measure_in        IN supply_workflow_hist.id_unit_measure%TYPE DEFAULT NULL,
        id_context_in             IN supply_workflow_hist.id_context%TYPE DEFAULT NULL,
        flg_context_in            IN supply_workflow_hist.flg_context%TYPE DEFAULT NULL,
        flg_status_in             IN supply_workflow_hist.flg_status%TYPE DEFAULT NULL,
        flg_reason_in             IN supply_workflow_hist.flg_reason%TYPE DEFAULT NULL,
        dt_request_in             IN supply_workflow_hist.dt_request%TYPE DEFAULT NULL,
        dt_returned_in            IN supply_workflow_hist.dt_returned%TYPE DEFAULT NULL,
        notes_in                  IN supply_workflow_hist.notes%TYPE DEFAULT NULL,
        id_prof_cancel_in         IN supply_workflow_hist.id_prof_cancel%TYPE DEFAULT NULL,
        dt_cancel_in              IN supply_workflow_hist.dt_cancel%TYPE DEFAULT NULL,
        notes_cancel_in           IN supply_workflow_hist.notes_cancel%TYPE DEFAULT NULL,
        id_cancel_reason_in       IN supply_workflow_hist.id_cancel_reason%TYPE DEFAULT NULL,
        notes_reject_in           IN supply_workflow_hist.notes_reject%TYPE DEFAULT NULL,
        dt_reject_in              IN supply_workflow_hist.dt_reject%TYPE DEFAULT NULL,
        id_prof_reject_in         IN supply_workflow_hist.id_prof_reject%TYPE DEFAULT NULL,
        dt_supply_workflow_in     IN supply_workflow_hist.dt_supply_workflow%TYPE DEFAULT NULL,
        create_user_in            IN supply_workflow_hist.create_user%TYPE DEFAULT NULL,
        create_time_in            IN supply_workflow_hist.create_time%TYPE DEFAULT NULL,
        create_institution_in     IN supply_workflow_hist.create_institution%TYPE DEFAULT NULL,
        update_user_in            IN supply_workflow_hist.update_user%TYPE DEFAULT NULL,
        update_time_in            IN supply_workflow_hist.update_time%TYPE DEFAULT NULL,
        update_institution_in     IN supply_workflow_hist.update_institution%TYPE DEFAULT NULL,
        id_req_reason_in          IN supply_workflow_hist.id_req_reason%TYPE DEFAULT NULL,
        id_del_reason_in          IN supply_workflow_hist.id_del_reason%TYPE DEFAULT NULL,
        id_supply_set_in          IN supply_workflow_hist.id_supply_set%TYPE DEFAULT NULL,
        id_sup_workflow_parent_in IN supply_workflow_hist.id_sup_workflow_parent%TYPE DEFAULT NULL,
        asset_number_in           IN supply_workflow_hist.asset_number%TYPE DEFAULT NULL,
        flg_outdated_in           IN supply_workflow_hist.flg_outdated%TYPE DEFAULT 'A',
        total_quantity_in         IN supply_workflow_hist.total_quantity%TYPE DEFAULT NULL,
        flg_cons_type_in          IN supply_workflow_hist.flg_cons_type%TYPE DEFAULT NULL,
        flg_reusable_in           IN supply_workflow_hist.flg_reusable%TYPE DEFAULT NULL,
        flg_editable_in           IN supply_workflow_hist.flg_editable%TYPE DEFAULT NULL,
        total_avail_quantity_in   IN supply_workflow_hist.total_avail_quantity%TYPE DEFAULT NULL,
        cod_table_in              IN supply_workflow_hist.cod_table%TYPE DEFAULT NULL,
        flg_preparing_in          IN supply_workflow_hist.flg_preparing%TYPE DEFAULT NULL,
        flg_countable_in          IN supply_workflow_hist.flg_countable%TYPE DEFAULT NULL,
        id_supply_area_in         IN supply_workflow_hist.id_supply_area%TYPE DEFAULT NULL,
        lot_in                    IN supply_workflow_hist.lot%TYPE DEFAULT NULL,
        dt_expiration_in          IN supply_workflow_hist.dt_expiration%TYPE DEFAULT NULL,
        flg_validation_in         IN supply_workflow_hist.flg_validation%TYPE DEFAULT NULL,
        supply_migration_in       IN supply_workflow_hist.supply_migration%TYPE DEFAULT NULL,
        id_consumption_parent_in  IN supply_workflow_hist.id_consumption_parent%TYPE DEFAULT NULL,
        id_dispense_parent_in     IN supply_workflow_hist.id_dispense_parent%TYPE DEFAULT NULL,
        -- Pass false if you want errors to propagate out unhandled
        handle_error_in IN BOOLEAN := FALSE,
        rows_out        OUT table_varchar
    ) RETURN supply_workflow_hist.id_supply_workflow_hist%TYPE;

    -- Insert one row with function, return generated primary key
    FUNCTION ins
    (
        id_supply_workflow_in     IN supply_workflow_hist.id_supply_workflow%TYPE DEFAULT NULL,
        id_professional_in        IN supply_workflow_hist.id_professional%TYPE DEFAULT NULL,
        id_episode_in             IN supply_workflow_hist.id_episode%TYPE DEFAULT NULL,
        id_supply_request_in      IN supply_workflow_hist.id_supply_request%TYPE DEFAULT NULL,
        id_supply_in              IN supply_workflow_hist.id_supply%TYPE DEFAULT NULL,
        id_supply_location_in     IN supply_workflow_hist.id_supply_location%TYPE DEFAULT NULL,
        barcode_req_in            IN supply_workflow_hist.barcode_req%TYPE DEFAULT NULL,
        barcode_scanned_in        IN supply_workflow_hist.barcode_scanned%TYPE DEFAULT NULL,
        quantity_in               IN supply_workflow_hist.quantity%TYPE DEFAULT NULL,
        id_unit_measure_in        IN supply_workflow_hist.id_unit_measure%TYPE DEFAULT NULL,
        id_context_in             IN supply_workflow_hist.id_context%TYPE DEFAULT NULL,
        flg_context_in            IN supply_workflow_hist.flg_context%TYPE DEFAULT NULL,
        flg_status_in             IN supply_workflow_hist.flg_status%TYPE DEFAULT NULL,
        flg_reason_in             IN supply_workflow_hist.flg_reason%TYPE DEFAULT NULL,
        dt_request_in             IN supply_workflow_hist.dt_request%TYPE DEFAULT NULL,
        dt_returned_in            IN supply_workflow_hist.dt_returned%TYPE DEFAULT NULL,
        notes_in                  IN supply_workflow_hist.notes%TYPE DEFAULT NULL,
        id_prof_cancel_in         IN supply_workflow_hist.id_prof_cancel%TYPE DEFAULT NULL,
        dt_cancel_in              IN supply_workflow_hist.dt_cancel%TYPE DEFAULT NULL,
        notes_cancel_in           IN supply_workflow_hist.notes_cancel%TYPE DEFAULT NULL,
        id_cancel_reason_in       IN supply_workflow_hist.id_cancel_reason%TYPE DEFAULT NULL,
        notes_reject_in           IN supply_workflow_hist.notes_reject%TYPE DEFAULT NULL,
        dt_reject_in              IN supply_workflow_hist.dt_reject%TYPE DEFAULT NULL,
        id_prof_reject_in         IN supply_workflow_hist.id_prof_reject%TYPE DEFAULT NULL,
        dt_supply_workflow_in     IN supply_workflow_hist.dt_supply_workflow%TYPE DEFAULT NULL,
        create_user_in            IN supply_workflow_hist.create_user%TYPE DEFAULT NULL,
        create_time_in            IN supply_workflow_hist.create_time%TYPE DEFAULT NULL,
        create_institution_in     IN supply_workflow_hist.create_institution%TYPE DEFAULT NULL,
        update_user_in            IN supply_workflow_hist.update_user%TYPE DEFAULT NULL,
        update_time_in            IN supply_workflow_hist.update_time%TYPE DEFAULT NULL,
        update_institution_in     IN supply_workflow_hist.update_institution%TYPE DEFAULT NULL,
        id_req_reason_in          IN supply_workflow_hist.id_req_reason%TYPE DEFAULT NULL,
        id_del_reason_in          IN supply_workflow_hist.id_del_reason%TYPE DEFAULT NULL,
        id_supply_set_in          IN supply_workflow_hist.id_supply_set%TYPE DEFAULT NULL,
        id_sup_workflow_parent_in IN supply_workflow_hist.id_sup_workflow_parent%TYPE DEFAULT NULL,
        asset_number_in           IN supply_workflow_hist.asset_number%TYPE DEFAULT NULL,
        flg_outdated_in           IN supply_workflow_hist.flg_outdated%TYPE DEFAULT 'A',
        total_quantity_in         IN supply_workflow_hist.total_quantity%TYPE DEFAULT NULL,
        flg_cons_type_in          IN supply_workflow_hist.flg_cons_type%TYPE DEFAULT NULL,
        flg_reusable_in           IN supply_workflow_hist.flg_reusable%TYPE DEFAULT NULL,
        flg_editable_in           IN supply_workflow_hist.flg_editable%TYPE DEFAULT NULL,
        total_avail_quantity_in   IN supply_workflow_hist.total_avail_quantity%TYPE DEFAULT NULL,
        cod_table_in              IN supply_workflow_hist.cod_table%TYPE DEFAULT NULL,
        flg_preparing_in          IN supply_workflow_hist.flg_preparing%TYPE DEFAULT NULL,
        flg_countable_in          IN supply_workflow_hist.flg_countable%TYPE DEFAULT NULL,
        id_supply_area_in         IN supply_workflow_hist.id_supply_area%TYPE DEFAULT NULL,
        lot_in                    IN supply_workflow_hist.lot%TYPE DEFAULT NULL,
        dt_expiration_in          IN supply_workflow_hist.dt_expiration%TYPE DEFAULT NULL,
        flg_validation_in         IN supply_workflow_hist.flg_validation%TYPE DEFAULT NULL,
        supply_migration_in       IN supply_workflow_hist.supply_migration%TYPE DEFAULT NULL,
        id_consumption_parent_in  IN supply_workflow_hist.id_consumption_parent%TYPE DEFAULT NULL,
        id_dispense_parent_in     IN supply_workflow_hist.id_dispense_parent%TYPE DEFAULT NULL,
        -- Pass false if you want errors to propagate out unhandled
        handle_error_in IN BOOLEAN := FALSE
    ) RETURN supply_workflow_hist.id_supply_workflow_hist%TYPE;

    -- Update any/all columns by primary key. If you pass NULL, then
    -- the current column value is set to itself. If you need a more
    -- selected UPDATE then use one of the onecol procedures below.
    PROCEDURE upd
    (
        id_supply_workflow_hist_in IN supply_workflow_hist.id_supply_workflow_hist%TYPE,
        id_supply_workflow_in      IN supply_workflow_hist.id_supply_workflow%TYPE DEFAULT NULL,
        id_supply_workflow_nin     IN BOOLEAN := TRUE,
        id_professional_in         IN supply_workflow_hist.id_professional%TYPE DEFAULT NULL,
        id_professional_nin        IN BOOLEAN := TRUE,
        id_episode_in              IN supply_workflow_hist.id_episode%TYPE DEFAULT NULL,
        id_episode_nin             IN BOOLEAN := TRUE,
        id_supply_request_in       IN supply_workflow_hist.id_supply_request%TYPE DEFAULT NULL,
        id_supply_request_nin      IN BOOLEAN := TRUE,
        id_supply_in               IN supply_workflow_hist.id_supply%TYPE DEFAULT NULL,
        id_supply_nin              IN BOOLEAN := TRUE,
        id_supply_location_in      IN supply_workflow_hist.id_supply_location%TYPE DEFAULT NULL,
        id_supply_location_nin     IN BOOLEAN := TRUE,
        barcode_req_in             IN supply_workflow_hist.barcode_req%TYPE DEFAULT NULL,
        barcode_req_nin            IN BOOLEAN := TRUE,
        barcode_scanned_in         IN supply_workflow_hist.barcode_scanned%TYPE DEFAULT NULL,
        barcode_scanned_nin        IN BOOLEAN := TRUE,
        quantity_in                IN supply_workflow_hist.quantity%TYPE DEFAULT NULL,
        quantity_nin               IN BOOLEAN := TRUE,
        id_unit_measure_in         IN supply_workflow_hist.id_unit_measure%TYPE DEFAULT NULL,
        id_unit_measure_nin        IN BOOLEAN := TRUE,
        id_context_in              IN supply_workflow_hist.id_context%TYPE DEFAULT NULL,
        id_context_nin             IN BOOLEAN := TRUE,
        flg_context_in             IN supply_workflow_hist.flg_context%TYPE DEFAULT NULL,
        flg_context_nin            IN BOOLEAN := TRUE,
        flg_status_in              IN supply_workflow_hist.flg_status%TYPE DEFAULT NULL,
        flg_status_nin             IN BOOLEAN := TRUE,
        flg_reason_in              IN supply_workflow_hist.flg_reason%TYPE DEFAULT NULL,
        flg_reason_nin             IN BOOLEAN := TRUE,
        dt_request_in              IN supply_workflow_hist.dt_request%TYPE DEFAULT NULL,
        dt_request_nin             IN BOOLEAN := TRUE,
        dt_returned_in             IN supply_workflow_hist.dt_returned%TYPE DEFAULT NULL,
        dt_returned_nin            IN BOOLEAN := TRUE,
        notes_in                   IN supply_workflow_hist.notes%TYPE DEFAULT NULL,
        notes_nin                  IN BOOLEAN := TRUE,
        id_prof_cancel_in          IN supply_workflow_hist.id_prof_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_nin         IN BOOLEAN := TRUE,
        dt_cancel_in               IN supply_workflow_hist.dt_cancel%TYPE DEFAULT NULL,
        dt_cancel_nin              IN BOOLEAN := TRUE,
        notes_cancel_in            IN supply_workflow_hist.notes_cancel%TYPE DEFAULT NULL,
        notes_cancel_nin           IN BOOLEAN := TRUE,
        id_cancel_reason_in        IN supply_workflow_hist.id_cancel_reason%TYPE DEFAULT NULL,
        id_cancel_reason_nin       IN BOOLEAN := TRUE,
        notes_reject_in            IN supply_workflow_hist.notes_reject%TYPE DEFAULT NULL,
        notes_reject_nin           IN BOOLEAN := TRUE,
        dt_reject_in               IN supply_workflow_hist.dt_reject%TYPE DEFAULT NULL,
        dt_reject_nin              IN BOOLEAN := TRUE,
        id_prof_reject_in          IN supply_workflow_hist.id_prof_reject%TYPE DEFAULT NULL,
        id_prof_reject_nin         IN BOOLEAN := TRUE,
        dt_supply_workflow_in      IN supply_workflow_hist.dt_supply_workflow%TYPE DEFAULT NULL,
        dt_supply_workflow_nin     IN BOOLEAN := TRUE,
        create_user_in             IN supply_workflow_hist.create_user%TYPE DEFAULT NULL,
        create_user_nin            IN BOOLEAN := TRUE,
        create_time_in             IN supply_workflow_hist.create_time%TYPE DEFAULT NULL,
        create_time_nin            IN BOOLEAN := TRUE,
        create_institution_in      IN supply_workflow_hist.create_institution%TYPE DEFAULT NULL,
        create_institution_nin     IN BOOLEAN := TRUE,
        update_user_in             IN supply_workflow_hist.update_user%TYPE DEFAULT NULL,
        update_user_nin            IN BOOLEAN := TRUE,
        update_time_in             IN supply_workflow_hist.update_time%TYPE DEFAULT NULL,
        update_time_nin            IN BOOLEAN := TRUE,
        update_institution_in      IN supply_workflow_hist.update_institution%TYPE DEFAULT NULL,
        update_institution_nin     IN BOOLEAN := TRUE,
        id_req_reason_in           IN supply_workflow_hist.id_req_reason%TYPE DEFAULT NULL,
        id_req_reason_nin          IN BOOLEAN := TRUE,
        id_del_reason_in           IN supply_workflow_hist.id_del_reason%TYPE DEFAULT NULL,
        id_del_reason_nin          IN BOOLEAN := TRUE,
        id_supply_set_in           IN supply_workflow_hist.id_supply_set%TYPE DEFAULT NULL,
        id_supply_set_nin          IN BOOLEAN := TRUE,
        id_sup_workflow_parent_in  IN supply_workflow_hist.id_sup_workflow_parent%TYPE DEFAULT NULL,
        id_sup_workflow_parent_nin IN BOOLEAN := TRUE,
        asset_number_in            IN supply_workflow_hist.asset_number%TYPE DEFAULT NULL,
        asset_number_nin           IN BOOLEAN := TRUE,
        flg_outdated_in            IN supply_workflow_hist.flg_outdated%TYPE DEFAULT NULL,
        flg_outdated_nin           IN BOOLEAN := TRUE,
        total_quantity_in          IN supply_workflow_hist.total_quantity%TYPE DEFAULT NULL,
        total_quantity_nin         IN BOOLEAN := TRUE,
        flg_cons_type_in           IN supply_workflow_hist.flg_cons_type%TYPE DEFAULT NULL,
        flg_cons_type_nin          IN BOOLEAN := TRUE,
        flg_reusable_in            IN supply_workflow_hist.flg_reusable%TYPE DEFAULT NULL,
        flg_reusable_nin           IN BOOLEAN := TRUE,
        flg_editable_in            IN supply_workflow_hist.flg_editable%TYPE DEFAULT NULL,
        flg_editable_nin           IN BOOLEAN := TRUE,
        total_avail_quantity_in    IN supply_workflow_hist.total_avail_quantity%TYPE DEFAULT NULL,
        total_avail_quantity_nin   IN BOOLEAN := TRUE,
        cod_table_in               IN supply_workflow_hist.cod_table%TYPE DEFAULT NULL,
        cod_table_nin              IN BOOLEAN := TRUE,
        flg_preparing_in           IN supply_workflow_hist.flg_preparing%TYPE DEFAULT NULL,
        flg_preparing_nin          IN BOOLEAN := TRUE,
        flg_countable_in           IN supply_workflow_hist.flg_countable%TYPE DEFAULT NULL,
        flg_countable_nin          IN BOOLEAN := TRUE,
        id_supply_area_in          IN supply_workflow_hist.id_supply_area%TYPE DEFAULT NULL,
        id_supply_area_nin         IN BOOLEAN := TRUE,
        lot_in                     IN supply_workflow_hist.lot%TYPE DEFAULT NULL,
        lot_nin                    IN BOOLEAN := TRUE,
        dt_expiration_in           IN supply_workflow_hist.dt_expiration%TYPE DEFAULT NULL,
        dt_expiration_nin          IN BOOLEAN := TRUE,
        flg_validation_in          IN supply_workflow_hist.flg_validation%TYPE DEFAULT NULL,
        flg_validation_nin         IN BOOLEAN := TRUE,
        supply_migration_in        IN supply_workflow_hist.supply_migration%TYPE DEFAULT NULL,
        supply_migration_nin       IN BOOLEAN := TRUE,
        id_consumption_parent_in   IN supply_workflow_hist.id_consumption_parent%TYPE DEFAULT NULL,
        id_consumption_parent_nin  IN BOOLEAN := TRUE,
        id_dispense_parent_in      IN supply_workflow_hist.id_dispense_parent%TYPE DEFAULT NULL,
        id_dispense_parent_nin     IN BOOLEAN := TRUE,
        handle_error_in            IN BOOLEAN := FALSE,
        rows_out                   IN OUT table_varchar
    );

    -- Update any/all columns by primary key. If you pass NULL, then
    -- the current column value is set to itself. If you need a more
    -- selected UPDATE then use one of the onecol procedures below.
    PROCEDURE upd
    (
        id_supply_workflow_hist_in IN supply_workflow_hist.id_supply_workflow_hist%TYPE,
        id_supply_workflow_in      IN supply_workflow_hist.id_supply_workflow%TYPE DEFAULT NULL,
        id_supply_workflow_nin     IN BOOLEAN := TRUE,
        id_professional_in         IN supply_workflow_hist.id_professional%TYPE DEFAULT NULL,
        id_professional_nin        IN BOOLEAN := TRUE,
        id_episode_in              IN supply_workflow_hist.id_episode%TYPE DEFAULT NULL,
        id_episode_nin             IN BOOLEAN := TRUE,
        id_supply_request_in       IN supply_workflow_hist.id_supply_request%TYPE DEFAULT NULL,
        id_supply_request_nin      IN BOOLEAN := TRUE,
        id_supply_in               IN supply_workflow_hist.id_supply%TYPE DEFAULT NULL,
        id_supply_nin              IN BOOLEAN := TRUE,
        id_supply_location_in      IN supply_workflow_hist.id_supply_location%TYPE DEFAULT NULL,
        id_supply_location_nin     IN BOOLEAN := TRUE,
        barcode_req_in             IN supply_workflow_hist.barcode_req%TYPE DEFAULT NULL,
        barcode_req_nin            IN BOOLEAN := TRUE,
        barcode_scanned_in         IN supply_workflow_hist.barcode_scanned%TYPE DEFAULT NULL,
        barcode_scanned_nin        IN BOOLEAN := TRUE,
        quantity_in                IN supply_workflow_hist.quantity%TYPE DEFAULT NULL,
        quantity_nin               IN BOOLEAN := TRUE,
        id_unit_measure_in         IN supply_workflow_hist.id_unit_measure%TYPE DEFAULT NULL,
        id_unit_measure_nin        IN BOOLEAN := TRUE,
        id_context_in              IN supply_workflow_hist.id_context%TYPE DEFAULT NULL,
        id_context_nin             IN BOOLEAN := TRUE,
        flg_context_in             IN supply_workflow_hist.flg_context%TYPE DEFAULT NULL,
        flg_context_nin            IN BOOLEAN := TRUE,
        flg_status_in              IN supply_workflow_hist.flg_status%TYPE DEFAULT NULL,
        flg_status_nin             IN BOOLEAN := TRUE,
        flg_reason_in              IN supply_workflow_hist.flg_reason%TYPE DEFAULT NULL,
        flg_reason_nin             IN BOOLEAN := TRUE,
        dt_request_in              IN supply_workflow_hist.dt_request%TYPE DEFAULT NULL,
        dt_request_nin             IN BOOLEAN := TRUE,
        dt_returned_in             IN supply_workflow_hist.dt_returned%TYPE DEFAULT NULL,
        dt_returned_nin            IN BOOLEAN := TRUE,
        notes_in                   IN supply_workflow_hist.notes%TYPE DEFAULT NULL,
        notes_nin                  IN BOOLEAN := TRUE,
        id_prof_cancel_in          IN supply_workflow_hist.id_prof_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_nin         IN BOOLEAN := TRUE,
        dt_cancel_in               IN supply_workflow_hist.dt_cancel%TYPE DEFAULT NULL,
        dt_cancel_nin              IN BOOLEAN := TRUE,
        notes_cancel_in            IN supply_workflow_hist.notes_cancel%TYPE DEFAULT NULL,
        notes_cancel_nin           IN BOOLEAN := TRUE,
        id_cancel_reason_in        IN supply_workflow_hist.id_cancel_reason%TYPE DEFAULT NULL,
        id_cancel_reason_nin       IN BOOLEAN := TRUE,
        notes_reject_in            IN supply_workflow_hist.notes_reject%TYPE DEFAULT NULL,
        notes_reject_nin           IN BOOLEAN := TRUE,
        dt_reject_in               IN supply_workflow_hist.dt_reject%TYPE DEFAULT NULL,
        dt_reject_nin              IN BOOLEAN := TRUE,
        id_prof_reject_in          IN supply_workflow_hist.id_prof_reject%TYPE DEFAULT NULL,
        id_prof_reject_nin         IN BOOLEAN := TRUE,
        dt_supply_workflow_in      IN supply_workflow_hist.dt_supply_workflow%TYPE DEFAULT NULL,
        dt_supply_workflow_nin     IN BOOLEAN := TRUE,
        create_user_in             IN supply_workflow_hist.create_user%TYPE DEFAULT NULL,
        create_user_nin            IN BOOLEAN := TRUE,
        create_time_in             IN supply_workflow_hist.create_time%TYPE DEFAULT NULL,
        create_time_nin            IN BOOLEAN := TRUE,
        create_institution_in      IN supply_workflow_hist.create_institution%TYPE DEFAULT NULL,
        create_institution_nin     IN BOOLEAN := TRUE,
        update_user_in             IN supply_workflow_hist.update_user%TYPE DEFAULT NULL,
        update_user_nin            IN BOOLEAN := TRUE,
        update_time_in             IN supply_workflow_hist.update_time%TYPE DEFAULT NULL,
        update_time_nin            IN BOOLEAN := TRUE,
        update_institution_in      IN supply_workflow_hist.update_institution%TYPE DEFAULT NULL,
        update_institution_nin     IN BOOLEAN := TRUE,
        id_req_reason_in           IN supply_workflow_hist.id_req_reason%TYPE DEFAULT NULL,
        id_req_reason_nin          IN BOOLEAN := TRUE,
        id_del_reason_in           IN supply_workflow_hist.id_del_reason%TYPE DEFAULT NULL,
        id_del_reason_nin          IN BOOLEAN := TRUE,
        id_supply_set_in           IN supply_workflow_hist.id_supply_set%TYPE DEFAULT NULL,
        id_supply_set_nin          IN BOOLEAN := TRUE,
        id_sup_workflow_parent_in  IN supply_workflow_hist.id_sup_workflow_parent%TYPE DEFAULT NULL,
        id_sup_workflow_parent_nin IN BOOLEAN := TRUE,
        asset_number_in            IN supply_workflow_hist.asset_number%TYPE DEFAULT NULL,
        asset_number_nin           IN BOOLEAN := TRUE,
        flg_outdated_in            IN supply_workflow_hist.flg_outdated%TYPE DEFAULT NULL,
        flg_outdated_nin           IN BOOLEAN := TRUE,
        total_quantity_in          IN supply_workflow_hist.total_quantity%TYPE DEFAULT NULL,
        total_quantity_nin         IN BOOLEAN := TRUE,
        flg_cons_type_in           IN supply_workflow_hist.flg_cons_type%TYPE DEFAULT NULL,
        flg_cons_type_nin          IN BOOLEAN := TRUE,
        flg_reusable_in            IN supply_workflow_hist.flg_reusable%TYPE DEFAULT NULL,
        flg_reusable_nin           IN BOOLEAN := TRUE,
        flg_editable_in            IN supply_workflow_hist.flg_editable%TYPE DEFAULT NULL,
        flg_editable_nin           IN BOOLEAN := TRUE,
        total_avail_quantity_in    IN supply_workflow_hist.total_avail_quantity%TYPE DEFAULT NULL,
        total_avail_quantity_nin   IN BOOLEAN := TRUE,
        cod_table_in               IN supply_workflow_hist.cod_table%TYPE DEFAULT NULL,
        cod_table_nin              IN BOOLEAN := TRUE,
        flg_preparing_in           IN supply_workflow_hist.flg_preparing%TYPE DEFAULT NULL,
        flg_preparing_nin          IN BOOLEAN := TRUE,
        flg_countable_in           IN supply_workflow_hist.flg_countable%TYPE DEFAULT NULL,
        flg_countable_nin          IN BOOLEAN := TRUE,
        id_supply_area_in          IN supply_workflow_hist.id_supply_area%TYPE DEFAULT NULL,
        id_supply_area_nin         IN BOOLEAN := TRUE,
        lot_in                     IN supply_workflow_hist.lot%TYPE DEFAULT NULL,
        lot_nin                    IN BOOLEAN := TRUE,
        dt_expiration_in           IN supply_workflow_hist.dt_expiration%TYPE DEFAULT NULL,
        dt_expiration_nin          IN BOOLEAN := TRUE,
        flg_validation_in          IN supply_workflow_hist.flg_validation%TYPE DEFAULT NULL,
        flg_validation_nin         IN BOOLEAN := TRUE,
        supply_migration_in        IN supply_workflow_hist.supply_migration%TYPE DEFAULT NULL,
        supply_migration_nin       IN BOOLEAN := TRUE,
        id_consumption_parent_in   IN supply_workflow_hist.id_consumption_parent%TYPE DEFAULT NULL,
        id_consumption_parent_nin  IN BOOLEAN := TRUE,
        id_dispense_parent_in      IN supply_workflow_hist.id_dispense_parent%TYPE DEFAULT NULL,
        id_dispense_parent_nin     IN BOOLEAN := TRUE,
        handle_error_in            IN BOOLEAN := FALSE
    );

    --Update any/all columns by dynamic WHERE
    -- If you pass NULL, then the current column value is set to itself
    PROCEDURE upd
    (
        id_supply_workflow_in      IN supply_workflow_hist.id_supply_workflow%TYPE DEFAULT NULL,
        id_supply_workflow_nin     IN BOOLEAN := TRUE,
        id_professional_in         IN supply_workflow_hist.id_professional%TYPE DEFAULT NULL,
        id_professional_nin        IN BOOLEAN := TRUE,
        id_episode_in              IN supply_workflow_hist.id_episode%TYPE DEFAULT NULL,
        id_episode_nin             IN BOOLEAN := TRUE,
        id_supply_request_in       IN supply_workflow_hist.id_supply_request%TYPE DEFAULT NULL,
        id_supply_request_nin      IN BOOLEAN := TRUE,
        id_supply_in               IN supply_workflow_hist.id_supply%TYPE DEFAULT NULL,
        id_supply_nin              IN BOOLEAN := TRUE,
        id_supply_location_in      IN supply_workflow_hist.id_supply_location%TYPE DEFAULT NULL,
        id_supply_location_nin     IN BOOLEAN := TRUE,
        barcode_req_in             IN supply_workflow_hist.barcode_req%TYPE DEFAULT NULL,
        barcode_req_nin            IN BOOLEAN := TRUE,
        barcode_scanned_in         IN supply_workflow_hist.barcode_scanned%TYPE DEFAULT NULL,
        barcode_scanned_nin        IN BOOLEAN := TRUE,
        quantity_in                IN supply_workflow_hist.quantity%TYPE DEFAULT NULL,
        quantity_nin               IN BOOLEAN := TRUE,
        id_unit_measure_in         IN supply_workflow_hist.id_unit_measure%TYPE DEFAULT NULL,
        id_unit_measure_nin        IN BOOLEAN := TRUE,
        id_context_in              IN supply_workflow_hist.id_context%TYPE DEFAULT NULL,
        id_context_nin             IN BOOLEAN := TRUE,
        flg_context_in             IN supply_workflow_hist.flg_context%TYPE DEFAULT NULL,
        flg_context_nin            IN BOOLEAN := TRUE,
        flg_status_in              IN supply_workflow_hist.flg_status%TYPE DEFAULT NULL,
        flg_status_nin             IN BOOLEAN := TRUE,
        flg_reason_in              IN supply_workflow_hist.flg_reason%TYPE DEFAULT NULL,
        flg_reason_nin             IN BOOLEAN := TRUE,
        dt_request_in              IN supply_workflow_hist.dt_request%TYPE DEFAULT NULL,
        dt_request_nin             IN BOOLEAN := TRUE,
        dt_returned_in             IN supply_workflow_hist.dt_returned%TYPE DEFAULT NULL,
        dt_returned_nin            IN BOOLEAN := TRUE,
        notes_in                   IN supply_workflow_hist.notes%TYPE DEFAULT NULL,
        notes_nin                  IN BOOLEAN := TRUE,
        id_prof_cancel_in          IN supply_workflow_hist.id_prof_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_nin         IN BOOLEAN := TRUE,
        dt_cancel_in               IN supply_workflow_hist.dt_cancel%TYPE DEFAULT NULL,
        dt_cancel_nin              IN BOOLEAN := TRUE,
        notes_cancel_in            IN supply_workflow_hist.notes_cancel%TYPE DEFAULT NULL,
        notes_cancel_nin           IN BOOLEAN := TRUE,
        id_cancel_reason_in        IN supply_workflow_hist.id_cancel_reason%TYPE DEFAULT NULL,
        id_cancel_reason_nin       IN BOOLEAN := TRUE,
        notes_reject_in            IN supply_workflow_hist.notes_reject%TYPE DEFAULT NULL,
        notes_reject_nin           IN BOOLEAN := TRUE,
        dt_reject_in               IN supply_workflow_hist.dt_reject%TYPE DEFAULT NULL,
        dt_reject_nin              IN BOOLEAN := TRUE,
        id_prof_reject_in          IN supply_workflow_hist.id_prof_reject%TYPE DEFAULT NULL,
        id_prof_reject_nin         IN BOOLEAN := TRUE,
        dt_supply_workflow_in      IN supply_workflow_hist.dt_supply_workflow%TYPE DEFAULT NULL,
        dt_supply_workflow_nin     IN BOOLEAN := TRUE,
        create_user_in             IN supply_workflow_hist.create_user%TYPE DEFAULT NULL,
        create_user_nin            IN BOOLEAN := TRUE,
        create_time_in             IN supply_workflow_hist.create_time%TYPE DEFAULT NULL,
        create_time_nin            IN BOOLEAN := TRUE,
        create_institution_in      IN supply_workflow_hist.create_institution%TYPE DEFAULT NULL,
        create_institution_nin     IN BOOLEAN := TRUE,
        update_user_in             IN supply_workflow_hist.update_user%TYPE DEFAULT NULL,
        update_user_nin            IN BOOLEAN := TRUE,
        update_time_in             IN supply_workflow_hist.update_time%TYPE DEFAULT NULL,
        update_time_nin            IN BOOLEAN := TRUE,
        update_institution_in      IN supply_workflow_hist.update_institution%TYPE DEFAULT NULL,
        update_institution_nin     IN BOOLEAN := TRUE,
        id_req_reason_in           IN supply_workflow_hist.id_req_reason%TYPE DEFAULT NULL,
        id_req_reason_nin          IN BOOLEAN := TRUE,
        id_del_reason_in           IN supply_workflow_hist.id_del_reason%TYPE DEFAULT NULL,
        id_del_reason_nin          IN BOOLEAN := TRUE,
        id_supply_set_in           IN supply_workflow_hist.id_supply_set%TYPE DEFAULT NULL,
        id_supply_set_nin          IN BOOLEAN := TRUE,
        id_sup_workflow_parent_in  IN supply_workflow_hist.id_sup_workflow_parent%TYPE DEFAULT NULL,
        id_sup_workflow_parent_nin IN BOOLEAN := TRUE,
        asset_number_in            IN supply_workflow_hist.asset_number%TYPE DEFAULT NULL,
        asset_number_nin           IN BOOLEAN := TRUE,
        flg_outdated_in            IN supply_workflow_hist.flg_outdated%TYPE DEFAULT NULL,
        flg_outdated_nin           IN BOOLEAN := TRUE,
        total_quantity_in          IN supply_workflow_hist.total_quantity%TYPE DEFAULT NULL,
        total_quantity_nin         IN BOOLEAN := TRUE,
        flg_cons_type_in           IN supply_workflow_hist.flg_cons_type%TYPE DEFAULT NULL,
        flg_cons_type_nin          IN BOOLEAN := TRUE,
        flg_reusable_in            IN supply_workflow_hist.flg_reusable%TYPE DEFAULT NULL,
        flg_reusable_nin           IN BOOLEAN := TRUE,
        flg_editable_in            IN supply_workflow_hist.flg_editable%TYPE DEFAULT NULL,
        flg_editable_nin           IN BOOLEAN := TRUE,
        total_avail_quantity_in    IN supply_workflow_hist.total_avail_quantity%TYPE DEFAULT NULL,
        total_avail_quantity_nin   IN BOOLEAN := TRUE,
        cod_table_in               IN supply_workflow_hist.cod_table%TYPE DEFAULT NULL,
        cod_table_nin              IN BOOLEAN := TRUE,
        flg_preparing_in           IN supply_workflow_hist.flg_preparing%TYPE DEFAULT NULL,
        flg_preparing_nin          IN BOOLEAN := TRUE,
        flg_countable_in           IN supply_workflow_hist.flg_countable%TYPE DEFAULT NULL,
        flg_countable_nin          IN BOOLEAN := TRUE,
        id_supply_area_in          IN supply_workflow_hist.id_supply_area%TYPE DEFAULT NULL,
        id_supply_area_nin         IN BOOLEAN := TRUE,
        lot_in                     IN supply_workflow_hist.lot%TYPE DEFAULT NULL,
        lot_nin                    IN BOOLEAN := TRUE,
        dt_expiration_in           IN supply_workflow_hist.dt_expiration%TYPE DEFAULT NULL,
        dt_expiration_nin          IN BOOLEAN := TRUE,
        flg_validation_in          IN supply_workflow_hist.flg_validation%TYPE DEFAULT NULL,
        flg_validation_nin         IN BOOLEAN := TRUE,
        supply_migration_in        IN supply_workflow_hist.supply_migration%TYPE DEFAULT NULL,
        supply_migration_nin       IN BOOLEAN := TRUE,
        id_consumption_parent_in   IN supply_workflow_hist.id_consumption_parent%TYPE DEFAULT NULL,
        id_consumption_parent_nin  IN BOOLEAN := TRUE,
        id_dispense_parent_in      IN supply_workflow_hist.id_dispense_parent%TYPE DEFAULT NULL,
        id_dispense_parent_nin     IN BOOLEAN := TRUE,
        where_in                   IN VARCHAR2,
        handle_error_in            IN BOOLEAN := FALSE,
        rows_out                   IN OUT table_varchar
    );

    --Update any/all columns by dynamic WHERE
    -- If you pass NULL, then the current column value is set to itself
    PROCEDURE upd
    (
        id_supply_workflow_in      IN supply_workflow_hist.id_supply_workflow%TYPE DEFAULT NULL,
        id_supply_workflow_nin     IN BOOLEAN := TRUE,
        id_professional_in         IN supply_workflow_hist.id_professional%TYPE DEFAULT NULL,
        id_professional_nin        IN BOOLEAN := TRUE,
        id_episode_in              IN supply_workflow_hist.id_episode%TYPE DEFAULT NULL,
        id_episode_nin             IN BOOLEAN := TRUE,
        id_supply_request_in       IN supply_workflow_hist.id_supply_request%TYPE DEFAULT NULL,
        id_supply_request_nin      IN BOOLEAN := TRUE,
        id_supply_in               IN supply_workflow_hist.id_supply%TYPE DEFAULT NULL,
        id_supply_nin              IN BOOLEAN := TRUE,
        id_supply_location_in      IN supply_workflow_hist.id_supply_location%TYPE DEFAULT NULL,
        id_supply_location_nin     IN BOOLEAN := TRUE,
        barcode_req_in             IN supply_workflow_hist.barcode_req%TYPE DEFAULT NULL,
        barcode_req_nin            IN BOOLEAN := TRUE,
        barcode_scanned_in         IN supply_workflow_hist.barcode_scanned%TYPE DEFAULT NULL,
        barcode_scanned_nin        IN BOOLEAN := TRUE,
        quantity_in                IN supply_workflow_hist.quantity%TYPE DEFAULT NULL,
        quantity_nin               IN BOOLEAN := TRUE,
        id_unit_measure_in         IN supply_workflow_hist.id_unit_measure%TYPE DEFAULT NULL,
        id_unit_measure_nin        IN BOOLEAN := TRUE,
        id_context_in              IN supply_workflow_hist.id_context%TYPE DEFAULT NULL,
        id_context_nin             IN BOOLEAN := TRUE,
        flg_context_in             IN supply_workflow_hist.flg_context%TYPE DEFAULT NULL,
        flg_context_nin            IN BOOLEAN := TRUE,
        flg_status_in              IN supply_workflow_hist.flg_status%TYPE DEFAULT NULL,
        flg_status_nin             IN BOOLEAN := TRUE,
        flg_reason_in              IN supply_workflow_hist.flg_reason%TYPE DEFAULT NULL,
        flg_reason_nin             IN BOOLEAN := TRUE,
        dt_request_in              IN supply_workflow_hist.dt_request%TYPE DEFAULT NULL,
        dt_request_nin             IN BOOLEAN := TRUE,
        dt_returned_in             IN supply_workflow_hist.dt_returned%TYPE DEFAULT NULL,
        dt_returned_nin            IN BOOLEAN := TRUE,
        notes_in                   IN supply_workflow_hist.notes%TYPE DEFAULT NULL,
        notes_nin                  IN BOOLEAN := TRUE,
        id_prof_cancel_in          IN supply_workflow_hist.id_prof_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_nin         IN BOOLEAN := TRUE,
        dt_cancel_in               IN supply_workflow_hist.dt_cancel%TYPE DEFAULT NULL,
        dt_cancel_nin              IN BOOLEAN := TRUE,
        notes_cancel_in            IN supply_workflow_hist.notes_cancel%TYPE DEFAULT NULL,
        notes_cancel_nin           IN BOOLEAN := TRUE,
        id_cancel_reason_in        IN supply_workflow_hist.id_cancel_reason%TYPE DEFAULT NULL,
        id_cancel_reason_nin       IN BOOLEAN := TRUE,
        notes_reject_in            IN supply_workflow_hist.notes_reject%TYPE DEFAULT NULL,
        notes_reject_nin           IN BOOLEAN := TRUE,
        dt_reject_in               IN supply_workflow_hist.dt_reject%TYPE DEFAULT NULL,
        dt_reject_nin              IN BOOLEAN := TRUE,
        id_prof_reject_in          IN supply_workflow_hist.id_prof_reject%TYPE DEFAULT NULL,
        id_prof_reject_nin         IN BOOLEAN := TRUE,
        dt_supply_workflow_in      IN supply_workflow_hist.dt_supply_workflow%TYPE DEFAULT NULL,
        dt_supply_workflow_nin     IN BOOLEAN := TRUE,
        create_user_in             IN supply_workflow_hist.create_user%TYPE DEFAULT NULL,
        create_user_nin            IN BOOLEAN := TRUE,
        create_time_in             IN supply_workflow_hist.create_time%TYPE DEFAULT NULL,
        create_time_nin            IN BOOLEAN := TRUE,
        create_institution_in      IN supply_workflow_hist.create_institution%TYPE DEFAULT NULL,
        create_institution_nin     IN BOOLEAN := TRUE,
        update_user_in             IN supply_workflow_hist.update_user%TYPE DEFAULT NULL,
        update_user_nin            IN BOOLEAN := TRUE,
        update_time_in             IN supply_workflow_hist.update_time%TYPE DEFAULT NULL,
        update_time_nin            IN BOOLEAN := TRUE,
        update_institution_in      IN supply_workflow_hist.update_institution%TYPE DEFAULT NULL,
        update_institution_nin     IN BOOLEAN := TRUE,
        id_req_reason_in           IN supply_workflow_hist.id_req_reason%TYPE DEFAULT NULL,
        id_req_reason_nin          IN BOOLEAN := TRUE,
        id_del_reason_in           IN supply_workflow_hist.id_del_reason%TYPE DEFAULT NULL,
        id_del_reason_nin          IN BOOLEAN := TRUE,
        id_supply_set_in           IN supply_workflow_hist.id_supply_set%TYPE DEFAULT NULL,
        id_supply_set_nin          IN BOOLEAN := TRUE,
        id_sup_workflow_parent_in  IN supply_workflow_hist.id_sup_workflow_parent%TYPE DEFAULT NULL,
        id_sup_workflow_parent_nin IN BOOLEAN := TRUE,
        asset_number_in            IN supply_workflow_hist.asset_number%TYPE DEFAULT NULL,
        asset_number_nin           IN BOOLEAN := TRUE,
        flg_outdated_in            IN supply_workflow_hist.flg_outdated%TYPE DEFAULT NULL,
        flg_outdated_nin           IN BOOLEAN := TRUE,
        total_quantity_in          IN supply_workflow_hist.total_quantity%TYPE DEFAULT NULL,
        total_quantity_nin         IN BOOLEAN := TRUE,
        flg_cons_type_in           IN supply_workflow_hist.flg_cons_type%TYPE DEFAULT NULL,
        flg_cons_type_nin          IN BOOLEAN := TRUE,
        flg_reusable_in            IN supply_workflow_hist.flg_reusable%TYPE DEFAULT NULL,
        flg_reusable_nin           IN BOOLEAN := TRUE,
        flg_editable_in            IN supply_workflow_hist.flg_editable%TYPE DEFAULT NULL,
        flg_editable_nin           IN BOOLEAN := TRUE,
        total_avail_quantity_in    IN supply_workflow_hist.total_avail_quantity%TYPE DEFAULT NULL,
        total_avail_quantity_nin   IN BOOLEAN := TRUE,
        cod_table_in               IN supply_workflow_hist.cod_table%TYPE DEFAULT NULL,
        cod_table_nin              IN BOOLEAN := TRUE,
        flg_preparing_in           IN supply_workflow_hist.flg_preparing%TYPE DEFAULT NULL,
        flg_preparing_nin          IN BOOLEAN := TRUE,
        flg_countable_in           IN supply_workflow_hist.flg_countable%TYPE DEFAULT NULL,
        flg_countable_nin          IN BOOLEAN := TRUE,
        id_supply_area_in          IN supply_workflow_hist.id_supply_area%TYPE DEFAULT NULL,
        id_supply_area_nin         IN BOOLEAN := TRUE,
        lot_in                     IN supply_workflow_hist.lot%TYPE DEFAULT NULL,
        lot_nin                    IN BOOLEAN := TRUE,
        dt_expiration_in           IN supply_workflow_hist.dt_expiration%TYPE DEFAULT NULL,
        dt_expiration_nin          IN BOOLEAN := TRUE,
        flg_validation_in          IN supply_workflow_hist.flg_validation%TYPE DEFAULT NULL,
        flg_validation_nin         IN BOOLEAN := TRUE,
        supply_migration_in        IN supply_workflow_hist.supply_migration%TYPE DEFAULT NULL,
        supply_migration_nin       IN BOOLEAN := TRUE,
        id_consumption_parent_in   IN supply_workflow_hist.id_consumption_parent%TYPE DEFAULT NULL,
        id_consumption_parent_nin  IN BOOLEAN := TRUE,
        id_dispense_parent_in      IN supply_workflow_hist.id_dispense_parent%TYPE DEFAULT NULL,
        id_dispense_parent_nin     IN BOOLEAN := TRUE,
        where_in                   IN VARCHAR2,
        handle_error_in            IN BOOLEAN := FALSE
    );

    --Update/insert with columns (with rows_out)
    PROCEDURE upd_ins
    (
        id_supply_workflow_hist_in IN supply_workflow_hist.id_supply_workflow_hist%TYPE,
        id_supply_workflow_in      IN supply_workflow_hist.id_supply_workflow%TYPE DEFAULT NULL,
        id_professional_in         IN supply_workflow_hist.id_professional%TYPE DEFAULT NULL,
        id_episode_in              IN supply_workflow_hist.id_episode%TYPE DEFAULT NULL,
        id_supply_request_in       IN supply_workflow_hist.id_supply_request%TYPE DEFAULT NULL,
        id_supply_in               IN supply_workflow_hist.id_supply%TYPE DEFAULT NULL,
        id_supply_location_in      IN supply_workflow_hist.id_supply_location%TYPE DEFAULT NULL,
        barcode_req_in             IN supply_workflow_hist.barcode_req%TYPE DEFAULT NULL,
        barcode_scanned_in         IN supply_workflow_hist.barcode_scanned%TYPE DEFAULT NULL,
        quantity_in                IN supply_workflow_hist.quantity%TYPE DEFAULT NULL,
        id_unit_measure_in         IN supply_workflow_hist.id_unit_measure%TYPE DEFAULT NULL,
        id_context_in              IN supply_workflow_hist.id_context%TYPE DEFAULT NULL,
        flg_context_in             IN supply_workflow_hist.flg_context%TYPE DEFAULT NULL,
        flg_status_in              IN supply_workflow_hist.flg_status%TYPE DEFAULT NULL,
        flg_reason_in              IN supply_workflow_hist.flg_reason%TYPE DEFAULT NULL,
        dt_request_in              IN supply_workflow_hist.dt_request%TYPE DEFAULT NULL,
        dt_returned_in             IN supply_workflow_hist.dt_returned%TYPE DEFAULT NULL,
        notes_in                   IN supply_workflow_hist.notes%TYPE DEFAULT NULL,
        id_prof_cancel_in          IN supply_workflow_hist.id_prof_cancel%TYPE DEFAULT NULL,
        dt_cancel_in               IN supply_workflow_hist.dt_cancel%TYPE DEFAULT NULL,
        notes_cancel_in            IN supply_workflow_hist.notes_cancel%TYPE DEFAULT NULL,
        id_cancel_reason_in        IN supply_workflow_hist.id_cancel_reason%TYPE DEFAULT NULL,
        notes_reject_in            IN supply_workflow_hist.notes_reject%TYPE DEFAULT NULL,
        dt_reject_in               IN supply_workflow_hist.dt_reject%TYPE DEFAULT NULL,
        id_prof_reject_in          IN supply_workflow_hist.id_prof_reject%TYPE DEFAULT NULL,
        dt_supply_workflow_in      IN supply_workflow_hist.dt_supply_workflow%TYPE DEFAULT NULL,
        create_user_in             IN supply_workflow_hist.create_user%TYPE DEFAULT NULL,
        create_time_in             IN supply_workflow_hist.create_time%TYPE DEFAULT NULL,
        create_institution_in      IN supply_workflow_hist.create_institution%TYPE DEFAULT NULL,
        update_user_in             IN supply_workflow_hist.update_user%TYPE DEFAULT NULL,
        update_time_in             IN supply_workflow_hist.update_time%TYPE DEFAULT NULL,
        update_institution_in      IN supply_workflow_hist.update_institution%TYPE DEFAULT NULL,
        id_req_reason_in           IN supply_workflow_hist.id_req_reason%TYPE DEFAULT NULL,
        id_del_reason_in           IN supply_workflow_hist.id_del_reason%TYPE DEFAULT NULL,
        id_supply_set_in           IN supply_workflow_hist.id_supply_set%TYPE DEFAULT NULL,
        id_sup_workflow_parent_in  IN supply_workflow_hist.id_sup_workflow_parent%TYPE DEFAULT NULL,
        asset_number_in            IN supply_workflow_hist.asset_number%TYPE DEFAULT NULL,
        flg_outdated_in            IN supply_workflow_hist.flg_outdated%TYPE DEFAULT NULL,
        total_quantity_in          IN supply_workflow_hist.total_quantity%TYPE DEFAULT NULL,
        flg_cons_type_in           IN supply_workflow_hist.flg_cons_type%TYPE DEFAULT NULL,
        flg_reusable_in            IN supply_workflow_hist.flg_reusable%TYPE DEFAULT NULL,
        flg_editable_in            IN supply_workflow_hist.flg_editable%TYPE DEFAULT NULL,
        total_avail_quantity_in    IN supply_workflow_hist.total_avail_quantity%TYPE DEFAULT NULL,
        cod_table_in               IN supply_workflow_hist.cod_table%TYPE DEFAULT NULL,
        flg_preparing_in           IN supply_workflow_hist.flg_preparing%TYPE DEFAULT NULL,
        flg_countable_in           IN supply_workflow_hist.flg_countable%TYPE DEFAULT NULL,
        id_supply_area_in          IN supply_workflow_hist.id_supply_area%TYPE DEFAULT NULL,
        lot_in                     IN supply_workflow_hist.lot%TYPE DEFAULT NULL,
        dt_expiration_in           IN supply_workflow_hist.dt_expiration%TYPE DEFAULT NULL,
        flg_validation_in          IN supply_workflow_hist.flg_validation%TYPE DEFAULT NULL,
        supply_migration_in        IN supply_workflow_hist.supply_migration%TYPE DEFAULT NULL,
        id_consumption_parent_in   IN supply_workflow_hist.id_consumption_parent%TYPE DEFAULT NULL,
        id_dispense_parent_in      IN supply_workflow_hist.id_dispense_parent%TYPE DEFAULT NULL,
        handle_error_in            IN BOOLEAN := FALSE,
        rows_out                   IN OUT table_varchar
    );

    --Update/insert with columns (without rows_out)
    PROCEDURE upd_ins
    (
        id_supply_workflow_hist_in IN supply_workflow_hist.id_supply_workflow_hist%TYPE,
        id_supply_workflow_in      IN supply_workflow_hist.id_supply_workflow%TYPE DEFAULT NULL,
        id_professional_in         IN supply_workflow_hist.id_professional%TYPE DEFAULT NULL,
        id_episode_in              IN supply_workflow_hist.id_episode%TYPE DEFAULT NULL,
        id_supply_request_in       IN supply_workflow_hist.id_supply_request%TYPE DEFAULT NULL,
        id_supply_in               IN supply_workflow_hist.id_supply%TYPE DEFAULT NULL,
        id_supply_location_in      IN supply_workflow_hist.id_supply_location%TYPE DEFAULT NULL,
        barcode_req_in             IN supply_workflow_hist.barcode_req%TYPE DEFAULT NULL,
        barcode_scanned_in         IN supply_workflow_hist.barcode_scanned%TYPE DEFAULT NULL,
        quantity_in                IN supply_workflow_hist.quantity%TYPE DEFAULT NULL,
        id_unit_measure_in         IN supply_workflow_hist.id_unit_measure%TYPE DEFAULT NULL,
        id_context_in              IN supply_workflow_hist.id_context%TYPE DEFAULT NULL,
        flg_context_in             IN supply_workflow_hist.flg_context%TYPE DEFAULT NULL,
        flg_status_in              IN supply_workflow_hist.flg_status%TYPE DEFAULT NULL,
        flg_reason_in              IN supply_workflow_hist.flg_reason%TYPE DEFAULT NULL,
        dt_request_in              IN supply_workflow_hist.dt_request%TYPE DEFAULT NULL,
        dt_returned_in             IN supply_workflow_hist.dt_returned%TYPE DEFAULT NULL,
        notes_in                   IN supply_workflow_hist.notes%TYPE DEFAULT NULL,
        id_prof_cancel_in          IN supply_workflow_hist.id_prof_cancel%TYPE DEFAULT NULL,
        dt_cancel_in               IN supply_workflow_hist.dt_cancel%TYPE DEFAULT NULL,
        notes_cancel_in            IN supply_workflow_hist.notes_cancel%TYPE DEFAULT NULL,
        id_cancel_reason_in        IN supply_workflow_hist.id_cancel_reason%TYPE DEFAULT NULL,
        notes_reject_in            IN supply_workflow_hist.notes_reject%TYPE DEFAULT NULL,
        dt_reject_in               IN supply_workflow_hist.dt_reject%TYPE DEFAULT NULL,
        id_prof_reject_in          IN supply_workflow_hist.id_prof_reject%TYPE DEFAULT NULL,
        dt_supply_workflow_in      IN supply_workflow_hist.dt_supply_workflow%TYPE DEFAULT NULL,
        create_user_in             IN supply_workflow_hist.create_user%TYPE DEFAULT NULL,
        create_time_in             IN supply_workflow_hist.create_time%TYPE DEFAULT NULL,
        create_institution_in      IN supply_workflow_hist.create_institution%TYPE DEFAULT NULL,
        update_user_in             IN supply_workflow_hist.update_user%TYPE DEFAULT NULL,
        update_time_in             IN supply_workflow_hist.update_time%TYPE DEFAULT NULL,
        update_institution_in      IN supply_workflow_hist.update_institution%TYPE DEFAULT NULL,
        id_req_reason_in           IN supply_workflow_hist.id_req_reason%TYPE DEFAULT NULL,
        id_del_reason_in           IN supply_workflow_hist.id_del_reason%TYPE DEFAULT NULL,
        id_supply_set_in           IN supply_workflow_hist.id_supply_set%TYPE DEFAULT NULL,
        id_sup_workflow_parent_in  IN supply_workflow_hist.id_sup_workflow_parent%TYPE DEFAULT NULL,
        asset_number_in            IN supply_workflow_hist.asset_number%TYPE DEFAULT NULL,
        flg_outdated_in            IN supply_workflow_hist.flg_outdated%TYPE DEFAULT NULL,
        total_quantity_in          IN supply_workflow_hist.total_quantity%TYPE DEFAULT NULL,
        flg_cons_type_in           IN supply_workflow_hist.flg_cons_type%TYPE DEFAULT NULL,
        flg_reusable_in            IN supply_workflow_hist.flg_reusable%TYPE DEFAULT NULL,
        flg_editable_in            IN supply_workflow_hist.flg_editable%TYPE DEFAULT NULL,
        total_avail_quantity_in    IN supply_workflow_hist.total_avail_quantity%TYPE DEFAULT NULL,
        cod_table_in               IN supply_workflow_hist.cod_table%TYPE DEFAULT NULL,
        flg_preparing_in           IN supply_workflow_hist.flg_preparing%TYPE DEFAULT NULL,
        flg_countable_in           IN supply_workflow_hist.flg_countable%TYPE DEFAULT NULL,
        id_supply_area_in          IN supply_workflow_hist.id_supply_area%TYPE DEFAULT NULL,
        lot_in                     IN supply_workflow_hist.lot%TYPE DEFAULT NULL,
        dt_expiration_in           IN supply_workflow_hist.dt_expiration%TYPE DEFAULT NULL,
        flg_validation_in          IN supply_workflow_hist.flg_validation%TYPE DEFAULT NULL,
        supply_migration_in        IN supply_workflow_hist.supply_migration%TYPE DEFAULT NULL,
        id_consumption_parent_in   IN supply_workflow_hist.id_consumption_parent%TYPE DEFAULT NULL,
        id_dispense_parent_in      IN supply_workflow_hist.id_dispense_parent%TYPE DEFAULT NULL,
        handle_error_in            IN BOOLEAN := FALSE
    );

    --Update record (with rows_out)
    PROCEDURE upd
    (
        rec_in          IN supply_workflow_hist%ROWTYPE,
        handle_error_in IN BOOLEAN := FALSE,
        rows_out        IN OUT table_varchar
    );

    --Update record (without rows_out)
    PROCEDURE upd
    (
        rec_in          IN supply_workflow_hist%ROWTYPE,
        handle_error_in IN BOOLEAN := FALSE
    );

    --Update collection (with rows_out)
    PROCEDURE upd
    (
        col_in            IN supply_workflow_hist_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := FALSE,
        rows_out          IN OUT table_varchar
    );

    --Update collection (without rows_out)
    PROCEDURE upd
    (
        col_in            IN supply_workflow_hist_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := FALSE
    );

    -- Use Native Dynamic SQL increment a single NUMBER column
    -- for all rows specified by the dynamic WHERE clause
    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := FALSE,
        rows_out           OUT table_varchar
    );

    --increment column value
    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := FALSE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_supply_workflow_hist_in IN supply_workflow_hist.id_supply_workflow_hist%TYPE,
        handle_error_in            IN BOOLEAN := FALSE,
        rows_out                   OUT table_varchar
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_supply_workflow_hist_in IN supply_workflow_hist.id_supply_workflow_hist%TYPE,
        handle_error_in            IN BOOLEAN := FALSE
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := FALSE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := FALSE
    );

    -- Delete all rows for this SPFH_CRS_FK foreign key value
    PROCEDURE del_spfh_crs_fk
    (
        id_cancel_reason_in IN supply_workflow_hist.id_cancel_reason%TYPE,
        handle_error_in     IN BOOLEAN := FALSE,
        rows_out            OUT table_varchar
    );

    -- Delete all rows for this SPFH_EPI_FK foreign key value
    PROCEDURE del_spfh_epi_fk
    (
        id_episode_in   IN supply_workflow_hist.id_episode%TYPE,
        handle_error_in IN BOOLEAN := FALSE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for this SPFH_PCA_FK foreign key value
    PROCEDURE del_spfh_pca_fk
    (
        id_prof_cancel_in IN supply_workflow_hist.id_prof_cancel%TYPE,
        handle_error_in   IN BOOLEAN := FALSE,
        rows_out          OUT table_varchar
    );

    -- Delete all rows for this SPFH_PRF_FK foreign key value
    PROCEDURE del_spfh_prf_fk
    (
        id_professional_in IN supply_workflow_hist.id_professional%TYPE,
        handle_error_in    IN BOOLEAN := FALSE,
        rows_out           OUT table_varchar
    );

    -- Delete all rows for this SPFH_PRJ_FK foreign key value
    PROCEDURE del_spfh_prj_fk
    (
        id_prof_reject_in IN supply_workflow_hist.id_prof_reject%TYPE,
        handle_error_in   IN BOOLEAN := FALSE,
        rows_out          OUT table_varchar
    );

    -- Delete all rows for this SPFH_SLO_FK foreign key value
    PROCEDURE del_spfh_slo_fk
    (
        id_supply_location_in IN supply_workflow_hist.id_supply_location%TYPE,
        handle_error_in       IN BOOLEAN := FALSE,
        rows_out              OUT table_varchar
    );

    -- Delete all rows for this SPFH_SPY_FK foreign key value
    PROCEDURE del_spfh_spy_fk
    (
        id_supply_in    IN supply_workflow_hist.id_supply%TYPE,
        handle_error_in IN BOOLEAN := FALSE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for this SPFH_SRQ_FK foreign key value
    PROCEDURE del_spfh_srq_fk
    (
        id_supply_request_in IN supply_workflow_hist.id_supply_request%TYPE,
        handle_error_in      IN BOOLEAN := FALSE,
        rows_out             OUT table_varchar
    );

    -- Delete all rows for this SPFH_UME_FK foreign key value
    PROCEDURE del_spfh_ume_fk
    (
        id_unit_measure_in IN supply_workflow_hist.id_unit_measure%TYPE,
        handle_error_in    IN BOOLEAN := FALSE,
        rows_out           OUT table_varchar
    );

    -- Delete all rows for this SWH_CP_FK foreign key value
    PROCEDURE del_swh_cp_fk
    (
        id_consumption_parent_in IN supply_workflow_hist.id_consumption_parent%TYPE,
        handle_error_in          IN BOOLEAN := FALSE,
        rows_out                 OUT table_varchar
    );

    -- Delete all rows for this SWWH_SPS_FK foreign key value
    PROCEDURE del_swwh_sps_fk
    (
        id_supply_set_in IN supply_workflow_hist.id_supply_set%TYPE,
        handle_error_in  IN BOOLEAN := FALSE,
        rows_out         OUT table_varchar
    );

    -- Delete all rows for this SWWH_SRND_FK foreign key value
    PROCEDURE del_swwh_srnd_fk
    (
        id_del_reason_in IN supply_workflow_hist.id_del_reason%TYPE,
        handle_error_in  IN BOOLEAN := FALSE,
        rows_out         OUT table_varchar
    );

    -- Delete all rows for this SWWH_SRNR_FK foreign key value
    PROCEDURE del_swwh_srnr_fk
    (
        id_req_reason_in IN supply_workflow_hist.id_req_reason%TYPE,
        handle_error_in  IN BOOLEAN := FALSE,
        rows_out         OUT table_varchar
    );

    -- Delete all rows for this SPFH_CRS_FK foreign key value
    PROCEDURE del_spfh_crs_fk
    (
        id_cancel_reason_in IN supply_workflow_hist.id_cancel_reason%TYPE,
        handle_error_in     IN BOOLEAN := FALSE
    );

    -- Delete all rows for this SPFH_EPI_FK foreign key value
    PROCEDURE del_spfh_epi_fk
    (
        id_episode_in   IN supply_workflow_hist.id_episode%TYPE,
        handle_error_in IN BOOLEAN := FALSE
    );

    -- Delete all rows for this SPFH_PCA_FK foreign key value
    PROCEDURE del_spfh_pca_fk
    (
        id_prof_cancel_in IN supply_workflow_hist.id_prof_cancel%TYPE,
        handle_error_in   IN BOOLEAN := FALSE
    );

    -- Delete all rows for this SPFH_PRF_FK foreign key value
    PROCEDURE del_spfh_prf_fk
    (
        id_professional_in IN supply_workflow_hist.id_professional%TYPE,
        handle_error_in    IN BOOLEAN := FALSE
    );

    -- Delete all rows for this SPFH_PRJ_FK foreign key value
    PROCEDURE del_spfh_prj_fk
    (
        id_prof_reject_in IN supply_workflow_hist.id_prof_reject%TYPE,
        handle_error_in   IN BOOLEAN := FALSE
    );

    -- Delete all rows for this SPFH_SLO_FK foreign key value
    PROCEDURE del_spfh_slo_fk
    (
        id_supply_location_in IN supply_workflow_hist.id_supply_location%TYPE,
        handle_error_in       IN BOOLEAN := FALSE
    );

    -- Delete all rows for this SPFH_SPY_FK foreign key value
    PROCEDURE del_spfh_spy_fk
    (
        id_supply_in    IN supply_workflow_hist.id_supply%TYPE,
        handle_error_in IN BOOLEAN := FALSE
    );

    -- Delete all rows for this SPFH_SRQ_FK foreign key value
    PROCEDURE del_spfh_srq_fk
    (
        id_supply_request_in IN supply_workflow_hist.id_supply_request%TYPE,
        handle_error_in      IN BOOLEAN := FALSE
    );

    -- Delete all rows for this SPFH_UME_FK foreign key value
    PROCEDURE del_spfh_ume_fk
    (
        id_unit_measure_in IN supply_workflow_hist.id_unit_measure%TYPE,
        handle_error_in    IN BOOLEAN := FALSE
    );

    -- Delete all rows for this SWH_CP_FK foreign key value
    PROCEDURE del_swh_cp_fk
    (
        id_consumption_parent_in IN supply_workflow_hist.id_consumption_parent%TYPE,
        handle_error_in          IN BOOLEAN := FALSE
    );

    -- Delete all rows for this SWWH_SPS_FK foreign key value
    PROCEDURE del_swwh_sps_fk
    (
        id_supply_set_in IN supply_workflow_hist.id_supply_set%TYPE,
        handle_error_in  IN BOOLEAN := FALSE
    );

    -- Delete all rows for this SWWH_SRND_FK foreign key value
    PROCEDURE del_swwh_srnd_fk
    (
        id_del_reason_in IN supply_workflow_hist.id_del_reason%TYPE,
        handle_error_in  IN BOOLEAN := FALSE
    );

    -- Delete all rows for this SWWH_SRNR_FK foreign key value
    PROCEDURE del_swwh_srnr_fk
    (
        id_req_reason_in IN supply_workflow_hist.id_req_reason%TYPE,
        handle_error_in  IN BOOLEAN := FALSE
    );

    -- Initialize a record with default values for columns in the table (prc)
    PROCEDURE initrec(supply_workflow_hist_inout IN OUT supply_workflow_hist%ROWTYPE);

    -- Initialize a record with default values for columns in the table (fnc)
    FUNCTION initrec RETURN supply_workflow_hist%ROWTYPE;

    -- Get data rowid
    FUNCTION get_data_rowid(rows_in IN table_varchar) RETURN supply_workflow_hist_tc;

    -- Get data rowid pragma autonomous transaccion
    FUNCTION get_data_rowid_pat(rows_in IN table_varchar) RETURN supply_workflow_hist_tc;

END ts_supply_workflow_hist;
/
