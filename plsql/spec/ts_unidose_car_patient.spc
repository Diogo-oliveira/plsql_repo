/*-- Last Change Revision: $Rev: 2029402 $*/
/*-- Last Change by: $Author: mario.fernandes $*/
/*-- Date of last change: $Date: 2022-08-02 18:51:28 +0100 (ter, 02 ago 2022) $*/

CREATE OR REPLACE PACKAGE ts_unidose_car_patient
/*
| Generated by or retrieved from QCGU - DO NOT MODIFY!
| QCGU - "Get it right, do it fast" - www.ToadWorld.com
| QCGU Universal ID: {1BD37A66-EA60-4927-9A64-F6DD89237236}
| Created On: Novembro 21, 2008 18:26:43
| Created By: ALERT
*/
 IS

    -- Collection of %ROWTYPE records based on "UNIDOSE_CAR_PATIENT"
    TYPE unidose_car_patient_tc IS TABLE OF unidose_car_patient%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE unidose_car_patient_ntt IS TABLE OF unidose_car_patient%ROWTYPE;
    TYPE unidose_car_patient_vat IS VARRAY(100) OF unidose_car_patient%ROWTYPE;

    -- Same type structure, with a static name.
    TYPE aat IS TABLE OF unidose_car_patient%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE ntt IS TABLE OF unidose_car_patient%ROWTYPE;
    TYPE vat IS VARRAY(100) OF unidose_car_patient%ROWTYPE;

    -- Column Collection based on column "ID_UNIDOSE_CAR_PATIENT"
    TYPE id_unidose_car_patient_cc IS TABLE OF unidose_car_patient.id_unidose_car_patient%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_UNIDOSE_CAR"
    TYPE id_unidose_car_cc IS TABLE OF unidose_car_patient.id_unidose_car%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_INSTITUTION"
    TYPE id_institution_cc IS TABLE OF unidose_car_patient.id_institution%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_PATIENT"
    TYPE id_patient_cc IS TABLE OF unidose_car_patient.id_patient%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_CONTAINER"
    TYPE id_container_cc IS TABLE OF unidose_car_patient.id_container%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_UNIDOSE_CAR_ROUTE"
    TYPE id_unidose_car_route_cc IS TABLE OF unidose_car_patient.id_unidose_car_route%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "FLG_AVAILABLE"
    TYPE flg_available_cc IS TABLE OF unidose_car_patient.flg_available%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ADW_DATE"
    TYPE adw_date_cc IS TABLE OF unidose_car_patient.adw_date%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "BAR_CODE"
    TYPE bar_code_cc IS TABLE OF unidose_car_patient.bar_code%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_EPISODE"
    TYPE id_episode_cc IS TABLE OF unidose_car_patient.id_episode%TYPE INDEX BY BINARY_INTEGER;

    -- Insert one row, providing primary key if present
    PROCEDURE ins
    (
        id_unidose_car_patient_in IN unidose_car_patient.id_unidose_car_patient%TYPE,
        id_unidose_car_in         IN unidose_car_patient.id_unidose_car%TYPE DEFAULT NULL,
        id_institution_in         IN unidose_car_patient.id_institution%TYPE DEFAULT NULL,
        id_patient_in             IN unidose_car_patient.id_patient%TYPE DEFAULT NULL,
        id_container_in           IN unidose_car_patient.id_container%TYPE DEFAULT NULL,
        id_unidose_car_route_in   IN unidose_car_patient.id_unidose_car_route%TYPE DEFAULT NULL,
        flg_available_in          IN unidose_car_patient.flg_available%TYPE DEFAULT NULL,
        adw_date_in               IN unidose_car_patient.adw_date%TYPE DEFAULT NULL,
        bar_code_in               IN unidose_car_patient.bar_code%TYPE DEFAULT NULL,
        id_episode_in             IN unidose_car_patient.id_episode%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_unidose_car_patient_in IN unidose_car_patient.id_unidose_car_patient%TYPE,
        id_unidose_car_in         IN unidose_car_patient.id_unidose_car%TYPE DEFAULT NULL,
        id_institution_in         IN unidose_car_patient.id_institution%TYPE DEFAULT NULL,
        id_patient_in             IN unidose_car_patient.id_patient%TYPE DEFAULT NULL,
        id_container_in           IN unidose_car_patient.id_container%TYPE DEFAULT NULL,
        id_unidose_car_route_in   IN unidose_car_patient.id_unidose_car_route%TYPE DEFAULT NULL,
        flg_available_in          IN unidose_car_patient.flg_available%TYPE DEFAULT NULL,
        adw_date_in               IN unidose_car_patient.adw_date%TYPE DEFAULT NULL,
        bar_code_in               IN unidose_car_patient.bar_code%TYPE DEFAULT NULL,
        id_episode_in             IN unidose_car_patient.id_episode%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a row based on a record.
    -- Specify whether or not a primary key value should be generated.
    PROCEDURE ins
    (
        rec_in          IN unidose_car_patient%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rec_in          IN unidose_car_patient%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a collection of rows using FORALL; all primary key values
    -- must have already been generated, or are handled in triggers.
    PROCEDURE ins
    (
        rows_in         IN unidose_car_patient_tc,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rows_in         IN unidose_car_patient_tc,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Return next primary key value using the named sequence.
    FUNCTION next_key(sequence_in IN VARCHAR2 := NULL) RETURN unidose_car_patient.id_unidose_car_patient%TYPE;

    -- Insert one row, generating hidden primary key using a sequence
    PROCEDURE ins
    (
        id_unidose_car_in       IN unidose_car_patient.id_unidose_car%TYPE DEFAULT NULL,
        id_institution_in       IN unidose_car_patient.id_institution%TYPE DEFAULT NULL,
        id_patient_in           IN unidose_car_patient.id_patient%TYPE DEFAULT NULL,
        id_container_in         IN unidose_car_patient.id_container%TYPE DEFAULT NULL,
        id_unidose_car_route_in IN unidose_car_patient.id_unidose_car_route%TYPE DEFAULT NULL,
        flg_available_in        IN unidose_car_patient.flg_available%TYPE DEFAULT NULL,
        adw_date_in             IN unidose_car_patient.adw_date%TYPE DEFAULT NULL,
        bar_code_in             IN unidose_car_patient.bar_code%TYPE DEFAULT NULL,
        id_episode_in           IN unidose_car_patient.id_episode%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_unidose_car_in       IN unidose_car_patient.id_unidose_car%TYPE DEFAULT NULL,
        id_institution_in       IN unidose_car_patient.id_institution%TYPE DEFAULT NULL,
        id_patient_in           IN unidose_car_patient.id_patient%TYPE DEFAULT NULL,
        id_container_in         IN unidose_car_patient.id_container%TYPE DEFAULT NULL,
        id_unidose_car_route_in IN unidose_car_patient.id_unidose_car_route%TYPE DEFAULT NULL,
        flg_available_in        IN unidose_car_patient.flg_available%TYPE DEFAULT NULL,
        adw_date_in             IN unidose_car_patient.adw_date%TYPE DEFAULT NULL,
        bar_code_in             IN unidose_car_patient.bar_code%TYPE DEFAULT NULL,
        id_episode_in           IN unidose_car_patient.id_episode%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert one row, returning primary key generated by sequence
    PROCEDURE ins
    (
        id_unidose_car_in          IN unidose_car_patient.id_unidose_car%TYPE DEFAULT NULL,
        id_institution_in          IN unidose_car_patient.id_institution%TYPE DEFAULT NULL,
        id_patient_in              IN unidose_car_patient.id_patient%TYPE DEFAULT NULL,
        id_container_in            IN unidose_car_patient.id_container%TYPE DEFAULT NULL,
        id_unidose_car_route_in    IN unidose_car_patient.id_unidose_car_route%TYPE DEFAULT NULL,
        flg_available_in           IN unidose_car_patient.flg_available%TYPE DEFAULT NULL,
        adw_date_in                IN unidose_car_patient.adw_date%TYPE DEFAULT NULL,
        bar_code_in                IN unidose_car_patient.bar_code%TYPE DEFAULT NULL,
        id_episode_in              IN unidose_car_patient.id_episode%TYPE DEFAULT NULL,
        id_unidose_car_patient_out IN OUT unidose_car_patient.id_unidose_car_patient%TYPE
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_unidose_car_in          IN unidose_car_patient.id_unidose_car%TYPE DEFAULT NULL,
        id_institution_in          IN unidose_car_patient.id_institution%TYPE DEFAULT NULL,
        id_patient_in              IN unidose_car_patient.id_patient%TYPE DEFAULT NULL,
        id_container_in            IN unidose_car_patient.id_container%TYPE DEFAULT NULL,
        id_unidose_car_route_in    IN unidose_car_patient.id_unidose_car_route%TYPE DEFAULT NULL,
        flg_available_in           IN unidose_car_patient.flg_available%TYPE DEFAULT NULL,
        adw_date_in                IN unidose_car_patient.adw_date%TYPE DEFAULT NULL,
        bar_code_in                IN unidose_car_patient.bar_code%TYPE DEFAULT NULL,
        id_episode_in              IN unidose_car_patient.id_episode%TYPE DEFAULT NULL,
        id_unidose_car_patient_out IN OUT unidose_car_patient.id_unidose_car_patient%TYPE
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert one row with function, return generated primary key
    FUNCTION ins
    (
        id_unidose_car_in       IN unidose_car_patient.id_unidose_car%TYPE DEFAULT NULL,
        id_institution_in       IN unidose_car_patient.id_institution%TYPE DEFAULT NULL,
        id_patient_in           IN unidose_car_patient.id_patient%TYPE DEFAULT NULL,
        id_container_in         IN unidose_car_patient.id_container%TYPE DEFAULT NULL,
        id_unidose_car_route_in IN unidose_car_patient.id_unidose_car_route%TYPE DEFAULT NULL,
        flg_available_in        IN unidose_car_patient.flg_available%TYPE DEFAULT NULL,
        adw_date_in             IN unidose_car_patient.adw_date%TYPE DEFAULT NULL,
        bar_code_in             IN unidose_car_patient.bar_code%TYPE DEFAULT NULL,
        id_episode_in           IN unidose_car_patient.id_episode%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) RETURN unidose_car_patient.id_unidose_car_patient%TYPE;

    FUNCTION ins
    (
        id_unidose_car_in       IN unidose_car_patient.id_unidose_car%TYPE DEFAULT NULL,
        id_institution_in       IN unidose_car_patient.id_institution%TYPE DEFAULT NULL,
        id_patient_in           IN unidose_car_patient.id_patient%TYPE DEFAULT NULL,
        id_container_in         IN unidose_car_patient.id_container%TYPE DEFAULT NULL,
        id_unidose_car_route_in IN unidose_car_patient.id_unidose_car_route%TYPE DEFAULT NULL,
        flg_available_in        IN unidose_car_patient.flg_available%TYPE DEFAULT NULL,
        adw_date_in             IN unidose_car_patient.adw_date%TYPE DEFAULT NULL,
        bar_code_in             IN unidose_car_patient.bar_code%TYPE DEFAULT NULL,
        id_episode_in           IN unidose_car_patient.id_episode%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    ) RETURN unidose_car_patient.id_unidose_car_patient%TYPE;

    -- Update any/all columns by primary key. If you pass NULL, then
    -- the current column value is set to itself. If you need a more
    -- selected UPDATE then use one of the onecol procedures below.

    PROCEDURE upd
    (
        id_unidose_car_patient_in IN unidose_car_patient.id_unidose_car_patient%TYPE,
        id_unidose_car_in         IN unidose_car_patient.id_unidose_car%TYPE DEFAULT NULL,
        id_unidose_car_nin        IN BOOLEAN := TRUE,
        id_institution_in         IN unidose_car_patient.id_institution%TYPE DEFAULT NULL,
        id_institution_nin        IN BOOLEAN := TRUE,
        id_patient_in             IN unidose_car_patient.id_patient%TYPE DEFAULT NULL,
        id_patient_nin            IN BOOLEAN := TRUE,
        id_container_in           IN unidose_car_patient.id_container%TYPE DEFAULT NULL,
        id_container_nin          IN BOOLEAN := TRUE,
        id_unidose_car_route_in   IN unidose_car_patient.id_unidose_car_route%TYPE DEFAULT NULL,
        id_unidose_car_route_nin  IN BOOLEAN := TRUE,
        flg_available_in          IN unidose_car_patient.flg_available%TYPE DEFAULT NULL,
        flg_available_nin         IN BOOLEAN := TRUE,
        adw_date_in               IN unidose_car_patient.adw_date%TYPE DEFAULT NULL,
        adw_date_nin              IN BOOLEAN := TRUE,
        bar_code_in               IN unidose_car_patient.bar_code%TYPE DEFAULT NULL,
        bar_code_nin              IN BOOLEAN := TRUE,
        id_episode_in             IN unidose_car_patient.id_episode%TYPE DEFAULT NULL,
        id_episode_nin            IN BOOLEAN := TRUE,
        handle_error_in           IN BOOLEAN := TRUE,
        rows_out                  IN OUT table_varchar
    );

    PROCEDURE upd
    (
        id_unidose_car_patient_in IN unidose_car_patient.id_unidose_car_patient%TYPE,
        id_unidose_car_in         IN unidose_car_patient.id_unidose_car%TYPE DEFAULT NULL,
        id_unidose_car_nin        IN BOOLEAN := TRUE,
        id_institution_in         IN unidose_car_patient.id_institution%TYPE DEFAULT NULL,
        id_institution_nin        IN BOOLEAN := TRUE,
        id_patient_in             IN unidose_car_patient.id_patient%TYPE DEFAULT NULL,
        id_patient_nin            IN BOOLEAN := TRUE,
        id_container_in           IN unidose_car_patient.id_container%TYPE DEFAULT NULL,
        id_container_nin          IN BOOLEAN := TRUE,
        id_unidose_car_route_in   IN unidose_car_patient.id_unidose_car_route%TYPE DEFAULT NULL,
        id_unidose_car_route_nin  IN BOOLEAN := TRUE,
        flg_available_in          IN unidose_car_patient.flg_available%TYPE DEFAULT NULL,
        flg_available_nin         IN BOOLEAN := TRUE,
        adw_date_in               IN unidose_car_patient.adw_date%TYPE DEFAULT NULL,
        adw_date_nin              IN BOOLEAN := TRUE,
        bar_code_in               IN unidose_car_patient.bar_code%TYPE DEFAULT NULL,
        bar_code_nin              IN BOOLEAN := TRUE,
        id_episode_in             IN unidose_car_patient.id_episode%TYPE DEFAULT NULL,
        id_episode_nin            IN BOOLEAN := TRUE,
        handle_error_in           IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        id_unidose_car_in        IN unidose_car_patient.id_unidose_car%TYPE DEFAULT NULL,
        id_unidose_car_nin       IN BOOLEAN := TRUE,
        id_institution_in        IN unidose_car_patient.id_institution%TYPE DEFAULT NULL,
        id_institution_nin       IN BOOLEAN := TRUE,
        id_patient_in            IN unidose_car_patient.id_patient%TYPE DEFAULT NULL,
        id_patient_nin           IN BOOLEAN := TRUE,
        id_container_in          IN unidose_car_patient.id_container%TYPE DEFAULT NULL,
        id_container_nin         IN BOOLEAN := TRUE,
        id_unidose_car_route_in  IN unidose_car_patient.id_unidose_car_route%TYPE DEFAULT NULL,
        id_unidose_car_route_nin IN BOOLEAN := TRUE,
        flg_available_in         IN unidose_car_patient.flg_available%TYPE DEFAULT NULL,
        flg_available_nin        IN BOOLEAN := TRUE,
        adw_date_in              IN unidose_car_patient.adw_date%TYPE DEFAULT NULL,
        adw_date_nin             IN BOOLEAN := TRUE,
        bar_code_in              IN unidose_car_patient.bar_code%TYPE DEFAULT NULL,
        bar_code_nin             IN BOOLEAN := TRUE,
        id_episode_in            IN unidose_car_patient.id_episode%TYPE DEFAULT NULL,
        id_episode_nin           IN BOOLEAN := TRUE,
        where_in                 VARCHAR2 DEFAULT NULL,
        handle_error_in          IN BOOLEAN := TRUE,
        rows_out                 IN OUT table_varchar
    );

    PROCEDURE upd
    (
        id_unidose_car_in        IN unidose_car_patient.id_unidose_car%TYPE DEFAULT NULL,
        id_unidose_car_nin       IN BOOLEAN := TRUE,
        id_institution_in        IN unidose_car_patient.id_institution%TYPE DEFAULT NULL,
        id_institution_nin       IN BOOLEAN := TRUE,
        id_patient_in            IN unidose_car_patient.id_patient%TYPE DEFAULT NULL,
        id_patient_nin           IN BOOLEAN := TRUE,
        id_container_in          IN unidose_car_patient.id_container%TYPE DEFAULT NULL,
        id_container_nin         IN BOOLEAN := TRUE,
        id_unidose_car_route_in  IN unidose_car_patient.id_unidose_car_route%TYPE DEFAULT NULL,
        id_unidose_car_route_nin IN BOOLEAN := TRUE,
        flg_available_in         IN unidose_car_patient.flg_available%TYPE DEFAULT NULL,
        flg_available_nin        IN BOOLEAN := TRUE,
        adw_date_in              IN unidose_car_patient.adw_date%TYPE DEFAULT NULL,
        adw_date_nin             IN BOOLEAN := TRUE,
        bar_code_in              IN unidose_car_patient.bar_code%TYPE DEFAULT NULL,
        bar_code_nin             IN BOOLEAN := TRUE,
        id_episode_in            IN unidose_car_patient.id_episode%TYPE DEFAULT NULL,
        id_episode_nin           IN BOOLEAN := TRUE,
        where_in                 VARCHAR2 DEFAULT NULL,
        handle_error_in          IN BOOLEAN := TRUE
    );

    PROCEDURE upd_ins
    (
        id_unidose_car_patient_in IN unidose_car_patient.id_unidose_car_patient%TYPE,
        id_unidose_car_in         IN unidose_car_patient.id_unidose_car%TYPE DEFAULT NULL,
        id_institution_in         IN unidose_car_patient.id_institution%TYPE DEFAULT NULL,
        id_patient_in             IN unidose_car_patient.id_patient%TYPE DEFAULT NULL,
        id_container_in           IN unidose_car_patient.id_container%TYPE DEFAULT NULL,
        id_unidose_car_route_in   IN unidose_car_patient.id_unidose_car_route%TYPE DEFAULT NULL,
        flg_available_in          IN unidose_car_patient.flg_available%TYPE DEFAULT NULL,
        adw_date_in               IN unidose_car_patient.adw_date%TYPE DEFAULT NULL,
        bar_code_in               IN unidose_car_patient.bar_code%TYPE DEFAULT NULL,
        id_episode_in             IN unidose_car_patient.id_episode%TYPE DEFAULT NULL,
        handle_error_in           IN BOOLEAN := TRUE,
        rows_out                  OUT table_varchar
    );

    PROCEDURE upd_ins
    (
        id_unidose_car_patient_in IN unidose_car_patient.id_unidose_car_patient%TYPE,
        id_unidose_car_in         IN unidose_car_patient.id_unidose_car%TYPE DEFAULT NULL,
        id_institution_in         IN unidose_car_patient.id_institution%TYPE DEFAULT NULL,
        id_patient_in             IN unidose_car_patient.id_patient%TYPE DEFAULT NULL,
        id_container_in           IN unidose_car_patient.id_container%TYPE DEFAULT NULL,
        id_unidose_car_route_in   IN unidose_car_patient.id_unidose_car_route%TYPE DEFAULT NULL,
        flg_available_in          IN unidose_car_patient.flg_available%TYPE DEFAULT NULL,
        adw_date_in               IN unidose_car_patient.adw_date%TYPE DEFAULT NULL,
        bar_code_in               IN unidose_car_patient.bar_code%TYPE DEFAULT NULL,
        id_episode_in             IN unidose_car_patient.id_episode%TYPE DEFAULT NULL,
        handle_error_in           IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        rec_in          IN unidose_car_patient%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        IN OUT table_varchar
    );

    PROCEDURE upd
    (
        rec_in          IN unidose_car_patient%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        col_in            IN unidose_car_patient_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          IN OUT table_varchar
    );

    PROCEDURE upd
    (
        col_in            IN unidose_car_patient_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE
    );

    -- Use Native Dynamic SQL increment a single NUMBER column
    -- for all rows specified by the dynamic WHERE clause
    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_unidose_car_patient_in IN unidose_car_patient.id_unidose_car_patient%TYPE,
        handle_error_in           IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_unidose_car_patient_in IN unidose_car_patient.id_unidose_car_patient%TYPE,
        handle_error_in           IN BOOLEAN := TRUE,
        rows_out                  OUT table_varchar
    );

    -- Delete all rows for primary key column ID_UNIDOSE_CAR_PATIENT
    PROCEDURE del_id_unidose_car_patient
    (
        id_unidose_car_patient_in IN unidose_car_patient.id_unidose_car_patient%TYPE,
        handle_error_in           IN BOOLEAN := TRUE
    );

    -- Delete all rows for primary key column ID_UNIDOSE_CAR_PATIENT
    PROCEDURE del_id_unidose_car_patient
    (
        id_unidose_car_patient_in IN unidose_car_patient.id_unidose_car_patient%TYPE,
        handle_error_in           IN BOOLEAN := TRUE,
        rows_out                  OUT table_varchar
    );

    -- Delete all rows for this FK_UNIDOSE_CAR_PATIENT_C foreign key value
    PROCEDURE del_fk_unidose_car_patient_c
    (
        id_unidose_car_in IN unidose_car_patient.id_unidose_car%TYPE,
        handle_error_in   IN BOOLEAN := TRUE
    );

    -- Delete all rows for this FK_UNIDOSE_CAR_PATIENT_C foreign key value
    PROCEDURE del_fk_unidose_car_patient_c
    (
        id_unidose_car_in IN unidose_car_patient.id_unidose_car%TYPE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          OUT table_varchar
    );

    -- Delete all rows for this FK_UNIDOSE_CAR_PATIENT_I foreign key value
    PROCEDURE del_fk_unidose_car_patient_i
    (
        id_institution_in IN unidose_car_patient.id_institution%TYPE,
        handle_error_in   IN BOOLEAN := TRUE
    );

    -- Delete all rows for this FK_UNIDOSE_CAR_PATIENT_I foreign key value
    PROCEDURE del_fk_unidose_car_patient_i
    (
        id_institution_in IN unidose_car_patient.id_institution%TYPE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          OUT table_varchar
    );

    -- Delete all rows for this FK_UNIDOSE_CAR_PATIENT_P foreign key value
    PROCEDURE del_fk_unidose_car_patient_p
    (
        id_patient_in   IN unidose_car_patient.id_patient%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this FK_UNIDOSE_CAR_PATIENT_P foreign key value
    PROCEDURE del_fk_unidose_car_patient_p
    (
        id_patient_in   IN unidose_car_patient.id_patient%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for this FK_UNIDOSE_CAR_PATIENT_R foreign key value
    PROCEDURE del_fk_unidose_car_patient_r
    (
        id_unidose_car_route_in IN unidose_car_patient.id_unidose_car_route%TYPE,
        handle_error_in         IN BOOLEAN := TRUE
    );

    -- Delete all rows for this FK_UNIDOSE_CAR_PATIENT_R foreign key value
    PROCEDURE del_fk_unidose_car_patient_r
    (
        id_unidose_car_route_in IN unidose_car_patient.id_unidose_car_route%TYPE,
        handle_error_in         IN BOOLEAN := TRUE,
        rows_out                OUT table_varchar
    );

    -- Delete all rows for this UCT_EPIS_FK foreign key value
    PROCEDURE del_uct_epis_fk
    (
        id_episode_in   IN unidose_car_patient.id_episode%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this UCT_EPIS_FK foreign key value
    PROCEDURE del_uct_epis_fk
    (
        id_episode_in   IN unidose_car_patient.id_episode%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Initialize a record with default values for columns in the table.
    PROCEDURE initrec(unidose_car_patient_inout IN OUT unidose_car_patient%ROWTYPE);

    FUNCTION initrec RETURN unidose_car_patient%ROWTYPE;

    FUNCTION get_data_rowid(rows_in IN table_varchar) RETURN unidose_car_patient_tc;

    FUNCTION get_data_rowid_pat(rows_in IN table_varchar) RETURN unidose_car_patient_tc;

END ts_unidose_car_patient;
/
