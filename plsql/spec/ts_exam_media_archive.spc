/*-- Last Change Revision: $Rev: 1636149 $*/
/*-- Last Change by: $Author: ana.matos $*/
/*-- Date of last change: $Date: 2014-09-19 15:25:19 +0100 (sex, 19 set 2014) $*/

CREATE OR REPLACE PACKAGE ts_exam_media_archive
/*
| Generated by or retrieved from QCGU - DO NOT MODIFY!
| QCGU - "Get it right, do it fast" - www.ToadWorld.com
| QCGU Universal ID: {1BD37A66-EA60-4927-9A64-F6DD89237236}
| Created On: Setembro 17, 2014 10:14:59
| Created By: ALERT
*/
 IS

    -- Collection of %ROWTYPE records based on "EXAM_MEDIA_ARCHIVE"
    TYPE exam_media_archive_tc IS TABLE OF exam_media_archive%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE exam_media_archive_ntt IS TABLE OF exam_media_archive%ROWTYPE;
    TYPE exam_media_archive_vat IS VARRAY(100) OF exam_media_archive%ROWTYPE;

    -- Same type structure, with a static name.
    TYPE aat IS TABLE OF exam_media_archive%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE ntt IS TABLE OF exam_media_archive%ROWTYPE;
    TYPE vat IS VARRAY(100) OF exam_media_archive%ROWTYPE;

    -- Column Collection based on column "ID_EXAM_REQ_DET"
    TYPE id_exam_req_det_cc IS TABLE OF exam_media_archive.id_exam_req_det%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_EXAM_RESULT"
    TYPE id_exam_result_cc IS TABLE OF exam_media_archive.id_exam_result%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_DOC_EXTERNAL"
    TYPE id_doc_external_cc IS TABLE OF exam_media_archive.id_doc_external%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "FLG_TYPE"
    TYPE flg_type_cc IS TABLE OF exam_media_archive.flg_type%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_USER"
    TYPE create_user_cc IS TABLE OF exam_media_archive.create_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_TIME"
    TYPE create_time_cc IS TABLE OF exam_media_archive.create_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_INSTITUTION"
    TYPE create_institution_cc IS TABLE OF exam_media_archive.create_institution%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_USER"
    TYPE update_user_cc IS TABLE OF exam_media_archive.update_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_TIME"
    TYPE update_time_cc IS TABLE OF exam_media_archive.update_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_INSTITUTION"
    TYPE update_institution_cc IS TABLE OF exam_media_archive.update_institution%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_EXAM_MEDIA_ARCHIVE"
    TYPE id_exam_media_archive_cc IS TABLE OF exam_media_archive.id_exam_media_archive%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "FLG_STATUS"
    TYPE flg_status_cc IS TABLE OF exam_media_archive.flg_status%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_PROF_LAST_UPDATE"
    TYPE id_prof_last_update_cc IS TABLE OF exam_media_archive.id_prof_last_update%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "DT_LAST_UPDATE_TSTZ"
    TYPE dt_last_update_tstz_cc IS TABLE OF exam_media_archive.dt_last_update_tstz%TYPE INDEX BY BINARY_INTEGER;

    -- Insert one row, providing primary key if present
    PROCEDURE ins
    (
        id_exam_media_archive_in IN exam_media_archive.id_exam_media_archive%TYPE,
        id_exam_req_det_in       IN exam_media_archive.id_exam_req_det%TYPE DEFAULT NULL,
        id_exam_result_in        IN exam_media_archive.id_exam_result%TYPE DEFAULT NULL,
        id_doc_external_in       IN exam_media_archive.id_doc_external%TYPE DEFAULT NULL,
        flg_type_in              IN exam_media_archive.flg_type%TYPE DEFAULT NULL,
        create_user_in           IN exam_media_archive.create_user%TYPE DEFAULT NULL,
        create_time_in           IN exam_media_archive.create_time%TYPE DEFAULT NULL,
        create_institution_in    IN exam_media_archive.create_institution%TYPE DEFAULT NULL,
        update_user_in           IN exam_media_archive.update_user%TYPE DEFAULT NULL,
        update_time_in           IN exam_media_archive.update_time%TYPE DEFAULT NULL,
        update_institution_in    IN exam_media_archive.update_institution%TYPE DEFAULT NULL,
        flg_status_in            IN exam_media_archive.flg_status%TYPE DEFAULT NULL,
        id_prof_last_update_in   IN exam_media_archive.id_prof_last_update%TYPE DEFAULT NULL,
        dt_last_update_tstz_in   IN exam_media_archive.dt_last_update_tstz%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_exam_media_archive_in IN exam_media_archive.id_exam_media_archive%TYPE,
        id_exam_req_det_in       IN exam_media_archive.id_exam_req_det%TYPE DEFAULT NULL,
        id_exam_result_in        IN exam_media_archive.id_exam_result%TYPE DEFAULT NULL,
        id_doc_external_in       IN exam_media_archive.id_doc_external%TYPE DEFAULT NULL,
        flg_type_in              IN exam_media_archive.flg_type%TYPE DEFAULT NULL,
        create_user_in           IN exam_media_archive.create_user%TYPE DEFAULT NULL,
        create_time_in           IN exam_media_archive.create_time%TYPE DEFAULT NULL,
        create_institution_in    IN exam_media_archive.create_institution%TYPE DEFAULT NULL,
        update_user_in           IN exam_media_archive.update_user%TYPE DEFAULT NULL,
        update_time_in           IN exam_media_archive.update_time%TYPE DEFAULT NULL,
        update_institution_in    IN exam_media_archive.update_institution%TYPE DEFAULT NULL,
        flg_status_in            IN exam_media_archive.flg_status%TYPE DEFAULT NULL,
        id_prof_last_update_in   IN exam_media_archive.id_prof_last_update%TYPE DEFAULT NULL,
        dt_last_update_tstz_in   IN exam_media_archive.dt_last_update_tstz%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a row based on a record.
    -- Specify whether or not a primary key value should be generated.
    PROCEDURE ins
    (
        rec_in          IN exam_media_archive%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rec_in          IN exam_media_archive%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a collection of rows using FORALL; all primary key values
    -- must have already been generated, or are handled in triggers.
    PROCEDURE ins
    (
        rows_in         IN exam_media_archive_tc,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rows_in         IN exam_media_archive_tc,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Return next primary key value using the named sequence.
    FUNCTION next_key(sequence_in IN VARCHAR2 := NULL) RETURN exam_media_archive.id_exam_media_archive%TYPE;

    -- Insert one row, generating hidden primary key using a sequence
    PROCEDURE ins
    (
        id_exam_req_det_in     IN exam_media_archive.id_exam_req_det%TYPE DEFAULT NULL,
        id_exam_result_in      IN exam_media_archive.id_exam_result%TYPE DEFAULT NULL,
        id_doc_external_in     IN exam_media_archive.id_doc_external%TYPE DEFAULT NULL,
        flg_type_in            IN exam_media_archive.flg_type%TYPE DEFAULT NULL,
        create_user_in         IN exam_media_archive.create_user%TYPE DEFAULT NULL,
        create_time_in         IN exam_media_archive.create_time%TYPE DEFAULT NULL,
        create_institution_in  IN exam_media_archive.create_institution%TYPE DEFAULT NULL,
        update_user_in         IN exam_media_archive.update_user%TYPE DEFAULT NULL,
        update_time_in         IN exam_media_archive.update_time%TYPE DEFAULT NULL,
        update_institution_in  IN exam_media_archive.update_institution%TYPE DEFAULT NULL,
        flg_status_in          IN exam_media_archive.flg_status%TYPE DEFAULT NULL,
        id_prof_last_update_in IN exam_media_archive.id_prof_last_update%TYPE DEFAULT NULL,
        dt_last_update_tstz_in IN exam_media_archive.dt_last_update_tstz%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_exam_req_det_in     IN exam_media_archive.id_exam_req_det%TYPE DEFAULT NULL,
        id_exam_result_in      IN exam_media_archive.id_exam_result%TYPE DEFAULT NULL,
        id_doc_external_in     IN exam_media_archive.id_doc_external%TYPE DEFAULT NULL,
        flg_type_in            IN exam_media_archive.flg_type%TYPE DEFAULT NULL,
        create_user_in         IN exam_media_archive.create_user%TYPE DEFAULT NULL,
        create_time_in         IN exam_media_archive.create_time%TYPE DEFAULT NULL,
        create_institution_in  IN exam_media_archive.create_institution%TYPE DEFAULT NULL,
        update_user_in         IN exam_media_archive.update_user%TYPE DEFAULT NULL,
        update_time_in         IN exam_media_archive.update_time%TYPE DEFAULT NULL,
        update_institution_in  IN exam_media_archive.update_institution%TYPE DEFAULT NULL,
        flg_status_in          IN exam_media_archive.flg_status%TYPE DEFAULT NULL,
        id_prof_last_update_in IN exam_media_archive.id_prof_last_update%TYPE DEFAULT NULL,
        dt_last_update_tstz_in IN exam_media_archive.dt_last_update_tstz%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert one row, returning primary key generated by sequence
    PROCEDURE ins
    (
        id_exam_req_det_in        IN exam_media_archive.id_exam_req_det%TYPE DEFAULT NULL,
        id_exam_result_in         IN exam_media_archive.id_exam_result%TYPE DEFAULT NULL,
        id_doc_external_in        IN exam_media_archive.id_doc_external%TYPE DEFAULT NULL,
        flg_type_in               IN exam_media_archive.flg_type%TYPE DEFAULT NULL,
        create_user_in            IN exam_media_archive.create_user%TYPE DEFAULT NULL,
        create_time_in            IN exam_media_archive.create_time%TYPE DEFAULT NULL,
        create_institution_in     IN exam_media_archive.create_institution%TYPE DEFAULT NULL,
        update_user_in            IN exam_media_archive.update_user%TYPE DEFAULT NULL,
        update_time_in            IN exam_media_archive.update_time%TYPE DEFAULT NULL,
        update_institution_in     IN exam_media_archive.update_institution%TYPE DEFAULT NULL,
        flg_status_in             IN exam_media_archive.flg_status%TYPE DEFAULT NULL,
        id_prof_last_update_in    IN exam_media_archive.id_prof_last_update%TYPE DEFAULT NULL,
        dt_last_update_tstz_in    IN exam_media_archive.dt_last_update_tstz%TYPE DEFAULT NULL,
        id_exam_media_archive_out IN OUT exam_media_archive.id_exam_media_archive%TYPE
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_exam_req_det_in        IN exam_media_archive.id_exam_req_det%TYPE DEFAULT NULL,
        id_exam_result_in         IN exam_media_archive.id_exam_result%TYPE DEFAULT NULL,
        id_doc_external_in        IN exam_media_archive.id_doc_external%TYPE DEFAULT NULL,
        flg_type_in               IN exam_media_archive.flg_type%TYPE DEFAULT NULL,
        create_user_in            IN exam_media_archive.create_user%TYPE DEFAULT NULL,
        create_time_in            IN exam_media_archive.create_time%TYPE DEFAULT NULL,
        create_institution_in     IN exam_media_archive.create_institution%TYPE DEFAULT NULL,
        update_user_in            IN exam_media_archive.update_user%TYPE DEFAULT NULL,
        update_time_in            IN exam_media_archive.update_time%TYPE DEFAULT NULL,
        update_institution_in     IN exam_media_archive.update_institution%TYPE DEFAULT NULL,
        flg_status_in             IN exam_media_archive.flg_status%TYPE DEFAULT NULL,
        id_prof_last_update_in    IN exam_media_archive.id_prof_last_update%TYPE DEFAULT NULL,
        dt_last_update_tstz_in    IN exam_media_archive.dt_last_update_tstz%TYPE DEFAULT NULL,
        id_exam_media_archive_out IN OUT exam_media_archive.id_exam_media_archive%TYPE
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert one row with function, return generated primary key
    FUNCTION ins
    (
        id_exam_req_det_in     IN exam_media_archive.id_exam_req_det%TYPE DEFAULT NULL,
        id_exam_result_in      IN exam_media_archive.id_exam_result%TYPE DEFAULT NULL,
        id_doc_external_in     IN exam_media_archive.id_doc_external%TYPE DEFAULT NULL,
        flg_type_in            IN exam_media_archive.flg_type%TYPE DEFAULT NULL,
        create_user_in         IN exam_media_archive.create_user%TYPE DEFAULT NULL,
        create_time_in         IN exam_media_archive.create_time%TYPE DEFAULT NULL,
        create_institution_in  IN exam_media_archive.create_institution%TYPE DEFAULT NULL,
        update_user_in         IN exam_media_archive.update_user%TYPE DEFAULT NULL,
        update_time_in         IN exam_media_archive.update_time%TYPE DEFAULT NULL,
        update_institution_in  IN exam_media_archive.update_institution%TYPE DEFAULT NULL,
        flg_status_in          IN exam_media_archive.flg_status%TYPE DEFAULT NULL,
        id_prof_last_update_in IN exam_media_archive.id_prof_last_update%TYPE DEFAULT NULL,
        dt_last_update_tstz_in IN exam_media_archive.dt_last_update_tstz%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) RETURN exam_media_archive.id_exam_media_archive%TYPE;

    FUNCTION ins
    (
        id_exam_req_det_in     IN exam_media_archive.id_exam_req_det%TYPE DEFAULT NULL,
        id_exam_result_in      IN exam_media_archive.id_exam_result%TYPE DEFAULT NULL,
        id_doc_external_in     IN exam_media_archive.id_doc_external%TYPE DEFAULT NULL,
        flg_type_in            IN exam_media_archive.flg_type%TYPE DEFAULT NULL,
        create_user_in         IN exam_media_archive.create_user%TYPE DEFAULT NULL,
        create_time_in         IN exam_media_archive.create_time%TYPE DEFAULT NULL,
        create_institution_in  IN exam_media_archive.create_institution%TYPE DEFAULT NULL,
        update_user_in         IN exam_media_archive.update_user%TYPE DEFAULT NULL,
        update_time_in         IN exam_media_archive.update_time%TYPE DEFAULT NULL,
        update_institution_in  IN exam_media_archive.update_institution%TYPE DEFAULT NULL,
        flg_status_in          IN exam_media_archive.flg_status%TYPE DEFAULT NULL,
        id_prof_last_update_in IN exam_media_archive.id_prof_last_update%TYPE DEFAULT NULL,
        dt_last_update_tstz_in IN exam_media_archive.dt_last_update_tstz%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    ) RETURN exam_media_archive.id_exam_media_archive%TYPE;

    -- Update any/all columns by primary key. If you pass NULL, then
    -- the current column value is set to itself. If you need a more
    -- selected UPDATE then use one of the onecol procedures below.

    PROCEDURE upd
    (
        id_exam_media_archive_in IN exam_media_archive.id_exam_media_archive%TYPE,
        id_exam_req_det_in       IN exam_media_archive.id_exam_req_det%TYPE DEFAULT NULL,
        id_exam_req_det_nin      IN BOOLEAN := TRUE,
        id_exam_result_in        IN exam_media_archive.id_exam_result%TYPE DEFAULT NULL,
        id_exam_result_nin       IN BOOLEAN := TRUE,
        id_doc_external_in       IN exam_media_archive.id_doc_external%TYPE DEFAULT NULL,
        id_doc_external_nin      IN BOOLEAN := TRUE,
        flg_type_in              IN exam_media_archive.flg_type%TYPE DEFAULT NULL,
        flg_type_nin             IN BOOLEAN := TRUE,
        create_user_in           IN exam_media_archive.create_user%TYPE DEFAULT NULL,
        create_user_nin          IN BOOLEAN := TRUE,
        create_time_in           IN exam_media_archive.create_time%TYPE DEFAULT NULL,
        create_time_nin          IN BOOLEAN := TRUE,
        create_institution_in    IN exam_media_archive.create_institution%TYPE DEFAULT NULL,
        create_institution_nin   IN BOOLEAN := TRUE,
        update_user_in           IN exam_media_archive.update_user%TYPE DEFAULT NULL,
        update_user_nin          IN BOOLEAN := TRUE,
        update_time_in           IN exam_media_archive.update_time%TYPE DEFAULT NULL,
        update_time_nin          IN BOOLEAN := TRUE,
        update_institution_in    IN exam_media_archive.update_institution%TYPE DEFAULT NULL,
        update_institution_nin   IN BOOLEAN := TRUE,
        flg_status_in            IN exam_media_archive.flg_status%TYPE DEFAULT NULL,
        flg_status_nin           IN BOOLEAN := TRUE,
        id_prof_last_update_in   IN exam_media_archive.id_prof_last_update%TYPE DEFAULT NULL,
        id_prof_last_update_nin  IN BOOLEAN := TRUE,
        dt_last_update_tstz_in   IN exam_media_archive.dt_last_update_tstz%TYPE DEFAULT NULL,
        dt_last_update_tstz_nin  IN BOOLEAN := TRUE,
        handle_error_in          IN BOOLEAN := TRUE,
        rows_out                 IN OUT table_varchar
    );

    PROCEDURE upd
    (
        id_exam_media_archive_in IN exam_media_archive.id_exam_media_archive%TYPE,
        id_exam_req_det_in       IN exam_media_archive.id_exam_req_det%TYPE DEFAULT NULL,
        id_exam_req_det_nin      IN BOOLEAN := TRUE,
        id_exam_result_in        IN exam_media_archive.id_exam_result%TYPE DEFAULT NULL,
        id_exam_result_nin       IN BOOLEAN := TRUE,
        id_doc_external_in       IN exam_media_archive.id_doc_external%TYPE DEFAULT NULL,
        id_doc_external_nin      IN BOOLEAN := TRUE,
        flg_type_in              IN exam_media_archive.flg_type%TYPE DEFAULT NULL,
        flg_type_nin             IN BOOLEAN := TRUE,
        create_user_in           IN exam_media_archive.create_user%TYPE DEFAULT NULL,
        create_user_nin          IN BOOLEAN := TRUE,
        create_time_in           IN exam_media_archive.create_time%TYPE DEFAULT NULL,
        create_time_nin          IN BOOLEAN := TRUE,
        create_institution_in    IN exam_media_archive.create_institution%TYPE DEFAULT NULL,
        create_institution_nin   IN BOOLEAN := TRUE,
        update_user_in           IN exam_media_archive.update_user%TYPE DEFAULT NULL,
        update_user_nin          IN BOOLEAN := TRUE,
        update_time_in           IN exam_media_archive.update_time%TYPE DEFAULT NULL,
        update_time_nin          IN BOOLEAN := TRUE,
        update_institution_in    IN exam_media_archive.update_institution%TYPE DEFAULT NULL,
        update_institution_nin   IN BOOLEAN := TRUE,
        flg_status_in            IN exam_media_archive.flg_status%TYPE DEFAULT NULL,
        flg_status_nin           IN BOOLEAN := TRUE,
        id_prof_last_update_in   IN exam_media_archive.id_prof_last_update%TYPE DEFAULT NULL,
        id_prof_last_update_nin  IN BOOLEAN := TRUE,
        dt_last_update_tstz_in   IN exam_media_archive.dt_last_update_tstz%TYPE DEFAULT NULL,
        dt_last_update_tstz_nin  IN BOOLEAN := TRUE,
        handle_error_in          IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        id_exam_req_det_in      IN exam_media_archive.id_exam_req_det%TYPE DEFAULT NULL,
        id_exam_req_det_nin     IN BOOLEAN := TRUE,
        id_exam_result_in       IN exam_media_archive.id_exam_result%TYPE DEFAULT NULL,
        id_exam_result_nin      IN BOOLEAN := TRUE,
        id_doc_external_in      IN exam_media_archive.id_doc_external%TYPE DEFAULT NULL,
        id_doc_external_nin     IN BOOLEAN := TRUE,
        flg_type_in             IN exam_media_archive.flg_type%TYPE DEFAULT NULL,
        flg_type_nin            IN BOOLEAN := TRUE,
        create_user_in          IN exam_media_archive.create_user%TYPE DEFAULT NULL,
        create_user_nin         IN BOOLEAN := TRUE,
        create_time_in          IN exam_media_archive.create_time%TYPE DEFAULT NULL,
        create_time_nin         IN BOOLEAN := TRUE,
        create_institution_in   IN exam_media_archive.create_institution%TYPE DEFAULT NULL,
        create_institution_nin  IN BOOLEAN := TRUE,
        update_user_in          IN exam_media_archive.update_user%TYPE DEFAULT NULL,
        update_user_nin         IN BOOLEAN := TRUE,
        update_time_in          IN exam_media_archive.update_time%TYPE DEFAULT NULL,
        update_time_nin         IN BOOLEAN := TRUE,
        update_institution_in   IN exam_media_archive.update_institution%TYPE DEFAULT NULL,
        update_institution_nin  IN BOOLEAN := TRUE,
        flg_status_in           IN exam_media_archive.flg_status%TYPE DEFAULT NULL,
        flg_status_nin          IN BOOLEAN := TRUE,
        id_prof_last_update_in  IN exam_media_archive.id_prof_last_update%TYPE DEFAULT NULL,
        id_prof_last_update_nin IN BOOLEAN := TRUE,
        dt_last_update_tstz_in  IN exam_media_archive.dt_last_update_tstz%TYPE DEFAULT NULL,
        dt_last_update_tstz_nin IN BOOLEAN := TRUE,
        where_in                VARCHAR2,
        handle_error_in         IN BOOLEAN := TRUE,
        rows_out                IN OUT table_varchar
    );

    PROCEDURE upd
    (
        id_exam_req_det_in      IN exam_media_archive.id_exam_req_det%TYPE DEFAULT NULL,
        id_exam_req_det_nin     IN BOOLEAN := TRUE,
        id_exam_result_in       IN exam_media_archive.id_exam_result%TYPE DEFAULT NULL,
        id_exam_result_nin      IN BOOLEAN := TRUE,
        id_doc_external_in      IN exam_media_archive.id_doc_external%TYPE DEFAULT NULL,
        id_doc_external_nin     IN BOOLEAN := TRUE,
        flg_type_in             IN exam_media_archive.flg_type%TYPE DEFAULT NULL,
        flg_type_nin            IN BOOLEAN := TRUE,
        create_user_in          IN exam_media_archive.create_user%TYPE DEFAULT NULL,
        create_user_nin         IN BOOLEAN := TRUE,
        create_time_in          IN exam_media_archive.create_time%TYPE DEFAULT NULL,
        create_time_nin         IN BOOLEAN := TRUE,
        create_institution_in   IN exam_media_archive.create_institution%TYPE DEFAULT NULL,
        create_institution_nin  IN BOOLEAN := TRUE,
        update_user_in          IN exam_media_archive.update_user%TYPE DEFAULT NULL,
        update_user_nin         IN BOOLEAN := TRUE,
        update_time_in          IN exam_media_archive.update_time%TYPE DEFAULT NULL,
        update_time_nin         IN BOOLEAN := TRUE,
        update_institution_in   IN exam_media_archive.update_institution%TYPE DEFAULT NULL,
        update_institution_nin  IN BOOLEAN := TRUE,
        flg_status_in           IN exam_media_archive.flg_status%TYPE DEFAULT NULL,
        flg_status_nin          IN BOOLEAN := TRUE,
        id_prof_last_update_in  IN exam_media_archive.id_prof_last_update%TYPE DEFAULT NULL,
        id_prof_last_update_nin IN BOOLEAN := TRUE,
        dt_last_update_tstz_in  IN exam_media_archive.dt_last_update_tstz%TYPE DEFAULT NULL,
        dt_last_update_tstz_nin IN BOOLEAN := TRUE,
        where_in                VARCHAR2,
        handle_error_in         IN BOOLEAN := TRUE
    );

    PROCEDURE upd_ins
    (
        id_exam_media_archive_in IN exam_media_archive.id_exam_media_archive%TYPE,
        id_exam_req_det_in       IN exam_media_archive.id_exam_req_det%TYPE DEFAULT NULL,
        id_exam_result_in        IN exam_media_archive.id_exam_result%TYPE DEFAULT NULL,
        id_doc_external_in       IN exam_media_archive.id_doc_external%TYPE DEFAULT NULL,
        flg_type_in              IN exam_media_archive.flg_type%TYPE DEFAULT NULL,
        create_user_in           IN exam_media_archive.create_user%TYPE DEFAULT NULL,
        create_time_in           IN exam_media_archive.create_time%TYPE DEFAULT NULL,
        create_institution_in    IN exam_media_archive.create_institution%TYPE DEFAULT NULL,
        update_user_in           IN exam_media_archive.update_user%TYPE DEFAULT NULL,
        update_time_in           IN exam_media_archive.update_time%TYPE DEFAULT NULL,
        update_institution_in    IN exam_media_archive.update_institution%TYPE DEFAULT NULL,
        flg_status_in            IN exam_media_archive.flg_status%TYPE DEFAULT NULL,
        id_prof_last_update_in   IN exam_media_archive.id_prof_last_update%TYPE DEFAULT NULL,
        dt_last_update_tstz_in   IN exam_media_archive.dt_last_update_tstz%TYPE DEFAULT NULL,
        handle_error_in          IN BOOLEAN := TRUE,
        rows_out                 OUT table_varchar
    );

    PROCEDURE upd_ins
    (
        id_exam_media_archive_in IN exam_media_archive.id_exam_media_archive%TYPE,
        id_exam_req_det_in       IN exam_media_archive.id_exam_req_det%TYPE DEFAULT NULL,
        id_exam_result_in        IN exam_media_archive.id_exam_result%TYPE DEFAULT NULL,
        id_doc_external_in       IN exam_media_archive.id_doc_external%TYPE DEFAULT NULL,
        flg_type_in              IN exam_media_archive.flg_type%TYPE DEFAULT NULL,
        create_user_in           IN exam_media_archive.create_user%TYPE DEFAULT NULL,
        create_time_in           IN exam_media_archive.create_time%TYPE DEFAULT NULL,
        create_institution_in    IN exam_media_archive.create_institution%TYPE DEFAULT NULL,
        update_user_in           IN exam_media_archive.update_user%TYPE DEFAULT NULL,
        update_time_in           IN exam_media_archive.update_time%TYPE DEFAULT NULL,
        update_institution_in    IN exam_media_archive.update_institution%TYPE DEFAULT NULL,
        flg_status_in            IN exam_media_archive.flg_status%TYPE DEFAULT NULL,
        id_prof_last_update_in   IN exam_media_archive.id_prof_last_update%TYPE DEFAULT NULL,
        dt_last_update_tstz_in   IN exam_media_archive.dt_last_update_tstz%TYPE DEFAULT NULL,
        handle_error_in          IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        rec_in          IN exam_media_archive%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        IN OUT table_varchar
    );

    PROCEDURE upd
    (
        rec_in          IN exam_media_archive%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        col_in            IN exam_media_archive_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          IN OUT table_varchar
    );

    PROCEDURE upd
    (
        col_in            IN exam_media_archive_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE
    );

    -- Use Native Dynamic SQL increment a single NUMBER column
    -- for all rows specified by the dynamic WHERE clause
    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_exam_media_archive_in IN exam_media_archive.id_exam_media_archive%TYPE,
        handle_error_in          IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_exam_media_archive_in IN exam_media_archive.id_exam_media_archive%TYPE,
        handle_error_in          IN BOOLEAN := TRUE,
        rows_out                 OUT table_varchar
    );

    -- Delete all rows for primary key column ID_EXAM_MEDIA_ARCHIVE
    PROCEDURE del_id_exam_media_archive
    (
        id_exam_media_archive_in IN exam_media_archive.id_exam_media_archive%TYPE,
        handle_error_in          IN BOOLEAN := TRUE
    );

    -- Delete all rows for primary key column ID_EXAM_MEDIA_ARCHIVE
    PROCEDURE del_id_exam_media_archive
    (
        id_exam_media_archive_in IN exam_media_archive.id_exam_media_archive%TYPE,
        handle_error_in          IN BOOLEAN := TRUE,
        rows_out                 OUT table_varchar
    );

    -- Delete all rows for this EME_DEL_FK foreign key value
    PROCEDURE del_eme_del_fk
    (
        id_doc_external_in IN exam_media_archive.id_doc_external%TYPE,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete all rows for this EME_DEL_FK foreign key value
    PROCEDURE del_eme_del_fk
    (
        id_doc_external_in IN exam_media_archive.id_doc_external%TYPE,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    -- Delete all rows for this EME_ERES_FK foreign key value
    PROCEDURE del_eme_eres_fk
    (
        id_exam_result_in IN exam_media_archive.id_exam_result%TYPE,
        handle_error_in   IN BOOLEAN := TRUE
    );

    -- Delete all rows for this EME_ERES_FK foreign key value
    PROCEDURE del_eme_eres_fk
    (
        id_exam_result_in IN exam_media_archive.id_exam_result%TYPE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          OUT table_varchar
    );

    -- Delete all rows for this EME_ERT_FK foreign key value
    PROCEDURE del_eme_ert_fk
    (
        id_exam_req_det_in IN exam_media_archive.id_exam_req_det%TYPE,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete all rows for this EME_ERT_FK foreign key value
    PROCEDURE del_eme_ert_fk
    (
        id_exam_req_det_in IN exam_media_archive.id_exam_req_det%TYPE,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    -- Delete all rows for this EME_PL_FK foreign key value
    PROCEDURE del_eme_pl_fk
    (
        id_prof_last_update_in IN exam_media_archive.id_prof_last_update%TYPE,
        handle_error_in        IN BOOLEAN := TRUE
    );

    -- Delete all rows for this EME_PL_FK foreign key value
    PROCEDURE del_eme_pl_fk
    (
        id_prof_last_update_in IN exam_media_archive.id_prof_last_update%TYPE,
        handle_error_in        IN BOOLEAN := TRUE,
        rows_out               OUT table_varchar
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Initialize a record with default values for columns in the table.
    PROCEDURE initrec(exam_media_archive_inout IN OUT exam_media_archive%ROWTYPE);

    FUNCTION initrec RETURN exam_media_archive%ROWTYPE;

    FUNCTION get_data_rowid(rows_in IN table_varchar) RETURN exam_media_archive_tc;

    FUNCTION get_data_rowid_pat(rows_in IN table_varchar) RETURN exam_media_archive_tc;

END ts_exam_media_archive;
/
