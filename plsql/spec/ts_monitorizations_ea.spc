/*-- Last Change Revision: $Rev: 2029246 $*/
/*-- Last Change by: $Author: mario.fernandes $*/
/*-- Date of last change: $Date: 2022-08-02 18:50:36 +0100 (ter, 02 ago 2022) $*/
CREATE OR REPLACE PACKAGE ts_monitorizations_ea
/*
| Generated by or retrieved from QCGU - DO NOT MODIFY!
| QCGU - "Get it right, do it fast" - www.ToadWorld.com
| QCGU Universal ID: {1BD37A66-EA60-4927-9A64-F6DD89237236}
| Created On: Setembro 8, 2011 9:56:45
| Created By: ALERT
*/
 IS

    -- Collection of %ROWTYPE records based on "MONITORIZATIONS_EA"
    TYPE monitorizations_ea_tc IS TABLE OF monitorizations_ea%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE monitorizations_ea_ntt IS TABLE OF monitorizations_ea%ROWTYPE;
    TYPE monitorizations_ea_vat IS VARRAY(100) OF monitorizations_ea%ROWTYPE;

    -- Same type structure, with a static name.
    TYPE aat IS TABLE OF monitorizations_ea%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE ntt IS TABLE OF monitorizations_ea%ROWTYPE;
    TYPE vat IS VARRAY(100) OF monitorizations_ea%ROWTYPE;

    -- Column Collection based on column "ID_MONITORIZATION"
    TYPE id_monitorization_cc IS TABLE OF monitorizations_ea.id_monitorization%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_MONITORIZATION_VS"
    TYPE id_monitorization_vs_cc IS TABLE OF monitorizations_ea.id_monitorization_vs%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_MONITORIZATION_VS_PLAN"
    TYPE id_monitorization_vs_plan_cc IS TABLE OF monitorizations_ea.id_monitorization_vs_plan%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_VITAL_SIGN"
    TYPE id_vital_sign_cc IS TABLE OF monitorizations_ea.id_vital_sign%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "FLG_STATUS"
    TYPE flg_status_cc IS TABLE OF monitorizations_ea.flg_status%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "FLG_STATUS_DET"
    TYPE flg_status_det_cc IS TABLE OF monitorizations_ea.flg_status_det%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "FLG_STATUS_PLAN"
    TYPE flg_status_plan_cc IS TABLE OF monitorizations_ea.flg_status_plan%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "FLG_TIME"
    TYPE flg_time_cc IS TABLE OF monitorizations_ea.flg_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "DT_MONITORIZATION"
    TYPE dt_monitorization_cc IS TABLE OF monitorizations_ea.dt_monitorization%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "DT_PLAN"
    TYPE dt_plan_cc IS TABLE OF monitorizations_ea.dt_plan%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "INTERVAL"
    TYPE interval_cc IS TABLE OF monitorizations_ea.interval%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_EPISODE_ORIGIN"
    TYPE id_episode_origin_cc IS TABLE OF monitorizations_ea.id_episode_origin%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "DT_BEGIN"
    TYPE dt_begin_cc IS TABLE OF monitorizations_ea.dt_begin%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "DT_END"
    TYPE dt_end_cc IS TABLE OF monitorizations_ea.dt_end%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "NUM_MONIT"
    TYPE num_monit_cc IS TABLE OF monitorizations_ea.num_monit%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_VISIT"
    TYPE id_visit_cc IS TABLE OF monitorizations_ea.id_visit%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "STATUS_STR"
    TYPE status_str_cc IS TABLE OF monitorizations_ea.status_str%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "STATUS_MSG"
    TYPE status_msg_cc IS TABLE OF monitorizations_ea.status_msg%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "STATUS_ICON"
    TYPE status_icon_cc IS TABLE OF monitorizations_ea.status_icon%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "STATUS_FLG"
    TYPE status_flg_cc IS TABLE OF monitorizations_ea.status_flg%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "FLG_NOTES"
    TYPE flg_notes_cc IS TABLE OF monitorizations_ea.flg_notes%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_EPISODE"
    TYPE id_episode_cc IS TABLE OF monitorizations_ea.id_episode%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_PREV_EPISODE"
    TYPE id_prev_episode_cc IS TABLE OF monitorizations_ea.id_prev_episode%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_PATIENT"
    TYPE id_patient_cc IS TABLE OF monitorizations_ea.id_patient%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_PROFESSIONAL"
    TYPE id_professional_cc IS TABLE OF monitorizations_ea.id_professional%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "DT_DG_LAST_UPDATE"
    TYPE dt_dg_last_update_cc IS TABLE OF monitorizations_ea.dt_dg_last_update%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_USER"
    TYPE create_user_cc IS TABLE OF monitorizations_ea.create_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_TIME"
    TYPE create_time_cc IS TABLE OF monitorizations_ea.create_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_INSTITUTION"
    TYPE create_institution_cc IS TABLE OF monitorizations_ea.create_institution%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_USER"
    TYPE update_user_cc IS TABLE OF monitorizations_ea.update_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_TIME"
    TYPE update_time_cc IS TABLE OF monitorizations_ea.update_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_INSTITUTION"
    TYPE update_institution_cc IS TABLE OF monitorizations_ea.update_institution%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "DT_ORDER"
    TYPE dt_order_cc IS TABLE OF monitorizations_ea.dt_order%TYPE INDEX BY BINARY_INTEGER;

    -- Insert one row, providing primary key if present
    PROCEDURE ins
    (
        id_monitorization_vs_in      IN monitorizations_ea.id_monitorization_vs%TYPE,
        id_monitorization_in         IN monitorizations_ea.id_monitorization%TYPE DEFAULT NULL,
        id_monitorization_vs_plan_in IN monitorizations_ea.id_monitorization_vs_plan%TYPE DEFAULT NULL,
        id_vital_sign_in             IN monitorizations_ea.id_vital_sign%TYPE DEFAULT NULL,
        flg_status_in                IN monitorizations_ea.flg_status%TYPE DEFAULT NULL,
        flg_status_det_in            IN monitorizations_ea.flg_status_det%TYPE DEFAULT NULL,
        flg_status_plan_in           IN monitorizations_ea.flg_status_plan%TYPE DEFAULT NULL,
        flg_time_in                  IN monitorizations_ea.flg_time%TYPE DEFAULT NULL,
        dt_monitorization_in         IN monitorizations_ea.dt_monitorization%TYPE DEFAULT NULL,
        dt_plan_in                   IN monitorizations_ea.dt_plan%TYPE DEFAULT NULL,
        interval_in                  IN monitorizations_ea.interval%TYPE DEFAULT NULL,
        id_episode_origin_in         IN monitorizations_ea.id_episode_origin%TYPE DEFAULT NULL,
        dt_begin_in                  IN monitorizations_ea.dt_begin%TYPE DEFAULT NULL,
        dt_end_in                    IN monitorizations_ea.dt_end%TYPE DEFAULT NULL,
        num_monit_in                 IN monitorizations_ea.num_monit%TYPE DEFAULT NULL,
        id_visit_in                  IN monitorizations_ea.id_visit%TYPE DEFAULT NULL,
        status_str_in                IN monitorizations_ea.status_str%TYPE DEFAULT NULL,
        status_msg_in                IN monitorizations_ea.status_msg%TYPE DEFAULT NULL,
        status_icon_in               IN monitorizations_ea.status_icon%TYPE DEFAULT NULL,
        status_flg_in                IN monitorizations_ea.status_flg%TYPE DEFAULT NULL,
        flg_notes_in                 IN monitorizations_ea.flg_notes%TYPE DEFAULT NULL,
        id_episode_in                IN monitorizations_ea.id_episode%TYPE DEFAULT NULL,
        id_prev_episode_in           IN monitorizations_ea.id_prev_episode%TYPE DEFAULT NULL,
        id_patient_in                IN monitorizations_ea.id_patient%TYPE DEFAULT NULL,
        id_professional_in           IN monitorizations_ea.id_professional%TYPE DEFAULT NULL,
        dt_dg_last_update_in         IN monitorizations_ea.dt_dg_last_update%TYPE DEFAULT current_timestamp,
        create_user_in               IN monitorizations_ea.create_user%TYPE DEFAULT NULL,
        create_time_in               IN monitorizations_ea.create_time%TYPE DEFAULT NULL,
        create_institution_in        IN monitorizations_ea.create_institution%TYPE DEFAULT NULL,
        update_user_in               IN monitorizations_ea.update_user%TYPE DEFAULT NULL,
        update_time_in               IN monitorizations_ea.update_time%TYPE DEFAULT NULL,
        update_institution_in        IN monitorizations_ea.update_institution%TYPE DEFAULT NULL,
        dt_order_in                  IN monitorizations_ea.dt_order%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_monitorization_vs_in      IN monitorizations_ea.id_monitorization_vs%TYPE,
        id_monitorization_in         IN monitorizations_ea.id_monitorization%TYPE DEFAULT NULL,
        id_monitorization_vs_plan_in IN monitorizations_ea.id_monitorization_vs_plan%TYPE DEFAULT NULL,
        id_vital_sign_in             IN monitorizations_ea.id_vital_sign%TYPE DEFAULT NULL,
        flg_status_in                IN monitorizations_ea.flg_status%TYPE DEFAULT NULL,
        flg_status_det_in            IN monitorizations_ea.flg_status_det%TYPE DEFAULT NULL,
        flg_status_plan_in           IN monitorizations_ea.flg_status_plan%TYPE DEFAULT NULL,
        flg_time_in                  IN monitorizations_ea.flg_time%TYPE DEFAULT NULL,
        dt_monitorization_in         IN monitorizations_ea.dt_monitorization%TYPE DEFAULT NULL,
        dt_plan_in                   IN monitorizations_ea.dt_plan%TYPE DEFAULT NULL,
        interval_in                  IN monitorizations_ea.interval%TYPE DEFAULT NULL,
        id_episode_origin_in         IN monitorizations_ea.id_episode_origin%TYPE DEFAULT NULL,
        dt_begin_in                  IN monitorizations_ea.dt_begin%TYPE DEFAULT NULL,
        dt_end_in                    IN monitorizations_ea.dt_end%TYPE DEFAULT NULL,
        num_monit_in                 IN monitorizations_ea.num_monit%TYPE DEFAULT NULL,
        id_visit_in                  IN monitorizations_ea.id_visit%TYPE DEFAULT NULL,
        status_str_in                IN monitorizations_ea.status_str%TYPE DEFAULT NULL,
        status_msg_in                IN monitorizations_ea.status_msg%TYPE DEFAULT NULL,
        status_icon_in               IN monitorizations_ea.status_icon%TYPE DEFAULT NULL,
        status_flg_in                IN monitorizations_ea.status_flg%TYPE DEFAULT NULL,
        flg_notes_in                 IN monitorizations_ea.flg_notes%TYPE DEFAULT NULL,
        id_episode_in                IN monitorizations_ea.id_episode%TYPE DEFAULT NULL,
        id_prev_episode_in           IN monitorizations_ea.id_prev_episode%TYPE DEFAULT NULL,
        id_patient_in                IN monitorizations_ea.id_patient%TYPE DEFAULT NULL,
        id_professional_in           IN monitorizations_ea.id_professional%TYPE DEFAULT NULL,
        dt_dg_last_update_in         IN monitorizations_ea.dt_dg_last_update%TYPE DEFAULT current_timestamp,
        create_user_in               IN monitorizations_ea.create_user%TYPE DEFAULT NULL,
        create_time_in               IN monitorizations_ea.create_time%TYPE DEFAULT NULL,
        create_institution_in        IN monitorizations_ea.create_institution%TYPE DEFAULT NULL,
        update_user_in               IN monitorizations_ea.update_user%TYPE DEFAULT NULL,
        update_time_in               IN monitorizations_ea.update_time%TYPE DEFAULT NULL,
        update_institution_in        IN monitorizations_ea.update_institution%TYPE DEFAULT NULL,
        dt_order_in                  IN monitorizations_ea.dt_order%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a row based on a record.
    PROCEDURE ins
    (
        rec_in          IN monitorizations_ea%ROWTYPE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rec_in          IN monitorizations_ea%ROWTYPE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a collection of rows using FORALL; all primary key values
    -- must have already been generated, or are handled in triggers.
    PROCEDURE ins
    (
        rows_in         IN monitorizations_ea_tc,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rows_in         IN monitorizations_ea_tc,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Update any/all columns by primary key. If you pass NULL, then
    -- the current column value is set to itself. If you need a more
    -- selected UPDATE then use one of the onecol procedures below.

    PROCEDURE upd
    (
        id_monitorization_vs_in       IN monitorizations_ea.id_monitorization_vs%TYPE,
        id_monitorization_in          IN monitorizations_ea.id_monitorization%TYPE DEFAULT NULL,
        id_monitorization_nin         IN BOOLEAN := TRUE,
        id_monitorization_vs_plan_in  IN monitorizations_ea.id_monitorization_vs_plan%TYPE DEFAULT NULL,
        id_monitorization_vs_plan_nin IN BOOLEAN := TRUE,
        id_vital_sign_in              IN monitorizations_ea.id_vital_sign%TYPE DEFAULT NULL,
        id_vital_sign_nin             IN BOOLEAN := TRUE,
        flg_status_in                 IN monitorizations_ea.flg_status%TYPE DEFAULT NULL,
        flg_status_nin                IN BOOLEAN := TRUE,
        flg_status_det_in             IN monitorizations_ea.flg_status_det%TYPE DEFAULT NULL,
        flg_status_det_nin            IN BOOLEAN := TRUE,
        flg_status_plan_in            IN monitorizations_ea.flg_status_plan%TYPE DEFAULT NULL,
        flg_status_plan_nin           IN BOOLEAN := TRUE,
        flg_time_in                   IN monitorizations_ea.flg_time%TYPE DEFAULT NULL,
        flg_time_nin                  IN BOOLEAN := TRUE,
        dt_monitorization_in          IN monitorizations_ea.dt_monitorization%TYPE DEFAULT NULL,
        dt_monitorization_nin         IN BOOLEAN := TRUE,
        dt_plan_in                    IN monitorizations_ea.dt_plan%TYPE DEFAULT NULL,
        dt_plan_nin                   IN BOOLEAN := TRUE,
        interval_in                   IN monitorizations_ea.interval%TYPE DEFAULT NULL,
        interval_nin                  IN BOOLEAN := TRUE,
        id_episode_origin_in          IN monitorizations_ea.id_episode_origin%TYPE DEFAULT NULL,
        id_episode_origin_nin         IN BOOLEAN := TRUE,
        dt_begin_in                   IN monitorizations_ea.dt_begin%TYPE DEFAULT NULL,
        dt_begin_nin                  IN BOOLEAN := TRUE,
        dt_end_in                     IN monitorizations_ea.dt_end%TYPE DEFAULT NULL,
        dt_end_nin                    IN BOOLEAN := TRUE,
        num_monit_in                  IN monitorizations_ea.num_monit%TYPE DEFAULT NULL,
        num_monit_nin                 IN BOOLEAN := TRUE,
        id_visit_in                   IN monitorizations_ea.id_visit%TYPE DEFAULT NULL,
        id_visit_nin                  IN BOOLEAN := TRUE,
        status_str_in                 IN monitorizations_ea.status_str%TYPE DEFAULT NULL,
        status_str_nin                IN BOOLEAN := TRUE,
        status_msg_in                 IN monitorizations_ea.status_msg%TYPE DEFAULT NULL,
        status_msg_nin                IN BOOLEAN := TRUE,
        status_icon_in                IN monitorizations_ea.status_icon%TYPE DEFAULT NULL,
        status_icon_nin               IN BOOLEAN := TRUE,
        status_flg_in                 IN monitorizations_ea.status_flg%TYPE DEFAULT NULL,
        status_flg_nin                IN BOOLEAN := TRUE,
        flg_notes_in                  IN monitorizations_ea.flg_notes%TYPE DEFAULT NULL,
        flg_notes_nin                 IN BOOLEAN := TRUE,
        id_episode_in                 IN monitorizations_ea.id_episode%TYPE DEFAULT NULL,
        id_episode_nin                IN BOOLEAN := TRUE,
        id_prev_episode_in            IN monitorizations_ea.id_prev_episode%TYPE DEFAULT NULL,
        id_prev_episode_nin           IN BOOLEAN := TRUE,
        id_patient_in                 IN monitorizations_ea.id_patient%TYPE DEFAULT NULL,
        id_patient_nin                IN BOOLEAN := TRUE,
        id_professional_in            IN monitorizations_ea.id_professional%TYPE DEFAULT NULL,
        id_professional_nin           IN BOOLEAN := TRUE,
        dt_dg_last_update_in          IN monitorizations_ea.dt_dg_last_update%TYPE DEFAULT NULL,
        dt_dg_last_update_nin         IN BOOLEAN := TRUE,
        create_user_in                IN monitorizations_ea.create_user%TYPE DEFAULT NULL,
        create_user_nin               IN BOOLEAN := TRUE,
        create_time_in                IN monitorizations_ea.create_time%TYPE DEFAULT NULL,
        create_time_nin               IN BOOLEAN := TRUE,
        create_institution_in         IN monitorizations_ea.create_institution%TYPE DEFAULT NULL,
        create_institution_nin        IN BOOLEAN := TRUE,
        update_user_in                IN monitorizations_ea.update_user%TYPE DEFAULT NULL,
        update_user_nin               IN BOOLEAN := TRUE,
        update_time_in                IN monitorizations_ea.update_time%TYPE DEFAULT NULL,
        update_time_nin               IN BOOLEAN := TRUE,
        update_institution_in         IN monitorizations_ea.update_institution%TYPE DEFAULT NULL,
        update_institution_nin        IN BOOLEAN := TRUE,
        dt_order_in                   IN monitorizations_ea.dt_order%TYPE DEFAULT NULL,
        dt_order_nin                  IN BOOLEAN := TRUE,
        handle_error_in               IN BOOLEAN := TRUE,
        rows_out                      IN OUT table_varchar
    );

    PROCEDURE upd
    (
        id_monitorization_vs_in       IN monitorizations_ea.id_monitorization_vs%TYPE,
        id_monitorization_in          IN monitorizations_ea.id_monitorization%TYPE DEFAULT NULL,
        id_monitorization_nin         IN BOOLEAN := TRUE,
        id_monitorization_vs_plan_in  IN monitorizations_ea.id_monitorization_vs_plan%TYPE DEFAULT NULL,
        id_monitorization_vs_plan_nin IN BOOLEAN := TRUE,
        id_vital_sign_in              IN monitorizations_ea.id_vital_sign%TYPE DEFAULT NULL,
        id_vital_sign_nin             IN BOOLEAN := TRUE,
        flg_status_in                 IN monitorizations_ea.flg_status%TYPE DEFAULT NULL,
        flg_status_nin                IN BOOLEAN := TRUE,
        flg_status_det_in             IN monitorizations_ea.flg_status_det%TYPE DEFAULT NULL,
        flg_status_det_nin            IN BOOLEAN := TRUE,
        flg_status_plan_in            IN monitorizations_ea.flg_status_plan%TYPE DEFAULT NULL,
        flg_status_plan_nin           IN BOOLEAN := TRUE,
        flg_time_in                   IN monitorizations_ea.flg_time%TYPE DEFAULT NULL,
        flg_time_nin                  IN BOOLEAN := TRUE,
        dt_monitorization_in          IN monitorizations_ea.dt_monitorization%TYPE DEFAULT NULL,
        dt_monitorization_nin         IN BOOLEAN := TRUE,
        dt_plan_in                    IN monitorizations_ea.dt_plan%TYPE DEFAULT NULL,
        dt_plan_nin                   IN BOOLEAN := TRUE,
        interval_in                   IN monitorizations_ea.interval%TYPE DEFAULT NULL,
        interval_nin                  IN BOOLEAN := TRUE,
        id_episode_origin_in          IN monitorizations_ea.id_episode_origin%TYPE DEFAULT NULL,
        id_episode_origin_nin         IN BOOLEAN := TRUE,
        dt_begin_in                   IN monitorizations_ea.dt_begin%TYPE DEFAULT NULL,
        dt_begin_nin                  IN BOOLEAN := TRUE,
        dt_end_in                     IN monitorizations_ea.dt_end%TYPE DEFAULT NULL,
        dt_end_nin                    IN BOOLEAN := TRUE,
        num_monit_in                  IN monitorizations_ea.num_monit%TYPE DEFAULT NULL,
        num_monit_nin                 IN BOOLEAN := TRUE,
        id_visit_in                   IN monitorizations_ea.id_visit%TYPE DEFAULT NULL,
        id_visit_nin                  IN BOOLEAN := TRUE,
        status_str_in                 IN monitorizations_ea.status_str%TYPE DEFAULT NULL,
        status_str_nin                IN BOOLEAN := TRUE,
        status_msg_in                 IN monitorizations_ea.status_msg%TYPE DEFAULT NULL,
        status_msg_nin                IN BOOLEAN := TRUE,
        status_icon_in                IN monitorizations_ea.status_icon%TYPE DEFAULT NULL,
        status_icon_nin               IN BOOLEAN := TRUE,
        status_flg_in                 IN monitorizations_ea.status_flg%TYPE DEFAULT NULL,
        status_flg_nin                IN BOOLEAN := TRUE,
        flg_notes_in                  IN monitorizations_ea.flg_notes%TYPE DEFAULT NULL,
        flg_notes_nin                 IN BOOLEAN := TRUE,
        id_episode_in                 IN monitorizations_ea.id_episode%TYPE DEFAULT NULL,
        id_episode_nin                IN BOOLEAN := TRUE,
        id_prev_episode_in            IN monitorizations_ea.id_prev_episode%TYPE DEFAULT NULL,
        id_prev_episode_nin           IN BOOLEAN := TRUE,
        id_patient_in                 IN monitorizations_ea.id_patient%TYPE DEFAULT NULL,
        id_patient_nin                IN BOOLEAN := TRUE,
        id_professional_in            IN monitorizations_ea.id_professional%TYPE DEFAULT NULL,
        id_professional_nin           IN BOOLEAN := TRUE,
        dt_dg_last_update_in          IN monitorizations_ea.dt_dg_last_update%TYPE DEFAULT NULL,
        dt_dg_last_update_nin         IN BOOLEAN := TRUE,
        create_user_in                IN monitorizations_ea.create_user%TYPE DEFAULT NULL,
        create_user_nin               IN BOOLEAN := TRUE,
        create_time_in                IN monitorizations_ea.create_time%TYPE DEFAULT NULL,
        create_time_nin               IN BOOLEAN := TRUE,
        create_institution_in         IN monitorizations_ea.create_institution%TYPE DEFAULT NULL,
        create_institution_nin        IN BOOLEAN := TRUE,
        update_user_in                IN monitorizations_ea.update_user%TYPE DEFAULT NULL,
        update_user_nin               IN BOOLEAN := TRUE,
        update_time_in                IN monitorizations_ea.update_time%TYPE DEFAULT NULL,
        update_time_nin               IN BOOLEAN := TRUE,
        update_institution_in         IN monitorizations_ea.update_institution%TYPE DEFAULT NULL,
        update_institution_nin        IN BOOLEAN := TRUE,
        dt_order_in                   IN monitorizations_ea.dt_order%TYPE DEFAULT NULL,
        dt_order_nin                  IN BOOLEAN := TRUE,
        handle_error_in               IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        id_monitorization_in          IN monitorizations_ea.id_monitorization%TYPE DEFAULT NULL,
        id_monitorization_nin         IN BOOLEAN := TRUE,
        id_monitorization_vs_plan_in  IN monitorizations_ea.id_monitorization_vs_plan%TYPE DEFAULT NULL,
        id_monitorization_vs_plan_nin IN BOOLEAN := TRUE,
        id_vital_sign_in              IN monitorizations_ea.id_vital_sign%TYPE DEFAULT NULL,
        id_vital_sign_nin             IN BOOLEAN := TRUE,
        flg_status_in                 IN monitorizations_ea.flg_status%TYPE DEFAULT NULL,
        flg_status_nin                IN BOOLEAN := TRUE,
        flg_status_det_in             IN monitorizations_ea.flg_status_det%TYPE DEFAULT NULL,
        flg_status_det_nin            IN BOOLEAN := TRUE,
        flg_status_plan_in            IN monitorizations_ea.flg_status_plan%TYPE DEFAULT NULL,
        flg_status_plan_nin           IN BOOLEAN := TRUE,
        flg_time_in                   IN monitorizations_ea.flg_time%TYPE DEFAULT NULL,
        flg_time_nin                  IN BOOLEAN := TRUE,
        dt_monitorization_in          IN monitorizations_ea.dt_monitorization%TYPE DEFAULT NULL,
        dt_monitorization_nin         IN BOOLEAN := TRUE,
        dt_plan_in                    IN monitorizations_ea.dt_plan%TYPE DEFAULT NULL,
        dt_plan_nin                   IN BOOLEAN := TRUE,
        interval_in                   IN monitorizations_ea.interval%TYPE DEFAULT NULL,
        interval_nin                  IN BOOLEAN := TRUE,
        id_episode_origin_in          IN monitorizations_ea.id_episode_origin%TYPE DEFAULT NULL,
        id_episode_origin_nin         IN BOOLEAN := TRUE,
        dt_begin_in                   IN monitorizations_ea.dt_begin%TYPE DEFAULT NULL,
        dt_begin_nin                  IN BOOLEAN := TRUE,
        dt_end_in                     IN monitorizations_ea.dt_end%TYPE DEFAULT NULL,
        dt_end_nin                    IN BOOLEAN := TRUE,
        num_monit_in                  IN monitorizations_ea.num_monit%TYPE DEFAULT NULL,
        num_monit_nin                 IN BOOLEAN := TRUE,
        id_visit_in                   IN monitorizations_ea.id_visit%TYPE DEFAULT NULL,
        id_visit_nin                  IN BOOLEAN := TRUE,
        status_str_in                 IN monitorizations_ea.status_str%TYPE DEFAULT NULL,
        status_str_nin                IN BOOLEAN := TRUE,
        status_msg_in                 IN monitorizations_ea.status_msg%TYPE DEFAULT NULL,
        status_msg_nin                IN BOOLEAN := TRUE,
        status_icon_in                IN monitorizations_ea.status_icon%TYPE DEFAULT NULL,
        status_icon_nin               IN BOOLEAN := TRUE,
        status_flg_in                 IN monitorizations_ea.status_flg%TYPE DEFAULT NULL,
        status_flg_nin                IN BOOLEAN := TRUE,
        flg_notes_in                  IN monitorizations_ea.flg_notes%TYPE DEFAULT NULL,
        flg_notes_nin                 IN BOOLEAN := TRUE,
        id_episode_in                 IN monitorizations_ea.id_episode%TYPE DEFAULT NULL,
        id_episode_nin                IN BOOLEAN := TRUE,
        id_prev_episode_in            IN monitorizations_ea.id_prev_episode%TYPE DEFAULT NULL,
        id_prev_episode_nin           IN BOOLEAN := TRUE,
        id_patient_in                 IN monitorizations_ea.id_patient%TYPE DEFAULT NULL,
        id_patient_nin                IN BOOLEAN := TRUE,
        id_professional_in            IN monitorizations_ea.id_professional%TYPE DEFAULT NULL,
        id_professional_nin           IN BOOLEAN := TRUE,
        dt_dg_last_update_in          IN monitorizations_ea.dt_dg_last_update%TYPE DEFAULT NULL,
        dt_dg_last_update_nin         IN BOOLEAN := TRUE,
        create_user_in                IN monitorizations_ea.create_user%TYPE DEFAULT NULL,
        create_user_nin               IN BOOLEAN := TRUE,
        create_time_in                IN monitorizations_ea.create_time%TYPE DEFAULT NULL,
        create_time_nin               IN BOOLEAN := TRUE,
        create_institution_in         IN monitorizations_ea.create_institution%TYPE DEFAULT NULL,
        create_institution_nin        IN BOOLEAN := TRUE,
        update_user_in                IN monitorizations_ea.update_user%TYPE DEFAULT NULL,
        update_user_nin               IN BOOLEAN := TRUE,
        update_time_in                IN monitorizations_ea.update_time%TYPE DEFAULT NULL,
        update_time_nin               IN BOOLEAN := TRUE,
        update_institution_in         IN monitorizations_ea.update_institution%TYPE DEFAULT NULL,
        update_institution_nin        IN BOOLEAN := TRUE,
        dt_order_in                   IN monitorizations_ea.dt_order%TYPE DEFAULT NULL,
        dt_order_nin                  IN BOOLEAN := TRUE,
        where_in                      VARCHAR2 DEFAULT NULL,
        handle_error_in               IN BOOLEAN := TRUE,
        rows_out                      IN OUT table_varchar
    );

    PROCEDURE upd
    (
        id_monitorization_in          IN monitorizations_ea.id_monitorization%TYPE DEFAULT NULL,
        id_monitorization_nin         IN BOOLEAN := TRUE,
        id_monitorization_vs_plan_in  IN monitorizations_ea.id_monitorization_vs_plan%TYPE DEFAULT NULL,
        id_monitorization_vs_plan_nin IN BOOLEAN := TRUE,
        id_vital_sign_in              IN monitorizations_ea.id_vital_sign%TYPE DEFAULT NULL,
        id_vital_sign_nin             IN BOOLEAN := TRUE,
        flg_status_in                 IN monitorizations_ea.flg_status%TYPE DEFAULT NULL,
        flg_status_nin                IN BOOLEAN := TRUE,
        flg_status_det_in             IN monitorizations_ea.flg_status_det%TYPE DEFAULT NULL,
        flg_status_det_nin            IN BOOLEAN := TRUE,
        flg_status_plan_in            IN monitorizations_ea.flg_status_plan%TYPE DEFAULT NULL,
        flg_status_plan_nin           IN BOOLEAN := TRUE,
        flg_time_in                   IN monitorizations_ea.flg_time%TYPE DEFAULT NULL,
        flg_time_nin                  IN BOOLEAN := TRUE,
        dt_monitorization_in          IN monitorizations_ea.dt_monitorization%TYPE DEFAULT NULL,
        dt_monitorization_nin         IN BOOLEAN := TRUE,
        dt_plan_in                    IN monitorizations_ea.dt_plan%TYPE DEFAULT NULL,
        dt_plan_nin                   IN BOOLEAN := TRUE,
        interval_in                   IN monitorizations_ea.interval%TYPE DEFAULT NULL,
        interval_nin                  IN BOOLEAN := TRUE,
        id_episode_origin_in          IN monitorizations_ea.id_episode_origin%TYPE DEFAULT NULL,
        id_episode_origin_nin         IN BOOLEAN := TRUE,
        dt_begin_in                   IN monitorizations_ea.dt_begin%TYPE DEFAULT NULL,
        dt_begin_nin                  IN BOOLEAN := TRUE,
        dt_end_in                     IN monitorizations_ea.dt_end%TYPE DEFAULT NULL,
        dt_end_nin                    IN BOOLEAN := TRUE,
        num_monit_in                  IN monitorizations_ea.num_monit%TYPE DEFAULT NULL,
        num_monit_nin                 IN BOOLEAN := TRUE,
        id_visit_in                   IN monitorizations_ea.id_visit%TYPE DEFAULT NULL,
        id_visit_nin                  IN BOOLEAN := TRUE,
        status_str_in                 IN monitorizations_ea.status_str%TYPE DEFAULT NULL,
        status_str_nin                IN BOOLEAN := TRUE,
        status_msg_in                 IN monitorizations_ea.status_msg%TYPE DEFAULT NULL,
        status_msg_nin                IN BOOLEAN := TRUE,
        status_icon_in                IN monitorizations_ea.status_icon%TYPE DEFAULT NULL,
        status_icon_nin               IN BOOLEAN := TRUE,
        status_flg_in                 IN monitorizations_ea.status_flg%TYPE DEFAULT NULL,
        status_flg_nin                IN BOOLEAN := TRUE,
        flg_notes_in                  IN monitorizations_ea.flg_notes%TYPE DEFAULT NULL,
        flg_notes_nin                 IN BOOLEAN := TRUE,
        id_episode_in                 IN monitorizations_ea.id_episode%TYPE DEFAULT NULL,
        id_episode_nin                IN BOOLEAN := TRUE,
        id_prev_episode_in            IN monitorizations_ea.id_prev_episode%TYPE DEFAULT NULL,
        id_prev_episode_nin           IN BOOLEAN := TRUE,
        id_patient_in                 IN monitorizations_ea.id_patient%TYPE DEFAULT NULL,
        id_patient_nin                IN BOOLEAN := TRUE,
        id_professional_in            IN monitorizations_ea.id_professional%TYPE DEFAULT NULL,
        id_professional_nin           IN BOOLEAN := TRUE,
        dt_dg_last_update_in          IN monitorizations_ea.dt_dg_last_update%TYPE DEFAULT NULL,
        dt_dg_last_update_nin         IN BOOLEAN := TRUE,
        create_user_in                IN monitorizations_ea.create_user%TYPE DEFAULT NULL,
        create_user_nin               IN BOOLEAN := TRUE,
        create_time_in                IN monitorizations_ea.create_time%TYPE DEFAULT NULL,
        create_time_nin               IN BOOLEAN := TRUE,
        create_institution_in         IN monitorizations_ea.create_institution%TYPE DEFAULT NULL,
        create_institution_nin        IN BOOLEAN := TRUE,
        update_user_in                IN monitorizations_ea.update_user%TYPE DEFAULT NULL,
        update_user_nin               IN BOOLEAN := TRUE,
        update_time_in                IN monitorizations_ea.update_time%TYPE DEFAULT NULL,
        update_time_nin               IN BOOLEAN := TRUE,
        update_institution_in         IN monitorizations_ea.update_institution%TYPE DEFAULT NULL,
        update_institution_nin        IN BOOLEAN := TRUE,
        dt_order_in                   IN monitorizations_ea.dt_order%TYPE DEFAULT NULL,
        dt_order_nin                  IN BOOLEAN := TRUE,
        where_in                      VARCHAR2 DEFAULT NULL,
        handle_error_in               IN BOOLEAN := TRUE
    );

    PROCEDURE upd_ins
    (
        id_monitorization_vs_in      IN monitorizations_ea.id_monitorization_vs%TYPE,
        id_monitorization_in         IN monitorizations_ea.id_monitorization%TYPE DEFAULT NULL,
        id_monitorization_vs_plan_in IN monitorizations_ea.id_monitorization_vs_plan%TYPE DEFAULT NULL,
        id_vital_sign_in             IN monitorizations_ea.id_vital_sign%TYPE DEFAULT NULL,
        flg_status_in                IN monitorizations_ea.flg_status%TYPE DEFAULT NULL,
        flg_status_det_in            IN monitorizations_ea.flg_status_det%TYPE DEFAULT NULL,
        flg_status_plan_in           IN monitorizations_ea.flg_status_plan%TYPE DEFAULT NULL,
        flg_time_in                  IN monitorizations_ea.flg_time%TYPE DEFAULT NULL,
        dt_monitorization_in         IN monitorizations_ea.dt_monitorization%TYPE DEFAULT NULL,
        dt_plan_in                   IN monitorizations_ea.dt_plan%TYPE DEFAULT NULL,
        interval_in                  IN monitorizations_ea.interval%TYPE DEFAULT NULL,
        id_episode_origin_in         IN monitorizations_ea.id_episode_origin%TYPE DEFAULT NULL,
        dt_begin_in                  IN monitorizations_ea.dt_begin%TYPE DEFAULT NULL,
        dt_end_in                    IN monitorizations_ea.dt_end%TYPE DEFAULT NULL,
        num_monit_in                 IN monitorizations_ea.num_monit%TYPE DEFAULT NULL,
        id_visit_in                  IN monitorizations_ea.id_visit%TYPE DEFAULT NULL,
        status_str_in                IN monitorizations_ea.status_str%TYPE DEFAULT NULL,
        status_msg_in                IN monitorizations_ea.status_msg%TYPE DEFAULT NULL,
        status_icon_in               IN monitorizations_ea.status_icon%TYPE DEFAULT NULL,
        status_flg_in                IN monitorizations_ea.status_flg%TYPE DEFAULT NULL,
        flg_notes_in                 IN monitorizations_ea.flg_notes%TYPE DEFAULT NULL,
        id_episode_in                IN monitorizations_ea.id_episode%TYPE DEFAULT NULL,
        id_prev_episode_in           IN monitorizations_ea.id_prev_episode%TYPE DEFAULT NULL,
        id_patient_in                IN monitorizations_ea.id_patient%TYPE DEFAULT NULL,
        id_professional_in           IN monitorizations_ea.id_professional%TYPE DEFAULT NULL,
        dt_dg_last_update_in         IN monitorizations_ea.dt_dg_last_update%TYPE DEFAULT NULL,
        create_user_in               IN monitorizations_ea.create_user%TYPE DEFAULT NULL,
        create_time_in               IN monitorizations_ea.create_time%TYPE DEFAULT NULL,
        create_institution_in        IN monitorizations_ea.create_institution%TYPE DEFAULT NULL,
        update_user_in               IN monitorizations_ea.update_user%TYPE DEFAULT NULL,
        update_time_in               IN monitorizations_ea.update_time%TYPE DEFAULT NULL,
        update_institution_in        IN monitorizations_ea.update_institution%TYPE DEFAULT NULL,
        dt_order_in                  IN monitorizations_ea.dt_order%TYPE DEFAULT NULL,
        handle_error_in              IN BOOLEAN := TRUE,
        rows_out                     OUT table_varchar
    );

    PROCEDURE upd_ins
    (
        id_monitorization_vs_in      IN monitorizations_ea.id_monitorization_vs%TYPE,
        id_monitorization_in         IN monitorizations_ea.id_monitorization%TYPE DEFAULT NULL,
        id_monitorization_vs_plan_in IN monitorizations_ea.id_monitorization_vs_plan%TYPE DEFAULT NULL,
        id_vital_sign_in             IN monitorizations_ea.id_vital_sign%TYPE DEFAULT NULL,
        flg_status_in                IN monitorizations_ea.flg_status%TYPE DEFAULT NULL,
        flg_status_det_in            IN monitorizations_ea.flg_status_det%TYPE DEFAULT NULL,
        flg_status_plan_in           IN monitorizations_ea.flg_status_plan%TYPE DEFAULT NULL,
        flg_time_in                  IN monitorizations_ea.flg_time%TYPE DEFAULT NULL,
        dt_monitorization_in         IN monitorizations_ea.dt_monitorization%TYPE DEFAULT NULL,
        dt_plan_in                   IN monitorizations_ea.dt_plan%TYPE DEFAULT NULL,
        interval_in                  IN monitorizations_ea.interval%TYPE DEFAULT NULL,
        id_episode_origin_in         IN monitorizations_ea.id_episode_origin%TYPE DEFAULT NULL,
        dt_begin_in                  IN monitorizations_ea.dt_begin%TYPE DEFAULT NULL,
        dt_end_in                    IN monitorizations_ea.dt_end%TYPE DEFAULT NULL,
        num_monit_in                 IN monitorizations_ea.num_monit%TYPE DEFAULT NULL,
        id_visit_in                  IN monitorizations_ea.id_visit%TYPE DEFAULT NULL,
        status_str_in                IN monitorizations_ea.status_str%TYPE DEFAULT NULL,
        status_msg_in                IN monitorizations_ea.status_msg%TYPE DEFAULT NULL,
        status_icon_in               IN monitorizations_ea.status_icon%TYPE DEFAULT NULL,
        status_flg_in                IN monitorizations_ea.status_flg%TYPE DEFAULT NULL,
        flg_notes_in                 IN monitorizations_ea.flg_notes%TYPE DEFAULT NULL,
        id_episode_in                IN monitorizations_ea.id_episode%TYPE DEFAULT NULL,
        id_prev_episode_in           IN monitorizations_ea.id_prev_episode%TYPE DEFAULT NULL,
        id_patient_in                IN monitorizations_ea.id_patient%TYPE DEFAULT NULL,
        id_professional_in           IN monitorizations_ea.id_professional%TYPE DEFAULT NULL,
        dt_dg_last_update_in         IN monitorizations_ea.dt_dg_last_update%TYPE DEFAULT NULL,
        create_user_in               IN monitorizations_ea.create_user%TYPE DEFAULT NULL,
        create_time_in               IN monitorizations_ea.create_time%TYPE DEFAULT NULL,
        create_institution_in        IN monitorizations_ea.create_institution%TYPE DEFAULT NULL,
        update_user_in               IN monitorizations_ea.update_user%TYPE DEFAULT NULL,
        update_time_in               IN monitorizations_ea.update_time%TYPE DEFAULT NULL,
        update_institution_in        IN monitorizations_ea.update_institution%TYPE DEFAULT NULL,
        dt_order_in                  IN monitorizations_ea.dt_order%TYPE DEFAULT NULL,
        handle_error_in              IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        rec_in          IN monitorizations_ea%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        IN OUT table_varchar
    );

    PROCEDURE upd
    (
        rec_in          IN monitorizations_ea%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        col_in            IN monitorizations_ea_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          IN OUT table_varchar
    );

    PROCEDURE upd
    (
        col_in            IN monitorizations_ea_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE
    );

    -- Use Native Dynamic SQL increment a single NUMBER column
    -- for all rows specified by the dynamic WHERE clause
    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_monitorization_vs_in IN monitorizations_ea.id_monitorization_vs%TYPE,
        handle_error_in         IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_monitorization_vs_in IN monitorizations_ea.id_monitorization_vs%TYPE,
        handle_error_in         IN BOOLEAN := TRUE,
        rows_out                OUT table_varchar
    );

    -- Delete all rows for primary key column ID_MONITORIZATION_VS
    PROCEDURE del_id_monitorization_vs
    (
        id_monitorization_vs_in IN monitorizations_ea.id_monitorization_vs%TYPE,
        handle_error_in         IN BOOLEAN := TRUE
    );

    -- Delete all rows for primary key column ID_MONITORIZATION_VS
    PROCEDURE del_id_monitorization_vs
    (
        id_monitorization_vs_in IN monitorizations_ea.id_monitorization_vs%TYPE,
        handle_error_in         IN BOOLEAN := TRUE,
        rows_out                OUT table_varchar
    );

    -- Delete all rows for this MEA_EPIS_FK foreign key value
    PROCEDURE del_mea_epis_fk
    (
        id_episode_in   IN monitorizations_ea.id_episode%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this MEA_EPIS_FK foreign key value
    PROCEDURE del_mea_epis_fk
    (
        id_episode_in   IN monitorizations_ea.id_episode%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for this MEA_EPIS_ORIGIN_FK foreign key value
    PROCEDURE del_mea_epis_origin_fk
    (
        id_episode_origin_in IN monitorizations_ea.id_episode_origin%TYPE,
        handle_error_in      IN BOOLEAN := TRUE
    );

    -- Delete all rows for this MEA_EPIS_ORIGIN_FK foreign key value
    PROCEDURE del_mea_epis_origin_fk
    (
        id_episode_origin_in IN monitorizations_ea.id_episode_origin%TYPE,
        handle_error_in      IN BOOLEAN := TRUE,
        rows_out             OUT table_varchar
    );

    -- Delete all rows for this MEA_EPIS_PREV_FK foreign key value
    PROCEDURE del_mea_epis_prev_fk
    (
        id_prev_episode_in IN monitorizations_ea.id_prev_episode%TYPE,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete all rows for this MEA_EPIS_PREV_FK foreign key value
    PROCEDURE del_mea_epis_prev_fk
    (
        id_prev_episode_in IN monitorizations_ea.id_prev_episode%TYPE,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    -- Delete all rows for this MEA_MONT_FK foreign key value
    PROCEDURE del_mea_mont_fk
    (
        id_monitorization_in IN monitorizations_ea.id_monitorization%TYPE,
        handle_error_in      IN BOOLEAN := TRUE
    );

    -- Delete all rows for this MEA_MONT_FK foreign key value
    PROCEDURE del_mea_mont_fk
    (
        id_monitorization_in IN monitorizations_ea.id_monitorization%TYPE,
        handle_error_in      IN BOOLEAN := TRUE,
        rows_out             OUT table_varchar
    );

    -- Delete all rows for this MEA_MVS_FK foreign key value
    PROCEDURE del_mea_mvs_fk
    (
        id_monitorization_vs_in IN monitorizations_ea.id_monitorization_vs%TYPE,
        handle_error_in         IN BOOLEAN := TRUE
    );

    -- Delete all rows for this MEA_MVS_FK foreign key value
    PROCEDURE del_mea_mvs_fk
    (
        id_monitorization_vs_in IN monitorizations_ea.id_monitorization_vs%TYPE,
        handle_error_in         IN BOOLEAN := TRUE,
        rows_out                OUT table_varchar
    );

    -- Delete all rows for this MEA_MVSP_FK foreign key value
    PROCEDURE del_mea_mvsp_fk
    (
        id_monitorization_vs_plan_in IN monitorizations_ea.id_monitorization_vs_plan%TYPE,
        handle_error_in              IN BOOLEAN := TRUE
    );

    -- Delete all rows for this MEA_MVSP_FK foreign key value
    PROCEDURE del_mea_mvsp_fk
    (
        id_monitorization_vs_plan_in IN monitorizations_ea.id_monitorization_vs_plan%TYPE,
        handle_error_in              IN BOOLEAN := TRUE,
        rows_out                     OUT table_varchar
    );

    -- Delete all rows for this MEA_PAT_FK foreign key value
    PROCEDURE del_mea_pat_fk
    (
        id_patient_in   IN monitorizations_ea.id_patient%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this MEA_PAT_FK foreign key value
    PROCEDURE del_mea_pat_fk
    (
        id_patient_in   IN monitorizations_ea.id_patient%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for this MEA_PROF_FK foreign key value
    PROCEDURE del_mea_prof_fk
    (
        id_professional_in IN monitorizations_ea.id_professional%TYPE,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete all rows for this MEA_PROF_FK foreign key value
    PROCEDURE del_mea_prof_fk
    (
        id_professional_in IN monitorizations_ea.id_professional%TYPE,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    -- Delete all rows for this MEA_VIS_FK foreign key value
    PROCEDURE del_mea_vis_fk
    (
        id_visit_in     IN monitorizations_ea.id_visit%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this MEA_VIS_FK foreign key value
    PROCEDURE del_mea_vis_fk
    (
        id_visit_in     IN monitorizations_ea.id_visit%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for this MEA_VS_FK foreign key value
    PROCEDURE del_mea_vs_fk
    (
        id_vital_sign_in IN monitorizations_ea.id_vital_sign%TYPE,
        handle_error_in  IN BOOLEAN := TRUE
    );

    -- Delete all rows for this MEA_VS_FK foreign key value
    PROCEDURE del_mea_vs_fk
    (
        id_vital_sign_in IN monitorizations_ea.id_vital_sign%TYPE,
        handle_error_in  IN BOOLEAN := TRUE,
        rows_out         OUT table_varchar
    );

    -- Delete all rows for this TMP$$_MEA_VIS_FK0 foreign key value
    PROCEDURE del_tmp$$_mea_vis_fk0
    (
        id_visit_in     IN monitorizations_ea.id_visit%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this TMP$$_MEA_VIS_FK0 foreign key value
    PROCEDURE del_tmp$$_mea_vis_fk0
    (
        id_visit_in     IN monitorizations_ea.id_visit%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Initialize a record with default values for columns in the table.
    PROCEDURE initrec(monitorizations_ea_inout IN OUT monitorizations_ea%ROWTYPE);

    FUNCTION initrec RETURN monitorizations_ea%ROWTYPE;

    FUNCTION get_data_rowid(rows_in IN table_varchar) RETURN monitorizations_ea_tc;

    FUNCTION get_data_rowid_pat(rows_in IN table_varchar) RETURN monitorizations_ea_tc;

END ts_monitorizations_ea;
/
