/*-- Last Change Revision: $Rev: 413863 $*/
/*-- Last Change by: $Author: alexandre.santos $*/
/*-- Date of last change: $Date: 2010-02-22 11:23:14 +0000 (seg, 22 fev 2010) $*/

CREATE OR REPLACE PACKAGE ts_sys_list_group
/*
| Generated by or retrieved from QCGU - DO NOT MODIFY!
| QCGU - "Get it right, do it fast" - www.ToadWorld.com
| QCGU Universal ID: {1BD37A66-EA60-4927-9A64-F6DD89237236}
| Created On: Fevereiro 17, 2010 15:12:55
| Created By: ALERT
*/
 IS

    -- Collection of %ROWTYPE records based on "SYS_LIST_GROUP"
    TYPE sys_list_group_tc IS TABLE OF sys_list_group%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE sys_list_group_ntt IS TABLE OF sys_list_group%ROWTYPE;
    TYPE sys_list_group_vat IS VARRAY(100) OF sys_list_group%ROWTYPE;

    -- Same type structure, with a static name.
    TYPE aat IS TABLE OF sys_list_group%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE ntt IS TABLE OF sys_list_group%ROWTYPE;
    TYPE vat IS VARRAY(100) OF sys_list_group%ROWTYPE;

    -- Column Collection based on column "ID_SYS_LIST_GROUP"
    TYPE id_sys_list_group_cc IS TABLE OF sys_list_group.id_sys_list_group%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "INTERNAL_NAME"
    TYPE internal_name_cc IS TABLE OF sys_list_group.internal_name%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "INTERNAL_DESC"
    TYPE internal_desc_cc IS TABLE OF sys_list_group.internal_desc%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_USER"
    TYPE create_user_cc IS TABLE OF sys_list_group.create_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_TIME"
    TYPE create_time_cc IS TABLE OF sys_list_group.create_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_INSTITUTION"
    TYPE create_institution_cc IS TABLE OF sys_list_group.create_institution%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_USER"
    TYPE update_user_cc IS TABLE OF sys_list_group.update_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_TIME"
    TYPE update_time_cc IS TABLE OF sys_list_group.update_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_INSTITUTION"
    TYPE update_institution_cc IS TABLE OF sys_list_group.update_institution%TYPE INDEX BY BINARY_INTEGER;

    -- Insert one row, providing primary key if present
    PROCEDURE ins
    (
        id_sys_list_group_in  IN sys_list_group.id_sys_list_group%TYPE,
        internal_name_in      IN sys_list_group.internal_name%TYPE DEFAULT NULL,
        internal_desc_in      IN sys_list_group.internal_desc%TYPE DEFAULT NULL,
        create_user_in        IN sys_list_group.create_user%TYPE DEFAULT NULL,
        create_time_in        IN sys_list_group.create_time%TYPE DEFAULT NULL,
        create_institution_in IN sys_list_group.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN sys_list_group.update_user%TYPE DEFAULT NULL,
        update_time_in        IN sys_list_group.update_time%TYPE DEFAULT NULL,
        update_institution_in IN sys_list_group.update_institution%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_sys_list_group_in  IN sys_list_group.id_sys_list_group%TYPE,
        internal_name_in      IN sys_list_group.internal_name%TYPE DEFAULT NULL,
        internal_desc_in      IN sys_list_group.internal_desc%TYPE DEFAULT NULL,
        create_user_in        IN sys_list_group.create_user%TYPE DEFAULT NULL,
        create_time_in        IN sys_list_group.create_time%TYPE DEFAULT NULL,
        create_institution_in IN sys_list_group.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN sys_list_group.update_user%TYPE DEFAULT NULL,
        update_time_in        IN sys_list_group.update_time%TYPE DEFAULT NULL,
        update_institution_in IN sys_list_group.update_institution%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a row based on a record.
    -- Specify whether or not a primary key value should be generated.
    PROCEDURE ins
    (
        rec_in          IN sys_list_group%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rec_in          IN sys_list_group%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a collection of rows using FORALL; all primary key values
    -- must have already been generated, or are handled in triggers.
    PROCEDURE ins
    (
        rows_in         IN sys_list_group_tc,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rows_in         IN sys_list_group_tc,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Return next primary key value using the named sequence.
    FUNCTION next_key(sequence_in IN VARCHAR2 := NULL) RETURN sys_list_group.id_sys_list_group%TYPE;

    -- Insert one row, generating hidden primary key using a sequence
    PROCEDURE ins
    (
        internal_name_in      IN sys_list_group.internal_name%TYPE DEFAULT NULL,
        internal_desc_in      IN sys_list_group.internal_desc%TYPE DEFAULT NULL,
        create_user_in        IN sys_list_group.create_user%TYPE DEFAULT NULL,
        create_time_in        IN sys_list_group.create_time%TYPE DEFAULT NULL,
        create_institution_in IN sys_list_group.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN sys_list_group.update_user%TYPE DEFAULT NULL,
        update_time_in        IN sys_list_group.update_time%TYPE DEFAULT NULL,
        update_institution_in IN sys_list_group.update_institution%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        internal_name_in      IN sys_list_group.internal_name%TYPE DEFAULT NULL,
        internal_desc_in      IN sys_list_group.internal_desc%TYPE DEFAULT NULL,
        create_user_in        IN sys_list_group.create_user%TYPE DEFAULT NULL,
        create_time_in        IN sys_list_group.create_time%TYPE DEFAULT NULL,
        create_institution_in IN sys_list_group.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN sys_list_group.update_user%TYPE DEFAULT NULL,
        update_time_in        IN sys_list_group.update_time%TYPE DEFAULT NULL,
        update_institution_in IN sys_list_group.update_institution%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert one row, returning primary key generated by sequence
    PROCEDURE ins
    (
        internal_name_in      IN sys_list_group.internal_name%TYPE DEFAULT NULL,
        internal_desc_in      IN sys_list_group.internal_desc%TYPE DEFAULT NULL,
        create_user_in        IN sys_list_group.create_user%TYPE DEFAULT NULL,
        create_time_in        IN sys_list_group.create_time%TYPE DEFAULT NULL,
        create_institution_in IN sys_list_group.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN sys_list_group.update_user%TYPE DEFAULT NULL,
        update_time_in        IN sys_list_group.update_time%TYPE DEFAULT NULL,
        update_institution_in IN sys_list_group.update_institution%TYPE DEFAULT NULL,
        id_sys_list_group_out IN OUT sys_list_group.id_sys_list_group%TYPE
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        internal_name_in      IN sys_list_group.internal_name%TYPE DEFAULT NULL,
        internal_desc_in      IN sys_list_group.internal_desc%TYPE DEFAULT NULL,
        create_user_in        IN sys_list_group.create_user%TYPE DEFAULT NULL,
        create_time_in        IN sys_list_group.create_time%TYPE DEFAULT NULL,
        create_institution_in IN sys_list_group.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN sys_list_group.update_user%TYPE DEFAULT NULL,
        update_time_in        IN sys_list_group.update_time%TYPE DEFAULT NULL,
        update_institution_in IN sys_list_group.update_institution%TYPE DEFAULT NULL,
        id_sys_list_group_out IN OUT sys_list_group.id_sys_list_group%TYPE
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert one row with function, return generated primary key
    FUNCTION ins
    (
        internal_name_in      IN sys_list_group.internal_name%TYPE DEFAULT NULL,
        internal_desc_in      IN sys_list_group.internal_desc%TYPE DEFAULT NULL,
        create_user_in        IN sys_list_group.create_user%TYPE DEFAULT NULL,
        create_time_in        IN sys_list_group.create_time%TYPE DEFAULT NULL,
        create_institution_in IN sys_list_group.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN sys_list_group.update_user%TYPE DEFAULT NULL,
        update_time_in        IN sys_list_group.update_time%TYPE DEFAULT NULL,
        update_institution_in IN sys_list_group.update_institution%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) RETURN sys_list_group.id_sys_list_group%TYPE;

    FUNCTION ins
    (
        internal_name_in      IN sys_list_group.internal_name%TYPE DEFAULT NULL,
        internal_desc_in      IN sys_list_group.internal_desc%TYPE DEFAULT NULL,
        create_user_in        IN sys_list_group.create_user%TYPE DEFAULT NULL,
        create_time_in        IN sys_list_group.create_time%TYPE DEFAULT NULL,
        create_institution_in IN sys_list_group.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN sys_list_group.update_user%TYPE DEFAULT NULL,
        update_time_in        IN sys_list_group.update_time%TYPE DEFAULT NULL,
        update_institution_in IN sys_list_group.update_institution%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    ) RETURN sys_list_group.id_sys_list_group%TYPE;

    -- Update any/all columns by primary key. If you pass NULL, then
    -- the current column value is set to itself. If you need a more
    -- selected UPDATE then use one of the onecol procedures below.

    PROCEDURE upd
    (
        id_sys_list_group_in   IN sys_list_group.id_sys_list_group%TYPE,
        internal_name_in       IN sys_list_group.internal_name%TYPE DEFAULT NULL,
        internal_name_nin      IN BOOLEAN := TRUE,
        internal_desc_in       IN sys_list_group.internal_desc%TYPE DEFAULT NULL,
        internal_desc_nin      IN BOOLEAN := TRUE,
        create_user_in         IN sys_list_group.create_user%TYPE DEFAULT NULL,
        create_user_nin        IN BOOLEAN := TRUE,
        create_time_in         IN sys_list_group.create_time%TYPE DEFAULT NULL,
        create_time_nin        IN BOOLEAN := TRUE,
        create_institution_in  IN sys_list_group.create_institution%TYPE DEFAULT NULL,
        create_institution_nin IN BOOLEAN := TRUE,
        update_user_in         IN sys_list_group.update_user%TYPE DEFAULT NULL,
        update_user_nin        IN BOOLEAN := TRUE,
        update_time_in         IN sys_list_group.update_time%TYPE DEFAULT NULL,
        update_time_nin        IN BOOLEAN := TRUE,
        update_institution_in  IN sys_list_group.update_institution%TYPE DEFAULT NULL,
        update_institution_nin IN BOOLEAN := TRUE,
        handle_error_in        IN BOOLEAN := TRUE,
        rows_out               IN OUT table_varchar
    );

    PROCEDURE upd
    (
        id_sys_list_group_in   IN sys_list_group.id_sys_list_group%TYPE,
        internal_name_in       IN sys_list_group.internal_name%TYPE DEFAULT NULL,
        internal_name_nin      IN BOOLEAN := TRUE,
        internal_desc_in       IN sys_list_group.internal_desc%TYPE DEFAULT NULL,
        internal_desc_nin      IN BOOLEAN := TRUE,
        create_user_in         IN sys_list_group.create_user%TYPE DEFAULT NULL,
        create_user_nin        IN BOOLEAN := TRUE,
        create_time_in         IN sys_list_group.create_time%TYPE DEFAULT NULL,
        create_time_nin        IN BOOLEAN := TRUE,
        create_institution_in  IN sys_list_group.create_institution%TYPE DEFAULT NULL,
        create_institution_nin IN BOOLEAN := TRUE,
        update_user_in         IN sys_list_group.update_user%TYPE DEFAULT NULL,
        update_user_nin        IN BOOLEAN := TRUE,
        update_time_in         IN sys_list_group.update_time%TYPE DEFAULT NULL,
        update_time_nin        IN BOOLEAN := TRUE,
        update_institution_in  IN sys_list_group.update_institution%TYPE DEFAULT NULL,
        update_institution_nin IN BOOLEAN := TRUE,
        handle_error_in        IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        internal_name_in       IN sys_list_group.internal_name%TYPE DEFAULT NULL,
        internal_name_nin      IN BOOLEAN := TRUE,
        internal_desc_in       IN sys_list_group.internal_desc%TYPE DEFAULT NULL,
        internal_desc_nin      IN BOOLEAN := TRUE,
        create_user_in         IN sys_list_group.create_user%TYPE DEFAULT NULL,
        create_user_nin        IN BOOLEAN := TRUE,
        create_time_in         IN sys_list_group.create_time%TYPE DEFAULT NULL,
        create_time_nin        IN BOOLEAN := TRUE,
        create_institution_in  IN sys_list_group.create_institution%TYPE DEFAULT NULL,
        create_institution_nin IN BOOLEAN := TRUE,
        update_user_in         IN sys_list_group.update_user%TYPE DEFAULT NULL,
        update_user_nin        IN BOOLEAN := TRUE,
        update_time_in         IN sys_list_group.update_time%TYPE DEFAULT NULL,
        update_time_nin        IN BOOLEAN := TRUE,
        update_institution_in  IN sys_list_group.update_institution%TYPE DEFAULT NULL,
        update_institution_nin IN BOOLEAN := TRUE,
        where_in               VARCHAR2 DEFAULT NULL,
        handle_error_in        IN BOOLEAN := TRUE,
        rows_out               IN OUT table_varchar
    );

    PROCEDURE upd
    (
        internal_name_in       IN sys_list_group.internal_name%TYPE DEFAULT NULL,
        internal_name_nin      IN BOOLEAN := TRUE,
        internal_desc_in       IN sys_list_group.internal_desc%TYPE DEFAULT NULL,
        internal_desc_nin      IN BOOLEAN := TRUE,
        create_user_in         IN sys_list_group.create_user%TYPE DEFAULT NULL,
        create_user_nin        IN BOOLEAN := TRUE,
        create_time_in         IN sys_list_group.create_time%TYPE DEFAULT NULL,
        create_time_nin        IN BOOLEAN := TRUE,
        create_institution_in  IN sys_list_group.create_institution%TYPE DEFAULT NULL,
        create_institution_nin IN BOOLEAN := TRUE,
        update_user_in         IN sys_list_group.update_user%TYPE DEFAULT NULL,
        update_user_nin        IN BOOLEAN := TRUE,
        update_time_in         IN sys_list_group.update_time%TYPE DEFAULT NULL,
        update_time_nin        IN BOOLEAN := TRUE,
        update_institution_in  IN sys_list_group.update_institution%TYPE DEFAULT NULL,
        update_institution_nin IN BOOLEAN := TRUE,
        where_in               VARCHAR2 DEFAULT NULL,
        handle_error_in        IN BOOLEAN := TRUE
    );

    PROCEDURE upd_ins
    (
        id_sys_list_group_in  IN sys_list_group.id_sys_list_group%TYPE,
        internal_name_in      IN sys_list_group.internal_name%TYPE DEFAULT NULL,
        internal_desc_in      IN sys_list_group.internal_desc%TYPE DEFAULT NULL,
        create_user_in        IN sys_list_group.create_user%TYPE DEFAULT NULL,
        create_time_in        IN sys_list_group.create_time%TYPE DEFAULT NULL,
        create_institution_in IN sys_list_group.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN sys_list_group.update_user%TYPE DEFAULT NULL,
        update_time_in        IN sys_list_group.update_time%TYPE DEFAULT NULL,
        update_institution_in IN sys_list_group.update_institution%TYPE DEFAULT NULL,
        handle_error_in       IN BOOLEAN := TRUE,
        rows_out              OUT table_varchar
    );

    PROCEDURE upd_ins
    (
        id_sys_list_group_in  IN sys_list_group.id_sys_list_group%TYPE,
        internal_name_in      IN sys_list_group.internal_name%TYPE DEFAULT NULL,
        internal_desc_in      IN sys_list_group.internal_desc%TYPE DEFAULT NULL,
        create_user_in        IN sys_list_group.create_user%TYPE DEFAULT NULL,
        create_time_in        IN sys_list_group.create_time%TYPE DEFAULT NULL,
        create_institution_in IN sys_list_group.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN sys_list_group.update_user%TYPE DEFAULT NULL,
        update_time_in        IN sys_list_group.update_time%TYPE DEFAULT NULL,
        update_institution_in IN sys_list_group.update_institution%TYPE DEFAULT NULL,
        handle_error_in       IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        rec_in          IN sys_list_group%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        IN OUT table_varchar
    );

    PROCEDURE upd
    (
        rec_in          IN sys_list_group%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        col_in            IN sys_list_group_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          IN OUT table_varchar
    );

    PROCEDURE upd
    (
        col_in            IN sys_list_group_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE
    );

    -- Use Native Dynamic SQL increment a single NUMBER column
    -- for all rows specified by the dynamic WHERE clause
    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_sys_list_group_in IN sys_list_group.id_sys_list_group%TYPE,
        handle_error_in      IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_sys_list_group_in IN sys_list_group.id_sys_list_group%TYPE,
        handle_error_in      IN BOOLEAN := TRUE,
        rows_out             OUT table_varchar
    );

    -- Delete all rows for primary key column ID_SYS_LIST_GROUP
    PROCEDURE del_id_sys_list_group
    (
        id_sys_list_group_in IN sys_list_group.id_sys_list_group%TYPE,
        handle_error_in      IN BOOLEAN := TRUE
    );

    -- Delete all rows for primary key column ID_SYS_LIST_GROUP
    PROCEDURE del_id_sys_list_group
    (
        id_sys_list_group_in IN sys_list_group.id_sys_list_group%TYPE,
        handle_error_in      IN BOOLEAN := TRUE,
        rows_out             OUT table_varchar
    );

    -- Delete for unique value of SLG_INT_NAME_IDX
    PROCEDURE del_slg_int_name_idx
    (
        internal_name_in IN sys_list_group.internal_name%TYPE,
        handle_error_in  IN BOOLEAN := TRUE
    );

    -- Delete for unique value of SLG_INT_NAME_IDX
    PROCEDURE del_slg_int_name_idx
    (
        internal_name_in IN sys_list_group.internal_name%TYPE,
        handle_error_in  IN BOOLEAN := TRUE,
        rows_out         OUT table_varchar
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Initialize a record with default values for columns in the table.
    PROCEDURE initrec(sys_list_group_inout IN OUT sys_list_group%ROWTYPE);

    FUNCTION initrec RETURN sys_list_group%ROWTYPE;

    FUNCTION get_data_rowid(rows_in IN table_varchar) RETURN sys_list_group_tc;

    FUNCTION get_data_rowid_pat(rows_in IN table_varchar) RETURN sys_list_group_tc;

END ts_sys_list_group;
/
