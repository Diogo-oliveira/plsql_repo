/*-- Last Change Revision: $Rev: 2029385 $*/
/*-- Last Change by: $Author: mario.fernandes $*/
/*-- Date of last change: $Date: 2022-08-02 18:51:22 +0100 (ter, 02 ago 2022) $*/
CREATE OR REPLACE PACKAGE ts_sr_supply_relation
/*
| Generated by or retrieved from QCGU - DO NOT MODIFY!
| QCGU - "Get it right, do it fast" - www.ToadWorld.com
| QCGU Universal ID: {1BD37A66-EA60-4927-9A64-F6DD89237236}
| Created On: Novembro 5, 2010 17:35:56
| Created By: ALERT
*/
 IS

    -- Collection of %ROWTYPE records based on "SR_SUPPLY_RELATION"
    TYPE sr_supply_relation_tc IS TABLE OF sr_supply_relation%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE sr_supply_relation_ntt IS TABLE OF sr_supply_relation%ROWTYPE;
    TYPE sr_supply_relation_vat IS VARRAY(100) OF sr_supply_relation%ROWTYPE;

    -- Same type structure, with a static name.
    TYPE aat IS TABLE OF sr_supply_relation%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE ntt IS TABLE OF sr_supply_relation%ROWTYPE;
    TYPE vat IS VARRAY(100) OF sr_supply_relation%ROWTYPE;

    -- Column Collection based on column "ID_SUPPLY_WORKFLOW"
    TYPE id_supply_workflow_cc IS TABLE OF sr_supply_relation.id_supply_workflow%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_SR_SUPPLY_COUNT"
    TYPE id_sr_supply_count_cc IS TABLE OF sr_supply_relation.id_sr_supply_count%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_USER"
    TYPE create_user_cc IS TABLE OF sr_supply_relation.create_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_TIME"
    TYPE create_time_cc IS TABLE OF sr_supply_relation.create_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_INSTITUTION"
    TYPE create_institution_cc IS TABLE OF sr_supply_relation.create_institution%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_USER"
    TYPE update_user_cc IS TABLE OF sr_supply_relation.update_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_TIME"
    TYPE update_time_cc IS TABLE OF sr_supply_relation.update_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_INSTITUTION"
    TYPE update_institution_cc IS TABLE OF sr_supply_relation.update_institution%TYPE INDEX BY BINARY_INTEGER;

    -- Insert one row, providing primary key if present
    PROCEDURE ins
    (
        id_supply_workflow_in IN sr_supply_relation.id_supply_workflow%TYPE DEFAULT NULL,
        id_sr_supply_count_in IN sr_supply_relation.id_sr_supply_count%TYPE DEFAULT NULL,
        create_user_in        IN sr_supply_relation.create_user%TYPE DEFAULT NULL,
        create_time_in        IN sr_supply_relation.create_time%TYPE DEFAULT NULL,
        create_institution_in IN sr_supply_relation.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN sr_supply_relation.update_user%TYPE DEFAULT NULL,
        update_time_in        IN sr_supply_relation.update_time%TYPE DEFAULT NULL,
        update_institution_in IN sr_supply_relation.update_institution%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_supply_workflow_in IN sr_supply_relation.id_supply_workflow%TYPE DEFAULT NULL,
        id_sr_supply_count_in IN sr_supply_relation.id_sr_supply_count%TYPE DEFAULT NULL,
        create_user_in        IN sr_supply_relation.create_user%TYPE DEFAULT NULL,
        create_time_in        IN sr_supply_relation.create_time%TYPE DEFAULT NULL,
        create_institution_in IN sr_supply_relation.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN sr_supply_relation.update_user%TYPE DEFAULT NULL,
        update_time_in        IN sr_supply_relation.update_time%TYPE DEFAULT NULL,
        update_institution_in IN sr_supply_relation.update_institution%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a row based on a record.
    PROCEDURE ins
    (
        rec_in          IN sr_supply_relation%ROWTYPE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rec_in          IN sr_supply_relation%ROWTYPE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a collection of rows using FORALL; all primary key values
    -- must have already been generated, or are handled in triggers.
    PROCEDURE ins
    (
        rows_in         IN sr_supply_relation_tc,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rows_in         IN sr_supply_relation_tc,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Use Native Dynamic SQL increment a single NUMBER column
    -- for all rows specified by the dynamic WHERE clause
    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete all rows for this SSR_SUP_COUNT_FK foreign key value
    PROCEDURE del_ssr_sup_count_fk
    (
        id_sr_supply_count_in IN sr_supply_relation.id_sr_supply_count%TYPE,
        handle_error_in       IN BOOLEAN := TRUE
    );

    -- Delete all rows for this SSR_SUP_COUNT_FK foreign key value
    PROCEDURE del_ssr_sup_count_fk
    (
        id_sr_supply_count_in IN sr_supply_relation.id_sr_supply_count%TYPE,
        handle_error_in       IN BOOLEAN := TRUE,
        rows_out              OUT table_varchar
    );

    -- Delete all rows for this SSR_SUP_WORK_FK foreign key value
    PROCEDURE del_ssr_sup_work_fk
    (
        id_supply_workflow_in IN sr_supply_relation.id_supply_workflow%TYPE,
        handle_error_in       IN BOOLEAN := TRUE
    );

    -- Delete all rows for this SSR_SUP_WORK_FK foreign key value
    PROCEDURE del_ssr_sup_work_fk
    (
        id_supply_workflow_in IN sr_supply_relation.id_supply_workflow%TYPE,
        handle_error_in       IN BOOLEAN := TRUE,
        rows_out              OUT table_varchar
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Initialize a record with default values for columns in the table.
    PROCEDURE initrec(sr_supply_relation_inout IN OUT sr_supply_relation%ROWTYPE);

    FUNCTION initrec RETURN sr_supply_relation%ROWTYPE;

    FUNCTION get_data_rowid(rows_in IN table_varchar) RETURN sr_supply_relation_tc;

    FUNCTION get_data_rowid_pat(rows_in IN table_varchar) RETURN sr_supply_relation_tc;

END ts_sr_supply_relation;
/
