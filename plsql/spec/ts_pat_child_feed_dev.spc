/*-- Last Change Revision: $Rev: 2029280 $*/
/*-- Last Change by: $Author: mario.fernandes $*/
/*-- Date of last change: $Date: 2022-08-02 18:50:47 +0100 (ter, 02 ago 2022) $*/

CREATE OR REPLACE PACKAGE ts_pat_child_feed_dev
/*
| Generated by or retrieved from QCGU - DO NOT MODIFY!
| QCGU - "Get it right, do it fast" - www.ToadWorld.com
| QCGU Universal ID: {1BD37A66-EA60-4927-9A64-F6DD89237236}
| Created On: Novembro 21, 2008 18:24:7
| Created By: ALERT
*/
 IS

    -- Collection of %ROWTYPE records based on "PAT_CHILD_FEED_DEV"
    TYPE pat_child_feed_dev_tc IS TABLE OF pat_child_feed_dev%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE pat_child_feed_dev_ntt IS TABLE OF pat_child_feed_dev%ROWTYPE;
    TYPE pat_child_feed_dev_vat IS VARRAY(100) OF pat_child_feed_dev%ROWTYPE;

    -- Same type structure, with a static name.
    TYPE aat IS TABLE OF pat_child_feed_dev%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE ntt IS TABLE OF pat_child_feed_dev%ROWTYPE;
    TYPE vat IS VARRAY(100) OF pat_child_feed_dev%ROWTYPE;

    -- Column Collection based on column "ID_PAT_CHILD_FEED_DEV"
    TYPE id_pat_child_feed_dev_cc IS TABLE OF pat_child_feed_dev.id_pat_child_feed_dev%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "DT_PAT_CHILD_FEED_DEV"
    TYPE dt_pat_child_feed_dev_cc IS TABLE OF pat_child_feed_dev.dt_pat_child_feed_dev%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_PATIENT"
    TYPE id_patient_cc IS TABLE OF pat_child_feed_dev.id_patient%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_CHILD_FEED_DEV"
    TYPE id_child_feed_dev_cc IS TABLE OF pat_child_feed_dev.id_child_feed_dev%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_PROFESSIONAL"
    TYPE id_professional_cc IS TABLE OF pat_child_feed_dev.id_professional%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CHILD_AGE"
    TYPE child_age_cc IS TABLE OF pat_child_feed_dev.child_age%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "DT_CANCEL"
    TYPE dt_cancel_cc IS TABLE OF pat_child_feed_dev.dt_cancel%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_PROF_CANCEL"
    TYPE id_prof_cancel_cc IS TABLE OF pat_child_feed_dev.id_prof_cancel%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "FLG_STATUS"
    TYPE flg_status_cc IS TABLE OF pat_child_feed_dev.flg_status%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_EPISODE"
    TYPE id_episode_cc IS TABLE OF pat_child_feed_dev.id_episode%TYPE INDEX BY BINARY_INTEGER;

    -- Insert one row, providing primary key if present
    PROCEDURE ins
    (
        id_pat_child_feed_dev_in IN pat_child_feed_dev.id_pat_child_feed_dev%TYPE,
        dt_pat_child_feed_dev_in IN pat_child_feed_dev.dt_pat_child_feed_dev%TYPE DEFAULT NULL,
        id_patient_in            IN pat_child_feed_dev.id_patient%TYPE DEFAULT NULL,
        id_child_feed_dev_in     IN pat_child_feed_dev.id_child_feed_dev%TYPE DEFAULT NULL,
        id_professional_in       IN pat_child_feed_dev.id_professional%TYPE DEFAULT NULL,
        child_age_in             IN pat_child_feed_dev.child_age%TYPE DEFAULT NULL,
        dt_cancel_in             IN pat_child_feed_dev.dt_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_in        IN pat_child_feed_dev.id_prof_cancel%TYPE DEFAULT NULL,
        flg_status_in            IN pat_child_feed_dev.flg_status%TYPE DEFAULT NULL,
        id_episode_in            IN pat_child_feed_dev.id_episode%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_pat_child_feed_dev_in IN pat_child_feed_dev.id_pat_child_feed_dev%TYPE,
        dt_pat_child_feed_dev_in IN pat_child_feed_dev.dt_pat_child_feed_dev%TYPE DEFAULT NULL,
        id_patient_in            IN pat_child_feed_dev.id_patient%TYPE DEFAULT NULL,
        id_child_feed_dev_in     IN pat_child_feed_dev.id_child_feed_dev%TYPE DEFAULT NULL,
        id_professional_in       IN pat_child_feed_dev.id_professional%TYPE DEFAULT NULL,
        child_age_in             IN pat_child_feed_dev.child_age%TYPE DEFAULT NULL,
        dt_cancel_in             IN pat_child_feed_dev.dt_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_in        IN pat_child_feed_dev.id_prof_cancel%TYPE DEFAULT NULL,
        flg_status_in            IN pat_child_feed_dev.flg_status%TYPE DEFAULT NULL,
        id_episode_in            IN pat_child_feed_dev.id_episode%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a row based on a record.
    -- Specify whether or not a primary key value should be generated.
    PROCEDURE ins
    (
        rec_in          IN pat_child_feed_dev%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rec_in          IN pat_child_feed_dev%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a collection of rows using FORALL; all primary key values
    -- must have already been generated, or are handled in triggers.
    PROCEDURE ins
    (
        rows_in         IN pat_child_feed_dev_tc,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rows_in         IN pat_child_feed_dev_tc,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Return next primary key value using the named sequence.
    FUNCTION next_key(sequence_in IN VARCHAR2 := NULL) RETURN pat_child_feed_dev.id_pat_child_feed_dev%TYPE;

    -- Insert one row, generating hidden primary key using a sequence
    PROCEDURE ins
    (
        dt_pat_child_feed_dev_in IN pat_child_feed_dev.dt_pat_child_feed_dev%TYPE DEFAULT NULL,
        id_patient_in            IN pat_child_feed_dev.id_patient%TYPE DEFAULT NULL,
        id_child_feed_dev_in     IN pat_child_feed_dev.id_child_feed_dev%TYPE DEFAULT NULL,
        id_professional_in       IN pat_child_feed_dev.id_professional%TYPE DEFAULT NULL,
        child_age_in             IN pat_child_feed_dev.child_age%TYPE DEFAULT NULL,
        dt_cancel_in             IN pat_child_feed_dev.dt_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_in        IN pat_child_feed_dev.id_prof_cancel%TYPE DEFAULT NULL,
        flg_status_in            IN pat_child_feed_dev.flg_status%TYPE DEFAULT NULL,
        id_episode_in            IN pat_child_feed_dev.id_episode%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        dt_pat_child_feed_dev_in IN pat_child_feed_dev.dt_pat_child_feed_dev%TYPE DEFAULT NULL,
        id_patient_in            IN pat_child_feed_dev.id_patient%TYPE DEFAULT NULL,
        id_child_feed_dev_in     IN pat_child_feed_dev.id_child_feed_dev%TYPE DEFAULT NULL,
        id_professional_in       IN pat_child_feed_dev.id_professional%TYPE DEFAULT NULL,
        child_age_in             IN pat_child_feed_dev.child_age%TYPE DEFAULT NULL,
        dt_cancel_in             IN pat_child_feed_dev.dt_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_in        IN pat_child_feed_dev.id_prof_cancel%TYPE DEFAULT NULL,
        flg_status_in            IN pat_child_feed_dev.flg_status%TYPE DEFAULT NULL,
        id_episode_in            IN pat_child_feed_dev.id_episode%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert one row, returning primary key generated by sequence
    PROCEDURE ins
    (
        dt_pat_child_feed_dev_in  IN pat_child_feed_dev.dt_pat_child_feed_dev%TYPE DEFAULT NULL,
        id_patient_in             IN pat_child_feed_dev.id_patient%TYPE DEFAULT NULL,
        id_child_feed_dev_in      IN pat_child_feed_dev.id_child_feed_dev%TYPE DEFAULT NULL,
        id_professional_in        IN pat_child_feed_dev.id_professional%TYPE DEFAULT NULL,
        child_age_in              IN pat_child_feed_dev.child_age%TYPE DEFAULT NULL,
        dt_cancel_in              IN pat_child_feed_dev.dt_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_in         IN pat_child_feed_dev.id_prof_cancel%TYPE DEFAULT NULL,
        flg_status_in             IN pat_child_feed_dev.flg_status%TYPE DEFAULT NULL,
        id_episode_in             IN pat_child_feed_dev.id_episode%TYPE DEFAULT NULL,
        id_pat_child_feed_dev_out IN OUT pat_child_feed_dev.id_pat_child_feed_dev%TYPE
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        dt_pat_child_feed_dev_in  IN pat_child_feed_dev.dt_pat_child_feed_dev%TYPE DEFAULT NULL,
        id_patient_in             IN pat_child_feed_dev.id_patient%TYPE DEFAULT NULL,
        id_child_feed_dev_in      IN pat_child_feed_dev.id_child_feed_dev%TYPE DEFAULT NULL,
        id_professional_in        IN pat_child_feed_dev.id_professional%TYPE DEFAULT NULL,
        child_age_in              IN pat_child_feed_dev.child_age%TYPE DEFAULT NULL,
        dt_cancel_in              IN pat_child_feed_dev.dt_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_in         IN pat_child_feed_dev.id_prof_cancel%TYPE DEFAULT NULL,
        flg_status_in             IN pat_child_feed_dev.flg_status%TYPE DEFAULT NULL,
        id_episode_in             IN pat_child_feed_dev.id_episode%TYPE DEFAULT NULL,
        id_pat_child_feed_dev_out IN OUT pat_child_feed_dev.id_pat_child_feed_dev%TYPE
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert one row with function, return generated primary key
    FUNCTION ins
    (
        dt_pat_child_feed_dev_in IN pat_child_feed_dev.dt_pat_child_feed_dev%TYPE DEFAULT NULL,
        id_patient_in            IN pat_child_feed_dev.id_patient%TYPE DEFAULT NULL,
        id_child_feed_dev_in     IN pat_child_feed_dev.id_child_feed_dev%TYPE DEFAULT NULL,
        id_professional_in       IN pat_child_feed_dev.id_professional%TYPE DEFAULT NULL,
        child_age_in             IN pat_child_feed_dev.child_age%TYPE DEFAULT NULL,
        dt_cancel_in             IN pat_child_feed_dev.dt_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_in        IN pat_child_feed_dev.id_prof_cancel%TYPE DEFAULT NULL,
        flg_status_in            IN pat_child_feed_dev.flg_status%TYPE DEFAULT NULL,
        id_episode_in            IN pat_child_feed_dev.id_episode%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) RETURN pat_child_feed_dev.id_pat_child_feed_dev%TYPE;

    FUNCTION ins
    (
        dt_pat_child_feed_dev_in IN pat_child_feed_dev.dt_pat_child_feed_dev%TYPE DEFAULT NULL,
        id_patient_in            IN pat_child_feed_dev.id_patient%TYPE DEFAULT NULL,
        id_child_feed_dev_in     IN pat_child_feed_dev.id_child_feed_dev%TYPE DEFAULT NULL,
        id_professional_in       IN pat_child_feed_dev.id_professional%TYPE DEFAULT NULL,
        child_age_in             IN pat_child_feed_dev.child_age%TYPE DEFAULT NULL,
        dt_cancel_in             IN pat_child_feed_dev.dt_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_in        IN pat_child_feed_dev.id_prof_cancel%TYPE DEFAULT NULL,
        flg_status_in            IN pat_child_feed_dev.flg_status%TYPE DEFAULT NULL,
        id_episode_in            IN pat_child_feed_dev.id_episode%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    ) RETURN pat_child_feed_dev.id_pat_child_feed_dev%TYPE;

    -- Update any/all columns by primary key. If you pass NULL, then
    -- the current column value is set to itself. If you need a more
    -- selected UPDATE then use one of the onecol procedures below.

    PROCEDURE upd
    (
        id_pat_child_feed_dev_in  IN pat_child_feed_dev.id_pat_child_feed_dev%TYPE,
        dt_pat_child_feed_dev_in  IN pat_child_feed_dev.dt_pat_child_feed_dev%TYPE DEFAULT NULL,
        dt_pat_child_feed_dev_nin IN BOOLEAN := TRUE,
        id_patient_in             IN pat_child_feed_dev.id_patient%TYPE DEFAULT NULL,
        id_patient_nin            IN BOOLEAN := TRUE,
        id_child_feed_dev_in      IN pat_child_feed_dev.id_child_feed_dev%TYPE DEFAULT NULL,
        id_child_feed_dev_nin     IN BOOLEAN := TRUE,
        id_professional_in        IN pat_child_feed_dev.id_professional%TYPE DEFAULT NULL,
        id_professional_nin       IN BOOLEAN := TRUE,
        child_age_in              IN pat_child_feed_dev.child_age%TYPE DEFAULT NULL,
        child_age_nin             IN BOOLEAN := TRUE,
        dt_cancel_in              IN pat_child_feed_dev.dt_cancel%TYPE DEFAULT NULL,
        dt_cancel_nin             IN BOOLEAN := TRUE,
        id_prof_cancel_in         IN pat_child_feed_dev.id_prof_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_nin        IN BOOLEAN := TRUE,
        flg_status_in             IN pat_child_feed_dev.flg_status%TYPE DEFAULT NULL,
        flg_status_nin            IN BOOLEAN := TRUE,
        id_episode_in             IN pat_child_feed_dev.id_episode%TYPE DEFAULT NULL,
        id_episode_nin            IN BOOLEAN := TRUE,
        handle_error_in           IN BOOLEAN := TRUE,
        rows_out                  IN OUT table_varchar
    );

    PROCEDURE upd
    (
        id_pat_child_feed_dev_in  IN pat_child_feed_dev.id_pat_child_feed_dev%TYPE,
        dt_pat_child_feed_dev_in  IN pat_child_feed_dev.dt_pat_child_feed_dev%TYPE DEFAULT NULL,
        dt_pat_child_feed_dev_nin IN BOOLEAN := TRUE,
        id_patient_in             IN pat_child_feed_dev.id_patient%TYPE DEFAULT NULL,
        id_patient_nin            IN BOOLEAN := TRUE,
        id_child_feed_dev_in      IN pat_child_feed_dev.id_child_feed_dev%TYPE DEFAULT NULL,
        id_child_feed_dev_nin     IN BOOLEAN := TRUE,
        id_professional_in        IN pat_child_feed_dev.id_professional%TYPE DEFAULT NULL,
        id_professional_nin       IN BOOLEAN := TRUE,
        child_age_in              IN pat_child_feed_dev.child_age%TYPE DEFAULT NULL,
        child_age_nin             IN BOOLEAN := TRUE,
        dt_cancel_in              IN pat_child_feed_dev.dt_cancel%TYPE DEFAULT NULL,
        dt_cancel_nin             IN BOOLEAN := TRUE,
        id_prof_cancel_in         IN pat_child_feed_dev.id_prof_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_nin        IN BOOLEAN := TRUE,
        flg_status_in             IN pat_child_feed_dev.flg_status%TYPE DEFAULT NULL,
        flg_status_nin            IN BOOLEAN := TRUE,
        id_episode_in             IN pat_child_feed_dev.id_episode%TYPE DEFAULT NULL,
        id_episode_nin            IN BOOLEAN := TRUE,
        handle_error_in           IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        dt_pat_child_feed_dev_in  IN pat_child_feed_dev.dt_pat_child_feed_dev%TYPE DEFAULT NULL,
        dt_pat_child_feed_dev_nin IN BOOLEAN := TRUE,
        id_patient_in             IN pat_child_feed_dev.id_patient%TYPE DEFAULT NULL,
        id_patient_nin            IN BOOLEAN := TRUE,
        id_child_feed_dev_in      IN pat_child_feed_dev.id_child_feed_dev%TYPE DEFAULT NULL,
        id_child_feed_dev_nin     IN BOOLEAN := TRUE,
        id_professional_in        IN pat_child_feed_dev.id_professional%TYPE DEFAULT NULL,
        id_professional_nin       IN BOOLEAN := TRUE,
        child_age_in              IN pat_child_feed_dev.child_age%TYPE DEFAULT NULL,
        child_age_nin             IN BOOLEAN := TRUE,
        dt_cancel_in              IN pat_child_feed_dev.dt_cancel%TYPE DEFAULT NULL,
        dt_cancel_nin             IN BOOLEAN := TRUE,
        id_prof_cancel_in         IN pat_child_feed_dev.id_prof_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_nin        IN BOOLEAN := TRUE,
        flg_status_in             IN pat_child_feed_dev.flg_status%TYPE DEFAULT NULL,
        flg_status_nin            IN BOOLEAN := TRUE,
        id_episode_in             IN pat_child_feed_dev.id_episode%TYPE DEFAULT NULL,
        id_episode_nin            IN BOOLEAN := TRUE,
        where_in                  VARCHAR2 DEFAULT NULL,
        handle_error_in           IN BOOLEAN := TRUE,
        rows_out                  IN OUT table_varchar
    );

    PROCEDURE upd
    (
        dt_pat_child_feed_dev_in  IN pat_child_feed_dev.dt_pat_child_feed_dev%TYPE DEFAULT NULL,
        dt_pat_child_feed_dev_nin IN BOOLEAN := TRUE,
        id_patient_in             IN pat_child_feed_dev.id_patient%TYPE DEFAULT NULL,
        id_patient_nin            IN BOOLEAN := TRUE,
        id_child_feed_dev_in      IN pat_child_feed_dev.id_child_feed_dev%TYPE DEFAULT NULL,
        id_child_feed_dev_nin     IN BOOLEAN := TRUE,
        id_professional_in        IN pat_child_feed_dev.id_professional%TYPE DEFAULT NULL,
        id_professional_nin       IN BOOLEAN := TRUE,
        child_age_in              IN pat_child_feed_dev.child_age%TYPE DEFAULT NULL,
        child_age_nin             IN BOOLEAN := TRUE,
        dt_cancel_in              IN pat_child_feed_dev.dt_cancel%TYPE DEFAULT NULL,
        dt_cancel_nin             IN BOOLEAN := TRUE,
        id_prof_cancel_in         IN pat_child_feed_dev.id_prof_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_nin        IN BOOLEAN := TRUE,
        flg_status_in             IN pat_child_feed_dev.flg_status%TYPE DEFAULT NULL,
        flg_status_nin            IN BOOLEAN := TRUE,
        id_episode_in             IN pat_child_feed_dev.id_episode%TYPE DEFAULT NULL,
        id_episode_nin            IN BOOLEAN := TRUE,
        where_in                  VARCHAR2 DEFAULT NULL,
        handle_error_in           IN BOOLEAN := TRUE
    );

    PROCEDURE upd_ins
    (
        id_pat_child_feed_dev_in IN pat_child_feed_dev.id_pat_child_feed_dev%TYPE,
        dt_pat_child_feed_dev_in IN pat_child_feed_dev.dt_pat_child_feed_dev%TYPE DEFAULT NULL,
        id_patient_in            IN pat_child_feed_dev.id_patient%TYPE DEFAULT NULL,
        id_child_feed_dev_in     IN pat_child_feed_dev.id_child_feed_dev%TYPE DEFAULT NULL,
        id_professional_in       IN pat_child_feed_dev.id_professional%TYPE DEFAULT NULL,
        child_age_in             IN pat_child_feed_dev.child_age%TYPE DEFAULT NULL,
        dt_cancel_in             IN pat_child_feed_dev.dt_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_in        IN pat_child_feed_dev.id_prof_cancel%TYPE DEFAULT NULL,
        flg_status_in            IN pat_child_feed_dev.flg_status%TYPE DEFAULT NULL,
        id_episode_in            IN pat_child_feed_dev.id_episode%TYPE DEFAULT NULL,
        handle_error_in          IN BOOLEAN := TRUE,
        rows_out                 OUT table_varchar
    );

    PROCEDURE upd_ins
    (
        id_pat_child_feed_dev_in IN pat_child_feed_dev.id_pat_child_feed_dev%TYPE,
        dt_pat_child_feed_dev_in IN pat_child_feed_dev.dt_pat_child_feed_dev%TYPE DEFAULT NULL,
        id_patient_in            IN pat_child_feed_dev.id_patient%TYPE DEFAULT NULL,
        id_child_feed_dev_in     IN pat_child_feed_dev.id_child_feed_dev%TYPE DEFAULT NULL,
        id_professional_in       IN pat_child_feed_dev.id_professional%TYPE DEFAULT NULL,
        child_age_in             IN pat_child_feed_dev.child_age%TYPE DEFAULT NULL,
        dt_cancel_in             IN pat_child_feed_dev.dt_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_in        IN pat_child_feed_dev.id_prof_cancel%TYPE DEFAULT NULL,
        flg_status_in            IN pat_child_feed_dev.flg_status%TYPE DEFAULT NULL,
        id_episode_in            IN pat_child_feed_dev.id_episode%TYPE DEFAULT NULL,
        handle_error_in          IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        rec_in          IN pat_child_feed_dev%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        IN OUT table_varchar
    );

    PROCEDURE upd
    (
        rec_in          IN pat_child_feed_dev%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        col_in            IN pat_child_feed_dev_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          IN OUT table_varchar
    );

    PROCEDURE upd
    (
        col_in            IN pat_child_feed_dev_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE
    );

    -- Use Native Dynamic SQL increment a single NUMBER column
    -- for all rows specified by the dynamic WHERE clause
    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_pat_child_feed_dev_in IN pat_child_feed_dev.id_pat_child_feed_dev%TYPE,
        handle_error_in          IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_pat_child_feed_dev_in IN pat_child_feed_dev.id_pat_child_feed_dev%TYPE,
        handle_error_in          IN BOOLEAN := TRUE,
        rows_out                 OUT table_varchar
    );

    -- Delete all rows for primary key column ID_PAT_CHILD_FEED_DEV
    PROCEDURE del_id_pat_child_feed_dev
    (
        id_pat_child_feed_dev_in IN pat_child_feed_dev.id_pat_child_feed_dev%TYPE,
        handle_error_in          IN BOOLEAN := TRUE
    );

    -- Delete all rows for primary key column ID_PAT_CHILD_FEED_DEV
    PROCEDURE del_id_pat_child_feed_dev
    (
        id_pat_child_feed_dev_in IN pat_child_feed_dev.id_pat_child_feed_dev%TYPE,
        handle_error_in          IN BOOLEAN := TRUE,
        rows_out                 OUT table_varchar
    );

    -- Delete all rows for this PCF_EPIS_FK foreign key value
    PROCEDURE del_pcf_epis_fk
    (
        id_episode_in   IN pat_child_feed_dev.id_episode%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this PCF_EPIS_FK foreign key value
    PROCEDURE del_pcf_epis_fk
    (
        id_episode_in   IN pat_child_feed_dev.id_episode%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for this PCF_FDPC_FK foreign key value
    PROCEDURE del_pcf_fdpc_fk
    (
        id_prof_cancel_in IN pat_child_feed_dev.id_prof_cancel%TYPE,
        handle_error_in   IN BOOLEAN := TRUE
    );

    -- Delete all rows for this PCF_FDPC_FK foreign key value
    PROCEDURE del_pcf_fdpc_fk
    (
        id_prof_cancel_in IN pat_child_feed_dev.id_prof_cancel%TYPE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          OUT table_varchar
    );

    -- Delete all rows for this PCF_FDP_FK foreign key value
    PROCEDURE del_pcf_fdp_fk
    (
        id_child_feed_dev_in IN pat_child_feed_dev.id_child_feed_dev%TYPE,
        handle_error_in      IN BOOLEAN := TRUE
    );

    -- Delete all rows for this PCF_FDP_FK foreign key value
    PROCEDURE del_pcf_fdp_fk
    (
        id_child_feed_dev_in IN pat_child_feed_dev.id_child_feed_dev%TYPE,
        handle_error_in      IN BOOLEAN := TRUE,
        rows_out             OUT table_varchar
    );

    -- Delete all rows for this PCF_PAT_FK foreign key value
    PROCEDURE del_pcf_pat_fk
    (
        id_patient_in   IN pat_child_feed_dev.id_patient%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this PCF_PAT_FK foreign key value
    PROCEDURE del_pcf_pat_fk
    (
        id_patient_in   IN pat_child_feed_dev.id_patient%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for this PCF_PROF_FK foreign key value
    PROCEDURE del_pcf_prof_fk
    (
        id_professional_in IN pat_child_feed_dev.id_professional%TYPE,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete all rows for this PCF_PROF_FK foreign key value
    PROCEDURE del_pcf_prof_fk
    (
        id_professional_in IN pat_child_feed_dev.id_professional%TYPE,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Initialize a record with default values for columns in the table.
    PROCEDURE initrec(pat_child_feed_dev_inout IN OUT pat_child_feed_dev%ROWTYPE);

    FUNCTION initrec RETURN pat_child_feed_dev%ROWTYPE;

    FUNCTION get_data_rowid(rows_in IN table_varchar) RETURN pat_child_feed_dev_tc;

    FUNCTION get_data_rowid_pat(rows_in IN table_varchar) RETURN pat_child_feed_dev_tc;

END ts_pat_child_feed_dev;
/
