/*-- Last Change Revision: $Rev: 2029102 $*/
/*-- Last Change by: $Author: mario.fernandes $*/
/*-- Date of last change: $Date: 2022-08-02 18:49:47 +0100 (ter, 02 ago 2022) $*/

CREATE OR REPLACE PACKAGE ts_clin_record
/*
| Generated by or retrieved from QCGU - DO NOT MODIFY!
| QCGU - "Get it right, do it fast" - www.ToadWorld.com
| QCGU Universal ID: {1BD37A66-EA60-4927-9A64-F6DD89237236}
| Created On: Novembro 21, 2008 18:23:0
| Created By: ALERT
*/
 IS

    -- Collection of %ROWTYPE records based on "CLIN_RECORD"
    TYPE clin_record_tc IS TABLE OF clin_record%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE clin_record_ntt IS TABLE OF clin_record%ROWTYPE;
    TYPE clin_record_vat IS VARRAY(100) OF clin_record%ROWTYPE;

    -- Same type structure, with a static name.
    TYPE aat IS TABLE OF clin_record%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE ntt IS TABLE OF clin_record%ROWTYPE;
    TYPE vat IS VARRAY(100) OF clin_record%ROWTYPE;

    -- Column Collection based on column "ID_CLIN_RECORD"
    TYPE id_clin_record_cc IS TABLE OF clin_record.id_clin_record%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "FLG_STATUS"
    TYPE flg_status_cc IS TABLE OF clin_record.flg_status%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_PATIENT"
    TYPE id_patient_cc IS TABLE OF clin_record.id_patient%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_INSTITUTION"
    TYPE id_institution_cc IS TABLE OF clin_record.id_institution%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_PAT_FAMILY"
    TYPE id_pat_family_cc IS TABLE OF clin_record.id_pat_family%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "NUM_CLIN_RECORD"
    TYPE num_clin_record_cc IS TABLE OF clin_record.num_clin_record%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_INSTIT_ENROLED"
    TYPE id_instit_enroled_cc IS TABLE OF clin_record.id_instit_enroled%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_EPISODE"
    TYPE id_episode_cc IS TABLE OF clin_record.id_episode%TYPE INDEX BY BINARY_INTEGER;

    -- Insert one row, providing primary key if present
    PROCEDURE ins
    (
        id_clin_record_in    IN clin_record.id_clin_record%TYPE,
        flg_status_in        IN clin_record.flg_status%TYPE DEFAULT NULL,
        id_patient_in        IN clin_record.id_patient%TYPE DEFAULT NULL,
        id_institution_in    IN clin_record.id_institution%TYPE DEFAULT NULL,
        id_pat_family_in     IN clin_record.id_pat_family%TYPE DEFAULT NULL,
        num_clin_record_in   IN clin_record.num_clin_record%TYPE DEFAULT NULL,
        id_instit_enroled_in IN clin_record.id_instit_enroled%TYPE DEFAULT NULL,
        id_episode_in        IN clin_record.id_episode%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_clin_record_in    IN clin_record.id_clin_record%TYPE,
        flg_status_in        IN clin_record.flg_status%TYPE DEFAULT NULL,
        id_patient_in        IN clin_record.id_patient%TYPE DEFAULT NULL,
        id_institution_in    IN clin_record.id_institution%TYPE DEFAULT NULL,
        id_pat_family_in     IN clin_record.id_pat_family%TYPE DEFAULT NULL,
        num_clin_record_in   IN clin_record.num_clin_record%TYPE DEFAULT NULL,
        id_instit_enroled_in IN clin_record.id_instit_enroled%TYPE DEFAULT NULL,
        id_episode_in        IN clin_record.id_episode%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a row based on a record.
    -- Specify whether or not a primary key value should be generated.
    PROCEDURE ins
    (
        rec_in          IN clin_record%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rec_in          IN clin_record%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a collection of rows using FORALL; all primary key values
    -- must have already been generated, or are handled in triggers.
    PROCEDURE ins
    (
        rows_in         IN clin_record_tc,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rows_in         IN clin_record_tc,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Return next primary key value using the named sequence.
    FUNCTION next_key(sequence_in IN VARCHAR2 := NULL) RETURN clin_record.id_clin_record%TYPE;

    -- Insert one row, generating hidden primary key using a sequence
    PROCEDURE ins
    (
        flg_status_in        IN clin_record.flg_status%TYPE DEFAULT NULL,
        id_patient_in        IN clin_record.id_patient%TYPE DEFAULT NULL,
        id_institution_in    IN clin_record.id_institution%TYPE DEFAULT NULL,
        id_pat_family_in     IN clin_record.id_pat_family%TYPE DEFAULT NULL,
        num_clin_record_in   IN clin_record.num_clin_record%TYPE DEFAULT NULL,
        id_instit_enroled_in IN clin_record.id_instit_enroled%TYPE DEFAULT NULL,
        id_episode_in        IN clin_record.id_episode%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        flg_status_in        IN clin_record.flg_status%TYPE DEFAULT NULL,
        id_patient_in        IN clin_record.id_patient%TYPE DEFAULT NULL,
        id_institution_in    IN clin_record.id_institution%TYPE DEFAULT NULL,
        id_pat_family_in     IN clin_record.id_pat_family%TYPE DEFAULT NULL,
        num_clin_record_in   IN clin_record.num_clin_record%TYPE DEFAULT NULL,
        id_instit_enroled_in IN clin_record.id_instit_enroled%TYPE DEFAULT NULL,
        id_episode_in        IN clin_record.id_episode%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert one row, returning primary key generated by sequence
    PROCEDURE ins
    (
        flg_status_in        IN clin_record.flg_status%TYPE DEFAULT NULL,
        id_patient_in        IN clin_record.id_patient%TYPE DEFAULT NULL,
        id_institution_in    IN clin_record.id_institution%TYPE DEFAULT NULL,
        id_pat_family_in     IN clin_record.id_pat_family%TYPE DEFAULT NULL,
        num_clin_record_in   IN clin_record.num_clin_record%TYPE DEFAULT NULL,
        id_instit_enroled_in IN clin_record.id_instit_enroled%TYPE DEFAULT NULL,
        id_episode_in        IN clin_record.id_episode%TYPE DEFAULT NULL,
        id_clin_record_out   IN OUT clin_record.id_clin_record%TYPE
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        flg_status_in        IN clin_record.flg_status%TYPE DEFAULT NULL,
        id_patient_in        IN clin_record.id_patient%TYPE DEFAULT NULL,
        id_institution_in    IN clin_record.id_institution%TYPE DEFAULT NULL,
        id_pat_family_in     IN clin_record.id_pat_family%TYPE DEFAULT NULL,
        num_clin_record_in   IN clin_record.num_clin_record%TYPE DEFAULT NULL,
        id_instit_enroled_in IN clin_record.id_instit_enroled%TYPE DEFAULT NULL,
        id_episode_in        IN clin_record.id_episode%TYPE DEFAULT NULL,
        id_clin_record_out   IN OUT clin_record.id_clin_record%TYPE
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert one row with function, return generated primary key
    FUNCTION ins
    (
        flg_status_in        IN clin_record.flg_status%TYPE DEFAULT NULL,
        id_patient_in        IN clin_record.id_patient%TYPE DEFAULT NULL,
        id_institution_in    IN clin_record.id_institution%TYPE DEFAULT NULL,
        id_pat_family_in     IN clin_record.id_pat_family%TYPE DEFAULT NULL,
        num_clin_record_in   IN clin_record.num_clin_record%TYPE DEFAULT NULL,
        id_instit_enroled_in IN clin_record.id_instit_enroled%TYPE DEFAULT NULL,
        id_episode_in        IN clin_record.id_episode%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) RETURN clin_record.id_clin_record%TYPE;

    FUNCTION ins
    (
        flg_status_in        IN clin_record.flg_status%TYPE DEFAULT NULL,
        id_patient_in        IN clin_record.id_patient%TYPE DEFAULT NULL,
        id_institution_in    IN clin_record.id_institution%TYPE DEFAULT NULL,
        id_pat_family_in     IN clin_record.id_pat_family%TYPE DEFAULT NULL,
        num_clin_record_in   IN clin_record.num_clin_record%TYPE DEFAULT NULL,
        id_instit_enroled_in IN clin_record.id_instit_enroled%TYPE DEFAULT NULL,
        id_episode_in        IN clin_record.id_episode%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    ) RETURN clin_record.id_clin_record%TYPE;

    -- Update any/all columns by primary key. If you pass NULL, then
    -- the current column value is set to itself. If you need a more
    -- selected UPDATE then use one of the onecol procedures below.

    PROCEDURE upd
    (
        id_clin_record_in     IN clin_record.id_clin_record%TYPE,
        flg_status_in         IN clin_record.flg_status%TYPE DEFAULT NULL,
        flg_status_nin        IN BOOLEAN := TRUE,
        id_patient_in         IN clin_record.id_patient%TYPE DEFAULT NULL,
        id_patient_nin        IN BOOLEAN := TRUE,
        id_institution_in     IN clin_record.id_institution%TYPE DEFAULT NULL,
        id_institution_nin    IN BOOLEAN := TRUE,
        id_pat_family_in      IN clin_record.id_pat_family%TYPE DEFAULT NULL,
        id_pat_family_nin     IN BOOLEAN := TRUE,
        num_clin_record_in    IN clin_record.num_clin_record%TYPE DEFAULT NULL,
        num_clin_record_nin   IN BOOLEAN := TRUE,
        id_instit_enroled_in  IN clin_record.id_instit_enroled%TYPE DEFAULT NULL,
        id_instit_enroled_nin IN BOOLEAN := TRUE,
        id_episode_in         IN clin_record.id_episode%TYPE DEFAULT NULL,
        id_episode_nin        IN BOOLEAN := TRUE,
        handle_error_in       IN BOOLEAN := TRUE,
        rows_out              IN OUT table_varchar
    );

    PROCEDURE upd
    (
        id_clin_record_in     IN clin_record.id_clin_record%TYPE,
        flg_status_in         IN clin_record.flg_status%TYPE DEFAULT NULL,
        flg_status_nin        IN BOOLEAN := TRUE,
        id_patient_in         IN clin_record.id_patient%TYPE DEFAULT NULL,
        id_patient_nin        IN BOOLEAN := TRUE,
        id_institution_in     IN clin_record.id_institution%TYPE DEFAULT NULL,
        id_institution_nin    IN BOOLEAN := TRUE,
        id_pat_family_in      IN clin_record.id_pat_family%TYPE DEFAULT NULL,
        id_pat_family_nin     IN BOOLEAN := TRUE,
        num_clin_record_in    IN clin_record.num_clin_record%TYPE DEFAULT NULL,
        num_clin_record_nin   IN BOOLEAN := TRUE,
        id_instit_enroled_in  IN clin_record.id_instit_enroled%TYPE DEFAULT NULL,
        id_instit_enroled_nin IN BOOLEAN := TRUE,
        id_episode_in         IN clin_record.id_episode%TYPE DEFAULT NULL,
        id_episode_nin        IN BOOLEAN := TRUE,
        handle_error_in       IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        flg_status_in         IN clin_record.flg_status%TYPE DEFAULT NULL,
        flg_status_nin        IN BOOLEAN := TRUE,
        id_patient_in         IN clin_record.id_patient%TYPE DEFAULT NULL,
        id_patient_nin        IN BOOLEAN := TRUE,
        id_institution_in     IN clin_record.id_institution%TYPE DEFAULT NULL,
        id_institution_nin    IN BOOLEAN := TRUE,
        id_pat_family_in      IN clin_record.id_pat_family%TYPE DEFAULT NULL,
        id_pat_family_nin     IN BOOLEAN := TRUE,
        num_clin_record_in    IN clin_record.num_clin_record%TYPE DEFAULT NULL,
        num_clin_record_nin   IN BOOLEAN := TRUE,
        id_instit_enroled_in  IN clin_record.id_instit_enroled%TYPE DEFAULT NULL,
        id_instit_enroled_nin IN BOOLEAN := TRUE,
        id_episode_in         IN clin_record.id_episode%TYPE DEFAULT NULL,
        id_episode_nin        IN BOOLEAN := TRUE,
        where_in              VARCHAR2 DEFAULT NULL,
        handle_error_in       IN BOOLEAN := TRUE,
        rows_out              IN OUT table_varchar
    );

    PROCEDURE upd
    (
        flg_status_in         IN clin_record.flg_status%TYPE DEFAULT NULL,
        flg_status_nin        IN BOOLEAN := TRUE,
        id_patient_in         IN clin_record.id_patient%TYPE DEFAULT NULL,
        id_patient_nin        IN BOOLEAN := TRUE,
        id_institution_in     IN clin_record.id_institution%TYPE DEFAULT NULL,
        id_institution_nin    IN BOOLEAN := TRUE,
        id_pat_family_in      IN clin_record.id_pat_family%TYPE DEFAULT NULL,
        id_pat_family_nin     IN BOOLEAN := TRUE,
        num_clin_record_in    IN clin_record.num_clin_record%TYPE DEFAULT NULL,
        num_clin_record_nin   IN BOOLEAN := TRUE,
        id_instit_enroled_in  IN clin_record.id_instit_enroled%TYPE DEFAULT NULL,
        id_instit_enroled_nin IN BOOLEAN := TRUE,
        id_episode_in         IN clin_record.id_episode%TYPE DEFAULT NULL,
        id_episode_nin        IN BOOLEAN := TRUE,
        where_in              VARCHAR2 DEFAULT NULL,
        handle_error_in       IN BOOLEAN := TRUE
    );

    PROCEDURE upd_ins
    (
        id_clin_record_in    IN clin_record.id_clin_record%TYPE,
        flg_status_in        IN clin_record.flg_status%TYPE DEFAULT NULL,
        id_patient_in        IN clin_record.id_patient%TYPE DEFAULT NULL,
        id_institution_in    IN clin_record.id_institution%TYPE DEFAULT NULL,
        id_pat_family_in     IN clin_record.id_pat_family%TYPE DEFAULT NULL,
        num_clin_record_in   IN clin_record.num_clin_record%TYPE DEFAULT NULL,
        id_instit_enroled_in IN clin_record.id_instit_enroled%TYPE DEFAULT NULL,
        id_episode_in        IN clin_record.id_episode%TYPE DEFAULT NULL,
        handle_error_in      IN BOOLEAN := TRUE,
        rows_out             OUT table_varchar
    );

    PROCEDURE upd_ins
    (
        id_clin_record_in    IN clin_record.id_clin_record%TYPE,
        flg_status_in        IN clin_record.flg_status%TYPE DEFAULT NULL,
        id_patient_in        IN clin_record.id_patient%TYPE DEFAULT NULL,
        id_institution_in    IN clin_record.id_institution%TYPE DEFAULT NULL,
        id_pat_family_in     IN clin_record.id_pat_family%TYPE DEFAULT NULL,
        num_clin_record_in   IN clin_record.num_clin_record%TYPE DEFAULT NULL,
        id_instit_enroled_in IN clin_record.id_instit_enroled%TYPE DEFAULT NULL,
        id_episode_in        IN clin_record.id_episode%TYPE DEFAULT NULL,
        handle_error_in      IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        rec_in          IN clin_record%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        IN OUT table_varchar
    );

    PROCEDURE upd
    (
        rec_in          IN clin_record%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        col_in            IN clin_record_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          IN OUT table_varchar
    );

    PROCEDURE upd
    (
        col_in            IN clin_record_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE
    );

    -- Use Native Dynamic SQL increment a single NUMBER column
    -- for all rows specified by the dynamic WHERE clause
    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_clin_record_in IN clin_record.id_clin_record%TYPE,
        handle_error_in   IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_clin_record_in IN clin_record.id_clin_record%TYPE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          OUT table_varchar
    );

    -- Delete all rows for primary key column ID_CLIN_RECORD
    PROCEDURE del_id_clin_record
    (
        id_clin_record_in IN clin_record.id_clin_record%TYPE,
        handle_error_in   IN BOOLEAN := TRUE
    );

    -- Delete all rows for primary key column ID_CLIN_RECORD
    PROCEDURE del_id_clin_record
    (
        id_clin_record_in IN clin_record.id_clin_record%TYPE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          OUT table_varchar
    );

    -- Delete all rows for this CRN_EPIS_FK foreign key value
    PROCEDURE del_crn_epis_fk
    (
        id_episode_in   IN clin_record.id_episode%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this CRN_EPIS_FK foreign key value
    PROCEDURE del_crn_epis_fk
    (
        id_episode_in   IN clin_record.id_episode%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for this CRN_INST_FK foreign key value
    PROCEDURE del_crn_inst_fk
    (
        id_institution_in IN clin_record.id_institution%TYPE,
        handle_error_in   IN BOOLEAN := TRUE
    );

    -- Delete all rows for this CRN_INST_FK foreign key value
    PROCEDURE del_crn_inst_fk
    (
        id_institution_in IN clin_record.id_institution%TYPE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          OUT table_varchar
    );

    -- Delete all rows for this CRN_PAT_FK foreign key value
    PROCEDURE del_crn_pat_fk
    (
        id_patient_in   IN clin_record.id_patient%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this CRN_PAT_FK foreign key value
    PROCEDURE del_crn_pat_fk
    (
        id_patient_in   IN clin_record.id_patient%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for this CRN_PTFAM_FK foreign key value
    PROCEDURE del_crn_ptfam_fk
    (
        id_pat_family_in IN clin_record.id_pat_family%TYPE,
        handle_error_in  IN BOOLEAN := TRUE
    );

    -- Delete all rows for this CRN_PTFAM_FK foreign key value
    PROCEDURE del_crn_ptfam_fk
    (
        id_pat_family_in IN clin_record.id_pat_family%TYPE,
        handle_error_in  IN BOOLEAN := TRUE,
        rows_out         OUT table_varchar
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Initialize a record with default values for columns in the table.
    PROCEDURE initrec(clin_record_inout IN OUT clin_record%ROWTYPE);

    FUNCTION initrec RETURN clin_record%ROWTYPE;

    FUNCTION get_data_rowid(rows_in IN table_varchar) RETURN clin_record_tc;

    FUNCTION get_data_rowid_pat(rows_in IN table_varchar) RETURN clin_record_tc;

END ts_clin_record;
/
