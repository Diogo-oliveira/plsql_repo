/*-- Last Change Revision: $Rev: 1877368 $*/
/*-- Last Change by: $Author: adriano.ferreira $*/
/*-- Date of last change: $Date: 2018-11-12 15:39:19 +0000 (seg, 12 nov 2018) $*/
CREATE OR REPLACE PACKAGE ts_co_sign
/*
| Generated by or retrieved from QCGU - DO NOT MODIFY!
| QCGU - "Get it right, do it fast" - www.ToadWorld.com
| QCGU Universal ID: {1BD37A66-EA60-4927-9A64-F6DD89237236}
| Created On: Janeiro 7, 2015 13:17:23
| Created By: ALERT
*/
 IS

    -- Collection of %ROWTYPE records based on "CO_SIGN"
    TYPE co_sign_tc IS TABLE OF co_sign%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE co_sign_ntt IS TABLE OF co_sign%ROWTYPE;
    TYPE co_sign_vat IS VARRAY(100) OF co_sign%ROWTYPE;

    -- Same type structure, with a static name.
    TYPE aat IS TABLE OF co_sign%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE ntt IS TABLE OF co_sign%ROWTYPE;
    TYPE vat IS VARRAY(100) OF co_sign%ROWTYPE;

    -- Column Collection based on column "ID_CO_SIGN"
    TYPE id_co_sign_cc IS TABLE OF co_sign.id_co_sign%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_TASK"
    TYPE id_task_cc IS TABLE OF co_sign.id_task%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_TASK_GROUP"
    TYPE id_task_group_cc IS TABLE OF co_sign.id_task_group%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_TASK_TYPE"
    TYPE id_task_type_cc IS TABLE OF co_sign.id_task_type%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_ACTION"
    TYPE id_action_cc IS TABLE OF co_sign.id_action%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_ORDER_TYPE"
    TYPE id_order_type_cc IS TABLE OF co_sign.id_order_type%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_EPISODE"
    TYPE id_episode_cc IS TABLE OF co_sign.id_episode%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_PROF_CREATED"
    TYPE id_prof_created_cc IS TABLE OF co_sign.id_prof_created%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_PROF_ORDERED_BY"
    TYPE id_prof_ordered_by_cc IS TABLE OF co_sign.id_prof_ordered_by%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_PROF_CO_SIGNED"
    TYPE id_prof_co_signed_cc IS TABLE OF co_sign.id_prof_co_signed%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "DT_CREATED"
    TYPE dt_created_cc IS TABLE OF co_sign.dt_created%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "DT_ORDERED_BY"
    TYPE dt_ordered_by_cc IS TABLE OF co_sign.dt_ordered_by%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "DT_CO_SIGNED"
    TYPE dt_co_signed_cc IS TABLE OF co_sign.dt_co_signed%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "FLG_STATUS"
    TYPE flg_status_cc IS TABLE OF co_sign.flg_status%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "FLG_MADE_AUTH"
    TYPE flg_made_auth_cc IS TABLE OF co_sign.flg_made_auth%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_USER"
    TYPE create_user_cc IS TABLE OF co_sign.create_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_TIME"
    TYPE create_time_cc IS TABLE OF co_sign.create_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_INSTITUTION"
    TYPE create_institution_cc IS TABLE OF co_sign.create_institution%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_USER"
    TYPE update_user_cc IS TABLE OF co_sign.update_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_TIME"
    TYPE update_time_cc IS TABLE OF co_sign.update_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_INSTITUTION"
    TYPE update_institution_cc IS TABLE OF co_sign.update_institution%TYPE INDEX BY BINARY_INTEGER;

    -- Insert one row, providing primary key if present
    PROCEDURE ins
    (
        id_co_sign_in         IN co_sign.id_co_sign%TYPE,
        id_task_in            IN co_sign.id_task%TYPE DEFAULT NULL,
        id_task_group_in      IN co_sign.id_task_group%TYPE DEFAULT NULL,
        id_task_type_in       IN co_sign.id_task_type%TYPE DEFAULT NULL,
        id_action_in          IN co_sign.id_action%TYPE DEFAULT NULL,
        id_order_type_in      IN co_sign.id_order_type%TYPE DEFAULT NULL,
        id_episode_in         IN co_sign.id_episode%TYPE DEFAULT NULL,
        id_prof_created_in    IN co_sign.id_prof_created%TYPE DEFAULT NULL,
        id_prof_ordered_by_in IN co_sign.id_prof_ordered_by%TYPE DEFAULT NULL,
        id_prof_co_signed_in  IN co_sign.id_prof_co_signed%TYPE DEFAULT NULL,
        dt_created_in         IN co_sign.dt_created%TYPE DEFAULT NULL,
        dt_ordered_by_in      IN co_sign.dt_ordered_by%TYPE DEFAULT NULL,
        dt_co_signed_in       IN co_sign.dt_co_signed%TYPE DEFAULT NULL,
        flg_status_in         IN co_sign.flg_status%TYPE DEFAULT NULL,
        flg_made_auth_in      IN co_sign.flg_made_auth%TYPE DEFAULT NULL,
        create_user_in        IN co_sign.create_user%TYPE DEFAULT NULL,
        create_time_in        IN co_sign.create_time%TYPE DEFAULT NULL,
        create_institution_in IN co_sign.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN co_sign.update_user%TYPE DEFAULT NULL,
        update_time_in        IN co_sign.update_time%TYPE DEFAULT NULL,
        update_institution_in IN co_sign.update_institution%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_co_sign_in         IN co_sign.id_co_sign%TYPE,
        id_task_in            IN co_sign.id_task%TYPE DEFAULT NULL,
        id_task_group_in      IN co_sign.id_task_group%TYPE DEFAULT NULL,
        id_task_type_in       IN co_sign.id_task_type%TYPE DEFAULT NULL,
        id_action_in          IN co_sign.id_action%TYPE DEFAULT NULL,
        id_order_type_in      IN co_sign.id_order_type%TYPE DEFAULT NULL,
        id_episode_in         IN co_sign.id_episode%TYPE DEFAULT NULL,
        id_prof_created_in    IN co_sign.id_prof_created%TYPE DEFAULT NULL,
        id_prof_ordered_by_in IN co_sign.id_prof_ordered_by%TYPE DEFAULT NULL,
        id_prof_co_signed_in  IN co_sign.id_prof_co_signed%TYPE DEFAULT NULL,
        dt_created_in         IN co_sign.dt_created%TYPE DEFAULT NULL,
        dt_ordered_by_in      IN co_sign.dt_ordered_by%TYPE DEFAULT NULL,
        dt_co_signed_in       IN co_sign.dt_co_signed%TYPE DEFAULT NULL,
        flg_status_in         IN co_sign.flg_status%TYPE DEFAULT NULL,
        flg_made_auth_in      IN co_sign.flg_made_auth%TYPE DEFAULT NULL,
        create_user_in        IN co_sign.create_user%TYPE DEFAULT NULL,
        create_time_in        IN co_sign.create_time%TYPE DEFAULT NULL,
        create_institution_in IN co_sign.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN co_sign.update_user%TYPE DEFAULT NULL,
        update_time_in        IN co_sign.update_time%TYPE DEFAULT NULL,
        update_institution_in IN co_sign.update_institution%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a row based on a record.
    -- Specify whether or not a primary key value should be generated.
    PROCEDURE ins
    (
        rec_in          IN co_sign%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rec_in          IN co_sign%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a collection of rows using FORALL; all primary key values
    -- must have already been generated, or are handled in triggers.
    PROCEDURE ins
    (
        rows_in         IN co_sign_tc,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rows_in         IN co_sign_tc,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Return next primary key value using the named sequence.
    FUNCTION next_key(sequence_in IN VARCHAR2 := NULL) RETURN co_sign.id_co_sign%TYPE;

    -- Insert one row, generating hidden primary key using a sequence
    PROCEDURE ins
    (
        id_task_in            IN co_sign.id_task%TYPE DEFAULT NULL,
        id_task_group_in      IN co_sign.id_task_group%TYPE DEFAULT NULL,
        id_task_type_in       IN co_sign.id_task_type%TYPE DEFAULT NULL,
        id_action_in          IN co_sign.id_action%TYPE DEFAULT NULL,
        id_order_type_in      IN co_sign.id_order_type%TYPE DEFAULT NULL,
        id_episode_in         IN co_sign.id_episode%TYPE DEFAULT NULL,
        id_prof_created_in    IN co_sign.id_prof_created%TYPE DEFAULT NULL,
        id_prof_ordered_by_in IN co_sign.id_prof_ordered_by%TYPE DEFAULT NULL,
        id_prof_co_signed_in  IN co_sign.id_prof_co_signed%TYPE DEFAULT NULL,
        dt_created_in         IN co_sign.dt_created%TYPE DEFAULT NULL,
        dt_ordered_by_in      IN co_sign.dt_ordered_by%TYPE DEFAULT NULL,
        dt_co_signed_in       IN co_sign.dt_co_signed%TYPE DEFAULT NULL,
        flg_status_in         IN co_sign.flg_status%TYPE DEFAULT NULL,
        flg_made_auth_in      IN co_sign.flg_made_auth%TYPE DEFAULT NULL,
        create_user_in        IN co_sign.create_user%TYPE DEFAULT NULL,
        create_time_in        IN co_sign.create_time%TYPE DEFAULT NULL,
        create_institution_in IN co_sign.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN co_sign.update_user%TYPE DEFAULT NULL,
        update_time_in        IN co_sign.update_time%TYPE DEFAULT NULL,
        update_institution_in IN co_sign.update_institution%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_task_in            IN co_sign.id_task%TYPE DEFAULT NULL,
        id_task_group_in      IN co_sign.id_task_group%TYPE DEFAULT NULL,
        id_task_type_in       IN co_sign.id_task_type%TYPE DEFAULT NULL,
        id_action_in          IN co_sign.id_action%TYPE DEFAULT NULL,
        id_order_type_in      IN co_sign.id_order_type%TYPE DEFAULT NULL,
        id_episode_in         IN co_sign.id_episode%TYPE DEFAULT NULL,
        id_prof_created_in    IN co_sign.id_prof_created%TYPE DEFAULT NULL,
        id_prof_ordered_by_in IN co_sign.id_prof_ordered_by%TYPE DEFAULT NULL,
        id_prof_co_signed_in  IN co_sign.id_prof_co_signed%TYPE DEFAULT NULL,
        dt_created_in         IN co_sign.dt_created%TYPE DEFAULT NULL,
        dt_ordered_by_in      IN co_sign.dt_ordered_by%TYPE DEFAULT NULL,
        dt_co_signed_in       IN co_sign.dt_co_signed%TYPE DEFAULT NULL,
        flg_status_in         IN co_sign.flg_status%TYPE DEFAULT NULL,
        flg_made_auth_in      IN co_sign.flg_made_auth%TYPE DEFAULT NULL,
        create_user_in        IN co_sign.create_user%TYPE DEFAULT NULL,
        create_time_in        IN co_sign.create_time%TYPE DEFAULT NULL,
        create_institution_in IN co_sign.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN co_sign.update_user%TYPE DEFAULT NULL,
        update_time_in        IN co_sign.update_time%TYPE DEFAULT NULL,
        update_institution_in IN co_sign.update_institution%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert one row, returning primary key generated by sequence
    PROCEDURE ins
    (
        id_task_in            IN co_sign.id_task%TYPE DEFAULT NULL,
        id_task_group_in      IN co_sign.id_task_group%TYPE DEFAULT NULL,
        id_task_type_in       IN co_sign.id_task_type%TYPE DEFAULT NULL,
        id_action_in          IN co_sign.id_action%TYPE DEFAULT NULL,
        id_order_type_in      IN co_sign.id_order_type%TYPE DEFAULT NULL,
        id_episode_in         IN co_sign.id_episode%TYPE DEFAULT NULL,
        id_prof_created_in    IN co_sign.id_prof_created%TYPE DEFAULT NULL,
        id_prof_ordered_by_in IN co_sign.id_prof_ordered_by%TYPE DEFAULT NULL,
        id_prof_co_signed_in  IN co_sign.id_prof_co_signed%TYPE DEFAULT NULL,
        dt_created_in         IN co_sign.dt_created%TYPE DEFAULT NULL,
        dt_ordered_by_in      IN co_sign.dt_ordered_by%TYPE DEFAULT NULL,
        dt_co_signed_in       IN co_sign.dt_co_signed%TYPE DEFAULT NULL,
        flg_status_in         IN co_sign.flg_status%TYPE DEFAULT NULL,
        flg_made_auth_in      IN co_sign.flg_made_auth%TYPE DEFAULT NULL,
        create_user_in        IN co_sign.create_user%TYPE DEFAULT NULL,
        create_time_in        IN co_sign.create_time%TYPE DEFAULT NULL,
        create_institution_in IN co_sign.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN co_sign.update_user%TYPE DEFAULT NULL,
        update_time_in        IN co_sign.update_time%TYPE DEFAULT NULL,
        update_institution_in IN co_sign.update_institution%TYPE DEFAULT NULL,
        id_co_sign_out        IN OUT co_sign.id_co_sign%TYPE
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_task_in            IN co_sign.id_task%TYPE DEFAULT NULL,
        id_task_group_in      IN co_sign.id_task_group%TYPE DEFAULT NULL,
        id_task_type_in       IN co_sign.id_task_type%TYPE DEFAULT NULL,
        id_action_in          IN co_sign.id_action%TYPE DEFAULT NULL,
        id_order_type_in      IN co_sign.id_order_type%TYPE DEFAULT NULL,
        id_episode_in         IN co_sign.id_episode%TYPE DEFAULT NULL,
        id_prof_created_in    IN co_sign.id_prof_created%TYPE DEFAULT NULL,
        id_prof_ordered_by_in IN co_sign.id_prof_ordered_by%TYPE DEFAULT NULL,
        id_prof_co_signed_in  IN co_sign.id_prof_co_signed%TYPE DEFAULT NULL,
        dt_created_in         IN co_sign.dt_created%TYPE DEFAULT NULL,
        dt_ordered_by_in      IN co_sign.dt_ordered_by%TYPE DEFAULT NULL,
        dt_co_signed_in       IN co_sign.dt_co_signed%TYPE DEFAULT NULL,
        flg_status_in         IN co_sign.flg_status%TYPE DEFAULT NULL,
        flg_made_auth_in      IN co_sign.flg_made_auth%TYPE DEFAULT NULL,
        create_user_in        IN co_sign.create_user%TYPE DEFAULT NULL,
        create_time_in        IN co_sign.create_time%TYPE DEFAULT NULL,
        create_institution_in IN co_sign.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN co_sign.update_user%TYPE DEFAULT NULL,
        update_time_in        IN co_sign.update_time%TYPE DEFAULT NULL,
        update_institution_in IN co_sign.update_institution%TYPE DEFAULT NULL,
        id_co_sign_out        IN OUT co_sign.id_co_sign%TYPE
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert one row with function, return generated primary key
    FUNCTION ins
    (
        id_task_in            IN co_sign.id_task%TYPE DEFAULT NULL,
        id_task_group_in      IN co_sign.id_task_group%TYPE DEFAULT NULL,
        id_task_type_in       IN co_sign.id_task_type%TYPE DEFAULT NULL,
        id_action_in          IN co_sign.id_action%TYPE DEFAULT NULL,
        id_order_type_in      IN co_sign.id_order_type%TYPE DEFAULT NULL,
        id_episode_in         IN co_sign.id_episode%TYPE DEFAULT NULL,
        id_prof_created_in    IN co_sign.id_prof_created%TYPE DEFAULT NULL,
        id_prof_ordered_by_in IN co_sign.id_prof_ordered_by%TYPE DEFAULT NULL,
        id_prof_co_signed_in  IN co_sign.id_prof_co_signed%TYPE DEFAULT NULL,
        dt_created_in         IN co_sign.dt_created%TYPE DEFAULT NULL,
        dt_ordered_by_in      IN co_sign.dt_ordered_by%TYPE DEFAULT NULL,
        dt_co_signed_in       IN co_sign.dt_co_signed%TYPE DEFAULT NULL,
        flg_status_in         IN co_sign.flg_status%TYPE DEFAULT NULL,
        flg_made_auth_in      IN co_sign.flg_made_auth%TYPE DEFAULT NULL,
        create_user_in        IN co_sign.create_user%TYPE DEFAULT NULL,
        create_time_in        IN co_sign.create_time%TYPE DEFAULT NULL,
        create_institution_in IN co_sign.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN co_sign.update_user%TYPE DEFAULT NULL,
        update_time_in        IN co_sign.update_time%TYPE DEFAULT NULL,
        update_institution_in IN co_sign.update_institution%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) RETURN co_sign.id_co_sign%TYPE;

    FUNCTION ins
    (
        id_task_in            IN co_sign.id_task%TYPE DEFAULT NULL,
        id_task_group_in      IN co_sign.id_task_group%TYPE DEFAULT NULL,
        id_task_type_in       IN co_sign.id_task_type%TYPE DEFAULT NULL,
        id_action_in          IN co_sign.id_action%TYPE DEFAULT NULL,
        id_order_type_in      IN co_sign.id_order_type%TYPE DEFAULT NULL,
        id_episode_in         IN co_sign.id_episode%TYPE DEFAULT NULL,
        id_prof_created_in    IN co_sign.id_prof_created%TYPE DEFAULT NULL,
        id_prof_ordered_by_in IN co_sign.id_prof_ordered_by%TYPE DEFAULT NULL,
        id_prof_co_signed_in  IN co_sign.id_prof_co_signed%TYPE DEFAULT NULL,
        dt_created_in         IN co_sign.dt_created%TYPE DEFAULT NULL,
        dt_ordered_by_in      IN co_sign.dt_ordered_by%TYPE DEFAULT NULL,
        dt_co_signed_in       IN co_sign.dt_co_signed%TYPE DEFAULT NULL,
        flg_status_in         IN co_sign.flg_status%TYPE DEFAULT NULL,
        flg_made_auth_in      IN co_sign.flg_made_auth%TYPE DEFAULT NULL,
        create_user_in        IN co_sign.create_user%TYPE DEFAULT NULL,
        create_time_in        IN co_sign.create_time%TYPE DEFAULT NULL,
        create_institution_in IN co_sign.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN co_sign.update_user%TYPE DEFAULT NULL,
        update_time_in        IN co_sign.update_time%TYPE DEFAULT NULL,
        update_institution_in IN co_sign.update_institution%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    ) RETURN co_sign.id_co_sign%TYPE;

    -- Update any/all columns by primary key. If you pass NULL, then
    -- the current column value is set to itself. If you need a more
    -- selected UPDATE then use one of the onecol procedures below.

    PROCEDURE upd
    (
        id_co_sign_in          IN co_sign.id_co_sign%TYPE,
        id_task_in             IN co_sign.id_task%TYPE DEFAULT NULL,
        id_task_nin            IN BOOLEAN := TRUE,
        id_task_group_in       IN co_sign.id_task_group%TYPE DEFAULT NULL,
        id_task_group_nin      IN BOOLEAN := TRUE,
        id_task_type_in        IN co_sign.id_task_type%TYPE DEFAULT NULL,
        id_task_type_nin       IN BOOLEAN := TRUE,
        id_action_in           IN co_sign.id_action%TYPE DEFAULT NULL,
        id_action_nin          IN BOOLEAN := TRUE,
        id_order_type_in       IN co_sign.id_order_type%TYPE DEFAULT NULL,
        id_order_type_nin      IN BOOLEAN := TRUE,
        id_episode_in          IN co_sign.id_episode%TYPE DEFAULT NULL,
        id_episode_nin         IN BOOLEAN := TRUE,
        id_prof_created_in     IN co_sign.id_prof_created%TYPE DEFAULT NULL,
        id_prof_created_nin    IN BOOLEAN := TRUE,
        id_prof_ordered_by_in  IN co_sign.id_prof_ordered_by%TYPE DEFAULT NULL,
        id_prof_ordered_by_nin IN BOOLEAN := TRUE,
        id_prof_co_signed_in   IN co_sign.id_prof_co_signed%TYPE DEFAULT NULL,
        id_prof_co_signed_nin  IN BOOLEAN := TRUE,
        dt_created_in          IN co_sign.dt_created%TYPE DEFAULT NULL,
        dt_created_nin         IN BOOLEAN := TRUE,
        dt_ordered_by_in       IN co_sign.dt_ordered_by%TYPE DEFAULT NULL,
        dt_ordered_by_nin      IN BOOLEAN := TRUE,
        dt_co_signed_in        IN co_sign.dt_co_signed%TYPE DEFAULT NULL,
        dt_co_signed_nin       IN BOOLEAN := TRUE,
        flg_status_in          IN co_sign.flg_status%TYPE DEFAULT NULL,
        flg_status_nin         IN BOOLEAN := TRUE,
        flg_made_auth_in       IN co_sign.flg_made_auth%TYPE DEFAULT NULL,
        flg_made_auth_nin      IN BOOLEAN := TRUE,
        create_user_in         IN co_sign.create_user%TYPE DEFAULT NULL,
        create_user_nin        IN BOOLEAN := TRUE,
        create_time_in         IN co_sign.create_time%TYPE DEFAULT NULL,
        create_time_nin        IN BOOLEAN := TRUE,
        create_institution_in  IN co_sign.create_institution%TYPE DEFAULT NULL,
        create_institution_nin IN BOOLEAN := TRUE,
        update_user_in         IN co_sign.update_user%TYPE DEFAULT NULL,
        update_user_nin        IN BOOLEAN := TRUE,
        update_time_in         IN co_sign.update_time%TYPE DEFAULT NULL,
        update_time_nin        IN BOOLEAN := TRUE,
        update_institution_in  IN co_sign.update_institution%TYPE DEFAULT NULL,
        update_institution_nin IN BOOLEAN := TRUE,
        handle_error_in        IN BOOLEAN := TRUE,
        rows_out               IN OUT table_varchar
    );

    PROCEDURE upd
    (
        id_co_sign_in          IN co_sign.id_co_sign%TYPE,
        id_task_in             IN co_sign.id_task%TYPE DEFAULT NULL,
        id_task_nin            IN BOOLEAN := TRUE,
        id_task_group_in       IN co_sign.id_task_group%TYPE DEFAULT NULL,
        id_task_group_nin      IN BOOLEAN := TRUE,
        id_task_type_in        IN co_sign.id_task_type%TYPE DEFAULT NULL,
        id_task_type_nin       IN BOOLEAN := TRUE,
        id_action_in           IN co_sign.id_action%TYPE DEFAULT NULL,
        id_action_nin          IN BOOLEAN := TRUE,
        id_order_type_in       IN co_sign.id_order_type%TYPE DEFAULT NULL,
        id_order_type_nin      IN BOOLEAN := TRUE,
        id_episode_in          IN co_sign.id_episode%TYPE DEFAULT NULL,
        id_episode_nin         IN BOOLEAN := TRUE,
        id_prof_created_in     IN co_sign.id_prof_created%TYPE DEFAULT NULL,
        id_prof_created_nin    IN BOOLEAN := TRUE,
        id_prof_ordered_by_in  IN co_sign.id_prof_ordered_by%TYPE DEFAULT NULL,
        id_prof_ordered_by_nin IN BOOLEAN := TRUE,
        id_prof_co_signed_in   IN co_sign.id_prof_co_signed%TYPE DEFAULT NULL,
        id_prof_co_signed_nin  IN BOOLEAN := TRUE,
        dt_created_in          IN co_sign.dt_created%TYPE DEFAULT NULL,
        dt_created_nin         IN BOOLEAN := TRUE,
        dt_ordered_by_in       IN co_sign.dt_ordered_by%TYPE DEFAULT NULL,
        dt_ordered_by_nin      IN BOOLEAN := TRUE,
        dt_co_signed_in        IN co_sign.dt_co_signed%TYPE DEFAULT NULL,
        dt_co_signed_nin       IN BOOLEAN := TRUE,
        flg_status_in          IN co_sign.flg_status%TYPE DEFAULT NULL,
        flg_status_nin         IN BOOLEAN := TRUE,
        flg_made_auth_in       IN co_sign.flg_made_auth%TYPE DEFAULT NULL,
        flg_made_auth_nin      IN BOOLEAN := TRUE,
        create_user_in         IN co_sign.create_user%TYPE DEFAULT NULL,
        create_user_nin        IN BOOLEAN := TRUE,
        create_time_in         IN co_sign.create_time%TYPE DEFAULT NULL,
        create_time_nin        IN BOOLEAN := TRUE,
        create_institution_in  IN co_sign.create_institution%TYPE DEFAULT NULL,
        create_institution_nin IN BOOLEAN := TRUE,
        update_user_in         IN co_sign.update_user%TYPE DEFAULT NULL,
        update_user_nin        IN BOOLEAN := TRUE,
        update_time_in         IN co_sign.update_time%TYPE DEFAULT NULL,
        update_time_nin        IN BOOLEAN := TRUE,
        update_institution_in  IN co_sign.update_institution%TYPE DEFAULT NULL,
        update_institution_nin IN BOOLEAN := TRUE,
        handle_error_in        IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        id_task_in             IN co_sign.id_task%TYPE DEFAULT NULL,
        id_task_nin            IN BOOLEAN := TRUE,
        id_task_group_in       IN co_sign.id_task_group%TYPE DEFAULT NULL,
        id_task_group_nin      IN BOOLEAN := TRUE,
        id_task_type_in        IN co_sign.id_task_type%TYPE DEFAULT NULL,
        id_task_type_nin       IN BOOLEAN := TRUE,
        id_action_in           IN co_sign.id_action%TYPE DEFAULT NULL,
        id_action_nin          IN BOOLEAN := TRUE,
        id_order_type_in       IN co_sign.id_order_type%TYPE DEFAULT NULL,
        id_order_type_nin      IN BOOLEAN := TRUE,
        id_episode_in          IN co_sign.id_episode%TYPE DEFAULT NULL,
        id_episode_nin         IN BOOLEAN := TRUE,
        id_prof_created_in     IN co_sign.id_prof_created%TYPE DEFAULT NULL,
        id_prof_created_nin    IN BOOLEAN := TRUE,
        id_prof_ordered_by_in  IN co_sign.id_prof_ordered_by%TYPE DEFAULT NULL,
        id_prof_ordered_by_nin IN BOOLEAN := TRUE,
        id_prof_co_signed_in   IN co_sign.id_prof_co_signed%TYPE DEFAULT NULL,
        id_prof_co_signed_nin  IN BOOLEAN := TRUE,
        dt_created_in          IN co_sign.dt_created%TYPE DEFAULT NULL,
        dt_created_nin         IN BOOLEAN := TRUE,
        dt_ordered_by_in       IN co_sign.dt_ordered_by%TYPE DEFAULT NULL,
        dt_ordered_by_nin      IN BOOLEAN := TRUE,
        dt_co_signed_in        IN co_sign.dt_co_signed%TYPE DEFAULT NULL,
        dt_co_signed_nin       IN BOOLEAN := TRUE,
        flg_status_in          IN co_sign.flg_status%TYPE DEFAULT NULL,
        flg_status_nin         IN BOOLEAN := TRUE,
        flg_made_auth_in       IN co_sign.flg_made_auth%TYPE DEFAULT NULL,
        flg_made_auth_nin      IN BOOLEAN := TRUE,
        create_user_in         IN co_sign.create_user%TYPE DEFAULT NULL,
        create_user_nin        IN BOOLEAN := TRUE,
        create_time_in         IN co_sign.create_time%TYPE DEFAULT NULL,
        create_time_nin        IN BOOLEAN := TRUE,
        create_institution_in  IN co_sign.create_institution%TYPE DEFAULT NULL,
        create_institution_nin IN BOOLEAN := TRUE,
        update_user_in         IN co_sign.update_user%TYPE DEFAULT NULL,
        update_user_nin        IN BOOLEAN := TRUE,
        update_time_in         IN co_sign.update_time%TYPE DEFAULT NULL,
        update_time_nin        IN BOOLEAN := TRUE,
        update_institution_in  IN co_sign.update_institution%TYPE DEFAULT NULL,
        update_institution_nin IN BOOLEAN := TRUE,
        where_in               VARCHAR2,
        handle_error_in        IN BOOLEAN := TRUE,
        rows_out               IN OUT table_varchar
    );

    PROCEDURE upd
    (
        id_task_in             IN co_sign.id_task%TYPE DEFAULT NULL,
        id_task_nin            IN BOOLEAN := TRUE,
        id_task_group_in       IN co_sign.id_task_group%TYPE DEFAULT NULL,
        id_task_group_nin      IN BOOLEAN := TRUE,
        id_task_type_in        IN co_sign.id_task_type%TYPE DEFAULT NULL,
        id_task_type_nin       IN BOOLEAN := TRUE,
        id_action_in           IN co_sign.id_action%TYPE DEFAULT NULL,
        id_action_nin          IN BOOLEAN := TRUE,
        id_order_type_in       IN co_sign.id_order_type%TYPE DEFAULT NULL,
        id_order_type_nin      IN BOOLEAN := TRUE,
        id_episode_in          IN co_sign.id_episode%TYPE DEFAULT NULL,
        id_episode_nin         IN BOOLEAN := TRUE,
        id_prof_created_in     IN co_sign.id_prof_created%TYPE DEFAULT NULL,
        id_prof_created_nin    IN BOOLEAN := TRUE,
        id_prof_ordered_by_in  IN co_sign.id_prof_ordered_by%TYPE DEFAULT NULL,
        id_prof_ordered_by_nin IN BOOLEAN := TRUE,
        id_prof_co_signed_in   IN co_sign.id_prof_co_signed%TYPE DEFAULT NULL,
        id_prof_co_signed_nin  IN BOOLEAN := TRUE,
        dt_created_in          IN co_sign.dt_created%TYPE DEFAULT NULL,
        dt_created_nin         IN BOOLEAN := TRUE,
        dt_ordered_by_in       IN co_sign.dt_ordered_by%TYPE DEFAULT NULL,
        dt_ordered_by_nin      IN BOOLEAN := TRUE,
        dt_co_signed_in        IN co_sign.dt_co_signed%TYPE DEFAULT NULL,
        dt_co_signed_nin       IN BOOLEAN := TRUE,
        flg_status_in          IN co_sign.flg_status%TYPE DEFAULT NULL,
        flg_status_nin         IN BOOLEAN := TRUE,
        flg_made_auth_in       IN co_sign.flg_made_auth%TYPE DEFAULT NULL,
        flg_made_auth_nin      IN BOOLEAN := TRUE,
        create_user_in         IN co_sign.create_user%TYPE DEFAULT NULL,
        create_user_nin        IN BOOLEAN := TRUE,
        create_time_in         IN co_sign.create_time%TYPE DEFAULT NULL,
        create_time_nin        IN BOOLEAN := TRUE,
        create_institution_in  IN co_sign.create_institution%TYPE DEFAULT NULL,
        create_institution_nin IN BOOLEAN := TRUE,
        update_user_in         IN co_sign.update_user%TYPE DEFAULT NULL,
        update_user_nin        IN BOOLEAN := TRUE,
        update_time_in         IN co_sign.update_time%TYPE DEFAULT NULL,
        update_time_nin        IN BOOLEAN := TRUE,
        update_institution_in  IN co_sign.update_institution%TYPE DEFAULT NULL,
        update_institution_nin IN BOOLEAN := TRUE,
        where_in               VARCHAR2,
        handle_error_in        IN BOOLEAN := TRUE
    );

    PROCEDURE upd_ins
    (
        id_co_sign_in         IN co_sign.id_co_sign%TYPE,
        id_task_in            IN co_sign.id_task%TYPE DEFAULT NULL,
        id_task_group_in      IN co_sign.id_task_group%TYPE DEFAULT NULL,
        id_task_type_in       IN co_sign.id_task_type%TYPE DEFAULT NULL,
        id_action_in          IN co_sign.id_action%TYPE DEFAULT NULL,
        id_order_type_in      IN co_sign.id_order_type%TYPE DEFAULT NULL,
        id_episode_in         IN co_sign.id_episode%TYPE DEFAULT NULL,
        id_prof_created_in    IN co_sign.id_prof_created%TYPE DEFAULT NULL,
        id_prof_ordered_by_in IN co_sign.id_prof_ordered_by%TYPE DEFAULT NULL,
        id_prof_co_signed_in  IN co_sign.id_prof_co_signed%TYPE DEFAULT NULL,
        dt_created_in         IN co_sign.dt_created%TYPE DEFAULT NULL,
        dt_ordered_by_in      IN co_sign.dt_ordered_by%TYPE DEFAULT NULL,
        dt_co_signed_in       IN co_sign.dt_co_signed%TYPE DEFAULT NULL,
        flg_status_in         IN co_sign.flg_status%TYPE DEFAULT NULL,
        flg_made_auth_in      IN co_sign.flg_made_auth%TYPE DEFAULT NULL,
        create_user_in        IN co_sign.create_user%TYPE DEFAULT NULL,
        create_time_in        IN co_sign.create_time%TYPE DEFAULT NULL,
        create_institution_in IN co_sign.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN co_sign.update_user%TYPE DEFAULT NULL,
        update_time_in        IN co_sign.update_time%TYPE DEFAULT NULL,
        update_institution_in IN co_sign.update_institution%TYPE DEFAULT NULL,
        handle_error_in       IN BOOLEAN := TRUE,
        rows_out              OUT table_varchar
    );

    PROCEDURE upd_ins
    (
        id_co_sign_in         IN co_sign.id_co_sign%TYPE,
        id_task_in            IN co_sign.id_task%TYPE DEFAULT NULL,
        id_task_group_in      IN co_sign.id_task_group%TYPE DEFAULT NULL,
        id_task_type_in       IN co_sign.id_task_type%TYPE DEFAULT NULL,
        id_action_in          IN co_sign.id_action%TYPE DEFAULT NULL,
        id_order_type_in      IN co_sign.id_order_type%TYPE DEFAULT NULL,
        id_episode_in         IN co_sign.id_episode%TYPE DEFAULT NULL,
        id_prof_created_in    IN co_sign.id_prof_created%TYPE DEFAULT NULL,
        id_prof_ordered_by_in IN co_sign.id_prof_ordered_by%TYPE DEFAULT NULL,
        id_prof_co_signed_in  IN co_sign.id_prof_co_signed%TYPE DEFAULT NULL,
        dt_created_in         IN co_sign.dt_created%TYPE DEFAULT NULL,
        dt_ordered_by_in      IN co_sign.dt_ordered_by%TYPE DEFAULT NULL,
        dt_co_signed_in       IN co_sign.dt_co_signed%TYPE DEFAULT NULL,
        flg_status_in         IN co_sign.flg_status%TYPE DEFAULT NULL,
        flg_made_auth_in      IN co_sign.flg_made_auth%TYPE DEFAULT NULL,
        create_user_in        IN co_sign.create_user%TYPE DEFAULT NULL,
        create_time_in        IN co_sign.create_time%TYPE DEFAULT NULL,
        create_institution_in IN co_sign.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN co_sign.update_user%TYPE DEFAULT NULL,
        update_time_in        IN co_sign.update_time%TYPE DEFAULT NULL,
        update_institution_in IN co_sign.update_institution%TYPE DEFAULT NULL,
        handle_error_in       IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        rec_in          IN co_sign%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        IN OUT table_varchar
    );

    PROCEDURE upd
    (
        rec_in          IN co_sign%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        col_in            IN co_sign_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          IN OUT table_varchar
    );

    PROCEDURE upd
    (
        col_in            IN co_sign_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE
    );

    -- Use Native Dynamic SQL increment a single NUMBER column
    -- for all rows specified by the dynamic WHERE clause
    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_co_sign_in   IN co_sign.id_co_sign%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_co_sign_in   IN co_sign.id_co_sign%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for primary key column ID_CO_SIGN
    PROCEDURE del_id_co_sign
    (
        id_co_sign_in   IN co_sign.id_co_sign%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for primary key column ID_CO_SIGN
    PROCEDURE del_id_co_sign
    (
        id_co_sign_in   IN co_sign.id_co_sign%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete for unique value of CO_SIGN_UK
    PROCEDURE del_co_sign_uk
    (
        id_task_in      IN co_sign.id_task%TYPE,
        id_task_type_in IN co_sign.id_task_type%TYPE,
        id_action_in    IN co_sign.id_action%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete for unique value of CO_SIGN_UK
    PROCEDURE del_co_sign_uk
    (
        id_task_in      IN co_sign.id_task%TYPE,
        id_task_type_in IN co_sign.id_task_type%TYPE,
        id_action_in    IN co_sign.id_action%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete for unique value of CS_PK_IDX
    PROCEDURE del_cs_pk_idx
    (
        id_task_in      IN co_sign.id_task%TYPE,
        id_task_type_in IN co_sign.id_task_type%TYPE,
        id_action_in    IN co_sign.id_action%TYPE,
        id_co_sign_in   IN co_sign.id_co_sign%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete for unique value of CS_PK_IDX
    PROCEDURE del_cs_pk_idx
    (
        id_task_in      IN co_sign.id_task%TYPE,
        id_task_type_in IN co_sign.id_task_type%TYPE,
        id_action_in    IN co_sign.id_action%TYPE,
        id_co_sign_in   IN co_sign.id_co_sign%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for this CS_EPISODE_FK foreign key value
    PROCEDURE del_cs_episode_fk
    (
        id_episode_in   IN co_sign.id_episode%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this CS_EPISODE_FK foreign key value
    PROCEDURE del_cs_episode_fk
    (
        id_episode_in   IN co_sign.id_episode%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for this CS_ORDER_TYPE_FK foreign key value
    PROCEDURE del_cs_order_type_fk
    (
        id_order_type_in IN co_sign.id_order_type%TYPE,
        handle_error_in  IN BOOLEAN := TRUE
    );

    -- Delete all rows for this CS_ORDER_TYPE_FK foreign key value
    PROCEDURE del_cs_order_type_fk
    (
        id_order_type_in IN co_sign.id_order_type%TYPE,
        handle_error_in  IN BOOLEAN := TRUE,
        rows_out         OUT table_varchar
    );

    -- Delete all rows for this CS_PROF_CONSIGNED_FK foreign key value
    PROCEDURE del_cs_prof_consigned_fk
    (
        id_prof_co_signed_in IN co_sign.id_prof_co_signed%TYPE,
        handle_error_in      IN BOOLEAN := TRUE
    );

    -- Delete all rows for this CS_PROF_CONSIGNED_FK foreign key value
    PROCEDURE del_cs_prof_consigned_fk
    (
        id_prof_co_signed_in IN co_sign.id_prof_co_signed%TYPE,
        handle_error_in      IN BOOLEAN := TRUE,
        rows_out             OUT table_varchar
    );

    -- Delete all rows for this CS_PROF_CREATED_FK foreign key value
    PROCEDURE del_cs_prof_created_fk
    (
        id_prof_created_in IN co_sign.id_prof_created%TYPE,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete all rows for this CS_PROF_CREATED_FK foreign key value
    PROCEDURE del_cs_prof_created_fk
    (
        id_prof_created_in IN co_sign.id_prof_created%TYPE,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    -- Delete all rows for this CS_PROF_ORD_BY_FK foreign key value
    PROCEDURE del_cs_prof_ord_by_fk
    (
        id_prof_ordered_by_in IN co_sign.id_prof_ordered_by%TYPE,
        handle_error_in       IN BOOLEAN := TRUE
    );

    -- Delete all rows for this CS_PROF_ORD_BY_FK foreign key value
    PROCEDURE del_cs_prof_ord_by_fk
    (
        id_prof_ordered_by_in IN co_sign.id_prof_ordered_by%TYPE,
        handle_error_in       IN BOOLEAN := TRUE,
        rows_out              OUT table_varchar
    );

    -- Delete all rows for this CS_TASK_TYPE_ACTION_FK foreign key value
    PROCEDURE del_cs_task_type_action_fk
    (
        id_task_type_in IN co_sign.id_task_type%TYPE,
        id_action_in    IN co_sign.id_action%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this CS_TASK_TYPE_ACTION_FK foreign key value
    PROCEDURE del_cs_task_type_action_fk
    (
        id_task_type_in IN co_sign.id_task_type%TYPE,
        id_action_in    IN co_sign.id_action%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Initialize a record with default values for columns in the table.
    PROCEDURE initrec(co_sign_inout IN OUT co_sign%ROWTYPE);

    FUNCTION initrec RETURN co_sign%ROWTYPE;

    FUNCTION get_data_rowid(rows_in IN table_varchar) RETURN co_sign_tc;

    FUNCTION get_data_rowid_pat(rows_in IN table_varchar) RETURN co_sign_tc;

END ts_co_sign;
/
