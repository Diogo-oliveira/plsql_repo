/*-- Last Change Revision: $Rev: 2029378 $*/
/*-- Last Change by: $Author: mario.fernandes $*/
/*-- Date of last change: $Date: 2022-08-02 18:51:20 +0100 (ter, 02 ago 2022) $*/
CREATE OR REPLACE PACKAGE ts_sch_resource
/*
| Generated by or retrieved from QCGU - DO NOT MODIFY!
| QCGU - "Get it right, do it fast" - www.ToadWorld.com
| QCGU Universal ID: {1BD37A66-EA60-4927-9A64-F6DD89237236}
| Created On: Novembro 12, 2009 10:3:29
| Created By: ALERT
*/
 IS

    -- Collection of %ROWTYPE records based on "SCH_RESOURCE"
    TYPE sch_resource_tc IS TABLE OF sch_resource%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE sch_resource_ntt IS TABLE OF sch_resource%ROWTYPE;
    TYPE sch_resource_vat IS VARRAY(100) OF sch_resource%ROWTYPE;

    -- Same type structure, with a static name.
    TYPE aat IS TABLE OF sch_resource%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE ntt IS TABLE OF sch_resource%ROWTYPE;
    TYPE vat IS VARRAY(100) OF sch_resource%ROWTYPE;

    -- Column Collection based on column "ID_SCH_RESOURCE"
    TYPE id_sch_resource_cc IS TABLE OF sch_resource.id_sch_resource%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_SCHEDULE"
    TYPE id_schedule_cc IS TABLE OF sch_resource.id_schedule%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_INSTITUTION"
    TYPE id_institution_cc IS TABLE OF sch_resource.id_institution%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_PROFESSIONAL"
    TYPE id_professional_cc IS TABLE OF sch_resource.id_professional%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "DT_SCH_RESOURCE_TSTZ"
    TYPE dt_sch_resource_tstz_cc IS TABLE OF sch_resource.dt_sch_resource_tstz%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_USER"
    TYPE create_user_cc IS TABLE OF sch_resource.create_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_TIME"
    TYPE create_time_cc IS TABLE OF sch_resource.create_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_INSTITUTION"
    TYPE create_institution_cc IS TABLE OF sch_resource.create_institution%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_USER"
    TYPE update_user_cc IS TABLE OF sch_resource.update_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_TIME"
    TYPE update_time_cc IS TABLE OF sch_resource.update_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_INSTITUTION"
    TYPE update_institution_cc IS TABLE OF sch_resource.update_institution%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "FLG_LEADER"
    TYPE flg_leader_cc IS TABLE OF sch_resource.flg_leader%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_SCH_CONSULT_VACANCY"
    TYPE id_sch_consult_vacancy_cc IS TABLE OF sch_resource.id_sch_consult_vacancy%TYPE INDEX BY BINARY_INTEGER;

    -- Insert one row, providing primary key if present
    PROCEDURE ins
    (
        id_sch_resource_in        IN sch_resource.id_sch_resource%TYPE,
        id_schedule_in            IN sch_resource.id_schedule%TYPE DEFAULT NULL,
        id_institution_in         IN sch_resource.id_institution%TYPE DEFAULT 2,
        id_professional_in        IN sch_resource.id_professional%TYPE DEFAULT NULL,
        dt_sch_resource_tstz_in   IN sch_resource.dt_sch_resource_tstz%TYPE DEFAULT current_timestamp,
        create_user_in            IN sch_resource.create_user%TYPE DEFAULT NULL,
        create_time_in            IN sch_resource.create_time%TYPE DEFAULT NULL,
        create_institution_in     IN sch_resource.create_institution%TYPE DEFAULT NULL,
        update_user_in            IN sch_resource.update_user%TYPE DEFAULT NULL,
        update_time_in            IN sch_resource.update_time%TYPE DEFAULT NULL,
        update_institution_in     IN sch_resource.update_institution%TYPE DEFAULT NULL,
        flg_leader_in             IN sch_resource.flg_leader%TYPE DEFAULT NULL,
        id_sch_consult_vacancy_in IN sch_resource.id_sch_consult_vacancy%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_sch_resource_in        IN sch_resource.id_sch_resource%TYPE,
        id_schedule_in            IN sch_resource.id_schedule%TYPE DEFAULT NULL,
        id_institution_in         IN sch_resource.id_institution%TYPE DEFAULT 2,
        id_professional_in        IN sch_resource.id_professional%TYPE DEFAULT NULL,
        dt_sch_resource_tstz_in   IN sch_resource.dt_sch_resource_tstz%TYPE DEFAULT current_timestamp,
        create_user_in            IN sch_resource.create_user%TYPE DEFAULT NULL,
        create_time_in            IN sch_resource.create_time%TYPE DEFAULT NULL,
        create_institution_in     IN sch_resource.create_institution%TYPE DEFAULT NULL,
        update_user_in            IN sch_resource.update_user%TYPE DEFAULT NULL,
        update_time_in            IN sch_resource.update_time%TYPE DEFAULT NULL,
        update_institution_in     IN sch_resource.update_institution%TYPE DEFAULT NULL,
        flg_leader_in             IN sch_resource.flg_leader%TYPE DEFAULT NULL,
        id_sch_consult_vacancy_in IN sch_resource.id_sch_consult_vacancy%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a row based on a record.
    -- Specify whether or not a primary key value should be generated.
    PROCEDURE ins
    (
        rec_in          IN sch_resource%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rec_in          IN sch_resource%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a collection of rows using FORALL; all primary key values
    -- must have already been generated, or are handled in triggers.
    PROCEDURE ins
    (
        rows_in         IN sch_resource_tc,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rows_in         IN sch_resource_tc,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Return next primary key value using the named sequence.
    FUNCTION next_key(sequence_in IN VARCHAR2 := NULL) RETURN sch_resource.id_sch_resource%TYPE;

    -- Insert one row, generating hidden primary key using a sequence
    PROCEDURE ins
    (
        id_schedule_in            IN sch_resource.id_schedule%TYPE DEFAULT NULL,
        id_institution_in         IN sch_resource.id_institution%TYPE DEFAULT 2,
        id_professional_in        IN sch_resource.id_professional%TYPE DEFAULT NULL,
        dt_sch_resource_tstz_in   IN sch_resource.dt_sch_resource_tstz%TYPE DEFAULT current_timestamp,
        create_user_in            IN sch_resource.create_user%TYPE DEFAULT NULL,
        create_time_in            IN sch_resource.create_time%TYPE DEFAULT NULL,
        create_institution_in     IN sch_resource.create_institution%TYPE DEFAULT NULL,
        update_user_in            IN sch_resource.update_user%TYPE DEFAULT NULL,
        update_time_in            IN sch_resource.update_time%TYPE DEFAULT NULL,
        update_institution_in     IN sch_resource.update_institution%TYPE DEFAULT NULL,
        flg_leader_in             IN sch_resource.flg_leader%TYPE DEFAULT NULL,
        id_sch_consult_vacancy_in IN sch_resource.id_sch_consult_vacancy%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_schedule_in            IN sch_resource.id_schedule%TYPE DEFAULT NULL,
        id_institution_in         IN sch_resource.id_institution%TYPE DEFAULT 2,
        id_professional_in        IN sch_resource.id_professional%TYPE DEFAULT NULL,
        dt_sch_resource_tstz_in   IN sch_resource.dt_sch_resource_tstz%TYPE DEFAULT current_timestamp,
        create_user_in            IN sch_resource.create_user%TYPE DEFAULT NULL,
        create_time_in            IN sch_resource.create_time%TYPE DEFAULT NULL,
        create_institution_in     IN sch_resource.create_institution%TYPE DEFAULT NULL,
        update_user_in            IN sch_resource.update_user%TYPE DEFAULT NULL,
        update_time_in            IN sch_resource.update_time%TYPE DEFAULT NULL,
        update_institution_in     IN sch_resource.update_institution%TYPE DEFAULT NULL,
        flg_leader_in             IN sch_resource.flg_leader%TYPE DEFAULT NULL,
        id_sch_consult_vacancy_in IN sch_resource.id_sch_consult_vacancy%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert one row, returning primary key generated by sequence
    PROCEDURE ins
    (
        id_schedule_in            IN sch_resource.id_schedule%TYPE DEFAULT NULL,
        id_institution_in         IN sch_resource.id_institution%TYPE DEFAULT 2,
        id_professional_in        IN sch_resource.id_professional%TYPE DEFAULT NULL,
        dt_sch_resource_tstz_in   IN sch_resource.dt_sch_resource_tstz%TYPE DEFAULT current_timestamp,
        create_user_in            IN sch_resource.create_user%TYPE DEFAULT NULL,
        create_time_in            IN sch_resource.create_time%TYPE DEFAULT NULL,
        create_institution_in     IN sch_resource.create_institution%TYPE DEFAULT NULL,
        update_user_in            IN sch_resource.update_user%TYPE DEFAULT NULL,
        update_time_in            IN sch_resource.update_time%TYPE DEFAULT NULL,
        update_institution_in     IN sch_resource.update_institution%TYPE DEFAULT NULL,
        flg_leader_in             IN sch_resource.flg_leader%TYPE DEFAULT NULL,
        id_sch_consult_vacancy_in IN sch_resource.id_sch_consult_vacancy%TYPE DEFAULT NULL,
        id_sch_resource_out       IN OUT sch_resource.id_sch_resource%TYPE
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_schedule_in            IN sch_resource.id_schedule%TYPE DEFAULT NULL,
        id_institution_in         IN sch_resource.id_institution%TYPE DEFAULT 2,
        id_professional_in        IN sch_resource.id_professional%TYPE DEFAULT NULL,
        dt_sch_resource_tstz_in   IN sch_resource.dt_sch_resource_tstz%TYPE DEFAULT current_timestamp,
        create_user_in            IN sch_resource.create_user%TYPE DEFAULT NULL,
        create_time_in            IN sch_resource.create_time%TYPE DEFAULT NULL,
        create_institution_in     IN sch_resource.create_institution%TYPE DEFAULT NULL,
        update_user_in            IN sch_resource.update_user%TYPE DEFAULT NULL,
        update_time_in            IN sch_resource.update_time%TYPE DEFAULT NULL,
        update_institution_in     IN sch_resource.update_institution%TYPE DEFAULT NULL,
        flg_leader_in             IN sch_resource.flg_leader%TYPE DEFAULT NULL,
        id_sch_consult_vacancy_in IN sch_resource.id_sch_consult_vacancy%TYPE DEFAULT NULL,
        id_sch_resource_out       IN OUT sch_resource.id_sch_resource%TYPE
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert one row with function, return generated primary key
    FUNCTION ins
    (
        id_schedule_in            IN sch_resource.id_schedule%TYPE DEFAULT NULL,
        id_institution_in         IN sch_resource.id_institution%TYPE DEFAULT 2,
        id_professional_in        IN sch_resource.id_professional%TYPE DEFAULT NULL,
        dt_sch_resource_tstz_in   IN sch_resource.dt_sch_resource_tstz%TYPE DEFAULT current_timestamp,
        create_user_in            IN sch_resource.create_user%TYPE DEFAULT NULL,
        create_time_in            IN sch_resource.create_time%TYPE DEFAULT NULL,
        create_institution_in     IN sch_resource.create_institution%TYPE DEFAULT NULL,
        update_user_in            IN sch_resource.update_user%TYPE DEFAULT NULL,
        update_time_in            IN sch_resource.update_time%TYPE DEFAULT NULL,
        update_institution_in     IN sch_resource.update_institution%TYPE DEFAULT NULL,
        flg_leader_in             IN sch_resource.flg_leader%TYPE DEFAULT NULL,
        id_sch_consult_vacancy_in IN sch_resource.id_sch_consult_vacancy%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) RETURN sch_resource.id_sch_resource%TYPE;

    FUNCTION ins
    (
        id_schedule_in            IN sch_resource.id_schedule%TYPE DEFAULT NULL,
        id_institution_in         IN sch_resource.id_institution%TYPE DEFAULT 2,
        id_professional_in        IN sch_resource.id_professional%TYPE DEFAULT NULL,
        dt_sch_resource_tstz_in   IN sch_resource.dt_sch_resource_tstz%TYPE DEFAULT current_timestamp,
        create_user_in            IN sch_resource.create_user%TYPE DEFAULT NULL,
        create_time_in            IN sch_resource.create_time%TYPE DEFAULT NULL,
        create_institution_in     IN sch_resource.create_institution%TYPE DEFAULT NULL,
        update_user_in            IN sch_resource.update_user%TYPE DEFAULT NULL,
        update_time_in            IN sch_resource.update_time%TYPE DEFAULT NULL,
        update_institution_in     IN sch_resource.update_institution%TYPE DEFAULT NULL,
        flg_leader_in             IN sch_resource.flg_leader%TYPE DEFAULT NULL,
        id_sch_consult_vacancy_in IN sch_resource.id_sch_consult_vacancy%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    ) RETURN sch_resource.id_sch_resource%TYPE;

    -- Update any/all columns by primary key. If you pass NULL, then
    -- the current column value is set to itself. If you need a more
    -- selected UPDATE then use one of the onecol procedures below.

    PROCEDURE upd
    (
        id_sch_resource_in         IN sch_resource.id_sch_resource%TYPE,
        id_schedule_in             IN sch_resource.id_schedule%TYPE DEFAULT NULL,
        id_schedule_nin            IN BOOLEAN := TRUE,
        id_institution_in          IN sch_resource.id_institution%TYPE DEFAULT NULL,
        id_institution_nin         IN BOOLEAN := TRUE,
        id_professional_in         IN sch_resource.id_professional%TYPE DEFAULT NULL,
        id_professional_nin        IN BOOLEAN := TRUE,
        dt_sch_resource_tstz_in    IN sch_resource.dt_sch_resource_tstz%TYPE DEFAULT NULL,
        dt_sch_resource_tstz_nin   IN BOOLEAN := TRUE,
        create_user_in             IN sch_resource.create_user%TYPE DEFAULT NULL,
        create_user_nin            IN BOOLEAN := TRUE,
        create_time_in             IN sch_resource.create_time%TYPE DEFAULT NULL,
        create_time_nin            IN BOOLEAN := TRUE,
        create_institution_in      IN sch_resource.create_institution%TYPE DEFAULT NULL,
        create_institution_nin     IN BOOLEAN := TRUE,
        update_user_in             IN sch_resource.update_user%TYPE DEFAULT NULL,
        update_user_nin            IN BOOLEAN := TRUE,
        update_time_in             IN sch_resource.update_time%TYPE DEFAULT NULL,
        update_time_nin            IN BOOLEAN := TRUE,
        update_institution_in      IN sch_resource.update_institution%TYPE DEFAULT NULL,
        update_institution_nin     IN BOOLEAN := TRUE,
        flg_leader_in              IN sch_resource.flg_leader%TYPE DEFAULT NULL,
        flg_leader_nin             IN BOOLEAN := TRUE,
        id_sch_consult_vacancy_in  IN sch_resource.id_sch_consult_vacancy%TYPE DEFAULT NULL,
        id_sch_consult_vacancy_nin IN BOOLEAN := TRUE,
        handle_error_in            IN BOOLEAN := TRUE,
        rows_out                   IN OUT table_varchar
    );

    PROCEDURE upd
    (
        id_sch_resource_in         IN sch_resource.id_sch_resource%TYPE,
        id_schedule_in             IN sch_resource.id_schedule%TYPE DEFAULT NULL,
        id_schedule_nin            IN BOOLEAN := TRUE,
        id_institution_in          IN sch_resource.id_institution%TYPE DEFAULT NULL,
        id_institution_nin         IN BOOLEAN := TRUE,
        id_professional_in         IN sch_resource.id_professional%TYPE DEFAULT NULL,
        id_professional_nin        IN BOOLEAN := TRUE,
        dt_sch_resource_tstz_in    IN sch_resource.dt_sch_resource_tstz%TYPE DEFAULT NULL,
        dt_sch_resource_tstz_nin   IN BOOLEAN := TRUE,
        create_user_in             IN sch_resource.create_user%TYPE DEFAULT NULL,
        create_user_nin            IN BOOLEAN := TRUE,
        create_time_in             IN sch_resource.create_time%TYPE DEFAULT NULL,
        create_time_nin            IN BOOLEAN := TRUE,
        create_institution_in      IN sch_resource.create_institution%TYPE DEFAULT NULL,
        create_institution_nin     IN BOOLEAN := TRUE,
        update_user_in             IN sch_resource.update_user%TYPE DEFAULT NULL,
        update_user_nin            IN BOOLEAN := TRUE,
        update_time_in             IN sch_resource.update_time%TYPE DEFAULT NULL,
        update_time_nin            IN BOOLEAN := TRUE,
        update_institution_in      IN sch_resource.update_institution%TYPE DEFAULT NULL,
        update_institution_nin     IN BOOLEAN := TRUE,
        flg_leader_in              IN sch_resource.flg_leader%TYPE DEFAULT NULL,
        flg_leader_nin             IN BOOLEAN := TRUE,
        id_sch_consult_vacancy_in  IN sch_resource.id_sch_consult_vacancy%TYPE DEFAULT NULL,
        id_sch_consult_vacancy_nin IN BOOLEAN := TRUE,
        handle_error_in            IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        id_schedule_in             IN sch_resource.id_schedule%TYPE DEFAULT NULL,
        id_schedule_nin            IN BOOLEAN := TRUE,
        id_institution_in          IN sch_resource.id_institution%TYPE DEFAULT NULL,
        id_institution_nin         IN BOOLEAN := TRUE,
        id_professional_in         IN sch_resource.id_professional%TYPE DEFAULT NULL,
        id_professional_nin        IN BOOLEAN := TRUE,
        dt_sch_resource_tstz_in    IN sch_resource.dt_sch_resource_tstz%TYPE DEFAULT NULL,
        dt_sch_resource_tstz_nin   IN BOOLEAN := TRUE,
        create_user_in             IN sch_resource.create_user%TYPE DEFAULT NULL,
        create_user_nin            IN BOOLEAN := TRUE,
        create_time_in             IN sch_resource.create_time%TYPE DEFAULT NULL,
        create_time_nin            IN BOOLEAN := TRUE,
        create_institution_in      IN sch_resource.create_institution%TYPE DEFAULT NULL,
        create_institution_nin     IN BOOLEAN := TRUE,
        update_user_in             IN sch_resource.update_user%TYPE DEFAULT NULL,
        update_user_nin            IN BOOLEAN := TRUE,
        update_time_in             IN sch_resource.update_time%TYPE DEFAULT NULL,
        update_time_nin            IN BOOLEAN := TRUE,
        update_institution_in      IN sch_resource.update_institution%TYPE DEFAULT NULL,
        update_institution_nin     IN BOOLEAN := TRUE,
        flg_leader_in              IN sch_resource.flg_leader%TYPE DEFAULT NULL,
        flg_leader_nin             IN BOOLEAN := TRUE,
        id_sch_consult_vacancy_in  IN sch_resource.id_sch_consult_vacancy%TYPE DEFAULT NULL,
        id_sch_consult_vacancy_nin IN BOOLEAN := TRUE,
        where_in                   VARCHAR2 DEFAULT NULL,
        handle_error_in            IN BOOLEAN := TRUE,
        rows_out                   IN OUT table_varchar
    );

    PROCEDURE upd
    (
        id_schedule_in             IN sch_resource.id_schedule%TYPE DEFAULT NULL,
        id_schedule_nin            IN BOOLEAN := TRUE,
        id_institution_in          IN sch_resource.id_institution%TYPE DEFAULT NULL,
        id_institution_nin         IN BOOLEAN := TRUE,
        id_professional_in         IN sch_resource.id_professional%TYPE DEFAULT NULL,
        id_professional_nin        IN BOOLEAN := TRUE,
        dt_sch_resource_tstz_in    IN sch_resource.dt_sch_resource_tstz%TYPE DEFAULT NULL,
        dt_sch_resource_tstz_nin   IN BOOLEAN := TRUE,
        create_user_in             IN sch_resource.create_user%TYPE DEFAULT NULL,
        create_user_nin            IN BOOLEAN := TRUE,
        create_time_in             IN sch_resource.create_time%TYPE DEFAULT NULL,
        create_time_nin            IN BOOLEAN := TRUE,
        create_institution_in      IN sch_resource.create_institution%TYPE DEFAULT NULL,
        create_institution_nin     IN BOOLEAN := TRUE,
        update_user_in             IN sch_resource.update_user%TYPE DEFAULT NULL,
        update_user_nin            IN BOOLEAN := TRUE,
        update_time_in             IN sch_resource.update_time%TYPE DEFAULT NULL,
        update_time_nin            IN BOOLEAN := TRUE,
        update_institution_in      IN sch_resource.update_institution%TYPE DEFAULT NULL,
        update_institution_nin     IN BOOLEAN := TRUE,
        flg_leader_in              IN sch_resource.flg_leader%TYPE DEFAULT NULL,
        flg_leader_nin             IN BOOLEAN := TRUE,
        id_sch_consult_vacancy_in  IN sch_resource.id_sch_consult_vacancy%TYPE DEFAULT NULL,
        id_sch_consult_vacancy_nin IN BOOLEAN := TRUE,
        where_in                   VARCHAR2 DEFAULT NULL,
        handle_error_in            IN BOOLEAN := TRUE
    );

    PROCEDURE upd_ins
    (
        id_sch_resource_in        IN sch_resource.id_sch_resource%TYPE,
        id_schedule_in            IN sch_resource.id_schedule%TYPE DEFAULT NULL,
        id_institution_in         IN sch_resource.id_institution%TYPE DEFAULT NULL,
        id_professional_in        IN sch_resource.id_professional%TYPE DEFAULT NULL,
        dt_sch_resource_tstz_in   IN sch_resource.dt_sch_resource_tstz%TYPE DEFAULT NULL,
        create_user_in            IN sch_resource.create_user%TYPE DEFAULT NULL,
        create_time_in            IN sch_resource.create_time%TYPE DEFAULT NULL,
        create_institution_in     IN sch_resource.create_institution%TYPE DEFAULT NULL,
        update_user_in            IN sch_resource.update_user%TYPE DEFAULT NULL,
        update_time_in            IN sch_resource.update_time%TYPE DEFAULT NULL,
        update_institution_in     IN sch_resource.update_institution%TYPE DEFAULT NULL,
        flg_leader_in             IN sch_resource.flg_leader%TYPE DEFAULT NULL,
        id_sch_consult_vacancy_in IN sch_resource.id_sch_consult_vacancy%TYPE DEFAULT NULL,
        handle_error_in           IN BOOLEAN := TRUE,
        rows_out                  OUT table_varchar
    );

    PROCEDURE upd_ins
    (
        id_sch_resource_in        IN sch_resource.id_sch_resource%TYPE,
        id_schedule_in            IN sch_resource.id_schedule%TYPE DEFAULT NULL,
        id_institution_in         IN sch_resource.id_institution%TYPE DEFAULT NULL,
        id_professional_in        IN sch_resource.id_professional%TYPE DEFAULT NULL,
        dt_sch_resource_tstz_in   IN sch_resource.dt_sch_resource_tstz%TYPE DEFAULT NULL,
        create_user_in            IN sch_resource.create_user%TYPE DEFAULT NULL,
        create_time_in            IN sch_resource.create_time%TYPE DEFAULT NULL,
        create_institution_in     IN sch_resource.create_institution%TYPE DEFAULT NULL,
        update_user_in            IN sch_resource.update_user%TYPE DEFAULT NULL,
        update_time_in            IN sch_resource.update_time%TYPE DEFAULT NULL,
        update_institution_in     IN sch_resource.update_institution%TYPE DEFAULT NULL,
        flg_leader_in             IN sch_resource.flg_leader%TYPE DEFAULT NULL,
        id_sch_consult_vacancy_in IN sch_resource.id_sch_consult_vacancy%TYPE DEFAULT NULL,
        handle_error_in           IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        rec_in          IN sch_resource%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        IN OUT table_varchar
    );

    PROCEDURE upd
    (
        rec_in          IN sch_resource%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        col_in            IN sch_resource_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          IN OUT table_varchar
    );

    PROCEDURE upd
    (
        col_in            IN sch_resource_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE
    );

    -- Use Native Dynamic SQL increment a single NUMBER column
    -- for all rows specified by the dynamic WHERE clause
    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_sch_resource_in IN sch_resource.id_sch_resource%TYPE,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_sch_resource_in IN sch_resource.id_sch_resource%TYPE,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    -- Delete all rows for primary key column ID_SCH_RESOURCE
    PROCEDURE del_id_sch_resource
    (
        id_sch_resource_in IN sch_resource.id_sch_resource%TYPE,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete all rows for primary key column ID_SCH_RESOURCE
    PROCEDURE del_id_sch_resource
    (
        id_sch_resource_in IN sch_resource.id_sch_resource%TYPE,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    -- Delete for unique value of SRE_ID_PROF
    PROCEDURE del_sre_id_prof
    (
        id_professional_in IN sch_resource.id_professional%TYPE,
        id_schedule_in     IN sch_resource.id_schedule%TYPE,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete for unique value of SRE_ID_PROF
    PROCEDURE del_sre_id_prof
    (
        id_professional_in IN sch_resource.id_professional%TYPE,
        id_schedule_in     IN sch_resource.id_schedule%TYPE,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Initialize a record with default values for columns in the table.
    PROCEDURE initrec(sch_resource_inout IN OUT sch_resource%ROWTYPE);

    FUNCTION initrec RETURN sch_resource%ROWTYPE;

    FUNCTION get_data_rowid(rows_in IN table_varchar) RETURN sch_resource_tc;

    FUNCTION get_data_rowid_pat(rows_in IN table_varchar) RETURN sch_resource_tc;

END ts_sch_resource;
/
