/*-- Last Change Revision: $Rev: 2029376 $*/
/*-- Last Change by: $Author: mario.fernandes $*/
/*-- Date of last change: $Date: 2022-08-02 18:51:19 +0100 (ter, 02 ago 2022) $*/
CREATE OR REPLACE PACKAGE ts_sch_prof_outp
/*
| Generated by or retrieved from QCGU - DO NOT MODIFY!
| QCGU - "Get it right, do it fast" - www.ToadWorld.com
| QCGU Universal ID: {1BD37A66-EA60-4927-9A64-F6DD89237236}
| Created On: Novembro 12, 2009 10:3:22
| Created By: ALERT
*/
 IS

    -- Collection of %ROWTYPE records based on "SCH_PROF_OUTP"
    TYPE sch_prof_outp_tc IS TABLE OF sch_prof_outp%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE sch_prof_outp_ntt IS TABLE OF sch_prof_outp%ROWTYPE;
    TYPE sch_prof_outp_vat IS VARRAY(100) OF sch_prof_outp%ROWTYPE;

    -- Same type structure, with a static name.
    TYPE aat IS TABLE OF sch_prof_outp%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE ntt IS TABLE OF sch_prof_outp%ROWTYPE;
    TYPE vat IS VARRAY(100) OF sch_prof_outp%ROWTYPE;

    -- Column Collection based on column "ID_SCH_PROF_OUTP"
    TYPE id_sch_prof_outp_cc IS TABLE OF sch_prof_outp.id_sch_prof_outp%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_PROFESSIONAL"
    TYPE id_professional_cc IS TABLE OF sch_prof_outp.id_professional%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_SCHEDULE_OUTP"
    TYPE id_schedule_outp_cc IS TABLE OF sch_prof_outp.id_schedule_outp%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_USER"
    TYPE create_user_cc IS TABLE OF sch_prof_outp.create_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_TIME"
    TYPE create_time_cc IS TABLE OF sch_prof_outp.create_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_INSTITUTION"
    TYPE create_institution_cc IS TABLE OF sch_prof_outp.create_institution%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_USER"
    TYPE update_user_cc IS TABLE OF sch_prof_outp.update_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_TIME"
    TYPE update_time_cc IS TABLE OF sch_prof_outp.update_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_INSTITUTION"
    TYPE update_institution_cc IS TABLE OF sch_prof_outp.update_institution%TYPE INDEX BY BINARY_INTEGER;

    -- Insert one row, providing primary key if present
    PROCEDURE ins
    (
        id_sch_prof_outp_in   IN sch_prof_outp.id_sch_prof_outp%TYPE DEFAULT NULL,
        id_professional_in    IN sch_prof_outp.id_professional%TYPE DEFAULT NULL,
        id_schedule_outp_in   IN sch_prof_outp.id_schedule_outp%TYPE DEFAULT NULL,
        create_user_in        IN sch_prof_outp.create_user%TYPE DEFAULT NULL,
        create_time_in        IN sch_prof_outp.create_time%TYPE DEFAULT NULL,
        create_institution_in IN sch_prof_outp.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN sch_prof_outp.update_user%TYPE DEFAULT NULL,
        update_time_in        IN sch_prof_outp.update_time%TYPE DEFAULT NULL,
        update_institution_in IN sch_prof_outp.update_institution%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_professional_in    IN sch_prof_outp.id_professional%TYPE DEFAULT NULL,
        id_schedule_outp_in   IN sch_prof_outp.id_schedule_outp%TYPE DEFAULT NULL,
        create_user_in        IN sch_prof_outp.create_user%TYPE DEFAULT NULL,
        create_time_in        IN sch_prof_outp.create_time%TYPE DEFAULT NULL,
        create_institution_in IN sch_prof_outp.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN sch_prof_outp.update_user%TYPE DEFAULT NULL,
        update_time_in        IN sch_prof_outp.update_time%TYPE DEFAULT NULL,
        update_institution_in IN sch_prof_outp.update_institution%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a row based on a record.
    PROCEDURE ins
    (
        rec_in          IN sch_prof_outp%ROWTYPE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rec_in          IN sch_prof_outp%ROWTYPE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a collection of rows using FORALL; all primary key values
    -- must have already been generated, or are handled in triggers.
    PROCEDURE ins
    (
        rows_in         IN sch_prof_outp_tc,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rows_in         IN sch_prof_outp_tc,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Use Native Dynamic SQL increment a single NUMBER column
    -- for all rows specified by the dynamic WHERE clause
    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Initialize a record with default values for columns in the table.
    PROCEDURE initrec(sch_prof_outp_inout IN OUT sch_prof_outp%ROWTYPE);

    FUNCTION initrec RETURN sch_prof_outp%ROWTYPE;

    FUNCTION get_data_rowid(rows_in IN table_varchar) RETURN sch_prof_outp_tc;

    FUNCTION get_data_rowid_pat(rows_in IN table_varchar) RETURN sch_prof_outp_tc;

END ts_sch_prof_outp;
/
