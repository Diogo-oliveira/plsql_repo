/*-- Last Change Revision: $Rev: 2029146 $*/
/*-- Last Change by: $Author: mario.fernandes $*/
/*-- Date of last change: $Date: 2022-08-02 18:50:03 +0100 (ter, 02 ago 2022) $*/
CREATE OR REPLACE PACKAGE ts_epis_ges_msg
/*
| Generated by or retrieved from QCGU - DO NOT MODIFY!
| QCGU - "Get it right, do it fast" - www.ToadWorld.com
| QCGU Universal ID: {1BD37A66-EA60-4927-9A64-F6DD89237236}
| Created On: Novembro 14, 2011 15:41:26
| Created By: ALERT
*/
 IS

    -- Collection of %ROWTYPE records based on "EPIS_GES_MSG"
    TYPE epis_ges_msg_tc IS TABLE OF epis_ges_msg%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE epis_ges_msg_ntt IS TABLE OF epis_ges_msg%ROWTYPE;
    TYPE epis_ges_msg_vat IS VARRAY(100) OF epis_ges_msg%ROWTYPE;

    -- Same type structure, with a static name.
    TYPE aat IS TABLE OF epis_ges_msg%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE ntt IS TABLE OF epis_ges_msg%ROWTYPE;
    TYPE vat IS VARRAY(100) OF epis_ges_msg%ROWTYPE;

    -- Column Collection based on column "ID_EPIS_GES_MSG"
    TYPE id_epis_ges_msg_cc IS TABLE OF epis_ges_msg.id_epis_ges_msg%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "DT_EPIS_GES_MSG"
    TYPE dt_epis_ges_msg_cc IS TABLE OF epis_ges_msg.dt_epis_ges_msg%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_EPISODE"
    TYPE id_episode_cc IS TABLE OF epis_ges_msg.id_episode%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_PAT_HISTORY_DIAGNOSIS"
    TYPE id_pat_history_diagnosis_cc IS TABLE OF epis_ges_msg.id_pat_history_diagnosis%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_EPIS_DIAGNOSIS"
    TYPE id_epis_diagnosis_cc IS TABLE OF epis_ges_msg.id_epis_diagnosis%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "FLG_ORIGIN"
    TYPE flg_origin_cc IS TABLE OF epis_ges_msg.flg_origin%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "FLG_MSG_STATUS"
    TYPE flg_msg_status_cc IS TABLE OF epis_ges_msg.flg_msg_status%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "FLG_STATUS"
    TYPE flg_status_cc IS TABLE OF epis_ges_msg.flg_status%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_PROF_CREATE"
    TYPE id_prof_create_cc IS TABLE OF epis_ges_msg.id_prof_create%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_USER"
    TYPE create_user_cc IS TABLE OF epis_ges_msg.create_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_TIME"
    TYPE create_time_cc IS TABLE OF epis_ges_msg.create_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_INSTITUTION"
    TYPE create_institution_cc IS TABLE OF epis_ges_msg.create_institution%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_USER"
    TYPE update_user_cc IS TABLE OF epis_ges_msg.update_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_TIME"
    TYPE update_time_cc IS TABLE OF epis_ges_msg.update_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_INSTITUTION"
    TYPE update_institution_cc IS TABLE OF epis_ges_msg.update_institution%TYPE INDEX BY BINARY_INTEGER;

    -- Insert one row, providing primary key if present
    PROCEDURE ins
    (
        id_epis_ges_msg_in          IN epis_ges_msg.id_epis_ges_msg%TYPE,
        dt_epis_ges_msg_in          IN epis_ges_msg.dt_epis_ges_msg%TYPE,
        id_episode_in               IN epis_ges_msg.id_episode%TYPE DEFAULT NULL,
        id_pat_history_diagnosis_in IN epis_ges_msg.id_pat_history_diagnosis%TYPE DEFAULT NULL,
        id_epis_diagnosis_in        IN epis_ges_msg.id_epis_diagnosis%TYPE DEFAULT NULL,
        flg_origin_in               IN epis_ges_msg.flg_origin%TYPE DEFAULT NULL,
        flg_msg_status_in           IN epis_ges_msg.flg_msg_status%TYPE DEFAULT NULL,
        flg_status_in               IN epis_ges_msg.flg_status%TYPE DEFAULT NULL,
        id_prof_create_in           IN epis_ges_msg.id_prof_create%TYPE DEFAULT NULL,
        create_user_in              IN epis_ges_msg.create_user%TYPE DEFAULT NULL,
        create_time_in              IN epis_ges_msg.create_time%TYPE DEFAULT NULL,
        create_institution_in       IN epis_ges_msg.create_institution%TYPE DEFAULT NULL,
        update_user_in              IN epis_ges_msg.update_user%TYPE DEFAULT NULL,
        update_time_in              IN epis_ges_msg.update_time%TYPE DEFAULT NULL,
        update_institution_in       IN epis_ges_msg.update_institution%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_epis_ges_msg_in          IN epis_ges_msg.id_epis_ges_msg%TYPE,
        dt_epis_ges_msg_in          IN epis_ges_msg.dt_epis_ges_msg%TYPE,
        id_episode_in               IN epis_ges_msg.id_episode%TYPE DEFAULT NULL,
        id_pat_history_diagnosis_in IN epis_ges_msg.id_pat_history_diagnosis%TYPE DEFAULT NULL,
        id_epis_diagnosis_in        IN epis_ges_msg.id_epis_diagnosis%TYPE DEFAULT NULL,
        flg_origin_in               IN epis_ges_msg.flg_origin%TYPE DEFAULT NULL,
        flg_msg_status_in           IN epis_ges_msg.flg_msg_status%TYPE DEFAULT NULL,
        flg_status_in               IN epis_ges_msg.flg_status%TYPE DEFAULT NULL,
        id_prof_create_in           IN epis_ges_msg.id_prof_create%TYPE DEFAULT NULL,
        create_user_in              IN epis_ges_msg.create_user%TYPE DEFAULT NULL,
        create_time_in              IN epis_ges_msg.create_time%TYPE DEFAULT NULL,
        create_institution_in       IN epis_ges_msg.create_institution%TYPE DEFAULT NULL,
        update_user_in              IN epis_ges_msg.update_user%TYPE DEFAULT NULL,
        update_time_in              IN epis_ges_msg.update_time%TYPE DEFAULT NULL,
        update_institution_in       IN epis_ges_msg.update_institution%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a row based on a record.
    PROCEDURE ins
    (
        rec_in          IN epis_ges_msg%ROWTYPE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rec_in          IN epis_ges_msg%ROWTYPE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a collection of rows using FORALL; all primary key values
    -- must have already been generated, or are handled in triggers.
    PROCEDURE ins
    (
        rows_in         IN epis_ges_msg_tc,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rows_in         IN epis_ges_msg_tc,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Update any/all columns by primary key. If you pass NULL, then
    -- the current column value is set to itself. If you need a more
    -- selected UPDATE then use one of the onecol procedures below.

    PROCEDURE upd
    (
        id_epis_ges_msg_in           IN epis_ges_msg.id_epis_ges_msg%TYPE,
        dt_epis_ges_msg_in           IN epis_ges_msg.dt_epis_ges_msg%TYPE,
        id_episode_in                IN epis_ges_msg.id_episode%TYPE DEFAULT NULL,
        id_episode_nin               IN BOOLEAN := TRUE,
        id_pat_history_diagnosis_in  IN epis_ges_msg.id_pat_history_diagnosis%TYPE DEFAULT NULL,
        id_pat_history_diagnosis_nin IN BOOLEAN := TRUE,
        id_epis_diagnosis_in         IN epis_ges_msg.id_epis_diagnosis%TYPE DEFAULT NULL,
        id_epis_diagnosis_nin        IN BOOLEAN := TRUE,
        flg_origin_in                IN epis_ges_msg.flg_origin%TYPE DEFAULT NULL,
        flg_origin_nin               IN BOOLEAN := TRUE,
        flg_msg_status_in            IN epis_ges_msg.flg_msg_status%TYPE DEFAULT NULL,
        flg_msg_status_nin           IN BOOLEAN := TRUE,
        flg_status_in                IN epis_ges_msg.flg_status%TYPE DEFAULT NULL,
        flg_status_nin               IN BOOLEAN := TRUE,
        id_prof_create_in            IN epis_ges_msg.id_prof_create%TYPE DEFAULT NULL,
        id_prof_create_nin           IN BOOLEAN := TRUE,
        create_user_in               IN epis_ges_msg.create_user%TYPE DEFAULT NULL,
        create_user_nin              IN BOOLEAN := TRUE,
        create_time_in               IN epis_ges_msg.create_time%TYPE DEFAULT NULL,
        create_time_nin              IN BOOLEAN := TRUE,
        create_institution_in        IN epis_ges_msg.create_institution%TYPE DEFAULT NULL,
        create_institution_nin       IN BOOLEAN := TRUE,
        update_user_in               IN epis_ges_msg.update_user%TYPE DEFAULT NULL,
        update_user_nin              IN BOOLEAN := TRUE,
        update_time_in               IN epis_ges_msg.update_time%TYPE DEFAULT NULL,
        update_time_nin              IN BOOLEAN := TRUE,
        update_institution_in        IN epis_ges_msg.update_institution%TYPE DEFAULT NULL,
        update_institution_nin       IN BOOLEAN := TRUE,
        handle_error_in              IN BOOLEAN := TRUE,
        rows_out                     IN OUT table_varchar
    );

    PROCEDURE upd
    (
        id_epis_ges_msg_in           IN epis_ges_msg.id_epis_ges_msg%TYPE,
        dt_epis_ges_msg_in           IN epis_ges_msg.dt_epis_ges_msg%TYPE,
        id_episode_in                IN epis_ges_msg.id_episode%TYPE DEFAULT NULL,
        id_episode_nin               IN BOOLEAN := TRUE,
        id_pat_history_diagnosis_in  IN epis_ges_msg.id_pat_history_diagnosis%TYPE DEFAULT NULL,
        id_pat_history_diagnosis_nin IN BOOLEAN := TRUE,
        id_epis_diagnosis_in         IN epis_ges_msg.id_epis_diagnosis%TYPE DEFAULT NULL,
        id_epis_diagnosis_nin        IN BOOLEAN := TRUE,
        flg_origin_in                IN epis_ges_msg.flg_origin%TYPE DEFAULT NULL,
        flg_origin_nin               IN BOOLEAN := TRUE,
        flg_msg_status_in            IN epis_ges_msg.flg_msg_status%TYPE DEFAULT NULL,
        flg_msg_status_nin           IN BOOLEAN := TRUE,
        flg_status_in                IN epis_ges_msg.flg_status%TYPE DEFAULT NULL,
        flg_status_nin               IN BOOLEAN := TRUE,
        id_prof_create_in            IN epis_ges_msg.id_prof_create%TYPE DEFAULT NULL,
        id_prof_create_nin           IN BOOLEAN := TRUE,
        create_user_in               IN epis_ges_msg.create_user%TYPE DEFAULT NULL,
        create_user_nin              IN BOOLEAN := TRUE,
        create_time_in               IN epis_ges_msg.create_time%TYPE DEFAULT NULL,
        create_time_nin              IN BOOLEAN := TRUE,
        create_institution_in        IN epis_ges_msg.create_institution%TYPE DEFAULT NULL,
        create_institution_nin       IN BOOLEAN := TRUE,
        update_user_in               IN epis_ges_msg.update_user%TYPE DEFAULT NULL,
        update_user_nin              IN BOOLEAN := TRUE,
        update_time_in               IN epis_ges_msg.update_time%TYPE DEFAULT NULL,
        update_time_nin              IN BOOLEAN := TRUE,
        update_institution_in        IN epis_ges_msg.update_institution%TYPE DEFAULT NULL,
        update_institution_nin       IN BOOLEAN := TRUE,
        handle_error_in              IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        id_episode_in                IN epis_ges_msg.id_episode%TYPE DEFAULT NULL,
        id_episode_nin               IN BOOLEAN := TRUE,
        id_pat_history_diagnosis_in  IN epis_ges_msg.id_pat_history_diagnosis%TYPE DEFAULT NULL,
        id_pat_history_diagnosis_nin IN BOOLEAN := TRUE,
        id_epis_diagnosis_in         IN epis_ges_msg.id_epis_diagnosis%TYPE DEFAULT NULL,
        id_epis_diagnosis_nin        IN BOOLEAN := TRUE,
        flg_origin_in                IN epis_ges_msg.flg_origin%TYPE DEFAULT NULL,
        flg_origin_nin               IN BOOLEAN := TRUE,
        flg_msg_status_in            IN epis_ges_msg.flg_msg_status%TYPE DEFAULT NULL,
        flg_msg_status_nin           IN BOOLEAN := TRUE,
        flg_status_in                IN epis_ges_msg.flg_status%TYPE DEFAULT NULL,
        flg_status_nin               IN BOOLEAN := TRUE,
        id_prof_create_in            IN epis_ges_msg.id_prof_create%TYPE DEFAULT NULL,
        id_prof_create_nin           IN BOOLEAN := TRUE,
        create_user_in               IN epis_ges_msg.create_user%TYPE DEFAULT NULL,
        create_user_nin              IN BOOLEAN := TRUE,
        create_time_in               IN epis_ges_msg.create_time%TYPE DEFAULT NULL,
        create_time_nin              IN BOOLEAN := TRUE,
        create_institution_in        IN epis_ges_msg.create_institution%TYPE DEFAULT NULL,
        create_institution_nin       IN BOOLEAN := TRUE,
        update_user_in               IN epis_ges_msg.update_user%TYPE DEFAULT NULL,
        update_user_nin              IN BOOLEAN := TRUE,
        update_time_in               IN epis_ges_msg.update_time%TYPE DEFAULT NULL,
        update_time_nin              IN BOOLEAN := TRUE,
        update_institution_in        IN epis_ges_msg.update_institution%TYPE DEFAULT NULL,
        update_institution_nin       IN BOOLEAN := TRUE,
        where_in                     VARCHAR2,
        handle_error_in              IN BOOLEAN := TRUE,
        rows_out                     IN OUT table_varchar
    );

    PROCEDURE upd
    (
        id_episode_in                IN epis_ges_msg.id_episode%TYPE DEFAULT NULL,
        id_episode_nin               IN BOOLEAN := TRUE,
        id_pat_history_diagnosis_in  IN epis_ges_msg.id_pat_history_diagnosis%TYPE DEFAULT NULL,
        id_pat_history_diagnosis_nin IN BOOLEAN := TRUE,
        id_epis_diagnosis_in         IN epis_ges_msg.id_epis_diagnosis%TYPE DEFAULT NULL,
        id_epis_diagnosis_nin        IN BOOLEAN := TRUE,
        flg_origin_in                IN epis_ges_msg.flg_origin%TYPE DEFAULT NULL,
        flg_origin_nin               IN BOOLEAN := TRUE,
        flg_msg_status_in            IN epis_ges_msg.flg_msg_status%TYPE DEFAULT NULL,
        flg_msg_status_nin           IN BOOLEAN := TRUE,
        flg_status_in                IN epis_ges_msg.flg_status%TYPE DEFAULT NULL,
        flg_status_nin               IN BOOLEAN := TRUE,
        id_prof_create_in            IN epis_ges_msg.id_prof_create%TYPE DEFAULT NULL,
        id_prof_create_nin           IN BOOLEAN := TRUE,
        create_user_in               IN epis_ges_msg.create_user%TYPE DEFAULT NULL,
        create_user_nin              IN BOOLEAN := TRUE,
        create_time_in               IN epis_ges_msg.create_time%TYPE DEFAULT NULL,
        create_time_nin              IN BOOLEAN := TRUE,
        create_institution_in        IN epis_ges_msg.create_institution%TYPE DEFAULT NULL,
        create_institution_nin       IN BOOLEAN := TRUE,
        update_user_in               IN epis_ges_msg.update_user%TYPE DEFAULT NULL,
        update_user_nin              IN BOOLEAN := TRUE,
        update_time_in               IN epis_ges_msg.update_time%TYPE DEFAULT NULL,
        update_time_nin              IN BOOLEAN := TRUE,
        update_institution_in        IN epis_ges_msg.update_institution%TYPE DEFAULT NULL,
        update_institution_nin       IN BOOLEAN := TRUE,
        where_in                     VARCHAR2,
        handle_error_in              IN BOOLEAN := TRUE
    );

    PROCEDURE upd_ins
    (
        id_epis_ges_msg_in          IN epis_ges_msg.id_epis_ges_msg%TYPE,
        dt_epis_ges_msg_in          IN epis_ges_msg.dt_epis_ges_msg%TYPE,
        id_episode_in               IN epis_ges_msg.id_episode%TYPE DEFAULT NULL,
        id_pat_history_diagnosis_in IN epis_ges_msg.id_pat_history_diagnosis%TYPE DEFAULT NULL,
        id_epis_diagnosis_in        IN epis_ges_msg.id_epis_diagnosis%TYPE DEFAULT NULL,
        flg_origin_in               IN epis_ges_msg.flg_origin%TYPE DEFAULT NULL,
        flg_msg_status_in           IN epis_ges_msg.flg_msg_status%TYPE DEFAULT NULL,
        flg_status_in               IN epis_ges_msg.flg_status%TYPE DEFAULT NULL,
        id_prof_create_in           IN epis_ges_msg.id_prof_create%TYPE DEFAULT NULL,
        create_user_in              IN epis_ges_msg.create_user%TYPE DEFAULT NULL,
        create_time_in              IN epis_ges_msg.create_time%TYPE DEFAULT NULL,
        create_institution_in       IN epis_ges_msg.create_institution%TYPE DEFAULT NULL,
        update_user_in              IN epis_ges_msg.update_user%TYPE DEFAULT NULL,
        update_time_in              IN epis_ges_msg.update_time%TYPE DEFAULT NULL,
        update_institution_in       IN epis_ges_msg.update_institution%TYPE DEFAULT NULL,
        handle_error_in             IN BOOLEAN := TRUE,
        rows_out                    OUT table_varchar
    );

    PROCEDURE upd_ins
    (
        id_epis_ges_msg_in          IN epis_ges_msg.id_epis_ges_msg%TYPE,
        dt_epis_ges_msg_in          IN epis_ges_msg.dt_epis_ges_msg%TYPE,
        id_episode_in               IN epis_ges_msg.id_episode%TYPE DEFAULT NULL,
        id_pat_history_diagnosis_in IN epis_ges_msg.id_pat_history_diagnosis%TYPE DEFAULT NULL,
        id_epis_diagnosis_in        IN epis_ges_msg.id_epis_diagnosis%TYPE DEFAULT NULL,
        flg_origin_in               IN epis_ges_msg.flg_origin%TYPE DEFAULT NULL,
        flg_msg_status_in           IN epis_ges_msg.flg_msg_status%TYPE DEFAULT NULL,
        flg_status_in               IN epis_ges_msg.flg_status%TYPE DEFAULT NULL,
        id_prof_create_in           IN epis_ges_msg.id_prof_create%TYPE DEFAULT NULL,
        create_user_in              IN epis_ges_msg.create_user%TYPE DEFAULT NULL,
        create_time_in              IN epis_ges_msg.create_time%TYPE DEFAULT NULL,
        create_institution_in       IN epis_ges_msg.create_institution%TYPE DEFAULT NULL,
        update_user_in              IN epis_ges_msg.update_user%TYPE DEFAULT NULL,
        update_time_in              IN epis_ges_msg.update_time%TYPE DEFAULT NULL,
        update_institution_in       IN epis_ges_msg.update_institution%TYPE DEFAULT NULL,
        handle_error_in             IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        rec_in          IN epis_ges_msg%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        IN OUT table_varchar
    );

    PROCEDURE upd
    (
        rec_in          IN epis_ges_msg%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        col_in            IN epis_ges_msg_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          IN OUT table_varchar
    );

    PROCEDURE upd
    (
        col_in            IN epis_ges_msg_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE
    );

    -- Use Native Dynamic SQL increment a single NUMBER column
    -- for all rows specified by the dynamic WHERE clause
    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_epis_ges_msg_in IN epis_ges_msg.id_epis_ges_msg%TYPE,
        dt_epis_ges_msg_in IN epis_ges_msg.dt_epis_ges_msg%TYPE,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_epis_ges_msg_in IN epis_ges_msg.id_epis_ges_msg%TYPE,
        dt_epis_ges_msg_in IN epis_ges_msg.dt_epis_ges_msg%TYPE,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    -- Delete all rows for primary key column ID_EPIS_GES_MSG
    PROCEDURE del_id_epis_ges_msg
    (
        id_epis_ges_msg_in IN epis_ges_msg.id_epis_ges_msg%TYPE,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete all rows for primary key column ID_EPIS_GES_MSG
    PROCEDURE del_id_epis_ges_msg
    (
        id_epis_ges_msg_in IN epis_ges_msg.id_epis_ges_msg%TYPE,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    -- Delete all rows for primary key column DT_EPIS_GES_MSG
    PROCEDURE del_dt_epis_ges_msg
    (
        dt_epis_ges_msg_in IN epis_ges_msg.dt_epis_ges_msg%TYPE,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete all rows for primary key column DT_EPIS_GES_MSG
    PROCEDURE del_dt_epis_ges_msg
    (
        dt_epis_ges_msg_in IN epis_ges_msg.dt_epis_ges_msg%TYPE,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    -- Delete all rows for this EGMSG_ED_FK foreign key value
    PROCEDURE del_egmsg_ed_fk
    (
        id_epis_diagnosis_in IN epis_ges_msg.id_epis_diagnosis%TYPE,
        handle_error_in      IN BOOLEAN := TRUE
    );

    -- Delete all rows for this EGMSG_ED_FK foreign key value
    PROCEDURE del_egmsg_ed_fk
    (
        id_epis_diagnosis_in IN epis_ges_msg.id_epis_diagnosis%TYPE,
        handle_error_in      IN BOOLEAN := TRUE,
        rows_out             OUT table_varchar
    );

    -- Delete all rows for this EGMSG_EPIS_FK foreign key value
    PROCEDURE del_egmsg_epis_fk
    (
        id_episode_in   IN epis_ges_msg.id_episode%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this EGMSG_EPIS_FK foreign key value
    PROCEDURE del_egmsg_epis_fk
    (
        id_episode_in   IN epis_ges_msg.id_episode%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for this EGMSG_PHD_FK foreign key value
    PROCEDURE del_egmsg_phd_fk
    (
        id_pat_history_diagnosis_in IN epis_ges_msg.id_pat_history_diagnosis%TYPE,
        handle_error_in             IN BOOLEAN := TRUE
    );

    -- Delete all rows for this EGMSG_PHD_FK foreign key value
    PROCEDURE del_egmsg_phd_fk
    (
        id_pat_history_diagnosis_in IN epis_ges_msg.id_pat_history_diagnosis%TYPE,
        handle_error_in             IN BOOLEAN := TRUE,
        rows_out                    OUT table_varchar
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Initialize a record with default values for columns in the table.
    PROCEDURE initrec(epis_ges_msg_inout IN OUT epis_ges_msg%ROWTYPE);

    FUNCTION initrec RETURN epis_ges_msg%ROWTYPE;

    FUNCTION get_data_rowid(rows_in IN table_varchar) RETURN epis_ges_msg_tc;

    FUNCTION get_data_rowid_pat(rows_in IN table_varchar) RETURN epis_ges_msg_tc;

END ts_epis_ges_msg;
/
