/*-- Last Change Revision: $Rev: 1967122 $*/
/*-- Last Change by: $Author: elisabete.bugalho $*/
/*-- Date of last change: $Date: 2020-10-16 16:57:56 +0100 (sex, 16 out 2020) $*/
CREATE OR REPLACE PACKAGE ts_epis_complaint
/*
| Generated by or retrieved - DO NOT MODIFY!
| Created On: 2020-10-08 15:30:07
| Created By: ALERT
*/
 IS

    -- Collection of %ROWTYPE records based on epis_complaint
    TYPE epis_complaint_tc IS TABLE OF epis_complaint%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE epis_complaint_ntt IS TABLE OF epis_complaint%ROWTYPE;
    TYPE epis_complaint_vat IS VARRAY(100) OF epis_complaint%ROWTYPE;

    -- Column Collection based on column ID_EPIS_COMPLAINT
    TYPE id_epis_complaint_cc IS TABLE OF epis_complaint.id_epis_complaint%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_EPISODE
    TYPE id_episode_cc IS TABLE OF epis_complaint.id_episode%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_PROFESSIONAL
    TYPE id_professional_cc IS TABLE OF epis_complaint.id_professional%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_COMPLAINT
    TYPE id_complaint_cc IS TABLE OF epis_complaint.id_complaint%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column PATIENT_COMPLAINT
    TYPE patient_complaint_cc IS TABLE OF epis_complaint.patient_complaint%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column FLG_STATUS
    TYPE flg_status_cc IS TABLE OF epis_complaint.flg_status%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_EPIS_COMPLAINT_PARENT
    TYPE id_epis_complaint_parent_cc IS TABLE OF epis_complaint.id_epis_complaint_parent%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ADW_LAST_UPDATE_TSTZ
    TYPE adw_last_update_tstz_cc IS TABLE OF epis_complaint.adw_last_update_tstz%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column FLG_REPORTED_BY
    TYPE flg_reported_by_cc IS TABLE OF epis_complaint.flg_reported_by%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_EPIS_COMPLAINT_ROOT
    TYPE id_epis_complaint_root_cc IS TABLE OF epis_complaint.id_epis_complaint_root%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_DEP_CLIN_SERV
    TYPE id_dep_clin_serv_cc IS TABLE OF epis_complaint.id_dep_clin_serv%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column FLG_EDITION_TYPE
    TYPE flg_edition_type_cc IS TABLE OF epis_complaint.flg_edition_type%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column CREATE_USER
    TYPE create_user_cc IS TABLE OF epis_complaint.create_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column CREATE_TIME
    TYPE create_time_cc IS TABLE OF epis_complaint.create_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column CREATE_INSTITUTION
    TYPE create_institution_cc IS TABLE OF epis_complaint.create_institution%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column UPDATE_USER
    TYPE update_user_cc IS TABLE OF epis_complaint.update_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column UPDATE_TIME
    TYPE update_time_cc IS TABLE OF epis_complaint.update_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column UPDATE_INSTITUTION
    TYPE update_institution_cc IS TABLE OF epis_complaint.update_institution%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_CANCEL_INFO_DET
    TYPE id_cancel_info_det_cc IS TABLE OF epis_complaint.id_cancel_info_det%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column PATIENT_COMPLAINT_ARABIC
    TYPE patient_complaint_arabic_cc IS TABLE OF epis_complaint.patient_complaint_arabic%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_COMPLAINT_ALIAS
    TYPE id_complaint_alias_cc IS TABLE OF epis_complaint.id_complaint_alias%TYPE INDEX BY BINARY_INTEGER;

    -- Insert one row, providing primary key if present (with rows_out)
    PROCEDURE ins
    (
        id_epis_complaint_in        IN epis_complaint.id_epis_complaint%TYPE,
        id_episode_in               IN epis_complaint.id_episode%TYPE DEFAULT NULL,
        id_professional_in          IN epis_complaint.id_professional%TYPE DEFAULT NULL,
        id_complaint_in             IN epis_complaint.id_complaint%TYPE DEFAULT NULL,
        patient_complaint_in        IN epis_complaint.patient_complaint%TYPE DEFAULT NULL,
        flg_status_in               IN epis_complaint.flg_status%TYPE DEFAULT NULL,
        id_epis_complaint_parent_in IN epis_complaint.id_epis_complaint_parent%TYPE DEFAULT NULL,
        adw_last_update_tstz_in     IN epis_complaint.adw_last_update_tstz%TYPE DEFAULT NULL,
        flg_reported_by_in          IN epis_complaint.flg_reported_by%TYPE DEFAULT NULL,
        id_epis_complaint_root_in   IN epis_complaint.id_epis_complaint_root%TYPE DEFAULT NULL,
        id_dep_clin_serv_in         IN epis_complaint.id_dep_clin_serv%TYPE DEFAULT NULL,
        flg_edition_type_in         IN epis_complaint.flg_edition_type%TYPE DEFAULT NULL,
        create_user_in              IN epis_complaint.create_user%TYPE DEFAULT NULL,
        create_time_in              IN epis_complaint.create_time%TYPE DEFAULT NULL,
        create_institution_in       IN epis_complaint.create_institution%TYPE DEFAULT NULL,
        update_user_in              IN epis_complaint.update_user%TYPE DEFAULT NULL,
        update_time_in              IN epis_complaint.update_time%TYPE DEFAULT NULL,
        update_institution_in       IN epis_complaint.update_institution%TYPE DEFAULT NULL,
        id_cancel_info_det_in       IN epis_complaint.id_cancel_info_det%TYPE DEFAULT NULL,
        patient_complaint_arabic_in IN epis_complaint.patient_complaint_arabic%TYPE DEFAULT NULL,
        id_complaint_alias_in       IN epis_complaint.id_complaint_alias%TYPE DEFAULT NULL,
        handle_error_in             IN BOOLEAN := TRUE,
        rows_out                    OUT table_varchar
    );

    -- Insert one row, providing primary key if present (without rows_out)
    PROCEDURE ins
    (
        id_epis_complaint_in        IN epis_complaint.id_epis_complaint%TYPE,
        id_episode_in               IN epis_complaint.id_episode%TYPE DEFAULT NULL,
        id_professional_in          IN epis_complaint.id_professional%TYPE DEFAULT NULL,
        id_complaint_in             IN epis_complaint.id_complaint%TYPE DEFAULT NULL,
        patient_complaint_in        IN epis_complaint.patient_complaint%TYPE DEFAULT NULL,
        flg_status_in               IN epis_complaint.flg_status%TYPE DEFAULT NULL,
        id_epis_complaint_parent_in IN epis_complaint.id_epis_complaint_parent%TYPE DEFAULT NULL,
        adw_last_update_tstz_in     IN epis_complaint.adw_last_update_tstz%TYPE DEFAULT NULL,
        flg_reported_by_in          IN epis_complaint.flg_reported_by%TYPE DEFAULT NULL,
        id_epis_complaint_root_in   IN epis_complaint.id_epis_complaint_root%TYPE DEFAULT NULL,
        id_dep_clin_serv_in         IN epis_complaint.id_dep_clin_serv%TYPE DEFAULT NULL,
        flg_edition_type_in         IN epis_complaint.flg_edition_type%TYPE DEFAULT NULL,
        create_user_in              IN epis_complaint.create_user%TYPE DEFAULT NULL,
        create_time_in              IN epis_complaint.create_time%TYPE DEFAULT NULL,
        create_institution_in       IN epis_complaint.create_institution%TYPE DEFAULT NULL,
        update_user_in              IN epis_complaint.update_user%TYPE DEFAULT NULL,
        update_time_in              IN epis_complaint.update_time%TYPE DEFAULT NULL,
        update_institution_in       IN epis_complaint.update_institution%TYPE DEFAULT NULL,
        id_cancel_info_det_in       IN epis_complaint.id_cancel_info_det%TYPE DEFAULT NULL,
        patient_complaint_arabic_in IN epis_complaint.patient_complaint_arabic%TYPE DEFAULT NULL,
        id_complaint_alias_in       IN epis_complaint.id_complaint_alias%TYPE DEFAULT NULL,
        handle_error_in             IN BOOLEAN := TRUE
    );

    -- Insert a row based on a record
    -- Specify whether or not a primary key value should be generated
    PROCEDURE ins
    (
        rec_in          IN epis_complaint%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Insert a row based on a record
    -- Specify whether or not a primary key value should be generated
    PROCEDURE ins
    (
        rec_in          IN epis_complaint%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a collection of rows using FORALL; all primary key values
    -- must have already been generated, or are handled in triggers
    PROCEDURE ins
    (
        rows_in         IN epis_complaint_tc,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Insert a collection of rows using FORALL; all primary key values
    -- must have already been generated, or are handled in triggers
    PROCEDURE ins
    (
        rows_in         IN epis_complaint_tc,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Return next primary key value using the named sequence
    FUNCTION next_key(sequence_in IN VARCHAR2 := NULL) RETURN epis_complaint.id_epis_complaint%TYPE;

    -- Insert one row, generating hidden primary key using a sequence
    PROCEDURE ins
    (
        id_episode_in               IN epis_complaint.id_episode%TYPE DEFAULT NULL,
        id_professional_in          IN epis_complaint.id_professional%TYPE DEFAULT NULL,
        id_complaint_in             IN epis_complaint.id_complaint%TYPE DEFAULT NULL,
        patient_complaint_in        IN epis_complaint.patient_complaint%TYPE DEFAULT NULL,
        flg_status_in               IN epis_complaint.flg_status%TYPE DEFAULT NULL,
        id_epis_complaint_parent_in IN epis_complaint.id_epis_complaint_parent%TYPE DEFAULT NULL,
        adw_last_update_tstz_in     IN epis_complaint.adw_last_update_tstz%TYPE DEFAULT NULL,
        flg_reported_by_in          IN epis_complaint.flg_reported_by%TYPE DEFAULT NULL,
        id_epis_complaint_root_in   IN epis_complaint.id_epis_complaint_root%TYPE DEFAULT NULL,
        id_dep_clin_serv_in         IN epis_complaint.id_dep_clin_serv%TYPE DEFAULT NULL,
        flg_edition_type_in         IN epis_complaint.flg_edition_type%TYPE DEFAULT NULL,
        create_user_in              IN epis_complaint.create_user%TYPE DEFAULT NULL,
        create_time_in              IN epis_complaint.create_time%TYPE DEFAULT NULL,
        create_institution_in       IN epis_complaint.create_institution%TYPE DEFAULT NULL,
        update_user_in              IN epis_complaint.update_user%TYPE DEFAULT NULL,
        update_time_in              IN epis_complaint.update_time%TYPE DEFAULT NULL,
        update_institution_in       IN epis_complaint.update_institution%TYPE DEFAULT NULL,
        id_cancel_info_det_in       IN epis_complaint.id_cancel_info_det%TYPE DEFAULT NULL,
        patient_complaint_arabic_in IN epis_complaint.patient_complaint_arabic%TYPE DEFAULT NULL,
        id_complaint_alias_in       IN epis_complaint.id_complaint_alias%TYPE DEFAULT NULL,
        -- Pass false if you want errors to propagate out unhandled
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Insert one row, generating hidden primary key using a sequence
    PROCEDURE ins
    (
        id_episode_in               IN epis_complaint.id_episode%TYPE DEFAULT NULL,
        id_professional_in          IN epis_complaint.id_professional%TYPE DEFAULT NULL,
        id_complaint_in             IN epis_complaint.id_complaint%TYPE DEFAULT NULL,
        patient_complaint_in        IN epis_complaint.patient_complaint%TYPE DEFAULT NULL,
        flg_status_in               IN epis_complaint.flg_status%TYPE DEFAULT NULL,
        id_epis_complaint_parent_in IN epis_complaint.id_epis_complaint_parent%TYPE DEFAULT NULL,
        adw_last_update_tstz_in     IN epis_complaint.adw_last_update_tstz%TYPE DEFAULT NULL,
        flg_reported_by_in          IN epis_complaint.flg_reported_by%TYPE DEFAULT NULL,
        id_epis_complaint_root_in   IN epis_complaint.id_epis_complaint_root%TYPE DEFAULT NULL,
        id_dep_clin_serv_in         IN epis_complaint.id_dep_clin_serv%TYPE DEFAULT NULL,
        flg_edition_type_in         IN epis_complaint.flg_edition_type%TYPE DEFAULT NULL,
        create_user_in              IN epis_complaint.create_user%TYPE DEFAULT NULL,
        create_time_in              IN epis_complaint.create_time%TYPE DEFAULT NULL,
        create_institution_in       IN epis_complaint.create_institution%TYPE DEFAULT NULL,
        update_user_in              IN epis_complaint.update_user%TYPE DEFAULT NULL,
        update_time_in              IN epis_complaint.update_time%TYPE DEFAULT NULL,
        update_institution_in       IN epis_complaint.update_institution%TYPE DEFAULT NULL,
        id_cancel_info_det_in       IN epis_complaint.id_cancel_info_det%TYPE DEFAULT NULL,
        patient_complaint_arabic_in IN epis_complaint.patient_complaint_arabic%TYPE DEFAULT NULL,
        id_complaint_alias_in       IN epis_complaint.id_complaint_alias%TYPE DEFAULT NULL,
        -- Pass false if you want errors to propagate out unhandled
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert one row, returning primary key generated by sequence
    PROCEDURE ins
    (
        id_episode_in               IN epis_complaint.id_episode%TYPE DEFAULT NULL,
        id_professional_in          IN epis_complaint.id_professional%TYPE DEFAULT NULL,
        id_complaint_in             IN epis_complaint.id_complaint%TYPE DEFAULT NULL,
        patient_complaint_in        IN epis_complaint.patient_complaint%TYPE DEFAULT NULL,
        flg_status_in               IN epis_complaint.flg_status%TYPE DEFAULT NULL,
        id_epis_complaint_parent_in IN epis_complaint.id_epis_complaint_parent%TYPE DEFAULT NULL,
        adw_last_update_tstz_in     IN epis_complaint.adw_last_update_tstz%TYPE DEFAULT NULL,
        flg_reported_by_in          IN epis_complaint.flg_reported_by%TYPE DEFAULT NULL,
        id_epis_complaint_root_in   IN epis_complaint.id_epis_complaint_root%TYPE DEFAULT NULL,
        id_dep_clin_serv_in         IN epis_complaint.id_dep_clin_serv%TYPE DEFAULT NULL,
        flg_edition_type_in         IN epis_complaint.flg_edition_type%TYPE DEFAULT NULL,
        create_user_in              IN epis_complaint.create_user%TYPE DEFAULT NULL,
        create_time_in              IN epis_complaint.create_time%TYPE DEFAULT NULL,
        create_institution_in       IN epis_complaint.create_institution%TYPE DEFAULT NULL,
        update_user_in              IN epis_complaint.update_user%TYPE DEFAULT NULL,
        update_time_in              IN epis_complaint.update_time%TYPE DEFAULT NULL,
        update_institution_in       IN epis_complaint.update_institution%TYPE DEFAULT NULL,
        id_cancel_info_det_in       IN epis_complaint.id_cancel_info_det%TYPE DEFAULT NULL,
        patient_complaint_arabic_in IN epis_complaint.patient_complaint_arabic%TYPE DEFAULT NULL,
        id_complaint_alias_in       IN epis_complaint.id_complaint_alias%TYPE DEFAULT NULL,
        id_epis_complaint_out       IN OUT epis_complaint.id_epis_complaint%TYPE,
        -- Pass false if you want errors to propagate out unhandled
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Insert one row, returning primary key generated by sequence
    PROCEDURE ins
    (
        id_episode_in               IN epis_complaint.id_episode%TYPE DEFAULT NULL,
        id_professional_in          IN epis_complaint.id_professional%TYPE DEFAULT NULL,
        id_complaint_in             IN epis_complaint.id_complaint%TYPE DEFAULT NULL,
        patient_complaint_in        IN epis_complaint.patient_complaint%TYPE DEFAULT NULL,
        flg_status_in               IN epis_complaint.flg_status%TYPE DEFAULT NULL,
        id_epis_complaint_parent_in IN epis_complaint.id_epis_complaint_parent%TYPE DEFAULT NULL,
        adw_last_update_tstz_in     IN epis_complaint.adw_last_update_tstz%TYPE DEFAULT NULL,
        flg_reported_by_in          IN epis_complaint.flg_reported_by%TYPE DEFAULT NULL,
        id_epis_complaint_root_in   IN epis_complaint.id_epis_complaint_root%TYPE DEFAULT NULL,
        id_dep_clin_serv_in         IN epis_complaint.id_dep_clin_serv%TYPE DEFAULT NULL,
        flg_edition_type_in         IN epis_complaint.flg_edition_type%TYPE DEFAULT NULL,
        create_user_in              IN epis_complaint.create_user%TYPE DEFAULT NULL,
        create_time_in              IN epis_complaint.create_time%TYPE DEFAULT NULL,
        create_institution_in       IN epis_complaint.create_institution%TYPE DEFAULT NULL,
        update_user_in              IN epis_complaint.update_user%TYPE DEFAULT NULL,
        update_time_in              IN epis_complaint.update_time%TYPE DEFAULT NULL,
        update_institution_in       IN epis_complaint.update_institution%TYPE DEFAULT NULL,
        id_cancel_info_det_in       IN epis_complaint.id_cancel_info_det%TYPE DEFAULT NULL,
        patient_complaint_arabic_in IN epis_complaint.patient_complaint_arabic%TYPE DEFAULT NULL,
        id_complaint_alias_in       IN epis_complaint.id_complaint_alias%TYPE DEFAULT NULL,
        id_epis_complaint_out       IN OUT epis_complaint.id_epis_complaint%TYPE,
        -- Pass false if you want errors to propagate out unhandled
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert one row with function, return generated primary key
    FUNCTION ins
    (
        id_episode_in               IN epis_complaint.id_episode%TYPE DEFAULT NULL,
        id_professional_in          IN epis_complaint.id_professional%TYPE DEFAULT NULL,
        id_complaint_in             IN epis_complaint.id_complaint%TYPE DEFAULT NULL,
        patient_complaint_in        IN epis_complaint.patient_complaint%TYPE DEFAULT NULL,
        flg_status_in               IN epis_complaint.flg_status%TYPE DEFAULT NULL,
        id_epis_complaint_parent_in IN epis_complaint.id_epis_complaint_parent%TYPE DEFAULT NULL,
        adw_last_update_tstz_in     IN epis_complaint.adw_last_update_tstz%TYPE DEFAULT NULL,
        flg_reported_by_in          IN epis_complaint.flg_reported_by%TYPE DEFAULT NULL,
        id_epis_complaint_root_in   IN epis_complaint.id_epis_complaint_root%TYPE DEFAULT NULL,
        id_dep_clin_serv_in         IN epis_complaint.id_dep_clin_serv%TYPE DEFAULT NULL,
        flg_edition_type_in         IN epis_complaint.flg_edition_type%TYPE DEFAULT NULL,
        create_user_in              IN epis_complaint.create_user%TYPE DEFAULT NULL,
        create_time_in              IN epis_complaint.create_time%TYPE DEFAULT NULL,
        create_institution_in       IN epis_complaint.create_institution%TYPE DEFAULT NULL,
        update_user_in              IN epis_complaint.update_user%TYPE DEFAULT NULL,
        update_time_in              IN epis_complaint.update_time%TYPE DEFAULT NULL,
        update_institution_in       IN epis_complaint.update_institution%TYPE DEFAULT NULL,
        id_cancel_info_det_in       IN epis_complaint.id_cancel_info_det%TYPE DEFAULT NULL,
        patient_complaint_arabic_in IN epis_complaint.patient_complaint_arabic%TYPE DEFAULT NULL,
        id_complaint_alias_in       IN epis_complaint.id_complaint_alias%TYPE DEFAULT NULL,
        -- Pass false if you want errors to propagate out unhandled
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) RETURN epis_complaint.id_epis_complaint%TYPE;

    -- Insert one row with function, return generated primary key
    FUNCTION ins
    (
        id_episode_in               IN epis_complaint.id_episode%TYPE DEFAULT NULL,
        id_professional_in          IN epis_complaint.id_professional%TYPE DEFAULT NULL,
        id_complaint_in             IN epis_complaint.id_complaint%TYPE DEFAULT NULL,
        patient_complaint_in        IN epis_complaint.patient_complaint%TYPE DEFAULT NULL,
        flg_status_in               IN epis_complaint.flg_status%TYPE DEFAULT NULL,
        id_epis_complaint_parent_in IN epis_complaint.id_epis_complaint_parent%TYPE DEFAULT NULL,
        adw_last_update_tstz_in     IN epis_complaint.adw_last_update_tstz%TYPE DEFAULT NULL,
        flg_reported_by_in          IN epis_complaint.flg_reported_by%TYPE DEFAULT NULL,
        id_epis_complaint_root_in   IN epis_complaint.id_epis_complaint_root%TYPE DEFAULT NULL,
        id_dep_clin_serv_in         IN epis_complaint.id_dep_clin_serv%TYPE DEFAULT NULL,
        flg_edition_type_in         IN epis_complaint.flg_edition_type%TYPE DEFAULT NULL,
        create_user_in              IN epis_complaint.create_user%TYPE DEFAULT NULL,
        create_time_in              IN epis_complaint.create_time%TYPE DEFAULT NULL,
        create_institution_in       IN epis_complaint.create_institution%TYPE DEFAULT NULL,
        update_user_in              IN epis_complaint.update_user%TYPE DEFAULT NULL,
        update_time_in              IN epis_complaint.update_time%TYPE DEFAULT NULL,
        update_institution_in       IN epis_complaint.update_institution%TYPE DEFAULT NULL,
        id_cancel_info_det_in       IN epis_complaint.id_cancel_info_det%TYPE DEFAULT NULL,
        patient_complaint_arabic_in IN epis_complaint.patient_complaint_arabic%TYPE DEFAULT NULL,
        id_complaint_alias_in       IN epis_complaint.id_complaint_alias%TYPE DEFAULT NULL,
        -- Pass false if you want errors to propagate out unhandled
        handle_error_in IN BOOLEAN := TRUE
    ) RETURN epis_complaint.id_epis_complaint%TYPE;

    -- Update any/all columns by primary key. If you pass NULL, then
    -- the current column value is set to itself. If you need a more
    -- selected UPDATE then use one of the onecol procedures below.
    PROCEDURE upd
    (
        id_epis_complaint_in         IN epis_complaint.id_epis_complaint%TYPE,
        id_episode_in                IN epis_complaint.id_episode%TYPE DEFAULT NULL,
        id_episode_nin               IN BOOLEAN := TRUE,
        id_professional_in           IN epis_complaint.id_professional%TYPE DEFAULT NULL,
        id_professional_nin          IN BOOLEAN := TRUE,
        id_complaint_in              IN epis_complaint.id_complaint%TYPE DEFAULT NULL,
        id_complaint_nin             IN BOOLEAN := TRUE,
        patient_complaint_in         IN epis_complaint.patient_complaint%TYPE DEFAULT NULL,
        patient_complaint_nin        IN BOOLEAN := TRUE,
        flg_status_in                IN epis_complaint.flg_status%TYPE DEFAULT NULL,
        flg_status_nin               IN BOOLEAN := TRUE,
        id_epis_complaint_parent_in  IN epis_complaint.id_epis_complaint_parent%TYPE DEFAULT NULL,
        id_epis_complaint_parent_nin IN BOOLEAN := TRUE,
        adw_last_update_tstz_in      IN epis_complaint.adw_last_update_tstz%TYPE DEFAULT NULL,
        adw_last_update_tstz_nin     IN BOOLEAN := TRUE,
        flg_reported_by_in           IN epis_complaint.flg_reported_by%TYPE DEFAULT NULL,
        flg_reported_by_nin          IN BOOLEAN := TRUE,
        id_epis_complaint_root_in    IN epis_complaint.id_epis_complaint_root%TYPE DEFAULT NULL,
        id_epis_complaint_root_nin   IN BOOLEAN := TRUE,
        id_dep_clin_serv_in          IN epis_complaint.id_dep_clin_serv%TYPE DEFAULT NULL,
        id_dep_clin_serv_nin         IN BOOLEAN := TRUE,
        flg_edition_type_in          IN epis_complaint.flg_edition_type%TYPE DEFAULT NULL,
        flg_edition_type_nin         IN BOOLEAN := TRUE,
        create_user_in               IN epis_complaint.create_user%TYPE DEFAULT NULL,
        create_user_nin              IN BOOLEAN := TRUE,
        create_time_in               IN epis_complaint.create_time%TYPE DEFAULT NULL,
        create_time_nin              IN BOOLEAN := TRUE,
        create_institution_in        IN epis_complaint.create_institution%TYPE DEFAULT NULL,
        create_institution_nin       IN BOOLEAN := TRUE,
        update_user_in               IN epis_complaint.update_user%TYPE DEFAULT NULL,
        update_user_nin              IN BOOLEAN := TRUE,
        update_time_in               IN epis_complaint.update_time%TYPE DEFAULT NULL,
        update_time_nin              IN BOOLEAN := TRUE,
        update_institution_in        IN epis_complaint.update_institution%TYPE DEFAULT NULL,
        update_institution_nin       IN BOOLEAN := TRUE,
        id_cancel_info_det_in        IN epis_complaint.id_cancel_info_det%TYPE DEFAULT NULL,
        id_cancel_info_det_nin       IN BOOLEAN := TRUE,
        patient_complaint_arabic_in  IN epis_complaint.patient_complaint_arabic%TYPE DEFAULT NULL,
        patient_complaint_arabic_nin IN BOOLEAN := TRUE,
        id_complaint_alias_in        IN epis_complaint.id_complaint_alias%TYPE DEFAULT NULL,
        id_complaint_alias_nin       IN BOOLEAN := TRUE,
        handle_error_in              IN BOOLEAN := TRUE,
        rows_out                     IN OUT table_varchar
    );

    -- Update any/all columns by primary key. If you pass NULL, then
    -- the current column value is set to itself. If you need a more
    -- selected UPDATE then use one of the onecol procedures below.
    PROCEDURE upd
    (
        id_epis_complaint_in         IN epis_complaint.id_epis_complaint%TYPE,
        id_episode_in                IN epis_complaint.id_episode%TYPE DEFAULT NULL,
        id_episode_nin               IN BOOLEAN := TRUE,
        id_professional_in           IN epis_complaint.id_professional%TYPE DEFAULT NULL,
        id_professional_nin          IN BOOLEAN := TRUE,
        id_complaint_in              IN epis_complaint.id_complaint%TYPE DEFAULT NULL,
        id_complaint_nin             IN BOOLEAN := TRUE,
        patient_complaint_in         IN epis_complaint.patient_complaint%TYPE DEFAULT NULL,
        patient_complaint_nin        IN BOOLEAN := TRUE,
        flg_status_in                IN epis_complaint.flg_status%TYPE DEFAULT NULL,
        flg_status_nin               IN BOOLEAN := TRUE,
        id_epis_complaint_parent_in  IN epis_complaint.id_epis_complaint_parent%TYPE DEFAULT NULL,
        id_epis_complaint_parent_nin IN BOOLEAN := TRUE,
        adw_last_update_tstz_in      IN epis_complaint.adw_last_update_tstz%TYPE DEFAULT NULL,
        adw_last_update_tstz_nin     IN BOOLEAN := TRUE,
        flg_reported_by_in           IN epis_complaint.flg_reported_by%TYPE DEFAULT NULL,
        flg_reported_by_nin          IN BOOLEAN := TRUE,
        id_epis_complaint_root_in    IN epis_complaint.id_epis_complaint_root%TYPE DEFAULT NULL,
        id_epis_complaint_root_nin   IN BOOLEAN := TRUE,
        id_dep_clin_serv_in          IN epis_complaint.id_dep_clin_serv%TYPE DEFAULT NULL,
        id_dep_clin_serv_nin         IN BOOLEAN := TRUE,
        flg_edition_type_in          IN epis_complaint.flg_edition_type%TYPE DEFAULT NULL,
        flg_edition_type_nin         IN BOOLEAN := TRUE,
        create_user_in               IN epis_complaint.create_user%TYPE DEFAULT NULL,
        create_user_nin              IN BOOLEAN := TRUE,
        create_time_in               IN epis_complaint.create_time%TYPE DEFAULT NULL,
        create_time_nin              IN BOOLEAN := TRUE,
        create_institution_in        IN epis_complaint.create_institution%TYPE DEFAULT NULL,
        create_institution_nin       IN BOOLEAN := TRUE,
        update_user_in               IN epis_complaint.update_user%TYPE DEFAULT NULL,
        update_user_nin              IN BOOLEAN := TRUE,
        update_time_in               IN epis_complaint.update_time%TYPE DEFAULT NULL,
        update_time_nin              IN BOOLEAN := TRUE,
        update_institution_in        IN epis_complaint.update_institution%TYPE DEFAULT NULL,
        update_institution_nin       IN BOOLEAN := TRUE,
        id_cancel_info_det_in        IN epis_complaint.id_cancel_info_det%TYPE DEFAULT NULL,
        id_cancel_info_det_nin       IN BOOLEAN := TRUE,
        patient_complaint_arabic_in  IN epis_complaint.patient_complaint_arabic%TYPE DEFAULT NULL,
        patient_complaint_arabic_nin IN BOOLEAN := TRUE,
        id_complaint_alias_in        IN epis_complaint.id_complaint_alias%TYPE DEFAULT NULL,
        id_complaint_alias_nin       IN BOOLEAN := TRUE,
        handle_error_in              IN BOOLEAN := TRUE
    );

    --Update any/all columns by dynamic WHERE
    -- If you pass NULL, then the current column value is set to itself
    PROCEDURE upd
    (
        id_episode_in                IN epis_complaint.id_episode%TYPE DEFAULT NULL,
        id_episode_nin               IN BOOLEAN := TRUE,
        id_professional_in           IN epis_complaint.id_professional%TYPE DEFAULT NULL,
        id_professional_nin          IN BOOLEAN := TRUE,
        id_complaint_in              IN epis_complaint.id_complaint%TYPE DEFAULT NULL,
        id_complaint_nin             IN BOOLEAN := TRUE,
        patient_complaint_in         IN epis_complaint.patient_complaint%TYPE DEFAULT NULL,
        patient_complaint_nin        IN BOOLEAN := TRUE,
        flg_status_in                IN epis_complaint.flg_status%TYPE DEFAULT NULL,
        flg_status_nin               IN BOOLEAN := TRUE,
        id_epis_complaint_parent_in  IN epis_complaint.id_epis_complaint_parent%TYPE DEFAULT NULL,
        id_epis_complaint_parent_nin IN BOOLEAN := TRUE,
        adw_last_update_tstz_in      IN epis_complaint.adw_last_update_tstz%TYPE DEFAULT NULL,
        adw_last_update_tstz_nin     IN BOOLEAN := TRUE,
        flg_reported_by_in           IN epis_complaint.flg_reported_by%TYPE DEFAULT NULL,
        flg_reported_by_nin          IN BOOLEAN := TRUE,
        id_epis_complaint_root_in    IN epis_complaint.id_epis_complaint_root%TYPE DEFAULT NULL,
        id_epis_complaint_root_nin   IN BOOLEAN := TRUE,
        id_dep_clin_serv_in          IN epis_complaint.id_dep_clin_serv%TYPE DEFAULT NULL,
        id_dep_clin_serv_nin         IN BOOLEAN := TRUE,
        flg_edition_type_in          IN epis_complaint.flg_edition_type%TYPE DEFAULT NULL,
        flg_edition_type_nin         IN BOOLEAN := TRUE,
        create_user_in               IN epis_complaint.create_user%TYPE DEFAULT NULL,
        create_user_nin              IN BOOLEAN := TRUE,
        create_time_in               IN epis_complaint.create_time%TYPE DEFAULT NULL,
        create_time_nin              IN BOOLEAN := TRUE,
        create_institution_in        IN epis_complaint.create_institution%TYPE DEFAULT NULL,
        create_institution_nin       IN BOOLEAN := TRUE,
        update_user_in               IN epis_complaint.update_user%TYPE DEFAULT NULL,
        update_user_nin              IN BOOLEAN := TRUE,
        update_time_in               IN epis_complaint.update_time%TYPE DEFAULT NULL,
        update_time_nin              IN BOOLEAN := TRUE,
        update_institution_in        IN epis_complaint.update_institution%TYPE DEFAULT NULL,
        update_institution_nin       IN BOOLEAN := TRUE,
        id_cancel_info_det_in        IN epis_complaint.id_cancel_info_det%TYPE DEFAULT NULL,
        id_cancel_info_det_nin       IN BOOLEAN := TRUE,
        patient_complaint_arabic_in  IN epis_complaint.patient_complaint_arabic%TYPE DEFAULT NULL,
        patient_complaint_arabic_nin IN BOOLEAN := TRUE,
        id_complaint_alias_in        IN epis_complaint.id_complaint_alias%TYPE DEFAULT NULL,
        id_complaint_alias_nin       IN BOOLEAN := TRUE,
        where_in                     IN VARCHAR2,
        handle_error_in              IN BOOLEAN := TRUE,
        rows_out                     IN OUT table_varchar
    );

    --Update any/all columns by dynamic WHERE
    -- If you pass NULL, then the current column value is set to itself
    PROCEDURE upd
    (
        id_episode_in                IN epis_complaint.id_episode%TYPE DEFAULT NULL,
        id_episode_nin               IN BOOLEAN := TRUE,
        id_professional_in           IN epis_complaint.id_professional%TYPE DEFAULT NULL,
        id_professional_nin          IN BOOLEAN := TRUE,
        id_complaint_in              IN epis_complaint.id_complaint%TYPE DEFAULT NULL,
        id_complaint_nin             IN BOOLEAN := TRUE,
        patient_complaint_in         IN epis_complaint.patient_complaint%TYPE DEFAULT NULL,
        patient_complaint_nin        IN BOOLEAN := TRUE,
        flg_status_in                IN epis_complaint.flg_status%TYPE DEFAULT NULL,
        flg_status_nin               IN BOOLEAN := TRUE,
        id_epis_complaint_parent_in  IN epis_complaint.id_epis_complaint_parent%TYPE DEFAULT NULL,
        id_epis_complaint_parent_nin IN BOOLEAN := TRUE,
        adw_last_update_tstz_in      IN epis_complaint.adw_last_update_tstz%TYPE DEFAULT NULL,
        adw_last_update_tstz_nin     IN BOOLEAN := TRUE,
        flg_reported_by_in           IN epis_complaint.flg_reported_by%TYPE DEFAULT NULL,
        flg_reported_by_nin          IN BOOLEAN := TRUE,
        id_epis_complaint_root_in    IN epis_complaint.id_epis_complaint_root%TYPE DEFAULT NULL,
        id_epis_complaint_root_nin   IN BOOLEAN := TRUE,
        id_dep_clin_serv_in          IN epis_complaint.id_dep_clin_serv%TYPE DEFAULT NULL,
        id_dep_clin_serv_nin         IN BOOLEAN := TRUE,
        flg_edition_type_in          IN epis_complaint.flg_edition_type%TYPE DEFAULT NULL,
        flg_edition_type_nin         IN BOOLEAN := TRUE,
        create_user_in               IN epis_complaint.create_user%TYPE DEFAULT NULL,
        create_user_nin              IN BOOLEAN := TRUE,
        create_time_in               IN epis_complaint.create_time%TYPE DEFAULT NULL,
        create_time_nin              IN BOOLEAN := TRUE,
        create_institution_in        IN epis_complaint.create_institution%TYPE DEFAULT NULL,
        create_institution_nin       IN BOOLEAN := TRUE,
        update_user_in               IN epis_complaint.update_user%TYPE DEFAULT NULL,
        update_user_nin              IN BOOLEAN := TRUE,
        update_time_in               IN epis_complaint.update_time%TYPE DEFAULT NULL,
        update_time_nin              IN BOOLEAN := TRUE,
        update_institution_in        IN epis_complaint.update_institution%TYPE DEFAULT NULL,
        update_institution_nin       IN BOOLEAN := TRUE,
        id_cancel_info_det_in        IN epis_complaint.id_cancel_info_det%TYPE DEFAULT NULL,
        id_cancel_info_det_nin       IN BOOLEAN := TRUE,
        patient_complaint_arabic_in  IN epis_complaint.patient_complaint_arabic%TYPE DEFAULT NULL,
        patient_complaint_arabic_nin IN BOOLEAN := TRUE,
        id_complaint_alias_in        IN epis_complaint.id_complaint_alias%TYPE DEFAULT NULL,
        id_complaint_alias_nin       IN BOOLEAN := TRUE,
        where_in                     IN VARCHAR2,
        handle_error_in              IN BOOLEAN := TRUE
    );

    --Update/insert with columns (with rows_out)
    PROCEDURE upd_ins
    (
        id_epis_complaint_in        IN epis_complaint.id_epis_complaint%TYPE,
        id_episode_in               IN epis_complaint.id_episode%TYPE DEFAULT NULL,
        id_professional_in          IN epis_complaint.id_professional%TYPE DEFAULT NULL,
        id_complaint_in             IN epis_complaint.id_complaint%TYPE DEFAULT NULL,
        patient_complaint_in        IN epis_complaint.patient_complaint%TYPE DEFAULT NULL,
        flg_status_in               IN epis_complaint.flg_status%TYPE DEFAULT NULL,
        id_epis_complaint_parent_in IN epis_complaint.id_epis_complaint_parent%TYPE DEFAULT NULL,
        adw_last_update_tstz_in     IN epis_complaint.adw_last_update_tstz%TYPE DEFAULT NULL,
        flg_reported_by_in          IN epis_complaint.flg_reported_by%TYPE DEFAULT NULL,
        id_epis_complaint_root_in   IN epis_complaint.id_epis_complaint_root%TYPE DEFAULT NULL,
        id_dep_clin_serv_in         IN epis_complaint.id_dep_clin_serv%TYPE DEFAULT NULL,
        flg_edition_type_in         IN epis_complaint.flg_edition_type%TYPE DEFAULT NULL,
        create_user_in              IN epis_complaint.create_user%TYPE DEFAULT NULL,
        create_time_in              IN epis_complaint.create_time%TYPE DEFAULT NULL,
        create_institution_in       IN epis_complaint.create_institution%TYPE DEFAULT NULL,
        update_user_in              IN epis_complaint.update_user%TYPE DEFAULT NULL,
        update_time_in              IN epis_complaint.update_time%TYPE DEFAULT NULL,
        update_institution_in       IN epis_complaint.update_institution%TYPE DEFAULT NULL,
        id_cancel_info_det_in       IN epis_complaint.id_cancel_info_det%TYPE DEFAULT NULL,
        patient_complaint_arabic_in IN epis_complaint.patient_complaint_arabic%TYPE DEFAULT NULL,
        id_complaint_alias_in       IN epis_complaint.id_complaint_alias%TYPE DEFAULT NULL,
        handle_error_in             IN BOOLEAN := TRUE,
        rows_out                    IN OUT table_varchar
    );

    --Update/insert with columns (without rows_out)
    PROCEDURE upd_ins
    (
        id_epis_complaint_in        IN epis_complaint.id_epis_complaint%TYPE,
        id_episode_in               IN epis_complaint.id_episode%TYPE DEFAULT NULL,
        id_professional_in          IN epis_complaint.id_professional%TYPE DEFAULT NULL,
        id_complaint_in             IN epis_complaint.id_complaint%TYPE DEFAULT NULL,
        patient_complaint_in        IN epis_complaint.patient_complaint%TYPE DEFAULT NULL,
        flg_status_in               IN epis_complaint.flg_status%TYPE DEFAULT NULL,
        id_epis_complaint_parent_in IN epis_complaint.id_epis_complaint_parent%TYPE DEFAULT NULL,
        adw_last_update_tstz_in     IN epis_complaint.adw_last_update_tstz%TYPE DEFAULT NULL,
        flg_reported_by_in          IN epis_complaint.flg_reported_by%TYPE DEFAULT NULL,
        id_epis_complaint_root_in   IN epis_complaint.id_epis_complaint_root%TYPE DEFAULT NULL,
        id_dep_clin_serv_in         IN epis_complaint.id_dep_clin_serv%TYPE DEFAULT NULL,
        flg_edition_type_in         IN epis_complaint.flg_edition_type%TYPE DEFAULT NULL,
        create_user_in              IN epis_complaint.create_user%TYPE DEFAULT NULL,
        create_time_in              IN epis_complaint.create_time%TYPE DEFAULT NULL,
        create_institution_in       IN epis_complaint.create_institution%TYPE DEFAULT NULL,
        update_user_in              IN epis_complaint.update_user%TYPE DEFAULT NULL,
        update_time_in              IN epis_complaint.update_time%TYPE DEFAULT NULL,
        update_institution_in       IN epis_complaint.update_institution%TYPE DEFAULT NULL,
        id_cancel_info_det_in       IN epis_complaint.id_cancel_info_det%TYPE DEFAULT NULL,
        patient_complaint_arabic_in IN epis_complaint.patient_complaint_arabic%TYPE DEFAULT NULL,
        id_complaint_alias_in       IN epis_complaint.id_complaint_alias%TYPE DEFAULT NULL,
        handle_error_in             IN BOOLEAN := TRUE
    );

    --Update record (with rows_out)
    PROCEDURE upd
    (
        rec_in          IN epis_complaint%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        IN OUT table_varchar
    );

    --Update record (without rows_out)
    PROCEDURE upd
    (
        rec_in          IN epis_complaint%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    --Update collection (with rows_out)
    PROCEDURE upd
    (
        col_in            IN epis_complaint_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          IN OUT table_varchar
    );

    --Update collection (without rows_out)
    PROCEDURE upd
    (
        col_in            IN epis_complaint_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE
    );

    -- Use Native Dynamic SQL increment a single NUMBER column
    -- for all rows specified by the dynamic WHERE clause
    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    --increment column value
    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_epis_complaint_in IN epis_complaint.id_epis_complaint%TYPE,
        handle_error_in      IN BOOLEAN := TRUE,
        rows_out             OUT table_varchar
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_epis_complaint_in IN epis_complaint.id_epis_complaint%TYPE,
        handle_error_in      IN BOOLEAN := TRUE
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this ECOMPR_ECOMP_FK foreign key value
    PROCEDURE del_ecompr_ecomp_fk
    (
        id_epis_complaint_root_in IN epis_complaint.id_epis_complaint_root%TYPE,
        handle_error_in           IN BOOLEAN := TRUE,
        rows_out                  OUT table_varchar
    );

    -- Delete all rows for this ECOMP_CALIAS_FK foreign key value
    PROCEDURE del_ecomp_calias_fk
    (
        id_complaint_alias_in IN epis_complaint.id_complaint_alias%TYPE,
        handle_error_in       IN BOOLEAN := TRUE,
        rows_out              OUT table_varchar
    );

    -- Delete all rows for this ECOMP_CID_FK foreign key value
    PROCEDURE del_ecomp_cid_fk
    (
        id_cancel_info_det_in IN epis_complaint.id_cancel_info_det%TYPE,
        handle_error_in       IN BOOLEAN := TRUE,
        rows_out              OUT table_varchar
    );

    -- Delete all rows for this ECOMP_CMPLT_FK foreign key value
    PROCEDURE del_ecomp_cmplt_fk
    (
        id_complaint_in IN epis_complaint.id_complaint%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for this ECOMP_DCS_FK foreign key value
    PROCEDURE del_ecomp_dcs_fk
    (
        id_dep_clin_serv_in IN epis_complaint.id_dep_clin_serv%TYPE,
        handle_error_in     IN BOOLEAN := TRUE,
        rows_out            OUT table_varchar
    );

    -- Delete all rows for this ECOMP_EPIS_FK foreign key value
    PROCEDURE del_ecomp_epis_fk
    (
        id_episode_in   IN epis_complaint.id_episode%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for this ECOMP_PROF_FK foreign key value
    PROCEDURE del_ecomp_prof_fk
    (
        id_professional_in IN epis_complaint.id_professional%TYPE,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    -- Delete all rows for this ECOMPR_ECOMP_FK foreign key value
    PROCEDURE del_ecompr_ecomp_fk
    (
        id_epis_complaint_root_in IN epis_complaint.id_epis_complaint_root%TYPE,
        handle_error_in           IN BOOLEAN := TRUE
    );

    -- Delete all rows for this ECOMP_CALIAS_FK foreign key value
    PROCEDURE del_ecomp_calias_fk
    (
        id_complaint_alias_in IN epis_complaint.id_complaint_alias%TYPE,
        handle_error_in       IN BOOLEAN := TRUE
    );

    -- Delete all rows for this ECOMP_CID_FK foreign key value
    PROCEDURE del_ecomp_cid_fk
    (
        id_cancel_info_det_in IN epis_complaint.id_cancel_info_det%TYPE,
        handle_error_in       IN BOOLEAN := TRUE
    );

    -- Delete all rows for this ECOMP_CMPLT_FK foreign key value
    PROCEDURE del_ecomp_cmplt_fk
    (
        id_complaint_in IN epis_complaint.id_complaint%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this ECOMP_DCS_FK foreign key value
    PROCEDURE del_ecomp_dcs_fk
    (
        id_dep_clin_serv_in IN epis_complaint.id_dep_clin_serv%TYPE,
        handle_error_in     IN BOOLEAN := TRUE
    );

    -- Delete all rows for this ECOMP_EPIS_FK foreign key value
    PROCEDURE del_ecomp_epis_fk
    (
        id_episode_in   IN epis_complaint.id_episode%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this ECOMP_PROF_FK foreign key value
    PROCEDURE del_ecomp_prof_fk
    (
        id_professional_in IN epis_complaint.id_professional%TYPE,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Initialize a record with default values for columns in the table (prc)
    PROCEDURE initrec(epis_complaint_inout IN OUT epis_complaint%ROWTYPE);

    -- Initialize a record with default values for columns in the table (fnc)
    FUNCTION initrec RETURN epis_complaint%ROWTYPE;

    -- Get data rowid
    FUNCTION get_data_rowid(rows_in IN table_varchar) RETURN epis_complaint_tc;

    -- Get data rowid pragma autonomous transaccion
    FUNCTION get_data_rowid_pat(rows_in IN table_varchar) RETURN epis_complaint_tc;

END ts_epis_complaint;
/
