/*-- Last Change Revision: $Rev: 2029069 $*/
/*-- Last Change by: $Author: mario.fernandes $*/
/*-- Date of last change: $Date: 2022-08-02 18:49:36 +0100 (ter, 02 ago 2022) $*/

CREATE OR REPLACE PACKAGE ts_analysis_harvest
/*
| Generated by or retrieved from QCGU - DO NOT MODIFY!
| QCGU - "Get it right, do it fast" - www.ToadWorld.com
| QCGU Universal ID: {1BD37A66-EA60-4927-9A64-F6DD89237236}
| Created On: Janeiro 25, 2011 10:3:14
| Created By: ALERT
*/
 IS

    -- Collection of %ROWTYPE records based on "ANALYSIS_HARVEST"
    TYPE analysis_harvest_tc IS TABLE OF analysis_harvest%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE analysis_harvest_ntt IS TABLE OF analysis_harvest%ROWTYPE;
    TYPE analysis_harvest_vat IS VARRAY(100) OF analysis_harvest%ROWTYPE;

    -- Same type structure, with a static name.
    TYPE aat IS TABLE OF analysis_harvest%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE ntt IS TABLE OF analysis_harvest%ROWTYPE;
    TYPE vat IS VARRAY(100) OF analysis_harvest%ROWTYPE;

    -- Column Collection based on column "ID_ANALYSIS_HARVEST"
    TYPE id_analysis_harvest_cc IS TABLE OF analysis_harvest.id_analysis_harvest%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_ANALYSIS_REQ_DET"
    TYPE id_analysis_req_det_cc IS TABLE OF analysis_harvest.id_analysis_req_det%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_HARVEST"
    TYPE id_harvest_cc IS TABLE OF analysis_harvest.id_harvest%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_ANALYSIS_REQ_PAR"
    TYPE id_analysis_req_par_cc IS TABLE OF analysis_harvest.id_analysis_req_par%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_SAMPLE_RECIPIENT"
    TYPE id_sample_recipient_cc IS TABLE OF analysis_harvest.id_sample_recipient%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "NUM_RECIPIENT"
    TYPE num_recipient_cc IS TABLE OF analysis_harvest.num_recipient%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_USER"
    TYPE create_user_cc IS TABLE OF analysis_harvest.create_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_TIME"
    TYPE create_time_cc IS TABLE OF analysis_harvest.create_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_INSTITUTION"
    TYPE create_institution_cc IS TABLE OF analysis_harvest.create_institution%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_USER"
    TYPE update_user_cc IS TABLE OF analysis_harvest.update_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_TIME"
    TYPE update_time_cc IS TABLE OF analysis_harvest.update_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_INSTITUTION"
    TYPE update_institution_cc IS TABLE OF analysis_harvest.update_institution%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "FLG_STATUS"
    TYPE flg_status_cc IS TABLE OF analysis_harvest.flg_status%TYPE INDEX BY BINARY_INTEGER;

    -- Insert one row, providing primary key if present
    PROCEDURE ins
    (
        id_analysis_harvest_in IN analysis_harvest.id_analysis_harvest%TYPE,
        id_analysis_req_det_in IN analysis_harvest.id_analysis_req_det%TYPE DEFAULT NULL,
        id_harvest_in          IN analysis_harvest.id_harvest%TYPE DEFAULT NULL,
        id_analysis_req_par_in IN analysis_harvest.id_analysis_req_par%TYPE DEFAULT NULL,
        id_sample_recipient_in IN analysis_harvest.id_sample_recipient%TYPE DEFAULT NULL,
        num_recipient_in       IN analysis_harvest.num_recipient%TYPE DEFAULT NULL,
        create_user_in         IN analysis_harvest.create_user%TYPE DEFAULT NULL,
        create_time_in         IN analysis_harvest.create_time%TYPE DEFAULT NULL,
        create_institution_in  IN analysis_harvest.create_institution%TYPE DEFAULT NULL,
        update_user_in         IN analysis_harvest.update_user%TYPE DEFAULT NULL,
        update_time_in         IN analysis_harvest.update_time%TYPE DEFAULT NULL,
        update_institution_in  IN analysis_harvest.update_institution%TYPE DEFAULT NULL,
        flg_status_in          IN analysis_harvest.flg_status%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_analysis_harvest_in IN analysis_harvest.id_analysis_harvest%TYPE,
        id_analysis_req_det_in IN analysis_harvest.id_analysis_req_det%TYPE DEFAULT NULL,
        id_harvest_in          IN analysis_harvest.id_harvest%TYPE DEFAULT NULL,
        id_analysis_req_par_in IN analysis_harvest.id_analysis_req_par%TYPE DEFAULT NULL,
        id_sample_recipient_in IN analysis_harvest.id_sample_recipient%TYPE DEFAULT NULL,
        num_recipient_in       IN analysis_harvest.num_recipient%TYPE DEFAULT NULL,
        create_user_in         IN analysis_harvest.create_user%TYPE DEFAULT NULL,
        create_time_in         IN analysis_harvest.create_time%TYPE DEFAULT NULL,
        create_institution_in  IN analysis_harvest.create_institution%TYPE DEFAULT NULL,
        update_user_in         IN analysis_harvest.update_user%TYPE DEFAULT NULL,
        update_time_in         IN analysis_harvest.update_time%TYPE DEFAULT NULL,
        update_institution_in  IN analysis_harvest.update_institution%TYPE DEFAULT NULL,
        flg_status_in          IN analysis_harvest.flg_status%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a row based on a record.
    -- Specify whether or not a primary key value should be generated.
    PROCEDURE ins
    (
        rec_in          IN analysis_harvest%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rec_in          IN analysis_harvest%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a collection of rows using FORALL; all primary key values
    -- must have already been generated, or are handled in triggers.
    PROCEDURE ins
    (
        rows_in         IN analysis_harvest_tc,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rows_in         IN analysis_harvest_tc,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Return next primary key value using the named sequence.
    FUNCTION next_key(sequence_in IN VARCHAR2 := NULL) RETURN analysis_harvest.id_analysis_harvest%TYPE;

    -- Insert one row, generating hidden primary key using a sequence
    PROCEDURE ins
    (
        id_analysis_req_det_in IN analysis_harvest.id_analysis_req_det%TYPE DEFAULT NULL,
        id_harvest_in          IN analysis_harvest.id_harvest%TYPE DEFAULT NULL,
        id_analysis_req_par_in IN analysis_harvest.id_analysis_req_par%TYPE DEFAULT NULL,
        id_sample_recipient_in IN analysis_harvest.id_sample_recipient%TYPE DEFAULT NULL,
        num_recipient_in       IN analysis_harvest.num_recipient%TYPE DEFAULT NULL,
        create_user_in         IN analysis_harvest.create_user%TYPE DEFAULT NULL,
        create_time_in         IN analysis_harvest.create_time%TYPE DEFAULT NULL,
        create_institution_in  IN analysis_harvest.create_institution%TYPE DEFAULT NULL,
        update_user_in         IN analysis_harvest.update_user%TYPE DEFAULT NULL,
        update_time_in         IN analysis_harvest.update_time%TYPE DEFAULT NULL,
        update_institution_in  IN analysis_harvest.update_institution%TYPE DEFAULT NULL,
        flg_status_in          IN analysis_harvest.flg_status%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_analysis_req_det_in IN analysis_harvest.id_analysis_req_det%TYPE DEFAULT NULL,
        id_harvest_in          IN analysis_harvest.id_harvest%TYPE DEFAULT NULL,
        id_analysis_req_par_in IN analysis_harvest.id_analysis_req_par%TYPE DEFAULT NULL,
        id_sample_recipient_in IN analysis_harvest.id_sample_recipient%TYPE DEFAULT NULL,
        num_recipient_in       IN analysis_harvest.num_recipient%TYPE DEFAULT NULL,
        create_user_in         IN analysis_harvest.create_user%TYPE DEFAULT NULL,
        create_time_in         IN analysis_harvest.create_time%TYPE DEFAULT NULL,
        create_institution_in  IN analysis_harvest.create_institution%TYPE DEFAULT NULL,
        update_user_in         IN analysis_harvest.update_user%TYPE DEFAULT NULL,
        update_time_in         IN analysis_harvest.update_time%TYPE DEFAULT NULL,
        update_institution_in  IN analysis_harvest.update_institution%TYPE DEFAULT NULL,
        flg_status_in          IN analysis_harvest.flg_status%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert one row, returning primary key generated by sequence
    PROCEDURE ins
    (
        id_analysis_req_det_in  IN analysis_harvest.id_analysis_req_det%TYPE DEFAULT NULL,
        id_harvest_in           IN analysis_harvest.id_harvest%TYPE DEFAULT NULL,
        id_analysis_req_par_in  IN analysis_harvest.id_analysis_req_par%TYPE DEFAULT NULL,
        id_sample_recipient_in  IN analysis_harvest.id_sample_recipient%TYPE DEFAULT NULL,
        num_recipient_in        IN analysis_harvest.num_recipient%TYPE DEFAULT NULL,
        create_user_in          IN analysis_harvest.create_user%TYPE DEFAULT NULL,
        create_time_in          IN analysis_harvest.create_time%TYPE DEFAULT NULL,
        create_institution_in   IN analysis_harvest.create_institution%TYPE DEFAULT NULL,
        update_user_in          IN analysis_harvest.update_user%TYPE DEFAULT NULL,
        update_time_in          IN analysis_harvest.update_time%TYPE DEFAULT NULL,
        update_institution_in   IN analysis_harvest.update_institution%TYPE DEFAULT NULL,
        flg_status_in           IN analysis_harvest.flg_status%TYPE DEFAULT NULL,
        id_analysis_harvest_out IN OUT analysis_harvest.id_analysis_harvest%TYPE
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_analysis_req_det_in  IN analysis_harvest.id_analysis_req_det%TYPE DEFAULT NULL,
        id_harvest_in           IN analysis_harvest.id_harvest%TYPE DEFAULT NULL,
        id_analysis_req_par_in  IN analysis_harvest.id_analysis_req_par%TYPE DEFAULT NULL,
        id_sample_recipient_in  IN analysis_harvest.id_sample_recipient%TYPE DEFAULT NULL,
        num_recipient_in        IN analysis_harvest.num_recipient%TYPE DEFAULT NULL,
        create_user_in          IN analysis_harvest.create_user%TYPE DEFAULT NULL,
        create_time_in          IN analysis_harvest.create_time%TYPE DEFAULT NULL,
        create_institution_in   IN analysis_harvest.create_institution%TYPE DEFAULT NULL,
        update_user_in          IN analysis_harvest.update_user%TYPE DEFAULT NULL,
        update_time_in          IN analysis_harvest.update_time%TYPE DEFAULT NULL,
        update_institution_in   IN analysis_harvest.update_institution%TYPE DEFAULT NULL,
        flg_status_in           IN analysis_harvest.flg_status%TYPE DEFAULT NULL,
        id_analysis_harvest_out IN OUT analysis_harvest.id_analysis_harvest%TYPE
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert one row with function, return generated primary key
    FUNCTION ins
    (
        id_analysis_req_det_in IN analysis_harvest.id_analysis_req_det%TYPE DEFAULT NULL,
        id_harvest_in          IN analysis_harvest.id_harvest%TYPE DEFAULT NULL,
        id_analysis_req_par_in IN analysis_harvest.id_analysis_req_par%TYPE DEFAULT NULL,
        id_sample_recipient_in IN analysis_harvest.id_sample_recipient%TYPE DEFAULT NULL,
        num_recipient_in       IN analysis_harvest.num_recipient%TYPE DEFAULT NULL,
        create_user_in         IN analysis_harvest.create_user%TYPE DEFAULT NULL,
        create_time_in         IN analysis_harvest.create_time%TYPE DEFAULT NULL,
        create_institution_in  IN analysis_harvest.create_institution%TYPE DEFAULT NULL,
        update_user_in         IN analysis_harvest.update_user%TYPE DEFAULT NULL,
        update_time_in         IN analysis_harvest.update_time%TYPE DEFAULT NULL,
        update_institution_in  IN analysis_harvest.update_institution%TYPE DEFAULT NULL,
        flg_status_in          IN analysis_harvest.flg_status%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) RETURN analysis_harvest.id_analysis_harvest%TYPE;

    FUNCTION ins
    (
        id_analysis_req_det_in IN analysis_harvest.id_analysis_req_det%TYPE DEFAULT NULL,
        id_harvest_in          IN analysis_harvest.id_harvest%TYPE DEFAULT NULL,
        id_analysis_req_par_in IN analysis_harvest.id_analysis_req_par%TYPE DEFAULT NULL,
        id_sample_recipient_in IN analysis_harvest.id_sample_recipient%TYPE DEFAULT NULL,
        num_recipient_in       IN analysis_harvest.num_recipient%TYPE DEFAULT NULL,
        create_user_in         IN analysis_harvest.create_user%TYPE DEFAULT NULL,
        create_time_in         IN analysis_harvest.create_time%TYPE DEFAULT NULL,
        create_institution_in  IN analysis_harvest.create_institution%TYPE DEFAULT NULL,
        update_user_in         IN analysis_harvest.update_user%TYPE DEFAULT NULL,
        update_time_in         IN analysis_harvest.update_time%TYPE DEFAULT NULL,
        update_institution_in  IN analysis_harvest.update_institution%TYPE DEFAULT NULL,
        flg_status_in          IN analysis_harvest.flg_status%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    ) RETURN analysis_harvest.id_analysis_harvest%TYPE;

    -- Update any/all columns by primary key. If you pass NULL, then
    -- the current column value is set to itself. If you need a more
    -- selected UPDATE then use one of the onecol procedures below.

    PROCEDURE upd
    (
        id_analysis_harvest_in  IN analysis_harvest.id_analysis_harvest%TYPE,
        id_analysis_req_det_in  IN analysis_harvest.id_analysis_req_det%TYPE DEFAULT NULL,
        id_analysis_req_det_nin IN BOOLEAN := TRUE,
        id_harvest_in           IN analysis_harvest.id_harvest%TYPE DEFAULT NULL,
        id_harvest_nin          IN BOOLEAN := TRUE,
        id_analysis_req_par_in  IN analysis_harvest.id_analysis_req_par%TYPE DEFAULT NULL,
        id_analysis_req_par_nin IN BOOLEAN := TRUE,
        id_sample_recipient_in  IN analysis_harvest.id_sample_recipient%TYPE DEFAULT NULL,
        id_sample_recipient_nin IN BOOLEAN := TRUE,
        num_recipient_in        IN analysis_harvest.num_recipient%TYPE DEFAULT NULL,
        num_recipient_nin       IN BOOLEAN := TRUE,
        create_user_in          IN analysis_harvest.create_user%TYPE DEFAULT NULL,
        create_user_nin         IN BOOLEAN := TRUE,
        create_time_in          IN analysis_harvest.create_time%TYPE DEFAULT NULL,
        create_time_nin         IN BOOLEAN := TRUE,
        create_institution_in   IN analysis_harvest.create_institution%TYPE DEFAULT NULL,
        create_institution_nin  IN BOOLEAN := TRUE,
        update_user_in          IN analysis_harvest.update_user%TYPE DEFAULT NULL,
        update_user_nin         IN BOOLEAN := TRUE,
        update_time_in          IN analysis_harvest.update_time%TYPE DEFAULT NULL,
        update_time_nin         IN BOOLEAN := TRUE,
        update_institution_in   IN analysis_harvest.update_institution%TYPE DEFAULT NULL,
        update_institution_nin  IN BOOLEAN := TRUE,
        flg_status_in           IN analysis_harvest.flg_status%TYPE DEFAULT NULL,
        flg_status_nin          IN BOOLEAN := TRUE,
        handle_error_in         IN BOOLEAN := TRUE,
        rows_out                IN OUT table_varchar
    );

    PROCEDURE upd
    (
        id_analysis_harvest_in  IN analysis_harvest.id_analysis_harvest%TYPE,
        id_analysis_req_det_in  IN analysis_harvest.id_analysis_req_det%TYPE DEFAULT NULL,
        id_analysis_req_det_nin IN BOOLEAN := TRUE,
        id_harvest_in           IN analysis_harvest.id_harvest%TYPE DEFAULT NULL,
        id_harvest_nin          IN BOOLEAN := TRUE,
        id_analysis_req_par_in  IN analysis_harvest.id_analysis_req_par%TYPE DEFAULT NULL,
        id_analysis_req_par_nin IN BOOLEAN := TRUE,
        id_sample_recipient_in  IN analysis_harvest.id_sample_recipient%TYPE DEFAULT NULL,
        id_sample_recipient_nin IN BOOLEAN := TRUE,
        num_recipient_in        IN analysis_harvest.num_recipient%TYPE DEFAULT NULL,
        num_recipient_nin       IN BOOLEAN := TRUE,
        create_user_in          IN analysis_harvest.create_user%TYPE DEFAULT NULL,
        create_user_nin         IN BOOLEAN := TRUE,
        create_time_in          IN analysis_harvest.create_time%TYPE DEFAULT NULL,
        create_time_nin         IN BOOLEAN := TRUE,
        create_institution_in   IN analysis_harvest.create_institution%TYPE DEFAULT NULL,
        create_institution_nin  IN BOOLEAN := TRUE,
        update_user_in          IN analysis_harvest.update_user%TYPE DEFAULT NULL,
        update_user_nin         IN BOOLEAN := TRUE,
        update_time_in          IN analysis_harvest.update_time%TYPE DEFAULT NULL,
        update_time_nin         IN BOOLEAN := TRUE,
        update_institution_in   IN analysis_harvest.update_institution%TYPE DEFAULT NULL,
        update_institution_nin  IN BOOLEAN := TRUE,
        flg_status_in           IN analysis_harvest.flg_status%TYPE DEFAULT NULL,
        flg_status_nin          IN BOOLEAN := TRUE,
        handle_error_in         IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        id_analysis_req_det_in  IN analysis_harvest.id_analysis_req_det%TYPE DEFAULT NULL,
        id_analysis_req_det_nin IN BOOLEAN := TRUE,
        id_harvest_in           IN analysis_harvest.id_harvest%TYPE DEFAULT NULL,
        id_harvest_nin          IN BOOLEAN := TRUE,
        id_analysis_req_par_in  IN analysis_harvest.id_analysis_req_par%TYPE DEFAULT NULL,
        id_analysis_req_par_nin IN BOOLEAN := TRUE,
        id_sample_recipient_in  IN analysis_harvest.id_sample_recipient%TYPE DEFAULT NULL,
        id_sample_recipient_nin IN BOOLEAN := TRUE,
        num_recipient_in        IN analysis_harvest.num_recipient%TYPE DEFAULT NULL,
        num_recipient_nin       IN BOOLEAN := TRUE,
        create_user_in          IN analysis_harvest.create_user%TYPE DEFAULT NULL,
        create_user_nin         IN BOOLEAN := TRUE,
        create_time_in          IN analysis_harvest.create_time%TYPE DEFAULT NULL,
        create_time_nin         IN BOOLEAN := TRUE,
        create_institution_in   IN analysis_harvest.create_institution%TYPE DEFAULT NULL,
        create_institution_nin  IN BOOLEAN := TRUE,
        update_user_in          IN analysis_harvest.update_user%TYPE DEFAULT NULL,
        update_user_nin         IN BOOLEAN := TRUE,
        update_time_in          IN analysis_harvest.update_time%TYPE DEFAULT NULL,
        update_time_nin         IN BOOLEAN := TRUE,
        update_institution_in   IN analysis_harvest.update_institution%TYPE DEFAULT NULL,
        update_institution_nin  IN BOOLEAN := TRUE,
        flg_status_in           IN analysis_harvest.flg_status%TYPE DEFAULT NULL,
        flg_status_nin          IN BOOLEAN := TRUE,
        where_in                VARCHAR2 DEFAULT NULL,
        handle_error_in         IN BOOLEAN := TRUE,
        rows_out                IN OUT table_varchar
    );

    PROCEDURE upd
    (
        id_analysis_req_det_in  IN analysis_harvest.id_analysis_req_det%TYPE DEFAULT NULL,
        id_analysis_req_det_nin IN BOOLEAN := TRUE,
        id_harvest_in           IN analysis_harvest.id_harvest%TYPE DEFAULT NULL,
        id_harvest_nin          IN BOOLEAN := TRUE,
        id_analysis_req_par_in  IN analysis_harvest.id_analysis_req_par%TYPE DEFAULT NULL,
        id_analysis_req_par_nin IN BOOLEAN := TRUE,
        id_sample_recipient_in  IN analysis_harvest.id_sample_recipient%TYPE DEFAULT NULL,
        id_sample_recipient_nin IN BOOLEAN := TRUE,
        num_recipient_in        IN analysis_harvest.num_recipient%TYPE DEFAULT NULL,
        num_recipient_nin       IN BOOLEAN := TRUE,
        create_user_in          IN analysis_harvest.create_user%TYPE DEFAULT NULL,
        create_user_nin         IN BOOLEAN := TRUE,
        create_time_in          IN analysis_harvest.create_time%TYPE DEFAULT NULL,
        create_time_nin         IN BOOLEAN := TRUE,
        create_institution_in   IN analysis_harvest.create_institution%TYPE DEFAULT NULL,
        create_institution_nin  IN BOOLEAN := TRUE,
        update_user_in          IN analysis_harvest.update_user%TYPE DEFAULT NULL,
        update_user_nin         IN BOOLEAN := TRUE,
        update_time_in          IN analysis_harvest.update_time%TYPE DEFAULT NULL,
        update_time_nin         IN BOOLEAN := TRUE,
        update_institution_in   IN analysis_harvest.update_institution%TYPE DEFAULT NULL,
        update_institution_nin  IN BOOLEAN := TRUE,
        flg_status_in           IN analysis_harvest.flg_status%TYPE DEFAULT NULL,
        flg_status_nin          IN BOOLEAN := TRUE,
        where_in                VARCHAR2 DEFAULT NULL,
        handle_error_in         IN BOOLEAN := TRUE
    );

    PROCEDURE upd_ins
    (
        id_analysis_harvest_in IN analysis_harvest.id_analysis_harvest%TYPE,
        id_analysis_req_det_in IN analysis_harvest.id_analysis_req_det%TYPE DEFAULT NULL,
        id_harvest_in          IN analysis_harvest.id_harvest%TYPE DEFAULT NULL,
        id_analysis_req_par_in IN analysis_harvest.id_analysis_req_par%TYPE DEFAULT NULL,
        id_sample_recipient_in IN analysis_harvest.id_sample_recipient%TYPE DEFAULT NULL,
        num_recipient_in       IN analysis_harvest.num_recipient%TYPE DEFAULT NULL,
        create_user_in         IN analysis_harvest.create_user%TYPE DEFAULT NULL,
        create_time_in         IN analysis_harvest.create_time%TYPE DEFAULT NULL,
        create_institution_in  IN analysis_harvest.create_institution%TYPE DEFAULT NULL,
        update_user_in         IN analysis_harvest.update_user%TYPE DEFAULT NULL,
        update_time_in         IN analysis_harvest.update_time%TYPE DEFAULT NULL,
        update_institution_in  IN analysis_harvest.update_institution%TYPE DEFAULT NULL,
        flg_status_in          IN analysis_harvest.flg_status%TYPE DEFAULT NULL,
        handle_error_in        IN BOOLEAN := TRUE,
        rows_out               OUT table_varchar
    );

    PROCEDURE upd_ins
    (
        id_analysis_harvest_in IN analysis_harvest.id_analysis_harvest%TYPE,
        id_analysis_req_det_in IN analysis_harvest.id_analysis_req_det%TYPE DEFAULT NULL,
        id_harvest_in          IN analysis_harvest.id_harvest%TYPE DEFAULT NULL,
        id_analysis_req_par_in IN analysis_harvest.id_analysis_req_par%TYPE DEFAULT NULL,
        id_sample_recipient_in IN analysis_harvest.id_sample_recipient%TYPE DEFAULT NULL,
        num_recipient_in       IN analysis_harvest.num_recipient%TYPE DEFAULT NULL,
        create_user_in         IN analysis_harvest.create_user%TYPE DEFAULT NULL,
        create_time_in         IN analysis_harvest.create_time%TYPE DEFAULT NULL,
        create_institution_in  IN analysis_harvest.create_institution%TYPE DEFAULT NULL,
        update_user_in         IN analysis_harvest.update_user%TYPE DEFAULT NULL,
        update_time_in         IN analysis_harvest.update_time%TYPE DEFAULT NULL,
        update_institution_in  IN analysis_harvest.update_institution%TYPE DEFAULT NULL,
        flg_status_in          IN analysis_harvest.flg_status%TYPE DEFAULT NULL,
        handle_error_in        IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        rec_in          IN analysis_harvest%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        IN OUT table_varchar
    );

    PROCEDURE upd
    (
        rec_in          IN analysis_harvest%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        col_in            IN analysis_harvest_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          IN OUT table_varchar
    );

    PROCEDURE upd
    (
        col_in            IN analysis_harvest_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE
    );

    -- Use Native Dynamic SQL increment a single NUMBER column
    -- for all rows specified by the dynamic WHERE clause
    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_analysis_harvest_in IN analysis_harvest.id_analysis_harvest%TYPE,
        handle_error_in        IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_analysis_harvest_in IN analysis_harvest.id_analysis_harvest%TYPE,
        handle_error_in        IN BOOLEAN := TRUE,
        rows_out               OUT table_varchar
    );

    -- Delete all rows for primary key column ID_ANALYSIS_HARVEST
    PROCEDURE del_id_analysis_harvest
    (
        id_analysis_harvest_in IN analysis_harvest.id_analysis_harvest%TYPE,
        handle_error_in        IN BOOLEAN := TRUE
    );

    -- Delete all rows for primary key column ID_ANALYSIS_HARVEST
    PROCEDURE del_id_analysis_harvest
    (
        id_analysis_harvest_in IN analysis_harvest.id_analysis_harvest%TYPE,
        handle_error_in        IN BOOLEAN := TRUE,
        rows_out               OUT table_varchar
    );

    -- Delete for unique value of AHT_ARD_HARV_UI
    PROCEDURE del_aht_ard_harv_ui
    (
        id_analysis_req_det_in IN analysis_harvest.id_analysis_req_det%TYPE,
        id_harvest_in          IN analysis_harvest.id_harvest%TYPE,
        handle_error_in        IN BOOLEAN := TRUE
    );

    -- Delete for unique value of AHT_ARD_HARV_UI
    PROCEDURE del_aht_ard_harv_ui
    (
        id_analysis_req_det_in IN analysis_harvest.id_analysis_req_det%TYPE,
        id_harvest_in          IN analysis_harvest.id_harvest%TYPE,
        handle_error_in        IN BOOLEAN := TRUE,
        rows_out               OUT table_varchar
    );

    -- Delete all rows for this AHT_ARD_FK foreign key value
    PROCEDURE del_aht_ard_fk
    (
        id_analysis_req_det_in IN analysis_harvest.id_analysis_req_det%TYPE,
        handle_error_in        IN BOOLEAN := TRUE
    );

    -- Delete all rows for this AHT_ARD_FK foreign key value
    PROCEDURE del_aht_ard_fk
    (
        id_analysis_req_det_in IN analysis_harvest.id_analysis_req_det%TYPE,
        handle_error_in        IN BOOLEAN := TRUE,
        rows_out               OUT table_varchar
    );

    -- Delete all rows for this AHT_ARP_FK foreign key value
    PROCEDURE del_aht_arp_fk
    (
        id_analysis_req_par_in IN analysis_harvest.id_analysis_req_par%TYPE,
        handle_error_in        IN BOOLEAN := TRUE
    );

    -- Delete all rows for this AHT_ARP_FK foreign key value
    PROCEDURE del_aht_arp_fk
    (
        id_analysis_req_par_in IN analysis_harvest.id_analysis_req_par%TYPE,
        handle_error_in        IN BOOLEAN := TRUE,
        rows_out               OUT table_varchar
    );

    -- Delete all rows for this AHT_HARV_FK foreign key value
    PROCEDURE del_aht_harv_fk
    (
        id_harvest_in   IN analysis_harvest.id_harvest%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this AHT_HARV_FK foreign key value
    PROCEDURE del_aht_harv_fk
    (
        id_harvest_in   IN analysis_harvest.id_harvest%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for this AHT_SRT_FK foreign key value
    PROCEDURE del_aht_srt_fk
    (
        id_sample_recipient_in IN analysis_harvest.id_sample_recipient%TYPE,
        handle_error_in        IN BOOLEAN := TRUE
    );

    -- Delete all rows for this AHT_SRT_FK foreign key value
    PROCEDURE del_aht_srt_fk
    (
        id_sample_recipient_in IN analysis_harvest.id_sample_recipient%TYPE,
        handle_error_in        IN BOOLEAN := TRUE,
        rows_out               OUT table_varchar
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Initialize a record with default values for columns in the table.
    PROCEDURE initrec(analysis_harvest_inout IN OUT analysis_harvest%ROWTYPE);

    FUNCTION initrec RETURN analysis_harvest%ROWTYPE;

    FUNCTION get_data_rowid(rows_in IN table_varchar) RETURN analysis_harvest_tc;

    FUNCTION get_data_rowid_pat(rows_in IN table_varchar) RETURN analysis_harvest_tc;

END ts_analysis_harvest;
/
