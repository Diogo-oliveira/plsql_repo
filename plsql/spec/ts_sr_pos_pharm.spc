/*-- Last Change Revision: $Rev: 482111 $*/
/*-- Last Change by: $Author: gustavo.serrano $*/
/*-- Date of last change: $Date: 2010-04-21 14:29:24 +0100 (qua, 21 abr 2010) $*/

CREATE OR REPLACE PACKAGE ts_sr_pos_pharm
/*
| Generated by or retrieved from QCGU - DO NOT MODIFY!
| QCGU - "Get it right, do it fast" - www.ToadWorld.com
| QCGU Universal ID: {1BD37A66-EA60-4927-9A64-F6DD89237236}
| Created On: Abril 13, 2010 19:19:11
| Created By: ALERT
*/
 IS

    -- Collection of %ROWTYPE records based on "SR_POS_PHARM"
    TYPE sr_pos_pharm_tc IS TABLE OF sr_pos_pharm%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE sr_pos_pharm_ntt IS TABLE OF sr_pos_pharm%ROWTYPE;
    TYPE sr_pos_pharm_vat IS VARRAY(100) OF sr_pos_pharm%ROWTYPE;

    -- Same type structure, with a static name.
    TYPE aat IS TABLE OF sr_pos_pharm%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE ntt IS TABLE OF sr_pos_pharm%ROWTYPE;
    TYPE vat IS VARRAY(100) OF sr_pos_pharm%ROWTYPE;

    -- Column Collection based on column "ID_SR_POS_PHARM"
    TYPE id_sr_pos_pharm_cc IS TABLE OF sr_pos_pharm.id_sr_pos_pharm%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_SR_POS_SCHEDULE"
    TYPE id_sr_pos_schedule_cc IS TABLE OF sr_pos_pharm.id_sr_pos_schedule%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "FLG_STATUS"
    TYPE flg_status_cc IS TABLE OF sr_pos_pharm.flg_status%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "NOTES_EVALUATION"
    TYPE notes_evaluation_cc IS TABLE OF sr_pos_pharm.notes_evaluation%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_CANCEL_REASON"
    TYPE id_cancel_reason_cc IS TABLE OF sr_pos_pharm.id_cancel_reason%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "NOTES_CANCEL"
    TYPE notes_cancel_cc IS TABLE OF sr_pos_pharm.notes_cancel%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_PROF_REG"
    TYPE id_prof_reg_cc IS TABLE OF sr_pos_pharm.id_prof_reg%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "DT_REG"
    TYPE dt_reg_cc IS TABLE OF sr_pos_pharm.dt_reg%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_USER"
    TYPE create_user_cc IS TABLE OF sr_pos_pharm.create_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_TIME"
    TYPE create_time_cc IS TABLE OF sr_pos_pharm.create_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_INSTITUTION"
    TYPE create_institution_cc IS TABLE OF sr_pos_pharm.create_institution%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_USER"
    TYPE update_user_cc IS TABLE OF sr_pos_pharm.update_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_TIME"
    TYPE update_time_cc IS TABLE OF sr_pos_pharm.update_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_INSTITUTION"
    TYPE update_institution_cc IS TABLE OF sr_pos_pharm.update_institution%TYPE INDEX BY BINARY_INTEGER;

    -- Insert one row, providing primary key if present
    PROCEDURE ins
    (
        id_sr_pos_pharm_in    IN sr_pos_pharm.id_sr_pos_pharm%TYPE,
        id_sr_pos_schedule_in IN sr_pos_pharm.id_sr_pos_schedule%TYPE DEFAULT NULL,
        flg_status_in         IN sr_pos_pharm.flg_status%TYPE DEFAULT NULL,
        notes_evaluation_in   IN sr_pos_pharm.notes_evaluation%TYPE DEFAULT NULL,
        id_cancel_reason_in   IN sr_pos_pharm.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in       IN sr_pos_pharm.notes_cancel%TYPE DEFAULT NULL,
        id_prof_reg_in        IN sr_pos_pharm.id_prof_reg%TYPE DEFAULT NULL,
        dt_reg_in             IN sr_pos_pharm.dt_reg%TYPE DEFAULT NULL,
        create_user_in        IN sr_pos_pharm.create_user%TYPE DEFAULT NULL,
        create_time_in        IN sr_pos_pharm.create_time%TYPE DEFAULT NULL,
        create_institution_in IN sr_pos_pharm.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN sr_pos_pharm.update_user%TYPE DEFAULT NULL,
        update_time_in        IN sr_pos_pharm.update_time%TYPE DEFAULT NULL,
        update_institution_in IN sr_pos_pharm.update_institution%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_sr_pos_pharm_in    IN sr_pos_pharm.id_sr_pos_pharm%TYPE,
        id_sr_pos_schedule_in IN sr_pos_pharm.id_sr_pos_schedule%TYPE DEFAULT NULL,
        flg_status_in         IN sr_pos_pharm.flg_status%TYPE DEFAULT NULL,
        notes_evaluation_in   IN sr_pos_pharm.notes_evaluation%TYPE DEFAULT NULL,
        id_cancel_reason_in   IN sr_pos_pharm.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in       IN sr_pos_pharm.notes_cancel%TYPE DEFAULT NULL,
        id_prof_reg_in        IN sr_pos_pharm.id_prof_reg%TYPE DEFAULT NULL,
        dt_reg_in             IN sr_pos_pharm.dt_reg%TYPE DEFAULT NULL,
        create_user_in        IN sr_pos_pharm.create_user%TYPE DEFAULT NULL,
        create_time_in        IN sr_pos_pharm.create_time%TYPE DEFAULT NULL,
        create_institution_in IN sr_pos_pharm.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN sr_pos_pharm.update_user%TYPE DEFAULT NULL,
        update_time_in        IN sr_pos_pharm.update_time%TYPE DEFAULT NULL,
        update_institution_in IN sr_pos_pharm.update_institution%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a row based on a record.
    -- Specify whether or not a primary key value should be generated.
    PROCEDURE ins
    (
        rec_in          IN sr_pos_pharm%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rec_in          IN sr_pos_pharm%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a collection of rows using FORALL; all primary key values
    -- must have already been generated, or are handled in triggers.
    PROCEDURE ins
    (
        rows_in         IN sr_pos_pharm_tc,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rows_in         IN sr_pos_pharm_tc,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Return next primary key value using the named sequence.
    FUNCTION next_key(sequence_in IN VARCHAR2 := NULL) RETURN sr_pos_pharm.id_sr_pos_pharm%TYPE;

    -- Insert one row, generating hidden primary key using a sequence
    PROCEDURE ins
    (
        id_sr_pos_schedule_in IN sr_pos_pharm.id_sr_pos_schedule%TYPE DEFAULT NULL,
        flg_status_in         IN sr_pos_pharm.flg_status%TYPE DEFAULT NULL,
        notes_evaluation_in   IN sr_pos_pharm.notes_evaluation%TYPE DEFAULT NULL,
        id_cancel_reason_in   IN sr_pos_pharm.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in       IN sr_pos_pharm.notes_cancel%TYPE DEFAULT NULL,
        id_prof_reg_in        IN sr_pos_pharm.id_prof_reg%TYPE DEFAULT NULL,
        dt_reg_in             IN sr_pos_pharm.dt_reg%TYPE DEFAULT NULL,
        create_user_in        IN sr_pos_pharm.create_user%TYPE DEFAULT NULL,
        create_time_in        IN sr_pos_pharm.create_time%TYPE DEFAULT NULL,
        create_institution_in IN sr_pos_pharm.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN sr_pos_pharm.update_user%TYPE DEFAULT NULL,
        update_time_in        IN sr_pos_pharm.update_time%TYPE DEFAULT NULL,
        update_institution_in IN sr_pos_pharm.update_institution%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_sr_pos_schedule_in IN sr_pos_pharm.id_sr_pos_schedule%TYPE DEFAULT NULL,
        flg_status_in         IN sr_pos_pharm.flg_status%TYPE DEFAULT NULL,
        notes_evaluation_in   IN sr_pos_pharm.notes_evaluation%TYPE DEFAULT NULL,
        id_cancel_reason_in   IN sr_pos_pharm.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in       IN sr_pos_pharm.notes_cancel%TYPE DEFAULT NULL,
        id_prof_reg_in        IN sr_pos_pharm.id_prof_reg%TYPE DEFAULT NULL,
        dt_reg_in             IN sr_pos_pharm.dt_reg%TYPE DEFAULT NULL,
        create_user_in        IN sr_pos_pharm.create_user%TYPE DEFAULT NULL,
        create_time_in        IN sr_pos_pharm.create_time%TYPE DEFAULT NULL,
        create_institution_in IN sr_pos_pharm.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN sr_pos_pharm.update_user%TYPE DEFAULT NULL,
        update_time_in        IN sr_pos_pharm.update_time%TYPE DEFAULT NULL,
        update_institution_in IN sr_pos_pharm.update_institution%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert one row, returning primary key generated by sequence
    PROCEDURE ins
    (
        id_sr_pos_schedule_in IN sr_pos_pharm.id_sr_pos_schedule%TYPE DEFAULT NULL,
        flg_status_in         IN sr_pos_pharm.flg_status%TYPE DEFAULT NULL,
        notes_evaluation_in   IN sr_pos_pharm.notes_evaluation%TYPE DEFAULT NULL,
        id_cancel_reason_in   IN sr_pos_pharm.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in       IN sr_pos_pharm.notes_cancel%TYPE DEFAULT NULL,
        id_prof_reg_in        IN sr_pos_pharm.id_prof_reg%TYPE DEFAULT NULL,
        dt_reg_in             IN sr_pos_pharm.dt_reg%TYPE DEFAULT NULL,
        create_user_in        IN sr_pos_pharm.create_user%TYPE DEFAULT NULL,
        create_time_in        IN sr_pos_pharm.create_time%TYPE DEFAULT NULL,
        create_institution_in IN sr_pos_pharm.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN sr_pos_pharm.update_user%TYPE DEFAULT NULL,
        update_time_in        IN sr_pos_pharm.update_time%TYPE DEFAULT NULL,
        update_institution_in IN sr_pos_pharm.update_institution%TYPE DEFAULT NULL,
        id_sr_pos_pharm_out   IN OUT sr_pos_pharm.id_sr_pos_pharm%TYPE
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_sr_pos_schedule_in IN sr_pos_pharm.id_sr_pos_schedule%TYPE DEFAULT NULL,
        flg_status_in         IN sr_pos_pharm.flg_status%TYPE DEFAULT NULL,
        notes_evaluation_in   IN sr_pos_pharm.notes_evaluation%TYPE DEFAULT NULL,
        id_cancel_reason_in   IN sr_pos_pharm.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in       IN sr_pos_pharm.notes_cancel%TYPE DEFAULT NULL,
        id_prof_reg_in        IN sr_pos_pharm.id_prof_reg%TYPE DEFAULT NULL,
        dt_reg_in             IN sr_pos_pharm.dt_reg%TYPE DEFAULT NULL,
        create_user_in        IN sr_pos_pharm.create_user%TYPE DEFAULT NULL,
        create_time_in        IN sr_pos_pharm.create_time%TYPE DEFAULT NULL,
        create_institution_in IN sr_pos_pharm.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN sr_pos_pharm.update_user%TYPE DEFAULT NULL,
        update_time_in        IN sr_pos_pharm.update_time%TYPE DEFAULT NULL,
        update_institution_in IN sr_pos_pharm.update_institution%TYPE DEFAULT NULL,
        id_sr_pos_pharm_out   IN OUT sr_pos_pharm.id_sr_pos_pharm%TYPE
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert one row with function, return generated primary key
    FUNCTION ins
    (
        id_sr_pos_schedule_in IN sr_pos_pharm.id_sr_pos_schedule%TYPE DEFAULT NULL,
        flg_status_in         IN sr_pos_pharm.flg_status%TYPE DEFAULT NULL,
        notes_evaluation_in   IN sr_pos_pharm.notes_evaluation%TYPE DEFAULT NULL,
        id_cancel_reason_in   IN sr_pos_pharm.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in       IN sr_pos_pharm.notes_cancel%TYPE DEFAULT NULL,
        id_prof_reg_in        IN sr_pos_pharm.id_prof_reg%TYPE DEFAULT NULL,
        dt_reg_in             IN sr_pos_pharm.dt_reg%TYPE DEFAULT NULL,
        create_user_in        IN sr_pos_pharm.create_user%TYPE DEFAULT NULL,
        create_time_in        IN sr_pos_pharm.create_time%TYPE DEFAULT NULL,
        create_institution_in IN sr_pos_pharm.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN sr_pos_pharm.update_user%TYPE DEFAULT NULL,
        update_time_in        IN sr_pos_pharm.update_time%TYPE DEFAULT NULL,
        update_institution_in IN sr_pos_pharm.update_institution%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) RETURN sr_pos_pharm.id_sr_pos_pharm%TYPE;

    FUNCTION ins
    (
        id_sr_pos_schedule_in IN sr_pos_pharm.id_sr_pos_schedule%TYPE DEFAULT NULL,
        flg_status_in         IN sr_pos_pharm.flg_status%TYPE DEFAULT NULL,
        notes_evaluation_in   IN sr_pos_pharm.notes_evaluation%TYPE DEFAULT NULL,
        id_cancel_reason_in   IN sr_pos_pharm.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in       IN sr_pos_pharm.notes_cancel%TYPE DEFAULT NULL,
        id_prof_reg_in        IN sr_pos_pharm.id_prof_reg%TYPE DEFAULT NULL,
        dt_reg_in             IN sr_pos_pharm.dt_reg%TYPE DEFAULT NULL,
        create_user_in        IN sr_pos_pharm.create_user%TYPE DEFAULT NULL,
        create_time_in        IN sr_pos_pharm.create_time%TYPE DEFAULT NULL,
        create_institution_in IN sr_pos_pharm.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN sr_pos_pharm.update_user%TYPE DEFAULT NULL,
        update_time_in        IN sr_pos_pharm.update_time%TYPE DEFAULT NULL,
        update_institution_in IN sr_pos_pharm.update_institution%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    ) RETURN sr_pos_pharm.id_sr_pos_pharm%TYPE;

    -- Update any/all columns by primary key. If you pass NULL, then
    -- the current column value is set to itself. If you need a more
    -- selected UPDATE then use one of the onecol procedures below.

    PROCEDURE upd
    (
        id_sr_pos_pharm_in     IN sr_pos_pharm.id_sr_pos_pharm%TYPE,
        id_sr_pos_schedule_in  IN sr_pos_pharm.id_sr_pos_schedule%TYPE DEFAULT NULL,
        id_sr_pos_schedule_nin IN BOOLEAN := TRUE,
        flg_status_in          IN sr_pos_pharm.flg_status%TYPE DEFAULT NULL,
        flg_status_nin         IN BOOLEAN := TRUE,
        notes_evaluation_in    IN sr_pos_pharm.notes_evaluation%TYPE DEFAULT NULL,
        notes_evaluation_nin   IN BOOLEAN := TRUE,
        id_cancel_reason_in    IN sr_pos_pharm.id_cancel_reason%TYPE DEFAULT NULL,
        id_cancel_reason_nin   IN BOOLEAN := TRUE,
        notes_cancel_in        IN sr_pos_pharm.notes_cancel%TYPE DEFAULT NULL,
        notes_cancel_nin       IN BOOLEAN := TRUE,
        id_prof_reg_in         IN sr_pos_pharm.id_prof_reg%TYPE DEFAULT NULL,
        id_prof_reg_nin        IN BOOLEAN := TRUE,
        dt_reg_in              IN sr_pos_pharm.dt_reg%TYPE DEFAULT NULL,
        dt_reg_nin             IN BOOLEAN := TRUE,
        create_user_in         IN sr_pos_pharm.create_user%TYPE DEFAULT NULL,
        create_user_nin        IN BOOLEAN := TRUE,
        create_time_in         IN sr_pos_pharm.create_time%TYPE DEFAULT NULL,
        create_time_nin        IN BOOLEAN := TRUE,
        create_institution_in  IN sr_pos_pharm.create_institution%TYPE DEFAULT NULL,
        create_institution_nin IN BOOLEAN := TRUE,
        update_user_in         IN sr_pos_pharm.update_user%TYPE DEFAULT NULL,
        update_user_nin        IN BOOLEAN := TRUE,
        update_time_in         IN sr_pos_pharm.update_time%TYPE DEFAULT NULL,
        update_time_nin        IN BOOLEAN := TRUE,
        update_institution_in  IN sr_pos_pharm.update_institution%TYPE DEFAULT NULL,
        update_institution_nin IN BOOLEAN := TRUE,
        handle_error_in        IN BOOLEAN := TRUE,
        rows_out               IN OUT table_varchar
    );

    PROCEDURE upd
    (
        id_sr_pos_pharm_in     IN sr_pos_pharm.id_sr_pos_pharm%TYPE,
        id_sr_pos_schedule_in  IN sr_pos_pharm.id_sr_pos_schedule%TYPE DEFAULT NULL,
        id_sr_pos_schedule_nin IN BOOLEAN := TRUE,
        flg_status_in          IN sr_pos_pharm.flg_status%TYPE DEFAULT NULL,
        flg_status_nin         IN BOOLEAN := TRUE,
        notes_evaluation_in    IN sr_pos_pharm.notes_evaluation%TYPE DEFAULT NULL,
        notes_evaluation_nin   IN BOOLEAN := TRUE,
        id_cancel_reason_in    IN sr_pos_pharm.id_cancel_reason%TYPE DEFAULT NULL,
        id_cancel_reason_nin   IN BOOLEAN := TRUE,
        notes_cancel_in        IN sr_pos_pharm.notes_cancel%TYPE DEFAULT NULL,
        notes_cancel_nin       IN BOOLEAN := TRUE,
        id_prof_reg_in         IN sr_pos_pharm.id_prof_reg%TYPE DEFAULT NULL,
        id_prof_reg_nin        IN BOOLEAN := TRUE,
        dt_reg_in              IN sr_pos_pharm.dt_reg%TYPE DEFAULT NULL,
        dt_reg_nin             IN BOOLEAN := TRUE,
        create_user_in         IN sr_pos_pharm.create_user%TYPE DEFAULT NULL,
        create_user_nin        IN BOOLEAN := TRUE,
        create_time_in         IN sr_pos_pharm.create_time%TYPE DEFAULT NULL,
        create_time_nin        IN BOOLEAN := TRUE,
        create_institution_in  IN sr_pos_pharm.create_institution%TYPE DEFAULT NULL,
        create_institution_nin IN BOOLEAN := TRUE,
        update_user_in         IN sr_pos_pharm.update_user%TYPE DEFAULT NULL,
        update_user_nin        IN BOOLEAN := TRUE,
        update_time_in         IN sr_pos_pharm.update_time%TYPE DEFAULT NULL,
        update_time_nin        IN BOOLEAN := TRUE,
        update_institution_in  IN sr_pos_pharm.update_institution%TYPE DEFAULT NULL,
        update_institution_nin IN BOOLEAN := TRUE,
        handle_error_in        IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        id_sr_pos_schedule_in  IN sr_pos_pharm.id_sr_pos_schedule%TYPE DEFAULT NULL,
        id_sr_pos_schedule_nin IN BOOLEAN := TRUE,
        flg_status_in          IN sr_pos_pharm.flg_status%TYPE DEFAULT NULL,
        flg_status_nin         IN BOOLEAN := TRUE,
        notes_evaluation_in    IN sr_pos_pharm.notes_evaluation%TYPE DEFAULT NULL,
        notes_evaluation_nin   IN BOOLEAN := TRUE,
        id_cancel_reason_in    IN sr_pos_pharm.id_cancel_reason%TYPE DEFAULT NULL,
        id_cancel_reason_nin   IN BOOLEAN := TRUE,
        notes_cancel_in        IN sr_pos_pharm.notes_cancel%TYPE DEFAULT NULL,
        notes_cancel_nin       IN BOOLEAN := TRUE,
        id_prof_reg_in         IN sr_pos_pharm.id_prof_reg%TYPE DEFAULT NULL,
        id_prof_reg_nin        IN BOOLEAN := TRUE,
        dt_reg_in              IN sr_pos_pharm.dt_reg%TYPE DEFAULT NULL,
        dt_reg_nin             IN BOOLEAN := TRUE,
        create_user_in         IN sr_pos_pharm.create_user%TYPE DEFAULT NULL,
        create_user_nin        IN BOOLEAN := TRUE,
        create_time_in         IN sr_pos_pharm.create_time%TYPE DEFAULT NULL,
        create_time_nin        IN BOOLEAN := TRUE,
        create_institution_in  IN sr_pos_pharm.create_institution%TYPE DEFAULT NULL,
        create_institution_nin IN BOOLEAN := TRUE,
        update_user_in         IN sr_pos_pharm.update_user%TYPE DEFAULT NULL,
        update_user_nin        IN BOOLEAN := TRUE,
        update_time_in         IN sr_pos_pharm.update_time%TYPE DEFAULT NULL,
        update_time_nin        IN BOOLEAN := TRUE,
        update_institution_in  IN sr_pos_pharm.update_institution%TYPE DEFAULT NULL,
        update_institution_nin IN BOOLEAN := TRUE,
        where_in               VARCHAR2 DEFAULT NULL,
        handle_error_in        IN BOOLEAN := TRUE,
        rows_out               IN OUT table_varchar
    );

    PROCEDURE upd
    (
        id_sr_pos_schedule_in  IN sr_pos_pharm.id_sr_pos_schedule%TYPE DEFAULT NULL,
        id_sr_pos_schedule_nin IN BOOLEAN := TRUE,
        flg_status_in          IN sr_pos_pharm.flg_status%TYPE DEFAULT NULL,
        flg_status_nin         IN BOOLEAN := TRUE,
        notes_evaluation_in    IN sr_pos_pharm.notes_evaluation%TYPE DEFAULT NULL,
        notes_evaluation_nin   IN BOOLEAN := TRUE,
        id_cancel_reason_in    IN sr_pos_pharm.id_cancel_reason%TYPE DEFAULT NULL,
        id_cancel_reason_nin   IN BOOLEAN := TRUE,
        notes_cancel_in        IN sr_pos_pharm.notes_cancel%TYPE DEFAULT NULL,
        notes_cancel_nin       IN BOOLEAN := TRUE,
        id_prof_reg_in         IN sr_pos_pharm.id_prof_reg%TYPE DEFAULT NULL,
        id_prof_reg_nin        IN BOOLEAN := TRUE,
        dt_reg_in              IN sr_pos_pharm.dt_reg%TYPE DEFAULT NULL,
        dt_reg_nin             IN BOOLEAN := TRUE,
        create_user_in         IN sr_pos_pharm.create_user%TYPE DEFAULT NULL,
        create_user_nin        IN BOOLEAN := TRUE,
        create_time_in         IN sr_pos_pharm.create_time%TYPE DEFAULT NULL,
        create_time_nin        IN BOOLEAN := TRUE,
        create_institution_in  IN sr_pos_pharm.create_institution%TYPE DEFAULT NULL,
        create_institution_nin IN BOOLEAN := TRUE,
        update_user_in         IN sr_pos_pharm.update_user%TYPE DEFAULT NULL,
        update_user_nin        IN BOOLEAN := TRUE,
        update_time_in         IN sr_pos_pharm.update_time%TYPE DEFAULT NULL,
        update_time_nin        IN BOOLEAN := TRUE,
        update_institution_in  IN sr_pos_pharm.update_institution%TYPE DEFAULT NULL,
        update_institution_nin IN BOOLEAN := TRUE,
        where_in               VARCHAR2 DEFAULT NULL,
        handle_error_in        IN BOOLEAN := TRUE
    );

    PROCEDURE upd_ins
    (
        id_sr_pos_pharm_in    IN sr_pos_pharm.id_sr_pos_pharm%TYPE,
        id_sr_pos_schedule_in IN sr_pos_pharm.id_sr_pos_schedule%TYPE DEFAULT NULL,
        flg_status_in         IN sr_pos_pharm.flg_status%TYPE DEFAULT NULL,
        notes_evaluation_in   IN sr_pos_pharm.notes_evaluation%TYPE DEFAULT NULL,
        id_cancel_reason_in   IN sr_pos_pharm.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in       IN sr_pos_pharm.notes_cancel%TYPE DEFAULT NULL,
        id_prof_reg_in        IN sr_pos_pharm.id_prof_reg%TYPE DEFAULT NULL,
        dt_reg_in             IN sr_pos_pharm.dt_reg%TYPE DEFAULT NULL,
        create_user_in        IN sr_pos_pharm.create_user%TYPE DEFAULT NULL,
        create_time_in        IN sr_pos_pharm.create_time%TYPE DEFAULT NULL,
        create_institution_in IN sr_pos_pharm.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN sr_pos_pharm.update_user%TYPE DEFAULT NULL,
        update_time_in        IN sr_pos_pharm.update_time%TYPE DEFAULT NULL,
        update_institution_in IN sr_pos_pharm.update_institution%TYPE DEFAULT NULL,
        handle_error_in       IN BOOLEAN := TRUE,
        rows_out              OUT table_varchar
    );

    PROCEDURE upd_ins
    (
        id_sr_pos_pharm_in    IN sr_pos_pharm.id_sr_pos_pharm%TYPE,
        id_sr_pos_schedule_in IN sr_pos_pharm.id_sr_pos_schedule%TYPE DEFAULT NULL,
        flg_status_in         IN sr_pos_pharm.flg_status%TYPE DEFAULT NULL,
        notes_evaluation_in   IN sr_pos_pharm.notes_evaluation%TYPE DEFAULT NULL,
        id_cancel_reason_in   IN sr_pos_pharm.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in       IN sr_pos_pharm.notes_cancel%TYPE DEFAULT NULL,
        id_prof_reg_in        IN sr_pos_pharm.id_prof_reg%TYPE DEFAULT NULL,
        dt_reg_in             IN sr_pos_pharm.dt_reg%TYPE DEFAULT NULL,
        create_user_in        IN sr_pos_pharm.create_user%TYPE DEFAULT NULL,
        create_time_in        IN sr_pos_pharm.create_time%TYPE DEFAULT NULL,
        create_institution_in IN sr_pos_pharm.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN sr_pos_pharm.update_user%TYPE DEFAULT NULL,
        update_time_in        IN sr_pos_pharm.update_time%TYPE DEFAULT NULL,
        update_institution_in IN sr_pos_pharm.update_institution%TYPE DEFAULT NULL,
        handle_error_in       IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        rec_in          IN sr_pos_pharm%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        IN OUT table_varchar
    );

    PROCEDURE upd
    (
        rec_in          IN sr_pos_pharm%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        col_in            IN sr_pos_pharm_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          IN OUT table_varchar
    );

    PROCEDURE upd
    (
        col_in            IN sr_pos_pharm_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE
    );

    -- Use Native Dynamic SQL increment a single NUMBER column
    -- for all rows specified by the dynamic WHERE clause
    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_sr_pos_pharm_in IN sr_pos_pharm.id_sr_pos_pharm%TYPE,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_sr_pos_pharm_in IN sr_pos_pharm.id_sr_pos_pharm%TYPE,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    -- Delete all rows for primary key column ID_SR_POS_PHARM
    PROCEDURE del_id_sr_pos_pharm
    (
        id_sr_pos_pharm_in IN sr_pos_pharm.id_sr_pos_pharm%TYPE,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete all rows for primary key column ID_SR_POS_PHARM
    PROCEDURE del_id_sr_pos_pharm
    (
        id_sr_pos_pharm_in IN sr_pos_pharm.id_sr_pos_pharm%TYPE,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    -- Delete all rows for this SPP_CR_FK foreign key value
    PROCEDURE del_spp_cr_fk
    (
        id_cancel_reason_in IN sr_pos_pharm.id_cancel_reason%TYPE,
        handle_error_in     IN BOOLEAN := TRUE
    );

    -- Delete all rows for this SPP_CR_FK foreign key value
    PROCEDURE del_spp_cr_fk
    (
        id_cancel_reason_in IN sr_pos_pharm.id_cancel_reason%TYPE,
        handle_error_in     IN BOOLEAN := TRUE,
        rows_out            OUT table_varchar
    );

    -- Delete all rows for this SPP_RGPRF_FK foreign key value
    PROCEDURE del_spp_rgprf_fk
    (
        id_prof_reg_in  IN sr_pos_pharm.id_prof_reg%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this SPP_RGPRF_FK foreign key value
    PROCEDURE del_spp_rgprf_fk
    (
        id_prof_reg_in  IN sr_pos_pharm.id_prof_reg%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for this SPP_SPSC_FK foreign key value
    PROCEDURE del_spp_spsc_fk
    (
        id_sr_pos_schedule_in IN sr_pos_pharm.id_sr_pos_schedule%TYPE,
        handle_error_in       IN BOOLEAN := TRUE
    );

    -- Delete all rows for this SPP_SPSC_FK foreign key value
    PROCEDURE del_spp_spsc_fk
    (
        id_sr_pos_schedule_in IN sr_pos_pharm.id_sr_pos_schedule%TYPE,
        handle_error_in       IN BOOLEAN := TRUE,
        rows_out              OUT table_varchar
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Initialize a record with default values for columns in the table.
    PROCEDURE initrec(sr_pos_pharm_inout IN OUT sr_pos_pharm%ROWTYPE);

    FUNCTION initrec RETURN sr_pos_pharm%ROWTYPE;

    FUNCTION get_data_rowid(rows_in IN table_varchar) RETURN sr_pos_pharm_tc;

    FUNCTION get_data_rowid_pat(rows_in IN table_varchar) RETURN sr_pos_pharm_tc;

END ts_sr_pos_pharm;
/
