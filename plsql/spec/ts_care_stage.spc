/*-- Last Change Revision: $Rev: 2029098 $*/
/*-- Last Change by: $Author: mario.fernandes $*/
/*-- Date of last change: $Date: 2022-08-02 18:49:46 +0100 (ter, 02 ago 2022) $*/

CREATE OR REPLACE PACKAGE ts_care_stage
/*
| Generated by or retrieved from QCGU - DO NOT MODIFY!
| QCGU - "Get it right, do it fast" - www.ToadWorld.com
| QCGU Universal ID: {1BD37A66-EA60-4927-9A64-F6DD89237236}
| Created On: Outubro 24, 2011 9:55:37
| Created By: ALERT
*/
 IS

    -- Collection of %ROWTYPE records based on "CARE_STAGE"
    TYPE care_stage_tc IS TABLE OF care_stage%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE care_stage_ntt IS TABLE OF care_stage%ROWTYPE;
    TYPE care_stage_vat IS VARRAY(100) OF care_stage%ROWTYPE;

    -- Same type structure, with a static name.
    TYPE aat IS TABLE OF care_stage%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE ntt IS TABLE OF care_stage%ROWTYPE;
    TYPE vat IS VARRAY(100) OF care_stage%ROWTYPE;

    -- Column Collection based on column "ID_CARE_STAGE"
    TYPE id_care_stage_cc IS TABLE OF care_stage.id_care_stage%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_EPISODE"
    TYPE id_episode_cc IS TABLE OF care_stage.id_episode%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "DT_SET"
    TYPE dt_set_cc IS TABLE OF care_stage.dt_set%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "FLG_STAGE"
    TYPE flg_stage_cc IS TABLE OF care_stage.flg_stage%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "FLG_ACTIVE"
    TYPE flg_active_cc IS TABLE OF care_stage.flg_active%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "DT_WARN"
    TYPE dt_warn_cc IS TABLE OF care_stage.dt_warn%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_USER"
    TYPE create_user_cc IS TABLE OF care_stage.create_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_TIME"
    TYPE create_time_cc IS TABLE OF care_stage.create_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_INSTITUTION"
    TYPE create_institution_cc IS TABLE OF care_stage.create_institution%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_USER"
    TYPE update_user_cc IS TABLE OF care_stage.update_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_TIME"
    TYPE update_time_cc IS TABLE OF care_stage.update_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_INSTITUTION"
    TYPE update_institution_cc IS TABLE OF care_stage.update_institution%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "FLG_INS_TYPE"
    TYPE flg_ins_type_cc IS TABLE OF care_stage.flg_ins_type%TYPE INDEX BY BINARY_INTEGER;

    -- Insert one row, providing primary key if present
    PROCEDURE ins
    (
        id_care_stage_in      IN care_stage.id_care_stage%TYPE,
        id_episode_in         IN care_stage.id_episode%TYPE DEFAULT NULL,
        dt_set_in             IN care_stage.dt_set%TYPE DEFAULT NULL,
        flg_stage_in          IN care_stage.flg_stage%TYPE DEFAULT NULL,
        flg_active_in         IN care_stage.flg_active%TYPE DEFAULT NULL,
        dt_warn_in            IN care_stage.dt_warn%TYPE DEFAULT NULL,
        create_user_in        IN care_stage.create_user%TYPE DEFAULT NULL,
        create_time_in        IN care_stage.create_time%TYPE DEFAULT NULL,
        create_institution_in IN care_stage.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN care_stage.update_user%TYPE DEFAULT NULL,
        update_time_in        IN care_stage.update_time%TYPE DEFAULT NULL,
        update_institution_in IN care_stage.update_institution%TYPE DEFAULT NULL,
        flg_ins_type_in       IN care_stage.flg_ins_type%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_care_stage_in      IN care_stage.id_care_stage%TYPE,
        id_episode_in         IN care_stage.id_episode%TYPE DEFAULT NULL,
        dt_set_in             IN care_stage.dt_set%TYPE DEFAULT NULL,
        flg_stage_in          IN care_stage.flg_stage%TYPE DEFAULT NULL,
        flg_active_in         IN care_stage.flg_active%TYPE DEFAULT NULL,
        dt_warn_in            IN care_stage.dt_warn%TYPE DEFAULT NULL,
        create_user_in        IN care_stage.create_user%TYPE DEFAULT NULL,
        create_time_in        IN care_stage.create_time%TYPE DEFAULT NULL,
        create_institution_in IN care_stage.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN care_stage.update_user%TYPE DEFAULT NULL,
        update_time_in        IN care_stage.update_time%TYPE DEFAULT NULL,
        update_institution_in IN care_stage.update_institution%TYPE DEFAULT NULL,
        flg_ins_type_in       IN care_stage.flg_ins_type%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a row based on a record.
    -- Specify whether or not a primary key value should be generated.
    PROCEDURE ins
    (
        rec_in          IN care_stage%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rec_in          IN care_stage%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a collection of rows using FORALL; all primary key values
    -- must have already been generated, or are handled in triggers.
    PROCEDURE ins
    (
        rows_in         IN care_stage_tc,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rows_in         IN care_stage_tc,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Return next primary key value using the named sequence.
    FUNCTION next_key(sequence_in IN VARCHAR2 := NULL) RETURN care_stage.id_care_stage%TYPE;

    -- Insert one row, generating hidden primary key using a sequence
    PROCEDURE ins
    (
        id_episode_in         IN care_stage.id_episode%TYPE DEFAULT NULL,
        dt_set_in             IN care_stage.dt_set%TYPE DEFAULT NULL,
        flg_stage_in          IN care_stage.flg_stage%TYPE DEFAULT NULL,
        flg_active_in         IN care_stage.flg_active%TYPE DEFAULT NULL,
        dt_warn_in            IN care_stage.dt_warn%TYPE DEFAULT NULL,
        create_user_in        IN care_stage.create_user%TYPE DEFAULT NULL,
        create_time_in        IN care_stage.create_time%TYPE DEFAULT NULL,
        create_institution_in IN care_stage.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN care_stage.update_user%TYPE DEFAULT NULL,
        update_time_in        IN care_stage.update_time%TYPE DEFAULT NULL,
        update_institution_in IN care_stage.update_institution%TYPE DEFAULT NULL,
        flg_ins_type_in       IN care_stage.flg_ins_type%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_episode_in         IN care_stage.id_episode%TYPE DEFAULT NULL,
        dt_set_in             IN care_stage.dt_set%TYPE DEFAULT NULL,
        flg_stage_in          IN care_stage.flg_stage%TYPE DEFAULT NULL,
        flg_active_in         IN care_stage.flg_active%TYPE DEFAULT NULL,
        dt_warn_in            IN care_stage.dt_warn%TYPE DEFAULT NULL,
        create_user_in        IN care_stage.create_user%TYPE DEFAULT NULL,
        create_time_in        IN care_stage.create_time%TYPE DEFAULT NULL,
        create_institution_in IN care_stage.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN care_stage.update_user%TYPE DEFAULT NULL,
        update_time_in        IN care_stage.update_time%TYPE DEFAULT NULL,
        update_institution_in IN care_stage.update_institution%TYPE DEFAULT NULL,
        flg_ins_type_in       IN care_stage.flg_ins_type%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert one row, returning primary key generated by sequence
    PROCEDURE ins
    (
        id_episode_in         IN care_stage.id_episode%TYPE DEFAULT NULL,
        dt_set_in             IN care_stage.dt_set%TYPE DEFAULT NULL,
        flg_stage_in          IN care_stage.flg_stage%TYPE DEFAULT NULL,
        flg_active_in         IN care_stage.flg_active%TYPE DEFAULT NULL,
        dt_warn_in            IN care_stage.dt_warn%TYPE DEFAULT NULL,
        create_user_in        IN care_stage.create_user%TYPE DEFAULT NULL,
        create_time_in        IN care_stage.create_time%TYPE DEFAULT NULL,
        create_institution_in IN care_stage.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN care_stage.update_user%TYPE DEFAULT NULL,
        update_time_in        IN care_stage.update_time%TYPE DEFAULT NULL,
        update_institution_in IN care_stage.update_institution%TYPE DEFAULT NULL,
        flg_ins_type_in       IN care_stage.flg_ins_type%TYPE DEFAULT NULL,
        id_care_stage_out     IN OUT care_stage.id_care_stage%TYPE
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_episode_in         IN care_stage.id_episode%TYPE DEFAULT NULL,
        dt_set_in             IN care_stage.dt_set%TYPE DEFAULT NULL,
        flg_stage_in          IN care_stage.flg_stage%TYPE DEFAULT NULL,
        flg_active_in         IN care_stage.flg_active%TYPE DEFAULT NULL,
        dt_warn_in            IN care_stage.dt_warn%TYPE DEFAULT NULL,
        create_user_in        IN care_stage.create_user%TYPE DEFAULT NULL,
        create_time_in        IN care_stage.create_time%TYPE DEFAULT NULL,
        create_institution_in IN care_stage.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN care_stage.update_user%TYPE DEFAULT NULL,
        update_time_in        IN care_stage.update_time%TYPE DEFAULT NULL,
        update_institution_in IN care_stage.update_institution%TYPE DEFAULT NULL,
        flg_ins_type_in       IN care_stage.flg_ins_type%TYPE DEFAULT NULL,
        id_care_stage_out     IN OUT care_stage.id_care_stage%TYPE
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert one row with function, return generated primary key
    FUNCTION ins
    (
        id_episode_in         IN care_stage.id_episode%TYPE DEFAULT NULL,
        dt_set_in             IN care_stage.dt_set%TYPE DEFAULT NULL,
        flg_stage_in          IN care_stage.flg_stage%TYPE DEFAULT NULL,
        flg_active_in         IN care_stage.flg_active%TYPE DEFAULT NULL,
        dt_warn_in            IN care_stage.dt_warn%TYPE DEFAULT NULL,
        create_user_in        IN care_stage.create_user%TYPE DEFAULT NULL,
        create_time_in        IN care_stage.create_time%TYPE DEFAULT NULL,
        create_institution_in IN care_stage.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN care_stage.update_user%TYPE DEFAULT NULL,
        update_time_in        IN care_stage.update_time%TYPE DEFAULT NULL,
        update_institution_in IN care_stage.update_institution%TYPE DEFAULT NULL,
        flg_ins_type_in       IN care_stage.flg_ins_type%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) RETURN care_stage.id_care_stage%TYPE;

    FUNCTION ins
    (
        id_episode_in         IN care_stage.id_episode%TYPE DEFAULT NULL,
        dt_set_in             IN care_stage.dt_set%TYPE DEFAULT NULL,
        flg_stage_in          IN care_stage.flg_stage%TYPE DEFAULT NULL,
        flg_active_in         IN care_stage.flg_active%TYPE DEFAULT NULL,
        dt_warn_in            IN care_stage.dt_warn%TYPE DEFAULT NULL,
        create_user_in        IN care_stage.create_user%TYPE DEFAULT NULL,
        create_time_in        IN care_stage.create_time%TYPE DEFAULT NULL,
        create_institution_in IN care_stage.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN care_stage.update_user%TYPE DEFAULT NULL,
        update_time_in        IN care_stage.update_time%TYPE DEFAULT NULL,
        update_institution_in IN care_stage.update_institution%TYPE DEFAULT NULL,
        flg_ins_type_in       IN care_stage.flg_ins_type%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    ) RETURN care_stage.id_care_stage%TYPE;

    -- Update any/all columns by primary key. If you pass NULL, then
    -- the current column value is set to itself. If you need a more
    -- selected UPDATE then use one of the onecol procedures below.

    PROCEDURE upd
    (
        id_care_stage_in       IN care_stage.id_care_stage%TYPE,
        id_episode_in          IN care_stage.id_episode%TYPE DEFAULT NULL,
        id_episode_nin         IN BOOLEAN := TRUE,
        dt_set_in              IN care_stage.dt_set%TYPE DEFAULT NULL,
        dt_set_nin             IN BOOLEAN := TRUE,
        flg_stage_in           IN care_stage.flg_stage%TYPE DEFAULT NULL,
        flg_stage_nin          IN BOOLEAN := TRUE,
        flg_active_in          IN care_stage.flg_active%TYPE DEFAULT NULL,
        flg_active_nin         IN BOOLEAN := TRUE,
        dt_warn_in             IN care_stage.dt_warn%TYPE DEFAULT NULL,
        dt_warn_nin            IN BOOLEAN := TRUE,
        create_user_in         IN care_stage.create_user%TYPE DEFAULT NULL,
        create_user_nin        IN BOOLEAN := TRUE,
        create_time_in         IN care_stage.create_time%TYPE DEFAULT NULL,
        create_time_nin        IN BOOLEAN := TRUE,
        create_institution_in  IN care_stage.create_institution%TYPE DEFAULT NULL,
        create_institution_nin IN BOOLEAN := TRUE,
        update_user_in         IN care_stage.update_user%TYPE DEFAULT NULL,
        update_user_nin        IN BOOLEAN := TRUE,
        update_time_in         IN care_stage.update_time%TYPE DEFAULT NULL,
        update_time_nin        IN BOOLEAN := TRUE,
        update_institution_in  IN care_stage.update_institution%TYPE DEFAULT NULL,
        update_institution_nin IN BOOLEAN := TRUE,
        flg_ins_type_in        IN care_stage.flg_ins_type%TYPE DEFAULT NULL,
        flg_ins_type_nin       IN BOOLEAN := TRUE,
        handle_error_in        IN BOOLEAN := TRUE,
        rows_out               IN OUT table_varchar
    );

    PROCEDURE upd
    (
        id_care_stage_in       IN care_stage.id_care_stage%TYPE,
        id_episode_in          IN care_stage.id_episode%TYPE DEFAULT NULL,
        id_episode_nin         IN BOOLEAN := TRUE,
        dt_set_in              IN care_stage.dt_set%TYPE DEFAULT NULL,
        dt_set_nin             IN BOOLEAN := TRUE,
        flg_stage_in           IN care_stage.flg_stage%TYPE DEFAULT NULL,
        flg_stage_nin          IN BOOLEAN := TRUE,
        flg_active_in          IN care_stage.flg_active%TYPE DEFAULT NULL,
        flg_active_nin         IN BOOLEAN := TRUE,
        dt_warn_in             IN care_stage.dt_warn%TYPE DEFAULT NULL,
        dt_warn_nin            IN BOOLEAN := TRUE,
        create_user_in         IN care_stage.create_user%TYPE DEFAULT NULL,
        create_user_nin        IN BOOLEAN := TRUE,
        create_time_in         IN care_stage.create_time%TYPE DEFAULT NULL,
        create_time_nin        IN BOOLEAN := TRUE,
        create_institution_in  IN care_stage.create_institution%TYPE DEFAULT NULL,
        create_institution_nin IN BOOLEAN := TRUE,
        update_user_in         IN care_stage.update_user%TYPE DEFAULT NULL,
        update_user_nin        IN BOOLEAN := TRUE,
        update_time_in         IN care_stage.update_time%TYPE DEFAULT NULL,
        update_time_nin        IN BOOLEAN := TRUE,
        update_institution_in  IN care_stage.update_institution%TYPE DEFAULT NULL,
        update_institution_nin IN BOOLEAN := TRUE,
        flg_ins_type_in        IN care_stage.flg_ins_type%TYPE DEFAULT NULL,
        flg_ins_type_nin       IN BOOLEAN := TRUE,
        handle_error_in        IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        id_episode_in          IN care_stage.id_episode%TYPE DEFAULT NULL,
        id_episode_nin         IN BOOLEAN := TRUE,
        dt_set_in              IN care_stage.dt_set%TYPE DEFAULT NULL,
        dt_set_nin             IN BOOLEAN := TRUE,
        flg_stage_in           IN care_stage.flg_stage%TYPE DEFAULT NULL,
        flg_stage_nin          IN BOOLEAN := TRUE,
        flg_active_in          IN care_stage.flg_active%TYPE DEFAULT NULL,
        flg_active_nin         IN BOOLEAN := TRUE,
        dt_warn_in             IN care_stage.dt_warn%TYPE DEFAULT NULL,
        dt_warn_nin            IN BOOLEAN := TRUE,
        create_user_in         IN care_stage.create_user%TYPE DEFAULT NULL,
        create_user_nin        IN BOOLEAN := TRUE,
        create_time_in         IN care_stage.create_time%TYPE DEFAULT NULL,
        create_time_nin        IN BOOLEAN := TRUE,
        create_institution_in  IN care_stage.create_institution%TYPE DEFAULT NULL,
        create_institution_nin IN BOOLEAN := TRUE,
        update_user_in         IN care_stage.update_user%TYPE DEFAULT NULL,
        update_user_nin        IN BOOLEAN := TRUE,
        update_time_in         IN care_stage.update_time%TYPE DEFAULT NULL,
        update_time_nin        IN BOOLEAN := TRUE,
        update_institution_in  IN care_stage.update_institution%TYPE DEFAULT NULL,
        update_institution_nin IN BOOLEAN := TRUE,
        flg_ins_type_in        IN care_stage.flg_ins_type%TYPE DEFAULT NULL,
        flg_ins_type_nin       IN BOOLEAN := TRUE,
        where_in               VARCHAR2,
        handle_error_in        IN BOOLEAN := TRUE,
        rows_out               IN OUT table_varchar
    );

    PROCEDURE upd
    (
        id_episode_in          IN care_stage.id_episode%TYPE DEFAULT NULL,
        id_episode_nin         IN BOOLEAN := TRUE,
        dt_set_in              IN care_stage.dt_set%TYPE DEFAULT NULL,
        dt_set_nin             IN BOOLEAN := TRUE,
        flg_stage_in           IN care_stage.flg_stage%TYPE DEFAULT NULL,
        flg_stage_nin          IN BOOLEAN := TRUE,
        flg_active_in          IN care_stage.flg_active%TYPE DEFAULT NULL,
        flg_active_nin         IN BOOLEAN := TRUE,
        dt_warn_in             IN care_stage.dt_warn%TYPE DEFAULT NULL,
        dt_warn_nin            IN BOOLEAN := TRUE,
        create_user_in         IN care_stage.create_user%TYPE DEFAULT NULL,
        create_user_nin        IN BOOLEAN := TRUE,
        create_time_in         IN care_stage.create_time%TYPE DEFAULT NULL,
        create_time_nin        IN BOOLEAN := TRUE,
        create_institution_in  IN care_stage.create_institution%TYPE DEFAULT NULL,
        create_institution_nin IN BOOLEAN := TRUE,
        update_user_in         IN care_stage.update_user%TYPE DEFAULT NULL,
        update_user_nin        IN BOOLEAN := TRUE,
        update_time_in         IN care_stage.update_time%TYPE DEFAULT NULL,
        update_time_nin        IN BOOLEAN := TRUE,
        update_institution_in  IN care_stage.update_institution%TYPE DEFAULT NULL,
        update_institution_nin IN BOOLEAN := TRUE,
        flg_ins_type_in        IN care_stage.flg_ins_type%TYPE DEFAULT NULL,
        flg_ins_type_nin       IN BOOLEAN := TRUE,
        where_in               VARCHAR2,
        handle_error_in        IN BOOLEAN := TRUE
    );

    PROCEDURE upd_ins
    (
        id_care_stage_in      IN care_stage.id_care_stage%TYPE,
        id_episode_in         IN care_stage.id_episode%TYPE DEFAULT NULL,
        dt_set_in             IN care_stage.dt_set%TYPE DEFAULT NULL,
        flg_stage_in          IN care_stage.flg_stage%TYPE DEFAULT NULL,
        flg_active_in         IN care_stage.flg_active%TYPE DEFAULT NULL,
        dt_warn_in            IN care_stage.dt_warn%TYPE DEFAULT NULL,
        create_user_in        IN care_stage.create_user%TYPE DEFAULT NULL,
        create_time_in        IN care_stage.create_time%TYPE DEFAULT NULL,
        create_institution_in IN care_stage.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN care_stage.update_user%TYPE DEFAULT NULL,
        update_time_in        IN care_stage.update_time%TYPE DEFAULT NULL,
        update_institution_in IN care_stage.update_institution%TYPE DEFAULT NULL,
        flg_ins_type_in       IN care_stage.flg_ins_type%TYPE DEFAULT NULL,
        handle_error_in       IN BOOLEAN := TRUE,
        rows_out              OUT table_varchar
    );

    PROCEDURE upd_ins
    (
        id_care_stage_in      IN care_stage.id_care_stage%TYPE,
        id_episode_in         IN care_stage.id_episode%TYPE DEFAULT NULL,
        dt_set_in             IN care_stage.dt_set%TYPE DEFAULT NULL,
        flg_stage_in          IN care_stage.flg_stage%TYPE DEFAULT NULL,
        flg_active_in         IN care_stage.flg_active%TYPE DEFAULT NULL,
        dt_warn_in            IN care_stage.dt_warn%TYPE DEFAULT NULL,
        create_user_in        IN care_stage.create_user%TYPE DEFAULT NULL,
        create_time_in        IN care_stage.create_time%TYPE DEFAULT NULL,
        create_institution_in IN care_stage.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN care_stage.update_user%TYPE DEFAULT NULL,
        update_time_in        IN care_stage.update_time%TYPE DEFAULT NULL,
        update_institution_in IN care_stage.update_institution%TYPE DEFAULT NULL,
        flg_ins_type_in       IN care_stage.flg_ins_type%TYPE DEFAULT NULL,
        handle_error_in       IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        rec_in          IN care_stage%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        IN OUT table_varchar
    );

    PROCEDURE upd
    (
        rec_in          IN care_stage%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        col_in            IN care_stage_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          IN OUT table_varchar
    );

    PROCEDURE upd
    (
        col_in            IN care_stage_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE
    );

    -- Use Native Dynamic SQL increment a single NUMBER column
    -- for all rows specified by the dynamic WHERE clause
    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_care_stage_in IN care_stage.id_care_stage%TYPE,
        handle_error_in  IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_care_stage_in IN care_stage.id_care_stage%TYPE,
        handle_error_in  IN BOOLEAN := TRUE,
        rows_out         OUT table_varchar
    );

    -- Delete all rows for primary key column ID_CARE_STAGE
    PROCEDURE del_id_care_stage
    (
        id_care_stage_in IN care_stage.id_care_stage%TYPE,
        handle_error_in  IN BOOLEAN := TRUE
    );

    -- Delete all rows for primary key column ID_CARE_STAGE
    PROCEDURE del_id_care_stage
    (
        id_care_stage_in IN care_stage.id_care_stage%TYPE,
        handle_error_in  IN BOOLEAN := TRUE,
        rows_out         OUT table_varchar
    );

    -- Delete all rows for this CS_EPIS_FK foreign key value
    PROCEDURE del_cs_epis_fk
    (
        id_episode_in   IN care_stage.id_episode%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this CS_EPIS_FK foreign key value
    PROCEDURE del_cs_epis_fk
    (
        id_episode_in   IN care_stage.id_episode%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for this CS_PROF_FK foreign key value
    PROCEDURE del_cs_prof_fk
    (
        create_user_in  IN care_stage.create_user%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this CS_PROF_FK foreign key value
    PROCEDURE del_cs_prof_fk
    (
        create_user_in  IN care_stage.create_user%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Initialize a record with default values for columns in the table.
    PROCEDURE initrec(care_stage_inout IN OUT care_stage%ROWTYPE);

    FUNCTION initrec RETURN care_stage%ROWTYPE;

    FUNCTION get_data_rowid(rows_in IN table_varchar) RETURN care_stage_tc;

    FUNCTION get_data_rowid_pat(rows_in IN table_varchar) RETURN care_stage_tc;

END ts_care_stage;
/
