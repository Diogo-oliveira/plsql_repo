/*-- Last Change Revision: $Rev: 2029118 $*/
/*-- Last Change by: $Author: mario.fernandes $*/
/*-- Date of last change: $Date: 2022-08-02 18:49:52 +0100 (ter, 02 ago 2022) $*/
CREATE OR REPLACE PACKAGE ts_drug_prescription
/*
| Generated by or retrieved from QCGU - DO NOT MODIFY!
| QCGU - "Get it right, do it fast" - www.ToadWorld.com
| QCGU Universal ID: {1BD37A66-EA60-4927-9A64-F6DD89237236}
| Created On: Maio 20, 2014 20:27:43
| Created By: ALERT
*/
 IS

    -- Collection of %ROWTYPE records based on "DRUG_PRESCRIPTION"
    TYPE drug_prescription_tc IS TABLE OF drug_prescription%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE drug_prescription_ntt IS TABLE OF drug_prescription%ROWTYPE;
    TYPE drug_prescription_vat IS VARRAY(100) OF drug_prescription%ROWTYPE;

    -- Same type structure, with a static name.
    TYPE aat IS TABLE OF drug_prescription%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE ntt IS TABLE OF drug_prescription%ROWTYPE;
    TYPE vat IS VARRAY(100) OF drug_prescription%ROWTYPE;

    -- Column Collection based on column "ID_DRUG_PRESCRIPTION"
    TYPE id_drug_prescription_cc IS TABLE OF drug_prescription.id_drug_prescription%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_EPISODE"
    TYPE id_episode_cc IS TABLE OF drug_prescription.id_episode%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_PROFESSIONAL"
    TYPE id_professional_cc IS TABLE OF drug_prescription.id_professional%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "FLG_TYPE"
    TYPE flg_type_cc IS TABLE OF drug_prescription.flg_type%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "BARCODE"
    TYPE barcode_cc IS TABLE OF drug_prescription.barcode%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "NUM_DAYS_EXPIRE"
    TYPE num_days_expire_cc IS TABLE OF drug_prescription.num_days_expire%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "FLG_TIME"
    TYPE flg_time_cc IS TABLE OF drug_prescription.flg_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "FLG_STATUS"
    TYPE flg_status_cc IS TABLE OF drug_prescription.flg_status%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_PROF_CANCEL"
    TYPE id_prof_cancel_cc IS TABLE OF drug_prescription.id_prof_cancel%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "NOTES_CANCEL"
    TYPE notes_cancel_cc IS TABLE OF drug_prescription.notes_cancel%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_EPISODE_ORIGIN"
    TYPE id_episode_origin_cc IS TABLE OF drug_prescription.id_episode_origin%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_EPISODE_DESTINATION"
    TYPE id_episode_destination_cc IS TABLE OF drug_prescription.id_episode_destination%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_PROTOCOLS"
    TYPE id_protocols_cc IS TABLE OF drug_prescription.id_protocols%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_PREV_EPISODE"
    TYPE id_prev_episode_cc IS TABLE OF drug_prescription.id_prev_episode%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "DT_DRUG_PRESCRIPTION_TSTZ"
    TYPE dt_drug_prescription_tstz_cc IS TABLE OF drug_prescription.dt_drug_prescription_tstz%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "DT_BEGIN_TSTZ"
    TYPE dt_begin_tstz_cc IS TABLE OF drug_prescription.dt_begin_tstz%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "DT_CANCEL_TSTZ"
    TYPE dt_cancel_tstz_cc IS TABLE OF drug_prescription.dt_cancel_tstz%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_PATIENT"
    TYPE id_patient_cc IS TABLE OF drug_prescription.id_patient%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_USER"
    TYPE create_user_cc IS TABLE OF drug_prescription.create_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_TIME"
    TYPE create_time_cc IS TABLE OF drug_prescription.create_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_INSTITUTION"
    TYPE create_institution_cc IS TABLE OF drug_prescription.create_institution%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_USER"
    TYPE update_user_cc IS TABLE OF drug_prescription.update_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_TIME"
    TYPE update_time_cc IS TABLE OF drug_prescription.update_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_INSTITUTION"
    TYPE update_institution_cc IS TABLE OF drug_prescription.update_institution%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_PARENT"
    TYPE id_parent_cc IS TABLE OF drug_prescription.id_parent%TYPE INDEX BY BINARY_INTEGER;

    -- Insert one row, providing primary key if present
    PROCEDURE ins
    (
        id_drug_prescription_in      IN drug_prescription.id_drug_prescription%TYPE,
        id_episode_in                IN drug_prescription.id_episode%TYPE DEFAULT NULL,
        id_professional_in           IN drug_prescription.id_professional%TYPE DEFAULT NULL,
        flg_type_in                  IN drug_prescription.flg_type%TYPE DEFAULT NULL,
        barcode_in                   IN drug_prescription.barcode%TYPE DEFAULT NULL,
        num_days_expire_in           IN drug_prescription.num_days_expire%TYPE DEFAULT NULL,
        flg_time_in                  IN drug_prescription.flg_time%TYPE DEFAULT NULL,
        flg_status_in                IN drug_prescription.flg_status%TYPE DEFAULT NULL,
        id_prof_cancel_in            IN drug_prescription.id_prof_cancel%TYPE DEFAULT NULL,
        notes_cancel_in              IN drug_prescription.notes_cancel%TYPE DEFAULT NULL,
        id_episode_origin_in         IN drug_prescription.id_episode_origin%TYPE DEFAULT NULL,
        id_episode_destination_in    IN drug_prescription.id_episode_destination%TYPE DEFAULT NULL,
        id_protocols_in              IN drug_prescription.id_protocols%TYPE DEFAULT NULL,
        id_prev_episode_in           IN drug_prescription.id_prev_episode%TYPE DEFAULT NULL,
        dt_drug_prescription_tstz_in IN drug_prescription.dt_drug_prescription_tstz%TYPE DEFAULT NULL,
        dt_begin_tstz_in             IN drug_prescription.dt_begin_tstz%TYPE DEFAULT NULL,
        dt_cancel_tstz_in            IN drug_prescription.dt_cancel_tstz%TYPE DEFAULT NULL,
        id_patient_in                IN drug_prescription.id_patient%TYPE DEFAULT NULL,
        create_user_in               IN drug_prescription.create_user%TYPE DEFAULT NULL,
        create_time_in               IN drug_prescription.create_time%TYPE DEFAULT NULL,
        create_institution_in        IN drug_prescription.create_institution%TYPE DEFAULT NULL,
        update_user_in               IN drug_prescription.update_user%TYPE DEFAULT NULL,
        update_time_in               IN drug_prescription.update_time%TYPE DEFAULT NULL,
        update_institution_in        IN drug_prescription.update_institution%TYPE DEFAULT NULL,
        id_parent_in                 IN drug_prescription.id_parent%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_drug_prescription_in      IN drug_prescription.id_drug_prescription%TYPE,
        id_episode_in                IN drug_prescription.id_episode%TYPE DEFAULT NULL,
        id_professional_in           IN drug_prescription.id_professional%TYPE DEFAULT NULL,
        flg_type_in                  IN drug_prescription.flg_type%TYPE DEFAULT NULL,
        barcode_in                   IN drug_prescription.barcode%TYPE DEFAULT NULL,
        num_days_expire_in           IN drug_prescription.num_days_expire%TYPE DEFAULT NULL,
        flg_time_in                  IN drug_prescription.flg_time%TYPE DEFAULT NULL,
        flg_status_in                IN drug_prescription.flg_status%TYPE DEFAULT NULL,
        id_prof_cancel_in            IN drug_prescription.id_prof_cancel%TYPE DEFAULT NULL,
        notes_cancel_in              IN drug_prescription.notes_cancel%TYPE DEFAULT NULL,
        id_episode_origin_in         IN drug_prescription.id_episode_origin%TYPE DEFAULT NULL,
        id_episode_destination_in    IN drug_prescription.id_episode_destination%TYPE DEFAULT NULL,
        id_protocols_in              IN drug_prescription.id_protocols%TYPE DEFAULT NULL,
        id_prev_episode_in           IN drug_prescription.id_prev_episode%TYPE DEFAULT NULL,
        dt_drug_prescription_tstz_in IN drug_prescription.dt_drug_prescription_tstz%TYPE DEFAULT NULL,
        dt_begin_tstz_in             IN drug_prescription.dt_begin_tstz%TYPE DEFAULT NULL,
        dt_cancel_tstz_in            IN drug_prescription.dt_cancel_tstz%TYPE DEFAULT NULL,
        id_patient_in                IN drug_prescription.id_patient%TYPE DEFAULT NULL,
        create_user_in               IN drug_prescription.create_user%TYPE DEFAULT NULL,
        create_time_in               IN drug_prescription.create_time%TYPE DEFAULT NULL,
        create_institution_in        IN drug_prescription.create_institution%TYPE DEFAULT NULL,
        update_user_in               IN drug_prescription.update_user%TYPE DEFAULT NULL,
        update_time_in               IN drug_prescription.update_time%TYPE DEFAULT NULL,
        update_institution_in        IN drug_prescription.update_institution%TYPE DEFAULT NULL,
        id_parent_in                 IN drug_prescription.id_parent%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a row based on a record.
    -- Specify whether or not a primary key value should be generated.
    PROCEDURE ins
    (
        rec_in          IN drug_prescription%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rec_in          IN drug_prescription%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a collection of rows using FORALL; all primary key values
    -- must have already been generated, or are handled in triggers.
    PROCEDURE ins
    (
        rows_in         IN drug_prescription_tc,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rows_in         IN drug_prescription_tc,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Return next primary key value using the named sequence.
    FUNCTION next_key(sequence_in IN VARCHAR2 := NULL) RETURN drug_prescription.id_drug_prescription%TYPE;

    -- Insert one row, generating hidden primary key using a sequence
    PROCEDURE ins
    (
        id_episode_in                IN drug_prescription.id_episode%TYPE DEFAULT NULL,
        id_professional_in           IN drug_prescription.id_professional%TYPE DEFAULT NULL,
        flg_type_in                  IN drug_prescription.flg_type%TYPE DEFAULT NULL,
        barcode_in                   IN drug_prescription.barcode%TYPE DEFAULT NULL,
        num_days_expire_in           IN drug_prescription.num_days_expire%TYPE DEFAULT NULL,
        flg_time_in                  IN drug_prescription.flg_time%TYPE DEFAULT NULL,
        flg_status_in                IN drug_prescription.flg_status%TYPE DEFAULT NULL,
        id_prof_cancel_in            IN drug_prescription.id_prof_cancel%TYPE DEFAULT NULL,
        notes_cancel_in              IN drug_prescription.notes_cancel%TYPE DEFAULT NULL,
        id_episode_origin_in         IN drug_prescription.id_episode_origin%TYPE DEFAULT NULL,
        id_episode_destination_in    IN drug_prescription.id_episode_destination%TYPE DEFAULT NULL,
        id_protocols_in              IN drug_prescription.id_protocols%TYPE DEFAULT NULL,
        id_prev_episode_in           IN drug_prescription.id_prev_episode%TYPE DEFAULT NULL,
        dt_drug_prescription_tstz_in IN drug_prescription.dt_drug_prescription_tstz%TYPE DEFAULT NULL,
        dt_begin_tstz_in             IN drug_prescription.dt_begin_tstz%TYPE DEFAULT NULL,
        dt_cancel_tstz_in            IN drug_prescription.dt_cancel_tstz%TYPE DEFAULT NULL,
        id_patient_in                IN drug_prescription.id_patient%TYPE DEFAULT NULL,
        create_user_in               IN drug_prescription.create_user%TYPE DEFAULT NULL,
        create_time_in               IN drug_prescription.create_time%TYPE DEFAULT NULL,
        create_institution_in        IN drug_prescription.create_institution%TYPE DEFAULT NULL,
        update_user_in               IN drug_prescription.update_user%TYPE DEFAULT NULL,
        update_time_in               IN drug_prescription.update_time%TYPE DEFAULT NULL,
        update_institution_in        IN drug_prescription.update_institution%TYPE DEFAULT NULL,
        id_parent_in                 IN drug_prescription.id_parent%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_episode_in                IN drug_prescription.id_episode%TYPE DEFAULT NULL,
        id_professional_in           IN drug_prescription.id_professional%TYPE DEFAULT NULL,
        flg_type_in                  IN drug_prescription.flg_type%TYPE DEFAULT NULL,
        barcode_in                   IN drug_prescription.barcode%TYPE DEFAULT NULL,
        num_days_expire_in           IN drug_prescription.num_days_expire%TYPE DEFAULT NULL,
        flg_time_in                  IN drug_prescription.flg_time%TYPE DEFAULT NULL,
        flg_status_in                IN drug_prescription.flg_status%TYPE DEFAULT NULL,
        id_prof_cancel_in            IN drug_prescription.id_prof_cancel%TYPE DEFAULT NULL,
        notes_cancel_in              IN drug_prescription.notes_cancel%TYPE DEFAULT NULL,
        id_episode_origin_in         IN drug_prescription.id_episode_origin%TYPE DEFAULT NULL,
        id_episode_destination_in    IN drug_prescription.id_episode_destination%TYPE DEFAULT NULL,
        id_protocols_in              IN drug_prescription.id_protocols%TYPE DEFAULT NULL,
        id_prev_episode_in           IN drug_prescription.id_prev_episode%TYPE DEFAULT NULL,
        dt_drug_prescription_tstz_in IN drug_prescription.dt_drug_prescription_tstz%TYPE DEFAULT NULL,
        dt_begin_tstz_in             IN drug_prescription.dt_begin_tstz%TYPE DEFAULT NULL,
        dt_cancel_tstz_in            IN drug_prescription.dt_cancel_tstz%TYPE DEFAULT NULL,
        id_patient_in                IN drug_prescription.id_patient%TYPE DEFAULT NULL,
        create_user_in               IN drug_prescription.create_user%TYPE DEFAULT NULL,
        create_time_in               IN drug_prescription.create_time%TYPE DEFAULT NULL,
        create_institution_in        IN drug_prescription.create_institution%TYPE DEFAULT NULL,
        update_user_in               IN drug_prescription.update_user%TYPE DEFAULT NULL,
        update_time_in               IN drug_prescription.update_time%TYPE DEFAULT NULL,
        update_institution_in        IN drug_prescription.update_institution%TYPE DEFAULT NULL,
        id_parent_in                 IN drug_prescription.id_parent%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert one row, returning primary key generated by sequence
    PROCEDURE ins
    (
        id_episode_in                IN drug_prescription.id_episode%TYPE DEFAULT NULL,
        id_professional_in           IN drug_prescription.id_professional%TYPE DEFAULT NULL,
        flg_type_in                  IN drug_prescription.flg_type%TYPE DEFAULT NULL,
        barcode_in                   IN drug_prescription.barcode%TYPE DEFAULT NULL,
        num_days_expire_in           IN drug_prescription.num_days_expire%TYPE DEFAULT NULL,
        flg_time_in                  IN drug_prescription.flg_time%TYPE DEFAULT NULL,
        flg_status_in                IN drug_prescription.flg_status%TYPE DEFAULT NULL,
        id_prof_cancel_in            IN drug_prescription.id_prof_cancel%TYPE DEFAULT NULL,
        notes_cancel_in              IN drug_prescription.notes_cancel%TYPE DEFAULT NULL,
        id_episode_origin_in         IN drug_prescription.id_episode_origin%TYPE DEFAULT NULL,
        id_episode_destination_in    IN drug_prescription.id_episode_destination%TYPE DEFAULT NULL,
        id_protocols_in              IN drug_prescription.id_protocols%TYPE DEFAULT NULL,
        id_prev_episode_in           IN drug_prescription.id_prev_episode%TYPE DEFAULT NULL,
        dt_drug_prescription_tstz_in IN drug_prescription.dt_drug_prescription_tstz%TYPE DEFAULT NULL,
        dt_begin_tstz_in             IN drug_prescription.dt_begin_tstz%TYPE DEFAULT NULL,
        dt_cancel_tstz_in            IN drug_prescription.dt_cancel_tstz%TYPE DEFAULT NULL,
        id_patient_in                IN drug_prescription.id_patient%TYPE DEFAULT NULL,
        create_user_in               IN drug_prescription.create_user%TYPE DEFAULT NULL,
        create_time_in               IN drug_prescription.create_time%TYPE DEFAULT NULL,
        create_institution_in        IN drug_prescription.create_institution%TYPE DEFAULT NULL,
        update_user_in               IN drug_prescription.update_user%TYPE DEFAULT NULL,
        update_time_in               IN drug_prescription.update_time%TYPE DEFAULT NULL,
        update_institution_in        IN drug_prescription.update_institution%TYPE DEFAULT NULL,
        id_parent_in                 IN drug_prescription.id_parent%TYPE DEFAULT NULL,
        id_drug_prescription_out     IN OUT drug_prescription.id_drug_prescription%TYPE
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_episode_in                IN drug_prescription.id_episode%TYPE DEFAULT NULL,
        id_professional_in           IN drug_prescription.id_professional%TYPE DEFAULT NULL,
        flg_type_in                  IN drug_prescription.flg_type%TYPE DEFAULT NULL,
        barcode_in                   IN drug_prescription.barcode%TYPE DEFAULT NULL,
        num_days_expire_in           IN drug_prescription.num_days_expire%TYPE DEFAULT NULL,
        flg_time_in                  IN drug_prescription.flg_time%TYPE DEFAULT NULL,
        flg_status_in                IN drug_prescription.flg_status%TYPE DEFAULT NULL,
        id_prof_cancel_in            IN drug_prescription.id_prof_cancel%TYPE DEFAULT NULL,
        notes_cancel_in              IN drug_prescription.notes_cancel%TYPE DEFAULT NULL,
        id_episode_origin_in         IN drug_prescription.id_episode_origin%TYPE DEFAULT NULL,
        id_episode_destination_in    IN drug_prescription.id_episode_destination%TYPE DEFAULT NULL,
        id_protocols_in              IN drug_prescription.id_protocols%TYPE DEFAULT NULL,
        id_prev_episode_in           IN drug_prescription.id_prev_episode%TYPE DEFAULT NULL,
        dt_drug_prescription_tstz_in IN drug_prescription.dt_drug_prescription_tstz%TYPE DEFAULT NULL,
        dt_begin_tstz_in             IN drug_prescription.dt_begin_tstz%TYPE DEFAULT NULL,
        dt_cancel_tstz_in            IN drug_prescription.dt_cancel_tstz%TYPE DEFAULT NULL,
        id_patient_in                IN drug_prescription.id_patient%TYPE DEFAULT NULL,
        create_user_in               IN drug_prescription.create_user%TYPE DEFAULT NULL,
        create_time_in               IN drug_prescription.create_time%TYPE DEFAULT NULL,
        create_institution_in        IN drug_prescription.create_institution%TYPE DEFAULT NULL,
        update_user_in               IN drug_prescription.update_user%TYPE DEFAULT NULL,
        update_time_in               IN drug_prescription.update_time%TYPE DEFAULT NULL,
        update_institution_in        IN drug_prescription.update_institution%TYPE DEFAULT NULL,
        id_parent_in                 IN drug_prescription.id_parent%TYPE DEFAULT NULL,
        id_drug_prescription_out     IN OUT drug_prescription.id_drug_prescription%TYPE
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert one row with function, return generated primary key
    FUNCTION ins
    (
        id_episode_in                IN drug_prescription.id_episode%TYPE DEFAULT NULL,
        id_professional_in           IN drug_prescription.id_professional%TYPE DEFAULT NULL,
        flg_type_in                  IN drug_prescription.flg_type%TYPE DEFAULT NULL,
        barcode_in                   IN drug_prescription.barcode%TYPE DEFAULT NULL,
        num_days_expire_in           IN drug_prescription.num_days_expire%TYPE DEFAULT NULL,
        flg_time_in                  IN drug_prescription.flg_time%TYPE DEFAULT NULL,
        flg_status_in                IN drug_prescription.flg_status%TYPE DEFAULT NULL,
        id_prof_cancel_in            IN drug_prescription.id_prof_cancel%TYPE DEFAULT NULL,
        notes_cancel_in              IN drug_prescription.notes_cancel%TYPE DEFAULT NULL,
        id_episode_origin_in         IN drug_prescription.id_episode_origin%TYPE DEFAULT NULL,
        id_episode_destination_in    IN drug_prescription.id_episode_destination%TYPE DEFAULT NULL,
        id_protocols_in              IN drug_prescription.id_protocols%TYPE DEFAULT NULL,
        id_prev_episode_in           IN drug_prescription.id_prev_episode%TYPE DEFAULT NULL,
        dt_drug_prescription_tstz_in IN drug_prescription.dt_drug_prescription_tstz%TYPE DEFAULT NULL,
        dt_begin_tstz_in             IN drug_prescription.dt_begin_tstz%TYPE DEFAULT NULL,
        dt_cancel_tstz_in            IN drug_prescription.dt_cancel_tstz%TYPE DEFAULT NULL,
        id_patient_in                IN drug_prescription.id_patient%TYPE DEFAULT NULL,
        create_user_in               IN drug_prescription.create_user%TYPE DEFAULT NULL,
        create_time_in               IN drug_prescription.create_time%TYPE DEFAULT NULL,
        create_institution_in        IN drug_prescription.create_institution%TYPE DEFAULT NULL,
        update_user_in               IN drug_prescription.update_user%TYPE DEFAULT NULL,
        update_time_in               IN drug_prescription.update_time%TYPE DEFAULT NULL,
        update_institution_in        IN drug_prescription.update_institution%TYPE DEFAULT NULL,
        id_parent_in                 IN drug_prescription.id_parent%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) RETURN drug_prescription.id_drug_prescription%TYPE;

    FUNCTION ins
    (
        id_episode_in                IN drug_prescription.id_episode%TYPE DEFAULT NULL,
        id_professional_in           IN drug_prescription.id_professional%TYPE DEFAULT NULL,
        flg_type_in                  IN drug_prescription.flg_type%TYPE DEFAULT NULL,
        barcode_in                   IN drug_prescription.barcode%TYPE DEFAULT NULL,
        num_days_expire_in           IN drug_prescription.num_days_expire%TYPE DEFAULT NULL,
        flg_time_in                  IN drug_prescription.flg_time%TYPE DEFAULT NULL,
        flg_status_in                IN drug_prescription.flg_status%TYPE DEFAULT NULL,
        id_prof_cancel_in            IN drug_prescription.id_prof_cancel%TYPE DEFAULT NULL,
        notes_cancel_in              IN drug_prescription.notes_cancel%TYPE DEFAULT NULL,
        id_episode_origin_in         IN drug_prescription.id_episode_origin%TYPE DEFAULT NULL,
        id_episode_destination_in    IN drug_prescription.id_episode_destination%TYPE DEFAULT NULL,
        id_protocols_in              IN drug_prescription.id_protocols%TYPE DEFAULT NULL,
        id_prev_episode_in           IN drug_prescription.id_prev_episode%TYPE DEFAULT NULL,
        dt_drug_prescription_tstz_in IN drug_prescription.dt_drug_prescription_tstz%TYPE DEFAULT NULL,
        dt_begin_tstz_in             IN drug_prescription.dt_begin_tstz%TYPE DEFAULT NULL,
        dt_cancel_tstz_in            IN drug_prescription.dt_cancel_tstz%TYPE DEFAULT NULL,
        id_patient_in                IN drug_prescription.id_patient%TYPE DEFAULT NULL,
        create_user_in               IN drug_prescription.create_user%TYPE DEFAULT NULL,
        create_time_in               IN drug_prescription.create_time%TYPE DEFAULT NULL,
        create_institution_in        IN drug_prescription.create_institution%TYPE DEFAULT NULL,
        update_user_in               IN drug_prescription.update_user%TYPE DEFAULT NULL,
        update_time_in               IN drug_prescription.update_time%TYPE DEFAULT NULL,
        update_institution_in        IN drug_prescription.update_institution%TYPE DEFAULT NULL,
        id_parent_in                 IN drug_prescription.id_parent%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    ) RETURN drug_prescription.id_drug_prescription%TYPE;

    -- Update any/all columns by primary key. If you pass NULL, then
    -- the current column value is set to itself. If you need a more
    -- selected UPDATE then use one of the onecol procedures below.

    PROCEDURE upd
    (
        id_drug_prescription_in       IN drug_prescription.id_drug_prescription%TYPE,
        id_episode_in                 IN drug_prescription.id_episode%TYPE DEFAULT NULL,
        id_episode_nin                IN BOOLEAN := TRUE,
        id_professional_in            IN drug_prescription.id_professional%TYPE DEFAULT NULL,
        id_professional_nin           IN BOOLEAN := TRUE,
        flg_type_in                   IN drug_prescription.flg_type%TYPE DEFAULT NULL,
        flg_type_nin                  IN BOOLEAN := TRUE,
        barcode_in                    IN drug_prescription.barcode%TYPE DEFAULT NULL,
        barcode_nin                   IN BOOLEAN := TRUE,
        num_days_expire_in            IN drug_prescription.num_days_expire%TYPE DEFAULT NULL,
        num_days_expire_nin           IN BOOLEAN := TRUE,
        flg_time_in                   IN drug_prescription.flg_time%TYPE DEFAULT NULL,
        flg_time_nin                  IN BOOLEAN := TRUE,
        flg_status_in                 IN drug_prescription.flg_status%TYPE DEFAULT NULL,
        flg_status_nin                IN BOOLEAN := TRUE,
        id_prof_cancel_in             IN drug_prescription.id_prof_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_nin            IN BOOLEAN := TRUE,
        notes_cancel_in               IN drug_prescription.notes_cancel%TYPE DEFAULT NULL,
        notes_cancel_nin              IN BOOLEAN := TRUE,
        id_episode_origin_in          IN drug_prescription.id_episode_origin%TYPE DEFAULT NULL,
        id_episode_origin_nin         IN BOOLEAN := TRUE,
        id_episode_destination_in     IN drug_prescription.id_episode_destination%TYPE DEFAULT NULL,
        id_episode_destination_nin    IN BOOLEAN := TRUE,
        id_protocols_in               IN drug_prescription.id_protocols%TYPE DEFAULT NULL,
        id_protocols_nin              IN BOOLEAN := TRUE,
        id_prev_episode_in            IN drug_prescription.id_prev_episode%TYPE DEFAULT NULL,
        id_prev_episode_nin           IN BOOLEAN := TRUE,
        dt_drug_prescription_tstz_in  IN drug_prescription.dt_drug_prescription_tstz%TYPE DEFAULT NULL,
        dt_drug_prescription_tstz_nin IN BOOLEAN := TRUE,
        dt_begin_tstz_in              IN drug_prescription.dt_begin_tstz%TYPE DEFAULT NULL,
        dt_begin_tstz_nin             IN BOOLEAN := TRUE,
        dt_cancel_tstz_in             IN drug_prescription.dt_cancel_tstz%TYPE DEFAULT NULL,
        dt_cancel_tstz_nin            IN BOOLEAN := TRUE,
        id_patient_in                 IN drug_prescription.id_patient%TYPE DEFAULT NULL,
        id_patient_nin                IN BOOLEAN := TRUE,
        create_user_in                IN drug_prescription.create_user%TYPE DEFAULT NULL,
        create_user_nin               IN BOOLEAN := TRUE,
        create_time_in                IN drug_prescription.create_time%TYPE DEFAULT NULL,
        create_time_nin               IN BOOLEAN := TRUE,
        create_institution_in         IN drug_prescription.create_institution%TYPE DEFAULT NULL,
        create_institution_nin        IN BOOLEAN := TRUE,
        update_user_in                IN drug_prescription.update_user%TYPE DEFAULT NULL,
        update_user_nin               IN BOOLEAN := TRUE,
        update_time_in                IN drug_prescription.update_time%TYPE DEFAULT NULL,
        update_time_nin               IN BOOLEAN := TRUE,
        update_institution_in         IN drug_prescription.update_institution%TYPE DEFAULT NULL,
        update_institution_nin        IN BOOLEAN := TRUE,
        id_parent_in                  IN drug_prescription.id_parent%TYPE DEFAULT NULL,
        id_parent_nin                 IN BOOLEAN := TRUE,
        handle_error_in               IN BOOLEAN := TRUE,
        rows_out                      IN OUT table_varchar
    );

    PROCEDURE upd
    (
        id_drug_prescription_in       IN drug_prescription.id_drug_prescription%TYPE,
        id_episode_in                 IN drug_prescription.id_episode%TYPE DEFAULT NULL,
        id_episode_nin                IN BOOLEAN := TRUE,
        id_professional_in            IN drug_prescription.id_professional%TYPE DEFAULT NULL,
        id_professional_nin           IN BOOLEAN := TRUE,
        flg_type_in                   IN drug_prescription.flg_type%TYPE DEFAULT NULL,
        flg_type_nin                  IN BOOLEAN := TRUE,
        barcode_in                    IN drug_prescription.barcode%TYPE DEFAULT NULL,
        barcode_nin                   IN BOOLEAN := TRUE,
        num_days_expire_in            IN drug_prescription.num_days_expire%TYPE DEFAULT NULL,
        num_days_expire_nin           IN BOOLEAN := TRUE,
        flg_time_in                   IN drug_prescription.flg_time%TYPE DEFAULT NULL,
        flg_time_nin                  IN BOOLEAN := TRUE,
        flg_status_in                 IN drug_prescription.flg_status%TYPE DEFAULT NULL,
        flg_status_nin                IN BOOLEAN := TRUE,
        id_prof_cancel_in             IN drug_prescription.id_prof_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_nin            IN BOOLEAN := TRUE,
        notes_cancel_in               IN drug_prescription.notes_cancel%TYPE DEFAULT NULL,
        notes_cancel_nin              IN BOOLEAN := TRUE,
        id_episode_origin_in          IN drug_prescription.id_episode_origin%TYPE DEFAULT NULL,
        id_episode_origin_nin         IN BOOLEAN := TRUE,
        id_episode_destination_in     IN drug_prescription.id_episode_destination%TYPE DEFAULT NULL,
        id_episode_destination_nin    IN BOOLEAN := TRUE,
        id_protocols_in               IN drug_prescription.id_protocols%TYPE DEFAULT NULL,
        id_protocols_nin              IN BOOLEAN := TRUE,
        id_prev_episode_in            IN drug_prescription.id_prev_episode%TYPE DEFAULT NULL,
        id_prev_episode_nin           IN BOOLEAN := TRUE,
        dt_drug_prescription_tstz_in  IN drug_prescription.dt_drug_prescription_tstz%TYPE DEFAULT NULL,
        dt_drug_prescription_tstz_nin IN BOOLEAN := TRUE,
        dt_begin_tstz_in              IN drug_prescription.dt_begin_tstz%TYPE DEFAULT NULL,
        dt_begin_tstz_nin             IN BOOLEAN := TRUE,
        dt_cancel_tstz_in             IN drug_prescription.dt_cancel_tstz%TYPE DEFAULT NULL,
        dt_cancel_tstz_nin            IN BOOLEAN := TRUE,
        id_patient_in                 IN drug_prescription.id_patient%TYPE DEFAULT NULL,
        id_patient_nin                IN BOOLEAN := TRUE,
        create_user_in                IN drug_prescription.create_user%TYPE DEFAULT NULL,
        create_user_nin               IN BOOLEAN := TRUE,
        create_time_in                IN drug_prescription.create_time%TYPE DEFAULT NULL,
        create_time_nin               IN BOOLEAN := TRUE,
        create_institution_in         IN drug_prescription.create_institution%TYPE DEFAULT NULL,
        create_institution_nin        IN BOOLEAN := TRUE,
        update_user_in                IN drug_prescription.update_user%TYPE DEFAULT NULL,
        update_user_nin               IN BOOLEAN := TRUE,
        update_time_in                IN drug_prescription.update_time%TYPE DEFAULT NULL,
        update_time_nin               IN BOOLEAN := TRUE,
        update_institution_in         IN drug_prescription.update_institution%TYPE DEFAULT NULL,
        update_institution_nin        IN BOOLEAN := TRUE,
        id_parent_in                  IN drug_prescription.id_parent%TYPE DEFAULT NULL,
        id_parent_nin                 IN BOOLEAN := TRUE,
        handle_error_in               IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        id_episode_in                 IN drug_prescription.id_episode%TYPE DEFAULT NULL,
        id_episode_nin                IN BOOLEAN := TRUE,
        id_professional_in            IN drug_prescription.id_professional%TYPE DEFAULT NULL,
        id_professional_nin           IN BOOLEAN := TRUE,
        flg_type_in                   IN drug_prescription.flg_type%TYPE DEFAULT NULL,
        flg_type_nin                  IN BOOLEAN := TRUE,
        barcode_in                    IN drug_prescription.barcode%TYPE DEFAULT NULL,
        barcode_nin                   IN BOOLEAN := TRUE,
        num_days_expire_in            IN drug_prescription.num_days_expire%TYPE DEFAULT NULL,
        num_days_expire_nin           IN BOOLEAN := TRUE,
        flg_time_in                   IN drug_prescription.flg_time%TYPE DEFAULT NULL,
        flg_time_nin                  IN BOOLEAN := TRUE,
        flg_status_in                 IN drug_prescription.flg_status%TYPE DEFAULT NULL,
        flg_status_nin                IN BOOLEAN := TRUE,
        id_prof_cancel_in             IN drug_prescription.id_prof_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_nin            IN BOOLEAN := TRUE,
        notes_cancel_in               IN drug_prescription.notes_cancel%TYPE DEFAULT NULL,
        notes_cancel_nin              IN BOOLEAN := TRUE,
        id_episode_origin_in          IN drug_prescription.id_episode_origin%TYPE DEFAULT NULL,
        id_episode_origin_nin         IN BOOLEAN := TRUE,
        id_episode_destination_in     IN drug_prescription.id_episode_destination%TYPE DEFAULT NULL,
        id_episode_destination_nin    IN BOOLEAN := TRUE,
        id_protocols_in               IN drug_prescription.id_protocols%TYPE DEFAULT NULL,
        id_protocols_nin              IN BOOLEAN := TRUE,
        id_prev_episode_in            IN drug_prescription.id_prev_episode%TYPE DEFAULT NULL,
        id_prev_episode_nin           IN BOOLEAN := TRUE,
        dt_drug_prescription_tstz_in  IN drug_prescription.dt_drug_prescription_tstz%TYPE DEFAULT NULL,
        dt_drug_prescription_tstz_nin IN BOOLEAN := TRUE,
        dt_begin_tstz_in              IN drug_prescription.dt_begin_tstz%TYPE DEFAULT NULL,
        dt_begin_tstz_nin             IN BOOLEAN := TRUE,
        dt_cancel_tstz_in             IN drug_prescription.dt_cancel_tstz%TYPE DEFAULT NULL,
        dt_cancel_tstz_nin            IN BOOLEAN := TRUE,
        id_patient_in                 IN drug_prescription.id_patient%TYPE DEFAULT NULL,
        id_patient_nin                IN BOOLEAN := TRUE,
        create_user_in                IN drug_prescription.create_user%TYPE DEFAULT NULL,
        create_user_nin               IN BOOLEAN := TRUE,
        create_time_in                IN drug_prescription.create_time%TYPE DEFAULT NULL,
        create_time_nin               IN BOOLEAN := TRUE,
        create_institution_in         IN drug_prescription.create_institution%TYPE DEFAULT NULL,
        create_institution_nin        IN BOOLEAN := TRUE,
        update_user_in                IN drug_prescription.update_user%TYPE DEFAULT NULL,
        update_user_nin               IN BOOLEAN := TRUE,
        update_time_in                IN drug_prescription.update_time%TYPE DEFAULT NULL,
        update_time_nin               IN BOOLEAN := TRUE,
        update_institution_in         IN drug_prescription.update_institution%TYPE DEFAULT NULL,
        update_institution_nin        IN BOOLEAN := TRUE,
        id_parent_in                  IN drug_prescription.id_parent%TYPE DEFAULT NULL,
        id_parent_nin                 IN BOOLEAN := TRUE,
        where_in                      VARCHAR2,
        handle_error_in               IN BOOLEAN := TRUE,
        rows_out                      IN OUT table_varchar
    );

    PROCEDURE upd
    (
        id_episode_in                 IN drug_prescription.id_episode%TYPE DEFAULT NULL,
        id_episode_nin                IN BOOLEAN := TRUE,
        id_professional_in            IN drug_prescription.id_professional%TYPE DEFAULT NULL,
        id_professional_nin           IN BOOLEAN := TRUE,
        flg_type_in                   IN drug_prescription.flg_type%TYPE DEFAULT NULL,
        flg_type_nin                  IN BOOLEAN := TRUE,
        barcode_in                    IN drug_prescription.barcode%TYPE DEFAULT NULL,
        barcode_nin                   IN BOOLEAN := TRUE,
        num_days_expire_in            IN drug_prescription.num_days_expire%TYPE DEFAULT NULL,
        num_days_expire_nin           IN BOOLEAN := TRUE,
        flg_time_in                   IN drug_prescription.flg_time%TYPE DEFAULT NULL,
        flg_time_nin                  IN BOOLEAN := TRUE,
        flg_status_in                 IN drug_prescription.flg_status%TYPE DEFAULT NULL,
        flg_status_nin                IN BOOLEAN := TRUE,
        id_prof_cancel_in             IN drug_prescription.id_prof_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_nin            IN BOOLEAN := TRUE,
        notes_cancel_in               IN drug_prescription.notes_cancel%TYPE DEFAULT NULL,
        notes_cancel_nin              IN BOOLEAN := TRUE,
        id_episode_origin_in          IN drug_prescription.id_episode_origin%TYPE DEFAULT NULL,
        id_episode_origin_nin         IN BOOLEAN := TRUE,
        id_episode_destination_in     IN drug_prescription.id_episode_destination%TYPE DEFAULT NULL,
        id_episode_destination_nin    IN BOOLEAN := TRUE,
        id_protocols_in               IN drug_prescription.id_protocols%TYPE DEFAULT NULL,
        id_protocols_nin              IN BOOLEAN := TRUE,
        id_prev_episode_in            IN drug_prescription.id_prev_episode%TYPE DEFAULT NULL,
        id_prev_episode_nin           IN BOOLEAN := TRUE,
        dt_drug_prescription_tstz_in  IN drug_prescription.dt_drug_prescription_tstz%TYPE DEFAULT NULL,
        dt_drug_prescription_tstz_nin IN BOOLEAN := TRUE,
        dt_begin_tstz_in              IN drug_prescription.dt_begin_tstz%TYPE DEFAULT NULL,
        dt_begin_tstz_nin             IN BOOLEAN := TRUE,
        dt_cancel_tstz_in             IN drug_prescription.dt_cancel_tstz%TYPE DEFAULT NULL,
        dt_cancel_tstz_nin            IN BOOLEAN := TRUE,
        id_patient_in                 IN drug_prescription.id_patient%TYPE DEFAULT NULL,
        id_patient_nin                IN BOOLEAN := TRUE,
        create_user_in                IN drug_prescription.create_user%TYPE DEFAULT NULL,
        create_user_nin               IN BOOLEAN := TRUE,
        create_time_in                IN drug_prescription.create_time%TYPE DEFAULT NULL,
        create_time_nin               IN BOOLEAN := TRUE,
        create_institution_in         IN drug_prescription.create_institution%TYPE DEFAULT NULL,
        create_institution_nin        IN BOOLEAN := TRUE,
        update_user_in                IN drug_prescription.update_user%TYPE DEFAULT NULL,
        update_user_nin               IN BOOLEAN := TRUE,
        update_time_in                IN drug_prescription.update_time%TYPE DEFAULT NULL,
        update_time_nin               IN BOOLEAN := TRUE,
        update_institution_in         IN drug_prescription.update_institution%TYPE DEFAULT NULL,
        update_institution_nin        IN BOOLEAN := TRUE,
        id_parent_in                  IN drug_prescription.id_parent%TYPE DEFAULT NULL,
        id_parent_nin                 IN BOOLEAN := TRUE,
        where_in                      VARCHAR2,
        handle_error_in               IN BOOLEAN := TRUE
    );

    PROCEDURE upd_ins
    (
        id_drug_prescription_in      IN drug_prescription.id_drug_prescription%TYPE,
        id_episode_in                IN drug_prescription.id_episode%TYPE DEFAULT NULL,
        id_professional_in           IN drug_prescription.id_professional%TYPE DEFAULT NULL,
        flg_type_in                  IN drug_prescription.flg_type%TYPE DEFAULT NULL,
        barcode_in                   IN drug_prescription.barcode%TYPE DEFAULT NULL,
        num_days_expire_in           IN drug_prescription.num_days_expire%TYPE DEFAULT NULL,
        flg_time_in                  IN drug_prescription.flg_time%TYPE DEFAULT NULL,
        flg_status_in                IN drug_prescription.flg_status%TYPE DEFAULT NULL,
        id_prof_cancel_in            IN drug_prescription.id_prof_cancel%TYPE DEFAULT NULL,
        notes_cancel_in              IN drug_prescription.notes_cancel%TYPE DEFAULT NULL,
        id_episode_origin_in         IN drug_prescription.id_episode_origin%TYPE DEFAULT NULL,
        id_episode_destination_in    IN drug_prescription.id_episode_destination%TYPE DEFAULT NULL,
        id_protocols_in              IN drug_prescription.id_protocols%TYPE DEFAULT NULL,
        id_prev_episode_in           IN drug_prescription.id_prev_episode%TYPE DEFAULT NULL,
        dt_drug_prescription_tstz_in IN drug_prescription.dt_drug_prescription_tstz%TYPE DEFAULT NULL,
        dt_begin_tstz_in             IN drug_prescription.dt_begin_tstz%TYPE DEFAULT NULL,
        dt_cancel_tstz_in            IN drug_prescription.dt_cancel_tstz%TYPE DEFAULT NULL,
        id_patient_in                IN drug_prescription.id_patient%TYPE DEFAULT NULL,
        create_user_in               IN drug_prescription.create_user%TYPE DEFAULT NULL,
        create_time_in               IN drug_prescription.create_time%TYPE DEFAULT NULL,
        create_institution_in        IN drug_prescription.create_institution%TYPE DEFAULT NULL,
        update_user_in               IN drug_prescription.update_user%TYPE DEFAULT NULL,
        update_time_in               IN drug_prescription.update_time%TYPE DEFAULT NULL,
        update_institution_in        IN drug_prescription.update_institution%TYPE DEFAULT NULL,
        id_parent_in                 IN drug_prescription.id_parent%TYPE DEFAULT NULL,
        handle_error_in              IN BOOLEAN := TRUE,
        rows_out                     OUT table_varchar
    );

    PROCEDURE upd_ins
    (
        id_drug_prescription_in      IN drug_prescription.id_drug_prescription%TYPE,
        id_episode_in                IN drug_prescription.id_episode%TYPE DEFAULT NULL,
        id_professional_in           IN drug_prescription.id_professional%TYPE DEFAULT NULL,
        flg_type_in                  IN drug_prescription.flg_type%TYPE DEFAULT NULL,
        barcode_in                   IN drug_prescription.barcode%TYPE DEFAULT NULL,
        num_days_expire_in           IN drug_prescription.num_days_expire%TYPE DEFAULT NULL,
        flg_time_in                  IN drug_prescription.flg_time%TYPE DEFAULT NULL,
        flg_status_in                IN drug_prescription.flg_status%TYPE DEFAULT NULL,
        id_prof_cancel_in            IN drug_prescription.id_prof_cancel%TYPE DEFAULT NULL,
        notes_cancel_in              IN drug_prescription.notes_cancel%TYPE DEFAULT NULL,
        id_episode_origin_in         IN drug_prescription.id_episode_origin%TYPE DEFAULT NULL,
        id_episode_destination_in    IN drug_prescription.id_episode_destination%TYPE DEFAULT NULL,
        id_protocols_in              IN drug_prescription.id_protocols%TYPE DEFAULT NULL,
        id_prev_episode_in           IN drug_prescription.id_prev_episode%TYPE DEFAULT NULL,
        dt_drug_prescription_tstz_in IN drug_prescription.dt_drug_prescription_tstz%TYPE DEFAULT NULL,
        dt_begin_tstz_in             IN drug_prescription.dt_begin_tstz%TYPE DEFAULT NULL,
        dt_cancel_tstz_in            IN drug_prescription.dt_cancel_tstz%TYPE DEFAULT NULL,
        id_patient_in                IN drug_prescription.id_patient%TYPE DEFAULT NULL,
        create_user_in               IN drug_prescription.create_user%TYPE DEFAULT NULL,
        create_time_in               IN drug_prescription.create_time%TYPE DEFAULT NULL,
        create_institution_in        IN drug_prescription.create_institution%TYPE DEFAULT NULL,
        update_user_in               IN drug_prescription.update_user%TYPE DEFAULT NULL,
        update_time_in               IN drug_prescription.update_time%TYPE DEFAULT NULL,
        update_institution_in        IN drug_prescription.update_institution%TYPE DEFAULT NULL,
        id_parent_in                 IN drug_prescription.id_parent%TYPE DEFAULT NULL,
        handle_error_in              IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        rec_in          IN drug_prescription%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        IN OUT table_varchar
    );

    PROCEDURE upd
    (
        rec_in          IN drug_prescription%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        col_in            IN drug_prescription_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          IN OUT table_varchar
    );

    PROCEDURE upd
    (
        col_in            IN drug_prescription_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE
    );

    -- Use Native Dynamic SQL increment a single NUMBER column
    -- for all rows specified by the dynamic WHERE clause
    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_drug_prescription_in IN drug_prescription.id_drug_prescription%TYPE,
        handle_error_in         IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_drug_prescription_in IN drug_prescription.id_drug_prescription%TYPE,
        handle_error_in         IN BOOLEAN := TRUE,
        rows_out                OUT table_varchar
    );

    -- Delete all rows for primary key column ID_DRUG_PRESCRIPTION
    PROCEDURE del_id_drug_prescription
    (
        id_drug_prescription_in IN drug_prescription.id_drug_prescription%TYPE,
        handle_error_in         IN BOOLEAN := TRUE
    );

    -- Delete all rows for primary key column ID_DRUG_PRESCRIPTION
    PROCEDURE del_id_drug_prescription
    (
        id_drug_prescription_in IN drug_prescription.id_drug_prescription%TYPE,
        handle_error_in         IN BOOLEAN := TRUE,
        rows_out                OUT table_varchar
    );

    -- Delete all rows for this DPN_PAT_FK foreign key value
    PROCEDURE del_dpn_pat_fk
    (
        id_patient_in   IN drug_prescription.id_patient%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this DPN_PAT_FK foreign key value
    PROCEDURE del_dpn_pat_fk
    (
        id_patient_in   IN drug_prescription.id_patient%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for this DPN_PROF_CANCEL_FK foreign key value
    PROCEDURE del_dpn_prof_cancel_fk
    (
        id_prof_cancel_in IN drug_prescription.id_prof_cancel%TYPE,
        handle_error_in   IN BOOLEAN := TRUE
    );

    -- Delete all rows for this DPN_PROF_CANCEL_FK foreign key value
    PROCEDURE del_dpn_prof_cancel_fk
    (
        id_prof_cancel_in IN drug_prescription.id_prof_cancel%TYPE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          OUT table_varchar
    );

    -- Delete all rows for this DPN_PROF_FK foreign key value
    PROCEDURE del_dpn_prof_fk
    (
        id_professional_in IN drug_prescription.id_professional%TYPE,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete all rows for this DPN_PROF_FK foreign key value
    PROCEDURE del_dpn_prof_fk
    (
        id_professional_in IN drug_prescription.id_professional%TYPE,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    -- Delete all rows for this DPN_PRT_FK foreign key value
    PROCEDURE del_dpn_prt_fk
    (
        id_protocols_in IN drug_prescription.id_protocols%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this DPN_PRT_FK foreign key value
    PROCEDURE del_dpn_prt_fk
    (
        id_protocols_in IN drug_prescription.id_protocols%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Initialize a record with default values for columns in the table.
    PROCEDURE initrec(drug_prescription_inout IN OUT drug_prescription%ROWTYPE);

    FUNCTION initrec RETURN drug_prescription%ROWTYPE;

    FUNCTION get_data_rowid(rows_in IN table_varchar) RETURN drug_prescription_tc;

    FUNCTION get_data_rowid_pat(rows_in IN table_varchar) RETURN drug_prescription_tc;

END ts_drug_prescription;
/
