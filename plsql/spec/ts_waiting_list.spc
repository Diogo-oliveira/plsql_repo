/*-- Last Change Revision: $Rev: 437109 $*/
/*-- Last Change by: $Author: ricardo.almeida $*/
/*-- Date of last change: $Date: 2010-03-12 19:15:09 +0000 (sex, 12 mar 2010) $*/



CREATE OR REPLACE PACKAGE ts_waiting_list
/*
| Generated by or retrieved from QCGU - DO NOT MODIFY!
| QCGU - "Get it right, do it fast" - www.ToadWorld.com
| QCGU Universal ID: {1BD37A66-EA60-4927-9A64-F6DD89237236}
| Created On: Dezembro 22, 2009 9:49:15
| Created By: ALERT
*/
 IS

    -- Collection of %ROWTYPE records based on "WAITING_LIST"
    TYPE waiting_list_tc IS TABLE OF waiting_list%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE waiting_list_ntt IS TABLE OF waiting_list%ROWTYPE;
    TYPE waiting_list_vat IS VARRAY(100) OF waiting_list%ROWTYPE;

    -- Same type structure, with a static name.
    TYPE aat IS TABLE OF waiting_list%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE ntt IS TABLE OF waiting_list%ROWTYPE;
    TYPE vat IS VARRAY(100) OF waiting_list%ROWTYPE;

    -- Column Collection based on column "ID_WAITING_LIST"
    TYPE id_waiting_list_cc IS TABLE OF waiting_list.id_waiting_list%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_PATIENT"
    TYPE id_patient_cc IS TABLE OF waiting_list.id_patient%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_PROF_REQ"
    TYPE id_prof_req_cc IS TABLE OF waiting_list.id_prof_req%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "DT_PLACEMENT"
    TYPE dt_placement_cc IS TABLE OF waiting_list.dt_placement%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "FLG_TYPE"
    TYPE flg_type_cc IS TABLE OF waiting_list.flg_type%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "FLG_STATUS"
    TYPE flg_status_cc IS TABLE OF waiting_list.flg_status%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "DT_DPB"
    TYPE dt_dpb_cc IS TABLE OF waiting_list.dt_dpb%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "DT_DPA"
    TYPE dt_dpa_cc IS TABLE OF waiting_list.dt_dpa%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "DT_SURGERY"
    TYPE dt_surgery_cc IS TABLE OF waiting_list.dt_surgery%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "DT_ADMISSION"
    TYPE dt_admission_cc IS TABLE OF waiting_list.dt_admission%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "MIN_INFORM_TIME"
    TYPE min_inform_time_cc IS TABLE OF waiting_list.min_inform_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_WTL_URG_LEVEL"
    TYPE id_wtl_urg_level_cc IS TABLE OF waiting_list.id_wtl_urg_level%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_PROF_REG"
    TYPE id_prof_reg_cc IS TABLE OF waiting_list.id_prof_reg%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "DT_REG"
    TYPE dt_reg_cc IS TABLE OF waiting_list.dt_reg%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_USER"
    TYPE create_user_cc IS TABLE OF waiting_list.create_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_TIME"
    TYPE create_time_cc IS TABLE OF waiting_list.create_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "CREATE_INSTITUTION"
    TYPE create_institution_cc IS TABLE OF waiting_list.create_institution%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_USER"
    TYPE update_user_cc IS TABLE OF waiting_list.update_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_TIME"
    TYPE update_time_cc IS TABLE OF waiting_list.update_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "UPDATE_INSTITUTION"
    TYPE update_institution_cc IS TABLE OF waiting_list.update_institution%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_CANCEL_REASON"
    TYPE id_cancel_reason_cc IS TABLE OF waiting_list.id_cancel_reason%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "NOTES_CANCEL"
    TYPE notes_cancel_cc IS TABLE OF waiting_list.notes_cancel%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_PROF_CANCEL"
    TYPE id_prof_cancel_cc IS TABLE OF waiting_list.id_prof_cancel%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "DT_CANCEL"
    TYPE dt_cancel_cc IS TABLE OF waiting_list.dt_cancel%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "ID_EXTERNAL_REQUEST"
    TYPE id_external_request_cc IS TABLE OF waiting_list.id_external_request%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "FUNC_EVAL_SCORE"
    TYPE func_eval_score_cc IS TABLE OF waiting_list.func_eval_score%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column "NOTES_EDIT"
    TYPE notes_edit_cc IS TABLE OF waiting_list.notes_edit%TYPE INDEX BY BINARY_INTEGER;

    -- Insert one row, providing primary key if present
    PROCEDURE ins
    (
        id_waiting_list_in     IN waiting_list.id_waiting_list%TYPE,
        id_patient_in          IN waiting_list.id_patient%TYPE DEFAULT NULL,
        id_prof_req_in         IN waiting_list.id_prof_req%TYPE DEFAULT NULL,
        dt_placement_in        IN waiting_list.dt_placement%TYPE DEFAULT NULL,
        flg_type_in            IN waiting_list.flg_type%TYPE DEFAULT NULL,
        flg_status_in          IN waiting_list.flg_status%TYPE DEFAULT 'I',
        dt_dpb_in              IN waiting_list.dt_dpb%TYPE DEFAULT NULL,
        dt_dpa_in              IN waiting_list.dt_dpa%TYPE DEFAULT NULL,
        dt_surgery_in          IN waiting_list.dt_surgery%TYPE DEFAULT NULL,
        dt_admission_in        IN waiting_list.dt_admission%TYPE DEFAULT NULL,
        min_inform_time_in     IN waiting_list.min_inform_time%TYPE DEFAULT NULL,
        id_wtl_urg_level_in    IN waiting_list.id_wtl_urg_level%TYPE DEFAULT NULL,
        id_prof_reg_in         IN waiting_list.id_prof_reg%TYPE DEFAULT NULL,
        dt_reg_in              IN waiting_list.dt_reg%TYPE DEFAULT NULL,
        create_user_in         IN waiting_list.create_user%TYPE DEFAULT NULL,
        create_time_in         IN waiting_list.create_time%TYPE DEFAULT NULL,
        create_institution_in  IN waiting_list.create_institution%TYPE DEFAULT NULL,
        update_user_in         IN waiting_list.update_user%TYPE DEFAULT NULL,
        update_time_in         IN waiting_list.update_time%TYPE DEFAULT NULL,
        update_institution_in  IN waiting_list.update_institution%TYPE DEFAULT NULL,
        id_cancel_reason_in    IN waiting_list.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in        IN waiting_list.notes_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_in      IN waiting_list.id_prof_cancel%TYPE DEFAULT NULL,
        dt_cancel_in           IN waiting_list.dt_cancel%TYPE DEFAULT NULL,
        id_external_request_in IN waiting_list.id_external_request%TYPE DEFAULT NULL,
        func_eval_score_in     IN waiting_list.func_eval_score%TYPE DEFAULT NULL,
        notes_edit_in          IN waiting_list.notes_edit%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_waiting_list_in     IN waiting_list.id_waiting_list%TYPE,
        id_patient_in          IN waiting_list.id_patient%TYPE DEFAULT NULL,
        id_prof_req_in         IN waiting_list.id_prof_req%TYPE DEFAULT NULL,
        dt_placement_in        IN waiting_list.dt_placement%TYPE DEFAULT NULL,
        flg_type_in            IN waiting_list.flg_type%TYPE DEFAULT NULL,
        flg_status_in          IN waiting_list.flg_status%TYPE DEFAULT 'I',
        dt_dpb_in              IN waiting_list.dt_dpb%TYPE DEFAULT NULL,
        dt_dpa_in              IN waiting_list.dt_dpa%TYPE DEFAULT NULL,
        dt_surgery_in          IN waiting_list.dt_surgery%TYPE DEFAULT NULL,
        dt_admission_in        IN waiting_list.dt_admission%TYPE DEFAULT NULL,
        min_inform_time_in     IN waiting_list.min_inform_time%TYPE DEFAULT NULL,
        id_wtl_urg_level_in    IN waiting_list.id_wtl_urg_level%TYPE DEFAULT NULL,
        id_prof_reg_in         IN waiting_list.id_prof_reg%TYPE DEFAULT NULL,
        dt_reg_in              IN waiting_list.dt_reg%TYPE DEFAULT NULL,
        create_user_in         IN waiting_list.create_user%TYPE DEFAULT NULL,
        create_time_in         IN waiting_list.create_time%TYPE DEFAULT NULL,
        create_institution_in  IN waiting_list.create_institution%TYPE DEFAULT NULL,
        update_user_in         IN waiting_list.update_user%TYPE DEFAULT NULL,
        update_time_in         IN waiting_list.update_time%TYPE DEFAULT NULL,
        update_institution_in  IN waiting_list.update_institution%TYPE DEFAULT NULL,
        id_cancel_reason_in    IN waiting_list.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in        IN waiting_list.notes_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_in      IN waiting_list.id_prof_cancel%TYPE DEFAULT NULL,
        dt_cancel_in           IN waiting_list.dt_cancel%TYPE DEFAULT NULL,
        id_external_request_in IN waiting_list.id_external_request%TYPE DEFAULT NULL,
        func_eval_score_in     IN waiting_list.func_eval_score%TYPE DEFAULT NULL,
        notes_edit_in          IN waiting_list.notes_edit%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a row based on a record.
    -- Specify whether or not a primary key value should be generated.
    PROCEDURE ins
    (
        rec_in          IN waiting_list%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rec_in          IN waiting_list%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a collection of rows using FORALL; all primary key values
    -- must have already been generated, or are handled in triggers.
    PROCEDURE ins
    (
        rows_in         IN waiting_list_tc,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        rows_in         IN waiting_list_tc,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Return next primary key value using the named sequence.
    FUNCTION next_key(sequence_in IN VARCHAR2 := NULL) RETURN waiting_list.id_waiting_list%TYPE;

    -- Insert one row, generating hidden primary key using a sequence
    PROCEDURE ins
    (
        id_patient_in          IN waiting_list.id_patient%TYPE DEFAULT NULL,
        id_prof_req_in         IN waiting_list.id_prof_req%TYPE DEFAULT NULL,
        dt_placement_in        IN waiting_list.dt_placement%TYPE DEFAULT NULL,
        flg_type_in            IN waiting_list.flg_type%TYPE DEFAULT NULL,
        flg_status_in          IN waiting_list.flg_status%TYPE DEFAULT 'I',
        dt_dpb_in              IN waiting_list.dt_dpb%TYPE DEFAULT NULL,
        dt_dpa_in              IN waiting_list.dt_dpa%TYPE DEFAULT NULL,
        dt_surgery_in          IN waiting_list.dt_surgery%TYPE DEFAULT NULL,
        dt_admission_in        IN waiting_list.dt_admission%TYPE DEFAULT NULL,
        min_inform_time_in     IN waiting_list.min_inform_time%TYPE DEFAULT NULL,
        id_wtl_urg_level_in    IN waiting_list.id_wtl_urg_level%TYPE DEFAULT NULL,
        id_prof_reg_in         IN waiting_list.id_prof_reg%TYPE DEFAULT NULL,
        dt_reg_in              IN waiting_list.dt_reg%TYPE DEFAULT NULL,
        create_user_in         IN waiting_list.create_user%TYPE DEFAULT NULL,
        create_time_in         IN waiting_list.create_time%TYPE DEFAULT NULL,
        create_institution_in  IN waiting_list.create_institution%TYPE DEFAULT NULL,
        update_user_in         IN waiting_list.update_user%TYPE DEFAULT NULL,
        update_time_in         IN waiting_list.update_time%TYPE DEFAULT NULL,
        update_institution_in  IN waiting_list.update_institution%TYPE DEFAULT NULL,
        id_cancel_reason_in    IN waiting_list.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in        IN waiting_list.notes_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_in      IN waiting_list.id_prof_cancel%TYPE DEFAULT NULL,
        dt_cancel_in           IN waiting_list.dt_cancel%TYPE DEFAULT NULL,
        id_external_request_in IN waiting_list.id_external_request%TYPE DEFAULT NULL,
        func_eval_score_in     IN waiting_list.func_eval_score%TYPE DEFAULT NULL,
        notes_edit_in          IN waiting_list.notes_edit%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_patient_in          IN waiting_list.id_patient%TYPE DEFAULT NULL,
        id_prof_req_in         IN waiting_list.id_prof_req%TYPE DEFAULT NULL,
        dt_placement_in        IN waiting_list.dt_placement%TYPE DEFAULT NULL,
        flg_type_in            IN waiting_list.flg_type%TYPE DEFAULT NULL,
        flg_status_in          IN waiting_list.flg_status%TYPE DEFAULT 'I',
        dt_dpb_in              IN waiting_list.dt_dpb%TYPE DEFAULT NULL,
        dt_dpa_in              IN waiting_list.dt_dpa%TYPE DEFAULT NULL,
        dt_surgery_in          IN waiting_list.dt_surgery%TYPE DEFAULT NULL,
        dt_admission_in        IN waiting_list.dt_admission%TYPE DEFAULT NULL,
        min_inform_time_in     IN waiting_list.min_inform_time%TYPE DEFAULT NULL,
        id_wtl_urg_level_in    IN waiting_list.id_wtl_urg_level%TYPE DEFAULT NULL,
        id_prof_reg_in         IN waiting_list.id_prof_reg%TYPE DEFAULT NULL,
        dt_reg_in              IN waiting_list.dt_reg%TYPE DEFAULT NULL,
        create_user_in         IN waiting_list.create_user%TYPE DEFAULT NULL,
        create_time_in         IN waiting_list.create_time%TYPE DEFAULT NULL,
        create_institution_in  IN waiting_list.create_institution%TYPE DEFAULT NULL,
        update_user_in         IN waiting_list.update_user%TYPE DEFAULT NULL,
        update_time_in         IN waiting_list.update_time%TYPE DEFAULT NULL,
        update_institution_in  IN waiting_list.update_institution%TYPE DEFAULT NULL,
        id_cancel_reason_in    IN waiting_list.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in        IN waiting_list.notes_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_in      IN waiting_list.id_prof_cancel%TYPE DEFAULT NULL,
        dt_cancel_in           IN waiting_list.dt_cancel%TYPE DEFAULT NULL,
        id_external_request_in IN waiting_list.id_external_request%TYPE DEFAULT NULL,
        func_eval_score_in     IN waiting_list.func_eval_score%TYPE DEFAULT NULL,
        notes_edit_in          IN waiting_list.notes_edit%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert one row, returning primary key generated by sequence
    PROCEDURE ins
    (
        id_patient_in          IN waiting_list.id_patient%TYPE DEFAULT NULL,
        id_prof_req_in         IN waiting_list.id_prof_req%TYPE DEFAULT NULL,
        dt_placement_in        IN waiting_list.dt_placement%TYPE DEFAULT NULL,
        flg_type_in            IN waiting_list.flg_type%TYPE DEFAULT NULL,
        flg_status_in          IN waiting_list.flg_status%TYPE DEFAULT 'I',
        dt_dpb_in              IN waiting_list.dt_dpb%TYPE DEFAULT NULL,
        dt_dpa_in              IN waiting_list.dt_dpa%TYPE DEFAULT NULL,
        dt_surgery_in          IN waiting_list.dt_surgery%TYPE DEFAULT NULL,
        dt_admission_in        IN waiting_list.dt_admission%TYPE DEFAULT NULL,
        min_inform_time_in     IN waiting_list.min_inform_time%TYPE DEFAULT NULL,
        id_wtl_urg_level_in    IN waiting_list.id_wtl_urg_level%TYPE DEFAULT NULL,
        id_prof_reg_in         IN waiting_list.id_prof_reg%TYPE DEFAULT NULL,
        dt_reg_in              IN waiting_list.dt_reg%TYPE DEFAULT NULL,
        create_user_in         IN waiting_list.create_user%TYPE DEFAULT NULL,
        create_time_in         IN waiting_list.create_time%TYPE DEFAULT NULL,
        create_institution_in  IN waiting_list.create_institution%TYPE DEFAULT NULL,
        update_user_in         IN waiting_list.update_user%TYPE DEFAULT NULL,
        update_time_in         IN waiting_list.update_time%TYPE DEFAULT NULL,
        update_institution_in  IN waiting_list.update_institution%TYPE DEFAULT NULL,
        id_cancel_reason_in    IN waiting_list.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in        IN waiting_list.notes_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_in      IN waiting_list.id_prof_cancel%TYPE DEFAULT NULL,
        dt_cancel_in           IN waiting_list.dt_cancel%TYPE DEFAULT NULL,
        id_external_request_in IN waiting_list.id_external_request%TYPE DEFAULT NULL,
        func_eval_score_in     IN waiting_list.func_eval_score%TYPE DEFAULT NULL,
        notes_edit_in          IN waiting_list.notes_edit%TYPE DEFAULT NULL,
        id_waiting_list_out    IN OUT waiting_list.id_waiting_list%TYPE
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    PROCEDURE ins
    (
        id_patient_in          IN waiting_list.id_patient%TYPE DEFAULT NULL,
        id_prof_req_in         IN waiting_list.id_prof_req%TYPE DEFAULT NULL,
        dt_placement_in        IN waiting_list.dt_placement%TYPE DEFAULT NULL,
        flg_type_in            IN waiting_list.flg_type%TYPE DEFAULT NULL,
        flg_status_in          IN waiting_list.flg_status%TYPE DEFAULT 'I',
        dt_dpb_in              IN waiting_list.dt_dpb%TYPE DEFAULT NULL,
        dt_dpa_in              IN waiting_list.dt_dpa%TYPE DEFAULT NULL,
        dt_surgery_in          IN waiting_list.dt_surgery%TYPE DEFAULT NULL,
        dt_admission_in        IN waiting_list.dt_admission%TYPE DEFAULT NULL,
        min_inform_time_in     IN waiting_list.min_inform_time%TYPE DEFAULT NULL,
        id_wtl_urg_level_in    IN waiting_list.id_wtl_urg_level%TYPE DEFAULT NULL,
        id_prof_reg_in         IN waiting_list.id_prof_reg%TYPE DEFAULT NULL,
        dt_reg_in              IN waiting_list.dt_reg%TYPE DEFAULT NULL,
        create_user_in         IN waiting_list.create_user%TYPE DEFAULT NULL,
        create_time_in         IN waiting_list.create_time%TYPE DEFAULT NULL,
        create_institution_in  IN waiting_list.create_institution%TYPE DEFAULT NULL,
        update_user_in         IN waiting_list.update_user%TYPE DEFAULT NULL,
        update_time_in         IN waiting_list.update_time%TYPE DEFAULT NULL,
        update_institution_in  IN waiting_list.update_institution%TYPE DEFAULT NULL,
        id_cancel_reason_in    IN waiting_list.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in        IN waiting_list.notes_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_in      IN waiting_list.id_prof_cancel%TYPE DEFAULT NULL,
        dt_cancel_in           IN waiting_list.dt_cancel%TYPE DEFAULT NULL,
        id_external_request_in IN waiting_list.id_external_request%TYPE DEFAULT NULL,
        func_eval_score_in     IN waiting_list.func_eval_score%TYPE DEFAULT NULL,
        notes_edit_in          IN waiting_list.notes_edit%TYPE DEFAULT NULL,
        id_waiting_list_out    IN OUT waiting_list.id_waiting_list%TYPE
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert one row with function, return generated primary key
    FUNCTION ins
    (
        id_patient_in          IN waiting_list.id_patient%TYPE DEFAULT NULL,
        id_prof_req_in         IN waiting_list.id_prof_req%TYPE DEFAULT NULL,
        dt_placement_in        IN waiting_list.dt_placement%TYPE DEFAULT NULL,
        flg_type_in            IN waiting_list.flg_type%TYPE DEFAULT NULL,
        flg_status_in          IN waiting_list.flg_status%TYPE DEFAULT 'I',
        dt_dpb_in              IN waiting_list.dt_dpb%TYPE DEFAULT NULL,
        dt_dpa_in              IN waiting_list.dt_dpa%TYPE DEFAULT NULL,
        dt_surgery_in          IN waiting_list.dt_surgery%TYPE DEFAULT NULL,
        dt_admission_in        IN waiting_list.dt_admission%TYPE DEFAULT NULL,
        min_inform_time_in     IN waiting_list.min_inform_time%TYPE DEFAULT NULL,
        id_wtl_urg_level_in    IN waiting_list.id_wtl_urg_level%TYPE DEFAULT NULL,
        id_prof_reg_in         IN waiting_list.id_prof_reg%TYPE DEFAULT NULL,
        dt_reg_in              IN waiting_list.dt_reg%TYPE DEFAULT NULL,
        create_user_in         IN waiting_list.create_user%TYPE DEFAULT NULL,
        create_time_in         IN waiting_list.create_time%TYPE DEFAULT NULL,
        create_institution_in  IN waiting_list.create_institution%TYPE DEFAULT NULL,
        update_user_in         IN waiting_list.update_user%TYPE DEFAULT NULL,
        update_time_in         IN waiting_list.update_time%TYPE DEFAULT NULL,
        update_institution_in  IN waiting_list.update_institution%TYPE DEFAULT NULL,
        id_cancel_reason_in    IN waiting_list.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in        IN waiting_list.notes_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_in      IN waiting_list.id_prof_cancel%TYPE DEFAULT NULL,
        dt_cancel_in           IN waiting_list.dt_cancel%TYPE DEFAULT NULL,
        id_external_request_in IN waiting_list.id_external_request%TYPE DEFAULT NULL,
        func_eval_score_in     IN waiting_list.func_eval_score%TYPE DEFAULT NULL,
        notes_edit_in          IN waiting_list.notes_edit%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) RETURN waiting_list.id_waiting_list%TYPE;

    FUNCTION ins
    (
        id_patient_in          IN waiting_list.id_patient%TYPE DEFAULT NULL,
        id_prof_req_in         IN waiting_list.id_prof_req%TYPE DEFAULT NULL,
        dt_placement_in        IN waiting_list.dt_placement%TYPE DEFAULT NULL,
        flg_type_in            IN waiting_list.flg_type%TYPE DEFAULT NULL,
        flg_status_in          IN waiting_list.flg_status%TYPE DEFAULT 'I',
        dt_dpb_in              IN waiting_list.dt_dpb%TYPE DEFAULT NULL,
        dt_dpa_in              IN waiting_list.dt_dpa%TYPE DEFAULT NULL,
        dt_surgery_in          IN waiting_list.dt_surgery%TYPE DEFAULT NULL,
        dt_admission_in        IN waiting_list.dt_admission%TYPE DEFAULT NULL,
        min_inform_time_in     IN waiting_list.min_inform_time%TYPE DEFAULT NULL,
        id_wtl_urg_level_in    IN waiting_list.id_wtl_urg_level%TYPE DEFAULT NULL,
        id_prof_reg_in         IN waiting_list.id_prof_reg%TYPE DEFAULT NULL,
        dt_reg_in              IN waiting_list.dt_reg%TYPE DEFAULT NULL,
        create_user_in         IN waiting_list.create_user%TYPE DEFAULT NULL,
        create_time_in         IN waiting_list.create_time%TYPE DEFAULT NULL,
        create_institution_in  IN waiting_list.create_institution%TYPE DEFAULT NULL,
        update_user_in         IN waiting_list.update_user%TYPE DEFAULT NULL,
        update_time_in         IN waiting_list.update_time%TYPE DEFAULT NULL,
        update_institution_in  IN waiting_list.update_institution%TYPE DEFAULT NULL,
        id_cancel_reason_in    IN waiting_list.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in        IN waiting_list.notes_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_in      IN waiting_list.id_prof_cancel%TYPE DEFAULT NULL,
        dt_cancel_in           IN waiting_list.dt_cancel%TYPE DEFAULT NULL,
        id_external_request_in IN waiting_list.id_external_request%TYPE DEFAULT NULL,
        func_eval_score_in     IN waiting_list.func_eval_score%TYPE DEFAULT NULL,
        notes_edit_in          IN waiting_list.notes_edit%TYPE DEFAULT NULL
        -- Pass false if you want errors to propagate out unhandled
       ,
        handle_error_in IN BOOLEAN := TRUE
    ) RETURN waiting_list.id_waiting_list%TYPE;

    -- Update any/all columns by primary key. If you pass NULL, then
    -- the current column value is set to itself. If you need a more
    -- selected UPDATE then use one of the onecol procedures below.

    PROCEDURE upd
    (
        id_waiting_list_in      IN waiting_list.id_waiting_list%TYPE,
        id_patient_in           IN waiting_list.id_patient%TYPE DEFAULT NULL,
        id_patient_nin          IN BOOLEAN := TRUE,
        id_prof_req_in          IN waiting_list.id_prof_req%TYPE DEFAULT NULL,
        id_prof_req_nin         IN BOOLEAN := TRUE,
        dt_placement_in         IN waiting_list.dt_placement%TYPE DEFAULT NULL,
        dt_placement_nin        IN BOOLEAN := TRUE,
        flg_type_in             IN waiting_list.flg_type%TYPE DEFAULT NULL,
        flg_type_nin            IN BOOLEAN := TRUE,
        flg_status_in           IN waiting_list.flg_status%TYPE DEFAULT NULL,
        flg_status_nin          IN BOOLEAN := TRUE,
        dt_dpb_in               IN waiting_list.dt_dpb%TYPE DEFAULT NULL,
        dt_dpb_nin              IN BOOLEAN := TRUE,
        dt_dpa_in               IN waiting_list.dt_dpa%TYPE DEFAULT NULL,
        dt_dpa_nin              IN BOOLEAN := TRUE,
        dt_surgery_in           IN waiting_list.dt_surgery%TYPE DEFAULT NULL,
        dt_surgery_nin          IN BOOLEAN := TRUE,
        dt_admission_in         IN waiting_list.dt_admission%TYPE DEFAULT NULL,
        dt_admission_nin        IN BOOLEAN := TRUE,
        min_inform_time_in      IN waiting_list.min_inform_time%TYPE DEFAULT NULL,
        min_inform_time_nin     IN BOOLEAN := TRUE,
        id_wtl_urg_level_in     IN waiting_list.id_wtl_urg_level%TYPE DEFAULT NULL,
        id_wtl_urg_level_nin    IN BOOLEAN := TRUE,
        id_prof_reg_in          IN waiting_list.id_prof_reg%TYPE DEFAULT NULL,
        id_prof_reg_nin         IN BOOLEAN := TRUE,
        dt_reg_in               IN waiting_list.dt_reg%TYPE DEFAULT NULL,
        dt_reg_nin              IN BOOLEAN := TRUE,
        create_user_in          IN waiting_list.create_user%TYPE DEFAULT NULL,
        create_user_nin         IN BOOLEAN := TRUE,
        create_time_in          IN waiting_list.create_time%TYPE DEFAULT NULL,
        create_time_nin         IN BOOLEAN := TRUE,
        create_institution_in   IN waiting_list.create_institution%TYPE DEFAULT NULL,
        create_institution_nin  IN BOOLEAN := TRUE,
        update_user_in          IN waiting_list.update_user%TYPE DEFAULT NULL,
        update_user_nin         IN BOOLEAN := TRUE,
        update_time_in          IN waiting_list.update_time%TYPE DEFAULT NULL,
        update_time_nin         IN BOOLEAN := TRUE,
        update_institution_in   IN waiting_list.update_institution%TYPE DEFAULT NULL,
        update_institution_nin  IN BOOLEAN := TRUE,
        id_cancel_reason_in     IN waiting_list.id_cancel_reason%TYPE DEFAULT NULL,
        id_cancel_reason_nin    IN BOOLEAN := TRUE,
        notes_cancel_in         IN waiting_list.notes_cancel%TYPE DEFAULT NULL,
        notes_cancel_nin        IN BOOLEAN := TRUE,
        id_prof_cancel_in       IN waiting_list.id_prof_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_nin      IN BOOLEAN := TRUE,
        dt_cancel_in            IN waiting_list.dt_cancel%TYPE DEFAULT NULL,
        dt_cancel_nin           IN BOOLEAN := TRUE,
        id_external_request_in  IN waiting_list.id_external_request%TYPE DEFAULT NULL,
        id_external_request_nin IN BOOLEAN := TRUE,
        func_eval_score_in      IN waiting_list.func_eval_score%TYPE DEFAULT NULL,
        func_eval_score_nin     IN BOOLEAN := TRUE,
        notes_edit_in           IN waiting_list.notes_edit%TYPE DEFAULT NULL,
        notes_edit_nin          IN BOOLEAN := TRUE,
        handle_error_in         IN BOOLEAN := TRUE,
        rows_out                IN OUT table_varchar
    );

    PROCEDURE upd
    (
        id_waiting_list_in      IN waiting_list.id_waiting_list%TYPE,
        id_patient_in           IN waiting_list.id_patient%TYPE DEFAULT NULL,
        id_patient_nin          IN BOOLEAN := TRUE,
        id_prof_req_in          IN waiting_list.id_prof_req%TYPE DEFAULT NULL,
        id_prof_req_nin         IN BOOLEAN := TRUE,
        dt_placement_in         IN waiting_list.dt_placement%TYPE DEFAULT NULL,
        dt_placement_nin        IN BOOLEAN := TRUE,
        flg_type_in             IN waiting_list.flg_type%TYPE DEFAULT NULL,
        flg_type_nin            IN BOOLEAN := TRUE,
        flg_status_in           IN waiting_list.flg_status%TYPE DEFAULT NULL,
        flg_status_nin          IN BOOLEAN := TRUE,
        dt_dpb_in               IN waiting_list.dt_dpb%TYPE DEFAULT NULL,
        dt_dpb_nin              IN BOOLEAN := TRUE,
        dt_dpa_in               IN waiting_list.dt_dpa%TYPE DEFAULT NULL,
        dt_dpa_nin              IN BOOLEAN := TRUE,
        dt_surgery_in           IN waiting_list.dt_surgery%TYPE DEFAULT NULL,
        dt_surgery_nin          IN BOOLEAN := TRUE,
        dt_admission_in         IN waiting_list.dt_admission%TYPE DEFAULT NULL,
        dt_admission_nin        IN BOOLEAN := TRUE,
        min_inform_time_in      IN waiting_list.min_inform_time%TYPE DEFAULT NULL,
        min_inform_time_nin     IN BOOLEAN := TRUE,
        id_wtl_urg_level_in     IN waiting_list.id_wtl_urg_level%TYPE DEFAULT NULL,
        id_wtl_urg_level_nin    IN BOOLEAN := TRUE,
        id_prof_reg_in          IN waiting_list.id_prof_reg%TYPE DEFAULT NULL,
        id_prof_reg_nin         IN BOOLEAN := TRUE,
        dt_reg_in               IN waiting_list.dt_reg%TYPE DEFAULT NULL,
        dt_reg_nin              IN BOOLEAN := TRUE,
        create_user_in          IN waiting_list.create_user%TYPE DEFAULT NULL,
        create_user_nin         IN BOOLEAN := TRUE,
        create_time_in          IN waiting_list.create_time%TYPE DEFAULT NULL,
        create_time_nin         IN BOOLEAN := TRUE,
        create_institution_in   IN waiting_list.create_institution%TYPE DEFAULT NULL,
        create_institution_nin  IN BOOLEAN := TRUE,
        update_user_in          IN waiting_list.update_user%TYPE DEFAULT NULL,
        update_user_nin         IN BOOLEAN := TRUE,
        update_time_in          IN waiting_list.update_time%TYPE DEFAULT NULL,
        update_time_nin         IN BOOLEAN := TRUE,
        update_institution_in   IN waiting_list.update_institution%TYPE DEFAULT NULL,
        update_institution_nin  IN BOOLEAN := TRUE,
        id_cancel_reason_in     IN waiting_list.id_cancel_reason%TYPE DEFAULT NULL,
        id_cancel_reason_nin    IN BOOLEAN := TRUE,
        notes_cancel_in         IN waiting_list.notes_cancel%TYPE DEFAULT NULL,
        notes_cancel_nin        IN BOOLEAN := TRUE,
        id_prof_cancel_in       IN waiting_list.id_prof_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_nin      IN BOOLEAN := TRUE,
        dt_cancel_in            IN waiting_list.dt_cancel%TYPE DEFAULT NULL,
        dt_cancel_nin           IN BOOLEAN := TRUE,
        id_external_request_in  IN waiting_list.id_external_request%TYPE DEFAULT NULL,
        id_external_request_nin IN BOOLEAN := TRUE,
        func_eval_score_in      IN waiting_list.func_eval_score%TYPE DEFAULT NULL,
        func_eval_score_nin     IN BOOLEAN := TRUE,
        notes_edit_in           IN waiting_list.notes_edit%TYPE DEFAULT NULL,
        notes_edit_nin          IN BOOLEAN := TRUE,
        handle_error_in         IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        id_patient_in           IN waiting_list.id_patient%TYPE DEFAULT NULL,
        id_patient_nin          IN BOOLEAN := TRUE,
        id_prof_req_in          IN waiting_list.id_prof_req%TYPE DEFAULT NULL,
        id_prof_req_nin         IN BOOLEAN := TRUE,
        dt_placement_in         IN waiting_list.dt_placement%TYPE DEFAULT NULL,
        dt_placement_nin        IN BOOLEAN := TRUE,
        flg_type_in             IN waiting_list.flg_type%TYPE DEFAULT NULL,
        flg_type_nin            IN BOOLEAN := TRUE,
        flg_status_in           IN waiting_list.flg_status%TYPE DEFAULT NULL,
        flg_status_nin          IN BOOLEAN := TRUE,
        dt_dpb_in               IN waiting_list.dt_dpb%TYPE DEFAULT NULL,
        dt_dpb_nin              IN BOOLEAN := TRUE,
        dt_dpa_in               IN waiting_list.dt_dpa%TYPE DEFAULT NULL,
        dt_dpa_nin              IN BOOLEAN := TRUE,
        dt_surgery_in           IN waiting_list.dt_surgery%TYPE DEFAULT NULL,
        dt_surgery_nin          IN BOOLEAN := TRUE,
        dt_admission_in         IN waiting_list.dt_admission%TYPE DEFAULT NULL,
        dt_admission_nin        IN BOOLEAN := TRUE,
        min_inform_time_in      IN waiting_list.min_inform_time%TYPE DEFAULT NULL,
        min_inform_time_nin     IN BOOLEAN := TRUE,
        id_wtl_urg_level_in     IN waiting_list.id_wtl_urg_level%TYPE DEFAULT NULL,
        id_wtl_urg_level_nin    IN BOOLEAN := TRUE,
        id_prof_reg_in          IN waiting_list.id_prof_reg%TYPE DEFAULT NULL,
        id_prof_reg_nin         IN BOOLEAN := TRUE,
        dt_reg_in               IN waiting_list.dt_reg%TYPE DEFAULT NULL,
        dt_reg_nin              IN BOOLEAN := TRUE,
        create_user_in          IN waiting_list.create_user%TYPE DEFAULT NULL,
        create_user_nin         IN BOOLEAN := TRUE,
        create_time_in          IN waiting_list.create_time%TYPE DEFAULT NULL,
        create_time_nin         IN BOOLEAN := TRUE,
        create_institution_in   IN waiting_list.create_institution%TYPE DEFAULT NULL,
        create_institution_nin  IN BOOLEAN := TRUE,
        update_user_in          IN waiting_list.update_user%TYPE DEFAULT NULL,
        update_user_nin         IN BOOLEAN := TRUE,
        update_time_in          IN waiting_list.update_time%TYPE DEFAULT NULL,
        update_time_nin         IN BOOLEAN := TRUE,
        update_institution_in   IN waiting_list.update_institution%TYPE DEFAULT NULL,
        update_institution_nin  IN BOOLEAN := TRUE,
        id_cancel_reason_in     IN waiting_list.id_cancel_reason%TYPE DEFAULT NULL,
        id_cancel_reason_nin    IN BOOLEAN := TRUE,
        notes_cancel_in         IN waiting_list.notes_cancel%TYPE DEFAULT NULL,
        notes_cancel_nin        IN BOOLEAN := TRUE,
        id_prof_cancel_in       IN waiting_list.id_prof_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_nin      IN BOOLEAN := TRUE,
        dt_cancel_in            IN waiting_list.dt_cancel%TYPE DEFAULT NULL,
        dt_cancel_nin           IN BOOLEAN := TRUE,
        id_external_request_in  IN waiting_list.id_external_request%TYPE DEFAULT NULL,
        id_external_request_nin IN BOOLEAN := TRUE,
        func_eval_score_in      IN waiting_list.func_eval_score%TYPE DEFAULT NULL,
        func_eval_score_nin     IN BOOLEAN := TRUE,
        notes_edit_in           IN waiting_list.notes_edit%TYPE DEFAULT NULL,
        notes_edit_nin          IN BOOLEAN := TRUE,
        where_in                VARCHAR2 DEFAULT NULL,
        handle_error_in         IN BOOLEAN := TRUE,
        rows_out                IN OUT table_varchar
    );

    PROCEDURE upd
    (
        id_patient_in           IN waiting_list.id_patient%TYPE DEFAULT NULL,
        id_patient_nin          IN BOOLEAN := TRUE,
        id_prof_req_in          IN waiting_list.id_prof_req%TYPE DEFAULT NULL,
        id_prof_req_nin         IN BOOLEAN := TRUE,
        dt_placement_in         IN waiting_list.dt_placement%TYPE DEFAULT NULL,
        dt_placement_nin        IN BOOLEAN := TRUE,
        flg_type_in             IN waiting_list.flg_type%TYPE DEFAULT NULL,
        flg_type_nin            IN BOOLEAN := TRUE,
        flg_status_in           IN waiting_list.flg_status%TYPE DEFAULT NULL,
        flg_status_nin          IN BOOLEAN := TRUE,
        dt_dpb_in               IN waiting_list.dt_dpb%TYPE DEFAULT NULL,
        dt_dpb_nin              IN BOOLEAN := TRUE,
        dt_dpa_in               IN waiting_list.dt_dpa%TYPE DEFAULT NULL,
        dt_dpa_nin              IN BOOLEAN := TRUE,
        dt_surgery_in           IN waiting_list.dt_surgery%TYPE DEFAULT NULL,
        dt_surgery_nin          IN BOOLEAN := TRUE,
        dt_admission_in         IN waiting_list.dt_admission%TYPE DEFAULT NULL,
        dt_admission_nin        IN BOOLEAN := TRUE,
        min_inform_time_in      IN waiting_list.min_inform_time%TYPE DEFAULT NULL,
        min_inform_time_nin     IN BOOLEAN := TRUE,
        id_wtl_urg_level_in     IN waiting_list.id_wtl_urg_level%TYPE DEFAULT NULL,
        id_wtl_urg_level_nin    IN BOOLEAN := TRUE,
        id_prof_reg_in          IN waiting_list.id_prof_reg%TYPE DEFAULT NULL,
        id_prof_reg_nin         IN BOOLEAN := TRUE,
        dt_reg_in               IN waiting_list.dt_reg%TYPE DEFAULT NULL,
        dt_reg_nin              IN BOOLEAN := TRUE,
        create_user_in          IN waiting_list.create_user%TYPE DEFAULT NULL,
        create_user_nin         IN BOOLEAN := TRUE,
        create_time_in          IN waiting_list.create_time%TYPE DEFAULT NULL,
        create_time_nin         IN BOOLEAN := TRUE,
        create_institution_in   IN waiting_list.create_institution%TYPE DEFAULT NULL,
        create_institution_nin  IN BOOLEAN := TRUE,
        update_user_in          IN waiting_list.update_user%TYPE DEFAULT NULL,
        update_user_nin         IN BOOLEAN := TRUE,
        update_time_in          IN waiting_list.update_time%TYPE DEFAULT NULL,
        update_time_nin         IN BOOLEAN := TRUE,
        update_institution_in   IN waiting_list.update_institution%TYPE DEFAULT NULL,
        update_institution_nin  IN BOOLEAN := TRUE,
        id_cancel_reason_in     IN waiting_list.id_cancel_reason%TYPE DEFAULT NULL,
        id_cancel_reason_nin    IN BOOLEAN := TRUE,
        notes_cancel_in         IN waiting_list.notes_cancel%TYPE DEFAULT NULL,
        notes_cancel_nin        IN BOOLEAN := TRUE,
        id_prof_cancel_in       IN waiting_list.id_prof_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_nin      IN BOOLEAN := TRUE,
        dt_cancel_in            IN waiting_list.dt_cancel%TYPE DEFAULT NULL,
        dt_cancel_nin           IN BOOLEAN := TRUE,
        id_external_request_in  IN waiting_list.id_external_request%TYPE DEFAULT NULL,
        id_external_request_nin IN BOOLEAN := TRUE,
        func_eval_score_in      IN waiting_list.func_eval_score%TYPE DEFAULT NULL,
        func_eval_score_nin     IN BOOLEAN := TRUE,
        notes_edit_in           IN waiting_list.notes_edit%TYPE DEFAULT NULL,
        notes_edit_nin          IN BOOLEAN := TRUE,
        where_in                VARCHAR2 DEFAULT NULL,
        handle_error_in         IN BOOLEAN := TRUE
    );

    PROCEDURE upd_ins
    (
        id_waiting_list_in     IN waiting_list.id_waiting_list%TYPE,
        id_patient_in          IN waiting_list.id_patient%TYPE DEFAULT NULL,
        id_prof_req_in         IN waiting_list.id_prof_req%TYPE DEFAULT NULL,
        dt_placement_in        IN waiting_list.dt_placement%TYPE DEFAULT NULL,
        flg_type_in            IN waiting_list.flg_type%TYPE DEFAULT NULL,
        flg_status_in          IN waiting_list.flg_status%TYPE DEFAULT NULL,
        dt_dpb_in              IN waiting_list.dt_dpb%TYPE DEFAULT NULL,
        dt_dpa_in              IN waiting_list.dt_dpa%TYPE DEFAULT NULL,
        dt_surgery_in          IN waiting_list.dt_surgery%TYPE DEFAULT NULL,
        dt_admission_in        IN waiting_list.dt_admission%TYPE DEFAULT NULL,
        min_inform_time_in     IN waiting_list.min_inform_time%TYPE DEFAULT NULL,
        id_wtl_urg_level_in    IN waiting_list.id_wtl_urg_level%TYPE DEFAULT NULL,
        id_prof_reg_in         IN waiting_list.id_prof_reg%TYPE DEFAULT NULL,
        dt_reg_in              IN waiting_list.dt_reg%TYPE DEFAULT NULL,
        create_user_in         IN waiting_list.create_user%TYPE DEFAULT NULL,
        create_time_in         IN waiting_list.create_time%TYPE DEFAULT NULL,
        create_institution_in  IN waiting_list.create_institution%TYPE DEFAULT NULL,
        update_user_in         IN waiting_list.update_user%TYPE DEFAULT NULL,
        update_time_in         IN waiting_list.update_time%TYPE DEFAULT NULL,
        update_institution_in  IN waiting_list.update_institution%TYPE DEFAULT NULL,
        id_cancel_reason_in    IN waiting_list.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in        IN waiting_list.notes_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_in      IN waiting_list.id_prof_cancel%TYPE DEFAULT NULL,
        dt_cancel_in           IN waiting_list.dt_cancel%TYPE DEFAULT NULL,
        id_external_request_in IN waiting_list.id_external_request%TYPE DEFAULT NULL,
        func_eval_score_in     IN waiting_list.func_eval_score%TYPE DEFAULT NULL,
        notes_edit_in          IN waiting_list.notes_edit%TYPE DEFAULT NULL,
        handle_error_in        IN BOOLEAN := TRUE,
        rows_out               OUT table_varchar
    );

    PROCEDURE upd_ins
    (
        id_waiting_list_in     IN waiting_list.id_waiting_list%TYPE,
        id_patient_in          IN waiting_list.id_patient%TYPE DEFAULT NULL,
        id_prof_req_in         IN waiting_list.id_prof_req%TYPE DEFAULT NULL,
        dt_placement_in        IN waiting_list.dt_placement%TYPE DEFAULT NULL,
        flg_type_in            IN waiting_list.flg_type%TYPE DEFAULT NULL,
        flg_status_in          IN waiting_list.flg_status%TYPE DEFAULT NULL,
        dt_dpb_in              IN waiting_list.dt_dpb%TYPE DEFAULT NULL,
        dt_dpa_in              IN waiting_list.dt_dpa%TYPE DEFAULT NULL,
        dt_surgery_in          IN waiting_list.dt_surgery%TYPE DEFAULT NULL,
        dt_admission_in        IN waiting_list.dt_admission%TYPE DEFAULT NULL,
        min_inform_time_in     IN waiting_list.min_inform_time%TYPE DEFAULT NULL,
        id_wtl_urg_level_in    IN waiting_list.id_wtl_urg_level%TYPE DEFAULT NULL,
        id_prof_reg_in         IN waiting_list.id_prof_reg%TYPE DEFAULT NULL,
        dt_reg_in              IN waiting_list.dt_reg%TYPE DEFAULT NULL,
        create_user_in         IN waiting_list.create_user%TYPE DEFAULT NULL,
        create_time_in         IN waiting_list.create_time%TYPE DEFAULT NULL,
        create_institution_in  IN waiting_list.create_institution%TYPE DEFAULT NULL,
        update_user_in         IN waiting_list.update_user%TYPE DEFAULT NULL,
        update_time_in         IN waiting_list.update_time%TYPE DEFAULT NULL,
        update_institution_in  IN waiting_list.update_institution%TYPE DEFAULT NULL,
        id_cancel_reason_in    IN waiting_list.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in        IN waiting_list.notes_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_in      IN waiting_list.id_prof_cancel%TYPE DEFAULT NULL,
        dt_cancel_in           IN waiting_list.dt_cancel%TYPE DEFAULT NULL,
        id_external_request_in IN waiting_list.id_external_request%TYPE DEFAULT NULL,
        func_eval_score_in     IN waiting_list.func_eval_score%TYPE DEFAULT NULL,
        notes_edit_in          IN waiting_list.notes_edit%TYPE DEFAULT NULL,
        handle_error_in        IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        rec_in          IN waiting_list%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        IN OUT table_varchar
    );

    PROCEDURE upd
    (
        rec_in          IN waiting_list%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    PROCEDURE upd
    (
        col_in            IN waiting_list_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          IN OUT table_varchar
    );

    PROCEDURE upd
    (
        col_in            IN waiting_list_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE
    );

    -- Use Native Dynamic SQL increment a single NUMBER column
    -- for all rows specified by the dynamic WHERE clause
    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_waiting_list_in IN waiting_list.id_waiting_list%TYPE,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_waiting_list_in IN waiting_list.id_waiting_list%TYPE,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    -- Delete all rows for primary key column ID_WAITING_LIST
    PROCEDURE del_id_waiting_list
    (
        id_waiting_list_in IN waiting_list.id_waiting_list%TYPE,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete all rows for primary key column ID_WAITING_LIST
    PROCEDURE del_id_waiting_list
    (
        id_waiting_list_in IN waiting_list.id_waiting_list%TYPE,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    -- Delete all rows for this WGHT_CRE_FK foreign key value
    PROCEDURE del_wght_cre_fk
    (
        id_cancel_reason_in IN waiting_list.id_cancel_reason%TYPE,
        handle_error_in     IN BOOLEAN := TRUE
    );

    -- Delete all rows for this WGHT_CRE_FK foreign key value
    PROCEDURE del_wght_cre_fk
    (
        id_cancel_reason_in IN waiting_list.id_cancel_reason%TYPE,
        handle_error_in     IN BOOLEAN := TRUE,
        rows_out            OUT table_varchar
    );

    -- Delete all rows for this WGLT_ERTX_FK foreign key value
    PROCEDURE del_wglt_ertx_fk
    (
        id_external_request_in IN waiting_list.id_external_request%TYPE,
        handle_error_in        IN BOOLEAN := TRUE
    );

    -- Delete all rows for this WGLT_ERTX_FK foreign key value
    PROCEDURE del_wglt_ertx_fk
    (
        id_external_request_in IN waiting_list.id_external_request%TYPE,
        handle_error_in        IN BOOLEAN := TRUE,
        rows_out               OUT table_varchar
    );

    -- Delete all rows for this WGLT_PAT_FK foreign key value
    PROCEDURE del_wglt_pat_fk
    (
        id_patient_in   IN waiting_list.id_patient%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this WGLT_PAT_FK foreign key value
    PROCEDURE del_wglt_pat_fk
    (
        id_patient_in   IN waiting_list.id_patient%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for this WGLT_PROF_FK foreign key value
    PROCEDURE del_wglt_prof_fk
    (
        id_prof_reg_in  IN waiting_list.id_prof_reg%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this WGLT_PROF_FK foreign key value
    PROCEDURE del_wglt_prof_fk
    (
        id_prof_reg_in  IN waiting_list.id_prof_reg%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for this WGLT_PROF_FK1 foreign key value
    PROCEDURE del_wglt_prof_fk1
    (
        id_prof_req_in  IN waiting_list.id_prof_req%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this WGLT_PROF_FK1 foreign key value
    PROCEDURE del_wglt_prof_fk1
    (
        id_prof_req_in  IN waiting_list.id_prof_req%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for this WGLT_PROF_FK2 foreign key value
    PROCEDURE del_wglt_prof_fk2
    (
        id_prof_cancel_in IN waiting_list.id_prof_cancel%TYPE,
        handle_error_in   IN BOOLEAN := TRUE
    );

    -- Delete all rows for this WGLT_PROF_FK2 foreign key value
    PROCEDURE del_wglt_prof_fk2
    (
        id_prof_cancel_in IN waiting_list.id_prof_cancel%TYPE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          OUT table_varchar
    );

    -- Delete all rows for this WGLT_WUL_FK foreign key value
    PROCEDURE del_wglt_wul_fk
    (
        id_wtl_urg_level_in IN waiting_list.id_wtl_urg_level%TYPE,
        handle_error_in     IN BOOLEAN := TRUE
    );

    -- Delete all rows for this WGLT_WUL_FK foreign key value
    PROCEDURE del_wglt_wul_fk
    (
        id_wtl_urg_level_in IN waiting_list.id_wtl_urg_level%TYPE,
        handle_error_in     IN BOOLEAN := TRUE,
        rows_out            OUT table_varchar
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified VARCHAR2 column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified DATE column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified TIMESTAMP column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows where the specified NUMBER column has
    -- a value that matches the specfified value.
    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Initialize a record with default values for columns in the table.
    PROCEDURE initrec(waiting_list_inout IN OUT waiting_list%ROWTYPE);

    FUNCTION initrec RETURN waiting_list%ROWTYPE;

    FUNCTION get_data_rowid(rows_in IN table_varchar) RETURN waiting_list_tc;

    FUNCTION get_data_rowid_pat(rows_in IN table_varchar) RETURN waiting_list_tc;

END ts_waiting_list;
/
