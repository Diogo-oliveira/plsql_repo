/*-- Last Change Revision: $Rev: 2029220 $*/
/*-- Last Change by: $Author: mario.fernandes $*/
/*-- Date of last change: $Date: 2022-08-02 18:50:27 +0100 (ter, 02 ago 2022) $*/

CREATE OR REPLACE PACKAGE TS_INTERV_EVALUATION_ICF
/*
| Generated by or retrieved from QCGU - DO NOT MODIFY!
| QCGU - "Get it right, do it fast" - www.ToadWorld.com
| QCGU Universal ID: {1BD37A66-EA60-4927-9A64-F6DD89237236}
| Created On: Janeiro 27, 2009 8:18:14
| Created By: ALERT
*/
IS

  -- Collection of %ROWTYPE records based on "INTERV_EVALUATION_ICF"
     TYPE INTERV_EVALUATION_ICF_tc IS TABLE OF INTERV_EVALUATION_ICF%ROWTYPE INDEX BY BINARY_INTEGER;
     TYPE interv_evaluation_icf_ntt IS TABLE OF INTERV_EVALUATION_ICF%ROWTYPE;
     TYPE interv_evaluation_icf_vat IS VARRAY(100) OF INTERV_EVALUATION_ICF%ROWTYPE;

  -- Same type structure, with a static name.
     TYPE aat IS TABLE OF INTERV_EVALUATION_ICF%ROWTYPE INDEX BY BINARY_INTEGER;
     TYPE ntt IS TABLE OF INTERV_EVALUATION_ICF%ROWTYPE;
     TYPE vat IS VARRAY(100) OF INTERV_EVALUATION_ICF%ROWTYPE;

   -- Column Collection based on column "ID_ICF"
   TYPE ID_ICF_cc IS TABLE OF INTERV_EVALUATION_ICF.ID_ICF%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "ID_INTERV_EVALUATION"
   TYPE ID_INTERV_EVALUATION_cc IS TABLE OF INTERV_EVALUATION_ICF.ID_INTERV_EVALUATION%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "NOTES"
   TYPE NOTES_cc IS TABLE OF INTERV_EVALUATION_ICF.NOTES%TYPE INDEX BY BINARY_INTEGER;

   -- Insert one row, providing primary key if present
   PROCEDURE ins (
      id_icf_in IN INTERV_EVALUATION_ICF.ID_ICF%TYPE,
      id_interv_evaluation_in IN INTERV_EVALUATION_ICF.ID_INTERV_EVALUATION%TYPE
      ,
      notes_in IN INTERV_EVALUATION_ICF.NOTES%TYPE DEFAULT NULL
      -- Pass false if you want errors to propagate out unhandled
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      );

   PROCEDURE ins (
      id_icf_in IN INTERV_EVALUATION_ICF.ID_ICF%TYPE,
      id_interv_evaluation_in IN INTERV_EVALUATION_ICF.ID_INTERV_EVALUATION%TYPE
      ,
      notes_in IN INTERV_EVALUATION_ICF.NOTES%TYPE DEFAULT NULL
      -- Pass false if you want errors to propagate out unhandled
     ,handle_error_in IN BOOLEAN := TRUE
      );


   -- Insert a row based on a record.
   PROCEDURE ins (
      rec_in IN INTERV_EVALUATION_ICF%ROWTYPE
     ,sequence_in IN VARCHAR2 := NULL
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   );

   PROCEDURE ins (
      rec_in IN INTERV_EVALUATION_ICF%ROWTYPE
     ,sequence_in IN VARCHAR2 := NULL
     ,handle_error_in IN BOOLEAN := TRUE
   );

   -- Insert a collection of rows using FORALL; all primary key values
   -- must have already been generated, or are handled in triggers.
   PROCEDURE ins (
      rows_in IN INTERV_EVALUATION_ICF_tc
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   );

   PROCEDURE ins (
      rows_in IN INTERV_EVALUATION_ICF_tc
     ,handle_error_in IN BOOLEAN := TRUE
   );

   -- Update any/all columns by primary key. If you pass NULL, then
   -- the current column value is set to itself. If you need a more
   -- selected UPDATE then use one of the onecol procedures below.

  PROCEDURE upd (
      id_icf_in IN INTERV_EVALUATION_ICF.ID_ICF%TYPE,
      id_interv_evaluation_in IN INTERV_EVALUATION_ICF.ID_INTERV_EVALUATION%TYPE,
      notes_in IN INTERV_EVALUATION_ICF.NOTES%TYPE DEFAULT NULL,
      NOTES_nin IN BOOLEAN := TRUE,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      );


       PROCEDURE upd (
      id_icf_in IN INTERV_EVALUATION_ICF.ID_ICF%TYPE,
      id_interv_evaluation_in IN INTERV_EVALUATION_ICF.ID_INTERV_EVALUATION%TYPE,
      notes_in IN INTERV_EVALUATION_ICF.NOTES%TYPE DEFAULT NULL,
      NOTES_nin IN BOOLEAN := TRUE,
     handle_error_in IN BOOLEAN := TRUE
      );



      PROCEDURE upd (
      notes_in IN INTERV_EVALUATION_ICF.NOTES%TYPE DEFAULT NULL,
      NOTES_nin IN BOOLEAN := TRUE,
    where_in varchar2 DEFAULT NULL,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      );


      PROCEDURE upd (
      notes_in IN INTERV_EVALUATION_ICF.NOTES%TYPE DEFAULT NULL,
      NOTES_nin IN BOOLEAN := TRUE,
    where_in varchar2 DEFAULT NULL,
     handle_error_in IN BOOLEAN := TRUE
      );



   PROCEDURE upd_ins (
      id_icf_in IN INTERV_EVALUATION_ICF.ID_ICF%TYPE,
      id_interv_evaluation_in IN INTERV_EVALUATION_ICF.ID_INTERV_EVALUATION%TYPE,
      notes_in IN INTERV_EVALUATION_ICF.NOTES%TYPE DEFAULT NULL,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      );

      PROCEDURE upd_ins (
      id_icf_in IN INTERV_EVALUATION_ICF.ID_ICF%TYPE,
      id_interv_evaluation_in IN INTERV_EVALUATION_ICF.ID_INTERV_EVALUATION%TYPE,
      notes_in IN INTERV_EVALUATION_ICF.NOTES%TYPE DEFAULT NULL,
     handle_error_in IN BOOLEAN := TRUE
      );

   PROCEDURE upd (
      rec_in IN INTERV_EVALUATION_ICF%ROWTYPE,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      );

      PROCEDURE upd (
      rec_in IN INTERV_EVALUATION_ICF%ROWTYPE,
     handle_error_in IN BOOLEAN := TRUE
      );

      PROCEDURE upd (
      col_in IN INTERV_EVALUATION_ICF_tc,
      ignore_if_null_in IN BOOLEAN := TRUE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      );

      PROCEDURE upd (
      col_in IN INTERV_EVALUATION_ICF_tc,
      ignore_if_null_in IN BOOLEAN := TRUE
     ,handle_error_in IN BOOLEAN := TRUE
      );





   -- Use Native Dynamic SQL increment a single NUMBER column
   -- for all rows specified by the dynamic WHERE clause
   PROCEDURE increment_onecol (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL
      , increment_value_in IN NUMBER DEFAULT 1
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   );

   PROCEDURE increment_onecol (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL
      , increment_value_in IN NUMBER DEFAULT 1
     ,handle_error_in IN BOOLEAN := TRUE
   );









    -- Delete one row by primary key
   PROCEDURE del (
      id_icf_in IN INTERV_EVALUATION_ICF.ID_ICF%TYPE,
      id_interv_evaluation_in IN INTERV_EVALUATION_ICF.ID_INTERV_EVALUATION%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      );

-- Delete one row by primary key
   PROCEDURE del (
      id_icf_in IN INTERV_EVALUATION_ICF.ID_ICF%TYPE,
      id_interv_evaluation_in IN INTERV_EVALUATION_ICF.ID_INTERV_EVALUATION%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      );


   -- Delete all rows for primary key column ID_ICF
   PROCEDURE del_ID_ICF (
      id_icf_in IN INTERV_EVALUATION_ICF.ID_ICF%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     );

     -- Delete all rows for primary key column ID_ICF
   PROCEDURE del_ID_ICF (
      id_icf_in IN INTERV_EVALUATION_ICF.ID_ICF%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
     );


   -- Delete all rows for primary key column ID_INTERV_EVALUATION
   PROCEDURE del_ID_INTERV_EVALUATION (
      id_interv_evaluation_in IN INTERV_EVALUATION_ICF.ID_INTERV_EVALUATION%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     );

     -- Delete all rows for primary key column ID_INTERV_EVALUATION
   PROCEDURE del_ID_INTERV_EVALUATION (
      id_interv_evaluation_in IN INTERV_EVALUATION_ICF.ID_INTERV_EVALUATION%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
     );

   -- Delete all rows for this IEI_ICF_FK foreign key value
   PROCEDURE del_IEI_ICF_FK (
      id_icf_in IN INTERV_EVALUATION_ICF.ID_ICF%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      );

      -- Delete all rows for this IEI_ICF_FK foreign key value
   PROCEDURE del_IEI_ICF_FK (
      id_icf_in IN INTERV_EVALUATION_ICF.ID_ICF%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      );

   -- Delete all rows for this IEI_IEND_FK foreign key value
   PROCEDURE del_IEI_IEND_FK (
      id_interv_evaluation_in IN INTERV_EVALUATION_ICF.ID_INTERV_EVALUATION%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      );

      -- Delete all rows for this IEI_IEND_FK foreign key value
   PROCEDURE del_IEI_IEND_FK (
      id_interv_evaluation_in IN INTERV_EVALUATION_ICF.ID_INTERV_EVALUATION%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      );

    -- Delete all rows specified by dynamic WHERE clause
   PROCEDURE del_by (
      where_clause_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
      );

      -- Delete all rows specified by dynamic WHERE clause
   PROCEDURE del_by (
      where_clause_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      );

    -- Delete all rows where the specified VARCHAR2 column has
   -- a value that matches the specfified value.
   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
      );


      -- Delete all rows where the specified VARCHAR2 column has
   -- a value that matches the specfified value.
   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      );

    -- Delete all rows where the specified DATE column has
   -- a value that matches the specfified value.
   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN DATE
     ,handle_error_in IN BOOLEAN := TRUE
      );

      -- Delete all rows where the specified DATE column has
   -- a value that matches the specfified value.
   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN DATE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      );


      -- Delete all rows where the specified TIMESTAMP column has
   -- a value that matches the specfified value.
   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN TIMESTAMP WITH LOCAL TIME ZONE
     ,handle_error_in IN BOOLEAN := TRUE
      );

      -- Delete all rows where the specified TIMESTAMP column has
   -- a value that matches the specfified value.
   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN TIMESTAMP WITH LOCAL TIME ZONE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      );

    -- Delete all rows where the specified NUMBER column has
   -- a value that matches the specfified value.
   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN NUMBER
     ,handle_error_in IN BOOLEAN := TRUE
      );

      -- Delete all rows where the specified NUMBER column has
   -- a value that matches the specfified value.
   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN NUMBER
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      );

   -- Initialize a record with default values for columns in the table.
   PROCEDURE initrec (
      interv_evaluation_icf_inout IN OUT INTERV_EVALUATION_ICF%ROWTYPE
   );

   FUNCTION initrec RETURN INTERV_EVALUATION_ICF%ROWTYPE;



   FUNCTION get_data_rowid(
        rows_in IN TABLE_VARCHAR
        ) RETURN INTERV_EVALUATION_ICF_tc;


   FUNCTION get_data_rowid_pat(
        rows_in IN TABLE_VARCHAR
        ) RETURN INTERV_EVALUATION_ICF_tc;

END TS_INTERV_EVALUATION_ICF;
/
