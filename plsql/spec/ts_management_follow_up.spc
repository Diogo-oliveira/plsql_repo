/*-- Last Change Revision: $Rev: 2029240 $*/
/*-- Last Change by: $Author: mario.fernandes $*/
/*-- Date of last change: $Date: 2022-08-02 18:50:34 +0100 (ter, 02 ago 2022) $*/
CREATE OR REPLACE PACKAGE ts_management_follow_up
/*
| Generated by or retrieved - DO NOT MODIFY!
| Created On: 2020-03-09 08:03:57
| Created By: ALERT
*/
 IS

    -- Collection of %ROWTYPE records based on management_follow_up
    TYPE management_follow_up_tc IS TABLE OF management_follow_up%ROWTYPE INDEX BY BINARY_INTEGER;
    TYPE management_follow_up_ntt IS TABLE OF management_follow_up%ROWTYPE;
    TYPE management_follow_up_vat IS VARRAY(100) OF management_follow_up%ROWTYPE;

    -- Column Collection based on column ID_MANAGEMENT_FOLLOW_UP
    TYPE id_management_follow_up_cc IS TABLE OF management_follow_up.id_management_follow_up%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_EPISODE
    TYPE id_episode_cc IS TABLE OF management_follow_up.id_episode%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_EPIS_ENCOUNTER
    TYPE id_epis_encounter_cc IS TABLE OF management_follow_up.id_epis_encounter%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column TIME_SPENT
    TYPE time_spent_cc IS TABLE OF management_follow_up.time_spent%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column FLG_STATUS
    TYPE flg_status_cc IS TABLE OF management_follow_up.flg_status%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_UNIT_TIME
    TYPE id_unit_time_cc IS TABLE OF management_follow_up.id_unit_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column DT_REGISTER
    TYPE dt_register_cc IS TABLE OF management_follow_up.dt_register%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column NOTES
    TYPE notes_cc IS TABLE OF management_follow_up.notes%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_CANCEL_REASON
    TYPE id_cancel_reason_cc IS TABLE OF management_follow_up.id_cancel_reason%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column NOTES_CANCEL
    TYPE notes_cancel_cc IS TABLE OF management_follow_up.notes_cancel%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_PARENT
    TYPE id_parent_cc IS TABLE OF management_follow_up.id_parent%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column CREATE_USER
    TYPE create_user_cc IS TABLE OF management_follow_up.create_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column CREATE_TIME
    TYPE create_time_cc IS TABLE OF management_follow_up.create_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column CREATE_INSTITUTION
    TYPE create_institution_cc IS TABLE OF management_follow_up.create_institution%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column UPDATE_USER
    TYPE update_user_cc IS TABLE OF management_follow_up.update_user%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column UPDATE_TIME
    TYPE update_time_cc IS TABLE OF management_follow_up.update_time%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column UPDATE_INSTITUTION
    TYPE update_institution_cc IS TABLE OF management_follow_up.update_institution%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_PROFESSIONAL
    TYPE id_professional_cc IS TABLE OF management_follow_up.id_professional%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column DT_START
    TYPE dt_start_cc IS TABLE OF management_follow_up.dt_start%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column DT_NEXT_ENCOUNTER
    TYPE dt_next_encounter_cc IS TABLE OF management_follow_up.dt_next_encounter%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column FLG_END_FOLLOWUP
    TYPE flg_end_followup_cc IS TABLE OF management_follow_up.flg_end_followup%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_OPINION_TYPE
    TYPE id_opinion_type_cc IS TABLE OF management_follow_up.id_opinion_type%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column DT_NEXT_ENC_PRECISION
    TYPE dt_next_enc_precision_cc IS TABLE OF management_follow_up.dt_next_enc_precision%TYPE INDEX BY BINARY_INTEGER;
    -- Column Collection based on column ID_OPINION
    TYPE id_opinion_cc IS TABLE OF management_follow_up.id_opinion%TYPE INDEX BY BINARY_INTEGER;

    -- Insert one row, providing primary key if present (with rows_out)
    PROCEDURE ins
    (
        id_management_follow_up_in IN management_follow_up.id_management_follow_up%TYPE,
        id_episode_in              IN management_follow_up.id_episode%TYPE DEFAULT NULL,
        id_epis_encounter_in       IN management_follow_up.id_epis_encounter%TYPE DEFAULT NULL,
        time_spent_in              IN management_follow_up.time_spent%TYPE DEFAULT NULL,
        flg_status_in              IN management_follow_up.flg_status%TYPE DEFAULT NULL,
        id_unit_time_in            IN management_follow_up.id_unit_time%TYPE DEFAULT NULL,
        dt_register_in             IN management_follow_up.dt_register%TYPE DEFAULT NULL,
        notes_in                   IN management_follow_up.notes%TYPE DEFAULT NULL,
        id_cancel_reason_in        IN management_follow_up.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in            IN management_follow_up.notes_cancel%TYPE DEFAULT NULL,
        id_parent_in               IN management_follow_up.id_parent%TYPE DEFAULT NULL,
        create_user_in             IN management_follow_up.create_user%TYPE DEFAULT NULL,
        create_time_in             IN management_follow_up.create_time%TYPE DEFAULT NULL,
        create_institution_in      IN management_follow_up.create_institution%TYPE DEFAULT NULL,
        update_user_in             IN management_follow_up.update_user%TYPE DEFAULT NULL,
        update_time_in             IN management_follow_up.update_time%TYPE DEFAULT NULL,
        update_institution_in      IN management_follow_up.update_institution%TYPE DEFAULT NULL,
        id_professional_in         IN management_follow_up.id_professional%TYPE DEFAULT NULL,
        dt_start_in                IN management_follow_up.dt_start%TYPE DEFAULT NULL,
        dt_next_encounter_in       IN management_follow_up.dt_next_encounter%TYPE DEFAULT NULL,
        flg_end_followup_in        IN management_follow_up.flg_end_followup%TYPE DEFAULT NULL,
        id_opinion_type_in         IN management_follow_up.id_opinion_type%TYPE DEFAULT NULL,
        dt_next_enc_precision_in   IN management_follow_up.dt_next_enc_precision%TYPE DEFAULT NULL,
        id_opinion_in              IN management_follow_up.id_opinion%TYPE DEFAULT NULL,
        handle_error_in            IN BOOLEAN := TRUE,
        rows_out                   OUT table_varchar
    );

    -- Insert one row, providing primary key if present (without rows_out)
    PROCEDURE ins
    (
        id_management_follow_up_in IN management_follow_up.id_management_follow_up%TYPE,
        id_episode_in              IN management_follow_up.id_episode%TYPE DEFAULT NULL,
        id_epis_encounter_in       IN management_follow_up.id_epis_encounter%TYPE DEFAULT NULL,
        time_spent_in              IN management_follow_up.time_spent%TYPE DEFAULT NULL,
        flg_status_in              IN management_follow_up.flg_status%TYPE DEFAULT NULL,
        id_unit_time_in            IN management_follow_up.id_unit_time%TYPE DEFAULT NULL,
        dt_register_in             IN management_follow_up.dt_register%TYPE DEFAULT NULL,
        notes_in                   IN management_follow_up.notes%TYPE DEFAULT NULL,
        id_cancel_reason_in        IN management_follow_up.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in            IN management_follow_up.notes_cancel%TYPE DEFAULT NULL,
        id_parent_in               IN management_follow_up.id_parent%TYPE DEFAULT NULL,
        create_user_in             IN management_follow_up.create_user%TYPE DEFAULT NULL,
        create_time_in             IN management_follow_up.create_time%TYPE DEFAULT NULL,
        create_institution_in      IN management_follow_up.create_institution%TYPE DEFAULT NULL,
        update_user_in             IN management_follow_up.update_user%TYPE DEFAULT NULL,
        update_time_in             IN management_follow_up.update_time%TYPE DEFAULT NULL,
        update_institution_in      IN management_follow_up.update_institution%TYPE DEFAULT NULL,
        id_professional_in         IN management_follow_up.id_professional%TYPE DEFAULT NULL,
        dt_start_in                IN management_follow_up.dt_start%TYPE DEFAULT NULL,
        dt_next_encounter_in       IN management_follow_up.dt_next_encounter%TYPE DEFAULT NULL,
        flg_end_followup_in        IN management_follow_up.flg_end_followup%TYPE DEFAULT NULL,
        id_opinion_type_in         IN management_follow_up.id_opinion_type%TYPE DEFAULT NULL,
        dt_next_enc_precision_in   IN management_follow_up.dt_next_enc_precision%TYPE DEFAULT NULL,
        id_opinion_in              IN management_follow_up.id_opinion%TYPE DEFAULT NULL,
        handle_error_in            IN BOOLEAN := TRUE
    );

    -- Insert a row based on a record
    -- Specify whether or not a primary key value should be generated
    PROCEDURE ins
    (
        rec_in          IN management_follow_up%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Insert a row based on a record
    -- Specify whether or not a primary key value should be generated
    PROCEDURE ins
    (
        rec_in          IN management_follow_up%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert a collection of rows using FORALL; all primary key values
    -- must have already been generated, or are handled in triggers
    PROCEDURE ins
    (
        rows_in         IN management_follow_up_tc,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Insert a collection of rows using FORALL; all primary key values
    -- must have already been generated, or are handled in triggers
    PROCEDURE ins
    (
        rows_in         IN management_follow_up_tc,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Return next primary key value using the named sequence
    FUNCTION next_key(sequence_in IN VARCHAR2 := NULL) RETURN management_follow_up.id_management_follow_up%TYPE;

    -- Insert one row, generating hidden primary key using a sequence
    PROCEDURE ins
    (
        id_episode_in            IN management_follow_up.id_episode%TYPE DEFAULT NULL,
        id_epis_encounter_in     IN management_follow_up.id_epis_encounter%TYPE DEFAULT NULL,
        time_spent_in            IN management_follow_up.time_spent%TYPE DEFAULT NULL,
        flg_status_in            IN management_follow_up.flg_status%TYPE DEFAULT NULL,
        id_unit_time_in          IN management_follow_up.id_unit_time%TYPE DEFAULT NULL,
        dt_register_in           IN management_follow_up.dt_register%TYPE DEFAULT NULL,
        notes_in                 IN management_follow_up.notes%TYPE DEFAULT NULL,
        id_cancel_reason_in      IN management_follow_up.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in          IN management_follow_up.notes_cancel%TYPE DEFAULT NULL,
        id_parent_in             IN management_follow_up.id_parent%TYPE DEFAULT NULL,
        create_user_in           IN management_follow_up.create_user%TYPE DEFAULT NULL,
        create_time_in           IN management_follow_up.create_time%TYPE DEFAULT NULL,
        create_institution_in    IN management_follow_up.create_institution%TYPE DEFAULT NULL,
        update_user_in           IN management_follow_up.update_user%TYPE DEFAULT NULL,
        update_time_in           IN management_follow_up.update_time%TYPE DEFAULT NULL,
        update_institution_in    IN management_follow_up.update_institution%TYPE DEFAULT NULL,
        id_professional_in       IN management_follow_up.id_professional%TYPE DEFAULT NULL,
        dt_start_in              IN management_follow_up.dt_start%TYPE DEFAULT NULL,
        dt_next_encounter_in     IN management_follow_up.dt_next_encounter%TYPE DEFAULT NULL,
        flg_end_followup_in      IN management_follow_up.flg_end_followup%TYPE DEFAULT NULL,
        id_opinion_type_in       IN management_follow_up.id_opinion_type%TYPE DEFAULT NULL,
        dt_next_enc_precision_in IN management_follow_up.dt_next_enc_precision%TYPE DEFAULT NULL,
        id_opinion_in            IN management_follow_up.id_opinion%TYPE DEFAULT NULL,
        -- Pass false if you want errors to propagate out unhandled
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Insert one row, generating hidden primary key using a sequence
    PROCEDURE ins
    (
        id_episode_in            IN management_follow_up.id_episode%TYPE DEFAULT NULL,
        id_epis_encounter_in     IN management_follow_up.id_epis_encounter%TYPE DEFAULT NULL,
        time_spent_in            IN management_follow_up.time_spent%TYPE DEFAULT NULL,
        flg_status_in            IN management_follow_up.flg_status%TYPE DEFAULT NULL,
        id_unit_time_in          IN management_follow_up.id_unit_time%TYPE DEFAULT NULL,
        dt_register_in           IN management_follow_up.dt_register%TYPE DEFAULT NULL,
        notes_in                 IN management_follow_up.notes%TYPE DEFAULT NULL,
        id_cancel_reason_in      IN management_follow_up.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in          IN management_follow_up.notes_cancel%TYPE DEFAULT NULL,
        id_parent_in             IN management_follow_up.id_parent%TYPE DEFAULT NULL,
        create_user_in           IN management_follow_up.create_user%TYPE DEFAULT NULL,
        create_time_in           IN management_follow_up.create_time%TYPE DEFAULT NULL,
        create_institution_in    IN management_follow_up.create_institution%TYPE DEFAULT NULL,
        update_user_in           IN management_follow_up.update_user%TYPE DEFAULT NULL,
        update_time_in           IN management_follow_up.update_time%TYPE DEFAULT NULL,
        update_institution_in    IN management_follow_up.update_institution%TYPE DEFAULT NULL,
        id_professional_in       IN management_follow_up.id_professional%TYPE DEFAULT NULL,
        dt_start_in              IN management_follow_up.dt_start%TYPE DEFAULT NULL,
        dt_next_encounter_in     IN management_follow_up.dt_next_encounter%TYPE DEFAULT NULL,
        flg_end_followup_in      IN management_follow_up.flg_end_followup%TYPE DEFAULT NULL,
        id_opinion_type_in       IN management_follow_up.id_opinion_type%TYPE DEFAULT NULL,
        dt_next_enc_precision_in IN management_follow_up.dt_next_enc_precision%TYPE DEFAULT NULL,
        id_opinion_in            IN management_follow_up.id_opinion%TYPE DEFAULT NULL,
        -- Pass false if you want errors to propagate out unhandled
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert one row, returning primary key generated by sequence
    PROCEDURE ins
    (
        id_episode_in               IN management_follow_up.id_episode%TYPE DEFAULT NULL,
        id_epis_encounter_in        IN management_follow_up.id_epis_encounter%TYPE DEFAULT NULL,
        time_spent_in               IN management_follow_up.time_spent%TYPE DEFAULT NULL,
        flg_status_in               IN management_follow_up.flg_status%TYPE DEFAULT NULL,
        id_unit_time_in             IN management_follow_up.id_unit_time%TYPE DEFAULT NULL,
        dt_register_in              IN management_follow_up.dt_register%TYPE DEFAULT NULL,
        notes_in                    IN management_follow_up.notes%TYPE DEFAULT NULL,
        id_cancel_reason_in         IN management_follow_up.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in             IN management_follow_up.notes_cancel%TYPE DEFAULT NULL,
        id_parent_in                IN management_follow_up.id_parent%TYPE DEFAULT NULL,
        create_user_in              IN management_follow_up.create_user%TYPE DEFAULT NULL,
        create_time_in              IN management_follow_up.create_time%TYPE DEFAULT NULL,
        create_institution_in       IN management_follow_up.create_institution%TYPE DEFAULT NULL,
        update_user_in              IN management_follow_up.update_user%TYPE DEFAULT NULL,
        update_time_in              IN management_follow_up.update_time%TYPE DEFAULT NULL,
        update_institution_in       IN management_follow_up.update_institution%TYPE DEFAULT NULL,
        id_professional_in          IN management_follow_up.id_professional%TYPE DEFAULT NULL,
        dt_start_in                 IN management_follow_up.dt_start%TYPE DEFAULT NULL,
        dt_next_encounter_in        IN management_follow_up.dt_next_encounter%TYPE DEFAULT NULL,
        flg_end_followup_in         IN management_follow_up.flg_end_followup%TYPE DEFAULT NULL,
        id_opinion_type_in          IN management_follow_up.id_opinion_type%TYPE DEFAULT NULL,
        dt_next_enc_precision_in    IN management_follow_up.dt_next_enc_precision%TYPE DEFAULT NULL,
        id_opinion_in               IN management_follow_up.id_opinion%TYPE DEFAULT NULL,
        id_management_follow_up_out IN OUT management_follow_up.id_management_follow_up%TYPE,
        -- Pass false if you want errors to propagate out unhandled
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Insert one row, returning primary key generated by sequence
    PROCEDURE ins
    (
        id_episode_in               IN management_follow_up.id_episode%TYPE DEFAULT NULL,
        id_epis_encounter_in        IN management_follow_up.id_epis_encounter%TYPE DEFAULT NULL,
        time_spent_in               IN management_follow_up.time_spent%TYPE DEFAULT NULL,
        flg_status_in               IN management_follow_up.flg_status%TYPE DEFAULT NULL,
        id_unit_time_in             IN management_follow_up.id_unit_time%TYPE DEFAULT NULL,
        dt_register_in              IN management_follow_up.dt_register%TYPE DEFAULT NULL,
        notes_in                    IN management_follow_up.notes%TYPE DEFAULT NULL,
        id_cancel_reason_in         IN management_follow_up.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in             IN management_follow_up.notes_cancel%TYPE DEFAULT NULL,
        id_parent_in                IN management_follow_up.id_parent%TYPE DEFAULT NULL,
        create_user_in              IN management_follow_up.create_user%TYPE DEFAULT NULL,
        create_time_in              IN management_follow_up.create_time%TYPE DEFAULT NULL,
        create_institution_in       IN management_follow_up.create_institution%TYPE DEFAULT NULL,
        update_user_in              IN management_follow_up.update_user%TYPE DEFAULT NULL,
        update_time_in              IN management_follow_up.update_time%TYPE DEFAULT NULL,
        update_institution_in       IN management_follow_up.update_institution%TYPE DEFAULT NULL,
        id_professional_in          IN management_follow_up.id_professional%TYPE DEFAULT NULL,
        dt_start_in                 IN management_follow_up.dt_start%TYPE DEFAULT NULL,
        dt_next_encounter_in        IN management_follow_up.dt_next_encounter%TYPE DEFAULT NULL,
        flg_end_followup_in         IN management_follow_up.flg_end_followup%TYPE DEFAULT NULL,
        id_opinion_type_in          IN management_follow_up.id_opinion_type%TYPE DEFAULT NULL,
        dt_next_enc_precision_in    IN management_follow_up.dt_next_enc_precision%TYPE DEFAULT NULL,
        id_opinion_in               IN management_follow_up.id_opinion%TYPE DEFAULT NULL,
        id_management_follow_up_out IN OUT management_follow_up.id_management_follow_up%TYPE,
        -- Pass false if you want errors to propagate out unhandled
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Insert one row with function, return generated primary key
    FUNCTION ins
    (
        id_episode_in            IN management_follow_up.id_episode%TYPE DEFAULT NULL,
        id_epis_encounter_in     IN management_follow_up.id_epis_encounter%TYPE DEFAULT NULL,
        time_spent_in            IN management_follow_up.time_spent%TYPE DEFAULT NULL,
        flg_status_in            IN management_follow_up.flg_status%TYPE DEFAULT NULL,
        id_unit_time_in          IN management_follow_up.id_unit_time%TYPE DEFAULT NULL,
        dt_register_in           IN management_follow_up.dt_register%TYPE DEFAULT NULL,
        notes_in                 IN management_follow_up.notes%TYPE DEFAULT NULL,
        id_cancel_reason_in      IN management_follow_up.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in          IN management_follow_up.notes_cancel%TYPE DEFAULT NULL,
        id_parent_in             IN management_follow_up.id_parent%TYPE DEFAULT NULL,
        create_user_in           IN management_follow_up.create_user%TYPE DEFAULT NULL,
        create_time_in           IN management_follow_up.create_time%TYPE DEFAULT NULL,
        create_institution_in    IN management_follow_up.create_institution%TYPE DEFAULT NULL,
        update_user_in           IN management_follow_up.update_user%TYPE DEFAULT NULL,
        update_time_in           IN management_follow_up.update_time%TYPE DEFAULT NULL,
        update_institution_in    IN management_follow_up.update_institution%TYPE DEFAULT NULL,
        id_professional_in       IN management_follow_up.id_professional%TYPE DEFAULT NULL,
        dt_start_in              IN management_follow_up.dt_start%TYPE DEFAULT NULL,
        dt_next_encounter_in     IN management_follow_up.dt_next_encounter%TYPE DEFAULT NULL,
        flg_end_followup_in      IN management_follow_up.flg_end_followup%TYPE DEFAULT NULL,
        id_opinion_type_in       IN management_follow_up.id_opinion_type%TYPE DEFAULT NULL,
        dt_next_enc_precision_in IN management_follow_up.dt_next_enc_precision%TYPE DEFAULT NULL,
        id_opinion_in            IN management_follow_up.id_opinion%TYPE DEFAULT NULL,
        -- Pass false if you want errors to propagate out unhandled
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) RETURN management_follow_up.id_management_follow_up%TYPE;

    -- Insert one row with function, return generated primary key
    FUNCTION ins
    (
        id_episode_in            IN management_follow_up.id_episode%TYPE DEFAULT NULL,
        id_epis_encounter_in     IN management_follow_up.id_epis_encounter%TYPE DEFAULT NULL,
        time_spent_in            IN management_follow_up.time_spent%TYPE DEFAULT NULL,
        flg_status_in            IN management_follow_up.flg_status%TYPE DEFAULT NULL,
        id_unit_time_in          IN management_follow_up.id_unit_time%TYPE DEFAULT NULL,
        dt_register_in           IN management_follow_up.dt_register%TYPE DEFAULT NULL,
        notes_in                 IN management_follow_up.notes%TYPE DEFAULT NULL,
        id_cancel_reason_in      IN management_follow_up.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in          IN management_follow_up.notes_cancel%TYPE DEFAULT NULL,
        id_parent_in             IN management_follow_up.id_parent%TYPE DEFAULT NULL,
        create_user_in           IN management_follow_up.create_user%TYPE DEFAULT NULL,
        create_time_in           IN management_follow_up.create_time%TYPE DEFAULT NULL,
        create_institution_in    IN management_follow_up.create_institution%TYPE DEFAULT NULL,
        update_user_in           IN management_follow_up.update_user%TYPE DEFAULT NULL,
        update_time_in           IN management_follow_up.update_time%TYPE DEFAULT NULL,
        update_institution_in    IN management_follow_up.update_institution%TYPE DEFAULT NULL,
        id_professional_in       IN management_follow_up.id_professional%TYPE DEFAULT NULL,
        dt_start_in              IN management_follow_up.dt_start%TYPE DEFAULT NULL,
        dt_next_encounter_in     IN management_follow_up.dt_next_encounter%TYPE DEFAULT NULL,
        flg_end_followup_in      IN management_follow_up.flg_end_followup%TYPE DEFAULT NULL,
        id_opinion_type_in       IN management_follow_up.id_opinion_type%TYPE DEFAULT NULL,
        dt_next_enc_precision_in IN management_follow_up.dt_next_enc_precision%TYPE DEFAULT NULL,
        id_opinion_in            IN management_follow_up.id_opinion%TYPE DEFAULT NULL,
        -- Pass false if you want errors to propagate out unhandled
        handle_error_in IN BOOLEAN := TRUE
    ) RETURN management_follow_up.id_management_follow_up%TYPE;

    -- Update any/all columns by primary key. If you pass NULL, then
    -- the current column value is set to itself. If you need a more
    -- selected UPDATE then use one of the onecol procedures below.
    PROCEDURE upd
    (
        id_management_follow_up_in IN management_follow_up.id_management_follow_up%TYPE,
        id_episode_in              IN management_follow_up.id_episode%TYPE DEFAULT NULL,
        id_episode_nin             IN BOOLEAN := TRUE,
        id_epis_encounter_in       IN management_follow_up.id_epis_encounter%TYPE DEFAULT NULL,
        id_epis_encounter_nin      IN BOOLEAN := TRUE,
        time_spent_in              IN management_follow_up.time_spent%TYPE DEFAULT NULL,
        time_spent_nin             IN BOOLEAN := TRUE,
        flg_status_in              IN management_follow_up.flg_status%TYPE DEFAULT NULL,
        flg_status_nin             IN BOOLEAN := TRUE,
        id_unit_time_in            IN management_follow_up.id_unit_time%TYPE DEFAULT NULL,
        id_unit_time_nin           IN BOOLEAN := TRUE,
        dt_register_in             IN management_follow_up.dt_register%TYPE DEFAULT NULL,
        dt_register_nin            IN BOOLEAN := TRUE,
        notes_in                   IN management_follow_up.notes%TYPE DEFAULT NULL,
        notes_nin                  IN BOOLEAN := TRUE,
        id_cancel_reason_in        IN management_follow_up.id_cancel_reason%TYPE DEFAULT NULL,
        id_cancel_reason_nin       IN BOOLEAN := TRUE,
        notes_cancel_in            IN management_follow_up.notes_cancel%TYPE DEFAULT NULL,
        notes_cancel_nin           IN BOOLEAN := TRUE,
        id_parent_in               IN management_follow_up.id_parent%TYPE DEFAULT NULL,
        id_parent_nin              IN BOOLEAN := TRUE,
        create_user_in             IN management_follow_up.create_user%TYPE DEFAULT NULL,
        create_user_nin            IN BOOLEAN := TRUE,
        create_time_in             IN management_follow_up.create_time%TYPE DEFAULT NULL,
        create_time_nin            IN BOOLEAN := TRUE,
        create_institution_in      IN management_follow_up.create_institution%TYPE DEFAULT NULL,
        create_institution_nin     IN BOOLEAN := TRUE,
        update_user_in             IN management_follow_up.update_user%TYPE DEFAULT NULL,
        update_user_nin            IN BOOLEAN := TRUE,
        update_time_in             IN management_follow_up.update_time%TYPE DEFAULT NULL,
        update_time_nin            IN BOOLEAN := TRUE,
        update_institution_in      IN management_follow_up.update_institution%TYPE DEFAULT NULL,
        update_institution_nin     IN BOOLEAN := TRUE,
        id_professional_in         IN management_follow_up.id_professional%TYPE DEFAULT NULL,
        id_professional_nin        IN BOOLEAN := TRUE,
        dt_start_in                IN management_follow_up.dt_start%TYPE DEFAULT NULL,
        dt_start_nin               IN BOOLEAN := TRUE,
        dt_next_encounter_in       IN management_follow_up.dt_next_encounter%TYPE DEFAULT NULL,
        dt_next_encounter_nin      IN BOOLEAN := TRUE,
        flg_end_followup_in        IN management_follow_up.flg_end_followup%TYPE DEFAULT NULL,
        flg_end_followup_nin       IN BOOLEAN := TRUE,
        id_opinion_type_in         IN management_follow_up.id_opinion_type%TYPE DEFAULT NULL,
        id_opinion_type_nin        IN BOOLEAN := TRUE,
        dt_next_enc_precision_in   IN management_follow_up.dt_next_enc_precision%TYPE DEFAULT NULL,
        dt_next_enc_precision_nin  IN BOOLEAN := TRUE,
        id_opinion_in              IN management_follow_up.id_opinion%TYPE DEFAULT NULL,
        id_opinion_nin             IN BOOLEAN := TRUE,
        handle_error_in            IN BOOLEAN := TRUE,
        rows_out                   IN OUT table_varchar
    );

    -- Update any/all columns by primary key. If you pass NULL, then
    -- the current column value is set to itself. If you need a more
    -- selected UPDATE then use one of the onecol procedures below.
    PROCEDURE upd
    (
        id_management_follow_up_in IN management_follow_up.id_management_follow_up%TYPE,
        id_episode_in              IN management_follow_up.id_episode%TYPE DEFAULT NULL,
        id_episode_nin             IN BOOLEAN := TRUE,
        id_epis_encounter_in       IN management_follow_up.id_epis_encounter%TYPE DEFAULT NULL,
        id_epis_encounter_nin      IN BOOLEAN := TRUE,
        time_spent_in              IN management_follow_up.time_spent%TYPE DEFAULT NULL,
        time_spent_nin             IN BOOLEAN := TRUE,
        flg_status_in              IN management_follow_up.flg_status%TYPE DEFAULT NULL,
        flg_status_nin             IN BOOLEAN := TRUE,
        id_unit_time_in            IN management_follow_up.id_unit_time%TYPE DEFAULT NULL,
        id_unit_time_nin           IN BOOLEAN := TRUE,
        dt_register_in             IN management_follow_up.dt_register%TYPE DEFAULT NULL,
        dt_register_nin            IN BOOLEAN := TRUE,
        notes_in                   IN management_follow_up.notes%TYPE DEFAULT NULL,
        notes_nin                  IN BOOLEAN := TRUE,
        id_cancel_reason_in        IN management_follow_up.id_cancel_reason%TYPE DEFAULT NULL,
        id_cancel_reason_nin       IN BOOLEAN := TRUE,
        notes_cancel_in            IN management_follow_up.notes_cancel%TYPE DEFAULT NULL,
        notes_cancel_nin           IN BOOLEAN := TRUE,
        id_parent_in               IN management_follow_up.id_parent%TYPE DEFAULT NULL,
        id_parent_nin              IN BOOLEAN := TRUE,
        create_user_in             IN management_follow_up.create_user%TYPE DEFAULT NULL,
        create_user_nin            IN BOOLEAN := TRUE,
        create_time_in             IN management_follow_up.create_time%TYPE DEFAULT NULL,
        create_time_nin            IN BOOLEAN := TRUE,
        create_institution_in      IN management_follow_up.create_institution%TYPE DEFAULT NULL,
        create_institution_nin     IN BOOLEAN := TRUE,
        update_user_in             IN management_follow_up.update_user%TYPE DEFAULT NULL,
        update_user_nin            IN BOOLEAN := TRUE,
        update_time_in             IN management_follow_up.update_time%TYPE DEFAULT NULL,
        update_time_nin            IN BOOLEAN := TRUE,
        update_institution_in      IN management_follow_up.update_institution%TYPE DEFAULT NULL,
        update_institution_nin     IN BOOLEAN := TRUE,
        id_professional_in         IN management_follow_up.id_professional%TYPE DEFAULT NULL,
        id_professional_nin        IN BOOLEAN := TRUE,
        dt_start_in                IN management_follow_up.dt_start%TYPE DEFAULT NULL,
        dt_start_nin               IN BOOLEAN := TRUE,
        dt_next_encounter_in       IN management_follow_up.dt_next_encounter%TYPE DEFAULT NULL,
        dt_next_encounter_nin      IN BOOLEAN := TRUE,
        flg_end_followup_in        IN management_follow_up.flg_end_followup%TYPE DEFAULT NULL,
        flg_end_followup_nin       IN BOOLEAN := TRUE,
        id_opinion_type_in         IN management_follow_up.id_opinion_type%TYPE DEFAULT NULL,
        id_opinion_type_nin        IN BOOLEAN := TRUE,
        dt_next_enc_precision_in   IN management_follow_up.dt_next_enc_precision%TYPE DEFAULT NULL,
        dt_next_enc_precision_nin  IN BOOLEAN := TRUE,
        id_opinion_in              IN management_follow_up.id_opinion%TYPE DEFAULT NULL,
        id_opinion_nin             IN BOOLEAN := TRUE,
        handle_error_in            IN BOOLEAN := TRUE
    );

    --Update any/all columns by dynamic WHERE
    -- If you pass NULL, then the current column value is set to itself
    PROCEDURE upd
    (
        id_episode_in             IN management_follow_up.id_episode%TYPE DEFAULT NULL,
        id_episode_nin            IN BOOLEAN := TRUE,
        id_epis_encounter_in      IN management_follow_up.id_epis_encounter%TYPE DEFAULT NULL,
        id_epis_encounter_nin     IN BOOLEAN := TRUE,
        time_spent_in             IN management_follow_up.time_spent%TYPE DEFAULT NULL,
        time_spent_nin            IN BOOLEAN := TRUE,
        flg_status_in             IN management_follow_up.flg_status%TYPE DEFAULT NULL,
        flg_status_nin            IN BOOLEAN := TRUE,
        id_unit_time_in           IN management_follow_up.id_unit_time%TYPE DEFAULT NULL,
        id_unit_time_nin          IN BOOLEAN := TRUE,
        dt_register_in            IN management_follow_up.dt_register%TYPE DEFAULT NULL,
        dt_register_nin           IN BOOLEAN := TRUE,
        notes_in                  IN management_follow_up.notes%TYPE DEFAULT NULL,
        notes_nin                 IN BOOLEAN := TRUE,
        id_cancel_reason_in       IN management_follow_up.id_cancel_reason%TYPE DEFAULT NULL,
        id_cancel_reason_nin      IN BOOLEAN := TRUE,
        notes_cancel_in           IN management_follow_up.notes_cancel%TYPE DEFAULT NULL,
        notes_cancel_nin          IN BOOLEAN := TRUE,
        id_parent_in              IN management_follow_up.id_parent%TYPE DEFAULT NULL,
        id_parent_nin             IN BOOLEAN := TRUE,
        create_user_in            IN management_follow_up.create_user%TYPE DEFAULT NULL,
        create_user_nin           IN BOOLEAN := TRUE,
        create_time_in            IN management_follow_up.create_time%TYPE DEFAULT NULL,
        create_time_nin           IN BOOLEAN := TRUE,
        create_institution_in     IN management_follow_up.create_institution%TYPE DEFAULT NULL,
        create_institution_nin    IN BOOLEAN := TRUE,
        update_user_in            IN management_follow_up.update_user%TYPE DEFAULT NULL,
        update_user_nin           IN BOOLEAN := TRUE,
        update_time_in            IN management_follow_up.update_time%TYPE DEFAULT NULL,
        update_time_nin           IN BOOLEAN := TRUE,
        update_institution_in     IN management_follow_up.update_institution%TYPE DEFAULT NULL,
        update_institution_nin    IN BOOLEAN := TRUE,
        id_professional_in        IN management_follow_up.id_professional%TYPE DEFAULT NULL,
        id_professional_nin       IN BOOLEAN := TRUE,
        dt_start_in               IN management_follow_up.dt_start%TYPE DEFAULT NULL,
        dt_start_nin              IN BOOLEAN := TRUE,
        dt_next_encounter_in      IN management_follow_up.dt_next_encounter%TYPE DEFAULT NULL,
        dt_next_encounter_nin     IN BOOLEAN := TRUE,
        flg_end_followup_in       IN management_follow_up.flg_end_followup%TYPE DEFAULT NULL,
        flg_end_followup_nin      IN BOOLEAN := TRUE,
        id_opinion_type_in        IN management_follow_up.id_opinion_type%TYPE DEFAULT NULL,
        id_opinion_type_nin       IN BOOLEAN := TRUE,
        dt_next_enc_precision_in  IN management_follow_up.dt_next_enc_precision%TYPE DEFAULT NULL,
        dt_next_enc_precision_nin IN BOOLEAN := TRUE,
        id_opinion_in             IN management_follow_up.id_opinion%TYPE DEFAULT NULL,
        id_opinion_nin            IN BOOLEAN := TRUE,
        where_in                  IN VARCHAR2,
        handle_error_in           IN BOOLEAN := TRUE,
        rows_out                  IN OUT table_varchar
    );

    --Update any/all columns by dynamic WHERE
    -- If you pass NULL, then the current column value is set to itself
    PROCEDURE upd
    (
        id_episode_in             IN management_follow_up.id_episode%TYPE DEFAULT NULL,
        id_episode_nin            IN BOOLEAN := TRUE,
        id_epis_encounter_in      IN management_follow_up.id_epis_encounter%TYPE DEFAULT NULL,
        id_epis_encounter_nin     IN BOOLEAN := TRUE,
        time_spent_in             IN management_follow_up.time_spent%TYPE DEFAULT NULL,
        time_spent_nin            IN BOOLEAN := TRUE,
        flg_status_in             IN management_follow_up.flg_status%TYPE DEFAULT NULL,
        flg_status_nin            IN BOOLEAN := TRUE,
        id_unit_time_in           IN management_follow_up.id_unit_time%TYPE DEFAULT NULL,
        id_unit_time_nin          IN BOOLEAN := TRUE,
        dt_register_in            IN management_follow_up.dt_register%TYPE DEFAULT NULL,
        dt_register_nin           IN BOOLEAN := TRUE,
        notes_in                  IN management_follow_up.notes%TYPE DEFAULT NULL,
        notes_nin                 IN BOOLEAN := TRUE,
        id_cancel_reason_in       IN management_follow_up.id_cancel_reason%TYPE DEFAULT NULL,
        id_cancel_reason_nin      IN BOOLEAN := TRUE,
        notes_cancel_in           IN management_follow_up.notes_cancel%TYPE DEFAULT NULL,
        notes_cancel_nin          IN BOOLEAN := TRUE,
        id_parent_in              IN management_follow_up.id_parent%TYPE DEFAULT NULL,
        id_parent_nin             IN BOOLEAN := TRUE,
        create_user_in            IN management_follow_up.create_user%TYPE DEFAULT NULL,
        create_user_nin           IN BOOLEAN := TRUE,
        create_time_in            IN management_follow_up.create_time%TYPE DEFAULT NULL,
        create_time_nin           IN BOOLEAN := TRUE,
        create_institution_in     IN management_follow_up.create_institution%TYPE DEFAULT NULL,
        create_institution_nin    IN BOOLEAN := TRUE,
        update_user_in            IN management_follow_up.update_user%TYPE DEFAULT NULL,
        update_user_nin           IN BOOLEAN := TRUE,
        update_time_in            IN management_follow_up.update_time%TYPE DEFAULT NULL,
        update_time_nin           IN BOOLEAN := TRUE,
        update_institution_in     IN management_follow_up.update_institution%TYPE DEFAULT NULL,
        update_institution_nin    IN BOOLEAN := TRUE,
        id_professional_in        IN management_follow_up.id_professional%TYPE DEFAULT NULL,
        id_professional_nin       IN BOOLEAN := TRUE,
        dt_start_in               IN management_follow_up.dt_start%TYPE DEFAULT NULL,
        dt_start_nin              IN BOOLEAN := TRUE,
        dt_next_encounter_in      IN management_follow_up.dt_next_encounter%TYPE DEFAULT NULL,
        dt_next_encounter_nin     IN BOOLEAN := TRUE,
        flg_end_followup_in       IN management_follow_up.flg_end_followup%TYPE DEFAULT NULL,
        flg_end_followup_nin      IN BOOLEAN := TRUE,
        id_opinion_type_in        IN management_follow_up.id_opinion_type%TYPE DEFAULT NULL,
        id_opinion_type_nin       IN BOOLEAN := TRUE,
        dt_next_enc_precision_in  IN management_follow_up.dt_next_enc_precision%TYPE DEFAULT NULL,
        dt_next_enc_precision_nin IN BOOLEAN := TRUE,
        id_opinion_in             IN management_follow_up.id_opinion%TYPE DEFAULT NULL,
        id_opinion_nin            IN BOOLEAN := TRUE,
        where_in                  IN VARCHAR2,
        handle_error_in           IN BOOLEAN := TRUE
    );

    --Update/insert with columns (with rows_out)
    PROCEDURE upd_ins
    (
        id_management_follow_up_in IN management_follow_up.id_management_follow_up%TYPE,
        id_episode_in              IN management_follow_up.id_episode%TYPE DEFAULT NULL,
        id_epis_encounter_in       IN management_follow_up.id_epis_encounter%TYPE DEFAULT NULL,
        time_spent_in              IN management_follow_up.time_spent%TYPE DEFAULT NULL,
        flg_status_in              IN management_follow_up.flg_status%TYPE DEFAULT NULL,
        id_unit_time_in            IN management_follow_up.id_unit_time%TYPE DEFAULT NULL,
        dt_register_in             IN management_follow_up.dt_register%TYPE DEFAULT NULL,
        notes_in                   IN management_follow_up.notes%TYPE DEFAULT NULL,
        id_cancel_reason_in        IN management_follow_up.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in            IN management_follow_up.notes_cancel%TYPE DEFAULT NULL,
        id_parent_in               IN management_follow_up.id_parent%TYPE DEFAULT NULL,
        create_user_in             IN management_follow_up.create_user%TYPE DEFAULT NULL,
        create_time_in             IN management_follow_up.create_time%TYPE DEFAULT NULL,
        create_institution_in      IN management_follow_up.create_institution%TYPE DEFAULT NULL,
        update_user_in             IN management_follow_up.update_user%TYPE DEFAULT NULL,
        update_time_in             IN management_follow_up.update_time%TYPE DEFAULT NULL,
        update_institution_in      IN management_follow_up.update_institution%TYPE DEFAULT NULL,
        id_professional_in         IN management_follow_up.id_professional%TYPE DEFAULT NULL,
        dt_start_in                IN management_follow_up.dt_start%TYPE DEFAULT NULL,
        dt_next_encounter_in       IN management_follow_up.dt_next_encounter%TYPE DEFAULT NULL,
        flg_end_followup_in        IN management_follow_up.flg_end_followup%TYPE DEFAULT NULL,
        id_opinion_type_in         IN management_follow_up.id_opinion_type%TYPE DEFAULT NULL,
        dt_next_enc_precision_in   IN management_follow_up.dt_next_enc_precision%TYPE DEFAULT NULL,
        id_opinion_in              IN management_follow_up.id_opinion%TYPE DEFAULT NULL,
        handle_error_in            IN BOOLEAN := TRUE,
        rows_out                   IN OUT table_varchar
    );

    --Update/insert with columns (without rows_out)
    PROCEDURE upd_ins
    (
        id_management_follow_up_in IN management_follow_up.id_management_follow_up%TYPE,
        id_episode_in              IN management_follow_up.id_episode%TYPE DEFAULT NULL,
        id_epis_encounter_in       IN management_follow_up.id_epis_encounter%TYPE DEFAULT NULL,
        time_spent_in              IN management_follow_up.time_spent%TYPE DEFAULT NULL,
        flg_status_in              IN management_follow_up.flg_status%TYPE DEFAULT NULL,
        id_unit_time_in            IN management_follow_up.id_unit_time%TYPE DEFAULT NULL,
        dt_register_in             IN management_follow_up.dt_register%TYPE DEFAULT NULL,
        notes_in                   IN management_follow_up.notes%TYPE DEFAULT NULL,
        id_cancel_reason_in        IN management_follow_up.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in            IN management_follow_up.notes_cancel%TYPE DEFAULT NULL,
        id_parent_in               IN management_follow_up.id_parent%TYPE DEFAULT NULL,
        create_user_in             IN management_follow_up.create_user%TYPE DEFAULT NULL,
        create_time_in             IN management_follow_up.create_time%TYPE DEFAULT NULL,
        create_institution_in      IN management_follow_up.create_institution%TYPE DEFAULT NULL,
        update_user_in             IN management_follow_up.update_user%TYPE DEFAULT NULL,
        update_time_in             IN management_follow_up.update_time%TYPE DEFAULT NULL,
        update_institution_in      IN management_follow_up.update_institution%TYPE DEFAULT NULL,
        id_professional_in         IN management_follow_up.id_professional%TYPE DEFAULT NULL,
        dt_start_in                IN management_follow_up.dt_start%TYPE DEFAULT NULL,
        dt_next_encounter_in       IN management_follow_up.dt_next_encounter%TYPE DEFAULT NULL,
        flg_end_followup_in        IN management_follow_up.flg_end_followup%TYPE DEFAULT NULL,
        id_opinion_type_in         IN management_follow_up.id_opinion_type%TYPE DEFAULT NULL,
        dt_next_enc_precision_in   IN management_follow_up.dt_next_enc_precision%TYPE DEFAULT NULL,
        id_opinion_in              IN management_follow_up.id_opinion%TYPE DEFAULT NULL,
        handle_error_in            IN BOOLEAN := TRUE
    );

    --Update record (with rows_out)
    PROCEDURE upd
    (
        rec_in          IN management_follow_up%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        IN OUT table_varchar
    );

    --Update record (without rows_out)
    PROCEDURE upd
    (
        rec_in          IN management_follow_up%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    --Update collection (with rows_out)
    PROCEDURE upd
    (
        col_in            IN management_follow_up_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          IN OUT table_varchar
    );

    --Update collection (without rows_out)
    PROCEDURE upd
    (
        col_in            IN management_follow_up_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE
    );

    -- Use Native Dynamic SQL increment a single NUMBER column
    -- for all rows specified by the dynamic WHERE clause
    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    --increment column value
    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_management_follow_up_in IN management_follow_up.id_management_follow_up%TYPE,
        handle_error_in            IN BOOLEAN := TRUE,
        rows_out                   OUT table_varchar
    );

    -- Delete one row by primary key
    PROCEDURE del
    (
        id_management_follow_up_in IN management_follow_up.id_management_follow_up%TYPE,
        handle_error_in            IN BOOLEAN := TRUE
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this MFU_CANCEL_REASON_FK foreign key value
    PROCEDURE del_mfu_cancel_reason_fk
    (
        id_cancel_reason_in IN management_follow_up.id_cancel_reason%TYPE,
        handle_error_in     IN BOOLEAN := TRUE,
        rows_out            OUT table_varchar
    );

    -- Delete all rows for this MFU_EC_FK foreign key value
    PROCEDURE del_mfu_ec_fk
    (
        id_epis_encounter_in IN management_follow_up.id_epis_encounter%TYPE,
        handle_error_in      IN BOOLEAN := TRUE,
        rows_out             OUT table_varchar
    );

    -- Delete all rows for this MFU_EPISODE_FK foreign key value
    PROCEDURE del_mfu_episode_fk
    (
        id_episode_in   IN management_follow_up.id_episode%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for this MFU_MFU_PARENT_FK foreign key value
    PROCEDURE del_mfu_mfu_parent_fk
    (
        id_parent_in    IN management_follow_up.id_parent%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for this MFU_OPINION_FK foreign key value
    PROCEDURE del_mfu_opinion_fk
    (
        id_opinion_in   IN management_follow_up.id_opinion%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for this MFU_OPINION_TYPE_FK foreign key value
    PROCEDURE del_mfu_opinion_type_fk
    (
        id_opinion_type_in IN management_follow_up.id_opinion_type%TYPE,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    );

    -- Delete all rows for this MFU_UNIT_MS_FK foreign key value
    PROCEDURE del_mfu_unit_ms_fk
    (
        id_unit_time_in IN management_follow_up.id_unit_time%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    );

    -- Delete all rows for this MFU_CANCEL_REASON_FK foreign key value
    PROCEDURE del_mfu_cancel_reason_fk
    (
        id_cancel_reason_in IN management_follow_up.id_cancel_reason%TYPE,
        handle_error_in     IN BOOLEAN := TRUE
    );

    -- Delete all rows for this MFU_EC_FK foreign key value
    PROCEDURE del_mfu_ec_fk
    (
        id_epis_encounter_in IN management_follow_up.id_epis_encounter%TYPE,
        handle_error_in      IN BOOLEAN := TRUE
    );

    -- Delete all rows for this MFU_EPISODE_FK foreign key value
    PROCEDURE del_mfu_episode_fk
    (
        id_episode_in   IN management_follow_up.id_episode%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this MFU_MFU_PARENT_FK foreign key value
    PROCEDURE del_mfu_mfu_parent_fk
    (
        id_parent_in    IN management_follow_up.id_parent%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this MFU_OPINION_FK foreign key value
    PROCEDURE del_mfu_opinion_fk
    (
        id_opinion_in   IN management_follow_up.id_opinion%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Delete all rows for this MFU_OPINION_TYPE_FK foreign key value
    PROCEDURE del_mfu_opinion_type_fk
    (
        id_opinion_type_in IN management_follow_up.id_opinion_type%TYPE,
        handle_error_in    IN BOOLEAN := TRUE
    );

    -- Delete all rows for this MFU_UNIT_MS_FK foreign key value
    PROCEDURE del_mfu_unit_ms_fk
    (
        id_unit_time_in IN management_follow_up.id_unit_time%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    );

    -- Initialize a record with default values for columns in the table (prc)
    PROCEDURE initrec(management_follow_up_inout IN OUT management_follow_up%ROWTYPE);

    -- Initialize a record with default values for columns in the table (fnc)
    FUNCTION initrec RETURN management_follow_up%ROWTYPE;

    -- Get data rowid
    FUNCTION get_data_rowid(rows_in IN table_varchar) RETURN management_follow_up_tc;

    -- Get data rowid pragma autonomous transaccion
    FUNCTION get_data_rowid_pat(rows_in IN table_varchar) RETURN management_follow_up_tc;

END ts_management_follow_up;
/
