/*-- Last Change Revision: $Rev: 2029112 $*/
/*-- Last Change by: $Author: mario.fernandes $*/
/*-- Date of last change: $Date: 2022-08-02 18:49:50 +0100 (ter, 02 ago 2022) $*/
CREATE OR REPLACE PACKAGE TS_DISCHARGE_NOTES
/*
| Generated by or retrieved from QCGU - DO NOT MODIFY!
| QCGU - "Get it right, do it fast" - www.ToadWorld.com
| QCGU Universal ID: {1BD37A66-EA60-4927-9A64-F6DD89237236}
| Created On: Outubro 29, 2014 21:43:8
| Created By: ALERT
*/
IS

  -- Collection of %ROWTYPE records based on "DISCHARGE_NOTES"
     TYPE DISCHARGE_NOTES_tc IS TABLE OF DISCHARGE_NOTES%ROWTYPE INDEX BY BINARY_INTEGER;
     TYPE discharge_notes_ntt IS TABLE OF DISCHARGE_NOTES%ROWTYPE;
     TYPE discharge_notes_vat IS VARRAY(100) OF DISCHARGE_NOTES%ROWTYPE;

  -- Same type structure, with a static name.
     TYPE aat IS TABLE OF DISCHARGE_NOTES%ROWTYPE INDEX BY BINARY_INTEGER;
     TYPE ntt IS TABLE OF DISCHARGE_NOTES%ROWTYPE;
     TYPE vat IS VARRAY(100) OF DISCHARGE_NOTES%ROWTYPE;

   -- Column Collection based on column "ID_DISCHARGE_NOTES"
   TYPE ID_DISCHARGE_NOTES_cc IS TABLE OF DISCHARGE_NOTES.ID_DISCHARGE_NOTES%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "ID_EPISODE"
   TYPE ID_EPISODE_cc IS TABLE OF DISCHARGE_NOTES.ID_EPISODE%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "ID_PROFESSIONAL"
   TYPE ID_PROFESSIONAL_cc IS TABLE OF DISCHARGE_NOTES.ID_PROFESSIONAL%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "EPIS_COMPLAINT"
   TYPE EPIS_COMPLAINT_cc IS TABLE OF DISCHARGE_NOTES.EPIS_COMPLAINT%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "EPIS_DIAGNOSIS"
   TYPE EPIS_DIAGNOSIS_cc IS TABLE OF DISCHARGE_NOTES.EPIS_DIAGNOSIS%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "EPIS_TESTS"
   TYPE EPIS_TESTS_cc IS TABLE OF DISCHARGE_NOTES.EPIS_TESTS%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "EPIS_DRUGS"
   TYPE EPIS_DRUGS_cc IS TABLE OF DISCHARGE_NOTES.EPIS_DRUGS%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "RECOMMENDED"
   TYPE RECOMMENDED_cc IS TABLE OF DISCHARGE_NOTES.RECOMMENDED%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "FLG_STATUS"
   TYPE FLG_STATUS_cc IS TABLE OF DISCHARGE_NOTES.FLG_STATUS%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "ADW_LAST_UPDATE"
   TYPE ADW_LAST_UPDATE_cc IS TABLE OF DISCHARGE_NOTES.ADW_LAST_UPDATE%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "ID_PROF_CANCEL"
   TYPE ID_PROF_CANCEL_cc IS TABLE OF DISCHARGE_NOTES.ID_PROF_CANCEL%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "DT_CREATION_TSTZ"
   TYPE DT_CREATION_TSTZ_cc IS TABLE OF DISCHARGE_NOTES.DT_CREATION_TSTZ%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "DT_CANCEL_TSTZ"
   TYPE DT_CANCEL_TSTZ_cc IS TABLE OF DISCHARGE_NOTES.DT_CANCEL_TSTZ%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "RELEASE_FROM"
   TYPE RELEASE_FROM_cc IS TABLE OF DISCHARGE_NOTES.RELEASE_FROM%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "DT_FROM"
   TYPE DT_FROM_cc IS TABLE OF DISCHARGE_NOTES.DT_FROM%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "DT_UNTIL"
   TYPE DT_UNTIL_cc IS TABLE OF DISCHARGE_NOTES.DT_UNTIL%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "NOTES_RELEASE"
   TYPE NOTES_RELEASE_cc IS TABLE OF DISCHARGE_NOTES.NOTES_RELEASE%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "FOLLOW_UP_WITH"
   TYPE FOLLOW_UP_WITH_cc IS TABLE OF DISCHARGE_NOTES.FOLLOW_UP_WITH%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "FOLLOW_UP_IN"
   TYPE FOLLOW_UP_IN_cc IS TABLE OF DISCHARGE_NOTES.FOLLOW_UP_IN%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "INSTRUCTIONS_DISCUSSED"
   TYPE INSTRUCTIONS_DISCUSSED_cc IS TABLE OF DISCHARGE_NOTES.INSTRUCTIONS_DISCUSSED%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "ID_FOLLOW_UP_TYPE"
   TYPE ID_FOLLOW_UP_TYPE_cc IS TABLE OF DISCHARGE_NOTES.ID_FOLLOW_UP_TYPE%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "ID_EPIS_REPORT"
   TYPE ID_EPIS_REPORT_cc IS TABLE OF DISCHARGE_NOTES.ID_EPIS_REPORT%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "ID_PATIENT"
   TYPE ID_PATIENT_cc IS TABLE OF DISCHARGE_NOTES.ID_PATIENT%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "ID_PENDING_ISSUE"
   TYPE ID_PENDING_ISSUE_cc IS TABLE OF DISCHARGE_NOTES.ID_PENDING_ISSUE%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "CREATE_USER"
   TYPE CREATE_USER_cc IS TABLE OF DISCHARGE_NOTES.CREATE_USER%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "CREATE_TIME"
   TYPE CREATE_TIME_cc IS TABLE OF DISCHARGE_NOTES.CREATE_TIME%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "CREATE_INSTITUTION"
   TYPE CREATE_INSTITUTION_cc IS TABLE OF DISCHARGE_NOTES.CREATE_INSTITUTION%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "UPDATE_USER"
   TYPE UPDATE_USER_cc IS TABLE OF DISCHARGE_NOTES.UPDATE_USER%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "UPDATE_TIME"
   TYPE UPDATE_TIME_cc IS TABLE OF DISCHARGE_NOTES.UPDATE_TIME%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "UPDATE_INSTITUTION"
   TYPE UPDATE_INSTITUTION_cc IS TABLE OF DISCHARGE_NOTES.UPDATE_INSTITUTION%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "FLG_ISSUE_ASSIGN"
   TYPE FLG_ISSUE_ASSIGN_cc IS TABLE OF DISCHARGE_NOTES.FLG_ISSUE_ASSIGN%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "DISCHARGE_INSTRUCTIONS"
   TYPE DISCHARGE_INSTRUCTIONS_cc IS TABLE OF DISCHARGE_NOTES.DISCHARGE_INSTRUCTIONS%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "FLG_CSG_PATIENT"
   TYPE FLG_CSG_PATIENT_cc IS TABLE OF DISCHARGE_NOTES.FLG_CSG_PATIENT%TYPE INDEX BY BINARY_INTEGER;
   -- Column Collection based on column "DT_CSG_PATIENT"
   TYPE DT_CSG_PATIENT_cc IS TABLE OF DISCHARGE_NOTES.DT_CSG_PATIENT%TYPE INDEX BY BINARY_INTEGER;

   TYPE varchar2_t IS TABLE OF VARCHAR2(32767) INDEX BY PLS_INTEGER;
   /*
   START Special logic for handling LOB columns....
   */
   PROCEDURE n_ins_clobs_in_chunks (
      id_discharge_notes_in IN DISCHARGE_NOTES.ID_DISCHARGE_NOTES%TYPE,
      id_episode_in IN DISCHARGE_NOTES.ID_EPISODE%TYPE DEFAULT NULL,
      id_professional_in IN DISCHARGE_NOTES.ID_PROFESSIONAL%TYPE DEFAULT NULL,
      epis_complaint_in IN DISCHARGE_NOTES.EPIS_COMPLAINT%TYPE DEFAULT NULL,
      epis_diagnosis_in IN DISCHARGE_NOTES.EPIS_DIAGNOSIS%TYPE DEFAULT NULL,
      epis_tests_in IN DISCHARGE_NOTES.EPIS_TESTS%TYPE DEFAULT NULL,
      epis_drugs_in IN DISCHARGE_NOTES.EPIS_DRUGS%TYPE DEFAULT NULL,
      recommended_in IN DISCHARGE_NOTES.RECOMMENDED%TYPE DEFAULT NULL,
      flg_status_in IN DISCHARGE_NOTES.FLG_STATUS%TYPE DEFAULT NULL,
      adw_last_update_in IN DISCHARGE_NOTES.ADW_LAST_UPDATE%TYPE DEFAULT sysdate,
      id_prof_cancel_in IN DISCHARGE_NOTES.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      dt_creation_tstz_in IN DISCHARGE_NOTES.DT_CREATION_TSTZ%TYPE DEFAULT NULL,
      dt_cancel_tstz_in IN DISCHARGE_NOTES.DT_CANCEL_TSTZ%TYPE DEFAULT NULL,
      release_from_in IN DISCHARGE_NOTES.RELEASE_FROM%TYPE DEFAULT NULL,
      dt_from_in IN DISCHARGE_NOTES.DT_FROM%TYPE DEFAULT NULL,
      dt_until_in IN DISCHARGE_NOTES.DT_UNTIL%TYPE DEFAULT NULL,
      notes_release_in IN DISCHARGE_NOTES.NOTES_RELEASE%TYPE DEFAULT NULL,
      follow_up_with_in IN DISCHARGE_NOTES.FOLLOW_UP_WITH%TYPE DEFAULT NULL,
      follow_up_in_in IN DISCHARGE_NOTES.FOLLOW_UP_IN%TYPE DEFAULT NULL,
      instructions_discussed_in IN DISCHARGE_NOTES.INSTRUCTIONS_DISCUSSED%TYPE DEFAULT NULL,
      id_follow_up_type_in IN DISCHARGE_NOTES.ID_FOLLOW_UP_TYPE%TYPE DEFAULT NULL,
      id_epis_report_in IN DISCHARGE_NOTES.ID_EPIS_REPORT%TYPE DEFAULT NULL,
      id_patient_in IN DISCHARGE_NOTES.ID_PATIENT%TYPE DEFAULT NULL,
      id_pending_issue_in IN DISCHARGE_NOTES.ID_PENDING_ISSUE%TYPE DEFAULT NULL,
      create_user_in IN DISCHARGE_NOTES.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN DISCHARGE_NOTES.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN DISCHARGE_NOTES.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN DISCHARGE_NOTES.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN DISCHARGE_NOTES.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN DISCHARGE_NOTES.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      flg_issue_assign_in IN DISCHARGE_NOTES.FLG_ISSUE_ASSIGN%TYPE DEFAULT NULL,
      flg_csg_patient_in IN DISCHARGE_NOTES.FLG_CSG_PATIENT%TYPE DEFAULT NULL,
      dt_csg_patient_in IN DISCHARGE_NOTES.DT_CSG_PATIENT%TYPE DEFAULT NULL,
      handle_error_in IN BOOLEAN := TRUE,
      clob_columns_in IN varchar2_t,
      clob_pieces_in IN  varchar2_t
   );

   PROCEDURE n_upd_clobs_in_chunks (
      id_discharge_notes_in IN DISCHARGE_NOTES.ID_DISCHARGE_NOTES%TYPE,
      id_episode_in IN DISCHARGE_NOTES.ID_EPISODE%TYPE DEFAULT NULL,
      id_professional_in IN DISCHARGE_NOTES.ID_PROFESSIONAL%TYPE DEFAULT NULL,
      epis_complaint_in IN DISCHARGE_NOTES.EPIS_COMPLAINT%TYPE DEFAULT NULL,
      epis_diagnosis_in IN DISCHARGE_NOTES.EPIS_DIAGNOSIS%TYPE DEFAULT NULL,
      epis_tests_in IN DISCHARGE_NOTES.EPIS_TESTS%TYPE DEFAULT NULL,
      epis_drugs_in IN DISCHARGE_NOTES.EPIS_DRUGS%TYPE DEFAULT NULL,
      recommended_in IN DISCHARGE_NOTES.RECOMMENDED%TYPE DEFAULT NULL,
      flg_status_in IN DISCHARGE_NOTES.FLG_STATUS%TYPE DEFAULT NULL,
      adw_last_update_in IN DISCHARGE_NOTES.ADW_LAST_UPDATE%TYPE DEFAULT sysdate,
      id_prof_cancel_in IN DISCHARGE_NOTES.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      dt_creation_tstz_in IN DISCHARGE_NOTES.DT_CREATION_TSTZ%TYPE DEFAULT NULL,
      dt_cancel_tstz_in IN DISCHARGE_NOTES.DT_CANCEL_TSTZ%TYPE DEFAULT NULL,
      release_from_in IN DISCHARGE_NOTES.RELEASE_FROM%TYPE DEFAULT NULL,
      dt_from_in IN DISCHARGE_NOTES.DT_FROM%TYPE DEFAULT NULL,
      dt_until_in IN DISCHARGE_NOTES.DT_UNTIL%TYPE DEFAULT NULL,
      notes_release_in IN DISCHARGE_NOTES.NOTES_RELEASE%TYPE DEFAULT NULL,
      follow_up_with_in IN DISCHARGE_NOTES.FOLLOW_UP_WITH%TYPE DEFAULT NULL,
      follow_up_in_in IN DISCHARGE_NOTES.FOLLOW_UP_IN%TYPE DEFAULT NULL,
      instructions_discussed_in IN DISCHARGE_NOTES.INSTRUCTIONS_DISCUSSED%TYPE DEFAULT NULL,
      id_follow_up_type_in IN DISCHARGE_NOTES.ID_FOLLOW_UP_TYPE%TYPE DEFAULT NULL,
      id_epis_report_in IN DISCHARGE_NOTES.ID_EPIS_REPORT%TYPE DEFAULT NULL,
      id_patient_in IN DISCHARGE_NOTES.ID_PATIENT%TYPE DEFAULT NULL,
      id_pending_issue_in IN DISCHARGE_NOTES.ID_PENDING_ISSUE%TYPE DEFAULT NULL,
      create_user_in IN DISCHARGE_NOTES.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN DISCHARGE_NOTES.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN DISCHARGE_NOTES.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN DISCHARGE_NOTES.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN DISCHARGE_NOTES.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN DISCHARGE_NOTES.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      flg_issue_assign_in IN DISCHARGE_NOTES.FLG_ISSUE_ASSIGN%TYPE DEFAULT NULL,
      flg_csg_patient_in IN DISCHARGE_NOTES.FLG_CSG_PATIENT%TYPE DEFAULT NULL,
      dt_csg_patient_in IN DISCHARGE_NOTES.DT_CSG_PATIENT%TYPE DEFAULT NULL,
      ignore_if_null_in IN BOOLEAN := TRUE,
      handle_error_in IN BOOLEAN := TRUE,
      clob_columns_in IN varchar2_t,
      clob_pieces_in IN  varchar2_t
      );

   PROCEDURE n_upd_ins_clobs_in_chunks (
      id_discharge_notes_in IN DISCHARGE_NOTES.ID_DISCHARGE_NOTES%TYPE,
      id_episode_in IN DISCHARGE_NOTES.ID_EPISODE%TYPE DEFAULT NULL,
      id_professional_in IN DISCHARGE_NOTES.ID_PROFESSIONAL%TYPE DEFAULT NULL,
      epis_complaint_in IN DISCHARGE_NOTES.EPIS_COMPLAINT%TYPE DEFAULT NULL,
      epis_diagnosis_in IN DISCHARGE_NOTES.EPIS_DIAGNOSIS%TYPE DEFAULT NULL,
      epis_tests_in IN DISCHARGE_NOTES.EPIS_TESTS%TYPE DEFAULT NULL,
      epis_drugs_in IN DISCHARGE_NOTES.EPIS_DRUGS%TYPE DEFAULT NULL,
      recommended_in IN DISCHARGE_NOTES.RECOMMENDED%TYPE DEFAULT NULL,
      flg_status_in IN DISCHARGE_NOTES.FLG_STATUS%TYPE DEFAULT NULL,
      adw_last_update_in IN DISCHARGE_NOTES.ADW_LAST_UPDATE%TYPE DEFAULT sysdate,
      id_prof_cancel_in IN DISCHARGE_NOTES.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      dt_creation_tstz_in IN DISCHARGE_NOTES.DT_CREATION_TSTZ%TYPE DEFAULT NULL,
      dt_cancel_tstz_in IN DISCHARGE_NOTES.DT_CANCEL_TSTZ%TYPE DEFAULT NULL,
      release_from_in IN DISCHARGE_NOTES.RELEASE_FROM%TYPE DEFAULT NULL,
      dt_from_in IN DISCHARGE_NOTES.DT_FROM%TYPE DEFAULT NULL,
      dt_until_in IN DISCHARGE_NOTES.DT_UNTIL%TYPE DEFAULT NULL,
      notes_release_in IN DISCHARGE_NOTES.NOTES_RELEASE%TYPE DEFAULT NULL,
      follow_up_with_in IN DISCHARGE_NOTES.FOLLOW_UP_WITH%TYPE DEFAULT NULL,
      follow_up_in_in IN DISCHARGE_NOTES.FOLLOW_UP_IN%TYPE DEFAULT NULL,
      instructions_discussed_in IN DISCHARGE_NOTES.INSTRUCTIONS_DISCUSSED%TYPE DEFAULT NULL,
      id_follow_up_type_in IN DISCHARGE_NOTES.ID_FOLLOW_UP_TYPE%TYPE DEFAULT NULL,
      id_epis_report_in IN DISCHARGE_NOTES.ID_EPIS_REPORT%TYPE DEFAULT NULL,
      id_patient_in IN DISCHARGE_NOTES.ID_PATIENT%TYPE DEFAULT NULL,
      id_pending_issue_in IN DISCHARGE_NOTES.ID_PENDING_ISSUE%TYPE DEFAULT NULL,
      create_user_in IN DISCHARGE_NOTES.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN DISCHARGE_NOTES.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN DISCHARGE_NOTES.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN DISCHARGE_NOTES.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN DISCHARGE_NOTES.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN DISCHARGE_NOTES.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      flg_issue_assign_in IN DISCHARGE_NOTES.FLG_ISSUE_ASSIGN%TYPE DEFAULT NULL,
      flg_csg_patient_in IN DISCHARGE_NOTES.FLG_CSG_PATIENT%TYPE DEFAULT NULL,
      dt_csg_patient_in IN DISCHARGE_NOTES.DT_CSG_PATIENT%TYPE DEFAULT NULL,
      ignore_if_null_in IN BOOLEAN DEFAULT TRUE,
      handle_error_in IN BOOLEAN DEFAULT TRUE,
      clob_columns_in IN varchar2_t,
      clob_pieces_in IN  varchar2_t
      );

   /*
   END Special logic for handling LOB columns.
   */
   -- Insert one row, providing primary key if present
   PROCEDURE ins (
      id_discharge_notes_in IN DISCHARGE_NOTES.ID_DISCHARGE_NOTES%TYPE
      ,
      id_episode_in IN DISCHARGE_NOTES.ID_EPISODE%TYPE DEFAULT NULL,
      id_professional_in IN DISCHARGE_NOTES.ID_PROFESSIONAL%TYPE DEFAULT NULL,
      epis_complaint_in IN DISCHARGE_NOTES.EPIS_COMPLAINT%TYPE DEFAULT NULL,
      epis_diagnosis_in IN DISCHARGE_NOTES.EPIS_DIAGNOSIS%TYPE DEFAULT NULL,
      epis_tests_in IN DISCHARGE_NOTES.EPIS_TESTS%TYPE DEFAULT NULL,
      epis_drugs_in IN DISCHARGE_NOTES.EPIS_DRUGS%TYPE DEFAULT NULL,
      recommended_in IN DISCHARGE_NOTES.RECOMMENDED%TYPE DEFAULT NULL,
      flg_status_in IN DISCHARGE_NOTES.FLG_STATUS%TYPE DEFAULT NULL,
      adw_last_update_in IN DISCHARGE_NOTES.ADW_LAST_UPDATE%TYPE DEFAULT sysdate,
      id_prof_cancel_in IN DISCHARGE_NOTES.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      dt_creation_tstz_in IN DISCHARGE_NOTES.DT_CREATION_TSTZ%TYPE DEFAULT NULL,
      dt_cancel_tstz_in IN DISCHARGE_NOTES.DT_CANCEL_TSTZ%TYPE DEFAULT NULL,
      release_from_in IN DISCHARGE_NOTES.RELEASE_FROM%TYPE DEFAULT NULL,
      dt_from_in IN DISCHARGE_NOTES.DT_FROM%TYPE DEFAULT NULL,
      dt_until_in IN DISCHARGE_NOTES.DT_UNTIL%TYPE DEFAULT NULL,
      notes_release_in IN DISCHARGE_NOTES.NOTES_RELEASE%TYPE DEFAULT NULL,
      follow_up_with_in IN DISCHARGE_NOTES.FOLLOW_UP_WITH%TYPE DEFAULT NULL,
      follow_up_in_in IN DISCHARGE_NOTES.FOLLOW_UP_IN%TYPE DEFAULT NULL,
      instructions_discussed_in IN DISCHARGE_NOTES.INSTRUCTIONS_DISCUSSED%TYPE DEFAULT NULL,
      id_follow_up_type_in IN DISCHARGE_NOTES.ID_FOLLOW_UP_TYPE%TYPE DEFAULT NULL,
      id_epis_report_in IN DISCHARGE_NOTES.ID_EPIS_REPORT%TYPE DEFAULT NULL,
      id_patient_in IN DISCHARGE_NOTES.ID_PATIENT%TYPE DEFAULT NULL,
      id_pending_issue_in IN DISCHARGE_NOTES.ID_PENDING_ISSUE%TYPE DEFAULT NULL,
      create_user_in IN DISCHARGE_NOTES.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN DISCHARGE_NOTES.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN DISCHARGE_NOTES.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN DISCHARGE_NOTES.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN DISCHARGE_NOTES.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN DISCHARGE_NOTES.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      flg_issue_assign_in IN DISCHARGE_NOTES.FLG_ISSUE_ASSIGN%TYPE DEFAULT NULL,
      discharge_instructions_in IN DISCHARGE_NOTES.DISCHARGE_INSTRUCTIONS%TYPE DEFAULT NULL,
      flg_csg_patient_in IN DISCHARGE_NOTES.FLG_CSG_PATIENT%TYPE DEFAULT NULL,
      dt_csg_patient_in IN DISCHARGE_NOTES.DT_CSG_PATIENT%TYPE DEFAULT NULL
      -- Pass false if you want errors to propagate out unhandled
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      );

   PROCEDURE ins (
      id_discharge_notes_in IN DISCHARGE_NOTES.ID_DISCHARGE_NOTES%TYPE
      ,
      id_episode_in IN DISCHARGE_NOTES.ID_EPISODE%TYPE DEFAULT NULL,
      id_professional_in IN DISCHARGE_NOTES.ID_PROFESSIONAL%TYPE DEFAULT NULL,
      epis_complaint_in IN DISCHARGE_NOTES.EPIS_COMPLAINT%TYPE DEFAULT NULL,
      epis_diagnosis_in IN DISCHARGE_NOTES.EPIS_DIAGNOSIS%TYPE DEFAULT NULL,
      epis_tests_in IN DISCHARGE_NOTES.EPIS_TESTS%TYPE DEFAULT NULL,
      epis_drugs_in IN DISCHARGE_NOTES.EPIS_DRUGS%TYPE DEFAULT NULL,
      recommended_in IN DISCHARGE_NOTES.RECOMMENDED%TYPE DEFAULT NULL,
      flg_status_in IN DISCHARGE_NOTES.FLG_STATUS%TYPE DEFAULT NULL,
      adw_last_update_in IN DISCHARGE_NOTES.ADW_LAST_UPDATE%TYPE DEFAULT sysdate,
      id_prof_cancel_in IN DISCHARGE_NOTES.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      dt_creation_tstz_in IN DISCHARGE_NOTES.DT_CREATION_TSTZ%TYPE DEFAULT NULL,
      dt_cancel_tstz_in IN DISCHARGE_NOTES.DT_CANCEL_TSTZ%TYPE DEFAULT NULL,
      release_from_in IN DISCHARGE_NOTES.RELEASE_FROM%TYPE DEFAULT NULL,
      dt_from_in IN DISCHARGE_NOTES.DT_FROM%TYPE DEFAULT NULL,
      dt_until_in IN DISCHARGE_NOTES.DT_UNTIL%TYPE DEFAULT NULL,
      notes_release_in IN DISCHARGE_NOTES.NOTES_RELEASE%TYPE DEFAULT NULL,
      follow_up_with_in IN DISCHARGE_NOTES.FOLLOW_UP_WITH%TYPE DEFAULT NULL,
      follow_up_in_in IN DISCHARGE_NOTES.FOLLOW_UP_IN%TYPE DEFAULT NULL,
      instructions_discussed_in IN DISCHARGE_NOTES.INSTRUCTIONS_DISCUSSED%TYPE DEFAULT NULL,
      id_follow_up_type_in IN DISCHARGE_NOTES.ID_FOLLOW_UP_TYPE%TYPE DEFAULT NULL,
      id_epis_report_in IN DISCHARGE_NOTES.ID_EPIS_REPORT%TYPE DEFAULT NULL,
      id_patient_in IN DISCHARGE_NOTES.ID_PATIENT%TYPE DEFAULT NULL,
      id_pending_issue_in IN DISCHARGE_NOTES.ID_PENDING_ISSUE%TYPE DEFAULT NULL,
      create_user_in IN DISCHARGE_NOTES.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN DISCHARGE_NOTES.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN DISCHARGE_NOTES.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN DISCHARGE_NOTES.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN DISCHARGE_NOTES.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN DISCHARGE_NOTES.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      flg_issue_assign_in IN DISCHARGE_NOTES.FLG_ISSUE_ASSIGN%TYPE DEFAULT NULL,
      discharge_instructions_in IN DISCHARGE_NOTES.DISCHARGE_INSTRUCTIONS%TYPE DEFAULT NULL,
      flg_csg_patient_in IN DISCHARGE_NOTES.FLG_CSG_PATIENT%TYPE DEFAULT NULL,
      dt_csg_patient_in IN DISCHARGE_NOTES.DT_CSG_PATIENT%TYPE DEFAULT NULL
      -- Pass false if you want errors to propagate out unhandled
     ,handle_error_in IN BOOLEAN := TRUE
      );


   -- Insert a row based on a record.
   -- Specify whether or not a primary key value should be generated.
   PROCEDURE ins (
      rec_in IN DISCHARGE_NOTES%ROWTYPE
     ,gen_pky_in IN BOOLEAN DEFAULT FALSE
     ,sequence_in IN VARCHAR2 := NULL
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   );

   PROCEDURE ins (
      rec_in IN DISCHARGE_NOTES%ROWTYPE
     ,gen_pky_in IN BOOLEAN DEFAULT FALSE
     ,sequence_in IN VARCHAR2 := NULL
     ,handle_error_in IN BOOLEAN := TRUE
   );

   -- Insert a collection of rows using FORALL; all primary key values
   -- must have already been generated, or are handled in triggers.
   PROCEDURE ins (
      rows_in IN DISCHARGE_NOTES_tc
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   );

   PROCEDURE ins (
      rows_in IN DISCHARGE_NOTES_tc
     ,handle_error_in IN BOOLEAN := TRUE
   );

   -- Return next primary key value using the named sequence.
     FUNCTION next_key (sequence_in IN VARCHAR2 := NULL) RETURN DISCHARGE_NOTES.ID_DISCHARGE_NOTES%TYPE;

   -- Insert one row, generating hidden primary key using a sequence
   PROCEDURE ins (
      id_episode_in IN DISCHARGE_NOTES.ID_EPISODE%TYPE DEFAULT NULL,
      id_professional_in IN DISCHARGE_NOTES.ID_PROFESSIONAL%TYPE DEFAULT NULL,
      epis_complaint_in IN DISCHARGE_NOTES.EPIS_COMPLAINT%TYPE DEFAULT NULL,
      epis_diagnosis_in IN DISCHARGE_NOTES.EPIS_DIAGNOSIS%TYPE DEFAULT NULL,
      epis_tests_in IN DISCHARGE_NOTES.EPIS_TESTS%TYPE DEFAULT NULL,
      epis_drugs_in IN DISCHARGE_NOTES.EPIS_DRUGS%TYPE DEFAULT NULL,
      recommended_in IN DISCHARGE_NOTES.RECOMMENDED%TYPE DEFAULT NULL,
      flg_status_in IN DISCHARGE_NOTES.FLG_STATUS%TYPE DEFAULT NULL,
      adw_last_update_in IN DISCHARGE_NOTES.ADW_LAST_UPDATE%TYPE DEFAULT sysdate,
      id_prof_cancel_in IN DISCHARGE_NOTES.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      dt_creation_tstz_in IN DISCHARGE_NOTES.DT_CREATION_TSTZ%TYPE DEFAULT NULL,
      dt_cancel_tstz_in IN DISCHARGE_NOTES.DT_CANCEL_TSTZ%TYPE DEFAULT NULL,
      release_from_in IN DISCHARGE_NOTES.RELEASE_FROM%TYPE DEFAULT NULL,
      dt_from_in IN DISCHARGE_NOTES.DT_FROM%TYPE DEFAULT NULL,
      dt_until_in IN DISCHARGE_NOTES.DT_UNTIL%TYPE DEFAULT NULL,
      notes_release_in IN DISCHARGE_NOTES.NOTES_RELEASE%TYPE DEFAULT NULL,
      follow_up_with_in IN DISCHARGE_NOTES.FOLLOW_UP_WITH%TYPE DEFAULT NULL,
      follow_up_in_in IN DISCHARGE_NOTES.FOLLOW_UP_IN%TYPE DEFAULT NULL,
      instructions_discussed_in IN DISCHARGE_NOTES.INSTRUCTIONS_DISCUSSED%TYPE DEFAULT NULL,
      id_follow_up_type_in IN DISCHARGE_NOTES.ID_FOLLOW_UP_TYPE%TYPE DEFAULT NULL,
      id_epis_report_in IN DISCHARGE_NOTES.ID_EPIS_REPORT%TYPE DEFAULT NULL,
      id_patient_in IN DISCHARGE_NOTES.ID_PATIENT%TYPE DEFAULT NULL,
      id_pending_issue_in IN DISCHARGE_NOTES.ID_PENDING_ISSUE%TYPE DEFAULT NULL,
      create_user_in IN DISCHARGE_NOTES.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN DISCHARGE_NOTES.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN DISCHARGE_NOTES.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN DISCHARGE_NOTES.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN DISCHARGE_NOTES.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN DISCHARGE_NOTES.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      flg_issue_assign_in IN DISCHARGE_NOTES.FLG_ISSUE_ASSIGN%TYPE DEFAULT NULL,
      discharge_instructions_in IN DISCHARGE_NOTES.DISCHARGE_INSTRUCTIONS%TYPE DEFAULT NULL,
      flg_csg_patient_in IN DISCHARGE_NOTES.FLG_CSG_PATIENT%TYPE DEFAULT NULL,
      dt_csg_patient_in IN DISCHARGE_NOTES.DT_CSG_PATIENT%TYPE DEFAULT NULL
      -- Pass false if you want errors to propagate out unhandled
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      );

   PROCEDURE ins (
      id_episode_in IN DISCHARGE_NOTES.ID_EPISODE%TYPE DEFAULT NULL,
      id_professional_in IN DISCHARGE_NOTES.ID_PROFESSIONAL%TYPE DEFAULT NULL,
      epis_complaint_in IN DISCHARGE_NOTES.EPIS_COMPLAINT%TYPE DEFAULT NULL,
      epis_diagnosis_in IN DISCHARGE_NOTES.EPIS_DIAGNOSIS%TYPE DEFAULT NULL,
      epis_tests_in IN DISCHARGE_NOTES.EPIS_TESTS%TYPE DEFAULT NULL,
      epis_drugs_in IN DISCHARGE_NOTES.EPIS_DRUGS%TYPE DEFAULT NULL,
      recommended_in IN DISCHARGE_NOTES.RECOMMENDED%TYPE DEFAULT NULL,
      flg_status_in IN DISCHARGE_NOTES.FLG_STATUS%TYPE DEFAULT NULL,
      adw_last_update_in IN DISCHARGE_NOTES.ADW_LAST_UPDATE%TYPE DEFAULT sysdate,
      id_prof_cancel_in IN DISCHARGE_NOTES.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      dt_creation_tstz_in IN DISCHARGE_NOTES.DT_CREATION_TSTZ%TYPE DEFAULT NULL,
      dt_cancel_tstz_in IN DISCHARGE_NOTES.DT_CANCEL_TSTZ%TYPE DEFAULT NULL,
      release_from_in IN DISCHARGE_NOTES.RELEASE_FROM%TYPE DEFAULT NULL,
      dt_from_in IN DISCHARGE_NOTES.DT_FROM%TYPE DEFAULT NULL,
      dt_until_in IN DISCHARGE_NOTES.DT_UNTIL%TYPE DEFAULT NULL,
      notes_release_in IN DISCHARGE_NOTES.NOTES_RELEASE%TYPE DEFAULT NULL,
      follow_up_with_in IN DISCHARGE_NOTES.FOLLOW_UP_WITH%TYPE DEFAULT NULL,
      follow_up_in_in IN DISCHARGE_NOTES.FOLLOW_UP_IN%TYPE DEFAULT NULL,
      instructions_discussed_in IN DISCHARGE_NOTES.INSTRUCTIONS_DISCUSSED%TYPE DEFAULT NULL,
      id_follow_up_type_in IN DISCHARGE_NOTES.ID_FOLLOW_UP_TYPE%TYPE DEFAULT NULL,
      id_epis_report_in IN DISCHARGE_NOTES.ID_EPIS_REPORT%TYPE DEFAULT NULL,
      id_patient_in IN DISCHARGE_NOTES.ID_PATIENT%TYPE DEFAULT NULL,
      id_pending_issue_in IN DISCHARGE_NOTES.ID_PENDING_ISSUE%TYPE DEFAULT NULL,
      create_user_in IN DISCHARGE_NOTES.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN DISCHARGE_NOTES.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN DISCHARGE_NOTES.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN DISCHARGE_NOTES.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN DISCHARGE_NOTES.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN DISCHARGE_NOTES.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      flg_issue_assign_in IN DISCHARGE_NOTES.FLG_ISSUE_ASSIGN%TYPE DEFAULT NULL,
      discharge_instructions_in IN DISCHARGE_NOTES.DISCHARGE_INSTRUCTIONS%TYPE DEFAULT NULL,
      flg_csg_patient_in IN DISCHARGE_NOTES.FLG_CSG_PATIENT%TYPE DEFAULT NULL,
      dt_csg_patient_in IN DISCHARGE_NOTES.DT_CSG_PATIENT%TYPE DEFAULT NULL
      -- Pass false if you want errors to propagate out unhandled
     ,handle_error_in IN BOOLEAN := TRUE
      );

   -- Insert one row, returning primary key generated by sequence
   PROCEDURE ins (
      id_episode_in IN DISCHARGE_NOTES.ID_EPISODE%TYPE DEFAULT NULL,
      id_professional_in IN DISCHARGE_NOTES.ID_PROFESSIONAL%TYPE DEFAULT NULL,
      epis_complaint_in IN DISCHARGE_NOTES.EPIS_COMPLAINT%TYPE DEFAULT NULL,
      epis_diagnosis_in IN DISCHARGE_NOTES.EPIS_DIAGNOSIS%TYPE DEFAULT NULL,
      epis_tests_in IN DISCHARGE_NOTES.EPIS_TESTS%TYPE DEFAULT NULL,
      epis_drugs_in IN DISCHARGE_NOTES.EPIS_DRUGS%TYPE DEFAULT NULL,
      recommended_in IN DISCHARGE_NOTES.RECOMMENDED%TYPE DEFAULT NULL,
      flg_status_in IN DISCHARGE_NOTES.FLG_STATUS%TYPE DEFAULT NULL,
      adw_last_update_in IN DISCHARGE_NOTES.ADW_LAST_UPDATE%TYPE DEFAULT sysdate,
      id_prof_cancel_in IN DISCHARGE_NOTES.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      dt_creation_tstz_in IN DISCHARGE_NOTES.DT_CREATION_TSTZ%TYPE DEFAULT NULL,
      dt_cancel_tstz_in IN DISCHARGE_NOTES.DT_CANCEL_TSTZ%TYPE DEFAULT NULL,
      release_from_in IN DISCHARGE_NOTES.RELEASE_FROM%TYPE DEFAULT NULL,
      dt_from_in IN DISCHARGE_NOTES.DT_FROM%TYPE DEFAULT NULL,
      dt_until_in IN DISCHARGE_NOTES.DT_UNTIL%TYPE DEFAULT NULL,
      notes_release_in IN DISCHARGE_NOTES.NOTES_RELEASE%TYPE DEFAULT NULL,
      follow_up_with_in IN DISCHARGE_NOTES.FOLLOW_UP_WITH%TYPE DEFAULT NULL,
      follow_up_in_in IN DISCHARGE_NOTES.FOLLOW_UP_IN%TYPE DEFAULT NULL,
      instructions_discussed_in IN DISCHARGE_NOTES.INSTRUCTIONS_DISCUSSED%TYPE DEFAULT NULL,
      id_follow_up_type_in IN DISCHARGE_NOTES.ID_FOLLOW_UP_TYPE%TYPE DEFAULT NULL,
      id_epis_report_in IN DISCHARGE_NOTES.ID_EPIS_REPORT%TYPE DEFAULT NULL,
      id_patient_in IN DISCHARGE_NOTES.ID_PATIENT%TYPE DEFAULT NULL,
      id_pending_issue_in IN DISCHARGE_NOTES.ID_PENDING_ISSUE%TYPE DEFAULT NULL,
      create_user_in IN DISCHARGE_NOTES.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN DISCHARGE_NOTES.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN DISCHARGE_NOTES.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN DISCHARGE_NOTES.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN DISCHARGE_NOTES.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN DISCHARGE_NOTES.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      flg_issue_assign_in IN DISCHARGE_NOTES.FLG_ISSUE_ASSIGN%TYPE DEFAULT NULL,
      discharge_instructions_in IN DISCHARGE_NOTES.DISCHARGE_INSTRUCTIONS%TYPE DEFAULT NULL,
      flg_csg_patient_in IN DISCHARGE_NOTES.FLG_CSG_PATIENT%TYPE DEFAULT NULL,
      dt_csg_patient_in IN DISCHARGE_NOTES.DT_CSG_PATIENT%TYPE DEFAULT NULL,
      id_discharge_notes_out IN OUT DISCHARGE_NOTES.ID_DISCHARGE_NOTES%TYPE
      -- Pass false if you want errors to propagate out unhandled
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      );

   PROCEDURE ins (
      id_episode_in IN DISCHARGE_NOTES.ID_EPISODE%TYPE DEFAULT NULL,
      id_professional_in IN DISCHARGE_NOTES.ID_PROFESSIONAL%TYPE DEFAULT NULL,
      epis_complaint_in IN DISCHARGE_NOTES.EPIS_COMPLAINT%TYPE DEFAULT NULL,
      epis_diagnosis_in IN DISCHARGE_NOTES.EPIS_DIAGNOSIS%TYPE DEFAULT NULL,
      epis_tests_in IN DISCHARGE_NOTES.EPIS_TESTS%TYPE DEFAULT NULL,
      epis_drugs_in IN DISCHARGE_NOTES.EPIS_DRUGS%TYPE DEFAULT NULL,
      recommended_in IN DISCHARGE_NOTES.RECOMMENDED%TYPE DEFAULT NULL,
      flg_status_in IN DISCHARGE_NOTES.FLG_STATUS%TYPE DEFAULT NULL,
      adw_last_update_in IN DISCHARGE_NOTES.ADW_LAST_UPDATE%TYPE DEFAULT sysdate,
      id_prof_cancel_in IN DISCHARGE_NOTES.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      dt_creation_tstz_in IN DISCHARGE_NOTES.DT_CREATION_TSTZ%TYPE DEFAULT NULL,
      dt_cancel_tstz_in IN DISCHARGE_NOTES.DT_CANCEL_TSTZ%TYPE DEFAULT NULL,
      release_from_in IN DISCHARGE_NOTES.RELEASE_FROM%TYPE DEFAULT NULL,
      dt_from_in IN DISCHARGE_NOTES.DT_FROM%TYPE DEFAULT NULL,
      dt_until_in IN DISCHARGE_NOTES.DT_UNTIL%TYPE DEFAULT NULL,
      notes_release_in IN DISCHARGE_NOTES.NOTES_RELEASE%TYPE DEFAULT NULL,
      follow_up_with_in IN DISCHARGE_NOTES.FOLLOW_UP_WITH%TYPE DEFAULT NULL,
      follow_up_in_in IN DISCHARGE_NOTES.FOLLOW_UP_IN%TYPE DEFAULT NULL,
      instructions_discussed_in IN DISCHARGE_NOTES.INSTRUCTIONS_DISCUSSED%TYPE DEFAULT NULL,
      id_follow_up_type_in IN DISCHARGE_NOTES.ID_FOLLOW_UP_TYPE%TYPE DEFAULT NULL,
      id_epis_report_in IN DISCHARGE_NOTES.ID_EPIS_REPORT%TYPE DEFAULT NULL,
      id_patient_in IN DISCHARGE_NOTES.ID_PATIENT%TYPE DEFAULT NULL,
      id_pending_issue_in IN DISCHARGE_NOTES.ID_PENDING_ISSUE%TYPE DEFAULT NULL,
      create_user_in IN DISCHARGE_NOTES.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN DISCHARGE_NOTES.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN DISCHARGE_NOTES.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN DISCHARGE_NOTES.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN DISCHARGE_NOTES.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN DISCHARGE_NOTES.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      flg_issue_assign_in IN DISCHARGE_NOTES.FLG_ISSUE_ASSIGN%TYPE DEFAULT NULL,
      discharge_instructions_in IN DISCHARGE_NOTES.DISCHARGE_INSTRUCTIONS%TYPE DEFAULT NULL,
      flg_csg_patient_in IN DISCHARGE_NOTES.FLG_CSG_PATIENT%TYPE DEFAULT NULL,
      dt_csg_patient_in IN DISCHARGE_NOTES.DT_CSG_PATIENT%TYPE DEFAULT NULL,
      id_discharge_notes_out IN OUT DISCHARGE_NOTES.ID_DISCHARGE_NOTES%TYPE
      -- Pass false if you want errors to propagate out unhandled
     ,handle_error_in IN BOOLEAN := TRUE
      );

   -- Insert one row with function, return generated primary key
   FUNCTION ins (
      id_episode_in IN DISCHARGE_NOTES.ID_EPISODE%TYPE DEFAULT NULL,
      id_professional_in IN DISCHARGE_NOTES.ID_PROFESSIONAL%TYPE DEFAULT NULL,
      epis_complaint_in IN DISCHARGE_NOTES.EPIS_COMPLAINT%TYPE DEFAULT NULL,
      epis_diagnosis_in IN DISCHARGE_NOTES.EPIS_DIAGNOSIS%TYPE DEFAULT NULL,
      epis_tests_in IN DISCHARGE_NOTES.EPIS_TESTS%TYPE DEFAULT NULL,
      epis_drugs_in IN DISCHARGE_NOTES.EPIS_DRUGS%TYPE DEFAULT NULL,
      recommended_in IN DISCHARGE_NOTES.RECOMMENDED%TYPE DEFAULT NULL,
      flg_status_in IN DISCHARGE_NOTES.FLG_STATUS%TYPE DEFAULT NULL,
      adw_last_update_in IN DISCHARGE_NOTES.ADW_LAST_UPDATE%TYPE DEFAULT sysdate,
      id_prof_cancel_in IN DISCHARGE_NOTES.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      dt_creation_tstz_in IN DISCHARGE_NOTES.DT_CREATION_TSTZ%TYPE DEFAULT NULL,
      dt_cancel_tstz_in IN DISCHARGE_NOTES.DT_CANCEL_TSTZ%TYPE DEFAULT NULL,
      release_from_in IN DISCHARGE_NOTES.RELEASE_FROM%TYPE DEFAULT NULL,
      dt_from_in IN DISCHARGE_NOTES.DT_FROM%TYPE DEFAULT NULL,
      dt_until_in IN DISCHARGE_NOTES.DT_UNTIL%TYPE DEFAULT NULL,
      notes_release_in IN DISCHARGE_NOTES.NOTES_RELEASE%TYPE DEFAULT NULL,
      follow_up_with_in IN DISCHARGE_NOTES.FOLLOW_UP_WITH%TYPE DEFAULT NULL,
      follow_up_in_in IN DISCHARGE_NOTES.FOLLOW_UP_IN%TYPE DEFAULT NULL,
      instructions_discussed_in IN DISCHARGE_NOTES.INSTRUCTIONS_DISCUSSED%TYPE DEFAULT NULL,
      id_follow_up_type_in IN DISCHARGE_NOTES.ID_FOLLOW_UP_TYPE%TYPE DEFAULT NULL,
      id_epis_report_in IN DISCHARGE_NOTES.ID_EPIS_REPORT%TYPE DEFAULT NULL,
      id_patient_in IN DISCHARGE_NOTES.ID_PATIENT%TYPE DEFAULT NULL,
      id_pending_issue_in IN DISCHARGE_NOTES.ID_PENDING_ISSUE%TYPE DEFAULT NULL,
      create_user_in IN DISCHARGE_NOTES.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN DISCHARGE_NOTES.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN DISCHARGE_NOTES.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN DISCHARGE_NOTES.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN DISCHARGE_NOTES.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN DISCHARGE_NOTES.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      flg_issue_assign_in IN DISCHARGE_NOTES.FLG_ISSUE_ASSIGN%TYPE DEFAULT NULL,
      discharge_instructions_in IN DISCHARGE_NOTES.DISCHARGE_INSTRUCTIONS%TYPE DEFAULT NULL,
      flg_csg_patient_in IN DISCHARGE_NOTES.FLG_CSG_PATIENT%TYPE DEFAULT NULL,
      dt_csg_patient_in IN DISCHARGE_NOTES.DT_CSG_PATIENT%TYPE DEFAULT NULL
      -- Pass false if you want errors to propagate out unhandled
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
      RETURN
         DISCHARGE_NOTES.ID_DISCHARGE_NOTES%TYPE
      ;

   FUNCTION ins (
      id_episode_in IN DISCHARGE_NOTES.ID_EPISODE%TYPE DEFAULT NULL,
      id_professional_in IN DISCHARGE_NOTES.ID_PROFESSIONAL%TYPE DEFAULT NULL,
      epis_complaint_in IN DISCHARGE_NOTES.EPIS_COMPLAINT%TYPE DEFAULT NULL,
      epis_diagnosis_in IN DISCHARGE_NOTES.EPIS_DIAGNOSIS%TYPE DEFAULT NULL,
      epis_tests_in IN DISCHARGE_NOTES.EPIS_TESTS%TYPE DEFAULT NULL,
      epis_drugs_in IN DISCHARGE_NOTES.EPIS_DRUGS%TYPE DEFAULT NULL,
      recommended_in IN DISCHARGE_NOTES.RECOMMENDED%TYPE DEFAULT NULL,
      flg_status_in IN DISCHARGE_NOTES.FLG_STATUS%TYPE DEFAULT NULL,
      adw_last_update_in IN DISCHARGE_NOTES.ADW_LAST_UPDATE%TYPE DEFAULT sysdate,
      id_prof_cancel_in IN DISCHARGE_NOTES.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      dt_creation_tstz_in IN DISCHARGE_NOTES.DT_CREATION_TSTZ%TYPE DEFAULT NULL,
      dt_cancel_tstz_in IN DISCHARGE_NOTES.DT_CANCEL_TSTZ%TYPE DEFAULT NULL,
      release_from_in IN DISCHARGE_NOTES.RELEASE_FROM%TYPE DEFAULT NULL,
      dt_from_in IN DISCHARGE_NOTES.DT_FROM%TYPE DEFAULT NULL,
      dt_until_in IN DISCHARGE_NOTES.DT_UNTIL%TYPE DEFAULT NULL,
      notes_release_in IN DISCHARGE_NOTES.NOTES_RELEASE%TYPE DEFAULT NULL,
      follow_up_with_in IN DISCHARGE_NOTES.FOLLOW_UP_WITH%TYPE DEFAULT NULL,
      follow_up_in_in IN DISCHARGE_NOTES.FOLLOW_UP_IN%TYPE DEFAULT NULL,
      instructions_discussed_in IN DISCHARGE_NOTES.INSTRUCTIONS_DISCUSSED%TYPE DEFAULT NULL,
      id_follow_up_type_in IN DISCHARGE_NOTES.ID_FOLLOW_UP_TYPE%TYPE DEFAULT NULL,
      id_epis_report_in IN DISCHARGE_NOTES.ID_EPIS_REPORT%TYPE DEFAULT NULL,
      id_patient_in IN DISCHARGE_NOTES.ID_PATIENT%TYPE DEFAULT NULL,
      id_pending_issue_in IN DISCHARGE_NOTES.ID_PENDING_ISSUE%TYPE DEFAULT NULL,
      create_user_in IN DISCHARGE_NOTES.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN DISCHARGE_NOTES.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN DISCHARGE_NOTES.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN DISCHARGE_NOTES.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN DISCHARGE_NOTES.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN DISCHARGE_NOTES.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      flg_issue_assign_in IN DISCHARGE_NOTES.FLG_ISSUE_ASSIGN%TYPE DEFAULT NULL,
      discharge_instructions_in IN DISCHARGE_NOTES.DISCHARGE_INSTRUCTIONS%TYPE DEFAULT NULL,
      flg_csg_patient_in IN DISCHARGE_NOTES.FLG_CSG_PATIENT%TYPE DEFAULT NULL,
      dt_csg_patient_in IN DISCHARGE_NOTES.DT_CSG_PATIENT%TYPE DEFAULT NULL
      -- Pass false if you want errors to propagate out unhandled
     ,handle_error_in IN BOOLEAN := TRUE
      )
      RETURN
         DISCHARGE_NOTES.ID_DISCHARGE_NOTES%TYPE
      ;

   -- Update any/all columns by primary key. If you pass NULL, then
   -- the current column value is set to itself. If you need a more
   -- selected UPDATE then use one of the onecol procedures below.

  PROCEDURE upd (
      id_discharge_notes_in IN DISCHARGE_NOTES.ID_DISCHARGE_NOTES%TYPE,
      id_episode_in IN DISCHARGE_NOTES.ID_EPISODE%TYPE DEFAULT NULL,
      ID_EPISODE_nin IN BOOLEAN := TRUE,
      id_professional_in IN DISCHARGE_NOTES.ID_PROFESSIONAL%TYPE DEFAULT NULL,
      ID_PROFESSIONAL_nin IN BOOLEAN := TRUE,
      epis_complaint_in IN DISCHARGE_NOTES.EPIS_COMPLAINT%TYPE DEFAULT NULL,
      EPIS_COMPLAINT_nin IN BOOLEAN := TRUE,
      epis_diagnosis_in IN DISCHARGE_NOTES.EPIS_DIAGNOSIS%TYPE DEFAULT NULL,
      EPIS_DIAGNOSIS_nin IN BOOLEAN := TRUE,
      epis_tests_in IN DISCHARGE_NOTES.EPIS_TESTS%TYPE DEFAULT NULL,
      EPIS_TESTS_nin IN BOOLEAN := TRUE,
      epis_drugs_in IN DISCHARGE_NOTES.EPIS_DRUGS%TYPE DEFAULT NULL,
      EPIS_DRUGS_nin IN BOOLEAN := TRUE,
      recommended_in IN DISCHARGE_NOTES.RECOMMENDED%TYPE DEFAULT NULL,
      RECOMMENDED_nin IN BOOLEAN := TRUE,
      flg_status_in IN DISCHARGE_NOTES.FLG_STATUS%TYPE DEFAULT NULL,
      FLG_STATUS_nin IN BOOLEAN := TRUE,
      adw_last_update_in IN DISCHARGE_NOTES.ADW_LAST_UPDATE%TYPE DEFAULT NULL,
      ADW_LAST_UPDATE_nin IN BOOLEAN := TRUE,
      id_prof_cancel_in IN DISCHARGE_NOTES.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      ID_PROF_CANCEL_nin IN BOOLEAN := TRUE,
      dt_creation_tstz_in IN DISCHARGE_NOTES.DT_CREATION_TSTZ%TYPE DEFAULT NULL,
      DT_CREATION_TSTZ_nin IN BOOLEAN := TRUE,
      dt_cancel_tstz_in IN DISCHARGE_NOTES.DT_CANCEL_TSTZ%TYPE DEFAULT NULL,
      DT_CANCEL_TSTZ_nin IN BOOLEAN := TRUE,
      release_from_in IN DISCHARGE_NOTES.RELEASE_FROM%TYPE DEFAULT NULL,
      RELEASE_FROM_nin IN BOOLEAN := TRUE,
      dt_from_in IN DISCHARGE_NOTES.DT_FROM%TYPE DEFAULT NULL,
      DT_FROM_nin IN BOOLEAN := TRUE,
      dt_until_in IN DISCHARGE_NOTES.DT_UNTIL%TYPE DEFAULT NULL,
      DT_UNTIL_nin IN BOOLEAN := TRUE,
      notes_release_in IN DISCHARGE_NOTES.NOTES_RELEASE%TYPE DEFAULT NULL,
      NOTES_RELEASE_nin IN BOOLEAN := TRUE,
      follow_up_with_in IN DISCHARGE_NOTES.FOLLOW_UP_WITH%TYPE DEFAULT NULL,
      FOLLOW_UP_WITH_nin IN BOOLEAN := TRUE,
      follow_up_in_in IN DISCHARGE_NOTES.FOLLOW_UP_IN%TYPE DEFAULT NULL,
      FOLLOW_UP_IN_nin IN BOOLEAN := TRUE,
      instructions_discussed_in IN DISCHARGE_NOTES.INSTRUCTIONS_DISCUSSED%TYPE DEFAULT NULL,
      INSTRUCTIONS_DISCUSSED_nin IN BOOLEAN := TRUE,
      id_follow_up_type_in IN DISCHARGE_NOTES.ID_FOLLOW_UP_TYPE%TYPE DEFAULT NULL,
      ID_FOLLOW_UP_TYPE_nin IN BOOLEAN := TRUE,
      id_epis_report_in IN DISCHARGE_NOTES.ID_EPIS_REPORT%TYPE DEFAULT NULL,
      ID_EPIS_REPORT_nin IN BOOLEAN := TRUE,
      id_patient_in IN DISCHARGE_NOTES.ID_PATIENT%TYPE DEFAULT NULL,
      ID_PATIENT_nin IN BOOLEAN := TRUE,
      id_pending_issue_in IN DISCHARGE_NOTES.ID_PENDING_ISSUE%TYPE DEFAULT NULL,
      ID_PENDING_ISSUE_nin IN BOOLEAN := TRUE,
      create_user_in IN DISCHARGE_NOTES.CREATE_USER%TYPE DEFAULT NULL,
      CREATE_USER_nin IN BOOLEAN := TRUE,
      create_time_in IN DISCHARGE_NOTES.CREATE_TIME%TYPE DEFAULT NULL,
      CREATE_TIME_nin IN BOOLEAN := TRUE,
      create_institution_in IN DISCHARGE_NOTES.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      CREATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      update_user_in IN DISCHARGE_NOTES.UPDATE_USER%TYPE DEFAULT NULL,
      UPDATE_USER_nin IN BOOLEAN := TRUE,
      update_time_in IN DISCHARGE_NOTES.UPDATE_TIME%TYPE DEFAULT NULL,
      UPDATE_TIME_nin IN BOOLEAN := TRUE,
      update_institution_in IN DISCHARGE_NOTES.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      UPDATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      flg_issue_assign_in IN DISCHARGE_NOTES.FLG_ISSUE_ASSIGN%TYPE DEFAULT NULL,
      FLG_ISSUE_ASSIGN_nin IN BOOLEAN := TRUE,
      discharge_instructions_in IN DISCHARGE_NOTES.DISCHARGE_INSTRUCTIONS%TYPE DEFAULT NULL,
      DISCHARGE_INSTRUCTIONS_nin IN BOOLEAN := TRUE,
      flg_csg_patient_in IN DISCHARGE_NOTES.FLG_CSG_PATIENT%TYPE DEFAULT NULL,
      FLG_CSG_PATIENT_nin IN BOOLEAN := TRUE,
      dt_csg_patient_in IN DISCHARGE_NOTES.DT_CSG_PATIENT%TYPE DEFAULT NULL,
      DT_CSG_PATIENT_nin IN BOOLEAN := TRUE,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      );


       PROCEDURE upd (
      id_discharge_notes_in IN DISCHARGE_NOTES.ID_DISCHARGE_NOTES%TYPE,
      id_episode_in IN DISCHARGE_NOTES.ID_EPISODE%TYPE DEFAULT NULL,
      ID_EPISODE_nin IN BOOLEAN := TRUE,
      id_professional_in IN DISCHARGE_NOTES.ID_PROFESSIONAL%TYPE DEFAULT NULL,
      ID_PROFESSIONAL_nin IN BOOLEAN := TRUE,
      epis_complaint_in IN DISCHARGE_NOTES.EPIS_COMPLAINT%TYPE DEFAULT NULL,
      EPIS_COMPLAINT_nin IN BOOLEAN := TRUE,
      epis_diagnosis_in IN DISCHARGE_NOTES.EPIS_DIAGNOSIS%TYPE DEFAULT NULL,
      EPIS_DIAGNOSIS_nin IN BOOLEAN := TRUE,
      epis_tests_in IN DISCHARGE_NOTES.EPIS_TESTS%TYPE DEFAULT NULL,
      EPIS_TESTS_nin IN BOOLEAN := TRUE,
      epis_drugs_in IN DISCHARGE_NOTES.EPIS_DRUGS%TYPE DEFAULT NULL,
      EPIS_DRUGS_nin IN BOOLEAN := TRUE,
      recommended_in IN DISCHARGE_NOTES.RECOMMENDED%TYPE DEFAULT NULL,
      RECOMMENDED_nin IN BOOLEAN := TRUE,
      flg_status_in IN DISCHARGE_NOTES.FLG_STATUS%TYPE DEFAULT NULL,
      FLG_STATUS_nin IN BOOLEAN := TRUE,
      adw_last_update_in IN DISCHARGE_NOTES.ADW_LAST_UPDATE%TYPE DEFAULT NULL,
      ADW_LAST_UPDATE_nin IN BOOLEAN := TRUE,
      id_prof_cancel_in IN DISCHARGE_NOTES.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      ID_PROF_CANCEL_nin IN BOOLEAN := TRUE,
      dt_creation_tstz_in IN DISCHARGE_NOTES.DT_CREATION_TSTZ%TYPE DEFAULT NULL,
      DT_CREATION_TSTZ_nin IN BOOLEAN := TRUE,
      dt_cancel_tstz_in IN DISCHARGE_NOTES.DT_CANCEL_TSTZ%TYPE DEFAULT NULL,
      DT_CANCEL_TSTZ_nin IN BOOLEAN := TRUE,
      release_from_in IN DISCHARGE_NOTES.RELEASE_FROM%TYPE DEFAULT NULL,
      RELEASE_FROM_nin IN BOOLEAN := TRUE,
      dt_from_in IN DISCHARGE_NOTES.DT_FROM%TYPE DEFAULT NULL,
      DT_FROM_nin IN BOOLEAN := TRUE,
      dt_until_in IN DISCHARGE_NOTES.DT_UNTIL%TYPE DEFAULT NULL,
      DT_UNTIL_nin IN BOOLEAN := TRUE,
      notes_release_in IN DISCHARGE_NOTES.NOTES_RELEASE%TYPE DEFAULT NULL,
      NOTES_RELEASE_nin IN BOOLEAN := TRUE,
      follow_up_with_in IN DISCHARGE_NOTES.FOLLOW_UP_WITH%TYPE DEFAULT NULL,
      FOLLOW_UP_WITH_nin IN BOOLEAN := TRUE,
      follow_up_in_in IN DISCHARGE_NOTES.FOLLOW_UP_IN%TYPE DEFAULT NULL,
      FOLLOW_UP_IN_nin IN BOOLEAN := TRUE,
      instructions_discussed_in IN DISCHARGE_NOTES.INSTRUCTIONS_DISCUSSED%TYPE DEFAULT NULL,
      INSTRUCTIONS_DISCUSSED_nin IN BOOLEAN := TRUE,
      id_follow_up_type_in IN DISCHARGE_NOTES.ID_FOLLOW_UP_TYPE%TYPE DEFAULT NULL,
      ID_FOLLOW_UP_TYPE_nin IN BOOLEAN := TRUE,
      id_epis_report_in IN DISCHARGE_NOTES.ID_EPIS_REPORT%TYPE DEFAULT NULL,
      ID_EPIS_REPORT_nin IN BOOLEAN := TRUE,
      id_patient_in IN DISCHARGE_NOTES.ID_PATIENT%TYPE DEFAULT NULL,
      ID_PATIENT_nin IN BOOLEAN := TRUE,
      id_pending_issue_in IN DISCHARGE_NOTES.ID_PENDING_ISSUE%TYPE DEFAULT NULL,
      ID_PENDING_ISSUE_nin IN BOOLEAN := TRUE,
      create_user_in IN DISCHARGE_NOTES.CREATE_USER%TYPE DEFAULT NULL,
      CREATE_USER_nin IN BOOLEAN := TRUE,
      create_time_in IN DISCHARGE_NOTES.CREATE_TIME%TYPE DEFAULT NULL,
      CREATE_TIME_nin IN BOOLEAN := TRUE,
      create_institution_in IN DISCHARGE_NOTES.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      CREATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      update_user_in IN DISCHARGE_NOTES.UPDATE_USER%TYPE DEFAULT NULL,
      UPDATE_USER_nin IN BOOLEAN := TRUE,
      update_time_in IN DISCHARGE_NOTES.UPDATE_TIME%TYPE DEFAULT NULL,
      UPDATE_TIME_nin IN BOOLEAN := TRUE,
      update_institution_in IN DISCHARGE_NOTES.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      UPDATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      flg_issue_assign_in IN DISCHARGE_NOTES.FLG_ISSUE_ASSIGN%TYPE DEFAULT NULL,
      FLG_ISSUE_ASSIGN_nin IN BOOLEAN := TRUE,
      discharge_instructions_in IN DISCHARGE_NOTES.DISCHARGE_INSTRUCTIONS%TYPE DEFAULT NULL,
      DISCHARGE_INSTRUCTIONS_nin IN BOOLEAN := TRUE,
      flg_csg_patient_in IN DISCHARGE_NOTES.FLG_CSG_PATIENT%TYPE DEFAULT NULL,
      FLG_CSG_PATIENT_nin IN BOOLEAN := TRUE,
      dt_csg_patient_in IN DISCHARGE_NOTES.DT_CSG_PATIENT%TYPE DEFAULT NULL,
      DT_CSG_PATIENT_nin IN BOOLEAN := TRUE,
     handle_error_in IN BOOLEAN := TRUE
      );



      PROCEDURE upd (
      id_episode_in IN DISCHARGE_NOTES.ID_EPISODE%TYPE DEFAULT NULL,
      ID_EPISODE_nin IN BOOLEAN := TRUE,
      id_professional_in IN DISCHARGE_NOTES.ID_PROFESSIONAL%TYPE DEFAULT NULL,
      ID_PROFESSIONAL_nin IN BOOLEAN := TRUE,
      epis_complaint_in IN DISCHARGE_NOTES.EPIS_COMPLAINT%TYPE DEFAULT NULL,
      EPIS_COMPLAINT_nin IN BOOLEAN := TRUE,
      epis_diagnosis_in IN DISCHARGE_NOTES.EPIS_DIAGNOSIS%TYPE DEFAULT NULL,
      EPIS_DIAGNOSIS_nin IN BOOLEAN := TRUE,
      epis_tests_in IN DISCHARGE_NOTES.EPIS_TESTS%TYPE DEFAULT NULL,
      EPIS_TESTS_nin IN BOOLEAN := TRUE,
      epis_drugs_in IN DISCHARGE_NOTES.EPIS_DRUGS%TYPE DEFAULT NULL,
      EPIS_DRUGS_nin IN BOOLEAN := TRUE,
      recommended_in IN DISCHARGE_NOTES.RECOMMENDED%TYPE DEFAULT NULL,
      RECOMMENDED_nin IN BOOLEAN := TRUE,
      flg_status_in IN DISCHARGE_NOTES.FLG_STATUS%TYPE DEFAULT NULL,
      FLG_STATUS_nin IN BOOLEAN := TRUE,
      adw_last_update_in IN DISCHARGE_NOTES.ADW_LAST_UPDATE%TYPE DEFAULT NULL,
      ADW_LAST_UPDATE_nin IN BOOLEAN := TRUE,
      id_prof_cancel_in IN DISCHARGE_NOTES.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      ID_PROF_CANCEL_nin IN BOOLEAN := TRUE,
      dt_creation_tstz_in IN DISCHARGE_NOTES.DT_CREATION_TSTZ%TYPE DEFAULT NULL,
      DT_CREATION_TSTZ_nin IN BOOLEAN := TRUE,
      dt_cancel_tstz_in IN DISCHARGE_NOTES.DT_CANCEL_TSTZ%TYPE DEFAULT NULL,
      DT_CANCEL_TSTZ_nin IN BOOLEAN := TRUE,
      release_from_in IN DISCHARGE_NOTES.RELEASE_FROM%TYPE DEFAULT NULL,
      RELEASE_FROM_nin IN BOOLEAN := TRUE,
      dt_from_in IN DISCHARGE_NOTES.DT_FROM%TYPE DEFAULT NULL,
      DT_FROM_nin IN BOOLEAN := TRUE,
      dt_until_in IN DISCHARGE_NOTES.DT_UNTIL%TYPE DEFAULT NULL,
      DT_UNTIL_nin IN BOOLEAN := TRUE,
      notes_release_in IN DISCHARGE_NOTES.NOTES_RELEASE%TYPE DEFAULT NULL,
      NOTES_RELEASE_nin IN BOOLEAN := TRUE,
      follow_up_with_in IN DISCHARGE_NOTES.FOLLOW_UP_WITH%TYPE DEFAULT NULL,
      FOLLOW_UP_WITH_nin IN BOOLEAN := TRUE,
      follow_up_in_in IN DISCHARGE_NOTES.FOLLOW_UP_IN%TYPE DEFAULT NULL,
      FOLLOW_UP_IN_nin IN BOOLEAN := TRUE,
      instructions_discussed_in IN DISCHARGE_NOTES.INSTRUCTIONS_DISCUSSED%TYPE DEFAULT NULL,
      INSTRUCTIONS_DISCUSSED_nin IN BOOLEAN := TRUE,
      id_follow_up_type_in IN DISCHARGE_NOTES.ID_FOLLOW_UP_TYPE%TYPE DEFAULT NULL,
      ID_FOLLOW_UP_TYPE_nin IN BOOLEAN := TRUE,
      id_epis_report_in IN DISCHARGE_NOTES.ID_EPIS_REPORT%TYPE DEFAULT NULL,
      ID_EPIS_REPORT_nin IN BOOLEAN := TRUE,
      id_patient_in IN DISCHARGE_NOTES.ID_PATIENT%TYPE DEFAULT NULL,
      ID_PATIENT_nin IN BOOLEAN := TRUE,
      id_pending_issue_in IN DISCHARGE_NOTES.ID_PENDING_ISSUE%TYPE DEFAULT NULL,
      ID_PENDING_ISSUE_nin IN BOOLEAN := TRUE,
      create_user_in IN DISCHARGE_NOTES.CREATE_USER%TYPE DEFAULT NULL,
      CREATE_USER_nin IN BOOLEAN := TRUE,
      create_time_in IN DISCHARGE_NOTES.CREATE_TIME%TYPE DEFAULT NULL,
      CREATE_TIME_nin IN BOOLEAN := TRUE,
      create_institution_in IN DISCHARGE_NOTES.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      CREATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      update_user_in IN DISCHARGE_NOTES.UPDATE_USER%TYPE DEFAULT NULL,
      UPDATE_USER_nin IN BOOLEAN := TRUE,
      update_time_in IN DISCHARGE_NOTES.UPDATE_TIME%TYPE DEFAULT NULL,
      UPDATE_TIME_nin IN BOOLEAN := TRUE,
      update_institution_in IN DISCHARGE_NOTES.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      UPDATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      flg_issue_assign_in IN DISCHARGE_NOTES.FLG_ISSUE_ASSIGN%TYPE DEFAULT NULL,
      FLG_ISSUE_ASSIGN_nin IN BOOLEAN := TRUE,
      discharge_instructions_in IN DISCHARGE_NOTES.DISCHARGE_INSTRUCTIONS%TYPE DEFAULT NULL,
      DISCHARGE_INSTRUCTIONS_nin IN BOOLEAN := TRUE,
      flg_csg_patient_in IN DISCHARGE_NOTES.FLG_CSG_PATIENT%TYPE DEFAULT NULL,
      FLG_CSG_PATIENT_nin IN BOOLEAN := TRUE,
      dt_csg_patient_in IN DISCHARGE_NOTES.DT_CSG_PATIENT%TYPE DEFAULT NULL,
      DT_CSG_PATIENT_nin IN BOOLEAN := TRUE,
    where_in varchar2,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      );


      PROCEDURE upd (
      id_episode_in IN DISCHARGE_NOTES.ID_EPISODE%TYPE DEFAULT NULL,
      ID_EPISODE_nin IN BOOLEAN := TRUE,
      id_professional_in IN DISCHARGE_NOTES.ID_PROFESSIONAL%TYPE DEFAULT NULL,
      ID_PROFESSIONAL_nin IN BOOLEAN := TRUE,
      epis_complaint_in IN DISCHARGE_NOTES.EPIS_COMPLAINT%TYPE DEFAULT NULL,
      EPIS_COMPLAINT_nin IN BOOLEAN := TRUE,
      epis_diagnosis_in IN DISCHARGE_NOTES.EPIS_DIAGNOSIS%TYPE DEFAULT NULL,
      EPIS_DIAGNOSIS_nin IN BOOLEAN := TRUE,
      epis_tests_in IN DISCHARGE_NOTES.EPIS_TESTS%TYPE DEFAULT NULL,
      EPIS_TESTS_nin IN BOOLEAN := TRUE,
      epis_drugs_in IN DISCHARGE_NOTES.EPIS_DRUGS%TYPE DEFAULT NULL,
      EPIS_DRUGS_nin IN BOOLEAN := TRUE,
      recommended_in IN DISCHARGE_NOTES.RECOMMENDED%TYPE DEFAULT NULL,
      RECOMMENDED_nin IN BOOLEAN := TRUE,
      flg_status_in IN DISCHARGE_NOTES.FLG_STATUS%TYPE DEFAULT NULL,
      FLG_STATUS_nin IN BOOLEAN := TRUE,
      adw_last_update_in IN DISCHARGE_NOTES.ADW_LAST_UPDATE%TYPE DEFAULT NULL,
      ADW_LAST_UPDATE_nin IN BOOLEAN := TRUE,
      id_prof_cancel_in IN DISCHARGE_NOTES.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      ID_PROF_CANCEL_nin IN BOOLEAN := TRUE,
      dt_creation_tstz_in IN DISCHARGE_NOTES.DT_CREATION_TSTZ%TYPE DEFAULT NULL,
      DT_CREATION_TSTZ_nin IN BOOLEAN := TRUE,
      dt_cancel_tstz_in IN DISCHARGE_NOTES.DT_CANCEL_TSTZ%TYPE DEFAULT NULL,
      DT_CANCEL_TSTZ_nin IN BOOLEAN := TRUE,
      release_from_in IN DISCHARGE_NOTES.RELEASE_FROM%TYPE DEFAULT NULL,
      RELEASE_FROM_nin IN BOOLEAN := TRUE,
      dt_from_in IN DISCHARGE_NOTES.DT_FROM%TYPE DEFAULT NULL,
      DT_FROM_nin IN BOOLEAN := TRUE,
      dt_until_in IN DISCHARGE_NOTES.DT_UNTIL%TYPE DEFAULT NULL,
      DT_UNTIL_nin IN BOOLEAN := TRUE,
      notes_release_in IN DISCHARGE_NOTES.NOTES_RELEASE%TYPE DEFAULT NULL,
      NOTES_RELEASE_nin IN BOOLEAN := TRUE,
      follow_up_with_in IN DISCHARGE_NOTES.FOLLOW_UP_WITH%TYPE DEFAULT NULL,
      FOLLOW_UP_WITH_nin IN BOOLEAN := TRUE,
      follow_up_in_in IN DISCHARGE_NOTES.FOLLOW_UP_IN%TYPE DEFAULT NULL,
      FOLLOW_UP_IN_nin IN BOOLEAN := TRUE,
      instructions_discussed_in IN DISCHARGE_NOTES.INSTRUCTIONS_DISCUSSED%TYPE DEFAULT NULL,
      INSTRUCTIONS_DISCUSSED_nin IN BOOLEAN := TRUE,
      id_follow_up_type_in IN DISCHARGE_NOTES.ID_FOLLOW_UP_TYPE%TYPE DEFAULT NULL,
      ID_FOLLOW_UP_TYPE_nin IN BOOLEAN := TRUE,
      id_epis_report_in IN DISCHARGE_NOTES.ID_EPIS_REPORT%TYPE DEFAULT NULL,
      ID_EPIS_REPORT_nin IN BOOLEAN := TRUE,
      id_patient_in IN DISCHARGE_NOTES.ID_PATIENT%TYPE DEFAULT NULL,
      ID_PATIENT_nin IN BOOLEAN := TRUE,
      id_pending_issue_in IN DISCHARGE_NOTES.ID_PENDING_ISSUE%TYPE DEFAULT NULL,
      ID_PENDING_ISSUE_nin IN BOOLEAN := TRUE,
      create_user_in IN DISCHARGE_NOTES.CREATE_USER%TYPE DEFAULT NULL,
      CREATE_USER_nin IN BOOLEAN := TRUE,
      create_time_in IN DISCHARGE_NOTES.CREATE_TIME%TYPE DEFAULT NULL,
      CREATE_TIME_nin IN BOOLEAN := TRUE,
      create_institution_in IN DISCHARGE_NOTES.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      CREATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      update_user_in IN DISCHARGE_NOTES.UPDATE_USER%TYPE DEFAULT NULL,
      UPDATE_USER_nin IN BOOLEAN := TRUE,
      update_time_in IN DISCHARGE_NOTES.UPDATE_TIME%TYPE DEFAULT NULL,
      UPDATE_TIME_nin IN BOOLEAN := TRUE,
      update_institution_in IN DISCHARGE_NOTES.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      UPDATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      flg_issue_assign_in IN DISCHARGE_NOTES.FLG_ISSUE_ASSIGN%TYPE DEFAULT NULL,
      FLG_ISSUE_ASSIGN_nin IN BOOLEAN := TRUE,
      discharge_instructions_in IN DISCHARGE_NOTES.DISCHARGE_INSTRUCTIONS%TYPE DEFAULT NULL,
      DISCHARGE_INSTRUCTIONS_nin IN BOOLEAN := TRUE,
      flg_csg_patient_in IN DISCHARGE_NOTES.FLG_CSG_PATIENT%TYPE DEFAULT NULL,
      FLG_CSG_PATIENT_nin IN BOOLEAN := TRUE,
      dt_csg_patient_in IN DISCHARGE_NOTES.DT_CSG_PATIENT%TYPE DEFAULT NULL,
      DT_CSG_PATIENT_nin IN BOOLEAN := TRUE,
    where_in varchar2,
     handle_error_in IN BOOLEAN := TRUE
      );



   PROCEDURE upd_ins (
      id_discharge_notes_in IN DISCHARGE_NOTES.ID_DISCHARGE_NOTES%TYPE,
      id_episode_in IN DISCHARGE_NOTES.ID_EPISODE%TYPE DEFAULT NULL,
      id_professional_in IN DISCHARGE_NOTES.ID_PROFESSIONAL%TYPE DEFAULT NULL,
      epis_complaint_in IN DISCHARGE_NOTES.EPIS_COMPLAINT%TYPE DEFAULT NULL,
      epis_diagnosis_in IN DISCHARGE_NOTES.EPIS_DIAGNOSIS%TYPE DEFAULT NULL,
      epis_tests_in IN DISCHARGE_NOTES.EPIS_TESTS%TYPE DEFAULT NULL,
      epis_drugs_in IN DISCHARGE_NOTES.EPIS_DRUGS%TYPE DEFAULT NULL,
      recommended_in IN DISCHARGE_NOTES.RECOMMENDED%TYPE DEFAULT NULL,
      flg_status_in IN DISCHARGE_NOTES.FLG_STATUS%TYPE DEFAULT NULL,
      adw_last_update_in IN DISCHARGE_NOTES.ADW_LAST_UPDATE%TYPE DEFAULT NULL,
      id_prof_cancel_in IN DISCHARGE_NOTES.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      dt_creation_tstz_in IN DISCHARGE_NOTES.DT_CREATION_TSTZ%TYPE DEFAULT NULL,
      dt_cancel_tstz_in IN DISCHARGE_NOTES.DT_CANCEL_TSTZ%TYPE DEFAULT NULL,
      release_from_in IN DISCHARGE_NOTES.RELEASE_FROM%TYPE DEFAULT NULL,
      dt_from_in IN DISCHARGE_NOTES.DT_FROM%TYPE DEFAULT NULL,
      dt_until_in IN DISCHARGE_NOTES.DT_UNTIL%TYPE DEFAULT NULL,
      notes_release_in IN DISCHARGE_NOTES.NOTES_RELEASE%TYPE DEFAULT NULL,
      follow_up_with_in IN DISCHARGE_NOTES.FOLLOW_UP_WITH%TYPE DEFAULT NULL,
      follow_up_in_in IN DISCHARGE_NOTES.FOLLOW_UP_IN%TYPE DEFAULT NULL,
      instructions_discussed_in IN DISCHARGE_NOTES.INSTRUCTIONS_DISCUSSED%TYPE DEFAULT NULL,
      id_follow_up_type_in IN DISCHARGE_NOTES.ID_FOLLOW_UP_TYPE%TYPE DEFAULT NULL,
      id_epis_report_in IN DISCHARGE_NOTES.ID_EPIS_REPORT%TYPE DEFAULT NULL,
      id_patient_in IN DISCHARGE_NOTES.ID_PATIENT%TYPE DEFAULT NULL,
      id_pending_issue_in IN DISCHARGE_NOTES.ID_PENDING_ISSUE%TYPE DEFAULT NULL,
      create_user_in IN DISCHARGE_NOTES.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN DISCHARGE_NOTES.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN DISCHARGE_NOTES.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN DISCHARGE_NOTES.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN DISCHARGE_NOTES.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN DISCHARGE_NOTES.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      flg_issue_assign_in IN DISCHARGE_NOTES.FLG_ISSUE_ASSIGN%TYPE DEFAULT NULL,
      discharge_instructions_in IN DISCHARGE_NOTES.DISCHARGE_INSTRUCTIONS%TYPE DEFAULT NULL,
      flg_csg_patient_in IN DISCHARGE_NOTES.FLG_CSG_PATIENT%TYPE DEFAULT NULL,
      dt_csg_patient_in IN DISCHARGE_NOTES.DT_CSG_PATIENT%TYPE DEFAULT NULL,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      );

      PROCEDURE upd_ins (
      id_discharge_notes_in IN DISCHARGE_NOTES.ID_DISCHARGE_NOTES%TYPE,
      id_episode_in IN DISCHARGE_NOTES.ID_EPISODE%TYPE DEFAULT NULL,
      id_professional_in IN DISCHARGE_NOTES.ID_PROFESSIONAL%TYPE DEFAULT NULL,
      epis_complaint_in IN DISCHARGE_NOTES.EPIS_COMPLAINT%TYPE DEFAULT NULL,
      epis_diagnosis_in IN DISCHARGE_NOTES.EPIS_DIAGNOSIS%TYPE DEFAULT NULL,
      epis_tests_in IN DISCHARGE_NOTES.EPIS_TESTS%TYPE DEFAULT NULL,
      epis_drugs_in IN DISCHARGE_NOTES.EPIS_DRUGS%TYPE DEFAULT NULL,
      recommended_in IN DISCHARGE_NOTES.RECOMMENDED%TYPE DEFAULT NULL,
      flg_status_in IN DISCHARGE_NOTES.FLG_STATUS%TYPE DEFAULT NULL,
      adw_last_update_in IN DISCHARGE_NOTES.ADW_LAST_UPDATE%TYPE DEFAULT NULL,
      id_prof_cancel_in IN DISCHARGE_NOTES.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      dt_creation_tstz_in IN DISCHARGE_NOTES.DT_CREATION_TSTZ%TYPE DEFAULT NULL,
      dt_cancel_tstz_in IN DISCHARGE_NOTES.DT_CANCEL_TSTZ%TYPE DEFAULT NULL,
      release_from_in IN DISCHARGE_NOTES.RELEASE_FROM%TYPE DEFAULT NULL,
      dt_from_in IN DISCHARGE_NOTES.DT_FROM%TYPE DEFAULT NULL,
      dt_until_in IN DISCHARGE_NOTES.DT_UNTIL%TYPE DEFAULT NULL,
      notes_release_in IN DISCHARGE_NOTES.NOTES_RELEASE%TYPE DEFAULT NULL,
      follow_up_with_in IN DISCHARGE_NOTES.FOLLOW_UP_WITH%TYPE DEFAULT NULL,
      follow_up_in_in IN DISCHARGE_NOTES.FOLLOW_UP_IN%TYPE DEFAULT NULL,
      instructions_discussed_in IN DISCHARGE_NOTES.INSTRUCTIONS_DISCUSSED%TYPE DEFAULT NULL,
      id_follow_up_type_in IN DISCHARGE_NOTES.ID_FOLLOW_UP_TYPE%TYPE DEFAULT NULL,
      id_epis_report_in IN DISCHARGE_NOTES.ID_EPIS_REPORT%TYPE DEFAULT NULL,
      id_patient_in IN DISCHARGE_NOTES.ID_PATIENT%TYPE DEFAULT NULL,
      id_pending_issue_in IN DISCHARGE_NOTES.ID_PENDING_ISSUE%TYPE DEFAULT NULL,
      create_user_in IN DISCHARGE_NOTES.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN DISCHARGE_NOTES.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN DISCHARGE_NOTES.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN DISCHARGE_NOTES.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN DISCHARGE_NOTES.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN DISCHARGE_NOTES.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      flg_issue_assign_in IN DISCHARGE_NOTES.FLG_ISSUE_ASSIGN%TYPE DEFAULT NULL,
      discharge_instructions_in IN DISCHARGE_NOTES.DISCHARGE_INSTRUCTIONS%TYPE DEFAULT NULL,
      flg_csg_patient_in IN DISCHARGE_NOTES.FLG_CSG_PATIENT%TYPE DEFAULT NULL,
      dt_csg_patient_in IN DISCHARGE_NOTES.DT_CSG_PATIENT%TYPE DEFAULT NULL,
     handle_error_in IN BOOLEAN := TRUE
      );

   PROCEDURE upd (
      rec_in IN DISCHARGE_NOTES%ROWTYPE,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      );

      PROCEDURE upd (
      rec_in IN DISCHARGE_NOTES%ROWTYPE,
     handle_error_in IN BOOLEAN := TRUE
      );

      PROCEDURE upd (
      col_in IN DISCHARGE_NOTES_tc,
      ignore_if_null_in IN BOOLEAN := TRUE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      );

      PROCEDURE upd (
      col_in IN DISCHARGE_NOTES_tc,
      ignore_if_null_in IN BOOLEAN := TRUE
     ,handle_error_in IN BOOLEAN := TRUE
      );





   -- Use Native Dynamic SQL increment a single NUMBER column
   -- for all rows specified by the dynamic WHERE clause
   PROCEDURE increment_onecol (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL
      , increment_value_in IN NUMBER DEFAULT 1
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   );

   PROCEDURE increment_onecol (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL
      , increment_value_in IN NUMBER DEFAULT 1
     ,handle_error_in IN BOOLEAN := TRUE
   );









    -- Delete one row by primary key
   PROCEDURE del (
      id_discharge_notes_in IN DISCHARGE_NOTES.ID_DISCHARGE_NOTES%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      );

-- Delete one row by primary key
   PROCEDURE del (
      id_discharge_notes_in IN DISCHARGE_NOTES.ID_DISCHARGE_NOTES%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      );


   -- Delete all rows for primary key column ID_DISCHARGE_NOTES
   PROCEDURE del_ID_DISCHARGE_NOTES (
      id_discharge_notes_in IN DISCHARGE_NOTES.ID_DISCHARGE_NOTES%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     );

     -- Delete all rows for primary key column ID_DISCHARGE_NOTES
   PROCEDURE del_ID_DISCHARGE_NOTES (
      id_discharge_notes_in IN DISCHARGE_NOTES.ID_DISCHARGE_NOTES%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
     );

   -- Delete all rows for this DNT_EPI_RPT_FK foreign key value
   PROCEDURE del_DNT_EPI_RPT_FK (
      id_epis_report_in IN DISCHARGE_NOTES.ID_EPIS_REPORT%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      );

      -- Delete all rows for this DNT_EPI_RPT_FK foreign key value
   PROCEDURE del_DNT_EPI_RPT_FK (
      id_epis_report_in IN DISCHARGE_NOTES.ID_EPIS_REPORT%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      );

   -- Delete all rows for this DNT_EPIS_FK foreign key value
   PROCEDURE del_DNT_EPIS_FK (
      id_episode_in IN DISCHARGE_NOTES.ID_EPISODE%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      );

      -- Delete all rows for this DNT_EPIS_FK foreign key value
   PROCEDURE del_DNT_EPIS_FK (
      id_episode_in IN DISCHARGE_NOTES.ID_EPISODE%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      );

   -- Delete all rows for this DNT_F_UP_TY_FK foreign key value
   PROCEDURE del_DNT_F_UP_TY_FK (
      id_follow_up_type_in IN DISCHARGE_NOTES.ID_FOLLOW_UP_TYPE%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      );

      -- Delete all rows for this DNT_F_UP_TY_FK foreign key value
   PROCEDURE del_DNT_F_UP_TY_FK (
      id_follow_up_type_in IN DISCHARGE_NOTES.ID_FOLLOW_UP_TYPE%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      );

   -- Delete all rows for this DNT_PAT_FK foreign key value
   PROCEDURE del_DNT_PAT_FK (
      id_patient_in IN DISCHARGE_NOTES.ID_PATIENT%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      );

      -- Delete all rows for this DNT_PAT_FK foreign key value
   PROCEDURE del_DNT_PAT_FK (
      id_patient_in IN DISCHARGE_NOTES.ID_PATIENT%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      );

   -- Delete all rows for this DNT_PENDISSUE_FK foreign key value
   PROCEDURE del_DNT_PENDISSUE_FK (
      id_pending_issue_in IN DISCHARGE_NOTES.ID_PENDING_ISSUE%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      );

      -- Delete all rows for this DNT_PENDISSUE_FK foreign key value
   PROCEDURE del_DNT_PENDISSUE_FK (
      id_pending_issue_in IN DISCHARGE_NOTES.ID_PENDING_ISSUE%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      );

   -- Delete all rows for this DNT_PROF_FK foreign key value
   PROCEDURE del_DNT_PROF_FK (
      id_professional_in IN DISCHARGE_NOTES.ID_PROFESSIONAL%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      );

      -- Delete all rows for this DNT_PROF_FK foreign key value
   PROCEDURE del_DNT_PROF_FK (
      id_professional_in IN DISCHARGE_NOTES.ID_PROFESSIONAL%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      );

   -- Delete all rows for this DNT_PROF_FK2 foreign key value
   PROCEDURE del_DNT_PROF_FK2 (
      id_prof_cancel_in IN DISCHARGE_NOTES.ID_PROF_CANCEL%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      );

      -- Delete all rows for this DNT_PROF_FK2 foreign key value
   PROCEDURE del_DNT_PROF_FK2 (
      id_prof_cancel_in IN DISCHARGE_NOTES.ID_PROF_CANCEL%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      );

    -- Delete all rows specified by dynamic WHERE clause
   PROCEDURE del_by (
      where_clause_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
      );

      -- Delete all rows specified by dynamic WHERE clause
   PROCEDURE del_by (
      where_clause_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      );

    -- Delete all rows where the specified VARCHAR2 column has
   -- a value that matches the specfified value.
   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
      );


      -- Delete all rows where the specified VARCHAR2 column has
   -- a value that matches the specfified value.
   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      );

    -- Delete all rows where the specified DATE column has
   -- a value that matches the specfified value.
   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN DATE
     ,handle_error_in IN BOOLEAN := TRUE
      );

      -- Delete all rows where the specified DATE column has
   -- a value that matches the specfified value.
   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN DATE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      );


      -- Delete all rows where the specified TIMESTAMP column has
   -- a value that matches the specfified value.
   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN TIMESTAMP WITH LOCAL TIME ZONE
     ,handle_error_in IN BOOLEAN := TRUE
      );

      -- Delete all rows where the specified TIMESTAMP column has
   -- a value that matches the specfified value.
   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN TIMESTAMP WITH LOCAL TIME ZONE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      );

    -- Delete all rows where the specified NUMBER column has
   -- a value that matches the specfified value.
   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN NUMBER
     ,handle_error_in IN BOOLEAN := TRUE
      );

      -- Delete all rows where the specified NUMBER column has
   -- a value that matches the specfified value.
   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN NUMBER
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      );

   -- Initialize a record with default values for columns in the table.
   PROCEDURE initrec (
      discharge_notes_inout IN OUT DISCHARGE_NOTES%ROWTYPE
   );

   FUNCTION initrec RETURN DISCHARGE_NOTES%ROWTYPE;



   FUNCTION get_data_rowid(
        rows_in IN TABLE_VARCHAR
        ) RETURN DISCHARGE_NOTES_tc;


   FUNCTION get_data_rowid_pat(
        rows_in IN TABLE_VARCHAR
        ) RETURN DISCHARGE_NOTES_tc;

END TS_DISCHARGE_NOTES;
/
