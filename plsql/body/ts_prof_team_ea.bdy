/*-- Last Change Revision: $Rev: 2028347 $*/
/*-- Last Change by: $Author: mario.fernandes $*/
/*-- Date of last change: $Date: 2022-08-02 18:45:15 +0100 (ter, 02 ago 2022) $*/

CREATE OR REPLACE PACKAGE BODY TS_PROF_TEAM_EA
/*
| Generated by or retrieved from QCGU - DO NOT MODIFY!
| QCGU - "Get it right, do it fast" - www.ToadWorld.com
| QCGU Universal ID: {657BD35E-2E48-4CD4-B14E-41420B8433B3}
| Created On: July 3, 2009 11:27:30
| Created By: ALERT
*/
IS

   e_null_column_value EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_null_column_value, -1400);
   --
   e_existing_fky_reference EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_existing_fky_reference, -2266);
   --
   e_check_constraint_failure EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_check_constraint_failure, -2290);
   --
   e_no_parent_key EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_no_parent_key, -2291);
   --
   e_child_record_found EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_child_record_found, -2292);
   --
   e_forall_error EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_forall_error, -24381);
   --
   -- Defined for backward compatibilty.
   e_integ_constraint_failure EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_integ_constraint_failure, -2291);

    -- Private utilities
   PROCEDURE get_constraint_info (
      owner_out OUT ALL_CONSTRAINTS.OWNER%TYPE
     ,name_out OUT ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE)
   IS
      l_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
      dotloc PLS_INTEGER;
      leftloc PLS_INTEGER;
   BEGIN
      dotloc  := INSTR (l_errm,'.');
      leftloc := INSTR (l_errm,'(');
      owner_out := SUBSTR (l_errm, leftloc+1, dotloc-leftloc-1);
      name_out  := SUBSTR (l_errm, dotloc+1, INSTR (l_errm,')')-dotloc-1);
   END get_constraint_info;
   -- Public programs

   PROCEDURE ins (
      id_prof_team_in IN PROF_TEAM_EA.ID_PROF_TEAM%TYPE
      ,
      id_prof_team_leader_in IN PROF_TEAM_EA.ID_PROF_TEAM_LEADER%TYPE DEFAULT NULL,
      prof_team_name_in IN PROF_TEAM_EA.PROF_TEAM_NAME%TYPE DEFAULT NULL,
      prof_team_desc_in IN PROF_TEAM_EA.PROF_TEAM_DESC%TYPE DEFAULT NULL,
      flg_available_in IN PROF_TEAM_EA.FLG_AVAILABLE%TYPE DEFAULT NULL,
      flg_status_in IN PROF_TEAM_EA.FLG_STATUS%TYPE DEFAULT NULL,
      id_software_in IN PROF_TEAM_EA.ID_SOFTWARE%TYPE DEFAULT NULL,
      id_institution_in IN PROF_TEAM_EA.ID_INSTITUTION%TYPE DEFAULT NULL,
      flg_type_in IN PROF_TEAM_EA.FLG_TYPE%TYPE DEFAULT NULL,
      dt_begin_tstz_in IN PROF_TEAM_EA.DT_BEGIN_TSTZ%TYPE DEFAULT NULL,
      dt_end_tstz_in IN PROF_TEAM_EA.DT_END_TSTZ%TYPE DEFAULT NULL,
      id_department_in IN PROF_TEAM_EA.ID_DEPARTMENT%TYPE DEFAULT NULL,
      notes_in IN PROF_TEAM_EA.NOTES%TYPE DEFAULT NULL,
      create_time_in IN PROF_TEAM_EA.CREATE_TIME%TYPE DEFAULT NULL,
      create_user_in IN PROF_TEAM_EA.CREATE_USER%TYPE DEFAULT NULL,
      num_members_in IN PROF_TEAM_EA.NUM_MEMBERS%TYPE DEFAULT NULL,
      create_institution_in IN PROF_TEAM_EA.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN PROF_TEAM_EA.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN PROF_TEAM_EA.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN PROF_TEAM_EA.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      id_prof_register_in IN PROF_TEAM_EA.ID_PROF_REGISTER%TYPE DEFAULT NULL,
      dt_register_in IN PROF_TEAM_EA.DT_REGISTER%TYPE DEFAULT NULL,
      id_team_type_in IN PROF_TEAM_EA.ID_TEAM_TYPE%TYPE DEFAULT NULL
     ,handle_error_in IN BOOLEAN := TRUE
      , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN

     INSERT INTO PROF_TEAM_EA (
         ID_PROF_TEAM,
         ID_PROF_TEAM_LEADER,
         PROF_TEAM_NAME,
         PROF_TEAM_DESC,
         FLG_AVAILABLE,
         FLG_STATUS,
         ID_SOFTWARE,
         ID_INSTITUTION,
         FLG_TYPE,
         DT_BEGIN_TSTZ,
         DT_END_TSTZ,
         ID_DEPARTMENT,
         NOTES,
         CREATE_TIME,
         CREATE_USER,
         NUM_MEMBERS,
         CREATE_INSTITUTION,
         UPDATE_USER,
         UPDATE_TIME,
         UPDATE_INSTITUTION,
         ID_PROF_REGISTER,
         DT_REGISTER,
         ID_TEAM_TYPE
         )
      VALUES (
         id_prof_team_in,
         id_prof_team_leader_in,
         prof_team_name_in,
         prof_team_desc_in,
         flg_available_in,
         flg_status_in,
         id_software_in,
         id_institution_in,
         flg_type_in,
         dt_begin_tstz_in,
         dt_end_tstz_in,
         id_department_in,
         notes_in,
         create_time_in,
         create_user_in,
         num_members_in,
         create_institution_in,
         update_user_in,
         update_time_in,
         update_institution_in,
         id_prof_register_in,
         dt_register_in,
         id_team_type_in
         ) RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF FALSE THEN NULL; -- Placeholder in case no unique indexes
           ELSE
              pk_alert_exceptions.raise_error (
                    error_name_in => 'DUPLICATE-VALUE'
                    ,name1_in => 'OWNER'
                    ,value1_in => l_owner
                    ,name2_in => 'CONSTRAINT_NAME'
                    ,value2_in => l_name
                    ,name3_in => 'TABLE_NAME'
                    ,value3_in => 'PROF_TEAM_EA');
           END IF;
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PROF_TEAM_EA');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PROF_TEAM_EA');
           IF l_name = 'PROF_TEAMEA_DEP_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_DEPARTMENT'
               , value_in => id_department_in);
           END IF;
           IF l_name = 'PROF_TEAMEA_INST_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_INSTITUTION'
               , value_in => id_institution_in);
           END IF;
           IF l_name = 'PROF_TEAMEA_PROFRG_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_PROF_REGISTER'
               , value_in => id_prof_register_in);
           END IF;
           IF l_name = 'PROF_TEAMEA_PROFR_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'CREATE_USER'
               , value_in => create_user_in);
           END IF;
           IF l_name = 'PROF_TEAMEA_PROF_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_PROF_TEAM_LEADER'
               , value_in => id_prof_team_leader_in);
           END IF;
           IF l_name = 'PROF_TEAMEA_SOFT_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_SOFTWARE'
               , value_in => id_software_in);
           END IF;
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END ins;

   PROCEDURE ins (
      id_prof_team_in IN PROF_TEAM_EA.ID_PROF_TEAM%TYPE
      ,
      id_prof_team_leader_in IN PROF_TEAM_EA.ID_PROF_TEAM_LEADER%TYPE DEFAULT NULL,
      prof_team_name_in IN PROF_TEAM_EA.PROF_TEAM_NAME%TYPE DEFAULT NULL,
      prof_team_desc_in IN PROF_TEAM_EA.PROF_TEAM_DESC%TYPE DEFAULT NULL,
      flg_available_in IN PROF_TEAM_EA.FLG_AVAILABLE%TYPE DEFAULT NULL,
      flg_status_in IN PROF_TEAM_EA.FLG_STATUS%TYPE DEFAULT NULL,
      id_software_in IN PROF_TEAM_EA.ID_SOFTWARE%TYPE DEFAULT NULL,
      id_institution_in IN PROF_TEAM_EA.ID_INSTITUTION%TYPE DEFAULT NULL,
      flg_type_in IN PROF_TEAM_EA.FLG_TYPE%TYPE DEFAULT NULL,
      dt_begin_tstz_in IN PROF_TEAM_EA.DT_BEGIN_TSTZ%TYPE DEFAULT NULL,
      dt_end_tstz_in IN PROF_TEAM_EA.DT_END_TSTZ%TYPE DEFAULT NULL,
      id_department_in IN PROF_TEAM_EA.ID_DEPARTMENT%TYPE DEFAULT NULL,
      notes_in IN PROF_TEAM_EA.NOTES%TYPE DEFAULT NULL,
      create_time_in IN PROF_TEAM_EA.CREATE_TIME%TYPE DEFAULT NULL,
      create_user_in IN PROF_TEAM_EA.CREATE_USER%TYPE DEFAULT NULL,
      num_members_in IN PROF_TEAM_EA.NUM_MEMBERS%TYPE DEFAULT NULL,
      create_institution_in IN PROF_TEAM_EA.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN PROF_TEAM_EA.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN PROF_TEAM_EA.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN PROF_TEAM_EA.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      id_prof_register_in IN PROF_TEAM_EA.ID_PROF_REGISTER%TYPE DEFAULT NULL,
      dt_register_in IN PROF_TEAM_EA.DT_REGISTER%TYPE DEFAULT NULL,
      id_team_type_in IN PROF_TEAM_EA.ID_TEAM_TYPE%TYPE DEFAULT NULL
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN

     ins (
      id_prof_team_in => id_prof_team_in
      ,
      id_prof_team_leader_in => id_prof_team_leader_in,
      prof_team_name_in => prof_team_name_in,
      prof_team_desc_in => prof_team_desc_in,
      flg_available_in => flg_available_in,
      flg_status_in => flg_status_in,
      id_software_in => id_software_in,
      id_institution_in => id_institution_in,
      flg_type_in => flg_type_in,
      dt_begin_tstz_in => dt_begin_tstz_in,
      dt_end_tstz_in => dt_end_tstz_in,
      id_department_in => id_department_in,
      notes_in => notes_in,
      create_time_in => create_time_in,
      create_user_in => create_user_in,
      num_members_in => num_members_in,
      create_institution_in => create_institution_in,
      update_user_in => update_user_in,
      update_time_in => update_time_in,
      update_institution_in => update_institution_in,
      id_prof_register_in => id_prof_register_in,
      dt_register_in => dt_register_in,
      id_team_type_in => id_team_type_in
     ,handle_error_in => handle_error_in
      ,rows_out => rows_out
      );
   END ins;


   PROCEDURE ins (
      rec_in IN PROF_TEAM_EA%ROWTYPE
     ,sequence_in IN VARCHAR2 := NULL
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
      l_rec PROF_TEAM_EA%ROWTYPE := rec_in;
   BEGIN
      ins (
         id_prof_team_in => l_rec.ID_PROF_TEAM
         ,
         id_prof_team_leader_in => l_rec.ID_PROF_TEAM_LEADER,
         prof_team_name_in => l_rec.PROF_TEAM_NAME,
         prof_team_desc_in => l_rec.PROF_TEAM_DESC,
         flg_available_in => l_rec.FLG_AVAILABLE,
         flg_status_in => l_rec.FLG_STATUS,
         id_software_in => l_rec.ID_SOFTWARE,
         id_institution_in => l_rec.ID_INSTITUTION,
         flg_type_in => l_rec.FLG_TYPE,
         dt_begin_tstz_in => l_rec.DT_BEGIN_TSTZ,
         dt_end_tstz_in => l_rec.DT_END_TSTZ,
         id_department_in => l_rec.ID_DEPARTMENT,
         notes_in => l_rec.NOTES,
         create_time_in => l_rec.CREATE_TIME,
         create_user_in => l_rec.CREATE_USER,
         num_members_in => l_rec.NUM_MEMBERS,
         create_institution_in => l_rec.CREATE_INSTITUTION,
         update_user_in => l_rec.UPDATE_USER,
         update_time_in => l_rec.UPDATE_TIME,
         update_institution_in => l_rec.UPDATE_INSTITUTION,
         id_prof_register_in => l_rec.ID_PROF_REGISTER,
         dt_register_in => l_rec.DT_REGISTER,
         id_team_type_in => l_rec.ID_TEAM_TYPE
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
   END ins;

   PROCEDURE ins (
      rec_in IN PROF_TEAM_EA%ROWTYPE
     ,sequence_in IN VARCHAR2 := NULL
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
      rows_out TABLE_VARCHAR;
   BEGIN

  ins (
      rec_in => rec_in
     ,sequence_in => sequence_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
   );

   END ins;


    PROCEDURE ins (
      rows_in IN PROF_TEAM_EA_tc
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
   BEGIN
      IF rows_in.COUNT = 0
      THEN
         NULL;
      ELSE
         FORALL indx IN rows_in.FIRST .. rows_in.LAST
            SAVE EXCEPTIONS
            INSERT INTO PROF_TEAM_EA VALUES rows_in (indx) RETURNING ROWID BULK COLLECT INTO rows_out;
      END IF;
   EXCEPTION
     WHEN e_forall_error
     THEN
        -- In Oracle9i and above, SAVE EXCEPTIONS will direct control
        -- here if any error occurs. We can then save all the error
        -- information out to the error instance.
       IF NOT handle_error_in THEN RAISE;
       ELSE
          <<bulk_handler>>
          DECLARE
             l_err_instance_id NUMBER;
          BEGIN
             -- For each error, write to the log.
             FOR indx IN 1 .. SQL%BULK_EXCEPTIONS.COUNT
             LOOP
                pk_alert_exceptions.register_error (
                    error_name_in => 'FORALL-INSERT-FAILURE'
                   ,err_instance_id_out => l_err_instance_id
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'BINDING_ROW_' || indx
                  ,value_in => SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX
                  ,validate_in => FALSE
                );
                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ERROR_AT_ROW_' || indx
                  ,value_in => SQL%BULK_EXCEPTIONS (indx).ERROR_CODE
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_PROF_TEAM _' || indx
                  ,value_in => rows_in(indx).ID_PROF_TEAM
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_PROF_TEAM_LEADER _' || indx
                  ,value_in => rows_in(indx).ID_PROF_TEAM_LEADER
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'PROF_TEAM_NAME _' || indx
                  ,value_in => rows_in(indx).PROF_TEAM_NAME
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'PROF_TEAM_DESC _' || indx
                  ,value_in => rows_in(indx).PROF_TEAM_DESC
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'FLG_AVAILABLE _' || indx
                  ,value_in => rows_in(indx).FLG_AVAILABLE
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'FLG_STATUS _' || indx
                  ,value_in => rows_in(indx).FLG_STATUS
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_SOFTWARE _' || indx
                  ,value_in => rows_in(indx).ID_SOFTWARE
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_INSTITUTION _' || indx
                  ,value_in => rows_in(indx).ID_INSTITUTION
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'FLG_TYPE _' || indx
                  ,value_in => rows_in(indx).FLG_TYPE
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'DT_BEGIN_TSTZ _' || indx
                  ,value_in => rows_in(indx).DT_BEGIN_TSTZ
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'DT_END_TSTZ _' || indx
                  ,value_in => rows_in(indx).DT_END_TSTZ
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_DEPARTMENT _' || indx
                  ,value_in => rows_in(indx).ID_DEPARTMENT
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'NOTES _' || indx
                  ,value_in => rows_in(indx).NOTES
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'CREATE_TIME _' || indx
                  ,value_in => rows_in(indx).CREATE_TIME
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'CREATE_USER _' || indx
                  ,value_in => rows_in(indx).CREATE_USER
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'NUM_MEMBERS _' || indx
                  ,value_in => rows_in(indx).NUM_MEMBERS
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'CREATE_INSTITUTION _' || indx
                  ,value_in => rows_in(indx).CREATE_INSTITUTION
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'UPDATE_USER _' || indx
                  ,value_in => rows_in(indx).UPDATE_USER
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'UPDATE_TIME _' || indx
                  ,value_in => rows_in(indx).UPDATE_TIME
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'UPDATE_INSTITUTION _' || indx
                  ,value_in => rows_in(indx).UPDATE_INSTITUTION
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_PROF_REGISTER _' || indx
                  ,value_in => rows_in(indx).ID_PROF_REGISTER
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'DT_REGISTER _' || indx
                  ,value_in => rows_in(indx).DT_REGISTER
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_TEAM_TYPE _' || indx
                  ,value_in => rows_in(indx).ID_TEAM_TYPE
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.raise_error_instance( err_instance_id_in => l_err_instance_id );
             END LOOP;
          END bulk_handler;
        END IF;
     WHEN OTHERS
     THEN
       IF NOT handle_error_in THEN RAISE;
       ELSE
       pk_alert_exceptions.raise_error(
          error_name_in => 'FORALL-INSERT-FAILURE'
          ,name1_in => 'TABLE_NAME'
          ,value1_in => 'PROF_TEAM_EA'
          ,name2_in => 'ROW_COUNT'
          ,value2_in => rows_in.COUNT
           );
       END IF;
   END ins;

    PROCEDURE ins (
      rows_in IN PROF_TEAM_EA_tc
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
      rows_out TABLE_VARCHAR;
   BEGIN
      ins (
      rows_in => rows_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
   );
   END ins;


PROCEDURE upd (
      id_prof_team_in IN PROF_TEAM_EA.ID_PROF_TEAM%TYPE,
      id_prof_team_leader_in IN PROF_TEAM_EA.ID_PROF_TEAM_LEADER%TYPE DEFAULT NULL,
      ID_PROF_TEAM_LEADER_nin IN BOOLEAN := TRUE,
      prof_team_name_in IN PROF_TEAM_EA.PROF_TEAM_NAME%TYPE DEFAULT NULL,
      PROF_TEAM_NAME_nin IN BOOLEAN := TRUE,
      prof_team_desc_in IN PROF_TEAM_EA.PROF_TEAM_DESC%TYPE DEFAULT NULL,
      PROF_TEAM_DESC_nin IN BOOLEAN := TRUE,
      flg_available_in IN PROF_TEAM_EA.FLG_AVAILABLE%TYPE DEFAULT NULL,
      FLG_AVAILABLE_nin IN BOOLEAN := TRUE,
      flg_status_in IN PROF_TEAM_EA.FLG_STATUS%TYPE DEFAULT NULL,
      FLG_STATUS_nin IN BOOLEAN := TRUE,
      id_software_in IN PROF_TEAM_EA.ID_SOFTWARE%TYPE DEFAULT NULL,
      ID_SOFTWARE_nin IN BOOLEAN := TRUE,
      id_institution_in IN PROF_TEAM_EA.ID_INSTITUTION%TYPE DEFAULT NULL,
      ID_INSTITUTION_nin IN BOOLEAN := TRUE,
      flg_type_in IN PROF_TEAM_EA.FLG_TYPE%TYPE DEFAULT NULL,
      FLG_TYPE_nin IN BOOLEAN := TRUE,
      dt_begin_tstz_in IN PROF_TEAM_EA.DT_BEGIN_TSTZ%TYPE DEFAULT NULL,
      DT_BEGIN_TSTZ_nin IN BOOLEAN := TRUE,
      dt_end_tstz_in IN PROF_TEAM_EA.DT_END_TSTZ%TYPE DEFAULT NULL,
      DT_END_TSTZ_nin IN BOOLEAN := TRUE,
      id_department_in IN PROF_TEAM_EA.ID_DEPARTMENT%TYPE DEFAULT NULL,
      ID_DEPARTMENT_nin IN BOOLEAN := TRUE,
      notes_in IN PROF_TEAM_EA.NOTES%TYPE DEFAULT NULL,
      NOTES_nin IN BOOLEAN := TRUE,
      create_time_in IN PROF_TEAM_EA.CREATE_TIME%TYPE DEFAULT NULL,
      CREATE_TIME_nin IN BOOLEAN := TRUE,
      create_user_in IN PROF_TEAM_EA.CREATE_USER%TYPE DEFAULT NULL,
      CREATE_USER_nin IN BOOLEAN := TRUE,
      num_members_in IN PROF_TEAM_EA.NUM_MEMBERS%TYPE DEFAULT NULL,
      NUM_MEMBERS_nin IN BOOLEAN := TRUE,
      create_institution_in IN PROF_TEAM_EA.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      CREATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      update_user_in IN PROF_TEAM_EA.UPDATE_USER%TYPE DEFAULT NULL,
      UPDATE_USER_nin IN BOOLEAN := TRUE,
      update_time_in IN PROF_TEAM_EA.UPDATE_TIME%TYPE DEFAULT NULL,
      UPDATE_TIME_nin IN BOOLEAN := TRUE,
      update_institution_in IN PROF_TEAM_EA.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      UPDATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      id_prof_register_in IN PROF_TEAM_EA.ID_PROF_REGISTER%TYPE DEFAULT NULL,
      ID_PROF_REGISTER_nin IN BOOLEAN := TRUE,
      dt_register_in IN PROF_TEAM_EA.DT_REGISTER%TYPE DEFAULT NULL,
      DT_REGISTER_nin IN BOOLEAN := TRUE,
      id_team_type_in IN PROF_TEAM_EA.ID_TEAM_TYPE%TYPE DEFAULT NULL,
      ID_TEAM_TYPE_nin IN BOOLEAN := TRUE,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      )
   IS
   l_rows_out TABLE_VARCHAR;
   l_ID_PROF_TEAM_LEADER_n NUMBER(1);
   l_PROF_TEAM_NAME_n NUMBER(1);
   l_PROF_TEAM_DESC_n NUMBER(1);
   l_FLG_AVAILABLE_n NUMBER(1);
   l_FLG_STATUS_n NUMBER(1);
   l_ID_SOFTWARE_n NUMBER(1);
   l_ID_INSTITUTION_n NUMBER(1);
   l_FLG_TYPE_n NUMBER(1);
   l_DT_BEGIN_TSTZ_n NUMBER(1);
   l_DT_END_TSTZ_n NUMBER(1);
   l_ID_DEPARTMENT_n NUMBER(1);
   l_NOTES_n NUMBER(1);
   l_CREATE_TIME_n NUMBER(1);
   l_CREATE_USER_n NUMBER(1);
   l_NUM_MEMBERS_n NUMBER(1);
   l_CREATE_INSTITUTION_n NUMBER(1);
   l_UPDATE_USER_n NUMBER(1);
   l_UPDATE_TIME_n NUMBER(1);
   l_UPDATE_INSTITUTION_n NUMBER(1);
   l_ID_PROF_REGISTER_n NUMBER(1);
   l_DT_REGISTER_n NUMBER(1);
   l_ID_TEAM_TYPE_n NUMBER(1);
   BEGIN

   l_ID_PROF_TEAM_LEADER_n := sys.diutil.bool_to_int(ID_PROF_TEAM_LEADER_nin);
   l_PROF_TEAM_NAME_n := sys.diutil.bool_to_int(PROF_TEAM_NAME_nin);
   l_PROF_TEAM_DESC_n := sys.diutil.bool_to_int(PROF_TEAM_DESC_nin);
   l_FLG_AVAILABLE_n := sys.diutil.bool_to_int(FLG_AVAILABLE_nin);
   l_FLG_STATUS_n := sys.diutil.bool_to_int(FLG_STATUS_nin);
   l_ID_SOFTWARE_n := sys.diutil.bool_to_int(ID_SOFTWARE_nin);
   l_ID_INSTITUTION_n := sys.diutil.bool_to_int(ID_INSTITUTION_nin);
   l_FLG_TYPE_n := sys.diutil.bool_to_int(FLG_TYPE_nin);
   l_DT_BEGIN_TSTZ_n := sys.diutil.bool_to_int(DT_BEGIN_TSTZ_nin);
   l_DT_END_TSTZ_n := sys.diutil.bool_to_int(DT_END_TSTZ_nin);
   l_ID_DEPARTMENT_n := sys.diutil.bool_to_int(ID_DEPARTMENT_nin);
   l_NOTES_n := sys.diutil.bool_to_int(NOTES_nin);
   l_CREATE_TIME_n := sys.diutil.bool_to_int(CREATE_TIME_nin);
   l_CREATE_USER_n := sys.diutil.bool_to_int(CREATE_USER_nin);
   l_NUM_MEMBERS_n := sys.diutil.bool_to_int(NUM_MEMBERS_nin);
   l_CREATE_INSTITUTION_n := sys.diutil.bool_to_int(CREATE_INSTITUTION_nin);
   l_UPDATE_USER_n := sys.diutil.bool_to_int(UPDATE_USER_nin);
   l_UPDATE_TIME_n := sys.diutil.bool_to_int(UPDATE_TIME_nin);
   l_UPDATE_INSTITUTION_n := sys.diutil.bool_to_int(UPDATE_INSTITUTION_nin);
   l_ID_PROF_REGISTER_n := sys.diutil.bool_to_int(ID_PROF_REGISTER_nin);
   l_DT_REGISTER_n := sys.diutil.bool_to_int(DT_REGISTER_nin);
   l_ID_TEAM_TYPE_n := sys.diutil.bool_to_int(ID_TEAM_TYPE_nin);


         UPDATE PROF_TEAM_EA SET
     ID_PROF_TEAM_LEADER = decode (l_ID_PROF_TEAM_LEADER_n,0,id_prof_team_leader_in, NVL (id_prof_team_leader_in, ID_PROF_TEAM_LEADER)),
     PROF_TEAM_NAME = decode (l_PROF_TEAM_NAME_n,0,prof_team_name_in, NVL (prof_team_name_in, PROF_TEAM_NAME)),
     PROF_TEAM_DESC = decode (l_PROF_TEAM_DESC_n,0,prof_team_desc_in, NVL (prof_team_desc_in, PROF_TEAM_DESC)),
     FLG_AVAILABLE = decode (l_FLG_AVAILABLE_n,0,flg_available_in, NVL (flg_available_in, FLG_AVAILABLE)),
     FLG_STATUS = decode (l_FLG_STATUS_n,0,flg_status_in, NVL (flg_status_in, FLG_STATUS)),
     ID_SOFTWARE = decode (l_ID_SOFTWARE_n,0,id_software_in, NVL (id_software_in, ID_SOFTWARE)),
     ID_INSTITUTION = decode (l_ID_INSTITUTION_n,0,id_institution_in, NVL (id_institution_in, ID_INSTITUTION)),
     FLG_TYPE = decode (l_FLG_TYPE_n,0,flg_type_in, NVL (flg_type_in, FLG_TYPE)),
     DT_BEGIN_TSTZ = decode (l_DT_BEGIN_TSTZ_n,0,dt_begin_tstz_in, NVL (dt_begin_tstz_in, DT_BEGIN_TSTZ)),
     DT_END_TSTZ = decode (l_DT_END_TSTZ_n,0,dt_end_tstz_in, NVL (dt_end_tstz_in, DT_END_TSTZ)),
     ID_DEPARTMENT = decode (l_ID_DEPARTMENT_n,0,id_department_in, NVL (id_department_in, ID_DEPARTMENT)),
     NOTES = decode (l_NOTES_n,0,notes_in, NVL (notes_in, NOTES)),
     CREATE_TIME = decode (l_CREATE_TIME_n,0,create_time_in, NVL (create_time_in, CREATE_TIME)),
     CREATE_USER = decode (l_CREATE_USER_n,0,create_user_in, NVL (create_user_in, CREATE_USER)),
     NUM_MEMBERS = decode (l_NUM_MEMBERS_n,0,num_members_in, NVL (num_members_in, NUM_MEMBERS)),
     CREATE_INSTITUTION = decode (l_CREATE_INSTITUTION_n,0,create_institution_in, NVL (create_institution_in, CREATE_INSTITUTION)),
     UPDATE_USER = decode (l_UPDATE_USER_n,0,update_user_in, NVL (update_user_in, UPDATE_USER)),
     UPDATE_TIME = decode (l_UPDATE_TIME_n,0,update_time_in, NVL (update_time_in, UPDATE_TIME)),
     UPDATE_INSTITUTION = decode (l_UPDATE_INSTITUTION_n,0,update_institution_in, NVL (update_institution_in, UPDATE_INSTITUTION)),
     ID_PROF_REGISTER = decode (l_ID_PROF_REGISTER_n,0,id_prof_register_in, NVL (id_prof_register_in, ID_PROF_REGISTER)),
     DT_REGISTER = decode (l_DT_REGISTER_n,0,dt_register_in, NVL (dt_register_in, DT_REGISTER)),
     ID_TEAM_TYPE = decode (l_ID_TEAM_TYPE_n,0,id_team_type_in, NVL (id_team_type_in, ID_TEAM_TYPE))
          WHERE
             ID_PROF_TEAM = id_prof_team_in
         RETURNING ROWID BULK COLLECT INTO l_rows_out;


if(rows_out is null)
then
rows_out := table_varchar();
end if;

rows_out :=  rows_out MULTISET UNION DISTINCT l_rows_out;

   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF FALSE THEN NULL; -- Placeholder in case no unique indexes
           ELSE
              pk_alert_exceptions.raise_error (
                    error_name_in => 'DUPLICATE-VALUE'
                    ,name1_in => 'OWNER'
                    ,value1_in => l_owner
                    ,name2_in => 'CONSTRAINT_NAME'
                    ,value2_in => l_name
                    ,name3_in => 'TABLE_NAME'
                    ,value3_in => 'PROF_TEAM_EA');
           END IF;
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PROF_TEAM_EA');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PROF_TEAM_EA');
           IF l_name = 'PROF_TEAMEA_DEP_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_DEPARTMENT'
               , value_in => id_department_in);
           END IF;
           IF l_name = 'PROF_TEAMEA_INST_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_INSTITUTION'
               , value_in => id_institution_in);
           END IF;
           IF l_name = 'PROF_TEAMEA_PROFRG_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_PROF_REGISTER'
               , value_in => id_prof_register_in);
           END IF;
           IF l_name = 'PROF_TEAMEA_PROFR_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'CREATE_USER'
               , value_in => create_user_in);
           END IF;
           IF l_name = 'PROF_TEAMEA_PROF_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_PROF_TEAM_LEADER'
               , value_in => id_prof_team_leader_in);
           END IF;
           IF l_name = 'PROF_TEAMEA_SOFT_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_SOFTWARE'
               , value_in => id_software_in);
           END IF;
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END upd;


   PROCEDURE upd (
      id_prof_team_in IN PROF_TEAM_EA.ID_PROF_TEAM%TYPE,
      id_prof_team_leader_in IN PROF_TEAM_EA.ID_PROF_TEAM_LEADER%TYPE DEFAULT NULL,
      ID_PROF_TEAM_LEADER_nin IN BOOLEAN := TRUE,
      prof_team_name_in IN PROF_TEAM_EA.PROF_TEAM_NAME%TYPE DEFAULT NULL,
      PROF_TEAM_NAME_nin IN BOOLEAN := TRUE,
      prof_team_desc_in IN PROF_TEAM_EA.PROF_TEAM_DESC%TYPE DEFAULT NULL,
      PROF_TEAM_DESC_nin IN BOOLEAN := TRUE,
      flg_available_in IN PROF_TEAM_EA.FLG_AVAILABLE%TYPE DEFAULT NULL,
      FLG_AVAILABLE_nin IN BOOLEAN := TRUE,
      flg_status_in IN PROF_TEAM_EA.FLG_STATUS%TYPE DEFAULT NULL,
      FLG_STATUS_nin IN BOOLEAN := TRUE,
      id_software_in IN PROF_TEAM_EA.ID_SOFTWARE%TYPE DEFAULT NULL,
      ID_SOFTWARE_nin IN BOOLEAN := TRUE,
      id_institution_in IN PROF_TEAM_EA.ID_INSTITUTION%TYPE DEFAULT NULL,
      ID_INSTITUTION_nin IN BOOLEAN := TRUE,
      flg_type_in IN PROF_TEAM_EA.FLG_TYPE%TYPE DEFAULT NULL,
      FLG_TYPE_nin IN BOOLEAN := TRUE,
      dt_begin_tstz_in IN PROF_TEAM_EA.DT_BEGIN_TSTZ%TYPE DEFAULT NULL,
      DT_BEGIN_TSTZ_nin IN BOOLEAN := TRUE,
      dt_end_tstz_in IN PROF_TEAM_EA.DT_END_TSTZ%TYPE DEFAULT NULL,
      DT_END_TSTZ_nin IN BOOLEAN := TRUE,
      id_department_in IN PROF_TEAM_EA.ID_DEPARTMENT%TYPE DEFAULT NULL,
      ID_DEPARTMENT_nin IN BOOLEAN := TRUE,
      notes_in IN PROF_TEAM_EA.NOTES%TYPE DEFAULT NULL,
      NOTES_nin IN BOOLEAN := TRUE,
      create_time_in IN PROF_TEAM_EA.CREATE_TIME%TYPE DEFAULT NULL,
      CREATE_TIME_nin IN BOOLEAN := TRUE,
      create_user_in IN PROF_TEAM_EA.CREATE_USER%TYPE DEFAULT NULL,
      CREATE_USER_nin IN BOOLEAN := TRUE,
      num_members_in IN PROF_TEAM_EA.NUM_MEMBERS%TYPE DEFAULT NULL,
      NUM_MEMBERS_nin IN BOOLEAN := TRUE,
      create_institution_in IN PROF_TEAM_EA.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      CREATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      update_user_in IN PROF_TEAM_EA.UPDATE_USER%TYPE DEFAULT NULL,
      UPDATE_USER_nin IN BOOLEAN := TRUE,
      update_time_in IN PROF_TEAM_EA.UPDATE_TIME%TYPE DEFAULT NULL,
      UPDATE_TIME_nin IN BOOLEAN := TRUE,
      update_institution_in IN PROF_TEAM_EA.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      UPDATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      id_prof_register_in IN PROF_TEAM_EA.ID_PROF_REGISTER%TYPE DEFAULT NULL,
      ID_PROF_REGISTER_nin IN BOOLEAN := TRUE,
      dt_register_in IN PROF_TEAM_EA.DT_REGISTER%TYPE DEFAULT NULL,
      DT_REGISTER_nin IN BOOLEAN := TRUE,
      id_team_type_in IN PROF_TEAM_EA.ID_TEAM_TYPE%TYPE DEFAULT NULL,
      ID_TEAM_TYPE_nin IN BOOLEAN := TRUE,
     handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
     upd (
      id_prof_team_in => id_prof_team_in,
      id_prof_team_leader_in => id_prof_team_leader_in,
      ID_PROF_TEAM_LEADER_nin => ID_PROF_TEAM_LEADER_nin,
      prof_team_name_in => prof_team_name_in,
      PROF_TEAM_NAME_nin => PROF_TEAM_NAME_nin,
      prof_team_desc_in => prof_team_desc_in,
      PROF_TEAM_DESC_nin => PROF_TEAM_DESC_nin,
      flg_available_in => flg_available_in,
      FLG_AVAILABLE_nin => FLG_AVAILABLE_nin,
      flg_status_in => flg_status_in,
      FLG_STATUS_nin => FLG_STATUS_nin,
      id_software_in => id_software_in,
      ID_SOFTWARE_nin => ID_SOFTWARE_nin,
      id_institution_in => id_institution_in,
      ID_INSTITUTION_nin => ID_INSTITUTION_nin,
      flg_type_in => flg_type_in,
      FLG_TYPE_nin => FLG_TYPE_nin,
      dt_begin_tstz_in => dt_begin_tstz_in,
      DT_BEGIN_TSTZ_nin => DT_BEGIN_TSTZ_nin,
      dt_end_tstz_in => dt_end_tstz_in,
      DT_END_TSTZ_nin => DT_END_TSTZ_nin,
      id_department_in => id_department_in,
      ID_DEPARTMENT_nin => ID_DEPARTMENT_nin,
      notes_in => notes_in,
      NOTES_nin => NOTES_nin,
      create_time_in => create_time_in,
      CREATE_TIME_nin => CREATE_TIME_nin,
      create_user_in => create_user_in,
      CREATE_USER_nin => CREATE_USER_nin,
      num_members_in => num_members_in,
      NUM_MEMBERS_nin => NUM_MEMBERS_nin,
      create_institution_in => create_institution_in,
      CREATE_INSTITUTION_nin => CREATE_INSTITUTION_nin,
      update_user_in => update_user_in,
      UPDATE_USER_nin => UPDATE_USER_nin,
      update_time_in => update_time_in,
      UPDATE_TIME_nin => UPDATE_TIME_nin,
      update_institution_in => update_institution_in,
      UPDATE_INSTITUTION_nin => UPDATE_INSTITUTION_nin,
      id_prof_register_in => id_prof_register_in,
      ID_PROF_REGISTER_nin => ID_PROF_REGISTER_nin,
      dt_register_in => dt_register_in,
      DT_REGISTER_nin => DT_REGISTER_nin,
      id_team_type_in => id_team_type_in,
      ID_TEAM_TYPE_nin => ID_TEAM_TYPE_nin,
     handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END upd;

PROCEDURE upd (
      id_prof_team_leader_in IN PROF_TEAM_EA.ID_PROF_TEAM_LEADER%TYPE DEFAULT NULL,
      ID_PROF_TEAM_LEADER_nin IN BOOLEAN := TRUE,
      prof_team_name_in IN PROF_TEAM_EA.PROF_TEAM_NAME%TYPE DEFAULT NULL,
      PROF_TEAM_NAME_nin IN BOOLEAN := TRUE,
      prof_team_desc_in IN PROF_TEAM_EA.PROF_TEAM_DESC%TYPE DEFAULT NULL,
      PROF_TEAM_DESC_nin IN BOOLEAN := TRUE,
      flg_available_in IN PROF_TEAM_EA.FLG_AVAILABLE%TYPE DEFAULT NULL,
      FLG_AVAILABLE_nin IN BOOLEAN := TRUE,
      flg_status_in IN PROF_TEAM_EA.FLG_STATUS%TYPE DEFAULT NULL,
      FLG_STATUS_nin IN BOOLEAN := TRUE,
      id_software_in IN PROF_TEAM_EA.ID_SOFTWARE%TYPE DEFAULT NULL,
      ID_SOFTWARE_nin IN BOOLEAN := TRUE,
      id_institution_in IN PROF_TEAM_EA.ID_INSTITUTION%TYPE DEFAULT NULL,
      ID_INSTITUTION_nin IN BOOLEAN := TRUE,
      flg_type_in IN PROF_TEAM_EA.FLG_TYPE%TYPE DEFAULT NULL,
      FLG_TYPE_nin IN BOOLEAN := TRUE,
      dt_begin_tstz_in IN PROF_TEAM_EA.DT_BEGIN_TSTZ%TYPE DEFAULT NULL,
      DT_BEGIN_TSTZ_nin IN BOOLEAN := TRUE,
      dt_end_tstz_in IN PROF_TEAM_EA.DT_END_TSTZ%TYPE DEFAULT NULL,
      DT_END_TSTZ_nin IN BOOLEAN := TRUE,
      id_department_in IN PROF_TEAM_EA.ID_DEPARTMENT%TYPE DEFAULT NULL,
      ID_DEPARTMENT_nin IN BOOLEAN := TRUE,
      notes_in IN PROF_TEAM_EA.NOTES%TYPE DEFAULT NULL,
      NOTES_nin IN BOOLEAN := TRUE,
      create_time_in IN PROF_TEAM_EA.CREATE_TIME%TYPE DEFAULT NULL,
      CREATE_TIME_nin IN BOOLEAN := TRUE,
      create_user_in IN PROF_TEAM_EA.CREATE_USER%TYPE DEFAULT NULL,
      CREATE_USER_nin IN BOOLEAN := TRUE,
      num_members_in IN PROF_TEAM_EA.NUM_MEMBERS%TYPE DEFAULT NULL,
      NUM_MEMBERS_nin IN BOOLEAN := TRUE,
      create_institution_in IN PROF_TEAM_EA.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      CREATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      update_user_in IN PROF_TEAM_EA.UPDATE_USER%TYPE DEFAULT NULL,
      UPDATE_USER_nin IN BOOLEAN := TRUE,
      update_time_in IN PROF_TEAM_EA.UPDATE_TIME%TYPE DEFAULT NULL,
      UPDATE_TIME_nin IN BOOLEAN := TRUE,
      update_institution_in IN PROF_TEAM_EA.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      UPDATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      id_prof_register_in IN PROF_TEAM_EA.ID_PROF_REGISTER%TYPE DEFAULT NULL,
      ID_PROF_REGISTER_nin IN BOOLEAN := TRUE,
      dt_register_in IN PROF_TEAM_EA.DT_REGISTER%TYPE DEFAULT NULL,
      DT_REGISTER_nin IN BOOLEAN := TRUE,
      id_team_type_in IN PROF_TEAM_EA.ID_TEAM_TYPE%TYPE DEFAULT NULL,
      ID_TEAM_TYPE_nin IN BOOLEAN := TRUE,
    where_in varchar2 DEFAULT NULL,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      )
   IS
   l_sql VARCHAR2(32767);
   l_rows_out TABLE_VARCHAR;
   l_ID_PROF_TEAM_LEADER_n NUMBER(1);
   l_PROF_TEAM_NAME_n NUMBER(1);
   l_PROF_TEAM_DESC_n NUMBER(1);
   l_FLG_AVAILABLE_n NUMBER(1);
   l_FLG_STATUS_n NUMBER(1);
   l_ID_SOFTWARE_n NUMBER(1);
   l_ID_INSTITUTION_n NUMBER(1);
   l_FLG_TYPE_n NUMBER(1);
   l_DT_BEGIN_TSTZ_n NUMBER(1);
   l_DT_END_TSTZ_n NUMBER(1);
   l_ID_DEPARTMENT_n NUMBER(1);
   l_NOTES_n NUMBER(1);
   l_CREATE_TIME_n NUMBER(1);
   l_CREATE_USER_n NUMBER(1);
   l_NUM_MEMBERS_n NUMBER(1);
   l_CREATE_INSTITUTION_n NUMBER(1);
   l_UPDATE_USER_n NUMBER(1);
   l_UPDATE_TIME_n NUMBER(1);
   l_UPDATE_INSTITUTION_n NUMBER(1);
   l_ID_PROF_REGISTER_n NUMBER(1);
   l_DT_REGISTER_n NUMBER(1);
   l_ID_TEAM_TYPE_n NUMBER(1);
      id_prof_team_in PROF_TEAM_EA.ID_PROF_TEAM%TYPE;
   BEGIN



      l_ID_PROF_TEAM_LEADER_n := sys.diutil.bool_to_int(ID_PROF_TEAM_LEADER_nin);
      l_PROF_TEAM_NAME_n := sys.diutil.bool_to_int(PROF_TEAM_NAME_nin);
      l_PROF_TEAM_DESC_n := sys.diutil.bool_to_int(PROF_TEAM_DESC_nin);
      l_FLG_AVAILABLE_n := sys.diutil.bool_to_int(FLG_AVAILABLE_nin);
      l_FLG_STATUS_n := sys.diutil.bool_to_int(FLG_STATUS_nin);
      l_ID_SOFTWARE_n := sys.diutil.bool_to_int(ID_SOFTWARE_nin);
      l_ID_INSTITUTION_n := sys.diutil.bool_to_int(ID_INSTITUTION_nin);
      l_FLG_TYPE_n := sys.diutil.bool_to_int(FLG_TYPE_nin);
      l_DT_BEGIN_TSTZ_n := sys.diutil.bool_to_int(DT_BEGIN_TSTZ_nin);
      l_DT_END_TSTZ_n := sys.diutil.bool_to_int(DT_END_TSTZ_nin);
      l_ID_DEPARTMENT_n := sys.diutil.bool_to_int(ID_DEPARTMENT_nin);
      l_NOTES_n := sys.diutil.bool_to_int(NOTES_nin);
      l_CREATE_TIME_n := sys.diutil.bool_to_int(CREATE_TIME_nin);
      l_CREATE_USER_n := sys.diutil.bool_to_int(CREATE_USER_nin);
      l_NUM_MEMBERS_n := sys.diutil.bool_to_int(NUM_MEMBERS_nin);
      l_CREATE_INSTITUTION_n := sys.diutil.bool_to_int(CREATE_INSTITUTION_nin);
      l_UPDATE_USER_n := sys.diutil.bool_to_int(UPDATE_USER_nin);
      l_UPDATE_TIME_n := sys.diutil.bool_to_int(UPDATE_TIME_nin);
      l_UPDATE_INSTITUTION_n := sys.diutil.bool_to_int(UPDATE_INSTITUTION_nin);
      l_ID_PROF_REGISTER_n := sys.diutil.bool_to_int(ID_PROF_REGISTER_nin);
      l_DT_REGISTER_n := sys.diutil.bool_to_int(DT_REGISTER_nin);
      l_ID_TEAM_TYPE_n := sys.diutil.bool_to_int(ID_TEAM_TYPE_nin);



l_sql := 'UPDATE PROF_TEAM_EA SET '
     || ' ID_PROF_TEAM_LEADER = decode (' || l_ID_PROF_TEAM_LEADER_n || ',0,:id_prof_team_leader_in, NVL (:id_prof_team_leader_in, ID_PROF_TEAM_LEADER)) '|| ','
     || ' PROF_TEAM_NAME = decode (' || l_PROF_TEAM_NAME_n || ',0,:prof_team_name_in, NVL (:prof_team_name_in, PROF_TEAM_NAME)) '|| ','
     || ' PROF_TEAM_DESC = decode (' || l_PROF_TEAM_DESC_n || ',0,:prof_team_desc_in, NVL (:prof_team_desc_in, PROF_TEAM_DESC)) '|| ','
     || ' FLG_AVAILABLE = decode (' || l_FLG_AVAILABLE_n || ',0,:flg_available_in, NVL (:flg_available_in, FLG_AVAILABLE)) '|| ','
     || ' FLG_STATUS = decode (' || l_FLG_STATUS_n || ',0,:flg_status_in, NVL (:flg_status_in, FLG_STATUS)) '|| ','
     || ' ID_SOFTWARE = decode (' || l_ID_SOFTWARE_n || ',0,:id_software_in, NVL (:id_software_in, ID_SOFTWARE)) '|| ','
     || ' ID_INSTITUTION = decode (' || l_ID_INSTITUTION_n || ',0,:id_institution_in, NVL (:id_institution_in, ID_INSTITUTION)) '|| ','
     || ' FLG_TYPE = decode (' || l_FLG_TYPE_n || ',0,:flg_type_in, NVL (:flg_type_in, FLG_TYPE)) '|| ','
     || ' DT_BEGIN_TSTZ = decode (' || l_DT_BEGIN_TSTZ_n || ',0,:dt_begin_tstz_in, NVL (:dt_begin_tstz_in, DT_BEGIN_TSTZ)) '|| ','
     || ' DT_END_TSTZ = decode (' || l_DT_END_TSTZ_n || ',0,:dt_end_tstz_in, NVL (:dt_end_tstz_in, DT_END_TSTZ)) '|| ','
     || ' ID_DEPARTMENT = decode (' || l_ID_DEPARTMENT_n || ',0,:id_department_in, NVL (:id_department_in, ID_DEPARTMENT)) '|| ','
     || ' NOTES = decode (' || l_NOTES_n || ',0,:notes_in, NVL (:notes_in, NOTES)) '|| ','
     || ' CREATE_TIME = decode (' || l_CREATE_TIME_n || ',0,:create_time_in, NVL (:create_time_in, CREATE_TIME)) '|| ','
     || ' CREATE_USER = decode (' || l_CREATE_USER_n || ',0,:create_user_in, NVL (:create_user_in, CREATE_USER)) '|| ','
     || ' NUM_MEMBERS = decode (' || l_NUM_MEMBERS_n || ',0,:num_members_in, NVL (:num_members_in, NUM_MEMBERS)) '|| ','
     || ' CREATE_INSTITUTION = decode (' || l_CREATE_INSTITUTION_n || ',0,:create_institution_in, NVL (:create_institution_in, CREATE_INSTITUTION)) '|| ','
     || ' UPDATE_USER = decode (' || l_UPDATE_USER_n || ',0,:update_user_in, NVL (:update_user_in, UPDATE_USER)) '|| ','
     || ' UPDATE_TIME = decode (' || l_UPDATE_TIME_n || ',0,:update_time_in, NVL (:update_time_in, UPDATE_TIME)) '|| ','
     || ' UPDATE_INSTITUTION = decode (' || l_UPDATE_INSTITUTION_n || ',0,:update_institution_in, NVL (:update_institution_in, UPDATE_INSTITUTION)) '|| ','
     || ' ID_PROF_REGISTER = decode (' || l_ID_PROF_REGISTER_n || ',0,:id_prof_register_in, NVL (:id_prof_register_in, ID_PROF_REGISTER)) '|| ','
     || ' DT_REGISTER = decode (' || l_DT_REGISTER_n || ',0,:dt_register_in, NVL (:dt_register_in, DT_REGISTER)) '|| ','
     || ' ID_TEAM_TYPE = decode (' || l_ID_TEAM_TYPE_n || ',0,:id_team_type_in, NVL (:id_team_type_in, ID_TEAM_TYPE)) '
      || ' where ' || nvl(where_in,'(1=1)')
      || ' RETURNING ROWID BULK COLLECT INTO :l_rows_out';




execute immediate 'BEGIN ' || l_sql || '; END;' using in
     id_prof_team_leader_in,
     prof_team_name_in,
     prof_team_desc_in,
     flg_available_in,
     flg_status_in,
     id_software_in,
     id_institution_in,
     flg_type_in,
     dt_begin_tstz_in,
     dt_end_tstz_in,
     id_department_in,
     notes_in,
     create_time_in,
     create_user_in,
     num_members_in,
     create_institution_in,
     update_user_in,
     update_time_in,
     update_institution_in,
     id_prof_register_in,
     dt_register_in,
     id_team_type_in,
    OUT l_rows_out;

if(rows_out is null)
then
rows_out := table_varchar();
end if;

rows_out :=  rows_out MULTISET UNION DISTINCT l_rows_out;

   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF FALSE THEN NULL; -- Placeholder in case no unique indexes
           ELSE
              pk_alert_exceptions.raise_error (
                    error_name_in => 'DUPLICATE-VALUE'
                    ,name1_in => 'OWNER'
                    ,value1_in => l_owner
                    ,name2_in => 'CONSTRAINT_NAME'
                    ,value2_in => l_name
                    ,name3_in => 'TABLE_NAME'
                    ,value3_in => 'PROF_TEAM_EA');
           END IF;
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PROF_TEAM_EA');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PROF_TEAM_EA');
           IF l_name = 'PROF_TEAMEA_DEP_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_DEPARTMENT'
               , value_in => id_department_in);
           END IF;
           IF l_name = 'PROF_TEAMEA_INST_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_INSTITUTION'
               , value_in => id_institution_in);
           END IF;
           IF l_name = 'PROF_TEAMEA_PROFRG_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_PROF_REGISTER'
               , value_in => id_prof_register_in);
           END IF;
           IF l_name = 'PROF_TEAMEA_PROFR_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'CREATE_USER'
               , value_in => create_user_in);
           END IF;
           IF l_name = 'PROF_TEAMEA_PROF_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_PROF_TEAM_LEADER'
               , value_in => id_prof_team_leader_in);
           END IF;
           IF l_name = 'PROF_TEAMEA_SOFT_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_SOFTWARE'
               , value_in => id_software_in);
           END IF;
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END upd;





PROCEDURE upd (
      id_prof_team_leader_in IN PROF_TEAM_EA.ID_PROF_TEAM_LEADER%TYPE DEFAULT NULL,
      ID_PROF_TEAM_LEADER_nin IN BOOLEAN := TRUE,
      prof_team_name_in IN PROF_TEAM_EA.PROF_TEAM_NAME%TYPE DEFAULT NULL,
      PROF_TEAM_NAME_nin IN BOOLEAN := TRUE,
      prof_team_desc_in IN PROF_TEAM_EA.PROF_TEAM_DESC%TYPE DEFAULT NULL,
      PROF_TEAM_DESC_nin IN BOOLEAN := TRUE,
      flg_available_in IN PROF_TEAM_EA.FLG_AVAILABLE%TYPE DEFAULT NULL,
      FLG_AVAILABLE_nin IN BOOLEAN := TRUE,
      flg_status_in IN PROF_TEAM_EA.FLG_STATUS%TYPE DEFAULT NULL,
      FLG_STATUS_nin IN BOOLEAN := TRUE,
      id_software_in IN PROF_TEAM_EA.ID_SOFTWARE%TYPE DEFAULT NULL,
      ID_SOFTWARE_nin IN BOOLEAN := TRUE,
      id_institution_in IN PROF_TEAM_EA.ID_INSTITUTION%TYPE DEFAULT NULL,
      ID_INSTITUTION_nin IN BOOLEAN := TRUE,
      flg_type_in IN PROF_TEAM_EA.FLG_TYPE%TYPE DEFAULT NULL,
      FLG_TYPE_nin IN BOOLEAN := TRUE,
      dt_begin_tstz_in IN PROF_TEAM_EA.DT_BEGIN_TSTZ%TYPE DEFAULT NULL,
      DT_BEGIN_TSTZ_nin IN BOOLEAN := TRUE,
      dt_end_tstz_in IN PROF_TEAM_EA.DT_END_TSTZ%TYPE DEFAULT NULL,
      DT_END_TSTZ_nin IN BOOLEAN := TRUE,
      id_department_in IN PROF_TEAM_EA.ID_DEPARTMENT%TYPE DEFAULT NULL,
      ID_DEPARTMENT_nin IN BOOLEAN := TRUE,
      notes_in IN PROF_TEAM_EA.NOTES%TYPE DEFAULT NULL,
      NOTES_nin IN BOOLEAN := TRUE,
      create_time_in IN PROF_TEAM_EA.CREATE_TIME%TYPE DEFAULT NULL,
      CREATE_TIME_nin IN BOOLEAN := TRUE,
      create_user_in IN PROF_TEAM_EA.CREATE_USER%TYPE DEFAULT NULL,
      CREATE_USER_nin IN BOOLEAN := TRUE,
      num_members_in IN PROF_TEAM_EA.NUM_MEMBERS%TYPE DEFAULT NULL,
      NUM_MEMBERS_nin IN BOOLEAN := TRUE,
      create_institution_in IN PROF_TEAM_EA.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      CREATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      update_user_in IN PROF_TEAM_EA.UPDATE_USER%TYPE DEFAULT NULL,
      UPDATE_USER_nin IN BOOLEAN := TRUE,
      update_time_in IN PROF_TEAM_EA.UPDATE_TIME%TYPE DEFAULT NULL,
      UPDATE_TIME_nin IN BOOLEAN := TRUE,
      update_institution_in IN PROF_TEAM_EA.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      UPDATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      id_prof_register_in IN PROF_TEAM_EA.ID_PROF_REGISTER%TYPE DEFAULT NULL,
      ID_PROF_REGISTER_nin IN BOOLEAN := TRUE,
      dt_register_in IN PROF_TEAM_EA.DT_REGISTER%TYPE DEFAULT NULL,
      DT_REGISTER_nin IN BOOLEAN := TRUE,
      id_team_type_in IN PROF_TEAM_EA.ID_TEAM_TYPE%TYPE DEFAULT NULL,
      ID_TEAM_TYPE_nin IN BOOLEAN := TRUE,
    where_in varchar2 DEFAULT NULL,
     handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
      upd (
      id_prof_team_leader_in => id_prof_team_leader_in,
      ID_PROF_TEAM_LEADER_nin => ID_PROF_TEAM_LEADER_nin,
      prof_team_name_in => prof_team_name_in,
      PROF_TEAM_NAME_nin => PROF_TEAM_NAME_nin,
      prof_team_desc_in => prof_team_desc_in,
      PROF_TEAM_DESC_nin => PROF_TEAM_DESC_nin,
      flg_available_in => flg_available_in,
      FLG_AVAILABLE_nin => FLG_AVAILABLE_nin,
      flg_status_in => flg_status_in,
      FLG_STATUS_nin => FLG_STATUS_nin,
      id_software_in => id_software_in,
      ID_SOFTWARE_nin => ID_SOFTWARE_nin,
      id_institution_in => id_institution_in,
      ID_INSTITUTION_nin => ID_INSTITUTION_nin,
      flg_type_in => flg_type_in,
      FLG_TYPE_nin => FLG_TYPE_nin,
      dt_begin_tstz_in => dt_begin_tstz_in,
      DT_BEGIN_TSTZ_nin => DT_BEGIN_TSTZ_nin,
      dt_end_tstz_in => dt_end_tstz_in,
      DT_END_TSTZ_nin => DT_END_TSTZ_nin,
      id_department_in => id_department_in,
      ID_DEPARTMENT_nin => ID_DEPARTMENT_nin,
      notes_in => notes_in,
      NOTES_nin => NOTES_nin,
      create_time_in => create_time_in,
      CREATE_TIME_nin => CREATE_TIME_nin,
      create_user_in => create_user_in,
      CREATE_USER_nin => CREATE_USER_nin,
      num_members_in => num_members_in,
      NUM_MEMBERS_nin => NUM_MEMBERS_nin,
      create_institution_in => create_institution_in,
      CREATE_INSTITUTION_nin => CREATE_INSTITUTION_nin,
      update_user_in => update_user_in,
      UPDATE_USER_nin => UPDATE_USER_nin,
      update_time_in => update_time_in,
      UPDATE_TIME_nin => UPDATE_TIME_nin,
      update_institution_in => update_institution_in,
      UPDATE_INSTITUTION_nin => UPDATE_INSTITUTION_nin,
      id_prof_register_in => id_prof_register_in,
      ID_PROF_REGISTER_nin => ID_PROF_REGISTER_nin,
      dt_register_in => dt_register_in,
      DT_REGISTER_nin => DT_REGISTER_nin,
      id_team_type_in => id_team_type_in,
      ID_TEAM_TYPE_nin => ID_TEAM_TYPE_nin,
    where_in => where_in,
     handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END upd;

   PROCEDURE upd (
      rec_in IN PROF_TEAM_EA%ROWTYPE,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      upd (
         id_prof_team_in => rec_in.ID_PROF_TEAM,
         id_prof_team_leader_in => rec_in.ID_PROF_TEAM_LEADER,
         prof_team_name_in => rec_in.PROF_TEAM_NAME,
         prof_team_desc_in => rec_in.PROF_TEAM_DESC,
         flg_available_in => rec_in.FLG_AVAILABLE,
         flg_status_in => rec_in.FLG_STATUS,
         id_software_in => rec_in.ID_SOFTWARE,
         id_institution_in => rec_in.ID_INSTITUTION,
         flg_type_in => rec_in.FLG_TYPE,
         dt_begin_tstz_in => rec_in.DT_BEGIN_TSTZ,
         dt_end_tstz_in => rec_in.DT_END_TSTZ,
         id_department_in => rec_in.ID_DEPARTMENT,
         notes_in => rec_in.NOTES,
         create_time_in => rec_in.CREATE_TIME,
         create_user_in => rec_in.CREATE_USER,
         num_members_in => rec_in.NUM_MEMBERS,
         create_institution_in => rec_in.CREATE_INSTITUTION,
         update_user_in => rec_in.UPDATE_USER,
         update_time_in => rec_in.UPDATE_TIME,
         update_institution_in => rec_in.UPDATE_INSTITUTION,
         id_prof_register_in => rec_in.ID_PROF_REGISTER,
         dt_register_in => rec_in.DT_REGISTER,
         id_team_type_in => rec_in.ID_TEAM_TYPE

        ,handle_error_in => handle_error_in
        , rows_out => rows_out
       );
   END upd;

   PROCEDURE upd (
      rec_in IN PROF_TEAM_EA%ROWTYPE,
     handle_error_in IN BOOLEAN := TRUE
      )
   IS
        rows_out TABLE_VARCHAR;
   BEGIN
      upd (
         id_prof_team_in => rec_in.ID_PROF_TEAM,
         id_prof_team_leader_in => rec_in.ID_PROF_TEAM_LEADER,
         prof_team_name_in => rec_in.PROF_TEAM_NAME,
         prof_team_desc_in => rec_in.PROF_TEAM_DESC,
         flg_available_in => rec_in.FLG_AVAILABLE,
         flg_status_in => rec_in.FLG_STATUS,
         id_software_in => rec_in.ID_SOFTWARE,
         id_institution_in => rec_in.ID_INSTITUTION,
         flg_type_in => rec_in.FLG_TYPE,
         dt_begin_tstz_in => rec_in.DT_BEGIN_TSTZ,
         dt_end_tstz_in => rec_in.DT_END_TSTZ,
         id_department_in => rec_in.ID_DEPARTMENT,
         notes_in => rec_in.NOTES,
         create_time_in => rec_in.CREATE_TIME,
         create_user_in => rec_in.CREATE_USER,
         num_members_in => rec_in.NUM_MEMBERS,
         create_institution_in => rec_in.CREATE_INSTITUTION,
         update_user_in => rec_in.UPDATE_USER,
         update_time_in => rec_in.UPDATE_TIME,
         update_institution_in => rec_in.UPDATE_INSTITUTION,
         id_prof_register_in => rec_in.ID_PROF_REGISTER,
         dt_register_in => rec_in.DT_REGISTER,
         id_team_type_in => rec_in.ID_TEAM_TYPE

        ,handle_error_in => handle_error_in
        , rows_out => rows_out
       );
   END upd;

   PROCEDURE upd_ins (
      id_prof_team_in IN PROF_TEAM_EA.ID_PROF_TEAM%TYPE,
      id_prof_team_leader_in IN PROF_TEAM_EA.ID_PROF_TEAM_LEADER%TYPE DEFAULT NULL,
      prof_team_name_in IN PROF_TEAM_EA.PROF_TEAM_NAME%TYPE DEFAULT NULL,
      prof_team_desc_in IN PROF_TEAM_EA.PROF_TEAM_DESC%TYPE DEFAULT NULL,
      flg_available_in IN PROF_TEAM_EA.FLG_AVAILABLE%TYPE DEFAULT NULL,
      flg_status_in IN PROF_TEAM_EA.FLG_STATUS%TYPE DEFAULT NULL,
      id_software_in IN PROF_TEAM_EA.ID_SOFTWARE%TYPE DEFAULT NULL,
      id_institution_in IN PROF_TEAM_EA.ID_INSTITUTION%TYPE DEFAULT NULL,
      flg_type_in IN PROF_TEAM_EA.FLG_TYPE%TYPE DEFAULT NULL,
      dt_begin_tstz_in IN PROF_TEAM_EA.DT_BEGIN_TSTZ%TYPE DEFAULT NULL,
      dt_end_tstz_in IN PROF_TEAM_EA.DT_END_TSTZ%TYPE DEFAULT NULL,
      id_department_in IN PROF_TEAM_EA.ID_DEPARTMENT%TYPE DEFAULT NULL,
      notes_in IN PROF_TEAM_EA.NOTES%TYPE DEFAULT NULL,
      create_time_in IN PROF_TEAM_EA.CREATE_TIME%TYPE DEFAULT NULL,
      create_user_in IN PROF_TEAM_EA.CREATE_USER%TYPE DEFAULT NULL,
      num_members_in IN PROF_TEAM_EA.NUM_MEMBERS%TYPE DEFAULT NULL,
      create_institution_in IN PROF_TEAM_EA.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN PROF_TEAM_EA.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN PROF_TEAM_EA.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN PROF_TEAM_EA.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      id_prof_register_in IN PROF_TEAM_EA.ID_PROF_REGISTER%TYPE DEFAULT NULL,
      dt_register_in IN PROF_TEAM_EA.DT_REGISTER%TYPE DEFAULT NULL,
      id_team_type_in IN PROF_TEAM_EA.ID_TEAM_TYPE%TYPE DEFAULT NULL,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      upd (
         id_prof_team_in => id_prof_team_in,
         id_prof_team_leader_in => id_prof_team_leader_in,
         prof_team_name_in => prof_team_name_in,
         prof_team_desc_in => prof_team_desc_in,
         flg_available_in => flg_available_in,
         flg_status_in => flg_status_in,
         id_software_in => id_software_in,
         id_institution_in => id_institution_in,
         flg_type_in => flg_type_in,
         dt_begin_tstz_in => dt_begin_tstz_in,
         dt_end_tstz_in => dt_end_tstz_in,
         id_department_in => id_department_in,
         notes_in => notes_in,
         create_time_in => create_time_in,
         create_user_in => create_user_in,
         num_members_in => num_members_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in,
         id_prof_register_in => id_prof_register_in,
         dt_register_in => dt_register_in,
         id_team_type_in => id_team_type_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
       );
      IF SQL%ROWCOUNT = 0
      THEN
         ins (
            id_prof_team_in => id_prof_team_in,
            id_prof_team_leader_in => id_prof_team_leader_in,
            prof_team_name_in => prof_team_name_in,
            prof_team_desc_in => prof_team_desc_in,
            flg_available_in => flg_available_in,
            flg_status_in => flg_status_in,
            id_software_in => id_software_in,
            id_institution_in => id_institution_in,
            flg_type_in => flg_type_in,
            dt_begin_tstz_in => dt_begin_tstz_in,
            dt_end_tstz_in => dt_end_tstz_in,
            id_department_in => id_department_in,
            notes_in => notes_in,
            create_time_in => create_time_in,
            create_user_in => create_user_in,
            num_members_in => num_members_in,
            create_institution_in => create_institution_in,
            update_user_in => update_user_in,
            update_time_in => update_time_in,
            update_institution_in => update_institution_in,
            id_prof_register_in => id_prof_register_in,
            dt_register_in => dt_register_in,
            id_team_type_in => id_team_type_in
            ,handle_error_in => handle_error_in
            , rows_out => rows_out
         );
      END IF;
   END upd_ins;

   PROCEDURE upd_ins (
      id_prof_team_in IN PROF_TEAM_EA.ID_PROF_TEAM%TYPE,
      id_prof_team_leader_in IN PROF_TEAM_EA.ID_PROF_TEAM_LEADER%TYPE DEFAULT NULL,
      prof_team_name_in IN PROF_TEAM_EA.PROF_TEAM_NAME%TYPE DEFAULT NULL,
      prof_team_desc_in IN PROF_TEAM_EA.PROF_TEAM_DESC%TYPE DEFAULT NULL,
      flg_available_in IN PROF_TEAM_EA.FLG_AVAILABLE%TYPE DEFAULT NULL,
      flg_status_in IN PROF_TEAM_EA.FLG_STATUS%TYPE DEFAULT NULL,
      id_software_in IN PROF_TEAM_EA.ID_SOFTWARE%TYPE DEFAULT NULL,
      id_institution_in IN PROF_TEAM_EA.ID_INSTITUTION%TYPE DEFAULT NULL,
      flg_type_in IN PROF_TEAM_EA.FLG_TYPE%TYPE DEFAULT NULL,
      dt_begin_tstz_in IN PROF_TEAM_EA.DT_BEGIN_TSTZ%TYPE DEFAULT NULL,
      dt_end_tstz_in IN PROF_TEAM_EA.DT_END_TSTZ%TYPE DEFAULT NULL,
      id_department_in IN PROF_TEAM_EA.ID_DEPARTMENT%TYPE DEFAULT NULL,
      notes_in IN PROF_TEAM_EA.NOTES%TYPE DEFAULT NULL,
      create_time_in IN PROF_TEAM_EA.CREATE_TIME%TYPE DEFAULT NULL,
      create_user_in IN PROF_TEAM_EA.CREATE_USER%TYPE DEFAULT NULL,
      num_members_in IN PROF_TEAM_EA.NUM_MEMBERS%TYPE DEFAULT NULL,
      create_institution_in IN PROF_TEAM_EA.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN PROF_TEAM_EA.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN PROF_TEAM_EA.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN PROF_TEAM_EA.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      id_prof_register_in IN PROF_TEAM_EA.ID_PROF_REGISTER%TYPE DEFAULT NULL,
      dt_register_in IN PROF_TEAM_EA.DT_REGISTER%TYPE DEFAULT NULL,
      id_team_type_in IN PROF_TEAM_EA.ID_TEAM_TYPE%TYPE DEFAULT NULL,
     handle_error_in IN BOOLEAN := TRUE
      )
   IS
        rows_out TABLE_VARCHAR;
   BEGIN
      upd_ins (
      id_prof_team_in,
      id_prof_team_leader_in,
      prof_team_name_in,
      prof_team_desc_in,
      flg_available_in,
      flg_status_in,
      id_software_in,
      id_institution_in,
      flg_type_in,
      dt_begin_tstz_in,
      dt_end_tstz_in,
      id_department_in,
      notes_in,
      create_time_in,
      create_user_in,
      num_members_in,
      create_institution_in,
      update_user_in,
      update_time_in,
      update_institution_in,
      id_prof_register_in,
      dt_register_in,
      id_team_type_in,
     handle_error_in
     ,rows_out
      );
   END upd_ins;


   PROCEDURE upd (
      col_in IN PROF_TEAM_EA_tc,
      ignore_if_null_in IN BOOLEAN := TRUE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      )
   IS
      l_ID_PROF_TEAM ID_PROF_TEAM_cc;
      l_ID_PROF_TEAM_LEADER ID_PROF_TEAM_LEADER_cc;
      l_PROF_TEAM_NAME PROF_TEAM_NAME_cc;
      l_PROF_TEAM_DESC PROF_TEAM_DESC_cc;
      l_FLG_AVAILABLE FLG_AVAILABLE_cc;
      l_FLG_STATUS FLG_STATUS_cc;
      l_ID_SOFTWARE ID_SOFTWARE_cc;
      l_ID_INSTITUTION ID_INSTITUTION_cc;
      l_FLG_TYPE FLG_TYPE_cc;
      l_DT_BEGIN_TSTZ DT_BEGIN_TSTZ_cc;
      l_DT_END_TSTZ DT_END_TSTZ_cc;
      l_ID_DEPARTMENT ID_DEPARTMENT_cc;
      l_NOTES NOTES_cc;
      l_CREATE_TIME CREATE_TIME_cc;
      l_CREATE_USER CREATE_USER_cc;
      l_NUM_MEMBERS NUM_MEMBERS_cc;
      l_CREATE_INSTITUTION CREATE_INSTITUTION_cc;
      l_UPDATE_USER UPDATE_USER_cc;
      l_UPDATE_TIME UPDATE_TIME_cc;
      l_UPDATE_INSTITUTION UPDATE_INSTITUTION_cc;
      l_ID_PROF_REGISTER ID_PROF_REGISTER_cc;
      l_DT_REGISTER DT_REGISTER_cc;
      l_ID_TEAM_TYPE ID_TEAM_TYPE_cc;
   BEGIN
      FOR i IN col_in.FIRST .. col_in.LAST loop
         l_ID_PROF_TEAM(i) := col_in(i).ID_PROF_TEAM;
         l_ID_PROF_TEAM_LEADER(i) := col_in(i).ID_PROF_TEAM_LEADER;
         l_PROF_TEAM_NAME(i) := col_in(i).PROF_TEAM_NAME;
         l_PROF_TEAM_DESC(i) := col_in(i).PROF_TEAM_DESC;
         l_FLG_AVAILABLE(i) := col_in(i).FLG_AVAILABLE;
         l_FLG_STATUS(i) := col_in(i).FLG_STATUS;
         l_ID_SOFTWARE(i) := col_in(i).ID_SOFTWARE;
         l_ID_INSTITUTION(i) := col_in(i).ID_INSTITUTION;
         l_FLG_TYPE(i) := col_in(i).FLG_TYPE;
         l_DT_BEGIN_TSTZ(i) := col_in(i).DT_BEGIN_TSTZ;
         l_DT_END_TSTZ(i) := col_in(i).DT_END_TSTZ;
         l_ID_DEPARTMENT(i) := col_in(i).ID_DEPARTMENT;
         l_NOTES(i) := col_in(i).NOTES;
         l_CREATE_TIME(i) := col_in(i).CREATE_TIME;
         l_CREATE_USER(i) := col_in(i).CREATE_USER;
         l_NUM_MEMBERS(i) := col_in(i).NUM_MEMBERS;
         l_CREATE_INSTITUTION(i) := col_in(i).CREATE_INSTITUTION;
         l_UPDATE_USER(i) := col_in(i).UPDATE_USER;
         l_UPDATE_TIME(i) := col_in(i).UPDATE_TIME;
         l_UPDATE_INSTITUTION(i) := col_in(i).UPDATE_INSTITUTION;
         l_ID_PROF_REGISTER(i) := col_in(i).ID_PROF_REGISTER;
         l_DT_REGISTER(i) := col_in(i).DT_REGISTER;
         l_ID_TEAM_TYPE(i) := col_in(i).ID_TEAM_TYPE;
      END LOOP;
      IF NVL (ignore_if_null_in, FALSE)
      THEN
         -- Set any columns to their current values
         -- if incoming value is NULL.
         -- Put WHEN clause on column-level triggers!
         FORALL i IN col_in.FIRST .. col_in.LAST
            UPDATE PROF_TEAM_EA SET
               ID_PROF_TEAM_LEADER = NVL (l_ID_PROF_TEAM_LEADER(i), ID_PROF_TEAM_LEADER),
               PROF_TEAM_NAME = NVL (l_PROF_TEAM_NAME(i), PROF_TEAM_NAME),
               PROF_TEAM_DESC = NVL (l_PROF_TEAM_DESC(i), PROF_TEAM_DESC),
               FLG_AVAILABLE = NVL (l_FLG_AVAILABLE(i), FLG_AVAILABLE),
               FLG_STATUS = NVL (l_FLG_STATUS(i), FLG_STATUS),
               ID_SOFTWARE = NVL (l_ID_SOFTWARE(i), ID_SOFTWARE),
               ID_INSTITUTION = NVL (l_ID_INSTITUTION(i), ID_INSTITUTION),
               FLG_TYPE = NVL (l_FLG_TYPE(i), FLG_TYPE),
               DT_BEGIN_TSTZ = NVL (l_DT_BEGIN_TSTZ(i), DT_BEGIN_TSTZ),
               DT_END_TSTZ = NVL (l_DT_END_TSTZ(i), DT_END_TSTZ),
               ID_DEPARTMENT = NVL (l_ID_DEPARTMENT(i), ID_DEPARTMENT),
               NOTES = NVL (l_NOTES(i), NOTES),
               CREATE_TIME = NVL (l_CREATE_TIME(i), CREATE_TIME),
               CREATE_USER = NVL (l_CREATE_USER(i), CREATE_USER),
               NUM_MEMBERS = NVL (l_NUM_MEMBERS(i), NUM_MEMBERS),
               CREATE_INSTITUTION = NVL (l_CREATE_INSTITUTION(i), CREATE_INSTITUTION),
               UPDATE_USER = NVL (l_UPDATE_USER(i), UPDATE_USER),
               UPDATE_TIME = NVL (l_UPDATE_TIME(i), UPDATE_TIME),
               UPDATE_INSTITUTION = NVL (l_UPDATE_INSTITUTION(i), UPDATE_INSTITUTION),
               ID_PROF_REGISTER = NVL (l_ID_PROF_REGISTER(i), ID_PROF_REGISTER),
               DT_REGISTER = NVL (l_DT_REGISTER(i), DT_REGISTER),
               ID_TEAM_TYPE = NVL (l_ID_TEAM_TYPE(i), ID_TEAM_TYPE)
             WHERE
                ID_PROF_TEAM = l_ID_PROF_TEAM(i)
         ;
      ELSE
         FORALL i IN col_in.FIRST .. col_in.LAST
            UPDATE PROF_TEAM_EA SET
               ID_PROF_TEAM_LEADER = l_ID_PROF_TEAM_LEADER(i),
               PROF_TEAM_NAME = l_PROF_TEAM_NAME(i),
               PROF_TEAM_DESC = l_PROF_TEAM_DESC(i),
               FLG_AVAILABLE = l_FLG_AVAILABLE(i),
               FLG_STATUS = l_FLG_STATUS(i),
               ID_SOFTWARE = l_ID_SOFTWARE(i),
               ID_INSTITUTION = l_ID_INSTITUTION(i),
               FLG_TYPE = l_FLG_TYPE(i),
               DT_BEGIN_TSTZ = l_DT_BEGIN_TSTZ(i),
               DT_END_TSTZ = l_DT_END_TSTZ(i),
               ID_DEPARTMENT = l_ID_DEPARTMENT(i),
               NOTES = l_NOTES(i),
               CREATE_TIME = l_CREATE_TIME(i),
               CREATE_USER = l_CREATE_USER(i),
               NUM_MEMBERS = l_NUM_MEMBERS(i),
               CREATE_INSTITUTION = l_CREATE_INSTITUTION(i),
               UPDATE_USER = l_UPDATE_USER(i),
               UPDATE_TIME = l_UPDATE_TIME(i),
               UPDATE_INSTITUTION = l_UPDATE_INSTITUTION(i),
               ID_PROF_REGISTER = l_ID_PROF_REGISTER(i),
               DT_REGISTER = l_DT_REGISTER(i),
               ID_TEAM_TYPE = l_ID_TEAM_TYPE(i)
             WHERE
                ID_PROF_TEAM = l_ID_PROF_TEAM(i)
         ;
      END IF;
   END upd;


   PROCEDURE upd (
      col_in IN PROF_TEAM_EA_tc,
      ignore_if_null_in IN BOOLEAN := TRUE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
     rows_out TABLE_VARCHAR;
   BEGIN
      upd (
      col_in ,
      ignore_if_null_in
     ,handle_error_in
     , rows_out
      );
   END upd;

   FUNCTION dynupdstr (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL)

   RETURN VARCHAR2
   IS
   BEGIN
      RETURN
         'BEGIN UPDATE PROF_TEAM_EA
             SET ' || colname_in || ' = :value
           WHERE ' || NVL (where_in, '1=1') || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;' ;
   END dynupdstr;

   FUNCTION dynupdstr_no_rows_out (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL)

   RETURN VARCHAR2
   IS
   BEGIN
      RETURN
         'UPDATE PROF_TEAM_EA
             SET ' || colname_in || ' = :value
           WHERE ' || NVL (where_in, '1=1');
   END dynupdstr_no_rows_out;























  PROCEDURE increment_onecol (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL
      , increment_value_in IN NUMBER DEFAULT 1
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN UPDATE PROF_TEAM_EA set ' || colname_in || '=' || colname_in || ' + ' || nvl(increment_value_in,1) || ' WHERE ' || NVL (where_in, '1=1') || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
      USING OUT rows_out;
   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'DUPLICATE-VALUE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PROF_TEAM_EA');
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PROF_TEAM_EA');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PROF_TEAM_EA');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END increment_onecol;

   PROCEDURE increment_onecol (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL
     , increment_value_in IN NUMBER DEFAULT 1
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
   rows_out table_varchar;
   BEGIN
      EXECUTE IMMEDIATE 'UPDATE PROF_TEAM_EA set ' || colname_in || '=' || colname_in || ' + ' || nvl(increment_value_in,1) || ' WHERE ' || NVL (where_in, '1=1');
   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'DUPLICATE-VALUE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PROF_TEAM_EA');
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PROF_TEAM_EA');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PROF_TEAM_EA');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END increment_onecol;


   -- Delete functionality


   PROCEDURE del (
      id_prof_team_in IN PROF_TEAM_EA.ID_PROF_TEAM%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM PROF_TEAM_EA
       WHERE
          ID_PROF_TEAM = id_prof_team_in
       RETURNING ROWID BULK COLLECT INTO rows_out
         ;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PROF_TEAM_EA');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PROF_TEAM_EA');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del;




   PROCEDURE del (
      id_prof_team_in IN PROF_TEAM_EA.ID_PROF_TEAM%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
rows_out TABLE_VARCHAR;
   BEGIN

del (
      id_prof_team_in => id_prof_team_in
     ,handle_error_in => handle_error_in
, rows_out => rows_out
      );

   END del;








   -- Delete all rows for primary key column ID_PROF_TEAM
   PROCEDURE del_ID_PROF_TEAM (
      id_prof_team_in IN PROF_TEAM_EA.ID_PROF_TEAM%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
     )
   IS
   BEGIN
      DELETE FROM PROF_TEAM_EA
       WHERE ID_PROF_TEAM = id_prof_team_in
      RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PROF_TEAM_EA');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PROF_TEAM_EA');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_ID_PROF_TEAM;






   -- Delete all rows for primary key column ID_PROF_TEAM
   PROCEDURE del_ID_PROF_TEAM (
      id_prof_team_in IN PROF_TEAM_EA.ID_PROF_TEAM%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_ID_PROF_TEAM (
      id_prof_team_in => id_prof_team_in
     ,handle_error_in => handle_error_in
, rows_out => rows_out
     );
   END del_ID_PROF_TEAM;















   PROCEDURE del_PROF_TEAMEA_DEP_FK (
      id_department_in IN PROF_TEAM_EA.ID_DEPARTMENT%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM PROF_TEAM_EA
       WHERE
          ID_DEPARTMENT = del_PROF_TEAMEA_DEP_FK.id_department_in
       RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PROF_TEAM_EA');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PROF_TEAM_EA');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_PROF_TEAMEA_DEP_FK;



PROCEDURE del_PROF_TEAMEA_DEP_FK (
      id_department_in IN PROF_TEAM_EA.ID_DEPARTMENT%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_PROF_TEAMEA_DEP_FK (
      id_department_in => id_department_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END del_PROF_TEAMEA_DEP_FK;





   PROCEDURE del_PROF_TEAMEA_INST_FK (
      id_institution_in IN PROF_TEAM_EA.ID_INSTITUTION%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM PROF_TEAM_EA
       WHERE
          ID_INSTITUTION = del_PROF_TEAMEA_INST_FK.id_institution_in
       RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PROF_TEAM_EA');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PROF_TEAM_EA');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_PROF_TEAMEA_INST_FK;



PROCEDURE del_PROF_TEAMEA_INST_FK (
      id_institution_in IN PROF_TEAM_EA.ID_INSTITUTION%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_PROF_TEAMEA_INST_FK (
      id_institution_in => id_institution_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END del_PROF_TEAMEA_INST_FK;





   PROCEDURE del_PROF_TEAMEA_PROFRG_FK (
      id_prof_register_in IN PROF_TEAM_EA.ID_PROF_REGISTER%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM PROF_TEAM_EA
       WHERE
          ID_PROF_REGISTER = del_PROF_TEAMEA_PROFRG_FK.id_prof_register_in
       RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PROF_TEAM_EA');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PROF_TEAM_EA');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_PROF_TEAMEA_PROFRG_FK;



PROCEDURE del_PROF_TEAMEA_PROFRG_FK (
      id_prof_register_in IN PROF_TEAM_EA.ID_PROF_REGISTER%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_PROF_TEAMEA_PROFRG_FK (
      id_prof_register_in => id_prof_register_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END del_PROF_TEAMEA_PROFRG_FK;





   PROCEDURE del_PROF_TEAMEA_PROFR_FK (
      create_user_in IN PROF_TEAM_EA.CREATE_USER%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM PROF_TEAM_EA
       WHERE
          CREATE_USER = del_PROF_TEAMEA_PROFR_FK.create_user_in
       RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PROF_TEAM_EA');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PROF_TEAM_EA');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_PROF_TEAMEA_PROFR_FK;



PROCEDURE del_PROF_TEAMEA_PROFR_FK (
      create_user_in IN PROF_TEAM_EA.CREATE_USER%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_PROF_TEAMEA_PROFR_FK (
      create_user_in => create_user_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END del_PROF_TEAMEA_PROFR_FK;





   PROCEDURE del_PROF_TEAMEA_PROF_FK (
      id_prof_team_leader_in IN PROF_TEAM_EA.ID_PROF_TEAM_LEADER%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM PROF_TEAM_EA
       WHERE
          ID_PROF_TEAM_LEADER = del_PROF_TEAMEA_PROF_FK.id_prof_team_leader_in
       RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PROF_TEAM_EA');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PROF_TEAM_EA');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_PROF_TEAMEA_PROF_FK;



PROCEDURE del_PROF_TEAMEA_PROF_FK (
      id_prof_team_leader_in IN PROF_TEAM_EA.ID_PROF_TEAM_LEADER%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_PROF_TEAMEA_PROF_FK (
      id_prof_team_leader_in => id_prof_team_leader_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END del_PROF_TEAMEA_PROF_FK;





   PROCEDURE del_PROF_TEAMEA_SOFT_FK (
      id_software_in IN PROF_TEAM_EA.ID_SOFTWARE%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM PROF_TEAM_EA
       WHERE
          ID_SOFTWARE = del_PROF_TEAMEA_SOFT_FK.id_software_in
       RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PROF_TEAM_EA');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PROF_TEAM_EA');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_PROF_TEAMEA_SOFT_FK;



PROCEDURE del_PROF_TEAMEA_SOFT_FK (
      id_software_in IN PROF_TEAM_EA.ID_SOFTWARE%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_PROF_TEAMEA_SOFT_FK (
      id_software_in => id_software_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END del_PROF_TEAMEA_SOFT_FK;












   -- Deletions using dynamic SQL
   FUNCTION dyndelstr (where_in IN VARCHAR2) RETURN VARCHAR2
   IS
   BEGIN
      IF where_in IS NULL
      THEN
         RETURN 'DELETE FROM PROF_TEAM_EA';
      ELSE
         RETURN
            'DELETE FROM PROF_TEAM_EA WHERE ' || where_in;
      END IF;
   END dyndelstr;

   FUNCTION dyncoldelstr (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE)
      RETURN VARCHAR2
   IS
   BEGIN
      RETURN dyndelstr ( colname_in || ' = :value' );
   END;

   PROCEDURE del_by (
      where_clause_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyndelstr (where_clause_in);
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PROF_TEAM_EA');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PROF_TEAM_EA');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by;





   PROCEDURE del_by (
      where_clause_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN ' || dyndelstr (where_clause_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;' using OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PROF_TEAM_EA');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PROF_TEAM_EA');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by;





   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyncoldelstr (colname_in)
         USING colvalue_in;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PROF_TEAM_EA');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PROF_TEAM_EA');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;






   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr (colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
         USING IN colvalue_in, OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PROF_TEAM_EA');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PROF_TEAM_EA');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN DATE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyncoldelstr (colname_in)
         USING colvalue_in;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PROF_TEAM_EA');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PROF_TEAM_EA');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN DATE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr (colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
         USING IN colvalue_in, OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PROF_TEAM_EA');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PROF_TEAM_EA');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN TIMESTAMP WITH LOCAL TIME ZONE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyncoldelstr (colname_in)
         USING colvalue_in;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PROF_TEAM_EA');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PROF_TEAM_EA');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN TIMESTAMP WITH LOCAL TIME ZONE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr (colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
         USING IN colvalue_in, OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PROF_TEAM_EA');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PROF_TEAM_EA');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN NUMBER
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyncoldelstr (colname_in)
         USING colvalue_in;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PROF_TEAM_EA');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PROF_TEAM_EA');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;






   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN NUMBER
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
   BEGIN
     EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr (colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
         USING IN colvalue_in, OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PROF_TEAM_EA');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PROF_TEAM_EA');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   -- Initialize a record with default values for columns in the table.
   PROCEDURE initrec (
      prof_team_ea_inout IN OUT PROF_TEAM_EA%ROWTYPE
   )
   IS
   BEGIN
      prof_team_ea_inout.ID_PROF_TEAM := NULL;
      prof_team_ea_inout.ID_PROF_TEAM_LEADER := NULL;
      prof_team_ea_inout.PROF_TEAM_NAME := NULL;
      prof_team_ea_inout.PROF_TEAM_DESC := NULL;
      prof_team_ea_inout.FLG_AVAILABLE := NULL;
      prof_team_ea_inout.FLG_STATUS := NULL;
      prof_team_ea_inout.ID_SOFTWARE := NULL;
      prof_team_ea_inout.ID_INSTITUTION := NULL;
      prof_team_ea_inout.FLG_TYPE := NULL;
      prof_team_ea_inout.DT_BEGIN_TSTZ := NULL;
      prof_team_ea_inout.DT_END_TSTZ := NULL;
      prof_team_ea_inout.ID_DEPARTMENT := NULL;
      prof_team_ea_inout.NOTES := NULL;
      prof_team_ea_inout.CREATE_TIME := NULL;
      prof_team_ea_inout.CREATE_USER := NULL;
      prof_team_ea_inout.NUM_MEMBERS := NULL;
      prof_team_ea_inout.CREATE_INSTITUTION := NULL;
      prof_team_ea_inout.UPDATE_USER := NULL;
      prof_team_ea_inout.UPDATE_TIME := NULL;
      prof_team_ea_inout.UPDATE_INSTITUTION := NULL;
      prof_team_ea_inout.ID_PROF_REGISTER := NULL;
      prof_team_ea_inout.DT_REGISTER := NULL;
      prof_team_ea_inout.ID_TEAM_TYPE := NULL;
   END initrec;

   FUNCTION initrec RETURN PROF_TEAM_EA%ROWTYPE
   IS
      l_prof_team_ea PROF_TEAM_EA%ROWTYPE;
   BEGIN
      RETURN l_prof_team_ea;
   END initrec;


   FUNCTION get_data_rowid(
        rows_in IN TABLE_VARCHAR
        ) RETURN PROF_TEAM_EA_tc
   IS
        data PROF_TEAM_EA_tc;
   BEGIN
        select * bulk collect into data from PROF_TEAM_EA where rowid in (select * from table(rows_in));
        return data;
        EXCEPTION
      WHEN OTHERS THEN
        pk_alert_exceptions.raise_error (
           error_name_in => 'get_data_rowid'
           );
   END get_data_rowid;


   FUNCTION get_data_rowid_pat(
        rows_in IN TABLE_VARCHAR
        ) RETURN PROF_TEAM_EA_tc
   is
        PRAGMA AUTONOMOUS_TRANSACTION;
        data PROF_TEAM_EA_tc;
   BEGIN
        data := get_data_rowid(rows_in);
        commit;
        return data;
        EXCEPTION
      WHEN OTHERS THEN
        pk_alert_exceptions.raise_error (
           error_name_in => 'get_data_rowid'
           );
        rollback;
    END get_data_rowid_pat;

BEGIN
   NULL;
END TS_PROF_TEAM_EA;
/
