/*-- Last Change Revision: $Rev: 2028233 $*/
/*-- Last Change by: $Author: mario.fernandes $*/
/*-- Date of last change: $Date: 2022-08-02 18:44:33 +0100 (ter, 02 ago 2022) $*/

CREATE OR REPLACE PACKAGE BODY TS_INTERV_EVALUATION
/*
| Generated by or retrieved from QCGU - DO NOT MODIFY!
| QCGU - "Get it right, do it fast" - www.ToadWorld.com
| QCGU Universal ID: {657BD35E-2E48-4CD4-B14E-41420B8433B3}
| Created On: Abril 1, 2009 16:3:15
| Created By: ALERT
*/
IS

   e_null_column_value EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_null_column_value, -1400);
   --
   e_existing_fky_reference EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_existing_fky_reference, -2266);
   --
   e_check_constraint_failure EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_check_constraint_failure, -2290);
   --
   e_no_parent_key EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_no_parent_key, -2291);
   --
   e_child_record_found EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_child_record_found, -2292);
   --
   e_forall_error EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_forall_error, -24381);
   --
   -- Defined for backward compatibilty.
   e_integ_constraint_failure EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_integ_constraint_failure, -2291);

    -- Private utilities
   PROCEDURE get_constraint_info (
      owner_out OUT ALL_CONSTRAINTS.OWNER%TYPE
     ,name_out OUT ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE)
   IS
      l_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
      dotloc PLS_INTEGER;
      leftloc PLS_INTEGER;
   BEGIN
      dotloc  := INSTR (l_errm,'.');
      leftloc := INSTR (l_errm,'(');
      owner_out := SUBSTR (l_errm, leftloc+1, dotloc-leftloc-1);
      name_out  := SUBSTR (l_errm, dotloc+1, INSTR (l_errm,')')-dotloc-1);
   END get_constraint_info;
   -- Public programs

   PROCEDURE ins (
      id_interv_evaluation_in IN INTERV_EVALUATION.ID_INTERV_EVALUATION%TYPE
      ,
      dt_interv_evaluation_tstz_in IN INTERV_EVALUATION.DT_INTERV_EVALUATION_TSTZ%TYPE DEFAULT NULL,
      id_professional_in IN INTERV_EVALUATION.ID_PROFESSIONAL%TYPE DEFAULT NULL,
      notes_in IN INTERV_EVALUATION.NOTES%TYPE DEFAULT NULL,
      flg_status_in IN INTERV_EVALUATION.FLG_STATUS%TYPE DEFAULT NULL,
      dt_cancel_tstz_in IN INTERV_EVALUATION.DT_CANCEL_TSTZ%TYPE DEFAULT NULL,
      id_prof_cancel_in IN INTERV_EVALUATION.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      id_episode_in IN INTERV_EVALUATION.ID_EPISODE%TYPE DEFAULT NULL,
      flg_temp_in IN INTERV_EVALUATION.FLG_TEMP%TYPE DEFAULT NULL,
      notes_cancel_in IN INTERV_EVALUATION.NOTES_CANCEL%TYPE DEFAULT NULL,
      id_patient_in IN INTERV_EVALUATION.ID_PATIENT%TYPE DEFAULT NULL,
      flg_type_in IN INTERV_EVALUATION.FLG_TYPE%TYPE DEFAULT NULL
     ,handle_error_in IN BOOLEAN := TRUE
      , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN

     INSERT INTO INTERV_EVALUATION (
         ID_INTERV_EVALUATION,
         DT_INTERV_EVALUATION_TSTZ,
         ID_PROFESSIONAL,
         NOTES,
         FLG_STATUS,
         DT_CANCEL_TSTZ,
         ID_PROF_CANCEL,
         ID_EPISODE,
         FLG_TEMP,
         NOTES_CANCEL,
         ID_PATIENT,
         FLG_TYPE
         )
      VALUES (
         id_interv_evaluation_in,
         dt_interv_evaluation_tstz_in,
         id_professional_in,
         notes_in,
         flg_status_in,
         dt_cancel_tstz_in,
         id_prof_cancel_in,
         id_episode_in,
         flg_temp_in,
         notes_cancel_in,
         id_patient_in,
         flg_type_in
         ) RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF FALSE THEN NULL; -- Placeholder in case no unique indexes
           ELSE
              pk_alert_exceptions.raise_error (
                    error_name_in => 'DUPLICATE-VALUE'
                    ,name1_in => 'OWNER'
                    ,value1_in => l_owner
                    ,name2_in => 'CONSTRAINT_NAME'
                    ,value2_in => l_name
                    ,name3_in => 'TABLE_NAME'
                    ,value3_in => 'INTERV_EVALUATION');
           END IF;
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_EVALUATION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_EVALUATION');
           IF l_name = 'IEND_EPIS_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_EPISODE'
               , value_in => id_episode_in);
           END IF;
           IF l_name = 'IEND_PAT_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_PATIENT'
               , value_in => id_patient_in);
           END IF;
           IF l_name = 'IEND_PROF_CANC_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_PROF_CANCEL'
               , value_in => id_prof_cancel_in);
           END IF;
           IF l_name = 'IEND_PROF_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_PROFESSIONAL'
               , value_in => id_professional_in);
           END IF;
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END ins;

   PROCEDURE ins (
      id_interv_evaluation_in IN INTERV_EVALUATION.ID_INTERV_EVALUATION%TYPE
      ,
      dt_interv_evaluation_tstz_in IN INTERV_EVALUATION.DT_INTERV_EVALUATION_TSTZ%TYPE DEFAULT NULL,
      id_professional_in IN INTERV_EVALUATION.ID_PROFESSIONAL%TYPE DEFAULT NULL,
      notes_in IN INTERV_EVALUATION.NOTES%TYPE DEFAULT NULL,
      flg_status_in IN INTERV_EVALUATION.FLG_STATUS%TYPE DEFAULT NULL,
      dt_cancel_tstz_in IN INTERV_EVALUATION.DT_CANCEL_TSTZ%TYPE DEFAULT NULL,
      id_prof_cancel_in IN INTERV_EVALUATION.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      id_episode_in IN INTERV_EVALUATION.ID_EPISODE%TYPE DEFAULT NULL,
      flg_temp_in IN INTERV_EVALUATION.FLG_TEMP%TYPE DEFAULT NULL,
      notes_cancel_in IN INTERV_EVALUATION.NOTES_CANCEL%TYPE DEFAULT NULL,
      id_patient_in IN INTERV_EVALUATION.ID_PATIENT%TYPE DEFAULT NULL,
      flg_type_in IN INTERV_EVALUATION.FLG_TYPE%TYPE DEFAULT NULL
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN

     ins (
      id_interv_evaluation_in => id_interv_evaluation_in
      ,
      dt_interv_evaluation_tstz_in => dt_interv_evaluation_tstz_in,
      id_professional_in => id_professional_in,
      notes_in => notes_in,
      flg_status_in => flg_status_in,
      dt_cancel_tstz_in => dt_cancel_tstz_in,
      id_prof_cancel_in => id_prof_cancel_in,
      id_episode_in => id_episode_in,
      flg_temp_in => flg_temp_in,
      notes_cancel_in => notes_cancel_in,
      id_patient_in => id_patient_in,
      flg_type_in => flg_type_in
     ,handle_error_in => handle_error_in
      ,rows_out => rows_out
      );
   END ins;


   PROCEDURE ins (
      rec_in IN INTERV_EVALUATION%ROWTYPE
     ,gen_pky_in IN BOOLEAN DEFAULT FALSE
     ,sequence_in IN VARCHAR2 := NULL
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
      l_rec INTERV_EVALUATION%ROWTYPE := rec_in;
   BEGIN
      IF gen_pky_in THEN
         l_rec.ID_INTERV_EVALUATION := next_key (sequence_in);
      END IF;
      ins (
         id_interv_evaluation_in => l_rec.ID_INTERV_EVALUATION
         ,
         dt_interv_evaluation_tstz_in => l_rec.DT_INTERV_EVALUATION_TSTZ,
         id_professional_in => l_rec.ID_PROFESSIONAL,
         notes_in => l_rec.NOTES,
         flg_status_in => l_rec.FLG_STATUS,
         dt_cancel_tstz_in => l_rec.DT_CANCEL_TSTZ,
         id_prof_cancel_in => l_rec.ID_PROF_CANCEL,
         id_episode_in => l_rec.ID_EPISODE,
         flg_temp_in => l_rec.FLG_TEMP,
         notes_cancel_in => l_rec.NOTES_CANCEL,
         id_patient_in => l_rec.ID_PATIENT,
         flg_type_in => l_rec.FLG_TYPE
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
   END ins;

   PROCEDURE ins (
      rec_in IN INTERV_EVALUATION%ROWTYPE
     ,gen_pky_in IN BOOLEAN DEFAULT FALSE
     ,sequence_in IN VARCHAR2 := NULL
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
      rows_out TABLE_VARCHAR;
   BEGIN

  ins (
      rec_in => rec_in
     ,gen_pky_in => gen_pky_in
     ,sequence_in => sequence_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
   );

   END ins;

   FUNCTION next_key (sequence_in IN VARCHAR2 := NULL) RETURN INTERV_EVALUATION.ID_INTERV_EVALUATION%TYPE

   IS
     retval INTERV_EVALUATION.ID_INTERV_EVALUATION%TYPE;

   BEGIN
      IF sequence_in IS NULL
      THEN
         SELECT seq_INTERV_EVALUATION.NEXTVAL INTO retval FROM dual;
      ELSE
         EXECUTE IMMEDIATE
            'SELECT ' || sequence_in || '.NEXTVAL FROM dual'
            INTO retval;
      END IF;
      RETURN retval;
   EXCEPTION
      WHEN OTHERS THEN
        pk_alert_exceptions.raise_error (
           error_name_in => 'SEQUENCE-GENERATION-FAILURE'
           ,name1_in => 'SEQUENCE'
           ,value1_in => NVL (sequence_in, 'seq_INTERV_EVALUATION')
           );
   END next_key;

   PROCEDURE ins (
      dt_interv_evaluation_tstz_in IN INTERV_EVALUATION.DT_INTERV_EVALUATION_TSTZ%TYPE DEFAULT NULL,
      id_professional_in IN INTERV_EVALUATION.ID_PROFESSIONAL%TYPE DEFAULT NULL,
      notes_in IN INTERV_EVALUATION.NOTES%TYPE DEFAULT NULL,
      flg_status_in IN INTERV_EVALUATION.FLG_STATUS%TYPE DEFAULT NULL,
      dt_cancel_tstz_in IN INTERV_EVALUATION.DT_CANCEL_TSTZ%TYPE DEFAULT NULL,
      id_prof_cancel_in IN INTERV_EVALUATION.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      id_episode_in IN INTERV_EVALUATION.ID_EPISODE%TYPE DEFAULT NULL,
      flg_temp_in IN INTERV_EVALUATION.FLG_TEMP%TYPE DEFAULT NULL,
      notes_cancel_in IN INTERV_EVALUATION.NOTES_CANCEL%TYPE DEFAULT NULL,
      id_patient_in IN INTERV_EVALUATION.ID_PATIENT%TYPE DEFAULT NULL,
      flg_type_in IN INTERV_EVALUATION.FLG_TYPE%TYPE DEFAULT NULL,
      id_interv_evaluation_out IN OUT INTERV_EVALUATION.ID_INTERV_EVALUATION%TYPE
      ,handle_error_in IN BOOLEAN := TRUE
      , rows_out OUT TABLE_VARCHAR
   )
   IS
        l_pky INTERV_EVALUATION.ID_INTERV_EVALUATION%TYPE := next_key;
   BEGIN
      ins (
         id_interv_evaluation_in => l_pky,
         dt_interv_evaluation_tstz_in => dt_interv_evaluation_tstz_in,
         id_professional_in => id_professional_in,
         notes_in => notes_in,
         flg_status_in => flg_status_in,
         dt_cancel_tstz_in => dt_cancel_tstz_in,
         id_prof_cancel_in => id_prof_cancel_in,
         id_episode_in => id_episode_in,
         flg_temp_in => flg_temp_in,
         notes_cancel_in => notes_cancel_in,
         id_patient_in => id_patient_in,
         flg_type_in => flg_type_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
      id_interv_evaluation_out := l_pky;
   END ins;

   PROCEDURE ins (
      dt_interv_evaluation_tstz_in IN INTERV_EVALUATION.DT_INTERV_EVALUATION_TSTZ%TYPE DEFAULT NULL,
      id_professional_in IN INTERV_EVALUATION.ID_PROFESSIONAL%TYPE DEFAULT NULL,
      notes_in IN INTERV_EVALUATION.NOTES%TYPE DEFAULT NULL,
      flg_status_in IN INTERV_EVALUATION.FLG_STATUS%TYPE DEFAULT NULL,
      dt_cancel_tstz_in IN INTERV_EVALUATION.DT_CANCEL_TSTZ%TYPE DEFAULT NULL,
      id_prof_cancel_in IN INTERV_EVALUATION.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      id_episode_in IN INTERV_EVALUATION.ID_EPISODE%TYPE DEFAULT NULL,
      flg_temp_in IN INTERV_EVALUATION.FLG_TEMP%TYPE DEFAULT NULL,
      notes_cancel_in IN INTERV_EVALUATION.NOTES_CANCEL%TYPE DEFAULT NULL,
      id_patient_in IN INTERV_EVALUATION.ID_PATIENT%TYPE DEFAULT NULL,
      flg_type_in IN INTERV_EVALUATION.FLG_TYPE%TYPE DEFAULT NULL,
      id_interv_evaluation_out IN OUT INTERV_EVALUATION.ID_INTERV_EVALUATION%TYPE
      ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
        rows_out TABLE_VARCHAR;
   BEGIN
      ins (
      dt_interv_evaluation_tstz_in => dt_interv_evaluation_tstz_in,
      id_professional_in => id_professional_in,
      notes_in => notes_in,
      flg_status_in => flg_status_in,
      dt_cancel_tstz_in => dt_cancel_tstz_in,
      id_prof_cancel_in => id_prof_cancel_in,
      id_episode_in => id_episode_in,
      flg_temp_in => flg_temp_in,
      notes_cancel_in => notes_cancel_in,
      id_patient_in => id_patient_in,
      flg_type_in => flg_type_in,
      id_interv_evaluation_out => id_interv_evaluation_out
      ,handle_error_in => handle_error_in
      , rows_out => rows_out
   );
   END ins;

   FUNCTION ins (
      dt_interv_evaluation_tstz_in IN INTERV_EVALUATION.DT_INTERV_EVALUATION_TSTZ%TYPE DEFAULT NULL,
      id_professional_in IN INTERV_EVALUATION.ID_PROFESSIONAL%TYPE DEFAULT NULL,
      notes_in IN INTERV_EVALUATION.NOTES%TYPE DEFAULT NULL,
      flg_status_in IN INTERV_EVALUATION.FLG_STATUS%TYPE DEFAULT NULL,
      dt_cancel_tstz_in IN INTERV_EVALUATION.DT_CANCEL_TSTZ%TYPE DEFAULT NULL,
      id_prof_cancel_in IN INTERV_EVALUATION.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      id_episode_in IN INTERV_EVALUATION.ID_EPISODE%TYPE DEFAULT NULL,
      flg_temp_in IN INTERV_EVALUATION.FLG_TEMP%TYPE DEFAULT NULL,
      notes_cancel_in IN INTERV_EVALUATION.NOTES_CANCEL%TYPE DEFAULT NULL,
      id_patient_in IN INTERV_EVALUATION.ID_PATIENT%TYPE DEFAULT NULL,
      flg_type_in IN INTERV_EVALUATION.FLG_TYPE%TYPE DEFAULT NULL
      ,handle_error_in IN BOOLEAN := TRUE
      , rows_out OUT TABLE_VARCHAR
   )
      RETURN
         INTERV_EVALUATION.ID_INTERV_EVALUATION%TYPE
   IS
        l_pky INTERV_EVALUATION.ID_INTERV_EVALUATION%TYPE := next_key;
   BEGIN
      ins (
         id_interv_evaluation_in => l_pky,
         dt_interv_evaluation_tstz_in => dt_interv_evaluation_tstz_in,
         id_professional_in => id_professional_in,
         notes_in => notes_in,
         flg_status_in => flg_status_in,
         dt_cancel_tstz_in => dt_cancel_tstz_in,
         id_prof_cancel_in => id_prof_cancel_in,
         id_episode_in => id_episode_in,
         flg_temp_in => flg_temp_in,
         notes_cancel_in => notes_cancel_in,
         id_patient_in => id_patient_in,
         flg_type_in => flg_type_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
      RETURN l_pky;
   END ins;

   FUNCTION ins (
      dt_interv_evaluation_tstz_in IN INTERV_EVALUATION.DT_INTERV_EVALUATION_TSTZ%TYPE DEFAULT NULL,
      id_professional_in IN INTERV_EVALUATION.ID_PROFESSIONAL%TYPE DEFAULT NULL,
      notes_in IN INTERV_EVALUATION.NOTES%TYPE DEFAULT NULL,
      flg_status_in IN INTERV_EVALUATION.FLG_STATUS%TYPE DEFAULT NULL,
      dt_cancel_tstz_in IN INTERV_EVALUATION.DT_CANCEL_TSTZ%TYPE DEFAULT NULL,
      id_prof_cancel_in IN INTERV_EVALUATION.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      id_episode_in IN INTERV_EVALUATION.ID_EPISODE%TYPE DEFAULT NULL,
      flg_temp_in IN INTERV_EVALUATION.FLG_TEMP%TYPE DEFAULT NULL,
      notes_cancel_in IN INTERV_EVALUATION.NOTES_CANCEL%TYPE DEFAULT NULL,
      id_patient_in IN INTERV_EVALUATION.ID_PATIENT%TYPE DEFAULT NULL,
      flg_type_in IN INTERV_EVALUATION.FLG_TYPE%TYPE DEFAULT NULL
      ,handle_error_in IN BOOLEAN := TRUE
   )
      RETURN
         INTERV_EVALUATION.ID_INTERV_EVALUATION%TYPE
   IS
        l_pky INTERV_EVALUATION.ID_INTERV_EVALUATION%TYPE := next_key;
    rows_out TABLE_VARCHAR;
   BEGIN
      ins (
         id_interv_evaluation_in => l_pky,
         dt_interv_evaluation_tstz_in => dt_interv_evaluation_tstz_in,
         id_professional_in => id_professional_in,
         notes_in => notes_in,
         flg_status_in => flg_status_in,
         dt_cancel_tstz_in => dt_cancel_tstz_in,
         id_prof_cancel_in => id_prof_cancel_in,
         id_episode_in => id_episode_in,
         flg_temp_in => flg_temp_in,
         notes_cancel_in => notes_cancel_in,
         id_patient_in => id_patient_in,
         flg_type_in => flg_type_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
      RETURN l_pky;
   END ins;

      PROCEDURE ins (
      dt_interv_evaluation_tstz_in IN INTERV_EVALUATION.DT_INTERV_EVALUATION_TSTZ%TYPE DEFAULT NULL,
      id_professional_in IN INTERV_EVALUATION.ID_PROFESSIONAL%TYPE DEFAULT NULL,
      notes_in IN INTERV_EVALUATION.NOTES%TYPE DEFAULT NULL,
      flg_status_in IN INTERV_EVALUATION.FLG_STATUS%TYPE DEFAULT NULL,
      dt_cancel_tstz_in IN INTERV_EVALUATION.DT_CANCEL_TSTZ%TYPE DEFAULT NULL,
      id_prof_cancel_in IN INTERV_EVALUATION.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      id_episode_in IN INTERV_EVALUATION.ID_EPISODE%TYPE DEFAULT NULL,
      flg_temp_in IN INTERV_EVALUATION.FLG_TEMP%TYPE DEFAULT NULL,
      notes_cancel_in IN INTERV_EVALUATION.NOTES_CANCEL%TYPE DEFAULT NULL,
      id_patient_in IN INTERV_EVALUATION.ID_PATIENT%TYPE DEFAULT NULL,
      flg_type_in IN INTERV_EVALUATION.FLG_TYPE%TYPE DEFAULT NULL
      ,handle_error_in IN BOOLEAN := TRUE
      , rows_out OUT TABLE_VARCHAR
   )
   IS
        l_pky INTERV_EVALUATION.ID_INTERV_EVALUATION%TYPE := next_key;
   BEGIN
      ins (
         id_interv_evaluation_in => l_pky,
         dt_interv_evaluation_tstz_in => dt_interv_evaluation_tstz_in,
         id_professional_in => id_professional_in,
         notes_in => notes_in,
         flg_status_in => flg_status_in,
         dt_cancel_tstz_in => dt_cancel_tstz_in,
         id_prof_cancel_in => id_prof_cancel_in,
         id_episode_in => id_episode_in,
         flg_temp_in => flg_temp_in,
         notes_cancel_in => notes_cancel_in,
         id_patient_in => id_patient_in,
         flg_type_in => flg_type_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
   END ins;


     PROCEDURE ins (
      dt_interv_evaluation_tstz_in IN INTERV_EVALUATION.DT_INTERV_EVALUATION_TSTZ%TYPE DEFAULT NULL,
      id_professional_in IN INTERV_EVALUATION.ID_PROFESSIONAL%TYPE DEFAULT NULL,
      notes_in IN INTERV_EVALUATION.NOTES%TYPE DEFAULT NULL,
      flg_status_in IN INTERV_EVALUATION.FLG_STATUS%TYPE DEFAULT NULL,
      dt_cancel_tstz_in IN INTERV_EVALUATION.DT_CANCEL_TSTZ%TYPE DEFAULT NULL,
      id_prof_cancel_in IN INTERV_EVALUATION.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      id_episode_in IN INTERV_EVALUATION.ID_EPISODE%TYPE DEFAULT NULL,
      flg_temp_in IN INTERV_EVALUATION.FLG_TEMP%TYPE DEFAULT NULL,
      notes_cancel_in IN INTERV_EVALUATION.NOTES_CANCEL%TYPE DEFAULT NULL,
      id_patient_in IN INTERV_EVALUATION.ID_PATIENT%TYPE DEFAULT NULL,
      flg_type_in IN INTERV_EVALUATION.FLG_TYPE%TYPE DEFAULT NULL
      ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
        l_pky INTERV_EVALUATION.ID_INTERV_EVALUATION%TYPE := next_key;
      rows_out TABLE_VARCHAR;
   BEGIN
      ins (
         id_interv_evaluation_in => l_pky,
         dt_interv_evaluation_tstz_in => dt_interv_evaluation_tstz_in,
         id_professional_in => id_professional_in,
         notes_in => notes_in,
         flg_status_in => flg_status_in,
         dt_cancel_tstz_in => dt_cancel_tstz_in,
         id_prof_cancel_in => id_prof_cancel_in,
         id_episode_in => id_episode_in,
         flg_temp_in => flg_temp_in,
         notes_cancel_in => notes_cancel_in,
         id_patient_in => id_patient_in,
         flg_type_in => flg_type_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
   END ins;

    PROCEDURE ins (
      rows_in IN INTERV_EVALUATION_tc
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
   BEGIN
      IF rows_in.COUNT = 0
      THEN
         NULL;
      ELSE
         FORALL indx IN rows_in.FIRST .. rows_in.LAST
            SAVE EXCEPTIONS
            INSERT INTO INTERV_EVALUATION VALUES rows_in (indx) RETURNING ROWID BULK COLLECT INTO rows_out;
      END IF;
   EXCEPTION
     WHEN e_forall_error
     THEN
        -- In Oracle9i and above, SAVE EXCEPTIONS will direct control
        -- here if any error occurs. We can then save all the error
        -- information out to the error instance.
       IF NOT handle_error_in THEN RAISE;
       ELSE
          <<bulk_handler>>
          DECLARE
             l_err_instance_id NUMBER;
          BEGIN
             -- For each error, write to the log.
             FOR indx IN 1 .. SQL%BULK_EXCEPTIONS.COUNT
             LOOP
                pk_alert_exceptions.register_error (
                    error_name_in => 'FORALL-INSERT-FAILURE'
                   ,err_instance_id_out => l_err_instance_id
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'BINDING_ROW_' || indx
                  ,value_in => SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX
                  ,validate_in => FALSE
                );
                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ERROR_AT_ROW_' || indx
                  ,value_in => SQL%BULK_EXCEPTIONS (indx).ERROR_CODE
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_INTERV_EVALUATION _' || indx
                  ,value_in => rows_in(indx).ID_INTERV_EVALUATION
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'DT_INTERV_EVALUATION_TSTZ _' || indx
                  ,value_in => rows_in(indx).DT_INTERV_EVALUATION_TSTZ
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_PROFESSIONAL _' || indx
                  ,value_in => rows_in(indx).ID_PROFESSIONAL
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'NOTES _' || indx
                  ,value_in => rows_in(indx).NOTES
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'FLG_STATUS _' || indx
                  ,value_in => rows_in(indx).FLG_STATUS
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'DT_CANCEL_TSTZ _' || indx
                  ,value_in => rows_in(indx).DT_CANCEL_TSTZ
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_PROF_CANCEL _' || indx
                  ,value_in => rows_in(indx).ID_PROF_CANCEL
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_EPISODE _' || indx
                  ,value_in => rows_in(indx).ID_EPISODE
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'FLG_TEMP _' || indx
                  ,value_in => rows_in(indx).FLG_TEMP
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'NOTES_CANCEL _' || indx
                  ,value_in => rows_in(indx).NOTES_CANCEL
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_PATIENT _' || indx
                  ,value_in => rows_in(indx).ID_PATIENT
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'FLG_TYPE _' || indx
                  ,value_in => rows_in(indx).FLG_TYPE
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.raise_error_instance( err_instance_id_in => l_err_instance_id );
             END LOOP;
          END bulk_handler;
        END IF;
     WHEN OTHERS
     THEN
       IF NOT handle_error_in THEN RAISE;
       ELSE
       pk_alert_exceptions.raise_error(
          error_name_in => 'FORALL-INSERT-FAILURE'
          ,name1_in => 'TABLE_NAME'
          ,value1_in => 'INTERV_EVALUATION'
          ,name2_in => 'ROW_COUNT'
          ,value2_in => rows_in.COUNT
           );
       END IF;
   END ins;

    PROCEDURE ins (
      rows_in IN INTERV_EVALUATION_tc
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
      rows_out TABLE_VARCHAR;
   BEGIN
      ins (
      rows_in => rows_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
   );
   END ins;


PROCEDURE upd (
      id_interv_evaluation_in IN INTERV_EVALUATION.ID_INTERV_EVALUATION%TYPE,
      dt_interv_evaluation_tstz_in IN INTERV_EVALUATION.DT_INTERV_EVALUATION_TSTZ%TYPE DEFAULT NULL,
      DT_INTERV_EVALUATION_TSTZ_nin IN BOOLEAN := TRUE,
      id_professional_in IN INTERV_EVALUATION.ID_PROFESSIONAL%TYPE DEFAULT NULL,
      ID_PROFESSIONAL_nin IN BOOLEAN := TRUE,
      notes_in IN INTERV_EVALUATION.NOTES%TYPE DEFAULT NULL,
      NOTES_nin IN BOOLEAN := TRUE,
      flg_status_in IN INTERV_EVALUATION.FLG_STATUS%TYPE DEFAULT NULL,
      FLG_STATUS_nin IN BOOLEAN := TRUE,
      dt_cancel_tstz_in IN INTERV_EVALUATION.DT_CANCEL_TSTZ%TYPE DEFAULT NULL,
      DT_CANCEL_TSTZ_nin IN BOOLEAN := TRUE,
      id_prof_cancel_in IN INTERV_EVALUATION.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      ID_PROF_CANCEL_nin IN BOOLEAN := TRUE,
      id_episode_in IN INTERV_EVALUATION.ID_EPISODE%TYPE DEFAULT NULL,
      ID_EPISODE_nin IN BOOLEAN := TRUE,
      flg_temp_in IN INTERV_EVALUATION.FLG_TEMP%TYPE DEFAULT NULL,
      FLG_TEMP_nin IN BOOLEAN := TRUE,
      notes_cancel_in IN INTERV_EVALUATION.NOTES_CANCEL%TYPE DEFAULT NULL,
      NOTES_CANCEL_nin IN BOOLEAN := TRUE,
      id_patient_in IN INTERV_EVALUATION.ID_PATIENT%TYPE DEFAULT NULL,
      ID_PATIENT_nin IN BOOLEAN := TRUE,
      flg_type_in IN INTERV_EVALUATION.FLG_TYPE%TYPE DEFAULT NULL,
      FLG_TYPE_nin IN BOOLEAN := TRUE,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      )
   IS
   l_rows_out TABLE_VARCHAR;
   l_DT_INTERV_EVALUATION_TSTZ_n NUMBER(1);
   l_ID_PROFESSIONAL_n NUMBER(1);
   l_NOTES_n NUMBER(1);
   l_FLG_STATUS_n NUMBER(1);
   l_DT_CANCEL_TSTZ_n NUMBER(1);
   l_ID_PROF_CANCEL_n NUMBER(1);
   l_ID_EPISODE_n NUMBER(1);
   l_FLG_TEMP_n NUMBER(1);
   l_NOTES_CANCEL_n NUMBER(1);
   l_ID_PATIENT_n NUMBER(1);
   l_FLG_TYPE_n NUMBER(1);
   BEGIN

   l_DT_INTERV_EVALUATION_TSTZ_n := sys.diutil.bool_to_int(DT_INTERV_EVALUATION_TSTZ_nin);
   l_ID_PROFESSIONAL_n := sys.diutil.bool_to_int(ID_PROFESSIONAL_nin);
   l_NOTES_n := sys.diutil.bool_to_int(NOTES_nin);
   l_FLG_STATUS_n := sys.diutil.bool_to_int(FLG_STATUS_nin);
   l_DT_CANCEL_TSTZ_n := sys.diutil.bool_to_int(DT_CANCEL_TSTZ_nin);
   l_ID_PROF_CANCEL_n := sys.diutil.bool_to_int(ID_PROF_CANCEL_nin);
   l_ID_EPISODE_n := sys.diutil.bool_to_int(ID_EPISODE_nin);
   l_FLG_TEMP_n := sys.diutil.bool_to_int(FLG_TEMP_nin);
   l_NOTES_CANCEL_n := sys.diutil.bool_to_int(NOTES_CANCEL_nin);
   l_ID_PATIENT_n := sys.diutil.bool_to_int(ID_PATIENT_nin);
   l_FLG_TYPE_n := sys.diutil.bool_to_int(FLG_TYPE_nin);


         UPDATE INTERV_EVALUATION SET
     DT_INTERV_EVALUATION_TSTZ = decode (l_DT_INTERV_EVALUATION_TSTZ_n,0,dt_interv_evaluation_tstz_in, NVL (dt_interv_evaluation_tstz_in, DT_INTERV_EVALUATION_TSTZ)),
     ID_PROFESSIONAL = decode (l_ID_PROFESSIONAL_n,0,id_professional_in, NVL (id_professional_in, ID_PROFESSIONAL)),
     NOTES = decode (l_NOTES_n,0,notes_in, NVL (notes_in, NOTES)),
     FLG_STATUS = decode (l_FLG_STATUS_n,0,flg_status_in, NVL (flg_status_in, FLG_STATUS)),
     DT_CANCEL_TSTZ = decode (l_DT_CANCEL_TSTZ_n,0,dt_cancel_tstz_in, NVL (dt_cancel_tstz_in, DT_CANCEL_TSTZ)),
     ID_PROF_CANCEL = decode (l_ID_PROF_CANCEL_n,0,id_prof_cancel_in, NVL (id_prof_cancel_in, ID_PROF_CANCEL)),
     ID_EPISODE = decode (l_ID_EPISODE_n,0,id_episode_in, NVL (id_episode_in, ID_EPISODE)),
     FLG_TEMP = decode (l_FLG_TEMP_n,0,flg_temp_in, NVL (flg_temp_in, FLG_TEMP)),
     NOTES_CANCEL = decode (l_NOTES_CANCEL_n,0,notes_cancel_in, NVL (notes_cancel_in, NOTES_CANCEL)),
     ID_PATIENT = decode (l_ID_PATIENT_n,0,id_patient_in, NVL (id_patient_in, ID_PATIENT)),
     FLG_TYPE = decode (l_FLG_TYPE_n,0,flg_type_in, NVL (flg_type_in, FLG_TYPE))
          WHERE
             ID_INTERV_EVALUATION = id_interv_evaluation_in
         RETURNING ROWID BULK COLLECT INTO l_rows_out;


if(rows_out is null)
then
rows_out := table_varchar();
end if;

rows_out :=  rows_out MULTISET UNION DISTINCT l_rows_out;

   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF FALSE THEN NULL; -- Placeholder in case no unique indexes
           ELSE
              pk_alert_exceptions.raise_error (
                    error_name_in => 'DUPLICATE-VALUE'
                    ,name1_in => 'OWNER'
                    ,value1_in => l_owner
                    ,name2_in => 'CONSTRAINT_NAME'
                    ,value2_in => l_name
                    ,name3_in => 'TABLE_NAME'
                    ,value3_in => 'INTERV_EVALUATION');
           END IF;
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_EVALUATION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_EVALUATION');
           IF l_name = 'IEND_EPIS_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_EPISODE'
               , value_in => id_episode_in);
           END IF;
           IF l_name = 'IEND_PAT_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_PATIENT'
               , value_in => id_patient_in);
           END IF;
           IF l_name = 'IEND_PROF_CANC_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_PROF_CANCEL'
               , value_in => id_prof_cancel_in);
           END IF;
           IF l_name = 'IEND_PROF_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_PROFESSIONAL'
               , value_in => id_professional_in);
           END IF;
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END upd;


   PROCEDURE upd (
      id_interv_evaluation_in IN INTERV_EVALUATION.ID_INTERV_EVALUATION%TYPE,
      dt_interv_evaluation_tstz_in IN INTERV_EVALUATION.DT_INTERV_EVALUATION_TSTZ%TYPE DEFAULT NULL,
      DT_INTERV_EVALUATION_TSTZ_nin IN BOOLEAN := TRUE,
      id_professional_in IN INTERV_EVALUATION.ID_PROFESSIONAL%TYPE DEFAULT NULL,
      ID_PROFESSIONAL_nin IN BOOLEAN := TRUE,
      notes_in IN INTERV_EVALUATION.NOTES%TYPE DEFAULT NULL,
      NOTES_nin IN BOOLEAN := TRUE,
      flg_status_in IN INTERV_EVALUATION.FLG_STATUS%TYPE DEFAULT NULL,
      FLG_STATUS_nin IN BOOLEAN := TRUE,
      dt_cancel_tstz_in IN INTERV_EVALUATION.DT_CANCEL_TSTZ%TYPE DEFAULT NULL,
      DT_CANCEL_TSTZ_nin IN BOOLEAN := TRUE,
      id_prof_cancel_in IN INTERV_EVALUATION.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      ID_PROF_CANCEL_nin IN BOOLEAN := TRUE,
      id_episode_in IN INTERV_EVALUATION.ID_EPISODE%TYPE DEFAULT NULL,
      ID_EPISODE_nin IN BOOLEAN := TRUE,
      flg_temp_in IN INTERV_EVALUATION.FLG_TEMP%TYPE DEFAULT NULL,
      FLG_TEMP_nin IN BOOLEAN := TRUE,
      notes_cancel_in IN INTERV_EVALUATION.NOTES_CANCEL%TYPE DEFAULT NULL,
      NOTES_CANCEL_nin IN BOOLEAN := TRUE,
      id_patient_in IN INTERV_EVALUATION.ID_PATIENT%TYPE DEFAULT NULL,
      ID_PATIENT_nin IN BOOLEAN := TRUE,
      flg_type_in IN INTERV_EVALUATION.FLG_TYPE%TYPE DEFAULT NULL,
      FLG_TYPE_nin IN BOOLEAN := TRUE,
     handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
     upd (
      id_interv_evaluation_in => id_interv_evaluation_in,
      dt_interv_evaluation_tstz_in => dt_interv_evaluation_tstz_in,
      DT_INTERV_EVALUATION_TSTZ_nin => DT_INTERV_EVALUATION_TSTZ_nin,
      id_professional_in => id_professional_in,
      ID_PROFESSIONAL_nin => ID_PROFESSIONAL_nin,
      notes_in => notes_in,
      NOTES_nin => NOTES_nin,
      flg_status_in => flg_status_in,
      FLG_STATUS_nin => FLG_STATUS_nin,
      dt_cancel_tstz_in => dt_cancel_tstz_in,
      DT_CANCEL_TSTZ_nin => DT_CANCEL_TSTZ_nin,
      id_prof_cancel_in => id_prof_cancel_in,
      ID_PROF_CANCEL_nin => ID_PROF_CANCEL_nin,
      id_episode_in => id_episode_in,
      ID_EPISODE_nin => ID_EPISODE_nin,
      flg_temp_in => flg_temp_in,
      FLG_TEMP_nin => FLG_TEMP_nin,
      notes_cancel_in => notes_cancel_in,
      NOTES_CANCEL_nin => NOTES_CANCEL_nin,
      id_patient_in => id_patient_in,
      ID_PATIENT_nin => ID_PATIENT_nin,
      flg_type_in => flg_type_in,
      FLG_TYPE_nin => FLG_TYPE_nin,
     handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END upd;

PROCEDURE upd (
      dt_interv_evaluation_tstz_in IN INTERV_EVALUATION.DT_INTERV_EVALUATION_TSTZ%TYPE DEFAULT NULL,
      DT_INTERV_EVALUATION_TSTZ_nin IN BOOLEAN := TRUE,
      id_professional_in IN INTERV_EVALUATION.ID_PROFESSIONAL%TYPE DEFAULT NULL,
      ID_PROFESSIONAL_nin IN BOOLEAN := TRUE,
      notes_in IN INTERV_EVALUATION.NOTES%TYPE DEFAULT NULL,
      NOTES_nin IN BOOLEAN := TRUE,
      flg_status_in IN INTERV_EVALUATION.FLG_STATUS%TYPE DEFAULT NULL,
      FLG_STATUS_nin IN BOOLEAN := TRUE,
      dt_cancel_tstz_in IN INTERV_EVALUATION.DT_CANCEL_TSTZ%TYPE DEFAULT NULL,
      DT_CANCEL_TSTZ_nin IN BOOLEAN := TRUE,
      id_prof_cancel_in IN INTERV_EVALUATION.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      ID_PROF_CANCEL_nin IN BOOLEAN := TRUE,
      id_episode_in IN INTERV_EVALUATION.ID_EPISODE%TYPE DEFAULT NULL,
      ID_EPISODE_nin IN BOOLEAN := TRUE,
      flg_temp_in IN INTERV_EVALUATION.FLG_TEMP%TYPE DEFAULT NULL,
      FLG_TEMP_nin IN BOOLEAN := TRUE,
      notes_cancel_in IN INTERV_EVALUATION.NOTES_CANCEL%TYPE DEFAULT NULL,
      NOTES_CANCEL_nin IN BOOLEAN := TRUE,
      id_patient_in IN INTERV_EVALUATION.ID_PATIENT%TYPE DEFAULT NULL,
      ID_PATIENT_nin IN BOOLEAN := TRUE,
      flg_type_in IN INTERV_EVALUATION.FLG_TYPE%TYPE DEFAULT NULL,
      FLG_TYPE_nin IN BOOLEAN := TRUE,
    where_in varchar2 DEFAULT NULL,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      )
   IS
   l_sql VARCHAR2(32767);
   l_rows_out TABLE_VARCHAR;
   l_DT_INTERV_EVALUATION_TSTZ_n NUMBER(1);
   l_ID_PROFESSIONAL_n NUMBER(1);
   l_NOTES_n NUMBER(1);
   l_FLG_STATUS_n NUMBER(1);
   l_DT_CANCEL_TSTZ_n NUMBER(1);
   l_ID_PROF_CANCEL_n NUMBER(1);
   l_ID_EPISODE_n NUMBER(1);
   l_FLG_TEMP_n NUMBER(1);
   l_NOTES_CANCEL_n NUMBER(1);
   l_ID_PATIENT_n NUMBER(1);
   l_FLG_TYPE_n NUMBER(1);
      id_interv_evaluation_in INTERV_EVALUATION.ID_INTERV_EVALUATION%TYPE;
   BEGIN



      l_DT_INTERV_EVALUATION_TSTZ_n := sys.diutil.bool_to_int(DT_INTERV_EVALUATION_TSTZ_nin);
      l_ID_PROFESSIONAL_n := sys.diutil.bool_to_int(ID_PROFESSIONAL_nin);
      l_NOTES_n := sys.diutil.bool_to_int(NOTES_nin);
      l_FLG_STATUS_n := sys.diutil.bool_to_int(FLG_STATUS_nin);
      l_DT_CANCEL_TSTZ_n := sys.diutil.bool_to_int(DT_CANCEL_TSTZ_nin);
      l_ID_PROF_CANCEL_n := sys.diutil.bool_to_int(ID_PROF_CANCEL_nin);
      l_ID_EPISODE_n := sys.diutil.bool_to_int(ID_EPISODE_nin);
      l_FLG_TEMP_n := sys.diutil.bool_to_int(FLG_TEMP_nin);
      l_NOTES_CANCEL_n := sys.diutil.bool_to_int(NOTES_CANCEL_nin);
      l_ID_PATIENT_n := sys.diutil.bool_to_int(ID_PATIENT_nin);
      l_FLG_TYPE_n := sys.diutil.bool_to_int(FLG_TYPE_nin);



l_sql := 'UPDATE INTERV_EVALUATION SET '
     || ' DT_INTERV_EVALUATION_TSTZ = decode (' || l_DT_INTERV_EVALUATION_TSTZ_n || ',0,:dt_interv_evaluation_tstz_in, NVL (:dt_interv_evaluation_tstz_in, DT_INTERV_EVALUATION_TSTZ)) '|| ','
     || ' ID_PROFESSIONAL = decode (' || l_ID_PROFESSIONAL_n || ',0,:id_professional_in, NVL (:id_professional_in, ID_PROFESSIONAL)) '|| ','
     || ' NOTES = decode (' || l_NOTES_n || ',0,:notes_in, NVL (:notes_in, NOTES)) '|| ','
     || ' FLG_STATUS = decode (' || l_FLG_STATUS_n || ',0,:flg_status_in, NVL (:flg_status_in, FLG_STATUS)) '|| ','
     || ' DT_CANCEL_TSTZ = decode (' || l_DT_CANCEL_TSTZ_n || ',0,:dt_cancel_tstz_in, NVL (:dt_cancel_tstz_in, DT_CANCEL_TSTZ)) '|| ','
     || ' ID_PROF_CANCEL = decode (' || l_ID_PROF_CANCEL_n || ',0,:id_prof_cancel_in, NVL (:id_prof_cancel_in, ID_PROF_CANCEL)) '|| ','
     || ' ID_EPISODE = decode (' || l_ID_EPISODE_n || ',0,:id_episode_in, NVL (:id_episode_in, ID_EPISODE)) '|| ','
     || ' FLG_TEMP = decode (' || l_FLG_TEMP_n || ',0,:flg_temp_in, NVL (:flg_temp_in, FLG_TEMP)) '|| ','
     || ' NOTES_CANCEL = decode (' || l_NOTES_CANCEL_n || ',0,:notes_cancel_in, NVL (:notes_cancel_in, NOTES_CANCEL)) '|| ','
     || ' ID_PATIENT = decode (' || l_ID_PATIENT_n || ',0,:id_patient_in, NVL (:id_patient_in, ID_PATIENT)) '|| ','
     || ' FLG_TYPE = decode (' || l_FLG_TYPE_n || ',0,:flg_type_in, NVL (:flg_type_in, FLG_TYPE)) '
      || ' where ' || nvl(where_in,'(1=1)')
      || ' RETURNING ROWID BULK COLLECT INTO :l_rows_out';




execute immediate 'BEGIN ' || l_sql || '; END;' using in
     dt_interv_evaluation_tstz_in,
     id_professional_in,
     notes_in,
     flg_status_in,
     dt_cancel_tstz_in,
     id_prof_cancel_in,
     id_episode_in,
     flg_temp_in,
     notes_cancel_in,
     id_patient_in,
     flg_type_in,
    OUT l_rows_out;

if(rows_out is null)
then
rows_out := table_varchar();
end if;

rows_out :=  rows_out MULTISET UNION DISTINCT l_rows_out;

   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF FALSE THEN NULL; -- Placeholder in case no unique indexes
           ELSE
              pk_alert_exceptions.raise_error (
                    error_name_in => 'DUPLICATE-VALUE'
                    ,name1_in => 'OWNER'
                    ,value1_in => l_owner
                    ,name2_in => 'CONSTRAINT_NAME'
                    ,value2_in => l_name
                    ,name3_in => 'TABLE_NAME'
                    ,value3_in => 'INTERV_EVALUATION');
           END IF;
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_EVALUATION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_EVALUATION');
           IF l_name = 'IEND_EPIS_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_EPISODE'
               , value_in => id_episode_in);
           END IF;
           IF l_name = 'IEND_PAT_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_PATIENT'
               , value_in => id_patient_in);
           END IF;
           IF l_name = 'IEND_PROF_CANC_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_PROF_CANCEL'
               , value_in => id_prof_cancel_in);
           END IF;
           IF l_name = 'IEND_PROF_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_PROFESSIONAL'
               , value_in => id_professional_in);
           END IF;
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END upd;





PROCEDURE upd (
      dt_interv_evaluation_tstz_in IN INTERV_EVALUATION.DT_INTERV_EVALUATION_TSTZ%TYPE DEFAULT NULL,
      DT_INTERV_EVALUATION_TSTZ_nin IN BOOLEAN := TRUE,
      id_professional_in IN INTERV_EVALUATION.ID_PROFESSIONAL%TYPE DEFAULT NULL,
      ID_PROFESSIONAL_nin IN BOOLEAN := TRUE,
      notes_in IN INTERV_EVALUATION.NOTES%TYPE DEFAULT NULL,
      NOTES_nin IN BOOLEAN := TRUE,
      flg_status_in IN INTERV_EVALUATION.FLG_STATUS%TYPE DEFAULT NULL,
      FLG_STATUS_nin IN BOOLEAN := TRUE,
      dt_cancel_tstz_in IN INTERV_EVALUATION.DT_CANCEL_TSTZ%TYPE DEFAULT NULL,
      DT_CANCEL_TSTZ_nin IN BOOLEAN := TRUE,
      id_prof_cancel_in IN INTERV_EVALUATION.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      ID_PROF_CANCEL_nin IN BOOLEAN := TRUE,
      id_episode_in IN INTERV_EVALUATION.ID_EPISODE%TYPE DEFAULT NULL,
      ID_EPISODE_nin IN BOOLEAN := TRUE,
      flg_temp_in IN INTERV_EVALUATION.FLG_TEMP%TYPE DEFAULT NULL,
      FLG_TEMP_nin IN BOOLEAN := TRUE,
      notes_cancel_in IN INTERV_EVALUATION.NOTES_CANCEL%TYPE DEFAULT NULL,
      NOTES_CANCEL_nin IN BOOLEAN := TRUE,
      id_patient_in IN INTERV_EVALUATION.ID_PATIENT%TYPE DEFAULT NULL,
      ID_PATIENT_nin IN BOOLEAN := TRUE,
      flg_type_in IN INTERV_EVALUATION.FLG_TYPE%TYPE DEFAULT NULL,
      FLG_TYPE_nin IN BOOLEAN := TRUE,
    where_in varchar2 DEFAULT NULL,
     handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
      upd (
      dt_interv_evaluation_tstz_in => dt_interv_evaluation_tstz_in,
      DT_INTERV_EVALUATION_TSTZ_nin => DT_INTERV_EVALUATION_TSTZ_nin,
      id_professional_in => id_professional_in,
      ID_PROFESSIONAL_nin => ID_PROFESSIONAL_nin,
      notes_in => notes_in,
      NOTES_nin => NOTES_nin,
      flg_status_in => flg_status_in,
      FLG_STATUS_nin => FLG_STATUS_nin,
      dt_cancel_tstz_in => dt_cancel_tstz_in,
      DT_CANCEL_TSTZ_nin => DT_CANCEL_TSTZ_nin,
      id_prof_cancel_in => id_prof_cancel_in,
      ID_PROF_CANCEL_nin => ID_PROF_CANCEL_nin,
      id_episode_in => id_episode_in,
      ID_EPISODE_nin => ID_EPISODE_nin,
      flg_temp_in => flg_temp_in,
      FLG_TEMP_nin => FLG_TEMP_nin,
      notes_cancel_in => notes_cancel_in,
      NOTES_CANCEL_nin => NOTES_CANCEL_nin,
      id_patient_in => id_patient_in,
      ID_PATIENT_nin => ID_PATIENT_nin,
      flg_type_in => flg_type_in,
      FLG_TYPE_nin => FLG_TYPE_nin,
    where_in => where_in,
     handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END upd;

   PROCEDURE upd (
      rec_in IN INTERV_EVALUATION%ROWTYPE,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      upd (
         id_interv_evaluation_in => rec_in.ID_INTERV_EVALUATION,
         dt_interv_evaluation_tstz_in => rec_in.DT_INTERV_EVALUATION_TSTZ,
         id_professional_in => rec_in.ID_PROFESSIONAL,
         notes_in => rec_in.NOTES,
         flg_status_in => rec_in.FLG_STATUS,
         dt_cancel_tstz_in => rec_in.DT_CANCEL_TSTZ,
         id_prof_cancel_in => rec_in.ID_PROF_CANCEL,
         id_episode_in => rec_in.ID_EPISODE,
         flg_temp_in => rec_in.FLG_TEMP,
         notes_cancel_in => rec_in.NOTES_CANCEL,
         id_patient_in => rec_in.ID_PATIENT,
         flg_type_in => rec_in.FLG_TYPE

        ,handle_error_in => handle_error_in
        , rows_out => rows_out
       );
   END upd;

   PROCEDURE upd (
      rec_in IN INTERV_EVALUATION%ROWTYPE,
     handle_error_in IN BOOLEAN := TRUE
      )
   IS
        rows_out TABLE_VARCHAR;
   BEGIN
      upd (
         id_interv_evaluation_in => rec_in.ID_INTERV_EVALUATION,
         dt_interv_evaluation_tstz_in => rec_in.DT_INTERV_EVALUATION_TSTZ,
         id_professional_in => rec_in.ID_PROFESSIONAL,
         notes_in => rec_in.NOTES,
         flg_status_in => rec_in.FLG_STATUS,
         dt_cancel_tstz_in => rec_in.DT_CANCEL_TSTZ,
         id_prof_cancel_in => rec_in.ID_PROF_CANCEL,
         id_episode_in => rec_in.ID_EPISODE,
         flg_temp_in => rec_in.FLG_TEMP,
         notes_cancel_in => rec_in.NOTES_CANCEL,
         id_patient_in => rec_in.ID_PATIENT,
         flg_type_in => rec_in.FLG_TYPE

        ,handle_error_in => handle_error_in
        , rows_out => rows_out
       );
   END upd;

   PROCEDURE upd_ins (
      id_interv_evaluation_in IN INTERV_EVALUATION.ID_INTERV_EVALUATION%TYPE,
      dt_interv_evaluation_tstz_in IN INTERV_EVALUATION.DT_INTERV_EVALUATION_TSTZ%TYPE DEFAULT NULL,
      id_professional_in IN INTERV_EVALUATION.ID_PROFESSIONAL%TYPE DEFAULT NULL,
      notes_in IN INTERV_EVALUATION.NOTES%TYPE DEFAULT NULL,
      flg_status_in IN INTERV_EVALUATION.FLG_STATUS%TYPE DEFAULT NULL,
      dt_cancel_tstz_in IN INTERV_EVALUATION.DT_CANCEL_TSTZ%TYPE DEFAULT NULL,
      id_prof_cancel_in IN INTERV_EVALUATION.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      id_episode_in IN INTERV_EVALUATION.ID_EPISODE%TYPE DEFAULT NULL,
      flg_temp_in IN INTERV_EVALUATION.FLG_TEMP%TYPE DEFAULT NULL,
      notes_cancel_in IN INTERV_EVALUATION.NOTES_CANCEL%TYPE DEFAULT NULL,
      id_patient_in IN INTERV_EVALUATION.ID_PATIENT%TYPE DEFAULT NULL,
      flg_type_in IN INTERV_EVALUATION.FLG_TYPE%TYPE DEFAULT NULL,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      upd (
         id_interv_evaluation_in => id_interv_evaluation_in,
         dt_interv_evaluation_tstz_in => dt_interv_evaluation_tstz_in,
         id_professional_in => id_professional_in,
         notes_in => notes_in,
         flg_status_in => flg_status_in,
         dt_cancel_tstz_in => dt_cancel_tstz_in,
         id_prof_cancel_in => id_prof_cancel_in,
         id_episode_in => id_episode_in,
         flg_temp_in => flg_temp_in,
         notes_cancel_in => notes_cancel_in,
         id_patient_in => id_patient_in,
         flg_type_in => flg_type_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
       );
      IF SQL%ROWCOUNT = 0
      THEN
         ins (
            id_interv_evaluation_in => id_interv_evaluation_in,
            dt_interv_evaluation_tstz_in => dt_interv_evaluation_tstz_in,
            id_professional_in => id_professional_in,
            notes_in => notes_in,
            flg_status_in => flg_status_in,
            dt_cancel_tstz_in => dt_cancel_tstz_in,
            id_prof_cancel_in => id_prof_cancel_in,
            id_episode_in => id_episode_in,
            flg_temp_in => flg_temp_in,
            notes_cancel_in => notes_cancel_in,
            id_patient_in => id_patient_in,
            flg_type_in => flg_type_in
            ,handle_error_in => handle_error_in
            , rows_out => rows_out
         );
      END IF;
   END upd_ins;

   PROCEDURE upd_ins (
      id_interv_evaluation_in IN INTERV_EVALUATION.ID_INTERV_EVALUATION%TYPE,
      dt_interv_evaluation_tstz_in IN INTERV_EVALUATION.DT_INTERV_EVALUATION_TSTZ%TYPE DEFAULT NULL,
      id_professional_in IN INTERV_EVALUATION.ID_PROFESSIONAL%TYPE DEFAULT NULL,
      notes_in IN INTERV_EVALUATION.NOTES%TYPE DEFAULT NULL,
      flg_status_in IN INTERV_EVALUATION.FLG_STATUS%TYPE DEFAULT NULL,
      dt_cancel_tstz_in IN INTERV_EVALUATION.DT_CANCEL_TSTZ%TYPE DEFAULT NULL,
      id_prof_cancel_in IN INTERV_EVALUATION.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      id_episode_in IN INTERV_EVALUATION.ID_EPISODE%TYPE DEFAULT NULL,
      flg_temp_in IN INTERV_EVALUATION.FLG_TEMP%TYPE DEFAULT NULL,
      notes_cancel_in IN INTERV_EVALUATION.NOTES_CANCEL%TYPE DEFAULT NULL,
      id_patient_in IN INTERV_EVALUATION.ID_PATIENT%TYPE DEFAULT NULL,
      flg_type_in IN INTERV_EVALUATION.FLG_TYPE%TYPE DEFAULT NULL,
     handle_error_in IN BOOLEAN := TRUE
      )
   IS
        rows_out TABLE_VARCHAR;
   BEGIN
      upd_ins (
      id_interv_evaluation_in,
      dt_interv_evaluation_tstz_in,
      id_professional_in,
      notes_in,
      flg_status_in,
      dt_cancel_tstz_in,
      id_prof_cancel_in,
      id_episode_in,
      flg_temp_in,
      notes_cancel_in,
      id_patient_in,
      flg_type_in,
     handle_error_in
     ,rows_out
      );
   END upd_ins;


   PROCEDURE upd (
      col_in IN INTERV_EVALUATION_tc,
      ignore_if_null_in IN BOOLEAN := TRUE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      )
   IS
      l_ID_INTERV_EVALUATION ID_INTERV_EVALUATION_cc;
      l_DT_INTERV_EVALUATION_TSTZ DT_INTERV_EVALUATION_TSTZ_cc;
      l_ID_PROFESSIONAL ID_PROFESSIONAL_cc;
      l_NOTES NOTES_cc;
      l_FLG_STATUS FLG_STATUS_cc;
      l_DT_CANCEL_TSTZ DT_CANCEL_TSTZ_cc;
      l_ID_PROF_CANCEL ID_PROF_CANCEL_cc;
      l_ID_EPISODE ID_EPISODE_cc;
      l_FLG_TEMP FLG_TEMP_cc;
      l_NOTES_CANCEL NOTES_CANCEL_cc;
      l_ID_PATIENT ID_PATIENT_cc;
      l_FLG_TYPE FLG_TYPE_cc;
   BEGIN
      FOR i IN col_in.FIRST .. col_in.LAST loop
         l_ID_INTERV_EVALUATION(i) := col_in(i).ID_INTERV_EVALUATION;
         l_DT_INTERV_EVALUATION_TSTZ(i) := col_in(i).DT_INTERV_EVALUATION_TSTZ;
         l_ID_PROFESSIONAL(i) := col_in(i).ID_PROFESSIONAL;
         l_NOTES(i) := col_in(i).NOTES;
         l_FLG_STATUS(i) := col_in(i).FLG_STATUS;
         l_DT_CANCEL_TSTZ(i) := col_in(i).DT_CANCEL_TSTZ;
         l_ID_PROF_CANCEL(i) := col_in(i).ID_PROF_CANCEL;
         l_ID_EPISODE(i) := col_in(i).ID_EPISODE;
         l_FLG_TEMP(i) := col_in(i).FLG_TEMP;
         l_NOTES_CANCEL(i) := col_in(i).NOTES_CANCEL;
         l_ID_PATIENT(i) := col_in(i).ID_PATIENT;
         l_FLG_TYPE(i) := col_in(i).FLG_TYPE;
      END LOOP;
      IF NVL (ignore_if_null_in, FALSE)
      THEN
         -- Set any columns to their current values
         -- if incoming value is NULL.
         -- Put WHEN clause on column-level triggers!
         FORALL i IN col_in.FIRST .. col_in.LAST
            UPDATE INTERV_EVALUATION SET
               DT_INTERV_EVALUATION_TSTZ = NVL (l_DT_INTERV_EVALUATION_TSTZ(i), DT_INTERV_EVALUATION_TSTZ),
               ID_PROFESSIONAL = NVL (l_ID_PROFESSIONAL(i), ID_PROFESSIONAL),
               NOTES = NVL (l_NOTES(i), NOTES),
               FLG_STATUS = NVL (l_FLG_STATUS(i), FLG_STATUS),
               DT_CANCEL_TSTZ = NVL (l_DT_CANCEL_TSTZ(i), DT_CANCEL_TSTZ),
               ID_PROF_CANCEL = NVL (l_ID_PROF_CANCEL(i), ID_PROF_CANCEL),
               ID_EPISODE = NVL (l_ID_EPISODE(i), ID_EPISODE),
               FLG_TEMP = NVL (l_FLG_TEMP(i), FLG_TEMP),
               NOTES_CANCEL = NVL (l_NOTES_CANCEL(i), NOTES_CANCEL),
               ID_PATIENT = NVL (l_ID_PATIENT(i), ID_PATIENT),
               FLG_TYPE = NVL (l_FLG_TYPE(i), FLG_TYPE)
             WHERE
                ID_INTERV_EVALUATION = l_ID_INTERV_EVALUATION(i)
         ;
      ELSE
         FORALL i IN col_in.FIRST .. col_in.LAST
            UPDATE INTERV_EVALUATION SET
               DT_INTERV_EVALUATION_TSTZ = l_DT_INTERV_EVALUATION_TSTZ(i),
               ID_PROFESSIONAL = l_ID_PROFESSIONAL(i),
               NOTES = l_NOTES(i),
               FLG_STATUS = l_FLG_STATUS(i),
               DT_CANCEL_TSTZ = l_DT_CANCEL_TSTZ(i),
               ID_PROF_CANCEL = l_ID_PROF_CANCEL(i),
               ID_EPISODE = l_ID_EPISODE(i),
               FLG_TEMP = l_FLG_TEMP(i),
               NOTES_CANCEL = l_NOTES_CANCEL(i),
               ID_PATIENT = l_ID_PATIENT(i),
               FLG_TYPE = l_FLG_TYPE(i)
             WHERE
                ID_INTERV_EVALUATION = l_ID_INTERV_EVALUATION(i)
         ;
      END IF;
   END upd;


   PROCEDURE upd (
      col_in IN INTERV_EVALUATION_tc,
      ignore_if_null_in IN BOOLEAN := TRUE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
     rows_out TABLE_VARCHAR;
   BEGIN
      upd (
      col_in ,
      ignore_if_null_in
     ,handle_error_in
     , rows_out
      );
   END upd;

   FUNCTION dynupdstr (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL)

   RETURN VARCHAR2
   IS
   BEGIN
      RETURN
         'BEGIN UPDATE INTERV_EVALUATION
             SET ' || colname_in || ' = :value
           WHERE ' || NVL (where_in, '1=1') || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;' ;
   END dynupdstr;

   FUNCTION dynupdstr_no_rows_out (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL)

   RETURN VARCHAR2
   IS
   BEGIN
      RETURN
         'UPDATE INTERV_EVALUATION
             SET ' || colname_in || ' = :value
           WHERE ' || NVL (where_in, '1=1');
   END dynupdstr_no_rows_out;























  PROCEDURE increment_onecol (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL
      , increment_value_in IN NUMBER DEFAULT 1
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN UPDATE INTERV_EVALUATION set ' || colname_in || '=' || colname_in || ' + ' || nvl(increment_value_in,1) || ' WHERE ' || NVL (where_in, '1=1') || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
      USING OUT rows_out;
   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'DUPLICATE-VALUE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_EVALUATION');
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_EVALUATION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_EVALUATION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END increment_onecol;

   PROCEDURE increment_onecol (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL
     , increment_value_in IN NUMBER DEFAULT 1
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
   rows_out table_varchar;
   BEGIN
      EXECUTE IMMEDIATE 'UPDATE INTERV_EVALUATION set ' || colname_in || '=' || colname_in || ' + ' || nvl(increment_value_in,1) || ' WHERE ' || NVL (where_in, '1=1');
   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'DUPLICATE-VALUE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_EVALUATION');
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_EVALUATION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_EVALUATION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END increment_onecol;


   -- Delete functionality


   PROCEDURE del (
      id_interv_evaluation_in IN INTERV_EVALUATION.ID_INTERV_EVALUATION%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM INTERV_EVALUATION
       WHERE
          ID_INTERV_EVALUATION = id_interv_evaluation_in
       RETURNING ROWID BULK COLLECT INTO rows_out
         ;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_EVALUATION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_EVALUATION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del;




   PROCEDURE del (
      id_interv_evaluation_in IN INTERV_EVALUATION.ID_INTERV_EVALUATION%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
rows_out TABLE_VARCHAR;
   BEGIN

del (
      id_interv_evaluation_in => id_interv_evaluation_in
     ,handle_error_in => handle_error_in
, rows_out => rows_out
      );

   END del;








   -- Delete all rows for primary key column ID_INTERV_EVALUATION
   PROCEDURE del_ID_INTERV_EVALUATION (
      id_interv_evaluation_in IN INTERV_EVALUATION.ID_INTERV_EVALUATION%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
     )
   IS
   BEGIN
      DELETE FROM INTERV_EVALUATION
       WHERE ID_INTERV_EVALUATION = id_interv_evaluation_in
      RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_EVALUATION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_EVALUATION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_ID_INTERV_EVALUATION;






   -- Delete all rows for primary key column ID_INTERV_EVALUATION
   PROCEDURE del_ID_INTERV_EVALUATION (
      id_interv_evaluation_in IN INTERV_EVALUATION.ID_INTERV_EVALUATION%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_ID_INTERV_EVALUATION (
      id_interv_evaluation_in => id_interv_evaluation_in
     ,handle_error_in => handle_error_in
, rows_out => rows_out
     );
   END del_ID_INTERV_EVALUATION;















   PROCEDURE del_IEND_EPIS_FK (
      id_episode_in IN INTERV_EVALUATION.ID_EPISODE%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM INTERV_EVALUATION
       WHERE
          ID_EPISODE = del_IEND_EPIS_FK.id_episode_in
       RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_EVALUATION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_EVALUATION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_IEND_EPIS_FK;



PROCEDURE del_IEND_EPIS_FK (
      id_episode_in IN INTERV_EVALUATION.ID_EPISODE%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_IEND_EPIS_FK (
      id_episode_in => id_episode_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END del_IEND_EPIS_FK;





   PROCEDURE del_IEND_PAT_FK (
      id_patient_in IN INTERV_EVALUATION.ID_PATIENT%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM INTERV_EVALUATION
       WHERE
          ID_PATIENT = del_IEND_PAT_FK.id_patient_in
       RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_EVALUATION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_EVALUATION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_IEND_PAT_FK;



PROCEDURE del_IEND_PAT_FK (
      id_patient_in IN INTERV_EVALUATION.ID_PATIENT%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_IEND_PAT_FK (
      id_patient_in => id_patient_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END del_IEND_PAT_FK;





   PROCEDURE del_IEND_PROF_CANC_FK (
      id_prof_cancel_in IN INTERV_EVALUATION.ID_PROF_CANCEL%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM INTERV_EVALUATION
       WHERE
          ID_PROF_CANCEL = del_IEND_PROF_CANC_FK.id_prof_cancel_in
       RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_EVALUATION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_EVALUATION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_IEND_PROF_CANC_FK;



PROCEDURE del_IEND_PROF_CANC_FK (
      id_prof_cancel_in IN INTERV_EVALUATION.ID_PROF_CANCEL%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_IEND_PROF_CANC_FK (
      id_prof_cancel_in => id_prof_cancel_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END del_IEND_PROF_CANC_FK;





   PROCEDURE del_IEND_PROF_FK (
      id_professional_in IN INTERV_EVALUATION.ID_PROFESSIONAL%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM INTERV_EVALUATION
       WHERE
          ID_PROFESSIONAL = del_IEND_PROF_FK.id_professional_in
       RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_EVALUATION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_EVALUATION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_IEND_PROF_FK;



PROCEDURE del_IEND_PROF_FK (
      id_professional_in IN INTERV_EVALUATION.ID_PROFESSIONAL%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_IEND_PROF_FK (
      id_professional_in => id_professional_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END del_IEND_PROF_FK;












   -- Deletions using dynamic SQL
   FUNCTION dyndelstr (where_in IN VARCHAR2) RETURN VARCHAR2
   IS
   BEGIN
      IF where_in IS NULL
      THEN
         RETURN 'DELETE FROM INTERV_EVALUATION';
      ELSE
         RETURN
            'DELETE FROM INTERV_EVALUATION WHERE ' || where_in;
      END IF;
   END dyndelstr;

   FUNCTION dyncoldelstr (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE)
      RETURN VARCHAR2
   IS
   BEGIN
      RETURN dyndelstr ( colname_in || ' = :value' );
   END;

   PROCEDURE del_by (
      where_clause_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyndelstr (where_clause_in);
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_EVALUATION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_EVALUATION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by;





   PROCEDURE del_by (
      where_clause_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN ' || dyndelstr (where_clause_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;' using OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_EVALUATION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_EVALUATION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by;





   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyncoldelstr (colname_in)
         USING colvalue_in;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_EVALUATION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_EVALUATION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;






   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr (colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
         USING IN colvalue_in, OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_EVALUATION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_EVALUATION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN DATE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyncoldelstr (colname_in)
         USING colvalue_in;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_EVALUATION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_EVALUATION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN DATE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr (colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
         USING IN colvalue_in, OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_EVALUATION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_EVALUATION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN TIMESTAMP WITH LOCAL TIME ZONE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyncoldelstr (colname_in)
         USING colvalue_in;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_EVALUATION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_EVALUATION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN TIMESTAMP WITH LOCAL TIME ZONE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr (colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
         USING IN colvalue_in, OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_EVALUATION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_EVALUATION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN NUMBER
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyncoldelstr (colname_in)
         USING colvalue_in;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_EVALUATION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_EVALUATION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;






   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN NUMBER
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
   BEGIN
     EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr (colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
         USING IN colvalue_in, OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_EVALUATION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_EVALUATION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   -- Initialize a record with default values for columns in the table.
   PROCEDURE initrec (
      interv_evaluation_inout IN OUT INTERV_EVALUATION%ROWTYPE
   )
   IS
   BEGIN
      interv_evaluation_inout.ID_INTERV_EVALUATION := NULL;
      interv_evaluation_inout.DT_INTERV_EVALUATION_TSTZ := NULL;
      interv_evaluation_inout.ID_PROFESSIONAL := NULL;
      interv_evaluation_inout.NOTES := NULL;
      interv_evaluation_inout.FLG_STATUS := NULL;
      interv_evaluation_inout.DT_CANCEL_TSTZ := NULL;
      interv_evaluation_inout.ID_PROF_CANCEL := NULL;
      interv_evaluation_inout.ID_EPISODE := NULL;
      interv_evaluation_inout.FLG_TEMP := NULL;
      interv_evaluation_inout.NOTES_CANCEL := NULL;
      interv_evaluation_inout.ID_PATIENT := NULL;
      interv_evaluation_inout.FLG_TYPE := NULL;
   END initrec;

   FUNCTION initrec RETURN INTERV_EVALUATION%ROWTYPE
   IS
      l_interv_evaluation INTERV_EVALUATION%ROWTYPE;
   BEGIN
      RETURN l_interv_evaluation;
   END initrec;


   FUNCTION get_data_rowid(
        rows_in IN TABLE_VARCHAR
        ) RETURN INTERV_EVALUATION_tc
   IS
        data INTERV_EVALUATION_tc;
   BEGIN
        select * bulk collect into data from INTERV_EVALUATION where rowid in (select * from table(rows_in));
        return data;
        EXCEPTION
      WHEN OTHERS THEN
        pk_alert_exceptions.raise_error (
           error_name_in => 'get_data_rowid'
           );
   END get_data_rowid;


   FUNCTION get_data_rowid_pat(
        rows_in IN TABLE_VARCHAR
        ) RETURN INTERV_EVALUATION_tc
   is
        PRAGMA AUTONOMOUS_TRANSACTION;
        data INTERV_EVALUATION_tc;
   BEGIN
        data := get_data_rowid(rows_in);
        commit;
        return data;
        EXCEPTION
      WHEN OTHERS THEN
        pk_alert_exceptions.raise_error (
           error_name_in => 'get_data_rowid'
           );
        rollback;
    END get_data_rowid_pat;

BEGIN
   NULL;
END TS_INTERV_EVALUATION;
/
