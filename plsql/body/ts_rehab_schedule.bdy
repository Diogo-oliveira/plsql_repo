/*-- Last Change Revision: $Rev: 2028371 $*/
/*-- Last Change by: $Author: mario.fernandes $*/
/*-- Date of last change: $Date: 2022-08-02 18:45:23 +0100 (ter, 02 ago 2022) $*/

CREATE OR REPLACE PACKAGE BODY ts_rehab_schedule
/*
| Generated by or retrieved from QCGU - DO NOT MODIFY!
| QCGU - "Get it right, do it fast" - www.ToadWorld.com
| QCGU Universal ID: {657BD35E-2E48-4CD4-B14E-41420B8433B3}
| Created On: Julho 14, 2010 15:41:34
| Created By: ALERT
*/
 IS

    e_null_column_value EXCEPTION;
    PRAGMA EXCEPTION_INIT(e_null_column_value, -1400);
    --
    e_existing_fky_reference EXCEPTION;
    PRAGMA EXCEPTION_INIT(e_existing_fky_reference, -2266);
    --
    e_check_constraint_failure EXCEPTION;
    PRAGMA EXCEPTION_INIT(e_check_constraint_failure, -2290);
    --
    e_no_parent_key EXCEPTION;
    PRAGMA EXCEPTION_INIT(e_no_parent_key, -2291);
    --
    e_child_record_found EXCEPTION;
    PRAGMA EXCEPTION_INIT(e_child_record_found, -2292);
    --
    e_forall_error EXCEPTION;
    PRAGMA EXCEPTION_INIT(e_forall_error, -24381);
    --
    -- Defined for backward compatibilty.
    e_integ_constraint_failure EXCEPTION;
    PRAGMA EXCEPTION_INIT(e_integ_constraint_failure, -2291);

    -- Private utilities
    PROCEDURE get_constraint_info
    (
        owner_out OUT all_constraints.owner%TYPE,
        name_out  OUT all_constraints.constraint_name%TYPE
    ) IS
        l_errm  VARCHAR2(2000) := dbms_utility.format_error_stack;
        dotloc  PLS_INTEGER;
        leftloc PLS_INTEGER;
    BEGIN
        dotloc    := instr(l_errm, '.');
        leftloc   := instr(l_errm, '(');
        owner_out := substr(l_errm, leftloc + 1, dotloc - leftloc - 1);
        name_out  := substr(l_errm, dotloc + 1, instr(l_errm, ')') - dotloc - 1);
    END get_constraint_info;
    -- Public programs

    PROCEDURE ins
    (
        id_rehab_schedule_in  IN rehab_schedule.id_rehab_schedule%TYPE,
        id_rehab_sch_need_in  IN rehab_schedule.id_rehab_sch_need%TYPE DEFAULT NULL,
        id_professional_in    IN rehab_schedule.id_professional%TYPE DEFAULT NULL,
        id_schedule_in        IN rehab_schedule.id_schedule%TYPE DEFAULT NULL,
        dt_schedule_in        IN rehab_schedule.dt_schedule%TYPE DEFAULT NULL,
        flg_status_in         IN rehab_schedule.flg_status%TYPE DEFAULT NULL,
        id_cancel_reason_in   IN rehab_schedule.id_cancel_reason%TYPE DEFAULT NULL,
        dt_cancel_in          IN rehab_schedule.dt_cancel%TYPE DEFAULT NULL,
        id_cancel_prof_in     IN rehab_schedule.id_cancel_prof%TYPE DEFAULT NULL,
        notes_in              IN rehab_schedule.notes%TYPE DEFAULT NULL,
        create_user_in        IN rehab_schedule.create_user%TYPE DEFAULT NULL,
        create_time_in        IN rehab_schedule.create_time%TYPE DEFAULT NULL,
        create_institution_in IN rehab_schedule.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN rehab_schedule.update_user%TYPE DEFAULT NULL,
        update_time_in        IN rehab_schedule.update_time%TYPE DEFAULT NULL,
        update_institution_in IN rehab_schedule.update_institution%TYPE DEFAULT NULL,
        handle_error_in       IN BOOLEAN := TRUE,
        rows_out              OUT table_varchar
    ) IS
    BEGIN
    
        INSERT INTO rehab_schedule
            (id_rehab_schedule,
             id_rehab_sch_need,
             id_professional,
             id_schedule,
             dt_schedule,
             flg_status,
             id_cancel_reason,
             dt_cancel,
             id_cancel_prof,
             notes,
             create_user,
             create_time,
             create_institution,
             update_user,
             update_time,
             update_institution)
        VALUES
            (id_rehab_schedule_in,
             id_rehab_sch_need_in,
             id_professional_in,
             id_schedule_in,
             dt_schedule_in,
             flg_status_in,
             id_cancel_reason_in,
             dt_cancel_in,
             id_cancel_prof_in,
             notes_in,
             create_user_in,
             create_time_in,
             create_institution_in,
             update_user_in,
             update_time_in,
             update_institution_in)
        RETURNING ROWID BULK COLLECT INTO rows_out;
    EXCEPTION
        WHEN dup_val_on_index THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner           all_constraints.owner%TYPE;
                    l_name            all_constraints.constraint_name%TYPE;
                    l_err_instance_id PLS_INTEGER;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF FALSE
                    THEN
                        NULL; -- Placeholder in case no unique indexes
                    ELSE
                        pk_alert_exceptions.raise_error(error_name_in => 'DUPLICATE-VALUE',
                                                        name1_in      => 'OWNER',
                                                        value1_in     => l_owner,
                                                        name2_in      => 'CONSTRAINT_NAME',
                                                        value2_in     => l_name,
                                                        name3_in      => 'TABLE_NAME',
                                                        value3_in     => 'REHAB_SCHEDULE');
                    END IF;
                END;
            END IF;
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'REHAB_SCHEDULE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'REHAB_SCHEDULE');
                    IF l_name = 'RSC_PL_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_PROFESSIONAL',
                                                        value_in           => id_professional_in);
                    END IF;
                    IF l_name = 'RSC_PL_FK2'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_CANCEL_PROF',
                                                        value_in           => id_cancel_prof_in);
                    END IF;
                    IF l_name = 'RSC_RSD_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_REHAB_SCH_NEED',
                                                        value_in           => id_rehab_sch_need_in);
                    END IF;
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
        WHEN e_null_column_value THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    v_errm    VARCHAR2(2000) := dbms_utility.format_error_stack;
                    dot1loc   INTEGER;
                    dot2loc   INTEGER;
                    parenloc  INTEGER;
                    c_owner   all_constraints.owner%TYPE;
                    c_tabname all_tables.table_name%TYPE;
                    c_colname all_tab_columns.column_name%TYPE;
                BEGIN
                    dot1loc   := instr(v_errm, '.', 1, 1);
                    dot2loc   := instr(v_errm, '.', 1, 2);
                    parenloc  := instr(v_errm, '(');
                    c_owner   := substr(v_errm, parenloc + 1, dot1loc - parenloc - 1);
                    c_tabname := substr(v_errm, dot1loc + 1, dot2loc - dot1loc - 1);
                    c_colname := substr(v_errm, dot2loc + 1, instr(v_errm, ')') - dot2loc - 1);
                
                    pk_alert_exceptions.raise_error(error_name_in => 'COLUMN-CANNOT-BE-NULL',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => c_owner,
                                                    name2_in      => 'TABLE_NAME',
                                                    value2_in     => c_tabname,
                                                    name3_in      => 'COLUMN_NAME',
                                                    value3_in     => c_colname);
                END;
            END IF;
    END ins;

    PROCEDURE ins
    (
        id_rehab_schedule_in  IN rehab_schedule.id_rehab_schedule%TYPE,
        id_rehab_sch_need_in  IN rehab_schedule.id_rehab_sch_need%TYPE DEFAULT NULL,
        id_professional_in    IN rehab_schedule.id_professional%TYPE DEFAULT NULL,
        id_schedule_in        IN rehab_schedule.id_schedule%TYPE DEFAULT NULL,
        dt_schedule_in        IN rehab_schedule.dt_schedule%TYPE DEFAULT NULL,
        flg_status_in         IN rehab_schedule.flg_status%TYPE DEFAULT NULL,
        id_cancel_reason_in   IN rehab_schedule.id_cancel_reason%TYPE DEFAULT NULL,
        dt_cancel_in          IN rehab_schedule.dt_cancel%TYPE DEFAULT NULL,
        id_cancel_prof_in     IN rehab_schedule.id_cancel_prof%TYPE DEFAULT NULL,
        notes_in              IN rehab_schedule.notes%TYPE DEFAULT NULL,
        create_user_in        IN rehab_schedule.create_user%TYPE DEFAULT NULL,
        create_time_in        IN rehab_schedule.create_time%TYPE DEFAULT NULL,
        create_institution_in IN rehab_schedule.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN rehab_schedule.update_user%TYPE DEFAULT NULL,
        update_time_in        IN rehab_schedule.update_time%TYPE DEFAULT NULL,
        update_institution_in IN rehab_schedule.update_institution%TYPE DEFAULT NULL,
        handle_error_in       IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
    
        ins(id_rehab_schedule_in  => id_rehab_schedule_in,
            id_rehab_sch_need_in  => id_rehab_sch_need_in,
            id_professional_in    => id_professional_in,
            id_schedule_in        => id_schedule_in,
            dt_schedule_in        => dt_schedule_in,
            flg_status_in         => flg_status_in,
            id_cancel_reason_in   => id_cancel_reason_in,
            dt_cancel_in          => dt_cancel_in,
            id_cancel_prof_in     => id_cancel_prof_in,
            notes_in              => notes_in,
            create_user_in        => create_user_in,
            create_time_in        => create_time_in,
            create_institution_in => create_institution_in,
            update_user_in        => update_user_in,
            update_time_in        => update_time_in,
            update_institution_in => update_institution_in,
            handle_error_in       => handle_error_in,
            rows_out              => rows_out);
    END ins;

    PROCEDURE ins
    (
        rec_in          IN rehab_schedule%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) IS
        l_rec rehab_schedule%ROWTYPE := rec_in;
    BEGIN
        IF gen_pky_in
        THEN
            l_rec.id_rehab_schedule := next_key(sequence_in);
        END IF;
        ins(id_rehab_schedule_in  => l_rec.id_rehab_schedule,
            id_rehab_sch_need_in  => l_rec.id_rehab_sch_need,
            id_professional_in    => l_rec.id_professional,
            id_schedule_in        => l_rec.id_schedule,
            dt_schedule_in        => l_rec.dt_schedule,
            flg_status_in         => l_rec.flg_status,
            id_cancel_reason_in   => l_rec.id_cancel_reason,
            dt_cancel_in          => l_rec.dt_cancel,
            id_cancel_prof_in     => l_rec.id_cancel_prof,
            notes_in              => l_rec.notes,
            create_user_in        => l_rec.create_user,
            create_time_in        => l_rec.create_time,
            create_institution_in => l_rec.create_institution,
            update_user_in        => l_rec.update_user,
            update_time_in        => l_rec.update_time,
            update_institution_in => l_rec.update_institution,
            handle_error_in       => handle_error_in,
            rows_out              => rows_out);
    END ins;

    PROCEDURE ins
    (
        rec_in          IN rehab_schedule%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
    
        ins(rec_in          => rec_in,
            gen_pky_in      => gen_pky_in,
            sequence_in     => sequence_in,
            handle_error_in => handle_error_in,
            rows_out        => rows_out);
    
    END ins;

    FUNCTION next_key(sequence_in IN VARCHAR2 := NULL) RETURN rehab_schedule.id_rehab_schedule%TYPE
    
     IS
        retval rehab_schedule.id_rehab_schedule%TYPE;
    
    BEGIN
        IF sequence_in IS NULL
        THEN
            SELECT seq_rehab_schedule.NEXTVAL
              INTO retval
              FROM dual;
        ELSE
            EXECUTE IMMEDIATE 'SELECT ' || sequence_in || '.NEXTVAL FROM dual'
                INTO retval;
        END IF;
        RETURN retval;
    EXCEPTION
        WHEN OTHERS THEN
            pk_alert_exceptions.raise_error(error_name_in => 'SEQUENCE-GENERATION-FAILURE',
                                            name1_in      => 'SEQUENCE',
                                            value1_in     => nvl(sequence_in, 'seq_REHAB_SCHEDULE'));
    END next_key;

    PROCEDURE ins
    (
        id_rehab_sch_need_in  IN rehab_schedule.id_rehab_sch_need%TYPE DEFAULT NULL,
        id_professional_in    IN rehab_schedule.id_professional%TYPE DEFAULT NULL,
        id_schedule_in        IN rehab_schedule.id_schedule%TYPE DEFAULT NULL,
        dt_schedule_in        IN rehab_schedule.dt_schedule%TYPE DEFAULT NULL,
        flg_status_in         IN rehab_schedule.flg_status%TYPE DEFAULT NULL,
        id_cancel_reason_in   IN rehab_schedule.id_cancel_reason%TYPE DEFAULT NULL,
        dt_cancel_in          IN rehab_schedule.dt_cancel%TYPE DEFAULT NULL,
        id_cancel_prof_in     IN rehab_schedule.id_cancel_prof%TYPE DEFAULT NULL,
        notes_in              IN rehab_schedule.notes%TYPE DEFAULT NULL,
        create_user_in        IN rehab_schedule.create_user%TYPE DEFAULT NULL,
        create_time_in        IN rehab_schedule.create_time%TYPE DEFAULT NULL,
        create_institution_in IN rehab_schedule.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN rehab_schedule.update_user%TYPE DEFAULT NULL,
        update_time_in        IN rehab_schedule.update_time%TYPE DEFAULT NULL,
        update_institution_in IN rehab_schedule.update_institution%TYPE DEFAULT NULL,
        id_rehab_schedule_out IN OUT rehab_schedule.id_rehab_schedule%TYPE,
        handle_error_in       IN BOOLEAN := TRUE,
        rows_out              OUT table_varchar
    ) IS
        l_pky rehab_schedule.id_rehab_schedule%TYPE := next_key;
    BEGIN
        ins(id_rehab_schedule_in  => l_pky,
            id_rehab_sch_need_in  => id_rehab_sch_need_in,
            id_professional_in    => id_professional_in,
            id_schedule_in        => id_schedule_in,
            dt_schedule_in        => dt_schedule_in,
            flg_status_in         => flg_status_in,
            id_cancel_reason_in   => id_cancel_reason_in,
            dt_cancel_in          => dt_cancel_in,
            id_cancel_prof_in     => id_cancel_prof_in,
            notes_in              => notes_in,
            create_user_in        => create_user_in,
            create_time_in        => create_time_in,
            create_institution_in => create_institution_in,
            update_user_in        => update_user_in,
            update_time_in        => update_time_in,
            update_institution_in => update_institution_in,
            handle_error_in       => handle_error_in,
            rows_out              => rows_out);
        id_rehab_schedule_out := l_pky;
    END ins;

    PROCEDURE ins
    (
        id_rehab_sch_need_in  IN rehab_schedule.id_rehab_sch_need%TYPE DEFAULT NULL,
        id_professional_in    IN rehab_schedule.id_professional%TYPE DEFAULT NULL,
        id_schedule_in        IN rehab_schedule.id_schedule%TYPE DEFAULT NULL,
        dt_schedule_in        IN rehab_schedule.dt_schedule%TYPE DEFAULT NULL,
        flg_status_in         IN rehab_schedule.flg_status%TYPE DEFAULT NULL,
        id_cancel_reason_in   IN rehab_schedule.id_cancel_reason%TYPE DEFAULT NULL,
        dt_cancel_in          IN rehab_schedule.dt_cancel%TYPE DEFAULT NULL,
        id_cancel_prof_in     IN rehab_schedule.id_cancel_prof%TYPE DEFAULT NULL,
        notes_in              IN rehab_schedule.notes%TYPE DEFAULT NULL,
        create_user_in        IN rehab_schedule.create_user%TYPE DEFAULT NULL,
        create_time_in        IN rehab_schedule.create_time%TYPE DEFAULT NULL,
        create_institution_in IN rehab_schedule.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN rehab_schedule.update_user%TYPE DEFAULT NULL,
        update_time_in        IN rehab_schedule.update_time%TYPE DEFAULT NULL,
        update_institution_in IN rehab_schedule.update_institution%TYPE DEFAULT NULL,
        id_rehab_schedule_out IN OUT rehab_schedule.id_rehab_schedule%TYPE,
        handle_error_in       IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        ins(id_rehab_sch_need_in  => id_rehab_sch_need_in,
            id_professional_in    => id_professional_in,
            id_schedule_in        => id_schedule_in,
            dt_schedule_in        => dt_schedule_in,
            flg_status_in         => flg_status_in,
            id_cancel_reason_in   => id_cancel_reason_in,
            dt_cancel_in          => dt_cancel_in,
            id_cancel_prof_in     => id_cancel_prof_in,
            notes_in              => notes_in,
            create_user_in        => create_user_in,
            create_time_in        => create_time_in,
            create_institution_in => create_institution_in,
            update_user_in        => update_user_in,
            update_time_in        => update_time_in,
            update_institution_in => update_institution_in,
            id_rehab_schedule_out => id_rehab_schedule_out,
            handle_error_in       => handle_error_in,
            rows_out              => rows_out);
    END ins;

    FUNCTION ins
    (
        id_rehab_sch_need_in  IN rehab_schedule.id_rehab_sch_need%TYPE DEFAULT NULL,
        id_professional_in    IN rehab_schedule.id_professional%TYPE DEFAULT NULL,
        id_schedule_in        IN rehab_schedule.id_schedule%TYPE DEFAULT NULL,
        dt_schedule_in        IN rehab_schedule.dt_schedule%TYPE DEFAULT NULL,
        flg_status_in         IN rehab_schedule.flg_status%TYPE DEFAULT NULL,
        id_cancel_reason_in   IN rehab_schedule.id_cancel_reason%TYPE DEFAULT NULL,
        dt_cancel_in          IN rehab_schedule.dt_cancel%TYPE DEFAULT NULL,
        id_cancel_prof_in     IN rehab_schedule.id_cancel_prof%TYPE DEFAULT NULL,
        notes_in              IN rehab_schedule.notes%TYPE DEFAULT NULL,
        create_user_in        IN rehab_schedule.create_user%TYPE DEFAULT NULL,
        create_time_in        IN rehab_schedule.create_time%TYPE DEFAULT NULL,
        create_institution_in IN rehab_schedule.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN rehab_schedule.update_user%TYPE DEFAULT NULL,
        update_time_in        IN rehab_schedule.update_time%TYPE DEFAULT NULL,
        update_institution_in IN rehab_schedule.update_institution%TYPE DEFAULT NULL,
        handle_error_in       IN BOOLEAN := TRUE,
        rows_out              OUT table_varchar
    ) RETURN rehab_schedule.id_rehab_schedule%TYPE IS
        l_pky rehab_schedule.id_rehab_schedule%TYPE := next_key;
    BEGIN
        ins(id_rehab_schedule_in  => l_pky,
            id_rehab_sch_need_in  => id_rehab_sch_need_in,
            id_professional_in    => id_professional_in,
            id_schedule_in        => id_schedule_in,
            dt_schedule_in        => dt_schedule_in,
            flg_status_in         => flg_status_in,
            id_cancel_reason_in   => id_cancel_reason_in,
            dt_cancel_in          => dt_cancel_in,
            id_cancel_prof_in     => id_cancel_prof_in,
            notes_in              => notes_in,
            create_user_in        => create_user_in,
            create_time_in        => create_time_in,
            create_institution_in => create_institution_in,
            update_user_in        => update_user_in,
            update_time_in        => update_time_in,
            update_institution_in => update_institution_in,
            handle_error_in       => handle_error_in,
            rows_out              => rows_out);
        RETURN l_pky;
    END ins;

    FUNCTION ins
    (
        id_rehab_sch_need_in  IN rehab_schedule.id_rehab_sch_need%TYPE DEFAULT NULL,
        id_professional_in    IN rehab_schedule.id_professional%TYPE DEFAULT NULL,
        id_schedule_in        IN rehab_schedule.id_schedule%TYPE DEFAULT NULL,
        dt_schedule_in        IN rehab_schedule.dt_schedule%TYPE DEFAULT NULL,
        flg_status_in         IN rehab_schedule.flg_status%TYPE DEFAULT NULL,
        id_cancel_reason_in   IN rehab_schedule.id_cancel_reason%TYPE DEFAULT NULL,
        dt_cancel_in          IN rehab_schedule.dt_cancel%TYPE DEFAULT NULL,
        id_cancel_prof_in     IN rehab_schedule.id_cancel_prof%TYPE DEFAULT NULL,
        notes_in              IN rehab_schedule.notes%TYPE DEFAULT NULL,
        create_user_in        IN rehab_schedule.create_user%TYPE DEFAULT NULL,
        create_time_in        IN rehab_schedule.create_time%TYPE DEFAULT NULL,
        create_institution_in IN rehab_schedule.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN rehab_schedule.update_user%TYPE DEFAULT NULL,
        update_time_in        IN rehab_schedule.update_time%TYPE DEFAULT NULL,
        update_institution_in IN rehab_schedule.update_institution%TYPE DEFAULT NULL,
        handle_error_in       IN BOOLEAN := TRUE
    ) RETURN rehab_schedule.id_rehab_schedule%TYPE IS
        l_pky    rehab_schedule.id_rehab_schedule%TYPE := next_key;
        rows_out table_varchar;
    BEGIN
        ins(id_rehab_schedule_in  => l_pky,
            id_rehab_sch_need_in  => id_rehab_sch_need_in,
            id_professional_in    => id_professional_in,
            id_schedule_in        => id_schedule_in,
            dt_schedule_in        => dt_schedule_in,
            flg_status_in         => flg_status_in,
            id_cancel_reason_in   => id_cancel_reason_in,
            dt_cancel_in          => dt_cancel_in,
            id_cancel_prof_in     => id_cancel_prof_in,
            notes_in              => notes_in,
            create_user_in        => create_user_in,
            create_time_in        => create_time_in,
            create_institution_in => create_institution_in,
            update_user_in        => update_user_in,
            update_time_in        => update_time_in,
            update_institution_in => update_institution_in,
            handle_error_in       => handle_error_in,
            rows_out              => rows_out);
        RETURN l_pky;
    END ins;

    PROCEDURE ins
    (
        id_rehab_sch_need_in  IN rehab_schedule.id_rehab_sch_need%TYPE DEFAULT NULL,
        id_professional_in    IN rehab_schedule.id_professional%TYPE DEFAULT NULL,
        id_schedule_in        IN rehab_schedule.id_schedule%TYPE DEFAULT NULL,
        dt_schedule_in        IN rehab_schedule.dt_schedule%TYPE DEFAULT NULL,
        flg_status_in         IN rehab_schedule.flg_status%TYPE DEFAULT NULL,
        id_cancel_reason_in   IN rehab_schedule.id_cancel_reason%TYPE DEFAULT NULL,
        dt_cancel_in          IN rehab_schedule.dt_cancel%TYPE DEFAULT NULL,
        id_cancel_prof_in     IN rehab_schedule.id_cancel_prof%TYPE DEFAULT NULL,
        notes_in              IN rehab_schedule.notes%TYPE DEFAULT NULL,
        create_user_in        IN rehab_schedule.create_user%TYPE DEFAULT NULL,
        create_time_in        IN rehab_schedule.create_time%TYPE DEFAULT NULL,
        create_institution_in IN rehab_schedule.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN rehab_schedule.update_user%TYPE DEFAULT NULL,
        update_time_in        IN rehab_schedule.update_time%TYPE DEFAULT NULL,
        update_institution_in IN rehab_schedule.update_institution%TYPE DEFAULT NULL,
        handle_error_in       IN BOOLEAN := TRUE,
        rows_out              OUT table_varchar
    ) IS
        l_pky rehab_schedule.id_rehab_schedule%TYPE := next_key;
    BEGIN
        ins(id_rehab_schedule_in  => l_pky,
            id_rehab_sch_need_in  => id_rehab_sch_need_in,
            id_professional_in    => id_professional_in,
            id_schedule_in        => id_schedule_in,
            dt_schedule_in        => dt_schedule_in,
            flg_status_in         => flg_status_in,
            id_cancel_reason_in   => id_cancel_reason_in,
            dt_cancel_in          => dt_cancel_in,
            id_cancel_prof_in     => id_cancel_prof_in,
            notes_in              => notes_in,
            create_user_in        => create_user_in,
            create_time_in        => create_time_in,
            create_institution_in => create_institution_in,
            update_user_in        => update_user_in,
            update_time_in        => update_time_in,
            update_institution_in => update_institution_in,
            handle_error_in       => handle_error_in,
            rows_out              => rows_out);
    END ins;

    PROCEDURE ins
    (
        id_rehab_sch_need_in  IN rehab_schedule.id_rehab_sch_need%TYPE DEFAULT NULL,
        id_professional_in    IN rehab_schedule.id_professional%TYPE DEFAULT NULL,
        id_schedule_in        IN rehab_schedule.id_schedule%TYPE DEFAULT NULL,
        dt_schedule_in        IN rehab_schedule.dt_schedule%TYPE DEFAULT NULL,
        flg_status_in         IN rehab_schedule.flg_status%TYPE DEFAULT NULL,
        id_cancel_reason_in   IN rehab_schedule.id_cancel_reason%TYPE DEFAULT NULL,
        dt_cancel_in          IN rehab_schedule.dt_cancel%TYPE DEFAULT NULL,
        id_cancel_prof_in     IN rehab_schedule.id_cancel_prof%TYPE DEFAULT NULL,
        notes_in              IN rehab_schedule.notes%TYPE DEFAULT NULL,
        create_user_in        IN rehab_schedule.create_user%TYPE DEFAULT NULL,
        create_time_in        IN rehab_schedule.create_time%TYPE DEFAULT NULL,
        create_institution_in IN rehab_schedule.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN rehab_schedule.update_user%TYPE DEFAULT NULL,
        update_time_in        IN rehab_schedule.update_time%TYPE DEFAULT NULL,
        update_institution_in IN rehab_schedule.update_institution%TYPE DEFAULT NULL,
        handle_error_in       IN BOOLEAN := TRUE
    ) IS
        l_pky    rehab_schedule.id_rehab_schedule%TYPE := next_key;
        rows_out table_varchar;
    BEGIN
        ins(id_rehab_schedule_in  => l_pky,
            id_rehab_sch_need_in  => id_rehab_sch_need_in,
            id_professional_in    => id_professional_in,
            id_schedule_in        => id_schedule_in,
            dt_schedule_in        => dt_schedule_in,
            flg_status_in         => flg_status_in,
            id_cancel_reason_in   => id_cancel_reason_in,
            dt_cancel_in          => dt_cancel_in,
            id_cancel_prof_in     => id_cancel_prof_in,
            notes_in              => notes_in,
            create_user_in        => create_user_in,
            create_time_in        => create_time_in,
            create_institution_in => create_institution_in,
            update_user_in        => update_user_in,
            update_time_in        => update_time_in,
            update_institution_in => update_institution_in,
            handle_error_in       => handle_error_in,
            rows_out              => rows_out);
    END ins;

    PROCEDURE ins
    (
        rows_in         IN rehab_schedule_tc,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) IS
    BEGIN
        IF rows_in.COUNT = 0
        THEN
            NULL;
        ELSE
            FORALL indx IN rows_in.FIRST .. rows_in.LAST SAVE EXCEPTIONS
                INSERT INTO rehab_schedule
                VALUES rows_in
                    (indx)
                RETURNING ROWID BULK COLLECT INTO rows_out;
        END IF;
    EXCEPTION
        WHEN e_forall_error THEN
            -- In Oracle9i and above, SAVE EXCEPTIONS will direct control
            -- here if any error occurs. We can then save all the error
            -- information out to the error instance.
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                <<bulk_handler>>
                DECLARE
                    l_err_instance_id NUMBER;
                BEGIN
                    -- For each error, write to the log.
                    FOR indx IN 1 .. SQL%BULK_EXCEPTIONS.COUNT
                    LOOP
                        pk_alert_exceptions.register_error(error_name_in       => 'FORALL-INSERT-FAILURE',
                                                           err_instance_id_out => l_err_instance_id);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'BINDING_ROW_' || indx,
                                                        value_in           => SQL%BULK_EXCEPTIONS(indx).ERROR_INDEX,
                                                        validate_in        => FALSE);
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'ERROR_AT_ROW_' || indx,
                                                        value_in           => SQL%BULK_EXCEPTIONS(indx).ERROR_CODE,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'ID_REHAB_SCHEDULE _' || indx,
                                                        value_in           => rows_in(indx).id_rehab_schedule,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'ID_REHAB_SCH_NEED _' || indx,
                                                        value_in           => rows_in(indx).id_rehab_sch_need,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'ID_PROFESSIONAL _' || indx,
                                                        value_in           => rows_in(indx).id_professional,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'ID_SCHEDULE _' || indx,
                                                        value_in           => rows_in(indx).id_schedule,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'DT_SCHEDULE _' || indx,
                                                        value_in           => rows_in(indx).dt_schedule,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'FLG_STATUS _' || indx,
                                                        value_in           => rows_in(indx).flg_status,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'ID_CANCEL_REASON _' || indx,
                                                        value_in           => rows_in(indx).id_cancel_reason,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'DT_CANCEL _' || indx,
                                                        value_in           => rows_in(indx).dt_cancel,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'ID_CANCEL_PROF _' || indx,
                                                        value_in           => rows_in(indx).id_cancel_prof,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'NOTES _' || indx,
                                                        value_in           => rows_in(indx).notes,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'CREATE_USER _' || indx,
                                                        value_in           => rows_in(indx).create_user,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'CREATE_TIME _' || indx,
                                                        value_in           => rows_in(indx).create_time,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'CREATE_INSTITUTION _' || indx,
                                                        value_in           => rows_in(indx).create_institution,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'UPDATE_USER _' || indx,
                                                        value_in           => rows_in(indx).update_user,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'UPDATE_TIME _' || indx,
                                                        value_in           => rows_in(indx).update_time,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'UPDATE_INSTITUTION _' || indx,
                                                        value_in           => rows_in(indx).update_institution,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_err_instance_id);
                    END LOOP;
                END bulk_handler;
            END IF;
        WHEN OTHERS THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                pk_alert_exceptions.raise_error(error_name_in => 'FORALL-INSERT-FAILURE',
                                                name1_in      => 'TABLE_NAME',
                                                value1_in     => 'REHAB_SCHEDULE',
                                                name2_in      => 'ROW_COUNT',
                                                value2_in     => rows_in.COUNT);
            END IF;
    END ins;

    PROCEDURE ins
    (
        rows_in         IN rehab_schedule_tc,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        ins(rows_in => rows_in, handle_error_in => handle_error_in, rows_out => rows_out);
    END ins;

    PROCEDURE upd
    (
        id_rehab_schedule_in   IN rehab_schedule.id_rehab_schedule%TYPE,
        id_rehab_sch_need_in   IN rehab_schedule.id_rehab_sch_need%TYPE DEFAULT NULL,
        id_rehab_sch_need_nin  IN BOOLEAN := TRUE,
        id_professional_in     IN rehab_schedule.id_professional%TYPE DEFAULT NULL,
        id_professional_nin    IN BOOLEAN := TRUE,
        id_schedule_in         IN rehab_schedule.id_schedule%TYPE DEFAULT NULL,
        id_schedule_nin        IN BOOLEAN := TRUE,
        dt_schedule_in         IN rehab_schedule.dt_schedule%TYPE DEFAULT NULL,
        dt_schedule_nin        IN BOOLEAN := TRUE,
        flg_status_in          IN rehab_schedule.flg_status%TYPE DEFAULT NULL,
        flg_status_nin         IN BOOLEAN := TRUE,
        id_cancel_reason_in    IN rehab_schedule.id_cancel_reason%TYPE DEFAULT NULL,
        id_cancel_reason_nin   IN BOOLEAN := TRUE,
        dt_cancel_in           IN rehab_schedule.dt_cancel%TYPE DEFAULT NULL,
        dt_cancel_nin          IN BOOLEAN := TRUE,
        id_cancel_prof_in      IN rehab_schedule.id_cancel_prof%TYPE DEFAULT NULL,
        id_cancel_prof_nin     IN BOOLEAN := TRUE,
        notes_in               IN rehab_schedule.notes%TYPE DEFAULT NULL,
        notes_nin              IN BOOLEAN := TRUE,
        create_user_in         IN rehab_schedule.create_user%TYPE DEFAULT NULL,
        create_user_nin        IN BOOLEAN := TRUE,
        create_time_in         IN rehab_schedule.create_time%TYPE DEFAULT NULL,
        create_time_nin        IN BOOLEAN := TRUE,
        create_institution_in  IN rehab_schedule.create_institution%TYPE DEFAULT NULL,
        create_institution_nin IN BOOLEAN := TRUE,
        update_user_in         IN rehab_schedule.update_user%TYPE DEFAULT NULL,
        update_user_nin        IN BOOLEAN := TRUE,
        update_time_in         IN rehab_schedule.update_time%TYPE DEFAULT NULL,
        update_time_nin        IN BOOLEAN := TRUE,
        update_institution_in  IN rehab_schedule.update_institution%TYPE DEFAULT NULL,
        update_institution_nin IN BOOLEAN := TRUE,
        handle_error_in        IN BOOLEAN := TRUE,
        rows_out               IN OUT table_varchar
    ) IS
        l_rows_out             table_varchar;
        l_id_rehab_sch_need_n  NUMBER(1);
        l_id_professional_n    NUMBER(1);
        l_id_schedule_n        NUMBER(1);
        l_dt_schedule_n        NUMBER(1);
        l_flg_status_n         NUMBER(1);
        l_id_cancel_reason_n   NUMBER(1);
        l_dt_cancel_n          NUMBER(1);
        l_id_cancel_prof_n     NUMBER(1);
        l_notes_n              NUMBER(1);
        l_create_user_n        NUMBER(1);
        l_create_time_n        NUMBER(1);
        l_create_institution_n NUMBER(1);
        l_update_user_n        NUMBER(1);
        l_update_time_n        NUMBER(1);
        l_update_institution_n NUMBER(1);
    BEGIN
    
        l_id_rehab_sch_need_n  := sys.diutil.bool_to_int(id_rehab_sch_need_nin);
        l_id_professional_n    := sys.diutil.bool_to_int(id_professional_nin);
        l_id_schedule_n        := sys.diutil.bool_to_int(id_schedule_nin);
        l_dt_schedule_n        := sys.diutil.bool_to_int(dt_schedule_nin);
        l_flg_status_n         := sys.diutil.bool_to_int(flg_status_nin);
        l_id_cancel_reason_n   := sys.diutil.bool_to_int(id_cancel_reason_nin);
        l_dt_cancel_n          := sys.diutil.bool_to_int(dt_cancel_nin);
        l_id_cancel_prof_n     := sys.diutil.bool_to_int(id_cancel_prof_nin);
        l_notes_n              := sys.diutil.bool_to_int(notes_nin);
        l_create_user_n        := sys.diutil.bool_to_int(create_user_nin);
        l_create_time_n        := sys.diutil.bool_to_int(create_time_nin);
        l_create_institution_n := sys.diutil.bool_to_int(create_institution_nin);
        l_update_user_n        := sys.diutil.bool_to_int(update_user_nin);
        l_update_time_n        := sys.diutil.bool_to_int(update_time_nin);
        l_update_institution_n := sys.diutil.bool_to_int(update_institution_nin);
    
        UPDATE rehab_schedule
           SET id_rehab_sch_need  = decode(l_id_rehab_sch_need_n,
                                           0,
                                           id_rehab_sch_need_in,
                                           nvl(id_rehab_sch_need_in, id_rehab_sch_need)),
               id_professional    = decode(l_id_professional_n,
                                           0,
                                           id_professional_in,
                                           nvl(id_professional_in, id_professional)),
               id_schedule        = decode(l_id_schedule_n, 0, id_schedule_in, nvl(id_schedule_in, id_schedule)),
               dt_schedule        = decode(l_dt_schedule_n, 0, dt_schedule_in, nvl(dt_schedule_in, dt_schedule)),
               flg_status         = decode(l_flg_status_n, 0, flg_status_in, nvl(flg_status_in, flg_status)),
               id_cancel_reason   = decode(l_id_cancel_reason_n,
                                           0,
                                           id_cancel_reason_in,
                                           nvl(id_cancel_reason_in, id_cancel_reason)),
               dt_cancel          = decode(l_dt_cancel_n, 0, dt_cancel_in, nvl(dt_cancel_in, dt_cancel)),
               id_cancel_prof     = decode(l_id_cancel_prof_n,
                                           0,
                                           id_cancel_prof_in,
                                           nvl(id_cancel_prof_in, id_cancel_prof)),
               notes              = decode(l_notes_n, 0, notes_in, nvl(notes_in, notes)),
               create_user        = decode(l_create_user_n, 0, create_user_in, nvl(create_user_in, create_user)),
               create_time        = decode(l_create_time_n, 0, create_time_in, nvl(create_time_in, create_time)),
               create_institution = decode(l_create_institution_n,
                                           0,
                                           create_institution_in,
                                           nvl(create_institution_in, create_institution)),
               update_user        = decode(l_update_user_n, 0, update_user_in, nvl(update_user_in, update_user)),
               update_time        = decode(l_update_time_n, 0, update_time_in, nvl(update_time_in, update_time)),
               update_institution = decode(l_update_institution_n,
                                           0,
                                           update_institution_in,
                                           nvl(update_institution_in, update_institution))
         WHERE id_rehab_schedule = id_rehab_schedule_in
        RETURNING ROWID BULK COLLECT INTO l_rows_out;
    
        IF (rows_out IS NULL)
        THEN
            rows_out := table_varchar();
        END IF;
    
        rows_out := rows_out MULTISET UNION DISTINCT l_rows_out;
    
    EXCEPTION
        WHEN dup_val_on_index THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner           all_constraints.owner%TYPE;
                    l_name            all_constraints.constraint_name%TYPE;
                    l_err_instance_id PLS_INTEGER;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF FALSE
                    THEN
                        NULL; -- Placeholder in case no unique indexes
                    ELSE
                        pk_alert_exceptions.raise_error(error_name_in => 'DUPLICATE-VALUE',
                                                        name1_in      => 'OWNER',
                                                        value1_in     => l_owner,
                                                        name2_in      => 'CONSTRAINT_NAME',
                                                        value2_in     => l_name,
                                                        name3_in      => 'TABLE_NAME',
                                                        value3_in     => 'REHAB_SCHEDULE');
                    END IF;
                END;
            END IF;
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'REHAB_SCHEDULE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'REHAB_SCHEDULE');
                    IF l_name = 'RSC_PL_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_PROFESSIONAL',
                                                        value_in           => id_professional_in);
                    END IF;
                    IF l_name = 'RSC_PL_FK2'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_CANCEL_PROF',
                                                        value_in           => id_cancel_prof_in);
                    END IF;
                    IF l_name = 'RSC_RSD_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_REHAB_SCH_NEED',
                                                        value_in           => id_rehab_sch_need_in);
                    END IF;
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
        WHEN e_null_column_value THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    v_errm    VARCHAR2(2000) := dbms_utility.format_error_stack;
                    dot1loc   INTEGER;
                    dot2loc   INTEGER;
                    parenloc  INTEGER;
                    c_owner   all_constraints.owner%TYPE;
                    c_tabname all_tables.table_name%TYPE;
                    c_colname all_tab_columns.column_name%TYPE;
                BEGIN
                    dot1loc   := instr(v_errm, '.', 1, 1);
                    dot2loc   := instr(v_errm, '.', 1, 2);
                    parenloc  := instr(v_errm, '(');
                    c_owner   := substr(v_errm, parenloc + 1, dot1loc - parenloc - 1);
                    c_tabname := substr(v_errm, dot1loc + 1, dot2loc - dot1loc - 1);
                    c_colname := substr(v_errm, dot2loc + 1, instr(v_errm, ')') - dot2loc - 1);
                
                    pk_alert_exceptions.raise_error(error_name_in => 'COLUMN-CANNOT-BE-NULL',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => c_owner,
                                                    name2_in      => 'TABLE_NAME',
                                                    value2_in     => c_tabname,
                                                    name3_in      => 'COLUMN_NAME',
                                                    value3_in     => c_colname);
                END;
            END IF;
    END upd;

    PROCEDURE upd
    (
        id_rehab_schedule_in   IN rehab_schedule.id_rehab_schedule%TYPE,
        id_rehab_sch_need_in   IN rehab_schedule.id_rehab_sch_need%TYPE DEFAULT NULL,
        id_rehab_sch_need_nin  IN BOOLEAN := TRUE,
        id_professional_in     IN rehab_schedule.id_professional%TYPE DEFAULT NULL,
        id_professional_nin    IN BOOLEAN := TRUE,
        id_schedule_in         IN rehab_schedule.id_schedule%TYPE DEFAULT NULL,
        id_schedule_nin        IN BOOLEAN := TRUE,
        dt_schedule_in         IN rehab_schedule.dt_schedule%TYPE DEFAULT NULL,
        dt_schedule_nin        IN BOOLEAN := TRUE,
        flg_status_in          IN rehab_schedule.flg_status%TYPE DEFAULT NULL,
        flg_status_nin         IN BOOLEAN := TRUE,
        id_cancel_reason_in    IN rehab_schedule.id_cancel_reason%TYPE DEFAULT NULL,
        id_cancel_reason_nin   IN BOOLEAN := TRUE,
        dt_cancel_in           IN rehab_schedule.dt_cancel%TYPE DEFAULT NULL,
        dt_cancel_nin          IN BOOLEAN := TRUE,
        id_cancel_prof_in      IN rehab_schedule.id_cancel_prof%TYPE DEFAULT NULL,
        id_cancel_prof_nin     IN BOOLEAN := TRUE,
        notes_in               IN rehab_schedule.notes%TYPE DEFAULT NULL,
        notes_nin              IN BOOLEAN := TRUE,
        create_user_in         IN rehab_schedule.create_user%TYPE DEFAULT NULL,
        create_user_nin        IN BOOLEAN := TRUE,
        create_time_in         IN rehab_schedule.create_time%TYPE DEFAULT NULL,
        create_time_nin        IN BOOLEAN := TRUE,
        create_institution_in  IN rehab_schedule.create_institution%TYPE DEFAULT NULL,
        create_institution_nin IN BOOLEAN := TRUE,
        update_user_in         IN rehab_schedule.update_user%TYPE DEFAULT NULL,
        update_user_nin        IN BOOLEAN := TRUE,
        update_time_in         IN rehab_schedule.update_time%TYPE DEFAULT NULL,
        update_time_nin        IN BOOLEAN := TRUE,
        update_institution_in  IN rehab_schedule.update_institution%TYPE DEFAULT NULL,
        update_institution_nin IN BOOLEAN := TRUE,
        handle_error_in        IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        upd(id_rehab_schedule_in   => id_rehab_schedule_in,
            id_rehab_sch_need_in   => id_rehab_sch_need_in,
            id_rehab_sch_need_nin  => id_rehab_sch_need_nin,
            id_professional_in     => id_professional_in,
            id_professional_nin    => id_professional_nin,
            id_schedule_in         => id_schedule_in,
            id_schedule_nin        => id_schedule_nin,
            dt_schedule_in         => dt_schedule_in,
            dt_schedule_nin        => dt_schedule_nin,
            flg_status_in          => flg_status_in,
            flg_status_nin         => flg_status_nin,
            id_cancel_reason_in    => id_cancel_reason_in,
            id_cancel_reason_nin   => id_cancel_reason_nin,
            dt_cancel_in           => dt_cancel_in,
            dt_cancel_nin          => dt_cancel_nin,
            id_cancel_prof_in      => id_cancel_prof_in,
            id_cancel_prof_nin     => id_cancel_prof_nin,
            notes_in               => notes_in,
            notes_nin              => notes_nin,
            create_user_in         => create_user_in,
            create_user_nin        => create_user_nin,
            create_time_in         => create_time_in,
            create_time_nin        => create_time_nin,
            create_institution_in  => create_institution_in,
            create_institution_nin => create_institution_nin,
            update_user_in         => update_user_in,
            update_user_nin        => update_user_nin,
            update_time_in         => update_time_in,
            update_time_nin        => update_time_nin,
            update_institution_in  => update_institution_in,
            update_institution_nin => update_institution_nin,
            handle_error_in        => handle_error_in,
            rows_out               => rows_out);
    END upd;

    PROCEDURE upd
    (
        id_rehab_sch_need_in   IN rehab_schedule.id_rehab_sch_need%TYPE DEFAULT NULL,
        id_rehab_sch_need_nin  IN BOOLEAN := TRUE,
        id_professional_in     IN rehab_schedule.id_professional%TYPE DEFAULT NULL,
        id_professional_nin    IN BOOLEAN := TRUE,
        id_schedule_in         IN rehab_schedule.id_schedule%TYPE DEFAULT NULL,
        id_schedule_nin        IN BOOLEAN := TRUE,
        dt_schedule_in         IN rehab_schedule.dt_schedule%TYPE DEFAULT NULL,
        dt_schedule_nin        IN BOOLEAN := TRUE,
        flg_status_in          IN rehab_schedule.flg_status%TYPE DEFAULT NULL,
        flg_status_nin         IN BOOLEAN := TRUE,
        id_cancel_reason_in    IN rehab_schedule.id_cancel_reason%TYPE DEFAULT NULL,
        id_cancel_reason_nin   IN BOOLEAN := TRUE,
        dt_cancel_in           IN rehab_schedule.dt_cancel%TYPE DEFAULT NULL,
        dt_cancel_nin          IN BOOLEAN := TRUE,
        id_cancel_prof_in      IN rehab_schedule.id_cancel_prof%TYPE DEFAULT NULL,
        id_cancel_prof_nin     IN BOOLEAN := TRUE,
        notes_in               IN rehab_schedule.notes%TYPE DEFAULT NULL,
        notes_nin              IN BOOLEAN := TRUE,
        create_user_in         IN rehab_schedule.create_user%TYPE DEFAULT NULL,
        create_user_nin        IN BOOLEAN := TRUE,
        create_time_in         IN rehab_schedule.create_time%TYPE DEFAULT NULL,
        create_time_nin        IN BOOLEAN := TRUE,
        create_institution_in  IN rehab_schedule.create_institution%TYPE DEFAULT NULL,
        create_institution_nin IN BOOLEAN := TRUE,
        update_user_in         IN rehab_schedule.update_user%TYPE DEFAULT NULL,
        update_user_nin        IN BOOLEAN := TRUE,
        update_time_in         IN rehab_schedule.update_time%TYPE DEFAULT NULL,
        update_time_nin        IN BOOLEAN := TRUE,
        update_institution_in  IN rehab_schedule.update_institution%TYPE DEFAULT NULL,
        update_institution_nin IN BOOLEAN := TRUE,
        where_in               VARCHAR2 DEFAULT NULL,
        handle_error_in        IN BOOLEAN := TRUE,
        rows_out               IN OUT table_varchar
    ) IS
        l_sql                  VARCHAR2(32767);
        l_rows_out             table_varchar;
        l_id_rehab_sch_need_n  NUMBER(1);
        l_id_professional_n    NUMBER(1);
        l_id_schedule_n        NUMBER(1);
        l_dt_schedule_n        NUMBER(1);
        l_flg_status_n         NUMBER(1);
        l_id_cancel_reason_n   NUMBER(1);
        l_dt_cancel_n          NUMBER(1);
        l_id_cancel_prof_n     NUMBER(1);
        l_notes_n              NUMBER(1);
        l_create_user_n        NUMBER(1);
        l_create_time_n        NUMBER(1);
        l_create_institution_n NUMBER(1);
        l_update_user_n        NUMBER(1);
        l_update_time_n        NUMBER(1);
        l_update_institution_n NUMBER(1);
        id_rehab_schedule_in   rehab_schedule.id_rehab_schedule%TYPE;
    BEGIN
    
        l_id_rehab_sch_need_n  := sys.diutil.bool_to_int(id_rehab_sch_need_nin);
        l_id_professional_n    := sys.diutil.bool_to_int(id_professional_nin);
        l_id_schedule_n        := sys.diutil.bool_to_int(id_schedule_nin);
        l_dt_schedule_n        := sys.diutil.bool_to_int(dt_schedule_nin);
        l_flg_status_n         := sys.diutil.bool_to_int(flg_status_nin);
        l_id_cancel_reason_n   := sys.diutil.bool_to_int(id_cancel_reason_nin);
        l_dt_cancel_n          := sys.diutil.bool_to_int(dt_cancel_nin);
        l_id_cancel_prof_n     := sys.diutil.bool_to_int(id_cancel_prof_nin);
        l_notes_n              := sys.diutil.bool_to_int(notes_nin);
        l_create_user_n        := sys.diutil.bool_to_int(create_user_nin);
        l_create_time_n        := sys.diutil.bool_to_int(create_time_nin);
        l_create_institution_n := sys.diutil.bool_to_int(create_institution_nin);
        l_update_user_n        := sys.diutil.bool_to_int(update_user_nin);
        l_update_time_n        := sys.diutil.bool_to_int(update_time_nin);
        l_update_institution_n := sys.diutil.bool_to_int(update_institution_nin);
    
        l_sql := 'UPDATE REHAB_SCHEDULE SET ' || ' ID_REHAB_SCH_NEED = decode (' || l_id_rehab_sch_need_n ||
                 ',0,:id_rehab_sch_need_in, NVL (:id_rehab_sch_need_in, ID_REHAB_SCH_NEED)) ' || ',' ||
                 ' ID_PROFESSIONAL = decode (' || l_id_professional_n ||
                 ',0,:id_professional_in, NVL (:id_professional_in, ID_PROFESSIONAL)) ' || ',' ||
                 ' ID_SCHEDULE = decode (' || l_id_schedule_n ||
                 ',0,:id_schedule_in, NVL (:id_schedule_in, ID_SCHEDULE)) ' || ',' || ' DT_SCHEDULE = decode (' ||
                 l_dt_schedule_n || ',0,:dt_schedule_in, NVL (:dt_schedule_in, DT_SCHEDULE)) ' || ',' ||
                 ' FLG_STATUS = decode (' || l_flg_status_n || ',0,:flg_status_in, NVL (:flg_status_in, FLG_STATUS)) ' || ',' ||
                 ' ID_CANCEL_REASON = decode (' || l_id_cancel_reason_n ||
                 ',0,:id_cancel_reason_in, NVL (:id_cancel_reason_in, ID_CANCEL_REASON)) ' || ',' ||
                 ' DT_CANCEL = decode (' || l_dt_cancel_n || ',0,:dt_cancel_in, NVL (:dt_cancel_in, DT_CANCEL)) ' || ',' ||
                 ' ID_CANCEL_PROF = decode (' || l_id_cancel_prof_n ||
                 ',0,:id_cancel_prof_in, NVL (:id_cancel_prof_in, ID_CANCEL_PROF)) ' || ',' || ' NOTES = decode (' ||
                 l_notes_n || ',0,:notes_in, NVL (:notes_in, NOTES)) ' || ',' || ' CREATE_USER = decode (' ||
                 l_create_user_n || ',0,:create_user_in, NVL (:create_user_in, CREATE_USER)) ' || ',' ||
                 ' CREATE_TIME = decode (' || l_create_time_n ||
                 ',0,:create_time_in, NVL (:create_time_in, CREATE_TIME)) ' || ',' || ' CREATE_INSTITUTION = decode (' ||
                 l_create_institution_n ||
                 ',0,:create_institution_in, NVL (:create_institution_in, CREATE_INSTITUTION)) ' || ',' ||
                 ' UPDATE_USER = decode (' || l_update_user_n ||
                 ',0,:update_user_in, NVL (:update_user_in, UPDATE_USER)) ' || ',' || ' UPDATE_TIME = decode (' ||
                 l_update_time_n || ',0,:update_time_in, NVL (:update_time_in, UPDATE_TIME)) ' || ',' ||
                 ' UPDATE_INSTITUTION = decode (' || l_update_institution_n ||
                 ',0,:update_institution_in, NVL (:update_institution_in, UPDATE_INSTITUTION)) ' || ' where ' ||
                 nvl(where_in, '(1=1)') || ' RETURNING ROWID BULK COLLECT INTO :l_rows_out';
    
        EXECUTE IMMEDIATE 'BEGIN ' || l_sql || '; END;'
            USING IN id_rehab_sch_need_in, id_professional_in, id_schedule_in, dt_schedule_in, flg_status_in, id_cancel_reason_in, dt_cancel_in, id_cancel_prof_in, notes_in, create_user_in, create_time_in, create_institution_in, update_user_in, update_time_in, update_institution_in, OUT l_rows_out;
    
        IF (rows_out IS NULL)
        THEN
            rows_out := table_varchar();
        END IF;
    
        rows_out := rows_out MULTISET UNION DISTINCT l_rows_out;
    
    EXCEPTION
        WHEN dup_val_on_index THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner           all_constraints.owner%TYPE;
                    l_name            all_constraints.constraint_name%TYPE;
                    l_err_instance_id PLS_INTEGER;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF FALSE
                    THEN
                        NULL; -- Placeholder in case no unique indexes
                    ELSE
                        pk_alert_exceptions.raise_error(error_name_in => 'DUPLICATE-VALUE',
                                                        name1_in      => 'OWNER',
                                                        value1_in     => l_owner,
                                                        name2_in      => 'CONSTRAINT_NAME',
                                                        value2_in     => l_name,
                                                        name3_in      => 'TABLE_NAME',
                                                        value3_in     => 'REHAB_SCHEDULE');
                    END IF;
                END;
            END IF;
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'REHAB_SCHEDULE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'REHAB_SCHEDULE');
                    IF l_name = 'RSC_PL_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_PROFESSIONAL',
                                                        value_in           => id_professional_in);
                    END IF;
                    IF l_name = 'RSC_PL_FK2'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_CANCEL_PROF',
                                                        value_in           => id_cancel_prof_in);
                    END IF;
                    IF l_name = 'RSC_RSD_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_REHAB_SCH_NEED',
                                                        value_in           => id_rehab_sch_need_in);
                    END IF;
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
        WHEN e_null_column_value THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    v_errm    VARCHAR2(2000) := dbms_utility.format_error_stack;
                    dot1loc   INTEGER;
                    dot2loc   INTEGER;
                    parenloc  INTEGER;
                    c_owner   all_constraints.owner%TYPE;
                    c_tabname all_tables.table_name%TYPE;
                    c_colname all_tab_columns.column_name%TYPE;
                BEGIN
                    dot1loc   := instr(v_errm, '.', 1, 1);
                    dot2loc   := instr(v_errm, '.', 1, 2);
                    parenloc  := instr(v_errm, '(');
                    c_owner   := substr(v_errm, parenloc + 1, dot1loc - parenloc - 1);
                    c_tabname := substr(v_errm, dot1loc + 1, dot2loc - dot1loc - 1);
                    c_colname := substr(v_errm, dot2loc + 1, instr(v_errm, ')') - dot2loc - 1);
                
                    pk_alert_exceptions.raise_error(error_name_in => 'COLUMN-CANNOT-BE-NULL',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => c_owner,
                                                    name2_in      => 'TABLE_NAME',
                                                    value2_in     => c_tabname,
                                                    name3_in      => 'COLUMN_NAME',
                                                    value3_in     => c_colname);
                END;
            END IF;
    END upd;

    PROCEDURE upd
    (
        id_rehab_sch_need_in   IN rehab_schedule.id_rehab_sch_need%TYPE DEFAULT NULL,
        id_rehab_sch_need_nin  IN BOOLEAN := TRUE,
        id_professional_in     IN rehab_schedule.id_professional%TYPE DEFAULT NULL,
        id_professional_nin    IN BOOLEAN := TRUE,
        id_schedule_in         IN rehab_schedule.id_schedule%TYPE DEFAULT NULL,
        id_schedule_nin        IN BOOLEAN := TRUE,
        dt_schedule_in         IN rehab_schedule.dt_schedule%TYPE DEFAULT NULL,
        dt_schedule_nin        IN BOOLEAN := TRUE,
        flg_status_in          IN rehab_schedule.flg_status%TYPE DEFAULT NULL,
        flg_status_nin         IN BOOLEAN := TRUE,
        id_cancel_reason_in    IN rehab_schedule.id_cancel_reason%TYPE DEFAULT NULL,
        id_cancel_reason_nin   IN BOOLEAN := TRUE,
        dt_cancel_in           IN rehab_schedule.dt_cancel%TYPE DEFAULT NULL,
        dt_cancel_nin          IN BOOLEAN := TRUE,
        id_cancel_prof_in      IN rehab_schedule.id_cancel_prof%TYPE DEFAULT NULL,
        id_cancel_prof_nin     IN BOOLEAN := TRUE,
        notes_in               IN rehab_schedule.notes%TYPE DEFAULT NULL,
        notes_nin              IN BOOLEAN := TRUE,
        create_user_in         IN rehab_schedule.create_user%TYPE DEFAULT NULL,
        create_user_nin        IN BOOLEAN := TRUE,
        create_time_in         IN rehab_schedule.create_time%TYPE DEFAULT NULL,
        create_time_nin        IN BOOLEAN := TRUE,
        create_institution_in  IN rehab_schedule.create_institution%TYPE DEFAULT NULL,
        create_institution_nin IN BOOLEAN := TRUE,
        update_user_in         IN rehab_schedule.update_user%TYPE DEFAULT NULL,
        update_user_nin        IN BOOLEAN := TRUE,
        update_time_in         IN rehab_schedule.update_time%TYPE DEFAULT NULL,
        update_time_nin        IN BOOLEAN := TRUE,
        update_institution_in  IN rehab_schedule.update_institution%TYPE DEFAULT NULL,
        update_institution_nin IN BOOLEAN := TRUE,
        where_in               VARCHAR2 DEFAULT NULL,
        handle_error_in        IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        upd(id_rehab_sch_need_in   => id_rehab_sch_need_in,
            id_rehab_sch_need_nin  => id_rehab_sch_need_nin,
            id_professional_in     => id_professional_in,
            id_professional_nin    => id_professional_nin,
            id_schedule_in         => id_schedule_in,
            id_schedule_nin        => id_schedule_nin,
            dt_schedule_in         => dt_schedule_in,
            dt_schedule_nin        => dt_schedule_nin,
            flg_status_in          => flg_status_in,
            flg_status_nin         => flg_status_nin,
            id_cancel_reason_in    => id_cancel_reason_in,
            id_cancel_reason_nin   => id_cancel_reason_nin,
            dt_cancel_in           => dt_cancel_in,
            dt_cancel_nin          => dt_cancel_nin,
            id_cancel_prof_in      => id_cancel_prof_in,
            id_cancel_prof_nin     => id_cancel_prof_nin,
            notes_in               => notes_in,
            notes_nin              => notes_nin,
            create_user_in         => create_user_in,
            create_user_nin        => create_user_nin,
            create_time_in         => create_time_in,
            create_time_nin        => create_time_nin,
            create_institution_in  => create_institution_in,
            create_institution_nin => create_institution_nin,
            update_user_in         => update_user_in,
            update_user_nin        => update_user_nin,
            update_time_in         => update_time_in,
            update_time_nin        => update_time_nin,
            update_institution_in  => update_institution_in,
            update_institution_nin => update_institution_nin,
            where_in               => where_in,
            handle_error_in        => handle_error_in,
            rows_out               => rows_out);
    END upd;

    PROCEDURE upd
    (
        rec_in          IN rehab_schedule%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        IN OUT table_varchar
    ) IS
    BEGIN
        upd(id_rehab_schedule_in  => rec_in.id_rehab_schedule,
            id_rehab_sch_need_in  => rec_in.id_rehab_sch_need,
            id_professional_in    => rec_in.id_professional,
            id_schedule_in        => rec_in.id_schedule,
            dt_schedule_in        => rec_in.dt_schedule,
            flg_status_in         => rec_in.flg_status,
            id_cancel_reason_in   => rec_in.id_cancel_reason,
            dt_cancel_in          => rec_in.dt_cancel,
            id_cancel_prof_in     => rec_in.id_cancel_prof,
            notes_in              => rec_in.notes,
            create_user_in        => rec_in.create_user,
            create_time_in        => rec_in.create_time,
            create_institution_in => rec_in.create_institution,
            update_user_in        => rec_in.update_user,
            update_time_in        => rec_in.update_time,
            update_institution_in => rec_in.update_institution
            
           ,
            handle_error_in => handle_error_in,
            rows_out        => rows_out);
    END upd;

    PROCEDURE upd
    (
        rec_in          IN rehab_schedule%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        upd(id_rehab_schedule_in  => rec_in.id_rehab_schedule,
            id_rehab_sch_need_in  => rec_in.id_rehab_sch_need,
            id_professional_in    => rec_in.id_professional,
            id_schedule_in        => rec_in.id_schedule,
            dt_schedule_in        => rec_in.dt_schedule,
            flg_status_in         => rec_in.flg_status,
            id_cancel_reason_in   => rec_in.id_cancel_reason,
            dt_cancel_in          => rec_in.dt_cancel,
            id_cancel_prof_in     => rec_in.id_cancel_prof,
            notes_in              => rec_in.notes,
            create_user_in        => rec_in.create_user,
            create_time_in        => rec_in.create_time,
            create_institution_in => rec_in.create_institution,
            update_user_in        => rec_in.update_user,
            update_time_in        => rec_in.update_time,
            update_institution_in => rec_in.update_institution
            
           ,
            handle_error_in => handle_error_in,
            rows_out        => rows_out);
    END upd;

    PROCEDURE upd_ins
    (
        id_rehab_schedule_in  IN rehab_schedule.id_rehab_schedule%TYPE,
        id_rehab_sch_need_in  IN rehab_schedule.id_rehab_sch_need%TYPE DEFAULT NULL,
        id_professional_in    IN rehab_schedule.id_professional%TYPE DEFAULT NULL,
        id_schedule_in        IN rehab_schedule.id_schedule%TYPE DEFAULT NULL,
        dt_schedule_in        IN rehab_schedule.dt_schedule%TYPE DEFAULT NULL,
        flg_status_in         IN rehab_schedule.flg_status%TYPE DEFAULT NULL,
        id_cancel_reason_in   IN rehab_schedule.id_cancel_reason%TYPE DEFAULT NULL,
        dt_cancel_in          IN rehab_schedule.dt_cancel%TYPE DEFAULT NULL,
        id_cancel_prof_in     IN rehab_schedule.id_cancel_prof%TYPE DEFAULT NULL,
        notes_in              IN rehab_schedule.notes%TYPE DEFAULT NULL,
        create_user_in        IN rehab_schedule.create_user%TYPE DEFAULT NULL,
        create_time_in        IN rehab_schedule.create_time%TYPE DEFAULT NULL,
        create_institution_in IN rehab_schedule.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN rehab_schedule.update_user%TYPE DEFAULT NULL,
        update_time_in        IN rehab_schedule.update_time%TYPE DEFAULT NULL,
        update_institution_in IN rehab_schedule.update_institution%TYPE DEFAULT NULL,
        handle_error_in       IN BOOLEAN := TRUE,
        rows_out              OUT table_varchar
    ) IS
    BEGIN
        upd(id_rehab_schedule_in  => id_rehab_schedule_in,
            id_rehab_sch_need_in  => id_rehab_sch_need_in,
            id_professional_in    => id_professional_in,
            id_schedule_in        => id_schedule_in,
            dt_schedule_in        => dt_schedule_in,
            flg_status_in         => flg_status_in,
            id_cancel_reason_in   => id_cancel_reason_in,
            dt_cancel_in          => dt_cancel_in,
            id_cancel_prof_in     => id_cancel_prof_in,
            notes_in              => notes_in,
            create_user_in        => create_user_in,
            create_time_in        => create_time_in,
            create_institution_in => create_institution_in,
            update_user_in        => update_user_in,
            update_time_in        => update_time_in,
            update_institution_in => update_institution_in,
            handle_error_in       => handle_error_in,
            rows_out              => rows_out);
        IF SQL%ROWCOUNT = 0
        THEN
            ins(id_rehab_schedule_in  => id_rehab_schedule_in,
                id_rehab_sch_need_in  => id_rehab_sch_need_in,
                id_professional_in    => id_professional_in,
                id_schedule_in        => id_schedule_in,
                dt_schedule_in        => dt_schedule_in,
                flg_status_in         => flg_status_in,
                id_cancel_reason_in   => id_cancel_reason_in,
                dt_cancel_in          => dt_cancel_in,
                id_cancel_prof_in     => id_cancel_prof_in,
                notes_in              => notes_in,
                create_user_in        => create_user_in,
                create_time_in        => create_time_in,
                create_institution_in => create_institution_in,
                update_user_in        => update_user_in,
                update_time_in        => update_time_in,
                update_institution_in => update_institution_in,
                handle_error_in       => handle_error_in,
                rows_out              => rows_out);
        END IF;
    END upd_ins;

    PROCEDURE upd_ins
    (
        id_rehab_schedule_in  IN rehab_schedule.id_rehab_schedule%TYPE,
        id_rehab_sch_need_in  IN rehab_schedule.id_rehab_sch_need%TYPE DEFAULT NULL,
        id_professional_in    IN rehab_schedule.id_professional%TYPE DEFAULT NULL,
        id_schedule_in        IN rehab_schedule.id_schedule%TYPE DEFAULT NULL,
        dt_schedule_in        IN rehab_schedule.dt_schedule%TYPE DEFAULT NULL,
        flg_status_in         IN rehab_schedule.flg_status%TYPE DEFAULT NULL,
        id_cancel_reason_in   IN rehab_schedule.id_cancel_reason%TYPE DEFAULT NULL,
        dt_cancel_in          IN rehab_schedule.dt_cancel%TYPE DEFAULT NULL,
        id_cancel_prof_in     IN rehab_schedule.id_cancel_prof%TYPE DEFAULT NULL,
        notes_in              IN rehab_schedule.notes%TYPE DEFAULT NULL,
        create_user_in        IN rehab_schedule.create_user%TYPE DEFAULT NULL,
        create_time_in        IN rehab_schedule.create_time%TYPE DEFAULT NULL,
        create_institution_in IN rehab_schedule.create_institution%TYPE DEFAULT NULL,
        update_user_in        IN rehab_schedule.update_user%TYPE DEFAULT NULL,
        update_time_in        IN rehab_schedule.update_time%TYPE DEFAULT NULL,
        update_institution_in IN rehab_schedule.update_institution%TYPE DEFAULT NULL,
        handle_error_in       IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        upd_ins(id_rehab_schedule_in,
                id_rehab_sch_need_in,
                id_professional_in,
                id_schedule_in,
                dt_schedule_in,
                flg_status_in,
                id_cancel_reason_in,
                dt_cancel_in,
                id_cancel_prof_in,
                notes_in,
                create_user_in,
                create_time_in,
                create_institution_in,
                update_user_in,
                update_time_in,
                update_institution_in,
                handle_error_in,
                rows_out);
    END upd_ins;

    PROCEDURE upd
    (
        col_in            IN rehab_schedule_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          IN OUT table_varchar
    ) IS
        l_id_rehab_schedule  id_rehab_schedule_cc;
        l_id_rehab_sch_need  id_rehab_sch_need_cc;
        l_id_professional    id_professional_cc;
        l_id_schedule        id_schedule_cc;
        l_dt_schedule        dt_schedule_cc;
        l_flg_status         flg_status_cc;
        l_id_cancel_reason   id_cancel_reason_cc;
        l_dt_cancel          dt_cancel_cc;
        l_id_cancel_prof     id_cancel_prof_cc;
        l_notes              notes_cc;
        l_create_user        create_user_cc;
        l_create_time        create_time_cc;
        l_create_institution create_institution_cc;
        l_update_user        update_user_cc;
        l_update_time        update_time_cc;
        l_update_institution update_institution_cc;
    BEGIN
        FOR i IN col_in.FIRST .. col_in.LAST
        LOOP
            l_id_rehab_schedule(i) := col_in(i).id_rehab_schedule;
            l_id_rehab_sch_need(i) := col_in(i).id_rehab_sch_need;
            l_id_professional(i) := col_in(i).id_professional;
            l_id_schedule(i) := col_in(i).id_schedule;
            l_dt_schedule(i) := col_in(i).dt_schedule;
            l_flg_status(i) := col_in(i).flg_status;
            l_id_cancel_reason(i) := col_in(i).id_cancel_reason;
            l_dt_cancel(i) := col_in(i).dt_cancel;
            l_id_cancel_prof(i) := col_in(i).id_cancel_prof;
            l_notes(i) := col_in(i).notes;
            l_create_user(i) := col_in(i).create_user;
            l_create_time(i) := col_in(i).create_time;
            l_create_institution(i) := col_in(i).create_institution;
            l_update_user(i) := col_in(i).update_user;
            l_update_time(i) := col_in(i).update_time;
            l_update_institution(i) := col_in(i).update_institution;
        END LOOP;
        IF nvl(ignore_if_null_in, FALSE)
        THEN
            -- Set any columns to their current values
            -- if incoming value is NULL.
            -- Put WHEN clause on column-level triggers!
            FORALL i IN col_in.FIRST .. col_in.LAST
                UPDATE rehab_schedule
                   SET id_rehab_sch_need  = nvl(l_id_rehab_sch_need(i), id_rehab_sch_need),
                       id_professional    = nvl(l_id_professional(i), id_professional),
                       id_schedule        = nvl(l_id_schedule(i), id_schedule),
                       dt_schedule        = nvl(l_dt_schedule(i), dt_schedule),
                       flg_status         = nvl(l_flg_status(i), flg_status),
                       id_cancel_reason   = nvl(l_id_cancel_reason(i), id_cancel_reason),
                       dt_cancel          = nvl(l_dt_cancel(i), dt_cancel),
                       id_cancel_prof     = nvl(l_id_cancel_prof(i), id_cancel_prof),
                       notes              = nvl(l_notes(i), notes),
                       create_user        = nvl(l_create_user(i), create_user),
                       create_time        = nvl(l_create_time(i), create_time),
                       create_institution = nvl(l_create_institution(i), create_institution),
                       update_user        = nvl(l_update_user(i), update_user),
                       update_time        = nvl(l_update_time(i), update_time),
                       update_institution = nvl(l_update_institution(i), update_institution)
                 WHERE id_rehab_schedule = l_id_rehab_schedule(i);
        ELSE
            FORALL i IN col_in.FIRST .. col_in.LAST
                UPDATE rehab_schedule
                   SET id_rehab_sch_need  = l_id_rehab_sch_need(i),
                       id_professional    = l_id_professional(i),
                       id_schedule        = l_id_schedule(i),
                       dt_schedule        = l_dt_schedule(i),
                       flg_status         = l_flg_status(i),
                       id_cancel_reason   = l_id_cancel_reason(i),
                       dt_cancel          = l_dt_cancel(i),
                       id_cancel_prof     = l_id_cancel_prof(i),
                       notes              = l_notes(i),
                       create_user        = l_create_user(i),
                       create_time        = l_create_time(i),
                       create_institution = l_create_institution(i),
                       update_user        = l_update_user(i),
                       update_time        = l_update_time(i),
                       update_institution = l_update_institution(i)
                 WHERE id_rehab_schedule = l_id_rehab_schedule(i);
        END IF;
    END upd;

    PROCEDURE upd
    (
        col_in            IN rehab_schedule_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        upd(col_in, ignore_if_null_in, handle_error_in, rows_out);
    END upd;

    FUNCTION dynupdstr
    (
        colname_in IN all_tab_columns.column_name%TYPE,
        where_in   IN VARCHAR2 := NULL
    )
    
     RETURN VARCHAR2 IS
    BEGIN
        RETURN 'BEGIN UPDATE REHAB_SCHEDULE
             SET ' || colname_in || ' = :value
           WHERE ' || nvl(where_in, '1=1') || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;';
    END dynupdstr;

    FUNCTION dynupdstr_no_rows_out
    (
        colname_in IN all_tab_columns.column_name%TYPE,
        where_in   IN VARCHAR2 := NULL
    )
    
     RETURN VARCHAR2 IS
    BEGIN
        RETURN 'UPDATE REHAB_SCHEDULE
             SET ' || colname_in || ' = :value
           WHERE ' || nvl(where_in, '1=1');
    END dynupdstr_no_rows_out;

    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    ) IS
    BEGIN
        EXECUTE IMMEDIATE 'BEGIN UPDATE REHAB_SCHEDULE set ' || colname_in || '=' || colname_in || ' + ' ||
                          nvl(increment_value_in, 1) || ' WHERE ' || nvl(where_in, '1=1') ||
                          ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
            USING OUT rows_out;
    EXCEPTION
        WHEN dup_val_on_index THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner           all_constraints.owner%TYPE;
                    l_name            all_constraints.constraint_name%TYPE;
                    l_err_instance_id PLS_INTEGER;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'DUPLICATE-VALUE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'REHAB_SCHEDULE');
                END;
            END IF;
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'REHAB_SCHEDULE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'REHAB_SCHEDULE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
        WHEN e_null_column_value THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    v_errm    VARCHAR2(2000) := dbms_utility.format_error_stack;
                    dot1loc   INTEGER;
                    dot2loc   INTEGER;
                    parenloc  INTEGER;
                    c_owner   all_constraints.owner%TYPE;
                    c_tabname all_tables.table_name%TYPE;
                    c_colname all_tab_columns.column_name%TYPE;
                BEGIN
                    dot1loc   := instr(v_errm, '.', 1, 1);
                    dot2loc   := instr(v_errm, '.', 1, 2);
                    parenloc  := instr(v_errm, '(');
                    c_owner   := substr(v_errm, parenloc + 1, dot1loc - parenloc - 1);
                    c_tabname := substr(v_errm, dot1loc + 1, dot2loc - dot1loc - 1);
                    c_colname := substr(v_errm, dot2loc + 1, instr(v_errm, ')') - dot2loc - 1);
                
                    pk_alert_exceptions.raise_error(error_name_in => 'COLUMN-CANNOT-BE-NULL',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => c_owner,
                                                    name2_in      => 'TABLE_NAME',
                                                    value2_in     => c_tabname,
                                                    name3_in      => 'COLUMN_NAME',
                                                    value3_in     => c_colname);
                END;
            END IF;
    END increment_onecol;

    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        EXECUTE IMMEDIATE 'UPDATE REHAB_SCHEDULE set ' || colname_in || '=' || colname_in || ' + ' ||
                          nvl(increment_value_in, 1) || ' WHERE ' || nvl(where_in, '1=1');
    EXCEPTION
        WHEN dup_val_on_index THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner           all_constraints.owner%TYPE;
                    l_name            all_constraints.constraint_name%TYPE;
                    l_err_instance_id PLS_INTEGER;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'DUPLICATE-VALUE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'REHAB_SCHEDULE');
                END;
            END IF;
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'REHAB_SCHEDULE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'REHAB_SCHEDULE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
        WHEN e_null_column_value THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    v_errm    VARCHAR2(2000) := dbms_utility.format_error_stack;
                    dot1loc   INTEGER;
                    dot2loc   INTEGER;
                    parenloc  INTEGER;
                    c_owner   all_constraints.owner%TYPE;
                    c_tabname all_tables.table_name%TYPE;
                    c_colname all_tab_columns.column_name%TYPE;
                BEGIN
                    dot1loc   := instr(v_errm, '.', 1, 1);
                    dot2loc   := instr(v_errm, '.', 1, 2);
                    parenloc  := instr(v_errm, '(');
                    c_owner   := substr(v_errm, parenloc + 1, dot1loc - parenloc - 1);
                    c_tabname := substr(v_errm, dot1loc + 1, dot2loc - dot1loc - 1);
                    c_colname := substr(v_errm, dot2loc + 1, instr(v_errm, ')') - dot2loc - 1);
                
                    pk_alert_exceptions.raise_error(error_name_in => 'COLUMN-CANNOT-BE-NULL',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => c_owner,
                                                    name2_in      => 'TABLE_NAME',
                                                    value2_in     => c_tabname,
                                                    name3_in      => 'COLUMN_NAME',
                                                    value3_in     => c_colname);
                END;
            END IF;
    END increment_onecol;

    -- Delete functionality

    PROCEDURE del
    (
        id_rehab_schedule_in IN rehab_schedule.id_rehab_schedule%TYPE,
        handle_error_in      IN BOOLEAN := TRUE,
        rows_out             OUT table_varchar
    ) IS
    BEGIN
        DELETE FROM rehab_schedule
         WHERE id_rehab_schedule = id_rehab_schedule_in
        RETURNING ROWID BULK COLLECT INTO rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'REHAB_SCHEDULE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'REHAB_SCHEDULE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del;

    PROCEDURE del
    (
        id_rehab_schedule_in IN rehab_schedule.id_rehab_schedule%TYPE,
        handle_error_in      IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
    
        del(id_rehab_schedule_in => id_rehab_schedule_in, handle_error_in => handle_error_in, rows_out => rows_out);
    
    END del;

    -- Delete all rows for primary key column ID_REHAB_SCHEDULE
    PROCEDURE del_id_rehab_schedule
    (
        id_rehab_schedule_in IN rehab_schedule.id_rehab_schedule%TYPE,
        handle_error_in      IN BOOLEAN := TRUE,
        rows_out             OUT table_varchar
    ) IS
    BEGIN
        DELETE FROM rehab_schedule
         WHERE id_rehab_schedule = id_rehab_schedule_in
        RETURNING ROWID BULK COLLECT INTO rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'REHAB_SCHEDULE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'REHAB_SCHEDULE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_id_rehab_schedule;

    -- Delete all rows for primary key column ID_REHAB_SCHEDULE
    PROCEDURE del_id_rehab_schedule
    (
        id_rehab_schedule_in IN rehab_schedule.id_rehab_schedule%TYPE,
        handle_error_in      IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        del_id_rehab_schedule(id_rehab_schedule_in => id_rehab_schedule_in,
                              handle_error_in      => handle_error_in,
                              rows_out             => rows_out);
    END del_id_rehab_schedule;

    PROCEDURE del_rsc_pl_fk
    (
        id_professional_in IN rehab_schedule.id_professional%TYPE,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    ) IS
    BEGIN
        DELETE FROM rehab_schedule
         WHERE id_professional = del_rsc_pl_fk.id_professional_in
        RETURNING ROWID BULK COLLECT INTO rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'REHAB_SCHEDULE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'REHAB_SCHEDULE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_rsc_pl_fk;

    PROCEDURE del_rsc_pl_fk
    (
        id_professional_in IN rehab_schedule.id_professional%TYPE,
        handle_error_in    IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        del_rsc_pl_fk(id_professional_in => id_professional_in,
                      handle_error_in    => handle_error_in,
                      rows_out           => rows_out);
    END del_rsc_pl_fk;

    PROCEDURE del_rsc_pl_fk2
    (
        id_cancel_prof_in IN rehab_schedule.id_cancel_prof%TYPE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          OUT table_varchar
    ) IS
    BEGIN
        DELETE FROM rehab_schedule
         WHERE id_cancel_prof = del_rsc_pl_fk2.id_cancel_prof_in
        RETURNING ROWID BULK COLLECT INTO rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'REHAB_SCHEDULE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'REHAB_SCHEDULE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_rsc_pl_fk2;

    PROCEDURE del_rsc_pl_fk2
    (
        id_cancel_prof_in IN rehab_schedule.id_cancel_prof%TYPE,
        handle_error_in   IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        del_rsc_pl_fk2(id_cancel_prof_in => id_cancel_prof_in,
                       handle_error_in   => handle_error_in,
                       rows_out          => rows_out);
    END del_rsc_pl_fk2;

    PROCEDURE del_rsc_rsd_fk
    (
        id_rehab_sch_need_in IN rehab_schedule.id_rehab_sch_need%TYPE,
        handle_error_in      IN BOOLEAN := TRUE,
        rows_out             OUT table_varchar
    ) IS
    BEGIN
        DELETE FROM rehab_schedule
         WHERE id_rehab_sch_need = del_rsc_rsd_fk.id_rehab_sch_need_in
        RETURNING ROWID BULK COLLECT INTO rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'REHAB_SCHEDULE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'REHAB_SCHEDULE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_rsc_rsd_fk;

    PROCEDURE del_rsc_rsd_fk
    (
        id_rehab_sch_need_in IN rehab_schedule.id_rehab_sch_need%TYPE,
        handle_error_in      IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        del_rsc_rsd_fk(id_rehab_sch_need_in => id_rehab_sch_need_in,
                       handle_error_in      => handle_error_in,
                       rows_out             => rows_out);
    END del_rsc_rsd_fk;

    -- Deletions using dynamic SQL
    FUNCTION dyndelstr(where_in IN VARCHAR2) RETURN VARCHAR2 IS
    BEGIN
        IF where_in IS NULL
        THEN
            RETURN 'DELETE FROM REHAB_SCHEDULE';
        ELSE
            RETURN 'DELETE FROM REHAB_SCHEDULE WHERE ' || where_in;
        END IF;
    END dyndelstr;

    FUNCTION dyncoldelstr(colname_in IN all_tab_columns.column_name%TYPE) RETURN VARCHAR2 IS
    BEGIN
        RETURN dyndelstr(colname_in || ' = :value');
    END;

    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
    BEGIN
        EXECUTE IMMEDIATE dyndelstr(where_clause_in);
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'REHAB_SCHEDULE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'REHAB_SCHEDULE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_by;

    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) IS
    BEGIN
        EXECUTE IMMEDIATE 'BEGIN ' || dyndelstr(where_clause_in) ||
                          ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
            USING OUT rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'REHAB_SCHEDULE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'REHAB_SCHEDULE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_by;

    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
    BEGIN
        EXECUTE IMMEDIATE dyncoldelstr(colname_in)
            USING colvalue_in;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'REHAB_SCHEDULE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'REHAB_SCHEDULE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_by_col;

    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) IS
    BEGIN
        EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr(colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
            USING IN colvalue_in, OUT rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'REHAB_SCHEDULE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'REHAB_SCHEDULE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_by_col;

    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
    BEGIN
        EXECUTE IMMEDIATE dyncoldelstr(colname_in)
            USING colvalue_in;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'REHAB_SCHEDULE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'REHAB_SCHEDULE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_by_col;

    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) IS
    BEGIN
        EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr(colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
            USING IN colvalue_in, OUT rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'REHAB_SCHEDULE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'REHAB_SCHEDULE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_by_col;

    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
    BEGIN
        EXECUTE IMMEDIATE dyncoldelstr(colname_in)
            USING colvalue_in;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'REHAB_SCHEDULE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'REHAB_SCHEDULE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_by_col;

    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) IS
    BEGIN
        EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr(colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
            USING IN colvalue_in, OUT rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'REHAB_SCHEDULE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'REHAB_SCHEDULE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_by_col;

    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
    BEGIN
        EXECUTE IMMEDIATE dyncoldelstr(colname_in)
            USING colvalue_in;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'REHAB_SCHEDULE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'REHAB_SCHEDULE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_by_col;

    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) IS
    BEGIN
        EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr(colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
            USING IN colvalue_in, OUT rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'REHAB_SCHEDULE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'REHAB_SCHEDULE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_by_col;

    -- Initialize a record with default values for columns in the table.
    PROCEDURE initrec(rehab_schedule_inout IN OUT rehab_schedule%ROWTYPE) IS
    BEGIN
        rehab_schedule_inout.id_rehab_schedule  := NULL;
        rehab_schedule_inout.id_rehab_sch_need  := NULL;
        rehab_schedule_inout.id_professional    := NULL;
        rehab_schedule_inout.id_schedule        := NULL;
        rehab_schedule_inout.dt_schedule        := NULL;
        rehab_schedule_inout.flg_status         := NULL;
        rehab_schedule_inout.id_cancel_reason   := NULL;
        rehab_schedule_inout.dt_cancel          := NULL;
        rehab_schedule_inout.id_cancel_prof     := NULL;
        rehab_schedule_inout.notes              := NULL;
        rehab_schedule_inout.create_user        := NULL;
        rehab_schedule_inout.create_time        := NULL;
        rehab_schedule_inout.create_institution := NULL;
        rehab_schedule_inout.update_user        := NULL;
        rehab_schedule_inout.update_time        := NULL;
        rehab_schedule_inout.update_institution := NULL;
    END initrec;

    FUNCTION initrec RETURN rehab_schedule%ROWTYPE IS
        l_rehab_schedule rehab_schedule%ROWTYPE;
    BEGIN
        RETURN l_rehab_schedule;
    END initrec;

    FUNCTION get_data_rowid(rows_in IN table_varchar) RETURN rehab_schedule_tc IS
        data rehab_schedule_tc;
    BEGIN
        SELECT * BULK COLLECT
          INTO data
          FROM rehab_schedule
         WHERE ROWID IN (SELECT /*+opt_estimate(table,t,scale_rows=0.0000001))*/
                          *
                           FROM TABLE(rows_in) t);
        RETURN data;
    EXCEPTION
        WHEN OTHERS THEN
            pk_alert_exceptions.raise_error(error_name_in => 'get_data_rowid');
    END get_data_rowid;

    FUNCTION get_data_rowid_pat(rows_in IN table_varchar) RETURN rehab_schedule_tc IS
        PRAGMA AUTONOMOUS_TRANSACTION;
        data rehab_schedule_tc;
    BEGIN
        data := get_data_rowid(rows_in);
        COMMIT;
        RETURN data;
    EXCEPTION
        WHEN OTHERS THEN
            pk_alert_exceptions.raise_error(error_name_in => 'get_data_rowid');
            ROLLBACK;
    END get_data_rowid_pat;

BEGIN
    NULL;
END ts_rehab_schedule;
/
