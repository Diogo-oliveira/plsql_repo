/*-- Last Change Revision: $Rev: 2028043 $*/
/*-- Last Change by: $Author: mario.fernandes $*/
/*-- Date of last change: $Date: 2022-08-02 18:44:02 +0100 (ter, 02 ago 2022) $*/
CREATE OR REPLACE PACKAGE BODY TS_EPIS_DIAGNOSIS_NOTES
/*
| Generated by or retrieved from QCGU - DO NOT MODIFY!
| QCGU - "Get it right, do it fast" - www.ToadWorld.com
| QCGU Universal ID: {657BD35E-2E48-4CD4-B14E-41420B8433B3}
| Created On: Março 23, 2012 17:31:12
| Created By: ALERT
*/
IS


   e_null_column_value EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_null_column_value, -1400);
   --
   e_existing_fky_reference EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_existing_fky_reference, -2266);
   --
   e_check_constraint_failure EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_check_constraint_failure, -2290);
   --
   e_no_parent_key EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_no_parent_key, -2291);
   --
   e_child_record_found EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_child_record_found, -2292);
   --
   e_forall_error EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_forall_error, -24381);
   --
   -- Defined for backward compatibilty.
   e_integ_constraint_failure EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_integ_constraint_failure, -2291);

    -- Private utilities
   PROCEDURE get_constraint_info (
      owner_out OUT ALL_CONSTRAINTS.OWNER%TYPE
     ,name_out OUT ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE)
   IS
      l_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
      dotloc PLS_INTEGER;
      leftloc PLS_INTEGER;
   BEGIN
      dotloc  := INSTR (l_errm,'.');
      leftloc := INSTR (l_errm,'(');
      owner_out := SUBSTR (l_errm, leftloc+1, dotloc-leftloc-1);
      name_out  := SUBSTR (l_errm, dotloc+1, INSTR (l_errm,')')-dotloc-1);
   END get_constraint_info;
   -- Public programs

   PROCEDURE ins (
      id_epis_diagnosis_notes_in IN EPIS_DIAGNOSIS_NOTES.ID_EPIS_DIAGNOSIS_NOTES%TYPE
      ,
      notes_in IN EPIS_DIAGNOSIS_NOTES.NOTES%TYPE DEFAULT NULL,
      create_user_in IN EPIS_DIAGNOSIS_NOTES.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN EPIS_DIAGNOSIS_NOTES.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN EPIS_DIAGNOSIS_NOTES.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      id_episode_in IN EPIS_DIAGNOSIS_NOTES.ID_EPISODE%TYPE DEFAULT NULL,
      dt_epis_diagnosis_notes_in IN EPIS_DIAGNOSIS_NOTES.DT_EPIS_DIAGNOSIS_NOTES%TYPE DEFAULT NULL,
      id_prof_create_in IN EPIS_DIAGNOSIS_NOTES.ID_PROF_CREATE%TYPE DEFAULT NULL,
      id_cancel_reason_in IN EPIS_DIAGNOSIS_NOTES.ID_CANCEL_REASON%TYPE DEFAULT NULL,
      notes_cancel_in IN EPIS_DIAGNOSIS_NOTES.NOTES_CANCEL%TYPE DEFAULT NULL,
      id_prof_cancel_in IN EPIS_DIAGNOSIS_NOTES.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      dt_cancel_in IN EPIS_DIAGNOSIS_NOTES.DT_CANCEL%TYPE DEFAULT NULL,
      dt_create_in IN EPIS_DIAGNOSIS_NOTES.DT_CREATE%TYPE DEFAULT NULL
     ,handle_error_in IN BOOLEAN := TRUE
      , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN

     INSERT INTO EPIS_DIAGNOSIS_NOTES (
         ID_EPIS_DIAGNOSIS_NOTES,
         NOTES,
         CREATE_USER,
         CREATE_TIME,
         CREATE_INSTITUTION,
         UPDATE_USER,
         UPDATE_TIME,
         UPDATE_INSTITUTION,
         ID_EPISODE,
         DT_EPIS_DIAGNOSIS_NOTES,
         ID_PROF_CREATE,
         ID_CANCEL_REASON,
         NOTES_CANCEL,
         ID_PROF_CANCEL,
         DT_CANCEL,
         DT_CREATE
         )
      VALUES (
         id_epis_diagnosis_notes_in,
         notes_in,
         create_user_in,
         create_time_in,
         create_institution_in,
         update_user_in,
         update_time_in,
         update_institution_in,
         id_episode_in,
         dt_epis_diagnosis_notes_in,
         id_prof_create_in,
         id_cancel_reason_in,
         notes_cancel_in,
         id_prof_cancel_in,
         dt_cancel_in,
         dt_create_in
         ) RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF FALSE THEN NULL; -- Placeholder in case no unique indexes
           ELSE
              pk_alert_exceptions.raise_error (
                    error_name_in => 'DUPLICATE-VALUE'
                    ,name1_in => 'OWNER'
                    ,value1_in => l_owner
                    ,name2_in => 'CONSTRAINT_NAME'
                    ,value2_in => l_name
                    ,name3_in => 'TABLE_NAME'
                    ,value3_in => 'EPIS_DIAGNOSIS_NOTES');
           END IF;
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'EPIS_DIAGNOSIS_NOTES');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'EPIS_DIAGNOSIS_NOTES');
           IF l_name = 'EDN_CR_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_CANCEL_REASON'
               , value_in => id_cancel_reason_in);
           END IF;
           IF l_name = 'EDN_EPIS_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_EPISODE'
               , value_in => id_episode_in);
           END IF;
           IF l_name = 'EDN_PROF_CANCEL_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_PROF_CANCEL'
               , value_in => id_prof_cancel_in);
           END IF;
           IF l_name = 'EDN_PROF_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_PROF_CREATE'
               , value_in => id_prof_create_in);
           END IF;
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END ins;

   PROCEDURE ins (
      id_epis_diagnosis_notes_in IN EPIS_DIAGNOSIS_NOTES.ID_EPIS_DIAGNOSIS_NOTES%TYPE
      ,
      notes_in IN EPIS_DIAGNOSIS_NOTES.NOTES%TYPE DEFAULT NULL,
      create_user_in IN EPIS_DIAGNOSIS_NOTES.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN EPIS_DIAGNOSIS_NOTES.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN EPIS_DIAGNOSIS_NOTES.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      id_episode_in IN EPIS_DIAGNOSIS_NOTES.ID_EPISODE%TYPE DEFAULT NULL,
      dt_epis_diagnosis_notes_in IN EPIS_DIAGNOSIS_NOTES.DT_EPIS_DIAGNOSIS_NOTES%TYPE DEFAULT NULL,
      id_prof_create_in IN EPIS_DIAGNOSIS_NOTES.ID_PROF_CREATE%TYPE DEFAULT NULL,
      id_cancel_reason_in IN EPIS_DIAGNOSIS_NOTES.ID_CANCEL_REASON%TYPE DEFAULT NULL,
      notes_cancel_in IN EPIS_DIAGNOSIS_NOTES.NOTES_CANCEL%TYPE DEFAULT NULL,
      id_prof_cancel_in IN EPIS_DIAGNOSIS_NOTES.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      dt_cancel_in IN EPIS_DIAGNOSIS_NOTES.DT_CANCEL%TYPE DEFAULT NULL,
      dt_create_in IN EPIS_DIAGNOSIS_NOTES.DT_CREATE%TYPE DEFAULT NULL
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN

     ins (
      id_epis_diagnosis_notes_in => id_epis_diagnosis_notes_in
      ,
      notes_in => notes_in,
      create_user_in => create_user_in,
      create_time_in => create_time_in,
      create_institution_in => create_institution_in,
      update_user_in => update_user_in,
      update_time_in => update_time_in,
      update_institution_in => update_institution_in,
      id_episode_in => id_episode_in,
      dt_epis_diagnosis_notes_in => dt_epis_diagnosis_notes_in,
      id_prof_create_in => id_prof_create_in,
      id_cancel_reason_in => id_cancel_reason_in,
      notes_cancel_in => notes_cancel_in,
      id_prof_cancel_in => id_prof_cancel_in,
      dt_cancel_in => dt_cancel_in,
      dt_create_in => dt_create_in
     ,handle_error_in => handle_error_in
      ,rows_out => rows_out
      );
   END ins;


   PROCEDURE ins (
      rec_in IN EPIS_DIAGNOSIS_NOTES%ROWTYPE
     ,gen_pky_in IN BOOLEAN DEFAULT FALSE
     ,sequence_in IN VARCHAR2 := NULL
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
      l_rec EPIS_DIAGNOSIS_NOTES%ROWTYPE := rec_in;
   BEGIN
      IF gen_pky_in THEN
         l_rec.ID_EPIS_DIAGNOSIS_NOTES := next_key (sequence_in);
      END IF;
      ins (
         id_epis_diagnosis_notes_in => l_rec.ID_EPIS_DIAGNOSIS_NOTES
         ,
         notes_in => l_rec.NOTES,
         create_user_in => l_rec.CREATE_USER,
         create_time_in => l_rec.CREATE_TIME,
         create_institution_in => l_rec.CREATE_INSTITUTION,
         update_user_in => l_rec.UPDATE_USER,
         update_time_in => l_rec.UPDATE_TIME,
         update_institution_in => l_rec.UPDATE_INSTITUTION,
         id_episode_in => l_rec.ID_EPISODE,
         dt_epis_diagnosis_notes_in => l_rec.DT_EPIS_DIAGNOSIS_NOTES,
         id_prof_create_in => l_rec.ID_PROF_CREATE,
         id_cancel_reason_in => l_rec.ID_CANCEL_REASON,
         notes_cancel_in => l_rec.NOTES_CANCEL,
         id_prof_cancel_in => l_rec.ID_PROF_CANCEL,
         dt_cancel_in => l_rec.DT_CANCEL,
         dt_create_in => l_rec.DT_CREATE
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
   END ins;

   PROCEDURE ins (
      rec_in IN EPIS_DIAGNOSIS_NOTES%ROWTYPE
     ,gen_pky_in IN BOOLEAN DEFAULT FALSE
     ,sequence_in IN VARCHAR2 := NULL
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
      rows_out TABLE_VARCHAR;
   BEGIN

  ins (
      rec_in => rec_in
     ,gen_pky_in => gen_pky_in
     ,sequence_in => sequence_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
   );

   END ins;

   FUNCTION next_key (sequence_in IN VARCHAR2 := NULL) RETURN EPIS_DIAGNOSIS_NOTES.ID_EPIS_DIAGNOSIS_NOTES%TYPE

   IS
     retval EPIS_DIAGNOSIS_NOTES.ID_EPIS_DIAGNOSIS_NOTES%TYPE;

   BEGIN
      IF sequence_in IS NULL
      THEN
         SELECT seq_EPIS_DIAGNOSIS_NOTES.NEXTVAL INTO retval FROM dual;
      ELSE
         EXECUTE IMMEDIATE
            'SELECT ' || sequence_in || '.NEXTVAL FROM dual'
            INTO retval;
      END IF;
      RETURN retval;
   EXCEPTION
      WHEN OTHERS THEN
        pk_alert_exceptions.raise_error (
           error_name_in => 'SEQUENCE-GENERATION-FAILURE'
           ,name1_in => 'SEQUENCE'
           ,value1_in => NVL (sequence_in, 'seq_EPIS_DIAGNOSIS_NOTES')
           );
   END next_key;

   PROCEDURE ins (
      notes_in IN EPIS_DIAGNOSIS_NOTES.NOTES%TYPE DEFAULT NULL,
      create_user_in IN EPIS_DIAGNOSIS_NOTES.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN EPIS_DIAGNOSIS_NOTES.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN EPIS_DIAGNOSIS_NOTES.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      id_episode_in IN EPIS_DIAGNOSIS_NOTES.ID_EPISODE%TYPE DEFAULT NULL,
      dt_epis_diagnosis_notes_in IN EPIS_DIAGNOSIS_NOTES.DT_EPIS_DIAGNOSIS_NOTES%TYPE DEFAULT NULL,
      id_prof_create_in IN EPIS_DIAGNOSIS_NOTES.ID_PROF_CREATE%TYPE DEFAULT NULL,
      id_cancel_reason_in IN EPIS_DIAGNOSIS_NOTES.ID_CANCEL_REASON%TYPE DEFAULT NULL,
      notes_cancel_in IN EPIS_DIAGNOSIS_NOTES.NOTES_CANCEL%TYPE DEFAULT NULL,
      id_prof_cancel_in IN EPIS_DIAGNOSIS_NOTES.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      dt_cancel_in IN EPIS_DIAGNOSIS_NOTES.DT_CANCEL%TYPE DEFAULT NULL,
      dt_create_in IN EPIS_DIAGNOSIS_NOTES.DT_CREATE%TYPE DEFAULT NULL,
      id_epis_diagnosis_notes_out IN OUT EPIS_DIAGNOSIS_NOTES.ID_EPIS_DIAGNOSIS_NOTES%TYPE
      ,handle_error_in IN BOOLEAN := TRUE
      , rows_out OUT TABLE_VARCHAR
   )
   IS
        l_pky EPIS_DIAGNOSIS_NOTES.ID_EPIS_DIAGNOSIS_NOTES%TYPE := next_key;
   BEGIN
      ins (
         id_epis_diagnosis_notes_in => l_pky,
         notes_in => notes_in,
         create_user_in => create_user_in,
         create_time_in => create_time_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in,
         id_episode_in => id_episode_in,
         dt_epis_diagnosis_notes_in => dt_epis_diagnosis_notes_in,
         id_prof_create_in => id_prof_create_in,
         id_cancel_reason_in => id_cancel_reason_in,
         notes_cancel_in => notes_cancel_in,
         id_prof_cancel_in => id_prof_cancel_in,
         dt_cancel_in => dt_cancel_in,
         dt_create_in => dt_create_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
      id_epis_diagnosis_notes_out := l_pky;
   END ins;

   PROCEDURE ins (
      notes_in IN EPIS_DIAGNOSIS_NOTES.NOTES%TYPE DEFAULT NULL,
      create_user_in IN EPIS_DIAGNOSIS_NOTES.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN EPIS_DIAGNOSIS_NOTES.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN EPIS_DIAGNOSIS_NOTES.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      id_episode_in IN EPIS_DIAGNOSIS_NOTES.ID_EPISODE%TYPE DEFAULT NULL,
      dt_epis_diagnosis_notes_in IN EPIS_DIAGNOSIS_NOTES.DT_EPIS_DIAGNOSIS_NOTES%TYPE DEFAULT NULL,
      id_prof_create_in IN EPIS_DIAGNOSIS_NOTES.ID_PROF_CREATE%TYPE DEFAULT NULL,
      id_cancel_reason_in IN EPIS_DIAGNOSIS_NOTES.ID_CANCEL_REASON%TYPE DEFAULT NULL,
      notes_cancel_in IN EPIS_DIAGNOSIS_NOTES.NOTES_CANCEL%TYPE DEFAULT NULL,
      id_prof_cancel_in IN EPIS_DIAGNOSIS_NOTES.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      dt_cancel_in IN EPIS_DIAGNOSIS_NOTES.DT_CANCEL%TYPE DEFAULT NULL,
      dt_create_in IN EPIS_DIAGNOSIS_NOTES.DT_CREATE%TYPE DEFAULT NULL,
      id_epis_diagnosis_notes_out IN OUT EPIS_DIAGNOSIS_NOTES.ID_EPIS_DIAGNOSIS_NOTES%TYPE
      ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
        rows_out TABLE_VARCHAR;
   BEGIN
      ins (
      notes_in => notes_in,
      create_user_in => create_user_in,
      create_time_in => create_time_in,
      create_institution_in => create_institution_in,
      update_user_in => update_user_in,
      update_time_in => update_time_in,
      update_institution_in => update_institution_in,
      id_episode_in => id_episode_in,
      dt_epis_diagnosis_notes_in => dt_epis_diagnosis_notes_in,
      id_prof_create_in => id_prof_create_in,
      id_cancel_reason_in => id_cancel_reason_in,
      notes_cancel_in => notes_cancel_in,
      id_prof_cancel_in => id_prof_cancel_in,
      dt_cancel_in => dt_cancel_in,
      dt_create_in => dt_create_in,
      id_epis_diagnosis_notes_out => id_epis_diagnosis_notes_out
      ,handle_error_in => handle_error_in
      , rows_out => rows_out
   );
   END ins;

   FUNCTION ins (
      notes_in IN EPIS_DIAGNOSIS_NOTES.NOTES%TYPE DEFAULT NULL,
      create_user_in IN EPIS_DIAGNOSIS_NOTES.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN EPIS_DIAGNOSIS_NOTES.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN EPIS_DIAGNOSIS_NOTES.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      id_episode_in IN EPIS_DIAGNOSIS_NOTES.ID_EPISODE%TYPE DEFAULT NULL,
      dt_epis_diagnosis_notes_in IN EPIS_DIAGNOSIS_NOTES.DT_EPIS_DIAGNOSIS_NOTES%TYPE DEFAULT NULL,
      id_prof_create_in IN EPIS_DIAGNOSIS_NOTES.ID_PROF_CREATE%TYPE DEFAULT NULL,
      id_cancel_reason_in IN EPIS_DIAGNOSIS_NOTES.ID_CANCEL_REASON%TYPE DEFAULT NULL,
      notes_cancel_in IN EPIS_DIAGNOSIS_NOTES.NOTES_CANCEL%TYPE DEFAULT NULL,
      id_prof_cancel_in IN EPIS_DIAGNOSIS_NOTES.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      dt_cancel_in IN EPIS_DIAGNOSIS_NOTES.DT_CANCEL%TYPE DEFAULT NULL,
      dt_create_in IN EPIS_DIAGNOSIS_NOTES.DT_CREATE%TYPE DEFAULT NULL
      ,handle_error_in IN BOOLEAN := TRUE
      , rows_out OUT TABLE_VARCHAR
   )
      RETURN
         EPIS_DIAGNOSIS_NOTES.ID_EPIS_DIAGNOSIS_NOTES%TYPE
   IS
        l_pky EPIS_DIAGNOSIS_NOTES.ID_EPIS_DIAGNOSIS_NOTES%TYPE := next_key;
   BEGIN
      ins (
         id_epis_diagnosis_notes_in => l_pky,
         notes_in => notes_in,
         create_user_in => create_user_in,
         create_time_in => create_time_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in,
         id_episode_in => id_episode_in,
         dt_epis_diagnosis_notes_in => dt_epis_diagnosis_notes_in,
         id_prof_create_in => id_prof_create_in,
         id_cancel_reason_in => id_cancel_reason_in,
         notes_cancel_in => notes_cancel_in,
         id_prof_cancel_in => id_prof_cancel_in,
         dt_cancel_in => dt_cancel_in,
         dt_create_in => dt_create_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
      RETURN l_pky;
   END ins;

   FUNCTION ins (
      notes_in IN EPIS_DIAGNOSIS_NOTES.NOTES%TYPE DEFAULT NULL,
      create_user_in IN EPIS_DIAGNOSIS_NOTES.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN EPIS_DIAGNOSIS_NOTES.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN EPIS_DIAGNOSIS_NOTES.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      id_episode_in IN EPIS_DIAGNOSIS_NOTES.ID_EPISODE%TYPE DEFAULT NULL,
      dt_epis_diagnosis_notes_in IN EPIS_DIAGNOSIS_NOTES.DT_EPIS_DIAGNOSIS_NOTES%TYPE DEFAULT NULL,
      id_prof_create_in IN EPIS_DIAGNOSIS_NOTES.ID_PROF_CREATE%TYPE DEFAULT NULL,
      id_cancel_reason_in IN EPIS_DIAGNOSIS_NOTES.ID_CANCEL_REASON%TYPE DEFAULT NULL,
      notes_cancel_in IN EPIS_DIAGNOSIS_NOTES.NOTES_CANCEL%TYPE DEFAULT NULL,
      id_prof_cancel_in IN EPIS_DIAGNOSIS_NOTES.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      dt_cancel_in IN EPIS_DIAGNOSIS_NOTES.DT_CANCEL%TYPE DEFAULT NULL,
      dt_create_in IN EPIS_DIAGNOSIS_NOTES.DT_CREATE%TYPE DEFAULT NULL
      ,handle_error_in IN BOOLEAN := TRUE
   )
      RETURN
         EPIS_DIAGNOSIS_NOTES.ID_EPIS_DIAGNOSIS_NOTES%TYPE
   IS
        l_pky EPIS_DIAGNOSIS_NOTES.ID_EPIS_DIAGNOSIS_NOTES%TYPE := next_key;
    rows_out TABLE_VARCHAR;
   BEGIN
      ins (
         id_epis_diagnosis_notes_in => l_pky,
         notes_in => notes_in,
         create_user_in => create_user_in,
         create_time_in => create_time_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in,
         id_episode_in => id_episode_in,
         dt_epis_diagnosis_notes_in => dt_epis_diagnosis_notes_in,
         id_prof_create_in => id_prof_create_in,
         id_cancel_reason_in => id_cancel_reason_in,
         notes_cancel_in => notes_cancel_in,
         id_prof_cancel_in => id_prof_cancel_in,
         dt_cancel_in => dt_cancel_in,
         dt_create_in => dt_create_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
      RETURN l_pky;
   END ins;

      PROCEDURE ins (
      notes_in IN EPIS_DIAGNOSIS_NOTES.NOTES%TYPE DEFAULT NULL,
      create_user_in IN EPIS_DIAGNOSIS_NOTES.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN EPIS_DIAGNOSIS_NOTES.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN EPIS_DIAGNOSIS_NOTES.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      id_episode_in IN EPIS_DIAGNOSIS_NOTES.ID_EPISODE%TYPE DEFAULT NULL,
      dt_epis_diagnosis_notes_in IN EPIS_DIAGNOSIS_NOTES.DT_EPIS_DIAGNOSIS_NOTES%TYPE DEFAULT NULL,
      id_prof_create_in IN EPIS_DIAGNOSIS_NOTES.ID_PROF_CREATE%TYPE DEFAULT NULL,
      id_cancel_reason_in IN EPIS_DIAGNOSIS_NOTES.ID_CANCEL_REASON%TYPE DEFAULT NULL,
      notes_cancel_in IN EPIS_DIAGNOSIS_NOTES.NOTES_CANCEL%TYPE DEFAULT NULL,
      id_prof_cancel_in IN EPIS_DIAGNOSIS_NOTES.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      dt_cancel_in IN EPIS_DIAGNOSIS_NOTES.DT_CANCEL%TYPE DEFAULT NULL,
      dt_create_in IN EPIS_DIAGNOSIS_NOTES.DT_CREATE%TYPE DEFAULT NULL
      ,handle_error_in IN BOOLEAN := TRUE
      , rows_out OUT TABLE_VARCHAR
   )
   IS
        l_pky EPIS_DIAGNOSIS_NOTES.ID_EPIS_DIAGNOSIS_NOTES%TYPE := next_key;
   BEGIN
      ins (
         id_epis_diagnosis_notes_in => l_pky,
         notes_in => notes_in,
         create_user_in => create_user_in,
         create_time_in => create_time_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in,
         id_episode_in => id_episode_in,
         dt_epis_diagnosis_notes_in => dt_epis_diagnosis_notes_in,
         id_prof_create_in => id_prof_create_in,
         id_cancel_reason_in => id_cancel_reason_in,
         notes_cancel_in => notes_cancel_in,
         id_prof_cancel_in => id_prof_cancel_in,
         dt_cancel_in => dt_cancel_in,
         dt_create_in => dt_create_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
   END ins;


     PROCEDURE ins (
      notes_in IN EPIS_DIAGNOSIS_NOTES.NOTES%TYPE DEFAULT NULL,
      create_user_in IN EPIS_DIAGNOSIS_NOTES.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN EPIS_DIAGNOSIS_NOTES.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN EPIS_DIAGNOSIS_NOTES.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      id_episode_in IN EPIS_DIAGNOSIS_NOTES.ID_EPISODE%TYPE DEFAULT NULL,
      dt_epis_diagnosis_notes_in IN EPIS_DIAGNOSIS_NOTES.DT_EPIS_DIAGNOSIS_NOTES%TYPE DEFAULT NULL,
      id_prof_create_in IN EPIS_DIAGNOSIS_NOTES.ID_PROF_CREATE%TYPE DEFAULT NULL,
      id_cancel_reason_in IN EPIS_DIAGNOSIS_NOTES.ID_CANCEL_REASON%TYPE DEFAULT NULL,
      notes_cancel_in IN EPIS_DIAGNOSIS_NOTES.NOTES_CANCEL%TYPE DEFAULT NULL,
      id_prof_cancel_in IN EPIS_DIAGNOSIS_NOTES.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      dt_cancel_in IN EPIS_DIAGNOSIS_NOTES.DT_CANCEL%TYPE DEFAULT NULL,
      dt_create_in IN EPIS_DIAGNOSIS_NOTES.DT_CREATE%TYPE DEFAULT NULL
      ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
        l_pky EPIS_DIAGNOSIS_NOTES.ID_EPIS_DIAGNOSIS_NOTES%TYPE := next_key;
      rows_out TABLE_VARCHAR;
   BEGIN
      ins (
         id_epis_diagnosis_notes_in => l_pky,
         notes_in => notes_in,
         create_user_in => create_user_in,
         create_time_in => create_time_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in,
         id_episode_in => id_episode_in,
         dt_epis_diagnosis_notes_in => dt_epis_diagnosis_notes_in,
         id_prof_create_in => id_prof_create_in,
         id_cancel_reason_in => id_cancel_reason_in,
         notes_cancel_in => notes_cancel_in,
         id_prof_cancel_in => id_prof_cancel_in,
         dt_cancel_in => dt_cancel_in,
         dt_create_in => dt_create_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
   END ins;

    PROCEDURE ins (
      rows_in IN EPIS_DIAGNOSIS_NOTES_tc
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
   BEGIN
      IF rows_in.COUNT = 0
      THEN
         NULL;
      ELSE
         FORALL indx IN rows_in.FIRST .. rows_in.LAST
            SAVE EXCEPTIONS
            INSERT INTO EPIS_DIAGNOSIS_NOTES VALUES rows_in (indx) RETURNING ROWID BULK COLLECT INTO rows_out;
      END IF;
   EXCEPTION
     WHEN e_forall_error
     THEN
        -- In Oracle9i and above, SAVE EXCEPTIONS will direct control
        -- here if any error occurs. We can then save all the error
        -- information out to the error instance.
       IF NOT handle_error_in THEN RAISE;
       ELSE
          <<bulk_handler>>
          DECLARE
             l_err_instance_id NUMBER;
          BEGIN
             -- For each error, write to the log.
             FOR indx IN 1 .. SQL%BULK_EXCEPTIONS.COUNT
             LOOP
                pk_alert_exceptions.register_error (
                    error_name_in => 'FORALL-INSERT-FAILURE'
                   ,err_instance_id_out => l_err_instance_id
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'BINDING_ROW_' || indx
                  ,value_in => SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX
                  ,validate_in => FALSE
                );
                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ERROR_AT_ROW_' || indx
                  ,value_in => SQL%BULK_EXCEPTIONS (indx).ERROR_CODE
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_EPIS_DIAGNOSIS_NOTES _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).ID_EPIS_DIAGNOSIS_NOTES
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'NOTES _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).NOTES
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'CREATE_USER _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).CREATE_USER
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'CREATE_TIME _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).CREATE_TIME
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'CREATE_INSTITUTION _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).CREATE_INSTITUTION
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'UPDATE_USER _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).UPDATE_USER
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'UPDATE_TIME _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).UPDATE_TIME
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'UPDATE_INSTITUTION _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).UPDATE_INSTITUTION
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_EPISODE _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).ID_EPISODE
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'DT_EPIS_DIAGNOSIS_NOTES _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).DT_EPIS_DIAGNOSIS_NOTES
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_PROF_CREATE _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).ID_PROF_CREATE
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_CANCEL_REASON _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).ID_CANCEL_REASON
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'NOTES_CANCEL _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).NOTES_CANCEL
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_PROF_CANCEL _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).ID_PROF_CANCEL
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'DT_CANCEL _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).DT_CANCEL
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'DT_CREATE _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).DT_CREATE
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.raise_error_instance( err_instance_id_in => l_err_instance_id );
             END LOOP;
          END bulk_handler;
        END IF;
     WHEN OTHERS
     THEN
       IF NOT handle_error_in THEN RAISE;
       ELSE
       pk_alert_exceptions.raise_error(
          error_name_in => 'FORALL-INSERT-FAILURE'
          ,name1_in => 'TABLE_NAME'
          ,value1_in => 'EPIS_DIAGNOSIS_NOTES'
          ,name2_in => 'ROW_COUNT'
          ,value2_in => rows_in.COUNT
           );
       END IF;
   END ins;

    PROCEDURE ins (
      rows_in IN EPIS_DIAGNOSIS_NOTES_tc
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
      rows_out TABLE_VARCHAR;
   BEGIN
      ins (
      rows_in => rows_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
   );
   END ins;


PROCEDURE upd (
      id_epis_diagnosis_notes_in IN EPIS_DIAGNOSIS_NOTES.ID_EPIS_DIAGNOSIS_NOTES%TYPE,
      notes_in IN EPIS_DIAGNOSIS_NOTES.NOTES%TYPE DEFAULT NULL,
      NOTES_nin IN BOOLEAN := TRUE,
      create_user_in IN EPIS_DIAGNOSIS_NOTES.CREATE_USER%TYPE DEFAULT NULL,
      CREATE_USER_nin IN BOOLEAN := TRUE,
      create_time_in IN EPIS_DIAGNOSIS_NOTES.CREATE_TIME%TYPE DEFAULT NULL,
      CREATE_TIME_nin IN BOOLEAN := TRUE,
      create_institution_in IN EPIS_DIAGNOSIS_NOTES.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      CREATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      update_user_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_USER%TYPE DEFAULT NULL,
      UPDATE_USER_nin IN BOOLEAN := TRUE,
      update_time_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_TIME%TYPE DEFAULT NULL,
      UPDATE_TIME_nin IN BOOLEAN := TRUE,
      update_institution_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      UPDATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      id_episode_in IN EPIS_DIAGNOSIS_NOTES.ID_EPISODE%TYPE DEFAULT NULL,
      ID_EPISODE_nin IN BOOLEAN := TRUE,
      dt_epis_diagnosis_notes_in IN EPIS_DIAGNOSIS_NOTES.DT_EPIS_DIAGNOSIS_NOTES%TYPE DEFAULT NULL,
      DT_EPIS_DIAGNOSIS_NOTES_nin IN BOOLEAN := TRUE,
      id_prof_create_in IN EPIS_DIAGNOSIS_NOTES.ID_PROF_CREATE%TYPE DEFAULT NULL,
      ID_PROF_CREATE_nin IN BOOLEAN := TRUE,
      id_cancel_reason_in IN EPIS_DIAGNOSIS_NOTES.ID_CANCEL_REASON%TYPE DEFAULT NULL,
      ID_CANCEL_REASON_nin IN BOOLEAN := TRUE,
      notes_cancel_in IN EPIS_DIAGNOSIS_NOTES.NOTES_CANCEL%TYPE DEFAULT NULL,
      NOTES_CANCEL_nin IN BOOLEAN := TRUE,
      id_prof_cancel_in IN EPIS_DIAGNOSIS_NOTES.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      ID_PROF_CANCEL_nin IN BOOLEAN := TRUE,
      dt_cancel_in IN EPIS_DIAGNOSIS_NOTES.DT_CANCEL%TYPE DEFAULT NULL,
      DT_CANCEL_nin IN BOOLEAN := TRUE,
      dt_create_in IN EPIS_DIAGNOSIS_NOTES.DT_CREATE%TYPE DEFAULT NULL,
      DT_CREATE_nin IN BOOLEAN := TRUE,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      )
   IS
   l_rows_out TABLE_VARCHAR;
   l_NOTES_n NUMBER(1);
   l_CREATE_USER_n NUMBER(1);
   l_CREATE_TIME_n NUMBER(1);
   l_CREATE_INSTITUTION_n NUMBER(1);
   l_UPDATE_USER_n NUMBER(1);
   l_UPDATE_TIME_n NUMBER(1);
   l_UPDATE_INSTITUTION_n NUMBER(1);
   l_ID_EPISODE_n NUMBER(1);
   l_DT_EPIS_DIAGNOSIS_NOTES_n NUMBER(1);
   l_ID_PROF_CREATE_n NUMBER(1);
   l_ID_CANCEL_REASON_n NUMBER(1);
   l_NOTES_CANCEL_n NUMBER(1);
   l_ID_PROF_CANCEL_n NUMBER(1);
   l_DT_CANCEL_n NUMBER(1);
   l_DT_CREATE_n NUMBER(1);
   BEGIN

   l_NOTES_n := sys.diutil.bool_to_int(NOTES_nin);
   l_CREATE_USER_n := sys.diutil.bool_to_int(CREATE_USER_nin);
   l_CREATE_TIME_n := sys.diutil.bool_to_int(CREATE_TIME_nin);
   l_CREATE_INSTITUTION_n := sys.diutil.bool_to_int(CREATE_INSTITUTION_nin);
   l_UPDATE_USER_n := sys.diutil.bool_to_int(UPDATE_USER_nin);
   l_UPDATE_TIME_n := sys.diutil.bool_to_int(UPDATE_TIME_nin);
   l_UPDATE_INSTITUTION_n := sys.diutil.bool_to_int(UPDATE_INSTITUTION_nin);
   l_ID_EPISODE_n := sys.diutil.bool_to_int(ID_EPISODE_nin);
   l_DT_EPIS_DIAGNOSIS_NOTES_n := sys.diutil.bool_to_int(DT_EPIS_DIAGNOSIS_NOTES_nin);
   l_ID_PROF_CREATE_n := sys.diutil.bool_to_int(ID_PROF_CREATE_nin);
   l_ID_CANCEL_REASON_n := sys.diutil.bool_to_int(ID_CANCEL_REASON_nin);
   l_NOTES_CANCEL_n := sys.diutil.bool_to_int(NOTES_CANCEL_nin);
   l_ID_PROF_CANCEL_n := sys.diutil.bool_to_int(ID_PROF_CANCEL_nin);
   l_DT_CANCEL_n := sys.diutil.bool_to_int(DT_CANCEL_nin);
   l_DT_CREATE_n := sys.diutil.bool_to_int(DT_CREATE_nin);


         UPDATE EPIS_DIAGNOSIS_NOTES SET
     NOTES = decode (l_NOTES_n,0,notes_in, NVL (notes_in, NOTES)),
     CREATE_USER = decode (l_CREATE_USER_n,0,create_user_in, NVL (create_user_in, CREATE_USER)),
     CREATE_TIME = decode (l_CREATE_TIME_n,0,create_time_in, NVL (create_time_in, CREATE_TIME)),
     CREATE_INSTITUTION = decode (l_CREATE_INSTITUTION_n,0,create_institution_in, NVL (create_institution_in, CREATE_INSTITUTION)),
     UPDATE_USER = decode (l_UPDATE_USER_n,0,update_user_in, NVL (update_user_in, UPDATE_USER)),
     UPDATE_TIME = decode (l_UPDATE_TIME_n,0,update_time_in, NVL (update_time_in, UPDATE_TIME)),
     UPDATE_INSTITUTION = decode (l_UPDATE_INSTITUTION_n,0,update_institution_in, NVL (update_institution_in, UPDATE_INSTITUTION)),
     ID_EPISODE = decode (l_ID_EPISODE_n,0,id_episode_in, NVL (id_episode_in, ID_EPISODE)),
     DT_EPIS_DIAGNOSIS_NOTES = decode (l_DT_EPIS_DIAGNOSIS_NOTES_n,0,dt_epis_diagnosis_notes_in, NVL (dt_epis_diagnosis_notes_in, DT_EPIS_DIAGNOSIS_NOTES)),
     ID_PROF_CREATE = decode (l_ID_PROF_CREATE_n,0,id_prof_create_in, NVL (id_prof_create_in, ID_PROF_CREATE)),
     ID_CANCEL_REASON = decode (l_ID_CANCEL_REASON_n,0,id_cancel_reason_in, NVL (id_cancel_reason_in, ID_CANCEL_REASON)),
     NOTES_CANCEL = decode (l_NOTES_CANCEL_n,0,notes_cancel_in, NVL (notes_cancel_in, NOTES_CANCEL)),
     ID_PROF_CANCEL = decode (l_ID_PROF_CANCEL_n,0,id_prof_cancel_in, NVL (id_prof_cancel_in, ID_PROF_CANCEL)),
     DT_CANCEL = decode (l_DT_CANCEL_n,0,dt_cancel_in, NVL (dt_cancel_in, DT_CANCEL)),
     DT_CREATE = decode (l_DT_CREATE_n,0,dt_create_in, NVL (dt_create_in, DT_CREATE))
          WHERE
             ID_EPIS_DIAGNOSIS_NOTES = id_epis_diagnosis_notes_in
         RETURNING ROWID BULK COLLECT INTO l_rows_out;


if(rows_out is null)
then
rows_out := table_varchar();
end if;

rows_out :=  rows_out MULTISET UNION DISTINCT l_rows_out;

   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF FALSE THEN NULL; -- Placeholder in case no unique indexes
           ELSE
              pk_alert_exceptions.raise_error (
                    error_name_in => 'DUPLICATE-VALUE'
                    ,name1_in => 'OWNER'
                    ,value1_in => l_owner
                    ,name2_in => 'CONSTRAINT_NAME'
                    ,value2_in => l_name
                    ,name3_in => 'TABLE_NAME'
                    ,value3_in => 'EPIS_DIAGNOSIS_NOTES');
           END IF;
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'EPIS_DIAGNOSIS_NOTES');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'EPIS_DIAGNOSIS_NOTES');
           IF l_name = 'EDN_CR_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_CANCEL_REASON'
               , value_in => id_cancel_reason_in);
           END IF;
           IF l_name = 'EDN_EPIS_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_EPISODE'
               , value_in => id_episode_in);
           END IF;
           IF l_name = 'EDN_PROF_CANCEL_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_PROF_CANCEL'
               , value_in => id_prof_cancel_in);
           END IF;
           IF l_name = 'EDN_PROF_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_PROF_CREATE'
               , value_in => id_prof_create_in);
           END IF;
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END upd;


   PROCEDURE upd (
      id_epis_diagnosis_notes_in IN EPIS_DIAGNOSIS_NOTES.ID_EPIS_DIAGNOSIS_NOTES%TYPE,
      notes_in IN EPIS_DIAGNOSIS_NOTES.NOTES%TYPE DEFAULT NULL,
      NOTES_nin IN BOOLEAN := TRUE,
      create_user_in IN EPIS_DIAGNOSIS_NOTES.CREATE_USER%TYPE DEFAULT NULL,
      CREATE_USER_nin IN BOOLEAN := TRUE,
      create_time_in IN EPIS_DIAGNOSIS_NOTES.CREATE_TIME%TYPE DEFAULT NULL,
      CREATE_TIME_nin IN BOOLEAN := TRUE,
      create_institution_in IN EPIS_DIAGNOSIS_NOTES.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      CREATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      update_user_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_USER%TYPE DEFAULT NULL,
      UPDATE_USER_nin IN BOOLEAN := TRUE,
      update_time_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_TIME%TYPE DEFAULT NULL,
      UPDATE_TIME_nin IN BOOLEAN := TRUE,
      update_institution_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      UPDATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      id_episode_in IN EPIS_DIAGNOSIS_NOTES.ID_EPISODE%TYPE DEFAULT NULL,
      ID_EPISODE_nin IN BOOLEAN := TRUE,
      dt_epis_diagnosis_notes_in IN EPIS_DIAGNOSIS_NOTES.DT_EPIS_DIAGNOSIS_NOTES%TYPE DEFAULT NULL,
      DT_EPIS_DIAGNOSIS_NOTES_nin IN BOOLEAN := TRUE,
      id_prof_create_in IN EPIS_DIAGNOSIS_NOTES.ID_PROF_CREATE%TYPE DEFAULT NULL,
      ID_PROF_CREATE_nin IN BOOLEAN := TRUE,
      id_cancel_reason_in IN EPIS_DIAGNOSIS_NOTES.ID_CANCEL_REASON%TYPE DEFAULT NULL,
      ID_CANCEL_REASON_nin IN BOOLEAN := TRUE,
      notes_cancel_in IN EPIS_DIAGNOSIS_NOTES.NOTES_CANCEL%TYPE DEFAULT NULL,
      NOTES_CANCEL_nin IN BOOLEAN := TRUE,
      id_prof_cancel_in IN EPIS_DIAGNOSIS_NOTES.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      ID_PROF_CANCEL_nin IN BOOLEAN := TRUE,
      dt_cancel_in IN EPIS_DIAGNOSIS_NOTES.DT_CANCEL%TYPE DEFAULT NULL,
      DT_CANCEL_nin IN BOOLEAN := TRUE,
      dt_create_in IN EPIS_DIAGNOSIS_NOTES.DT_CREATE%TYPE DEFAULT NULL,
      DT_CREATE_nin IN BOOLEAN := TRUE,
     handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
     upd (
      id_epis_diagnosis_notes_in => id_epis_diagnosis_notes_in,
      notes_in => notes_in,
      NOTES_nin => NOTES_nin,
      create_user_in => create_user_in,
      CREATE_USER_nin => CREATE_USER_nin,
      create_time_in => create_time_in,
      CREATE_TIME_nin => CREATE_TIME_nin,
      create_institution_in => create_institution_in,
      CREATE_INSTITUTION_nin => CREATE_INSTITUTION_nin,
      update_user_in => update_user_in,
      UPDATE_USER_nin => UPDATE_USER_nin,
      update_time_in => update_time_in,
      UPDATE_TIME_nin => UPDATE_TIME_nin,
      update_institution_in => update_institution_in,
      UPDATE_INSTITUTION_nin => UPDATE_INSTITUTION_nin,
      id_episode_in => id_episode_in,
      ID_EPISODE_nin => ID_EPISODE_nin,
      dt_epis_diagnosis_notes_in => dt_epis_diagnosis_notes_in,
      DT_EPIS_DIAGNOSIS_NOTES_nin => DT_EPIS_DIAGNOSIS_NOTES_nin,
      id_prof_create_in => id_prof_create_in,
      ID_PROF_CREATE_nin => ID_PROF_CREATE_nin,
      id_cancel_reason_in => id_cancel_reason_in,
      ID_CANCEL_REASON_nin => ID_CANCEL_REASON_nin,
      notes_cancel_in => notes_cancel_in,
      NOTES_CANCEL_nin => NOTES_CANCEL_nin,
      id_prof_cancel_in => id_prof_cancel_in,
      ID_PROF_CANCEL_nin => ID_PROF_CANCEL_nin,
      dt_cancel_in => dt_cancel_in,
      DT_CANCEL_nin => DT_CANCEL_nin,
      dt_create_in => dt_create_in,
      DT_CREATE_nin => DT_CREATE_nin,
     handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END upd;

PROCEDURE upd (
      notes_in IN EPIS_DIAGNOSIS_NOTES.NOTES%TYPE DEFAULT NULL,
      NOTES_nin IN BOOLEAN := TRUE,
      create_user_in IN EPIS_DIAGNOSIS_NOTES.CREATE_USER%TYPE DEFAULT NULL,
      CREATE_USER_nin IN BOOLEAN := TRUE,
      create_time_in IN EPIS_DIAGNOSIS_NOTES.CREATE_TIME%TYPE DEFAULT NULL,
      CREATE_TIME_nin IN BOOLEAN := TRUE,
      create_institution_in IN EPIS_DIAGNOSIS_NOTES.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      CREATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      update_user_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_USER%TYPE DEFAULT NULL,
      UPDATE_USER_nin IN BOOLEAN := TRUE,
      update_time_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_TIME%TYPE DEFAULT NULL,
      UPDATE_TIME_nin IN BOOLEAN := TRUE,
      update_institution_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      UPDATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      id_episode_in IN EPIS_DIAGNOSIS_NOTES.ID_EPISODE%TYPE DEFAULT NULL,
      ID_EPISODE_nin IN BOOLEAN := TRUE,
      dt_epis_diagnosis_notes_in IN EPIS_DIAGNOSIS_NOTES.DT_EPIS_DIAGNOSIS_NOTES%TYPE DEFAULT NULL,
      DT_EPIS_DIAGNOSIS_NOTES_nin IN BOOLEAN := TRUE,
      id_prof_create_in IN EPIS_DIAGNOSIS_NOTES.ID_PROF_CREATE%TYPE DEFAULT NULL,
      ID_PROF_CREATE_nin IN BOOLEAN := TRUE,
      id_cancel_reason_in IN EPIS_DIAGNOSIS_NOTES.ID_CANCEL_REASON%TYPE DEFAULT NULL,
      ID_CANCEL_REASON_nin IN BOOLEAN := TRUE,
      notes_cancel_in IN EPIS_DIAGNOSIS_NOTES.NOTES_CANCEL%TYPE DEFAULT NULL,
      NOTES_CANCEL_nin IN BOOLEAN := TRUE,
      id_prof_cancel_in IN EPIS_DIAGNOSIS_NOTES.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      ID_PROF_CANCEL_nin IN BOOLEAN := TRUE,
      dt_cancel_in IN EPIS_DIAGNOSIS_NOTES.DT_CANCEL%TYPE DEFAULT NULL,
      DT_CANCEL_nin IN BOOLEAN := TRUE,
      dt_create_in IN EPIS_DIAGNOSIS_NOTES.DT_CREATE%TYPE DEFAULT NULL,
      DT_CREATE_nin IN BOOLEAN := TRUE,
    where_in varchar2,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      )
   IS
   l_sql VARCHAR2(32767);
   l_rows_out TABLE_VARCHAR;
   l_NOTES_n NUMBER(1);
   l_CREATE_USER_n NUMBER(1);
   l_CREATE_TIME_n NUMBER(1);
   l_CREATE_INSTITUTION_n NUMBER(1);
   l_UPDATE_USER_n NUMBER(1);
   l_UPDATE_TIME_n NUMBER(1);
   l_UPDATE_INSTITUTION_n NUMBER(1);
   l_ID_EPISODE_n NUMBER(1);
   l_DT_EPIS_DIAGNOSIS_NOTES_n NUMBER(1);
   l_ID_PROF_CREATE_n NUMBER(1);
   l_ID_CANCEL_REASON_n NUMBER(1);
   l_NOTES_CANCEL_n NUMBER(1);
   l_ID_PROF_CANCEL_n NUMBER(1);
   l_DT_CANCEL_n NUMBER(1);
   l_DT_CREATE_n NUMBER(1);
      id_epis_diagnosis_notes_in EPIS_DIAGNOSIS_NOTES.ID_EPIS_DIAGNOSIS_NOTES%TYPE;
   BEGIN



      l_NOTES_n := sys.diutil.bool_to_int(NOTES_nin);
      l_CREATE_USER_n := sys.diutil.bool_to_int(CREATE_USER_nin);
      l_CREATE_TIME_n := sys.diutil.bool_to_int(CREATE_TIME_nin);
      l_CREATE_INSTITUTION_n := sys.diutil.bool_to_int(CREATE_INSTITUTION_nin);
      l_UPDATE_USER_n := sys.diutil.bool_to_int(UPDATE_USER_nin);
      l_UPDATE_TIME_n := sys.diutil.bool_to_int(UPDATE_TIME_nin);
      l_UPDATE_INSTITUTION_n := sys.diutil.bool_to_int(UPDATE_INSTITUTION_nin);
      l_ID_EPISODE_n := sys.diutil.bool_to_int(ID_EPISODE_nin);
      l_DT_EPIS_DIAGNOSIS_NOTES_n := sys.diutil.bool_to_int(DT_EPIS_DIAGNOSIS_NOTES_nin);
      l_ID_PROF_CREATE_n := sys.diutil.bool_to_int(ID_PROF_CREATE_nin);
      l_ID_CANCEL_REASON_n := sys.diutil.bool_to_int(ID_CANCEL_REASON_nin);
      l_NOTES_CANCEL_n := sys.diutil.bool_to_int(NOTES_CANCEL_nin);
      l_ID_PROF_CANCEL_n := sys.diutil.bool_to_int(ID_PROF_CANCEL_nin);
      l_DT_CANCEL_n := sys.diutil.bool_to_int(DT_CANCEL_nin);
      l_DT_CREATE_n := sys.diutil.bool_to_int(DT_CREATE_nin);



l_sql := 'UPDATE EPIS_DIAGNOSIS_NOTES SET '
     || ' NOTES = decode (' || l_NOTES_n || ',0,:notes_in, NVL (:notes_in, NOTES)) '|| ','
     || ' CREATE_USER = decode (' || l_CREATE_USER_n || ',0,:create_user_in, NVL (:create_user_in, CREATE_USER)) '|| ','
     || ' CREATE_TIME = decode (' || l_CREATE_TIME_n || ',0,:create_time_in, NVL (:create_time_in, CREATE_TIME)) '|| ','
     || ' CREATE_INSTITUTION = decode (' || l_CREATE_INSTITUTION_n || ',0,:create_institution_in, NVL (:create_institution_in, CREATE_INSTITUTION)) '|| ','
     || ' UPDATE_USER = decode (' || l_UPDATE_USER_n || ',0,:update_user_in, NVL (:update_user_in, UPDATE_USER)) '|| ','
     || ' UPDATE_TIME = decode (' || l_UPDATE_TIME_n || ',0,:update_time_in, NVL (:update_time_in, UPDATE_TIME)) '|| ','
     || ' UPDATE_INSTITUTION = decode (' || l_UPDATE_INSTITUTION_n || ',0,:update_institution_in, NVL (:update_institution_in, UPDATE_INSTITUTION)) '|| ','
     || ' ID_EPISODE = decode (' || l_ID_EPISODE_n || ',0,:id_episode_in, NVL (:id_episode_in, ID_EPISODE)) '|| ','
     || ' DT_EPIS_DIAGNOSIS_NOTES = decode (' || l_DT_EPIS_DIAGNOSIS_NOTES_n || ',0,:dt_epis_diagnosis_notes_in, NVL (:dt_epis_diagnosis_notes_in, DT_EPIS_DIAGNOSIS_NOTES)) '|| ','
     || ' ID_PROF_CREATE = decode (' || l_ID_PROF_CREATE_n || ',0,:id_prof_create_in, NVL (:id_prof_create_in, ID_PROF_CREATE)) '|| ','
     || ' ID_CANCEL_REASON = decode (' || l_ID_CANCEL_REASON_n || ',0,:id_cancel_reason_in, NVL (:id_cancel_reason_in, ID_CANCEL_REASON)) '|| ','
     || ' NOTES_CANCEL = decode (' || l_NOTES_CANCEL_n || ',0,:notes_cancel_in, NVL (:notes_cancel_in, NOTES_CANCEL)) '|| ','
     || ' ID_PROF_CANCEL = decode (' || l_ID_PROF_CANCEL_n || ',0,:id_prof_cancel_in, NVL (:id_prof_cancel_in, ID_PROF_CANCEL)) '|| ','
     || ' DT_CANCEL = decode (' || l_DT_CANCEL_n || ',0,:dt_cancel_in, NVL (:dt_cancel_in, DT_CANCEL)) '|| ','
     || ' DT_CREATE = decode (' || l_DT_CREATE_n || ',0,:dt_create_in, NVL (:dt_create_in, DT_CREATE)) '
      || ' where ' || nvl(where_in,'(1=1)')
      || ' RETURNING ROWID BULK COLLECT INTO :l_rows_out';




execute immediate 'BEGIN ' || l_sql || '; END;' using in
     notes_in,
     create_user_in,
     create_time_in,
     create_institution_in,
     update_user_in,
     update_time_in,
     update_institution_in,
     id_episode_in,
     dt_epis_diagnosis_notes_in,
     id_prof_create_in,
     id_cancel_reason_in,
     notes_cancel_in,
     id_prof_cancel_in,
     dt_cancel_in,
     dt_create_in,
    OUT l_rows_out;

if(rows_out is null)
then
rows_out := table_varchar();
end if;

rows_out :=  rows_out MULTISET UNION DISTINCT l_rows_out;

   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF FALSE THEN NULL; -- Placeholder in case no unique indexes
           ELSE
              pk_alert_exceptions.raise_error (
                    error_name_in => 'DUPLICATE-VALUE'
                    ,name1_in => 'OWNER'
                    ,value1_in => l_owner
                    ,name2_in => 'CONSTRAINT_NAME'
                    ,value2_in => l_name
                    ,name3_in => 'TABLE_NAME'
                    ,value3_in => 'EPIS_DIAGNOSIS_NOTES');
           END IF;
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'EPIS_DIAGNOSIS_NOTES');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'EPIS_DIAGNOSIS_NOTES');
           IF l_name = 'EDN_CR_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_CANCEL_REASON'
               , value_in => id_cancel_reason_in);
           END IF;
           IF l_name = 'EDN_EPIS_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_EPISODE'
               , value_in => id_episode_in);
           END IF;
           IF l_name = 'EDN_PROF_CANCEL_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_PROF_CANCEL'
               , value_in => id_prof_cancel_in);
           END IF;
           IF l_name = 'EDN_PROF_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_PROF_CREATE'
               , value_in => id_prof_create_in);
           END IF;
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END upd;





PROCEDURE upd (
      notes_in IN EPIS_DIAGNOSIS_NOTES.NOTES%TYPE DEFAULT NULL,
      NOTES_nin IN BOOLEAN := TRUE,
      create_user_in IN EPIS_DIAGNOSIS_NOTES.CREATE_USER%TYPE DEFAULT NULL,
      CREATE_USER_nin IN BOOLEAN := TRUE,
      create_time_in IN EPIS_DIAGNOSIS_NOTES.CREATE_TIME%TYPE DEFAULT NULL,
      CREATE_TIME_nin IN BOOLEAN := TRUE,
      create_institution_in IN EPIS_DIAGNOSIS_NOTES.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      CREATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      update_user_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_USER%TYPE DEFAULT NULL,
      UPDATE_USER_nin IN BOOLEAN := TRUE,
      update_time_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_TIME%TYPE DEFAULT NULL,
      UPDATE_TIME_nin IN BOOLEAN := TRUE,
      update_institution_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      UPDATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      id_episode_in IN EPIS_DIAGNOSIS_NOTES.ID_EPISODE%TYPE DEFAULT NULL,
      ID_EPISODE_nin IN BOOLEAN := TRUE,
      dt_epis_diagnosis_notes_in IN EPIS_DIAGNOSIS_NOTES.DT_EPIS_DIAGNOSIS_NOTES%TYPE DEFAULT NULL,
      DT_EPIS_DIAGNOSIS_NOTES_nin IN BOOLEAN := TRUE,
      id_prof_create_in IN EPIS_DIAGNOSIS_NOTES.ID_PROF_CREATE%TYPE DEFAULT NULL,
      ID_PROF_CREATE_nin IN BOOLEAN := TRUE,
      id_cancel_reason_in IN EPIS_DIAGNOSIS_NOTES.ID_CANCEL_REASON%TYPE DEFAULT NULL,
      ID_CANCEL_REASON_nin IN BOOLEAN := TRUE,
      notes_cancel_in IN EPIS_DIAGNOSIS_NOTES.NOTES_CANCEL%TYPE DEFAULT NULL,
      NOTES_CANCEL_nin IN BOOLEAN := TRUE,
      id_prof_cancel_in IN EPIS_DIAGNOSIS_NOTES.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      ID_PROF_CANCEL_nin IN BOOLEAN := TRUE,
      dt_cancel_in IN EPIS_DIAGNOSIS_NOTES.DT_CANCEL%TYPE DEFAULT NULL,
      DT_CANCEL_nin IN BOOLEAN := TRUE,
      dt_create_in IN EPIS_DIAGNOSIS_NOTES.DT_CREATE%TYPE DEFAULT NULL,
      DT_CREATE_nin IN BOOLEAN := TRUE,
    where_in varchar2,
     handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
      upd (
      notes_in => notes_in,
      NOTES_nin => NOTES_nin,
      create_user_in => create_user_in,
      CREATE_USER_nin => CREATE_USER_nin,
      create_time_in => create_time_in,
      CREATE_TIME_nin => CREATE_TIME_nin,
      create_institution_in => create_institution_in,
      CREATE_INSTITUTION_nin => CREATE_INSTITUTION_nin,
      update_user_in => update_user_in,
      UPDATE_USER_nin => UPDATE_USER_nin,
      update_time_in => update_time_in,
      UPDATE_TIME_nin => UPDATE_TIME_nin,
      update_institution_in => update_institution_in,
      UPDATE_INSTITUTION_nin => UPDATE_INSTITUTION_nin,
      id_episode_in => id_episode_in,
      ID_EPISODE_nin => ID_EPISODE_nin,
      dt_epis_diagnosis_notes_in => dt_epis_diagnosis_notes_in,
      DT_EPIS_DIAGNOSIS_NOTES_nin => DT_EPIS_DIAGNOSIS_NOTES_nin,
      id_prof_create_in => id_prof_create_in,
      ID_PROF_CREATE_nin => ID_PROF_CREATE_nin,
      id_cancel_reason_in => id_cancel_reason_in,
      ID_CANCEL_REASON_nin => ID_CANCEL_REASON_nin,
      notes_cancel_in => notes_cancel_in,
      NOTES_CANCEL_nin => NOTES_CANCEL_nin,
      id_prof_cancel_in => id_prof_cancel_in,
      ID_PROF_CANCEL_nin => ID_PROF_CANCEL_nin,
      dt_cancel_in => dt_cancel_in,
      DT_CANCEL_nin => DT_CANCEL_nin,
      dt_create_in => dt_create_in,
      DT_CREATE_nin => DT_CREATE_nin,
    where_in => where_in,
     handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END upd;

   PROCEDURE upd (
      rec_in IN EPIS_DIAGNOSIS_NOTES%ROWTYPE,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      upd (
         id_epis_diagnosis_notes_in => rec_in.ID_EPIS_DIAGNOSIS_NOTES,
         notes_in => rec_in.NOTES,
         create_user_in => rec_in.CREATE_USER,
         create_time_in => rec_in.CREATE_TIME,
         create_institution_in => rec_in.CREATE_INSTITUTION,
         update_user_in => rec_in.UPDATE_USER,
         update_time_in => rec_in.UPDATE_TIME,
         update_institution_in => rec_in.UPDATE_INSTITUTION,
         id_episode_in => rec_in.ID_EPISODE,
         dt_epis_diagnosis_notes_in => rec_in.DT_EPIS_DIAGNOSIS_NOTES,
         id_prof_create_in => rec_in.ID_PROF_CREATE,
         id_cancel_reason_in => rec_in.ID_CANCEL_REASON,
         notes_cancel_in => rec_in.NOTES_CANCEL,
         id_prof_cancel_in => rec_in.ID_PROF_CANCEL,
         dt_cancel_in => rec_in.DT_CANCEL,
         dt_create_in => rec_in.DT_CREATE

        ,handle_error_in => handle_error_in
        , rows_out => rows_out
       );
   END upd;

   PROCEDURE upd (
      rec_in IN EPIS_DIAGNOSIS_NOTES%ROWTYPE,
     handle_error_in IN BOOLEAN := TRUE
      )
   IS
        rows_out TABLE_VARCHAR;
   BEGIN
      upd (
         id_epis_diagnosis_notes_in => rec_in.ID_EPIS_DIAGNOSIS_NOTES,
         notes_in => rec_in.NOTES,
         create_user_in => rec_in.CREATE_USER,
         create_time_in => rec_in.CREATE_TIME,
         create_institution_in => rec_in.CREATE_INSTITUTION,
         update_user_in => rec_in.UPDATE_USER,
         update_time_in => rec_in.UPDATE_TIME,
         update_institution_in => rec_in.UPDATE_INSTITUTION,
         id_episode_in => rec_in.ID_EPISODE,
         dt_epis_diagnosis_notes_in => rec_in.DT_EPIS_DIAGNOSIS_NOTES,
         id_prof_create_in => rec_in.ID_PROF_CREATE,
         id_cancel_reason_in => rec_in.ID_CANCEL_REASON,
         notes_cancel_in => rec_in.NOTES_CANCEL,
         id_prof_cancel_in => rec_in.ID_PROF_CANCEL,
         dt_cancel_in => rec_in.DT_CANCEL,
         dt_create_in => rec_in.DT_CREATE

        ,handle_error_in => handle_error_in
        , rows_out => rows_out
       );
   END upd;

   PROCEDURE upd_ins (
      id_epis_diagnosis_notes_in IN EPIS_DIAGNOSIS_NOTES.ID_EPIS_DIAGNOSIS_NOTES%TYPE,
      notes_in IN EPIS_DIAGNOSIS_NOTES.NOTES%TYPE DEFAULT NULL,
      create_user_in IN EPIS_DIAGNOSIS_NOTES.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN EPIS_DIAGNOSIS_NOTES.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN EPIS_DIAGNOSIS_NOTES.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      id_episode_in IN EPIS_DIAGNOSIS_NOTES.ID_EPISODE%TYPE DEFAULT NULL,
      dt_epis_diagnosis_notes_in IN EPIS_DIAGNOSIS_NOTES.DT_EPIS_DIAGNOSIS_NOTES%TYPE DEFAULT NULL,
      id_prof_create_in IN EPIS_DIAGNOSIS_NOTES.ID_PROF_CREATE%TYPE DEFAULT NULL,
      id_cancel_reason_in IN EPIS_DIAGNOSIS_NOTES.ID_CANCEL_REASON%TYPE DEFAULT NULL,
      notes_cancel_in IN EPIS_DIAGNOSIS_NOTES.NOTES_CANCEL%TYPE DEFAULT NULL,
      id_prof_cancel_in IN EPIS_DIAGNOSIS_NOTES.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      dt_cancel_in IN EPIS_DIAGNOSIS_NOTES.DT_CANCEL%TYPE DEFAULT NULL,
      dt_create_in IN EPIS_DIAGNOSIS_NOTES.DT_CREATE%TYPE DEFAULT NULL,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      upd (
         id_epis_diagnosis_notes_in => id_epis_diagnosis_notes_in,
         notes_in => notes_in,
         create_user_in => create_user_in,
         create_time_in => create_time_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in,
         id_episode_in => id_episode_in,
         dt_epis_diagnosis_notes_in => dt_epis_diagnosis_notes_in,
         id_prof_create_in => id_prof_create_in,
         id_cancel_reason_in => id_cancel_reason_in,
         notes_cancel_in => notes_cancel_in,
         id_prof_cancel_in => id_prof_cancel_in,
         dt_cancel_in => dt_cancel_in,
         dt_create_in => dt_create_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
       );
      IF SQL%ROWCOUNT = 0
      THEN
         ins (
            id_epis_diagnosis_notes_in => id_epis_diagnosis_notes_in,
            notes_in => notes_in,
            create_user_in => create_user_in,
            create_time_in => create_time_in,
            create_institution_in => create_institution_in,
            update_user_in => update_user_in,
            update_time_in => update_time_in,
            update_institution_in => update_institution_in,
            id_episode_in => id_episode_in,
            dt_epis_diagnosis_notes_in => dt_epis_diagnosis_notes_in,
            id_prof_create_in => id_prof_create_in,
            id_cancel_reason_in => id_cancel_reason_in,
            notes_cancel_in => notes_cancel_in,
            id_prof_cancel_in => id_prof_cancel_in,
            dt_cancel_in => dt_cancel_in,
            dt_create_in => dt_create_in
            ,handle_error_in => handle_error_in
            , rows_out => rows_out
         );
      END IF;
   END upd_ins;

   PROCEDURE upd_ins (
      id_epis_diagnosis_notes_in IN EPIS_DIAGNOSIS_NOTES.ID_EPIS_DIAGNOSIS_NOTES%TYPE,
      notes_in IN EPIS_DIAGNOSIS_NOTES.NOTES%TYPE DEFAULT NULL,
      create_user_in IN EPIS_DIAGNOSIS_NOTES.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN EPIS_DIAGNOSIS_NOTES.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN EPIS_DIAGNOSIS_NOTES.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN EPIS_DIAGNOSIS_NOTES.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      id_episode_in IN EPIS_DIAGNOSIS_NOTES.ID_EPISODE%TYPE DEFAULT NULL,
      dt_epis_diagnosis_notes_in IN EPIS_DIAGNOSIS_NOTES.DT_EPIS_DIAGNOSIS_NOTES%TYPE DEFAULT NULL,
      id_prof_create_in IN EPIS_DIAGNOSIS_NOTES.ID_PROF_CREATE%TYPE DEFAULT NULL,
      id_cancel_reason_in IN EPIS_DIAGNOSIS_NOTES.ID_CANCEL_REASON%TYPE DEFAULT NULL,
      notes_cancel_in IN EPIS_DIAGNOSIS_NOTES.NOTES_CANCEL%TYPE DEFAULT NULL,
      id_prof_cancel_in IN EPIS_DIAGNOSIS_NOTES.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      dt_cancel_in IN EPIS_DIAGNOSIS_NOTES.DT_CANCEL%TYPE DEFAULT NULL,
      dt_create_in IN EPIS_DIAGNOSIS_NOTES.DT_CREATE%TYPE DEFAULT NULL,
     handle_error_in IN BOOLEAN := TRUE
      )
   IS
        rows_out TABLE_VARCHAR;
   BEGIN
      upd_ins (
      id_epis_diagnosis_notes_in,
      notes_in,
      create_user_in,
      create_time_in,
      create_institution_in,
      update_user_in,
      update_time_in,
      update_institution_in,
      id_episode_in,
      dt_epis_diagnosis_notes_in,
      id_prof_create_in,
      id_cancel_reason_in,
      notes_cancel_in,
      id_prof_cancel_in,
      dt_cancel_in,
      dt_create_in,
     handle_error_in
     ,rows_out
      );
   END upd_ins;


   PROCEDURE upd (
      col_in IN EPIS_DIAGNOSIS_NOTES_tc,
      ignore_if_null_in IN BOOLEAN := TRUE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      )
   IS
      l_ID_EPIS_DIAGNOSIS_NOTES ID_EPIS_DIAGNOSIS_NOTES_cc;
      l_NOTES NOTES_cc;
      l_CREATE_USER CREATE_USER_cc;
      l_CREATE_TIME CREATE_TIME_cc;
      l_CREATE_INSTITUTION CREATE_INSTITUTION_cc;
      l_UPDATE_USER UPDATE_USER_cc;
      l_UPDATE_TIME UPDATE_TIME_cc;
      l_UPDATE_INSTITUTION UPDATE_INSTITUTION_cc;
      l_ID_EPISODE ID_EPISODE_cc;
      l_DT_EPIS_DIAGNOSIS_NOTES DT_EPIS_DIAGNOSIS_NOTES_cc;
      l_ID_PROF_CREATE ID_PROF_CREATE_cc;
      l_ID_CANCEL_REASON ID_CANCEL_REASON_cc;
      l_NOTES_CANCEL NOTES_CANCEL_cc;
      l_ID_PROF_CANCEL ID_PROF_CANCEL_cc;
      l_DT_CANCEL DT_CANCEL_cc;
      l_DT_CREATE DT_CREATE_cc;
   BEGIN
      FOR i IN col_in.FIRST .. col_in.LAST loop
         l_ID_EPIS_DIAGNOSIS_NOTES(i) := col_in(i).ID_EPIS_DIAGNOSIS_NOTES;
         l_NOTES(i) := col_in(i).NOTES;
         l_CREATE_USER(i) := col_in(i).CREATE_USER;
         l_CREATE_TIME(i) := col_in(i).CREATE_TIME;
         l_CREATE_INSTITUTION(i) := col_in(i).CREATE_INSTITUTION;
         l_UPDATE_USER(i) := col_in(i).UPDATE_USER;
         l_UPDATE_TIME(i) := col_in(i).UPDATE_TIME;
         l_UPDATE_INSTITUTION(i) := col_in(i).UPDATE_INSTITUTION;
         l_ID_EPISODE(i) := col_in(i).ID_EPISODE;
         l_DT_EPIS_DIAGNOSIS_NOTES(i) := col_in(i).DT_EPIS_DIAGNOSIS_NOTES;
         l_ID_PROF_CREATE(i) := col_in(i).ID_PROF_CREATE;
         l_ID_CANCEL_REASON(i) := col_in(i).ID_CANCEL_REASON;
         l_NOTES_CANCEL(i) := col_in(i).NOTES_CANCEL;
         l_ID_PROF_CANCEL(i) := col_in(i).ID_PROF_CANCEL;
         l_DT_CANCEL(i) := col_in(i).DT_CANCEL;
         l_DT_CREATE(i) := col_in(i).DT_CREATE;
      END LOOP;
      IF NVL (ignore_if_null_in, FALSE)
      THEN
         -- Set any columns to their current values
         -- if incoming value is NULL.
         -- Put WHEN clause on column-level triggers!
         FORALL i IN col_in.FIRST .. col_in.LAST
            UPDATE EPIS_DIAGNOSIS_NOTES SET
               NOTES = NVL (l_NOTES(i), NOTES),
               CREATE_USER = NVL (l_CREATE_USER(i), CREATE_USER),
               CREATE_TIME = NVL (l_CREATE_TIME(i), CREATE_TIME),
               CREATE_INSTITUTION = NVL (l_CREATE_INSTITUTION(i), CREATE_INSTITUTION),
               UPDATE_USER = NVL (l_UPDATE_USER(i), UPDATE_USER),
               UPDATE_TIME = NVL (l_UPDATE_TIME(i), UPDATE_TIME),
               UPDATE_INSTITUTION = NVL (l_UPDATE_INSTITUTION(i), UPDATE_INSTITUTION),
               ID_EPISODE = NVL (l_ID_EPISODE(i), ID_EPISODE),
               DT_EPIS_DIAGNOSIS_NOTES = NVL (l_DT_EPIS_DIAGNOSIS_NOTES(i), DT_EPIS_DIAGNOSIS_NOTES),
               ID_PROF_CREATE = NVL (l_ID_PROF_CREATE(i), ID_PROF_CREATE),
               ID_CANCEL_REASON = NVL (l_ID_CANCEL_REASON(i), ID_CANCEL_REASON),
               NOTES_CANCEL = NVL (l_NOTES_CANCEL(i), NOTES_CANCEL),
               ID_PROF_CANCEL = NVL (l_ID_PROF_CANCEL(i), ID_PROF_CANCEL),
               DT_CANCEL = NVL (l_DT_CANCEL(i), DT_CANCEL),
               DT_CREATE = NVL (l_DT_CREATE(i), DT_CREATE)
             WHERE
                ID_EPIS_DIAGNOSIS_NOTES = l_ID_EPIS_DIAGNOSIS_NOTES(i)
          returning rowid bulk collect into rows_out;
      ELSE
         FORALL i IN col_in.FIRST .. col_in.LAST
            UPDATE EPIS_DIAGNOSIS_NOTES SET
               NOTES = l_NOTES(i),
               CREATE_USER = l_CREATE_USER(i),
               CREATE_TIME = l_CREATE_TIME(i),
               CREATE_INSTITUTION = l_CREATE_INSTITUTION(i),
               UPDATE_USER = l_UPDATE_USER(i),
               UPDATE_TIME = l_UPDATE_TIME(i),
               UPDATE_INSTITUTION = l_UPDATE_INSTITUTION(i),
               ID_EPISODE = l_ID_EPISODE(i),
               DT_EPIS_DIAGNOSIS_NOTES = l_DT_EPIS_DIAGNOSIS_NOTES(i),
               ID_PROF_CREATE = l_ID_PROF_CREATE(i),
               ID_CANCEL_REASON = l_ID_CANCEL_REASON(i),
               NOTES_CANCEL = l_NOTES_CANCEL(i),
               ID_PROF_CANCEL = l_ID_PROF_CANCEL(i),
               DT_CANCEL = l_DT_CANCEL(i),
               DT_CREATE = l_DT_CREATE(i)
             WHERE
                ID_EPIS_DIAGNOSIS_NOTES = l_ID_EPIS_DIAGNOSIS_NOTES(i)
        returning rowid bulk collect into rows_out  ;
      END IF;
   END upd;


   PROCEDURE upd (
      col_in IN EPIS_DIAGNOSIS_NOTES_tc,
      ignore_if_null_in IN BOOLEAN := TRUE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
     rows_out TABLE_VARCHAR;
   BEGIN
      upd (
      col_in ,
      ignore_if_null_in
     ,handle_error_in
     , rows_out
      );
   END upd;

   FUNCTION dynupdstr (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL)

   RETURN VARCHAR2
   IS
   BEGIN
      RETURN
         'BEGIN UPDATE EPIS_DIAGNOSIS_NOTES
             SET ' || colname_in || ' = :value
           WHERE ' || NVL (where_in, '1=1') || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;' ;
   END dynupdstr;

   FUNCTION dynupdstr_no_rows_out (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL)

   RETURN VARCHAR2
   IS
   BEGIN
      RETURN
         'UPDATE EPIS_DIAGNOSIS_NOTES
             SET ' || colname_in || ' = :value
           WHERE ' || NVL (where_in, '1=1');
   END dynupdstr_no_rows_out;























  PROCEDURE increment_onecol (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL
      , increment_value_in IN NUMBER DEFAULT 1
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN UPDATE EPIS_DIAGNOSIS_NOTES set ' || colname_in || '=' || colname_in || ' + ' || nvl(increment_value_in,1) || ' WHERE ' || NVL (where_in, '1=1') || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
      USING OUT rows_out;
   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'DUPLICATE-VALUE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'EPIS_DIAGNOSIS_NOTES');
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'EPIS_DIAGNOSIS_NOTES');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'EPIS_DIAGNOSIS_NOTES');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END increment_onecol;

   PROCEDURE increment_onecol (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL
     , increment_value_in IN NUMBER DEFAULT 1
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
   rows_out table_varchar;
   BEGIN
      EXECUTE IMMEDIATE 'UPDATE EPIS_DIAGNOSIS_NOTES set ' || colname_in || '=' || colname_in || ' + ' || nvl(increment_value_in,1) || ' WHERE ' || NVL (where_in, '1=1');
   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'DUPLICATE-VALUE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'EPIS_DIAGNOSIS_NOTES');
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'EPIS_DIAGNOSIS_NOTES');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'EPIS_DIAGNOSIS_NOTES');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END increment_onecol;


   -- Delete functionality


   PROCEDURE del (
      id_epis_diagnosis_notes_in IN EPIS_DIAGNOSIS_NOTES.ID_EPIS_DIAGNOSIS_NOTES%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM EPIS_DIAGNOSIS_NOTES
       WHERE
          ID_EPIS_DIAGNOSIS_NOTES = id_epis_diagnosis_notes_in
       RETURNING ROWID BULK COLLECT INTO rows_out
         ;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'EPIS_DIAGNOSIS_NOTES');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'EPIS_DIAGNOSIS_NOTES');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del;




   PROCEDURE del (
      id_epis_diagnosis_notes_in IN EPIS_DIAGNOSIS_NOTES.ID_EPIS_DIAGNOSIS_NOTES%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
rows_out TABLE_VARCHAR;
   BEGIN

del (
      id_epis_diagnosis_notes_in => id_epis_diagnosis_notes_in
     ,handle_error_in => handle_error_in
, rows_out => rows_out
      );

   END del;








   -- Delete all rows for primary key column ID_EPIS_DIAGNOSIS_NOTES
   PROCEDURE del_ID_EPIS_DIAGNOSIS_NOTES (
      id_epis_diagnosis_notes_in IN EPIS_DIAGNOSIS_NOTES.ID_EPIS_DIAGNOSIS_NOTES%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
     )
   IS
   BEGIN
      DELETE FROM EPIS_DIAGNOSIS_NOTES
       WHERE ID_EPIS_DIAGNOSIS_NOTES = id_epis_diagnosis_notes_in
      RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'EPIS_DIAGNOSIS_NOTES');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'EPIS_DIAGNOSIS_NOTES');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_ID_EPIS_DIAGNOSIS_NOTES;






   -- Delete all rows for primary key column ID_EPIS_DIAGNOSIS_NOTES
   PROCEDURE del_ID_EPIS_DIAGNOSIS_NOTES (
      id_epis_diagnosis_notes_in IN EPIS_DIAGNOSIS_NOTES.ID_EPIS_DIAGNOSIS_NOTES%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_ID_EPIS_DIAGNOSIS_NOTES (
      id_epis_diagnosis_notes_in => id_epis_diagnosis_notes_in
     ,handle_error_in => handle_error_in
, rows_out => rows_out
     );
   END del_ID_EPIS_DIAGNOSIS_NOTES;















   PROCEDURE del_EDN_CR_FK (
      id_cancel_reason_in IN EPIS_DIAGNOSIS_NOTES.ID_CANCEL_REASON%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM EPIS_DIAGNOSIS_NOTES
       WHERE
          ID_CANCEL_REASON = del_EDN_CR_FK.id_cancel_reason_in
       RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'EPIS_DIAGNOSIS_NOTES');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'EPIS_DIAGNOSIS_NOTES');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_EDN_CR_FK;



PROCEDURE del_EDN_CR_FK (
      id_cancel_reason_in IN EPIS_DIAGNOSIS_NOTES.ID_CANCEL_REASON%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_EDN_CR_FK (
      id_cancel_reason_in => id_cancel_reason_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END del_EDN_CR_FK;





   PROCEDURE del_EDN_EPIS_FK (
      id_episode_in IN EPIS_DIAGNOSIS_NOTES.ID_EPISODE%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM EPIS_DIAGNOSIS_NOTES
       WHERE
          ID_EPISODE = del_EDN_EPIS_FK.id_episode_in
       RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'EPIS_DIAGNOSIS_NOTES');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'EPIS_DIAGNOSIS_NOTES');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_EDN_EPIS_FK;



PROCEDURE del_EDN_EPIS_FK (
      id_episode_in IN EPIS_DIAGNOSIS_NOTES.ID_EPISODE%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_EDN_EPIS_FK (
      id_episode_in => id_episode_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END del_EDN_EPIS_FK;





   PROCEDURE del_EDN_PROF_CANCEL_FK (
      id_prof_cancel_in IN EPIS_DIAGNOSIS_NOTES.ID_PROF_CANCEL%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM EPIS_DIAGNOSIS_NOTES
       WHERE
          ID_PROF_CANCEL = del_EDN_PROF_CANCEL_FK.id_prof_cancel_in
       RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'EPIS_DIAGNOSIS_NOTES');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'EPIS_DIAGNOSIS_NOTES');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_EDN_PROF_CANCEL_FK;



PROCEDURE del_EDN_PROF_CANCEL_FK (
      id_prof_cancel_in IN EPIS_DIAGNOSIS_NOTES.ID_PROF_CANCEL%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_EDN_PROF_CANCEL_FK (
      id_prof_cancel_in => id_prof_cancel_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END del_EDN_PROF_CANCEL_FK;





   PROCEDURE del_EDN_PROF_FK (
      id_prof_create_in IN EPIS_DIAGNOSIS_NOTES.ID_PROF_CREATE%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM EPIS_DIAGNOSIS_NOTES
       WHERE
          ID_PROF_CREATE = del_EDN_PROF_FK.id_prof_create_in
       RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'EPIS_DIAGNOSIS_NOTES');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'EPIS_DIAGNOSIS_NOTES');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_EDN_PROF_FK;



PROCEDURE del_EDN_PROF_FK (
      id_prof_create_in IN EPIS_DIAGNOSIS_NOTES.ID_PROF_CREATE%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_EDN_PROF_FK (
      id_prof_create_in => id_prof_create_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END del_EDN_PROF_FK;












   -- Deletions using dynamic SQL
   FUNCTION dyndelstr (where_in IN VARCHAR2) RETURN VARCHAR2
   IS
   BEGIN
      IF where_in IS NULL
      THEN
         RETURN 'DELETE FROM EPIS_DIAGNOSIS_NOTES';
      ELSE
         RETURN
            'DELETE FROM EPIS_DIAGNOSIS_NOTES WHERE ' || where_in;
      END IF;
   END dyndelstr;

   FUNCTION dyncoldelstr (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE)
      RETURN VARCHAR2
   IS
   BEGIN
      RETURN dyndelstr ( colname_in || ' = :value' );
   END;

   PROCEDURE del_by (
      where_clause_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyndelstr (where_clause_in);
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'EPIS_DIAGNOSIS_NOTES');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'EPIS_DIAGNOSIS_NOTES');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by;





   PROCEDURE del_by (
      where_clause_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN ' || dyndelstr (where_clause_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;' using OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'EPIS_DIAGNOSIS_NOTES');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'EPIS_DIAGNOSIS_NOTES');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by;





   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyncoldelstr (colname_in)
         USING colvalue_in;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'EPIS_DIAGNOSIS_NOTES');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'EPIS_DIAGNOSIS_NOTES');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;






   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr (colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
         USING IN colvalue_in, OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'EPIS_DIAGNOSIS_NOTES');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'EPIS_DIAGNOSIS_NOTES');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN DATE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyncoldelstr (colname_in)
         USING colvalue_in;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'EPIS_DIAGNOSIS_NOTES');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'EPIS_DIAGNOSIS_NOTES');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN DATE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr (colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
         USING IN colvalue_in, OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'EPIS_DIAGNOSIS_NOTES');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'EPIS_DIAGNOSIS_NOTES');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN TIMESTAMP WITH LOCAL TIME ZONE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyncoldelstr (colname_in)
         USING colvalue_in;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'EPIS_DIAGNOSIS_NOTES');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'EPIS_DIAGNOSIS_NOTES');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN TIMESTAMP WITH LOCAL TIME ZONE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr (colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
         USING IN colvalue_in, OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'EPIS_DIAGNOSIS_NOTES');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'EPIS_DIAGNOSIS_NOTES');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN NUMBER
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyncoldelstr (colname_in)
         USING colvalue_in;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'EPIS_DIAGNOSIS_NOTES');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'EPIS_DIAGNOSIS_NOTES');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;






   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN NUMBER
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
   BEGIN
     EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr (colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
         USING IN colvalue_in, OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'EPIS_DIAGNOSIS_NOTES');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'EPIS_DIAGNOSIS_NOTES');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   -- Initialize a record with default values for columns in the table.
   PROCEDURE initrec (
      epis_diagnosis_notes_inout IN OUT EPIS_DIAGNOSIS_NOTES%ROWTYPE
   )
   IS
   BEGIN
      epis_diagnosis_notes_inout.ID_EPIS_DIAGNOSIS_NOTES := NULL;
      epis_diagnosis_notes_inout.NOTES := NULL;
      epis_diagnosis_notes_inout.CREATE_USER := NULL;
      epis_diagnosis_notes_inout.CREATE_TIME := NULL;
      epis_diagnosis_notes_inout.CREATE_INSTITUTION := NULL;
      epis_diagnosis_notes_inout.UPDATE_USER := NULL;
      epis_diagnosis_notes_inout.UPDATE_TIME := NULL;
      epis_diagnosis_notes_inout.UPDATE_INSTITUTION := NULL;
      epis_diagnosis_notes_inout.ID_EPISODE := NULL;
      epis_diagnosis_notes_inout.DT_EPIS_DIAGNOSIS_NOTES := NULL;
      epis_diagnosis_notes_inout.ID_PROF_CREATE := NULL;
      epis_diagnosis_notes_inout.ID_CANCEL_REASON := NULL;
      epis_diagnosis_notes_inout.NOTES_CANCEL := NULL;
      epis_diagnosis_notes_inout.ID_PROF_CANCEL := NULL;
      epis_diagnosis_notes_inout.DT_CANCEL := NULL;
      epis_diagnosis_notes_inout.DT_CREATE := NULL;
   END initrec;

   FUNCTION initrec RETURN EPIS_DIAGNOSIS_NOTES%ROWTYPE
   IS
      l_epis_diagnosis_notes EPIS_DIAGNOSIS_NOTES%ROWTYPE;
   BEGIN
      RETURN l_epis_diagnosis_notes;
   END initrec;


   FUNCTION get_data_rowid(
        rows_in IN TABLE_VARCHAR
        ) RETURN EPIS_DIAGNOSIS_NOTES_tc
   IS
        data EPIS_DIAGNOSIS_NOTES_tc;
   BEGIN
        select * bulk collect into data from EPIS_DIAGNOSIS_NOTES where rowid in (select /*+opt_estimate(table,t,scale_rows=0.0000001))*/ * from table(rows_in) t);
        return data;
        EXCEPTION
      WHEN OTHERS THEN
        pk_alert_exceptions.raise_error (
           error_name_in => 'get_data_rowid'
           );
   END get_data_rowid;


   FUNCTION get_data_rowid_pat(
        rows_in IN TABLE_VARCHAR
        ) RETURN EPIS_DIAGNOSIS_NOTES_tc
   is
        PRAGMA AUTONOMOUS_TRANSACTION;
        data EPIS_DIAGNOSIS_NOTES_tc;
   BEGIN
        data := get_data_rowid(rows_in);
        commit;
        return data;
        EXCEPTION
      WHEN OTHERS THEN
        pk_alert_exceptions.raise_error (
           error_name_in => 'get_data_rowid'
           );
        rollback;
    END get_data_rowid_pat;

BEGIN
   NULL;
END TS_EPIS_DIAGNOSIS_NOTES;
/
