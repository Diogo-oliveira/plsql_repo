/*-- Last Change Revision: $Rev: 2028264 $*/
/*-- Last Change by: $Author: mario.fernandes $*/
/*-- Date of last change: $Date: 2022-08-02 18:44:44 +0100 (ter, 02 ago 2022) $*/

CREATE OR REPLACE PACKAGE BODY TS_MONITORIZATION_VS_PLAN
/*
| Generated by or retrieved from QCGU - DO NOT MODIFY!
| QCGU - "Get it right, do it fast" - www.ToadWorld.com
| QCGU Universal ID: {657BD35E-2E48-4CD4-B14E-41420B8433B3}
| Created On: Abril 18, 2011 12:26:35
| Created By: ALERT
*/
IS

   e_null_column_value EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_null_column_value, -1400);
   --
   e_existing_fky_reference EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_existing_fky_reference, -2266);
   --
   e_check_constraint_failure EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_check_constraint_failure, -2290);
   --
   e_no_parent_key EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_no_parent_key, -2291);
   --
   e_child_record_found EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_child_record_found, -2292);
   --
   e_forall_error EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_forall_error, -24381);
   --
   -- Defined for backward compatibilty.
   e_integ_constraint_failure EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_integ_constraint_failure, -2291);

    -- Private utilities
   PROCEDURE get_constraint_info (
      owner_out OUT ALL_CONSTRAINTS.OWNER%TYPE
     ,name_out OUT ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE)
   IS
      l_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
      dotloc PLS_INTEGER;
      leftloc PLS_INTEGER;
   BEGIN
      dotloc  := INSTR (l_errm,'.');
      leftloc := INSTR (l_errm,'(');
      owner_out := SUBSTR (l_errm, leftloc+1, dotloc-leftloc-1);
      name_out  := SUBSTR (l_errm, dotloc+1, INSTR (l_errm,')')-dotloc-1);
   END get_constraint_info;
   -- Public programs

   PROCEDURE ins (
      id_monitorization_vs_plan_in IN MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS_PLAN%TYPE
      ,
      flg_status_in IN MONITORIZATION_VS_PLAN.FLG_STATUS%TYPE DEFAULT NULL,
      id_monitorization_vs_in IN MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS%TYPE DEFAULT NULL,
      dt_plan_tstz_in IN MONITORIZATION_VS_PLAN.DT_PLAN_TSTZ%TYPE DEFAULT NULL,
      id_prof_performed_in IN MONITORIZATION_VS_PLAN.ID_PROF_PERFORMED%TYPE DEFAULT NULL,
      start_time_in IN MONITORIZATION_VS_PLAN.START_TIME%TYPE DEFAULT NULL,
      end_time_in IN MONITORIZATION_VS_PLAN.END_TIME%TYPE DEFAULT NULL,
      create_user_in IN MONITORIZATION_VS_PLAN.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN MONITORIZATION_VS_PLAN.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN MONITORIZATION_VS_PLAN.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN MONITORIZATION_VS_PLAN.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN MONITORIZATION_VS_PLAN.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN MONITORIZATION_VS_PLAN.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      flg_status_prev_in IN MONITORIZATION_VS_PLAN.FLG_STATUS_PREV%TYPE DEFAULT NULL
     ,handle_error_in IN BOOLEAN := TRUE
      , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN

     INSERT INTO MONITORIZATION_VS_PLAN (
         ID_MONITORIZATION_VS_PLAN,
         FLG_STATUS,
         ID_MONITORIZATION_VS,
         DT_PLAN_TSTZ,
         ID_PROF_PERFORMED,
         START_TIME,
         END_TIME,
         CREATE_USER,
         CREATE_TIME,
         CREATE_INSTITUTION,
         UPDATE_USER,
         UPDATE_TIME,
         UPDATE_INSTITUTION,
         FLG_STATUS_PREV
         )
      VALUES (
         id_monitorization_vs_plan_in,
         flg_status_in,
         id_monitorization_vs_in,
         dt_plan_tstz_in,
         id_prof_performed_in,
         start_time_in,
         end_time_in,
         create_user_in,
         create_time_in,
         create_institution_in,
         update_user_in,
         update_time_in,
         update_institution_in,
         flg_status_prev_in
         ) RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF FALSE THEN NULL; -- Placeholder in case no unique indexes
           ELSE
              pk_alert_exceptions.raise_error (
                    error_name_in => 'DUPLICATE-VALUE'
                    ,name1_in => 'OWNER'
                    ,value1_in => l_owner
                    ,name2_in => 'CONSTRAINT_NAME'
                    ,value2_in => l_name
                    ,name3_in => 'TABLE_NAME'
                    ,value3_in => 'MONITORIZATION_VS_PLAN');
           END IF;
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'MONITORIZATION_VS_PLAN');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'MONITORIZATION_VS_PLAN');
           IF l_name = 'MVSP_MVS_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_MONITORIZATION_VS'
               , value_in => id_monitorization_vs_in);
           END IF;
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END ins;

   PROCEDURE ins (
      id_monitorization_vs_plan_in IN MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS_PLAN%TYPE
      ,
      flg_status_in IN MONITORIZATION_VS_PLAN.FLG_STATUS%TYPE DEFAULT NULL,
      id_monitorization_vs_in IN MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS%TYPE DEFAULT NULL,
      dt_plan_tstz_in IN MONITORIZATION_VS_PLAN.DT_PLAN_TSTZ%TYPE DEFAULT NULL,
      id_prof_performed_in IN MONITORIZATION_VS_PLAN.ID_PROF_PERFORMED%TYPE DEFAULT NULL,
      start_time_in IN MONITORIZATION_VS_PLAN.START_TIME%TYPE DEFAULT NULL,
      end_time_in IN MONITORIZATION_VS_PLAN.END_TIME%TYPE DEFAULT NULL,
      create_user_in IN MONITORIZATION_VS_PLAN.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN MONITORIZATION_VS_PLAN.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN MONITORIZATION_VS_PLAN.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN MONITORIZATION_VS_PLAN.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN MONITORIZATION_VS_PLAN.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN MONITORIZATION_VS_PLAN.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      flg_status_prev_in IN MONITORIZATION_VS_PLAN.FLG_STATUS_PREV%TYPE DEFAULT NULL
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN

     ins (
      id_monitorization_vs_plan_in => id_monitorization_vs_plan_in
      ,
      flg_status_in => flg_status_in,
      id_monitorization_vs_in => id_monitorization_vs_in,
      dt_plan_tstz_in => dt_plan_tstz_in,
      id_prof_performed_in => id_prof_performed_in,
      start_time_in => start_time_in,
      end_time_in => end_time_in,
      create_user_in => create_user_in,
      create_time_in => create_time_in,
      create_institution_in => create_institution_in,
      update_user_in => update_user_in,
      update_time_in => update_time_in,
      update_institution_in => update_institution_in,
      flg_status_prev_in => flg_status_prev_in
     ,handle_error_in => handle_error_in
      ,rows_out => rows_out
      );
   END ins;


   PROCEDURE ins (
      rec_in IN MONITORIZATION_VS_PLAN%ROWTYPE
     ,gen_pky_in IN BOOLEAN DEFAULT FALSE
     ,sequence_in IN VARCHAR2 := NULL
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
      l_rec MONITORIZATION_VS_PLAN%ROWTYPE := rec_in;
   BEGIN
      IF gen_pky_in THEN
         l_rec.ID_MONITORIZATION_VS_PLAN := next_key (sequence_in);
      END IF;
      ins (
         id_monitorization_vs_plan_in => l_rec.ID_MONITORIZATION_VS_PLAN
         ,
         flg_status_in => l_rec.FLG_STATUS,
         id_monitorization_vs_in => l_rec.ID_MONITORIZATION_VS,
         dt_plan_tstz_in => l_rec.DT_PLAN_TSTZ,
         id_prof_performed_in => l_rec.ID_PROF_PERFORMED,
         start_time_in => l_rec.START_TIME,
         end_time_in => l_rec.END_TIME,
         create_user_in => l_rec.CREATE_USER,
         create_time_in => l_rec.CREATE_TIME,
         create_institution_in => l_rec.CREATE_INSTITUTION,
         update_user_in => l_rec.UPDATE_USER,
         update_time_in => l_rec.UPDATE_TIME,
         update_institution_in => l_rec.UPDATE_INSTITUTION,
         flg_status_prev_in => l_rec.FLG_STATUS_PREV
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
   END ins;

   PROCEDURE ins (
      rec_in IN MONITORIZATION_VS_PLAN%ROWTYPE
     ,gen_pky_in IN BOOLEAN DEFAULT FALSE
     ,sequence_in IN VARCHAR2 := NULL
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
      rows_out TABLE_VARCHAR;
   BEGIN

  ins (
      rec_in => rec_in
     ,gen_pky_in => gen_pky_in
     ,sequence_in => sequence_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
   );

   END ins;

   FUNCTION next_key (sequence_in IN VARCHAR2 := NULL) RETURN MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS_PLAN%TYPE

   IS
     retval MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS_PLAN%TYPE;

   BEGIN
      IF sequence_in IS NULL
      THEN
         SELECT seq_MONITORIZATION_VS_PLAN.NEXTVAL INTO retval FROM dual;
      ELSE
         EXECUTE IMMEDIATE
            'SELECT ' || sequence_in || '.NEXTVAL FROM dual'
            INTO retval;
      END IF;
      RETURN retval;
   EXCEPTION
      WHEN OTHERS THEN
        pk_alert_exceptions.raise_error (
           error_name_in => 'SEQUENCE-GENERATION-FAILURE'
           ,name1_in => 'SEQUENCE'
           ,value1_in => NVL (sequence_in, 'seq_MONITORIZATION_VS_PLAN')
           );
   END next_key;

   PROCEDURE ins (
      flg_status_in IN MONITORIZATION_VS_PLAN.FLG_STATUS%TYPE DEFAULT NULL,
      id_monitorization_vs_in IN MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS%TYPE DEFAULT NULL,
      dt_plan_tstz_in IN MONITORIZATION_VS_PLAN.DT_PLAN_TSTZ%TYPE DEFAULT NULL,
      id_prof_performed_in IN MONITORIZATION_VS_PLAN.ID_PROF_PERFORMED%TYPE DEFAULT NULL,
      start_time_in IN MONITORIZATION_VS_PLAN.START_TIME%TYPE DEFAULT NULL,
      end_time_in IN MONITORIZATION_VS_PLAN.END_TIME%TYPE DEFAULT NULL,
      create_user_in IN MONITORIZATION_VS_PLAN.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN MONITORIZATION_VS_PLAN.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN MONITORIZATION_VS_PLAN.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN MONITORIZATION_VS_PLAN.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN MONITORIZATION_VS_PLAN.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN MONITORIZATION_VS_PLAN.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      flg_status_prev_in IN MONITORIZATION_VS_PLAN.FLG_STATUS_PREV%TYPE DEFAULT NULL,
      id_monitorization_vs_plan_out IN OUT MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS_PLAN%TYPE
      ,handle_error_in IN BOOLEAN := TRUE
      , rows_out OUT TABLE_VARCHAR
   )
   IS
        l_pky MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS_PLAN%TYPE := next_key;
   BEGIN
      ins (
         id_monitorization_vs_plan_in => l_pky,
         flg_status_in => flg_status_in,
         id_monitorization_vs_in => id_monitorization_vs_in,
         dt_plan_tstz_in => dt_plan_tstz_in,
         id_prof_performed_in => id_prof_performed_in,
         start_time_in => start_time_in,
         end_time_in => end_time_in,
         create_user_in => create_user_in,
         create_time_in => create_time_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in,
         flg_status_prev_in => flg_status_prev_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
      id_monitorization_vs_plan_out := l_pky;
   END ins;

   PROCEDURE ins (
      flg_status_in IN MONITORIZATION_VS_PLAN.FLG_STATUS%TYPE DEFAULT NULL,
      id_monitorization_vs_in IN MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS%TYPE DEFAULT NULL,
      dt_plan_tstz_in IN MONITORIZATION_VS_PLAN.DT_PLAN_TSTZ%TYPE DEFAULT NULL,
      id_prof_performed_in IN MONITORIZATION_VS_PLAN.ID_PROF_PERFORMED%TYPE DEFAULT NULL,
      start_time_in IN MONITORIZATION_VS_PLAN.START_TIME%TYPE DEFAULT NULL,
      end_time_in IN MONITORIZATION_VS_PLAN.END_TIME%TYPE DEFAULT NULL,
      create_user_in IN MONITORIZATION_VS_PLAN.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN MONITORIZATION_VS_PLAN.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN MONITORIZATION_VS_PLAN.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN MONITORIZATION_VS_PLAN.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN MONITORIZATION_VS_PLAN.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN MONITORIZATION_VS_PLAN.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      flg_status_prev_in IN MONITORIZATION_VS_PLAN.FLG_STATUS_PREV%TYPE DEFAULT NULL,
      id_monitorization_vs_plan_out IN OUT MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS_PLAN%TYPE
      ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
        rows_out TABLE_VARCHAR;
   BEGIN
      ins (
      flg_status_in => flg_status_in,
      id_monitorization_vs_in => id_monitorization_vs_in,
      dt_plan_tstz_in => dt_plan_tstz_in,
      id_prof_performed_in => id_prof_performed_in,
      start_time_in => start_time_in,
      end_time_in => end_time_in,
      create_user_in => create_user_in,
      create_time_in => create_time_in,
      create_institution_in => create_institution_in,
      update_user_in => update_user_in,
      update_time_in => update_time_in,
      update_institution_in => update_institution_in,
      flg_status_prev_in => flg_status_prev_in,
      id_monitorization_vs_plan_out => id_monitorization_vs_plan_out
      ,handle_error_in => handle_error_in
      , rows_out => rows_out
   );
   END ins;

   FUNCTION ins (
      flg_status_in IN MONITORIZATION_VS_PLAN.FLG_STATUS%TYPE DEFAULT NULL,
      id_monitorization_vs_in IN MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS%TYPE DEFAULT NULL,
      dt_plan_tstz_in IN MONITORIZATION_VS_PLAN.DT_PLAN_TSTZ%TYPE DEFAULT NULL,
      id_prof_performed_in IN MONITORIZATION_VS_PLAN.ID_PROF_PERFORMED%TYPE DEFAULT NULL,
      start_time_in IN MONITORIZATION_VS_PLAN.START_TIME%TYPE DEFAULT NULL,
      end_time_in IN MONITORIZATION_VS_PLAN.END_TIME%TYPE DEFAULT NULL,
      create_user_in IN MONITORIZATION_VS_PLAN.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN MONITORIZATION_VS_PLAN.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN MONITORIZATION_VS_PLAN.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN MONITORIZATION_VS_PLAN.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN MONITORIZATION_VS_PLAN.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN MONITORIZATION_VS_PLAN.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      flg_status_prev_in IN MONITORIZATION_VS_PLAN.FLG_STATUS_PREV%TYPE DEFAULT NULL
      ,handle_error_in IN BOOLEAN := TRUE
      , rows_out OUT TABLE_VARCHAR
   )
      RETURN
         MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS_PLAN%TYPE
   IS
        l_pky MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS_PLAN%TYPE := next_key;
   BEGIN
      ins (
         id_monitorization_vs_plan_in => l_pky,
         flg_status_in => flg_status_in,
         id_monitorization_vs_in => id_monitorization_vs_in,
         dt_plan_tstz_in => dt_plan_tstz_in,
         id_prof_performed_in => id_prof_performed_in,
         start_time_in => start_time_in,
         end_time_in => end_time_in,
         create_user_in => create_user_in,
         create_time_in => create_time_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in,
         flg_status_prev_in => flg_status_prev_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
      RETURN l_pky;
   END ins;

   FUNCTION ins (
      flg_status_in IN MONITORIZATION_VS_PLAN.FLG_STATUS%TYPE DEFAULT NULL,
      id_monitorization_vs_in IN MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS%TYPE DEFAULT NULL,
      dt_plan_tstz_in IN MONITORIZATION_VS_PLAN.DT_PLAN_TSTZ%TYPE DEFAULT NULL,
      id_prof_performed_in IN MONITORIZATION_VS_PLAN.ID_PROF_PERFORMED%TYPE DEFAULT NULL,
      start_time_in IN MONITORIZATION_VS_PLAN.START_TIME%TYPE DEFAULT NULL,
      end_time_in IN MONITORIZATION_VS_PLAN.END_TIME%TYPE DEFAULT NULL,
      create_user_in IN MONITORIZATION_VS_PLAN.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN MONITORIZATION_VS_PLAN.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN MONITORIZATION_VS_PLAN.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN MONITORIZATION_VS_PLAN.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN MONITORIZATION_VS_PLAN.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN MONITORIZATION_VS_PLAN.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      flg_status_prev_in IN MONITORIZATION_VS_PLAN.FLG_STATUS_PREV%TYPE DEFAULT NULL
      ,handle_error_in IN BOOLEAN := TRUE
   )
      RETURN
         MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS_PLAN%TYPE
   IS
        l_pky MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS_PLAN%TYPE := next_key;
    rows_out TABLE_VARCHAR;
   BEGIN
      ins (
         id_monitorization_vs_plan_in => l_pky,
         flg_status_in => flg_status_in,
         id_monitorization_vs_in => id_monitorization_vs_in,
         dt_plan_tstz_in => dt_plan_tstz_in,
         id_prof_performed_in => id_prof_performed_in,
         start_time_in => start_time_in,
         end_time_in => end_time_in,
         create_user_in => create_user_in,
         create_time_in => create_time_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in,
         flg_status_prev_in => flg_status_prev_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
      RETURN l_pky;
   END ins;

      PROCEDURE ins (
      flg_status_in IN MONITORIZATION_VS_PLAN.FLG_STATUS%TYPE DEFAULT NULL,
      id_monitorization_vs_in IN MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS%TYPE DEFAULT NULL,
      dt_plan_tstz_in IN MONITORIZATION_VS_PLAN.DT_PLAN_TSTZ%TYPE DEFAULT NULL,
      id_prof_performed_in IN MONITORIZATION_VS_PLAN.ID_PROF_PERFORMED%TYPE DEFAULT NULL,
      start_time_in IN MONITORIZATION_VS_PLAN.START_TIME%TYPE DEFAULT NULL,
      end_time_in IN MONITORIZATION_VS_PLAN.END_TIME%TYPE DEFAULT NULL,
      create_user_in IN MONITORIZATION_VS_PLAN.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN MONITORIZATION_VS_PLAN.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN MONITORIZATION_VS_PLAN.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN MONITORIZATION_VS_PLAN.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN MONITORIZATION_VS_PLAN.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN MONITORIZATION_VS_PLAN.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      flg_status_prev_in IN MONITORIZATION_VS_PLAN.FLG_STATUS_PREV%TYPE DEFAULT NULL
      ,handle_error_in IN BOOLEAN := TRUE
      , rows_out OUT TABLE_VARCHAR
   )
   IS
        l_pky MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS_PLAN%TYPE := next_key;
   BEGIN
      ins (
         id_monitorization_vs_plan_in => l_pky,
         flg_status_in => flg_status_in,
         id_monitorization_vs_in => id_monitorization_vs_in,
         dt_plan_tstz_in => dt_plan_tstz_in,
         id_prof_performed_in => id_prof_performed_in,
         start_time_in => start_time_in,
         end_time_in => end_time_in,
         create_user_in => create_user_in,
         create_time_in => create_time_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in,
         flg_status_prev_in => flg_status_prev_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
   END ins;


     PROCEDURE ins (
      flg_status_in IN MONITORIZATION_VS_PLAN.FLG_STATUS%TYPE DEFAULT NULL,
      id_monitorization_vs_in IN MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS%TYPE DEFAULT NULL,
      dt_plan_tstz_in IN MONITORIZATION_VS_PLAN.DT_PLAN_TSTZ%TYPE DEFAULT NULL,
      id_prof_performed_in IN MONITORIZATION_VS_PLAN.ID_PROF_PERFORMED%TYPE DEFAULT NULL,
      start_time_in IN MONITORIZATION_VS_PLAN.START_TIME%TYPE DEFAULT NULL,
      end_time_in IN MONITORIZATION_VS_PLAN.END_TIME%TYPE DEFAULT NULL,
      create_user_in IN MONITORIZATION_VS_PLAN.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN MONITORIZATION_VS_PLAN.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN MONITORIZATION_VS_PLAN.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN MONITORIZATION_VS_PLAN.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN MONITORIZATION_VS_PLAN.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN MONITORIZATION_VS_PLAN.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      flg_status_prev_in IN MONITORIZATION_VS_PLAN.FLG_STATUS_PREV%TYPE DEFAULT NULL
      ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
        l_pky MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS_PLAN%TYPE := next_key;
      rows_out TABLE_VARCHAR;
   BEGIN
      ins (
         id_monitorization_vs_plan_in => l_pky,
         flg_status_in => flg_status_in,
         id_monitorization_vs_in => id_monitorization_vs_in,
         dt_plan_tstz_in => dt_plan_tstz_in,
         id_prof_performed_in => id_prof_performed_in,
         start_time_in => start_time_in,
         end_time_in => end_time_in,
         create_user_in => create_user_in,
         create_time_in => create_time_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in,
         flg_status_prev_in => flg_status_prev_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
   END ins;

    PROCEDURE ins (
      rows_in IN MONITORIZATION_VS_PLAN_tc
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
   BEGIN
      IF rows_in.COUNT = 0
      THEN
         NULL;
      ELSE
         FORALL indx IN rows_in.FIRST .. rows_in.LAST
            SAVE EXCEPTIONS
            INSERT INTO MONITORIZATION_VS_PLAN VALUES rows_in (indx) RETURNING ROWID BULK COLLECT INTO rows_out;
      END IF;
   EXCEPTION
     WHEN e_forall_error
     THEN
        -- In Oracle9i and above, SAVE EXCEPTIONS will direct control
        -- here if any error occurs. We can then save all the error
        -- information out to the error instance.
       IF NOT handle_error_in THEN RAISE;
       ELSE
          <<bulk_handler>>
          DECLARE
             l_err_instance_id NUMBER;
          BEGIN
             -- For each error, write to the log.
             FOR indx IN 1 .. SQL%BULK_EXCEPTIONS.COUNT
             LOOP
                pk_alert_exceptions.register_error (
                    error_name_in => 'FORALL-INSERT-FAILURE'
                   ,err_instance_id_out => l_err_instance_id
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'BINDING_ROW_' || indx
                  ,value_in => SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX
                  ,validate_in => FALSE
                );
                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ERROR_AT_ROW_' || indx
                  ,value_in => SQL%BULK_EXCEPTIONS (indx).ERROR_CODE
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_MONITORIZATION_VS_PLAN _' || indx
                  ,value_in => rows_in(indx).ID_MONITORIZATION_VS_PLAN
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'FLG_STATUS _' || indx
                  ,value_in => rows_in(indx).FLG_STATUS
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_MONITORIZATION_VS _' || indx
                  ,value_in => rows_in(indx).ID_MONITORIZATION_VS
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'DT_PLAN_TSTZ _' || indx
                  ,value_in => rows_in(indx).DT_PLAN_TSTZ
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_PROF_PERFORMED _' || indx
                  ,value_in => rows_in(indx).ID_PROF_PERFORMED
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'START_TIME _' || indx
                  ,value_in => rows_in(indx).START_TIME
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'END_TIME _' || indx
                  ,value_in => rows_in(indx).END_TIME
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'CREATE_USER _' || indx
                  ,value_in => rows_in(indx).CREATE_USER
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'CREATE_TIME _' || indx
                  ,value_in => rows_in(indx).CREATE_TIME
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'CREATE_INSTITUTION _' || indx
                  ,value_in => rows_in(indx).CREATE_INSTITUTION
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'UPDATE_USER _' || indx
                  ,value_in => rows_in(indx).UPDATE_USER
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'UPDATE_TIME _' || indx
                  ,value_in => rows_in(indx).UPDATE_TIME
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'UPDATE_INSTITUTION _' || indx
                  ,value_in => rows_in(indx).UPDATE_INSTITUTION
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'FLG_STATUS_PREV _' || indx
                  ,value_in => rows_in(indx).FLG_STATUS_PREV
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.raise_error_instance( err_instance_id_in => l_err_instance_id );
             END LOOP;
          END bulk_handler;
        END IF;
     WHEN OTHERS
     THEN
       IF NOT handle_error_in THEN RAISE;
       ELSE
       pk_alert_exceptions.raise_error(
          error_name_in => 'FORALL-INSERT-FAILURE'
          ,name1_in => 'TABLE_NAME'
          ,value1_in => 'MONITORIZATION_VS_PLAN'
          ,name2_in => 'ROW_COUNT'
          ,value2_in => rows_in.COUNT
           );
       END IF;
   END ins;

    PROCEDURE ins (
      rows_in IN MONITORIZATION_VS_PLAN_tc
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
      rows_out TABLE_VARCHAR;
   BEGIN
      ins (
      rows_in => rows_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
   );
   END ins;


PROCEDURE upd (
      id_monitorization_vs_plan_in IN MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS_PLAN%TYPE,
      flg_status_in IN MONITORIZATION_VS_PLAN.FLG_STATUS%TYPE DEFAULT NULL,
      FLG_STATUS_nin IN BOOLEAN := TRUE,
      id_monitorization_vs_in IN MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS%TYPE DEFAULT NULL,
      ID_MONITORIZATION_VS_nin IN BOOLEAN := TRUE,
      dt_plan_tstz_in IN MONITORIZATION_VS_PLAN.DT_PLAN_TSTZ%TYPE DEFAULT NULL,
      DT_PLAN_TSTZ_nin IN BOOLEAN := TRUE,
      id_prof_performed_in IN MONITORIZATION_VS_PLAN.ID_PROF_PERFORMED%TYPE DEFAULT NULL,
      ID_PROF_PERFORMED_nin IN BOOLEAN := TRUE,
      start_time_in IN MONITORIZATION_VS_PLAN.START_TIME%TYPE DEFAULT NULL,
      START_TIME_nin IN BOOLEAN := TRUE,
      end_time_in IN MONITORIZATION_VS_PLAN.END_TIME%TYPE DEFAULT NULL,
      END_TIME_nin IN BOOLEAN := TRUE,
      create_user_in IN MONITORIZATION_VS_PLAN.CREATE_USER%TYPE DEFAULT NULL,
      CREATE_USER_nin IN BOOLEAN := TRUE,
      create_time_in IN MONITORIZATION_VS_PLAN.CREATE_TIME%TYPE DEFAULT NULL,
      CREATE_TIME_nin IN BOOLEAN := TRUE,
      create_institution_in IN MONITORIZATION_VS_PLAN.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      CREATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      update_user_in IN MONITORIZATION_VS_PLAN.UPDATE_USER%TYPE DEFAULT NULL,
      UPDATE_USER_nin IN BOOLEAN := TRUE,
      update_time_in IN MONITORIZATION_VS_PLAN.UPDATE_TIME%TYPE DEFAULT NULL,
      UPDATE_TIME_nin IN BOOLEAN := TRUE,
      update_institution_in IN MONITORIZATION_VS_PLAN.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      UPDATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      flg_status_prev_in IN MONITORIZATION_VS_PLAN.FLG_STATUS_PREV%TYPE DEFAULT NULL,
      FLG_STATUS_PREV_nin IN BOOLEAN := TRUE,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      )
   IS
   l_rows_out TABLE_VARCHAR;
   l_FLG_STATUS_n NUMBER(1);
   l_ID_MONITORIZATION_VS_n NUMBER(1);
   l_DT_PLAN_TSTZ_n NUMBER(1);
   l_ID_PROF_PERFORMED_n NUMBER(1);
   l_START_TIME_n NUMBER(1);
   l_END_TIME_n NUMBER(1);
   l_CREATE_USER_n NUMBER(1);
   l_CREATE_TIME_n NUMBER(1);
   l_CREATE_INSTITUTION_n NUMBER(1);
   l_UPDATE_USER_n NUMBER(1);
   l_UPDATE_TIME_n NUMBER(1);
   l_UPDATE_INSTITUTION_n NUMBER(1);
   l_FLG_STATUS_PREV_n NUMBER(1);
   BEGIN

   l_FLG_STATUS_n := sys.diutil.bool_to_int(FLG_STATUS_nin);
   l_ID_MONITORIZATION_VS_n := sys.diutil.bool_to_int(ID_MONITORIZATION_VS_nin);
   l_DT_PLAN_TSTZ_n := sys.diutil.bool_to_int(DT_PLAN_TSTZ_nin);
   l_ID_PROF_PERFORMED_n := sys.diutil.bool_to_int(ID_PROF_PERFORMED_nin);
   l_START_TIME_n := sys.diutil.bool_to_int(START_TIME_nin);
   l_END_TIME_n := sys.diutil.bool_to_int(END_TIME_nin);
   l_CREATE_USER_n := sys.diutil.bool_to_int(CREATE_USER_nin);
   l_CREATE_TIME_n := sys.diutil.bool_to_int(CREATE_TIME_nin);
   l_CREATE_INSTITUTION_n := sys.diutil.bool_to_int(CREATE_INSTITUTION_nin);
   l_UPDATE_USER_n := sys.diutil.bool_to_int(UPDATE_USER_nin);
   l_UPDATE_TIME_n := sys.diutil.bool_to_int(UPDATE_TIME_nin);
   l_UPDATE_INSTITUTION_n := sys.diutil.bool_to_int(UPDATE_INSTITUTION_nin);
   l_FLG_STATUS_PREV_n := sys.diutil.bool_to_int(FLG_STATUS_PREV_nin);


         UPDATE MONITORIZATION_VS_PLAN SET
     FLG_STATUS = decode (l_FLG_STATUS_n,0,flg_status_in, NVL (flg_status_in, FLG_STATUS)),
     ID_MONITORIZATION_VS = decode (l_ID_MONITORIZATION_VS_n,0,id_monitorization_vs_in, NVL (id_monitorization_vs_in, ID_MONITORIZATION_VS)),
     DT_PLAN_TSTZ = decode (l_DT_PLAN_TSTZ_n,0,dt_plan_tstz_in, NVL (dt_plan_tstz_in, DT_PLAN_TSTZ)),
     ID_PROF_PERFORMED = decode (l_ID_PROF_PERFORMED_n,0,id_prof_performed_in, NVL (id_prof_performed_in, ID_PROF_PERFORMED)),
     START_TIME = decode (l_START_TIME_n,0,start_time_in, NVL (start_time_in, START_TIME)),
     END_TIME = decode (l_END_TIME_n,0,end_time_in, NVL (end_time_in, END_TIME)),
     CREATE_USER = decode (l_CREATE_USER_n,0,create_user_in, NVL (create_user_in, CREATE_USER)),
     CREATE_TIME = decode (l_CREATE_TIME_n,0,create_time_in, NVL (create_time_in, CREATE_TIME)),
     CREATE_INSTITUTION = decode (l_CREATE_INSTITUTION_n,0,create_institution_in, NVL (create_institution_in, CREATE_INSTITUTION)),
     UPDATE_USER = decode (l_UPDATE_USER_n,0,update_user_in, NVL (update_user_in, UPDATE_USER)),
     UPDATE_TIME = decode (l_UPDATE_TIME_n,0,update_time_in, NVL (update_time_in, UPDATE_TIME)),
     UPDATE_INSTITUTION = decode (l_UPDATE_INSTITUTION_n,0,update_institution_in, NVL (update_institution_in, UPDATE_INSTITUTION)),
     FLG_STATUS_PREV = decode (l_FLG_STATUS_PREV_n,0,flg_status_prev_in, NVL (flg_status_prev_in, FLG_STATUS_PREV))
          WHERE
             ID_MONITORIZATION_VS_PLAN = id_monitorization_vs_plan_in
         RETURNING ROWID BULK COLLECT INTO l_rows_out;


if(rows_out is null)
then
rows_out := table_varchar();
end if;

rows_out :=  rows_out MULTISET UNION DISTINCT l_rows_out;

   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF FALSE THEN NULL; -- Placeholder in case no unique indexes
           ELSE
              pk_alert_exceptions.raise_error (
                    error_name_in => 'DUPLICATE-VALUE'
                    ,name1_in => 'OWNER'
                    ,value1_in => l_owner
                    ,name2_in => 'CONSTRAINT_NAME'
                    ,value2_in => l_name
                    ,name3_in => 'TABLE_NAME'
                    ,value3_in => 'MONITORIZATION_VS_PLAN');
           END IF;
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'MONITORIZATION_VS_PLAN');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'MONITORIZATION_VS_PLAN');
           IF l_name = 'MVSP_MVS_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_MONITORIZATION_VS'
               , value_in => id_monitorization_vs_in);
           END IF;
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END upd;


   PROCEDURE upd (
      id_monitorization_vs_plan_in IN MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS_PLAN%TYPE,
      flg_status_in IN MONITORIZATION_VS_PLAN.FLG_STATUS%TYPE DEFAULT NULL,
      FLG_STATUS_nin IN BOOLEAN := TRUE,
      id_monitorization_vs_in IN MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS%TYPE DEFAULT NULL,
      ID_MONITORIZATION_VS_nin IN BOOLEAN := TRUE,
      dt_plan_tstz_in IN MONITORIZATION_VS_PLAN.DT_PLAN_TSTZ%TYPE DEFAULT NULL,
      DT_PLAN_TSTZ_nin IN BOOLEAN := TRUE,
      id_prof_performed_in IN MONITORIZATION_VS_PLAN.ID_PROF_PERFORMED%TYPE DEFAULT NULL,
      ID_PROF_PERFORMED_nin IN BOOLEAN := TRUE,
      start_time_in IN MONITORIZATION_VS_PLAN.START_TIME%TYPE DEFAULT NULL,
      START_TIME_nin IN BOOLEAN := TRUE,
      end_time_in IN MONITORIZATION_VS_PLAN.END_TIME%TYPE DEFAULT NULL,
      END_TIME_nin IN BOOLEAN := TRUE,
      create_user_in IN MONITORIZATION_VS_PLAN.CREATE_USER%TYPE DEFAULT NULL,
      CREATE_USER_nin IN BOOLEAN := TRUE,
      create_time_in IN MONITORIZATION_VS_PLAN.CREATE_TIME%TYPE DEFAULT NULL,
      CREATE_TIME_nin IN BOOLEAN := TRUE,
      create_institution_in IN MONITORIZATION_VS_PLAN.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      CREATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      update_user_in IN MONITORIZATION_VS_PLAN.UPDATE_USER%TYPE DEFAULT NULL,
      UPDATE_USER_nin IN BOOLEAN := TRUE,
      update_time_in IN MONITORIZATION_VS_PLAN.UPDATE_TIME%TYPE DEFAULT NULL,
      UPDATE_TIME_nin IN BOOLEAN := TRUE,
      update_institution_in IN MONITORIZATION_VS_PLAN.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      UPDATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      flg_status_prev_in IN MONITORIZATION_VS_PLAN.FLG_STATUS_PREV%TYPE DEFAULT NULL,
      FLG_STATUS_PREV_nin IN BOOLEAN := TRUE,
     handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
     upd (
      id_monitorization_vs_plan_in => id_monitorization_vs_plan_in,
      flg_status_in => flg_status_in,
      FLG_STATUS_nin => FLG_STATUS_nin,
      id_monitorization_vs_in => id_monitorization_vs_in,
      ID_MONITORIZATION_VS_nin => ID_MONITORIZATION_VS_nin,
      dt_plan_tstz_in => dt_plan_tstz_in,
      DT_PLAN_TSTZ_nin => DT_PLAN_TSTZ_nin,
      id_prof_performed_in => id_prof_performed_in,
      ID_PROF_PERFORMED_nin => ID_PROF_PERFORMED_nin,
      start_time_in => start_time_in,
      START_TIME_nin => START_TIME_nin,
      end_time_in => end_time_in,
      END_TIME_nin => END_TIME_nin,
      create_user_in => create_user_in,
      CREATE_USER_nin => CREATE_USER_nin,
      create_time_in => create_time_in,
      CREATE_TIME_nin => CREATE_TIME_nin,
      create_institution_in => create_institution_in,
      CREATE_INSTITUTION_nin => CREATE_INSTITUTION_nin,
      update_user_in => update_user_in,
      UPDATE_USER_nin => UPDATE_USER_nin,
      update_time_in => update_time_in,
      UPDATE_TIME_nin => UPDATE_TIME_nin,
      update_institution_in => update_institution_in,
      UPDATE_INSTITUTION_nin => UPDATE_INSTITUTION_nin,
      flg_status_prev_in => flg_status_prev_in,
      FLG_STATUS_PREV_nin => FLG_STATUS_PREV_nin,
     handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END upd;

PROCEDURE upd (
      flg_status_in IN MONITORIZATION_VS_PLAN.FLG_STATUS%TYPE DEFAULT NULL,
      FLG_STATUS_nin IN BOOLEAN := TRUE,
      id_monitorization_vs_in IN MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS%TYPE DEFAULT NULL,
      ID_MONITORIZATION_VS_nin IN BOOLEAN := TRUE,
      dt_plan_tstz_in IN MONITORIZATION_VS_PLAN.DT_PLAN_TSTZ%TYPE DEFAULT NULL,
      DT_PLAN_TSTZ_nin IN BOOLEAN := TRUE,
      id_prof_performed_in IN MONITORIZATION_VS_PLAN.ID_PROF_PERFORMED%TYPE DEFAULT NULL,
      ID_PROF_PERFORMED_nin IN BOOLEAN := TRUE,
      start_time_in IN MONITORIZATION_VS_PLAN.START_TIME%TYPE DEFAULT NULL,
      START_TIME_nin IN BOOLEAN := TRUE,
      end_time_in IN MONITORIZATION_VS_PLAN.END_TIME%TYPE DEFAULT NULL,
      END_TIME_nin IN BOOLEAN := TRUE,
      create_user_in IN MONITORIZATION_VS_PLAN.CREATE_USER%TYPE DEFAULT NULL,
      CREATE_USER_nin IN BOOLEAN := TRUE,
      create_time_in IN MONITORIZATION_VS_PLAN.CREATE_TIME%TYPE DEFAULT NULL,
      CREATE_TIME_nin IN BOOLEAN := TRUE,
      create_institution_in IN MONITORIZATION_VS_PLAN.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      CREATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      update_user_in IN MONITORIZATION_VS_PLAN.UPDATE_USER%TYPE DEFAULT NULL,
      UPDATE_USER_nin IN BOOLEAN := TRUE,
      update_time_in IN MONITORIZATION_VS_PLAN.UPDATE_TIME%TYPE DEFAULT NULL,
      UPDATE_TIME_nin IN BOOLEAN := TRUE,
      update_institution_in IN MONITORIZATION_VS_PLAN.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      UPDATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      flg_status_prev_in IN MONITORIZATION_VS_PLAN.FLG_STATUS_PREV%TYPE DEFAULT NULL,
      FLG_STATUS_PREV_nin IN BOOLEAN := TRUE,
    where_in varchar2 DEFAULT NULL,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      )
   IS
   l_sql VARCHAR2(32767);
   l_rows_out TABLE_VARCHAR;
   l_FLG_STATUS_n NUMBER(1);
   l_ID_MONITORIZATION_VS_n NUMBER(1);
   l_DT_PLAN_TSTZ_n NUMBER(1);
   l_ID_PROF_PERFORMED_n NUMBER(1);
   l_START_TIME_n NUMBER(1);
   l_END_TIME_n NUMBER(1);
   l_CREATE_USER_n NUMBER(1);
   l_CREATE_TIME_n NUMBER(1);
   l_CREATE_INSTITUTION_n NUMBER(1);
   l_UPDATE_USER_n NUMBER(1);
   l_UPDATE_TIME_n NUMBER(1);
   l_UPDATE_INSTITUTION_n NUMBER(1);
   l_FLG_STATUS_PREV_n NUMBER(1);
      id_monitorization_vs_plan_in MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS_PLAN%TYPE;
   BEGIN



      l_FLG_STATUS_n := sys.diutil.bool_to_int(FLG_STATUS_nin);
      l_ID_MONITORIZATION_VS_n := sys.diutil.bool_to_int(ID_MONITORIZATION_VS_nin);
      l_DT_PLAN_TSTZ_n := sys.diutil.bool_to_int(DT_PLAN_TSTZ_nin);
      l_ID_PROF_PERFORMED_n := sys.diutil.bool_to_int(ID_PROF_PERFORMED_nin);
      l_START_TIME_n := sys.diutil.bool_to_int(START_TIME_nin);
      l_END_TIME_n := sys.diutil.bool_to_int(END_TIME_nin);
      l_CREATE_USER_n := sys.diutil.bool_to_int(CREATE_USER_nin);
      l_CREATE_TIME_n := sys.diutil.bool_to_int(CREATE_TIME_nin);
      l_CREATE_INSTITUTION_n := sys.diutil.bool_to_int(CREATE_INSTITUTION_nin);
      l_UPDATE_USER_n := sys.diutil.bool_to_int(UPDATE_USER_nin);
      l_UPDATE_TIME_n := sys.diutil.bool_to_int(UPDATE_TIME_nin);
      l_UPDATE_INSTITUTION_n := sys.diutil.bool_to_int(UPDATE_INSTITUTION_nin);
      l_FLG_STATUS_PREV_n := sys.diutil.bool_to_int(FLG_STATUS_PREV_nin);



l_sql := 'UPDATE MONITORIZATION_VS_PLAN SET '
     || ' FLG_STATUS = decode (' || l_FLG_STATUS_n || ',0,:flg_status_in, NVL (:flg_status_in, FLG_STATUS)) '|| ','
     || ' ID_MONITORIZATION_VS = decode (' || l_ID_MONITORIZATION_VS_n || ',0,:id_monitorization_vs_in, NVL (:id_monitorization_vs_in, ID_MONITORIZATION_VS)) '|| ','
     || ' DT_PLAN_TSTZ = decode (' || l_DT_PLAN_TSTZ_n || ',0,:dt_plan_tstz_in, NVL (:dt_plan_tstz_in, DT_PLAN_TSTZ)) '|| ','
     || ' ID_PROF_PERFORMED = decode (' || l_ID_PROF_PERFORMED_n || ',0,:id_prof_performed_in, NVL (:id_prof_performed_in, ID_PROF_PERFORMED)) '|| ','
     || ' START_TIME = decode (' || l_START_TIME_n || ',0,:start_time_in, NVL (:start_time_in, START_TIME)) '|| ','
     || ' END_TIME = decode (' || l_END_TIME_n || ',0,:end_time_in, NVL (:end_time_in, END_TIME)) '|| ','
     || ' CREATE_USER = decode (' || l_CREATE_USER_n || ',0,:create_user_in, NVL (:create_user_in, CREATE_USER)) '|| ','
     || ' CREATE_TIME = decode (' || l_CREATE_TIME_n || ',0,:create_time_in, NVL (:create_time_in, CREATE_TIME)) '|| ','
     || ' CREATE_INSTITUTION = decode (' || l_CREATE_INSTITUTION_n || ',0,:create_institution_in, NVL (:create_institution_in, CREATE_INSTITUTION)) '|| ','
     || ' UPDATE_USER = decode (' || l_UPDATE_USER_n || ',0,:update_user_in, NVL (:update_user_in, UPDATE_USER)) '|| ','
     || ' UPDATE_TIME = decode (' || l_UPDATE_TIME_n || ',0,:update_time_in, NVL (:update_time_in, UPDATE_TIME)) '|| ','
     || ' UPDATE_INSTITUTION = decode (' || l_UPDATE_INSTITUTION_n || ',0,:update_institution_in, NVL (:update_institution_in, UPDATE_INSTITUTION)) '|| ','
     || ' FLG_STATUS_PREV = decode (' || l_FLG_STATUS_PREV_n || ',0,:flg_status_prev_in, NVL (:flg_status_prev_in, FLG_STATUS_PREV)) '
      || ' where ' || nvl(where_in,'(1=1)')
      || ' RETURNING ROWID BULK COLLECT INTO :l_rows_out';




execute immediate 'BEGIN ' || l_sql || '; END;' using in
     flg_status_in,
     id_monitorization_vs_in,
     dt_plan_tstz_in,
     id_prof_performed_in,
     start_time_in,
     end_time_in,
     create_user_in,
     create_time_in,
     create_institution_in,
     update_user_in,
     update_time_in,
     update_institution_in,
     flg_status_prev_in,
    OUT l_rows_out;

if(rows_out is null)
then
rows_out := table_varchar();
end if;

rows_out :=  rows_out MULTISET UNION DISTINCT l_rows_out;

   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF FALSE THEN NULL; -- Placeholder in case no unique indexes
           ELSE
              pk_alert_exceptions.raise_error (
                    error_name_in => 'DUPLICATE-VALUE'
                    ,name1_in => 'OWNER'
                    ,value1_in => l_owner
                    ,name2_in => 'CONSTRAINT_NAME'
                    ,value2_in => l_name
                    ,name3_in => 'TABLE_NAME'
                    ,value3_in => 'MONITORIZATION_VS_PLAN');
           END IF;
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'MONITORIZATION_VS_PLAN');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'MONITORIZATION_VS_PLAN');
           IF l_name = 'MVSP_MVS_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_MONITORIZATION_VS'
               , value_in => id_monitorization_vs_in);
           END IF;
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END upd;





PROCEDURE upd (
      flg_status_in IN MONITORIZATION_VS_PLAN.FLG_STATUS%TYPE DEFAULT NULL,
      FLG_STATUS_nin IN BOOLEAN := TRUE,
      id_monitorization_vs_in IN MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS%TYPE DEFAULT NULL,
      ID_MONITORIZATION_VS_nin IN BOOLEAN := TRUE,
      dt_plan_tstz_in IN MONITORIZATION_VS_PLAN.DT_PLAN_TSTZ%TYPE DEFAULT NULL,
      DT_PLAN_TSTZ_nin IN BOOLEAN := TRUE,
      id_prof_performed_in IN MONITORIZATION_VS_PLAN.ID_PROF_PERFORMED%TYPE DEFAULT NULL,
      ID_PROF_PERFORMED_nin IN BOOLEAN := TRUE,
      start_time_in IN MONITORIZATION_VS_PLAN.START_TIME%TYPE DEFAULT NULL,
      START_TIME_nin IN BOOLEAN := TRUE,
      end_time_in IN MONITORIZATION_VS_PLAN.END_TIME%TYPE DEFAULT NULL,
      END_TIME_nin IN BOOLEAN := TRUE,
      create_user_in IN MONITORIZATION_VS_PLAN.CREATE_USER%TYPE DEFAULT NULL,
      CREATE_USER_nin IN BOOLEAN := TRUE,
      create_time_in IN MONITORIZATION_VS_PLAN.CREATE_TIME%TYPE DEFAULT NULL,
      CREATE_TIME_nin IN BOOLEAN := TRUE,
      create_institution_in IN MONITORIZATION_VS_PLAN.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      CREATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      update_user_in IN MONITORIZATION_VS_PLAN.UPDATE_USER%TYPE DEFAULT NULL,
      UPDATE_USER_nin IN BOOLEAN := TRUE,
      update_time_in IN MONITORIZATION_VS_PLAN.UPDATE_TIME%TYPE DEFAULT NULL,
      UPDATE_TIME_nin IN BOOLEAN := TRUE,
      update_institution_in IN MONITORIZATION_VS_PLAN.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      UPDATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      flg_status_prev_in IN MONITORIZATION_VS_PLAN.FLG_STATUS_PREV%TYPE DEFAULT NULL,
      FLG_STATUS_PREV_nin IN BOOLEAN := TRUE,
    where_in varchar2 DEFAULT NULL,
     handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
      upd (
      flg_status_in => flg_status_in,
      FLG_STATUS_nin => FLG_STATUS_nin,
      id_monitorization_vs_in => id_monitorization_vs_in,
      ID_MONITORIZATION_VS_nin => ID_MONITORIZATION_VS_nin,
      dt_plan_tstz_in => dt_plan_tstz_in,
      DT_PLAN_TSTZ_nin => DT_PLAN_TSTZ_nin,
      id_prof_performed_in => id_prof_performed_in,
      ID_PROF_PERFORMED_nin => ID_PROF_PERFORMED_nin,
      start_time_in => start_time_in,
      START_TIME_nin => START_TIME_nin,
      end_time_in => end_time_in,
      END_TIME_nin => END_TIME_nin,
      create_user_in => create_user_in,
      CREATE_USER_nin => CREATE_USER_nin,
      create_time_in => create_time_in,
      CREATE_TIME_nin => CREATE_TIME_nin,
      create_institution_in => create_institution_in,
      CREATE_INSTITUTION_nin => CREATE_INSTITUTION_nin,
      update_user_in => update_user_in,
      UPDATE_USER_nin => UPDATE_USER_nin,
      update_time_in => update_time_in,
      UPDATE_TIME_nin => UPDATE_TIME_nin,
      update_institution_in => update_institution_in,
      UPDATE_INSTITUTION_nin => UPDATE_INSTITUTION_nin,
      flg_status_prev_in => flg_status_prev_in,
      FLG_STATUS_PREV_nin => FLG_STATUS_PREV_nin,
    where_in => where_in,
     handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END upd;

   PROCEDURE upd (
      rec_in IN MONITORIZATION_VS_PLAN%ROWTYPE,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      upd (
         id_monitorization_vs_plan_in => rec_in.ID_MONITORIZATION_VS_PLAN,
         flg_status_in => rec_in.FLG_STATUS,
         id_monitorization_vs_in => rec_in.ID_MONITORIZATION_VS,
         dt_plan_tstz_in => rec_in.DT_PLAN_TSTZ,
         id_prof_performed_in => rec_in.ID_PROF_PERFORMED,
         start_time_in => rec_in.START_TIME,
         end_time_in => rec_in.END_TIME,
         create_user_in => rec_in.CREATE_USER,
         create_time_in => rec_in.CREATE_TIME,
         create_institution_in => rec_in.CREATE_INSTITUTION,
         update_user_in => rec_in.UPDATE_USER,
         update_time_in => rec_in.UPDATE_TIME,
         update_institution_in => rec_in.UPDATE_INSTITUTION,
         flg_status_prev_in => rec_in.FLG_STATUS_PREV

        ,handle_error_in => handle_error_in
        , rows_out => rows_out
       );
   END upd;

   PROCEDURE upd (
      rec_in IN MONITORIZATION_VS_PLAN%ROWTYPE,
     handle_error_in IN BOOLEAN := TRUE
      )
   IS
        rows_out TABLE_VARCHAR;
   BEGIN
      upd (
         id_monitorization_vs_plan_in => rec_in.ID_MONITORIZATION_VS_PLAN,
         flg_status_in => rec_in.FLG_STATUS,
         id_monitorization_vs_in => rec_in.ID_MONITORIZATION_VS,
         dt_plan_tstz_in => rec_in.DT_PLAN_TSTZ,
         id_prof_performed_in => rec_in.ID_PROF_PERFORMED,
         start_time_in => rec_in.START_TIME,
         end_time_in => rec_in.END_TIME,
         create_user_in => rec_in.CREATE_USER,
         create_time_in => rec_in.CREATE_TIME,
         create_institution_in => rec_in.CREATE_INSTITUTION,
         update_user_in => rec_in.UPDATE_USER,
         update_time_in => rec_in.UPDATE_TIME,
         update_institution_in => rec_in.UPDATE_INSTITUTION,
         flg_status_prev_in => rec_in.FLG_STATUS_PREV

        ,handle_error_in => handle_error_in
        , rows_out => rows_out
       );
   END upd;

   PROCEDURE upd_ins (
      id_monitorization_vs_plan_in IN MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS_PLAN%TYPE,
      flg_status_in IN MONITORIZATION_VS_PLAN.FLG_STATUS%TYPE DEFAULT NULL,
      id_monitorization_vs_in IN MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS%TYPE DEFAULT NULL,
      dt_plan_tstz_in IN MONITORIZATION_VS_PLAN.DT_PLAN_TSTZ%TYPE DEFAULT NULL,
      id_prof_performed_in IN MONITORIZATION_VS_PLAN.ID_PROF_PERFORMED%TYPE DEFAULT NULL,
      start_time_in IN MONITORIZATION_VS_PLAN.START_TIME%TYPE DEFAULT NULL,
      end_time_in IN MONITORIZATION_VS_PLAN.END_TIME%TYPE DEFAULT NULL,
      create_user_in IN MONITORIZATION_VS_PLAN.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN MONITORIZATION_VS_PLAN.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN MONITORIZATION_VS_PLAN.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN MONITORIZATION_VS_PLAN.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN MONITORIZATION_VS_PLAN.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN MONITORIZATION_VS_PLAN.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      flg_status_prev_in IN MONITORIZATION_VS_PLAN.FLG_STATUS_PREV%TYPE DEFAULT NULL,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      upd (
         id_monitorization_vs_plan_in => id_monitorization_vs_plan_in,
         flg_status_in => flg_status_in,
         id_monitorization_vs_in => id_monitorization_vs_in,
         dt_plan_tstz_in => dt_plan_tstz_in,
         id_prof_performed_in => id_prof_performed_in,
         start_time_in => start_time_in,
         end_time_in => end_time_in,
         create_user_in => create_user_in,
         create_time_in => create_time_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in,
         flg_status_prev_in => flg_status_prev_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
       );
      IF SQL%ROWCOUNT = 0
      THEN
         ins (
            id_monitorization_vs_plan_in => id_monitorization_vs_plan_in,
            flg_status_in => flg_status_in,
            id_monitorization_vs_in => id_monitorization_vs_in,
            dt_plan_tstz_in => dt_plan_tstz_in,
            id_prof_performed_in => id_prof_performed_in,
            start_time_in => start_time_in,
            end_time_in => end_time_in,
            create_user_in => create_user_in,
            create_time_in => create_time_in,
            create_institution_in => create_institution_in,
            update_user_in => update_user_in,
            update_time_in => update_time_in,
            update_institution_in => update_institution_in,
            flg_status_prev_in => flg_status_prev_in
            ,handle_error_in => handle_error_in
            , rows_out => rows_out
         );
      END IF;
   END upd_ins;

   PROCEDURE upd_ins (
      id_monitorization_vs_plan_in IN MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS_PLAN%TYPE,
      flg_status_in IN MONITORIZATION_VS_PLAN.FLG_STATUS%TYPE DEFAULT NULL,
      id_monitorization_vs_in IN MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS%TYPE DEFAULT NULL,
      dt_plan_tstz_in IN MONITORIZATION_VS_PLAN.DT_PLAN_TSTZ%TYPE DEFAULT NULL,
      id_prof_performed_in IN MONITORIZATION_VS_PLAN.ID_PROF_PERFORMED%TYPE DEFAULT NULL,
      start_time_in IN MONITORIZATION_VS_PLAN.START_TIME%TYPE DEFAULT NULL,
      end_time_in IN MONITORIZATION_VS_PLAN.END_TIME%TYPE DEFAULT NULL,
      create_user_in IN MONITORIZATION_VS_PLAN.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN MONITORIZATION_VS_PLAN.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN MONITORIZATION_VS_PLAN.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN MONITORIZATION_VS_PLAN.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN MONITORIZATION_VS_PLAN.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN MONITORIZATION_VS_PLAN.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      flg_status_prev_in IN MONITORIZATION_VS_PLAN.FLG_STATUS_PREV%TYPE DEFAULT NULL,
     handle_error_in IN BOOLEAN := TRUE
      )
   IS
        rows_out TABLE_VARCHAR;
   BEGIN
      upd_ins (
      id_monitorization_vs_plan_in,
      flg_status_in,
      id_monitorization_vs_in,
      dt_plan_tstz_in,
      id_prof_performed_in,
      start_time_in,
      end_time_in,
      create_user_in,
      create_time_in,
      create_institution_in,
      update_user_in,
      update_time_in,
      update_institution_in,
      flg_status_prev_in,
     handle_error_in
     ,rows_out
      );
   END upd_ins;


   PROCEDURE upd (
      col_in IN MONITORIZATION_VS_PLAN_tc,
      ignore_if_null_in IN BOOLEAN := TRUE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      )
   IS
      l_ID_MONITORIZATION_VS_PLAN ID_MONITORIZATION_VS_PLAN_cc;
      l_FLG_STATUS FLG_STATUS_cc;
      l_ID_MONITORIZATION_VS ID_MONITORIZATION_VS_cc;
      l_DT_PLAN_TSTZ DT_PLAN_TSTZ_cc;
      l_ID_PROF_PERFORMED ID_PROF_PERFORMED_cc;
      l_START_TIME START_TIME_cc;
      l_END_TIME END_TIME_cc;
      l_CREATE_USER CREATE_USER_cc;
      l_CREATE_TIME CREATE_TIME_cc;
      l_CREATE_INSTITUTION CREATE_INSTITUTION_cc;
      l_UPDATE_USER UPDATE_USER_cc;
      l_UPDATE_TIME UPDATE_TIME_cc;
      l_UPDATE_INSTITUTION UPDATE_INSTITUTION_cc;
      l_FLG_STATUS_PREV FLG_STATUS_PREV_cc;
   BEGIN
      FOR i IN col_in.FIRST .. col_in.LAST loop
         l_ID_MONITORIZATION_VS_PLAN(i) := col_in(i).ID_MONITORIZATION_VS_PLAN;
         l_FLG_STATUS(i) := col_in(i).FLG_STATUS;
         l_ID_MONITORIZATION_VS(i) := col_in(i).ID_MONITORIZATION_VS;
         l_DT_PLAN_TSTZ(i) := col_in(i).DT_PLAN_TSTZ;
         l_ID_PROF_PERFORMED(i) := col_in(i).ID_PROF_PERFORMED;
         l_START_TIME(i) := col_in(i).START_TIME;
         l_END_TIME(i) := col_in(i).END_TIME;
         l_CREATE_USER(i) := col_in(i).CREATE_USER;
         l_CREATE_TIME(i) := col_in(i).CREATE_TIME;
         l_CREATE_INSTITUTION(i) := col_in(i).CREATE_INSTITUTION;
         l_UPDATE_USER(i) := col_in(i).UPDATE_USER;
         l_UPDATE_TIME(i) := col_in(i).UPDATE_TIME;
         l_UPDATE_INSTITUTION(i) := col_in(i).UPDATE_INSTITUTION;
         l_FLG_STATUS_PREV(i) := col_in(i).FLG_STATUS_PREV;
      END LOOP;
      IF NVL (ignore_if_null_in, FALSE)
      THEN
         -- Set any columns to their current values
         -- if incoming value is NULL.
         -- Put WHEN clause on column-level triggers!
         FORALL i IN col_in.FIRST .. col_in.LAST
            UPDATE MONITORIZATION_VS_PLAN SET
               FLG_STATUS = NVL (l_FLG_STATUS(i), FLG_STATUS),
               ID_MONITORIZATION_VS = NVL (l_ID_MONITORIZATION_VS(i), ID_MONITORIZATION_VS),
               DT_PLAN_TSTZ = NVL (l_DT_PLAN_TSTZ(i), DT_PLAN_TSTZ),
               ID_PROF_PERFORMED = NVL (l_ID_PROF_PERFORMED(i), ID_PROF_PERFORMED),
               START_TIME = NVL (l_START_TIME(i), START_TIME),
               END_TIME = NVL (l_END_TIME(i), END_TIME),
               CREATE_USER = NVL (l_CREATE_USER(i), CREATE_USER),
               CREATE_TIME = NVL (l_CREATE_TIME(i), CREATE_TIME),
               CREATE_INSTITUTION = NVL (l_CREATE_INSTITUTION(i), CREATE_INSTITUTION),
               UPDATE_USER = NVL (l_UPDATE_USER(i), UPDATE_USER),
               UPDATE_TIME = NVL (l_UPDATE_TIME(i), UPDATE_TIME),
               UPDATE_INSTITUTION = NVL (l_UPDATE_INSTITUTION(i), UPDATE_INSTITUTION),
               FLG_STATUS_PREV = NVL (l_FLG_STATUS_PREV(i), FLG_STATUS_PREV)
             WHERE
                ID_MONITORIZATION_VS_PLAN = l_ID_MONITORIZATION_VS_PLAN(i)
          returning rowid bulk collect into rows_out;
      ELSE
         FORALL i IN col_in.FIRST .. col_in.LAST
            UPDATE MONITORIZATION_VS_PLAN SET
               FLG_STATUS = l_FLG_STATUS(i),
               ID_MONITORIZATION_VS = l_ID_MONITORIZATION_VS(i),
               DT_PLAN_TSTZ = l_DT_PLAN_TSTZ(i),
               ID_PROF_PERFORMED = l_ID_PROF_PERFORMED(i),
               START_TIME = l_START_TIME(i),
               END_TIME = l_END_TIME(i),
               CREATE_USER = l_CREATE_USER(i),
               CREATE_TIME = l_CREATE_TIME(i),
               CREATE_INSTITUTION = l_CREATE_INSTITUTION(i),
               UPDATE_USER = l_UPDATE_USER(i),
               UPDATE_TIME = l_UPDATE_TIME(i),
               UPDATE_INSTITUTION = l_UPDATE_INSTITUTION(i),
               FLG_STATUS_PREV = l_FLG_STATUS_PREV(i)
             WHERE
                ID_MONITORIZATION_VS_PLAN = l_ID_MONITORIZATION_VS_PLAN(i)
        returning rowid bulk collect into rows_out  ;
      END IF;
   END upd;


   PROCEDURE upd (
      col_in IN MONITORIZATION_VS_PLAN_tc,
      ignore_if_null_in IN BOOLEAN := TRUE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
     rows_out TABLE_VARCHAR;
   BEGIN
      upd (
      col_in ,
      ignore_if_null_in
     ,handle_error_in
     , rows_out
      );
   END upd;

   FUNCTION dynupdstr (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL)

   RETURN VARCHAR2
   IS
   BEGIN
      RETURN
         'BEGIN UPDATE MONITORIZATION_VS_PLAN
             SET ' || colname_in || ' = :value
           WHERE ' || NVL (where_in, '1=1') || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;' ;
   END dynupdstr;

   FUNCTION dynupdstr_no_rows_out (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL)

   RETURN VARCHAR2
   IS
   BEGIN
      RETURN
         'UPDATE MONITORIZATION_VS_PLAN
             SET ' || colname_in || ' = :value
           WHERE ' || NVL (where_in, '1=1');
   END dynupdstr_no_rows_out;























  PROCEDURE increment_onecol (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL
      , increment_value_in IN NUMBER DEFAULT 1
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN UPDATE MONITORIZATION_VS_PLAN set ' || colname_in || '=' || colname_in || ' + ' || nvl(increment_value_in,1) || ' WHERE ' || NVL (where_in, '1=1') || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
      USING OUT rows_out;
   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'DUPLICATE-VALUE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'MONITORIZATION_VS_PLAN');
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'MONITORIZATION_VS_PLAN');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'MONITORIZATION_VS_PLAN');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END increment_onecol;

   PROCEDURE increment_onecol (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL
     , increment_value_in IN NUMBER DEFAULT 1
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
   rows_out table_varchar;
   BEGIN
      EXECUTE IMMEDIATE 'UPDATE MONITORIZATION_VS_PLAN set ' || colname_in || '=' || colname_in || ' + ' || nvl(increment_value_in,1) || ' WHERE ' || NVL (where_in, '1=1');
   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'DUPLICATE-VALUE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'MONITORIZATION_VS_PLAN');
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'MONITORIZATION_VS_PLAN');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'MONITORIZATION_VS_PLAN');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END increment_onecol;


   -- Delete functionality


   PROCEDURE del (
      id_monitorization_vs_plan_in IN MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS_PLAN%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM MONITORIZATION_VS_PLAN
       WHERE
          ID_MONITORIZATION_VS_PLAN = id_monitorization_vs_plan_in
       RETURNING ROWID BULK COLLECT INTO rows_out
         ;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'MONITORIZATION_VS_PLAN');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'MONITORIZATION_VS_PLAN');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del;




   PROCEDURE del (
      id_monitorization_vs_plan_in IN MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS_PLAN%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
rows_out TABLE_VARCHAR;
   BEGIN

del (
      id_monitorization_vs_plan_in => id_monitorization_vs_plan_in
     ,handle_error_in => handle_error_in
, rows_out => rows_out
      );

   END del;








   -- Delete all rows for primary key column ID_MONITORIZATION_VS_PLAN
   PROCEDURE del_ID_MONITORIZATION_VS_PLAN (
      id_monitorization_vs_plan_in IN MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS_PLAN%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
     )
   IS
   BEGIN
      DELETE FROM MONITORIZATION_VS_PLAN
       WHERE ID_MONITORIZATION_VS_PLAN = id_monitorization_vs_plan_in
      RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'MONITORIZATION_VS_PLAN');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'MONITORIZATION_VS_PLAN');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_ID_MONITORIZATION_VS_PLAN;






   -- Delete all rows for primary key column ID_MONITORIZATION_VS_PLAN
   PROCEDURE del_ID_MONITORIZATION_VS_PLAN (
      id_monitorization_vs_plan_in IN MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS_PLAN%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_ID_MONITORIZATION_VS_PLAN (
      id_monitorization_vs_plan_in => id_monitorization_vs_plan_in
     ,handle_error_in => handle_error_in
, rows_out => rows_out
     );
   END del_ID_MONITORIZATION_VS_PLAN;















   PROCEDURE del_MVSP_MVS_FK (
      id_monitorization_vs_in IN MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM MONITORIZATION_VS_PLAN
       WHERE
          ID_MONITORIZATION_VS = del_MVSP_MVS_FK.id_monitorization_vs_in
       RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'MONITORIZATION_VS_PLAN');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'MONITORIZATION_VS_PLAN');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_MVSP_MVS_FK;



PROCEDURE del_MVSP_MVS_FK (
      id_monitorization_vs_in IN MONITORIZATION_VS_PLAN.ID_MONITORIZATION_VS%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_MVSP_MVS_FK (
      id_monitorization_vs_in => id_monitorization_vs_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END del_MVSP_MVS_FK;












   -- Deletions using dynamic SQL
   FUNCTION dyndelstr (where_in IN VARCHAR2) RETURN VARCHAR2
   IS
   BEGIN
      IF where_in IS NULL
      THEN
         RETURN 'DELETE FROM MONITORIZATION_VS_PLAN';
      ELSE
         RETURN
            'DELETE FROM MONITORIZATION_VS_PLAN WHERE ' || where_in;
      END IF;
   END dyndelstr;

   FUNCTION dyncoldelstr (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE)
      RETURN VARCHAR2
   IS
   BEGIN
      RETURN dyndelstr ( colname_in || ' = :value' );
   END;

   PROCEDURE del_by (
      where_clause_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyndelstr (where_clause_in);
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'MONITORIZATION_VS_PLAN');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'MONITORIZATION_VS_PLAN');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by;





   PROCEDURE del_by (
      where_clause_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN ' || dyndelstr (where_clause_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;' using OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'MONITORIZATION_VS_PLAN');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'MONITORIZATION_VS_PLAN');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by;





   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyncoldelstr (colname_in)
         USING colvalue_in;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'MONITORIZATION_VS_PLAN');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'MONITORIZATION_VS_PLAN');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;






   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr (colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
         USING IN colvalue_in, OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'MONITORIZATION_VS_PLAN');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'MONITORIZATION_VS_PLAN');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN DATE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyncoldelstr (colname_in)
         USING colvalue_in;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'MONITORIZATION_VS_PLAN');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'MONITORIZATION_VS_PLAN');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN DATE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr (colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
         USING IN colvalue_in, OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'MONITORIZATION_VS_PLAN');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'MONITORIZATION_VS_PLAN');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN TIMESTAMP WITH LOCAL TIME ZONE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyncoldelstr (colname_in)
         USING colvalue_in;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'MONITORIZATION_VS_PLAN');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'MONITORIZATION_VS_PLAN');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN TIMESTAMP WITH LOCAL TIME ZONE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr (colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
         USING IN colvalue_in, OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'MONITORIZATION_VS_PLAN');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'MONITORIZATION_VS_PLAN');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN NUMBER
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyncoldelstr (colname_in)
         USING colvalue_in;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'MONITORIZATION_VS_PLAN');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'MONITORIZATION_VS_PLAN');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;






   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN NUMBER
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
   BEGIN
     EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr (colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
         USING IN colvalue_in, OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'MONITORIZATION_VS_PLAN');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'MONITORIZATION_VS_PLAN');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   -- Initialize a record with default values for columns in the table.
   PROCEDURE initrec (
      monitorization_vs_plan_inout IN OUT MONITORIZATION_VS_PLAN%ROWTYPE
   )
   IS
   BEGIN
      monitorization_vs_plan_inout.ID_MONITORIZATION_VS_PLAN := NULL;
      monitorization_vs_plan_inout.FLG_STATUS := NULL;
      monitorization_vs_plan_inout.ID_MONITORIZATION_VS := NULL;
      monitorization_vs_plan_inout.DT_PLAN_TSTZ := NULL;
      monitorization_vs_plan_inout.ID_PROF_PERFORMED := NULL;
      monitorization_vs_plan_inout.START_TIME := NULL;
      monitorization_vs_plan_inout.END_TIME := NULL;
      monitorization_vs_plan_inout.CREATE_USER := NULL;
      monitorization_vs_plan_inout.CREATE_TIME := NULL;
      monitorization_vs_plan_inout.CREATE_INSTITUTION := NULL;
      monitorization_vs_plan_inout.UPDATE_USER := NULL;
      monitorization_vs_plan_inout.UPDATE_TIME := NULL;
      monitorization_vs_plan_inout.UPDATE_INSTITUTION := NULL;
      monitorization_vs_plan_inout.FLG_STATUS_PREV := NULL;
   END initrec;

   FUNCTION initrec RETURN MONITORIZATION_VS_PLAN%ROWTYPE
   IS
      l_monitorization_vs_plan MONITORIZATION_VS_PLAN%ROWTYPE;
   BEGIN
      RETURN l_monitorization_vs_plan;
   END initrec;


   FUNCTION get_data_rowid(
        rows_in IN TABLE_VARCHAR
        ) RETURN MONITORIZATION_VS_PLAN_tc
   IS
        data MONITORIZATION_VS_PLAN_tc;
   BEGIN
        select * bulk collect into data from MONITORIZATION_VS_PLAN where rowid in (select /*+opt_estimate(table,t,scale_rows=0.0000001))*/ * from table(rows_in) t);
        return data;
        EXCEPTION
      WHEN OTHERS THEN
        pk_alert_exceptions.raise_error (
           error_name_in => 'get_data_rowid'
           );
   END get_data_rowid;


   FUNCTION get_data_rowid_pat(
        rows_in IN TABLE_VARCHAR
        ) RETURN MONITORIZATION_VS_PLAN_tc
   is
        PRAGMA AUTONOMOUS_TRANSACTION;
        data MONITORIZATION_VS_PLAN_tc;
   BEGIN
        data := get_data_rowid(rows_in);
        commit;
        return data;
        EXCEPTION
      WHEN OTHERS THEN
        pk_alert_exceptions.raise_error (
           error_name_in => 'get_data_rowid'
           );
        rollback;
    END get_data_rowid_pat;

BEGIN
   NULL;
END TS_MONITORIZATION_VS_PLAN;
/
