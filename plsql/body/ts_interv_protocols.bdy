/*-- Last Change Revision: $Rev: 1085666 $*/
/*-- Last Change by: $Author: nuno.neves $*/
/*-- Date of last change: $Date: 2011-09-08 11:57:31 +0100 (qui, 08 set 2011) $*/
CREATE OR REPLACE PACKAGE BODY TS_INTERV_PROTOCOLS
/*
| Generated by or retrieved from QCGU - DO NOT MODIFY!
| QCGU - "Get it right, do it fast" - www.ToadWorld.com
| QCGU Universal ID: {657BD35E-2E48-4CD4-B14E-41420B8433B3}
| Created On: Setembro 8, 2011 11:32:26
| Created By: ALERT
*/
IS


   e_null_column_value EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_null_column_value, -1400);
   --
   e_existing_fky_reference EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_existing_fky_reference, -2266);
   --
   e_check_constraint_failure EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_check_constraint_failure, -2290);
   --
   e_no_parent_key EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_no_parent_key, -2291);
   --
   e_child_record_found EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_child_record_found, -2292);
   --
   e_forall_error EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_forall_error, -24381);
   --
   -- Defined for backward compatibilty.
   e_integ_constraint_failure EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_integ_constraint_failure, -2291);

    -- Private utilities
   PROCEDURE get_constraint_info (
      owner_out OUT ALL_CONSTRAINTS.OWNER%TYPE
     ,name_out OUT ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE)
   IS
      l_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
      dotloc PLS_INTEGER;
      leftloc PLS_INTEGER;
   BEGIN
      dotloc  := INSTR (l_errm,'.');
      leftloc := INSTR (l_errm,'(');
      owner_out := SUBSTR (l_errm, leftloc+1, dotloc-leftloc-1);
      name_out  := SUBSTR (l_errm, dotloc+1, INSTR (l_errm,')')-dotloc-1);
   END get_constraint_info;
   -- Public programs

   PROCEDURE ins (
      id_interv_protocols_in IN INTERV_PROTOCOLS.ID_INTERV_PROTOCOLS%TYPE
      ,
      id_intervention_in IN INTERV_PROTOCOLS.ID_INTERVENTION%TYPE DEFAULT NULL,
      flg_available_in IN INTERV_PROTOCOLS.FLG_AVAILABLE%TYPE DEFAULT NULL,
      rank_in IN INTERV_PROTOCOLS.RANK%TYPE DEFAULT NULL,
      adw_last_update_in IN INTERV_PROTOCOLS.ADW_LAST_UPDATE%TYPE DEFAULT sysdate,
      flg_interv_type_in IN INTERV_PROTOCOLS.FLG_INTERV_TYPE%TYPE DEFAULT NULL,
      num_take_in IN INTERV_PROTOCOLS.NUM_TAKE%TYPE DEFAULT NULL,
      interval_in IN INTERV_PROTOCOLS.INTERVAL%TYPE DEFAULT NULL,
      dosage_in IN INTERV_PROTOCOLS.DOSAGE%TYPE DEFAULT NULL,
      create_user_in IN INTERV_PROTOCOLS.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN INTERV_PROTOCOLS.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN INTERV_PROTOCOLS.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN INTERV_PROTOCOLS.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN INTERV_PROTOCOLS.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN INTERV_PROTOCOLS.UPDATE_INSTITUTION%TYPE DEFAULT NULL
     ,handle_error_in IN BOOLEAN := TRUE
      , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN

     INSERT INTO INTERV_PROTOCOLS (
         ID_INTERV_PROTOCOLS,
         ID_INTERVENTION,
         FLG_AVAILABLE,
         RANK,
         ADW_LAST_UPDATE,
         FLG_INTERV_TYPE,
         NUM_TAKE,
         INTERVAL,
         DOSAGE,
         CREATE_USER,
         CREATE_TIME,
         CREATE_INSTITUTION,
         UPDATE_USER,
         UPDATE_TIME,
         UPDATE_INSTITUTION
         )
      VALUES (
         id_interv_protocols_in,
         id_intervention_in,
         flg_available_in,
         rank_in,
         adw_last_update_in,
         flg_interv_type_in,
         num_take_in,
         interval_in,
         dosage_in,
         create_user_in,
         create_time_in,
         create_institution_in,
         update_user_in,
         update_time_in,
         update_institution_in
         ) RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF FALSE THEN NULL; -- Placeholder in case no unique indexes
           ELSE
              pk_alert_exceptions.raise_error (
                    error_name_in => 'DUPLICATE-VALUE'
                    ,name1_in => 'OWNER'
                    ,value1_in => l_owner
                    ,name2_in => 'CONSTRAINT_NAME'
                    ,value2_in => l_name
                    ,name3_in => 'TABLE_NAME'
                    ,value3_in => 'INTERV_PROTOCOLS');
           END IF;
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_PROTOCOLS');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_PROTOCOLS');
           IF l_name = 'IPS_INT_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_INTERVENTION'
               , value_in => id_intervention_in);
           END IF;
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END ins;

   PROCEDURE ins (
      id_interv_protocols_in IN INTERV_PROTOCOLS.ID_INTERV_PROTOCOLS%TYPE
      ,
      id_intervention_in IN INTERV_PROTOCOLS.ID_INTERVENTION%TYPE DEFAULT NULL,
      flg_available_in IN INTERV_PROTOCOLS.FLG_AVAILABLE%TYPE DEFAULT NULL,
      rank_in IN INTERV_PROTOCOLS.RANK%TYPE DEFAULT NULL,
      adw_last_update_in IN INTERV_PROTOCOLS.ADW_LAST_UPDATE%TYPE DEFAULT sysdate,
      flg_interv_type_in IN INTERV_PROTOCOLS.FLG_INTERV_TYPE%TYPE DEFAULT NULL,
      num_take_in IN INTERV_PROTOCOLS.NUM_TAKE%TYPE DEFAULT NULL,
      interval_in IN INTERV_PROTOCOLS.INTERVAL%TYPE DEFAULT NULL,
      dosage_in IN INTERV_PROTOCOLS.DOSAGE%TYPE DEFAULT NULL,
      create_user_in IN INTERV_PROTOCOLS.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN INTERV_PROTOCOLS.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN INTERV_PROTOCOLS.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN INTERV_PROTOCOLS.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN INTERV_PROTOCOLS.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN INTERV_PROTOCOLS.UPDATE_INSTITUTION%TYPE DEFAULT NULL
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN

     ins (
      id_interv_protocols_in => id_interv_protocols_in
      ,
      id_intervention_in => id_intervention_in,
      flg_available_in => flg_available_in,
      rank_in => rank_in,
      adw_last_update_in => adw_last_update_in,
      flg_interv_type_in => flg_interv_type_in,
      num_take_in => num_take_in,
      interval_in => interval_in,
      dosage_in => dosage_in,
      create_user_in => create_user_in,
      create_time_in => create_time_in,
      create_institution_in => create_institution_in,
      update_user_in => update_user_in,
      update_time_in => update_time_in,
      update_institution_in => update_institution_in
     ,handle_error_in => handle_error_in
      ,rows_out => rows_out
      );
   END ins;


   PROCEDURE ins (
      rec_in IN INTERV_PROTOCOLS%ROWTYPE
     ,gen_pky_in IN BOOLEAN DEFAULT FALSE
     ,sequence_in IN VARCHAR2 := NULL
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
      l_rec INTERV_PROTOCOLS%ROWTYPE := rec_in;
   BEGIN
      IF gen_pky_in THEN
         l_rec.ID_INTERV_PROTOCOLS := next_key (sequence_in);
      END IF;
      ins (
         id_interv_protocols_in => l_rec.ID_INTERV_PROTOCOLS
         ,
         id_intervention_in => l_rec.ID_INTERVENTION,
         flg_available_in => l_rec.FLG_AVAILABLE,
         rank_in => l_rec.RANK,
         adw_last_update_in => l_rec.ADW_LAST_UPDATE,
         flg_interv_type_in => l_rec.FLG_INTERV_TYPE,
         num_take_in => l_rec.NUM_TAKE,
         interval_in => l_rec.INTERVAL,
         dosage_in => l_rec.DOSAGE,
         create_user_in => l_rec.CREATE_USER,
         create_time_in => l_rec.CREATE_TIME,
         create_institution_in => l_rec.CREATE_INSTITUTION,
         update_user_in => l_rec.UPDATE_USER,
         update_time_in => l_rec.UPDATE_TIME,
         update_institution_in => l_rec.UPDATE_INSTITUTION
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
   END ins;

   PROCEDURE ins (
      rec_in IN INTERV_PROTOCOLS%ROWTYPE
     ,gen_pky_in IN BOOLEAN DEFAULT FALSE
     ,sequence_in IN VARCHAR2 := NULL
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
      rows_out TABLE_VARCHAR;
   BEGIN

  ins (
      rec_in => rec_in
     ,gen_pky_in => gen_pky_in
     ,sequence_in => sequence_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
   );

   END ins;

   FUNCTION next_key (sequence_in IN VARCHAR2 := NULL) RETURN INTERV_PROTOCOLS.ID_INTERV_PROTOCOLS%TYPE

   IS
     retval INTERV_PROTOCOLS.ID_INTERV_PROTOCOLS%TYPE;

   BEGIN
      IF sequence_in IS NULL
      THEN
         SELECT seq_INTERV_PROTOCOLS.NEXTVAL INTO retval FROM dual;
      ELSE
         EXECUTE IMMEDIATE
            'SELECT ' || sequence_in || '.NEXTVAL FROM dual'
            INTO retval;
      END IF;
      RETURN retval;
   EXCEPTION
      WHEN OTHERS THEN
        pk_alert_exceptions.raise_error (
           error_name_in => 'SEQUENCE-GENERATION-FAILURE'
           ,name1_in => 'SEQUENCE'
           ,value1_in => NVL (sequence_in, 'seq_INTERV_PROTOCOLS')
           );
   END next_key;

   PROCEDURE ins (
      id_intervention_in IN INTERV_PROTOCOLS.ID_INTERVENTION%TYPE DEFAULT NULL,
      flg_available_in IN INTERV_PROTOCOLS.FLG_AVAILABLE%TYPE DEFAULT NULL,
      rank_in IN INTERV_PROTOCOLS.RANK%TYPE DEFAULT NULL,
      adw_last_update_in IN INTERV_PROTOCOLS.ADW_LAST_UPDATE%TYPE DEFAULT sysdate,
      flg_interv_type_in IN INTERV_PROTOCOLS.FLG_INTERV_TYPE%TYPE DEFAULT NULL,
      num_take_in IN INTERV_PROTOCOLS.NUM_TAKE%TYPE DEFAULT NULL,
      interval_in IN INTERV_PROTOCOLS.INTERVAL%TYPE DEFAULT NULL,
      dosage_in IN INTERV_PROTOCOLS.DOSAGE%TYPE DEFAULT NULL,
      create_user_in IN INTERV_PROTOCOLS.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN INTERV_PROTOCOLS.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN INTERV_PROTOCOLS.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN INTERV_PROTOCOLS.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN INTERV_PROTOCOLS.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN INTERV_PROTOCOLS.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      id_interv_protocols_out IN OUT INTERV_PROTOCOLS.ID_INTERV_PROTOCOLS%TYPE
      ,handle_error_in IN BOOLEAN := TRUE
      , rows_out OUT TABLE_VARCHAR
   )
   IS
        l_pky INTERV_PROTOCOLS.ID_INTERV_PROTOCOLS%TYPE := next_key;
   BEGIN
      ins (
         id_interv_protocols_in => l_pky,
         id_intervention_in => id_intervention_in,
         flg_available_in => flg_available_in,
         rank_in => rank_in,
         adw_last_update_in => adw_last_update_in,
         flg_interv_type_in => flg_interv_type_in,
         num_take_in => num_take_in,
         interval_in => interval_in,
         dosage_in => dosage_in,
         create_user_in => create_user_in,
         create_time_in => create_time_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
      id_interv_protocols_out := l_pky;
   END ins;

   PROCEDURE ins (
      id_intervention_in IN INTERV_PROTOCOLS.ID_INTERVENTION%TYPE DEFAULT NULL,
      flg_available_in IN INTERV_PROTOCOLS.FLG_AVAILABLE%TYPE DEFAULT NULL,
      rank_in IN INTERV_PROTOCOLS.RANK%TYPE DEFAULT NULL,
      adw_last_update_in IN INTERV_PROTOCOLS.ADW_LAST_UPDATE%TYPE DEFAULT sysdate,
      flg_interv_type_in IN INTERV_PROTOCOLS.FLG_INTERV_TYPE%TYPE DEFAULT NULL,
      num_take_in IN INTERV_PROTOCOLS.NUM_TAKE%TYPE DEFAULT NULL,
      interval_in IN INTERV_PROTOCOLS.INTERVAL%TYPE DEFAULT NULL,
      dosage_in IN INTERV_PROTOCOLS.DOSAGE%TYPE DEFAULT NULL,
      create_user_in IN INTERV_PROTOCOLS.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN INTERV_PROTOCOLS.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN INTERV_PROTOCOLS.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN INTERV_PROTOCOLS.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN INTERV_PROTOCOLS.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN INTERV_PROTOCOLS.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      id_interv_protocols_out IN OUT INTERV_PROTOCOLS.ID_INTERV_PROTOCOLS%TYPE
      ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
        rows_out TABLE_VARCHAR;
   BEGIN
      ins (
      id_intervention_in => id_intervention_in,
      flg_available_in => flg_available_in,
      rank_in => rank_in,
      adw_last_update_in => adw_last_update_in,
      flg_interv_type_in => flg_interv_type_in,
      num_take_in => num_take_in,
      interval_in => interval_in,
      dosage_in => dosage_in,
      create_user_in => create_user_in,
      create_time_in => create_time_in,
      create_institution_in => create_institution_in,
      update_user_in => update_user_in,
      update_time_in => update_time_in,
      update_institution_in => update_institution_in,
      id_interv_protocols_out => id_interv_protocols_out
      ,handle_error_in => handle_error_in
      , rows_out => rows_out
   );
   END ins;

   FUNCTION ins (
      id_intervention_in IN INTERV_PROTOCOLS.ID_INTERVENTION%TYPE DEFAULT NULL,
      flg_available_in IN INTERV_PROTOCOLS.FLG_AVAILABLE%TYPE DEFAULT NULL,
      rank_in IN INTERV_PROTOCOLS.RANK%TYPE DEFAULT NULL,
      adw_last_update_in IN INTERV_PROTOCOLS.ADW_LAST_UPDATE%TYPE DEFAULT sysdate,
      flg_interv_type_in IN INTERV_PROTOCOLS.FLG_INTERV_TYPE%TYPE DEFAULT NULL,
      num_take_in IN INTERV_PROTOCOLS.NUM_TAKE%TYPE DEFAULT NULL,
      interval_in IN INTERV_PROTOCOLS.INTERVAL%TYPE DEFAULT NULL,
      dosage_in IN INTERV_PROTOCOLS.DOSAGE%TYPE DEFAULT NULL,
      create_user_in IN INTERV_PROTOCOLS.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN INTERV_PROTOCOLS.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN INTERV_PROTOCOLS.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN INTERV_PROTOCOLS.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN INTERV_PROTOCOLS.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN INTERV_PROTOCOLS.UPDATE_INSTITUTION%TYPE DEFAULT NULL
      ,handle_error_in IN BOOLEAN := TRUE
      , rows_out OUT TABLE_VARCHAR
   )
      RETURN
         INTERV_PROTOCOLS.ID_INTERV_PROTOCOLS%TYPE
   IS
        l_pky INTERV_PROTOCOLS.ID_INTERV_PROTOCOLS%TYPE := next_key;
   BEGIN
      ins (
         id_interv_protocols_in => l_pky,
         id_intervention_in => id_intervention_in,
         flg_available_in => flg_available_in,
         rank_in => rank_in,
         adw_last_update_in => adw_last_update_in,
         flg_interv_type_in => flg_interv_type_in,
         num_take_in => num_take_in,
         interval_in => interval_in,
         dosage_in => dosage_in,
         create_user_in => create_user_in,
         create_time_in => create_time_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
      RETURN l_pky;
   END ins;

   FUNCTION ins (
      id_intervention_in IN INTERV_PROTOCOLS.ID_INTERVENTION%TYPE DEFAULT NULL,
      flg_available_in IN INTERV_PROTOCOLS.FLG_AVAILABLE%TYPE DEFAULT NULL,
      rank_in IN INTERV_PROTOCOLS.RANK%TYPE DEFAULT NULL,
      adw_last_update_in IN INTERV_PROTOCOLS.ADW_LAST_UPDATE%TYPE DEFAULT sysdate,
      flg_interv_type_in IN INTERV_PROTOCOLS.FLG_INTERV_TYPE%TYPE DEFAULT NULL,
      num_take_in IN INTERV_PROTOCOLS.NUM_TAKE%TYPE DEFAULT NULL,
      interval_in IN INTERV_PROTOCOLS.INTERVAL%TYPE DEFAULT NULL,
      dosage_in IN INTERV_PROTOCOLS.DOSAGE%TYPE DEFAULT NULL,
      create_user_in IN INTERV_PROTOCOLS.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN INTERV_PROTOCOLS.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN INTERV_PROTOCOLS.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN INTERV_PROTOCOLS.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN INTERV_PROTOCOLS.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN INTERV_PROTOCOLS.UPDATE_INSTITUTION%TYPE DEFAULT NULL
      ,handle_error_in IN BOOLEAN := TRUE
   )
      RETURN
         INTERV_PROTOCOLS.ID_INTERV_PROTOCOLS%TYPE
   IS
        l_pky INTERV_PROTOCOLS.ID_INTERV_PROTOCOLS%TYPE := next_key;
    rows_out TABLE_VARCHAR;
   BEGIN
      ins (
         id_interv_protocols_in => l_pky,
         id_intervention_in => id_intervention_in,
         flg_available_in => flg_available_in,
         rank_in => rank_in,
         adw_last_update_in => adw_last_update_in,
         flg_interv_type_in => flg_interv_type_in,
         num_take_in => num_take_in,
         interval_in => interval_in,
         dosage_in => dosage_in,
         create_user_in => create_user_in,
         create_time_in => create_time_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
      RETURN l_pky;
   END ins;

      PROCEDURE ins (
      id_intervention_in IN INTERV_PROTOCOLS.ID_INTERVENTION%TYPE DEFAULT NULL,
      flg_available_in IN INTERV_PROTOCOLS.FLG_AVAILABLE%TYPE DEFAULT NULL,
      rank_in IN INTERV_PROTOCOLS.RANK%TYPE DEFAULT NULL,
      adw_last_update_in IN INTERV_PROTOCOLS.ADW_LAST_UPDATE%TYPE DEFAULT sysdate,
      flg_interv_type_in IN INTERV_PROTOCOLS.FLG_INTERV_TYPE%TYPE DEFAULT NULL,
      num_take_in IN INTERV_PROTOCOLS.NUM_TAKE%TYPE DEFAULT NULL,
      interval_in IN INTERV_PROTOCOLS.INTERVAL%TYPE DEFAULT NULL,
      dosage_in IN INTERV_PROTOCOLS.DOSAGE%TYPE DEFAULT NULL,
      create_user_in IN INTERV_PROTOCOLS.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN INTERV_PROTOCOLS.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN INTERV_PROTOCOLS.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN INTERV_PROTOCOLS.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN INTERV_PROTOCOLS.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN INTERV_PROTOCOLS.UPDATE_INSTITUTION%TYPE DEFAULT NULL
      ,handle_error_in IN BOOLEAN := TRUE
      , rows_out OUT TABLE_VARCHAR
   )
   IS
        l_pky INTERV_PROTOCOLS.ID_INTERV_PROTOCOLS%TYPE := next_key;
   BEGIN
      ins (
         id_interv_protocols_in => l_pky,
         id_intervention_in => id_intervention_in,
         flg_available_in => flg_available_in,
         rank_in => rank_in,
         adw_last_update_in => adw_last_update_in,
         flg_interv_type_in => flg_interv_type_in,
         num_take_in => num_take_in,
         interval_in => interval_in,
         dosage_in => dosage_in,
         create_user_in => create_user_in,
         create_time_in => create_time_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
   END ins;


     PROCEDURE ins (
      id_intervention_in IN INTERV_PROTOCOLS.ID_INTERVENTION%TYPE DEFAULT NULL,
      flg_available_in IN INTERV_PROTOCOLS.FLG_AVAILABLE%TYPE DEFAULT NULL,
      rank_in IN INTERV_PROTOCOLS.RANK%TYPE DEFAULT NULL,
      adw_last_update_in IN INTERV_PROTOCOLS.ADW_LAST_UPDATE%TYPE DEFAULT sysdate,
      flg_interv_type_in IN INTERV_PROTOCOLS.FLG_INTERV_TYPE%TYPE DEFAULT NULL,
      num_take_in IN INTERV_PROTOCOLS.NUM_TAKE%TYPE DEFAULT NULL,
      interval_in IN INTERV_PROTOCOLS.INTERVAL%TYPE DEFAULT NULL,
      dosage_in IN INTERV_PROTOCOLS.DOSAGE%TYPE DEFAULT NULL,
      create_user_in IN INTERV_PROTOCOLS.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN INTERV_PROTOCOLS.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN INTERV_PROTOCOLS.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN INTERV_PROTOCOLS.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN INTERV_PROTOCOLS.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN INTERV_PROTOCOLS.UPDATE_INSTITUTION%TYPE DEFAULT NULL
      ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
        l_pky INTERV_PROTOCOLS.ID_INTERV_PROTOCOLS%TYPE := next_key;
      rows_out TABLE_VARCHAR;
   BEGIN
      ins (
         id_interv_protocols_in => l_pky,
         id_intervention_in => id_intervention_in,
         flg_available_in => flg_available_in,
         rank_in => rank_in,
         adw_last_update_in => adw_last_update_in,
         flg_interv_type_in => flg_interv_type_in,
         num_take_in => num_take_in,
         interval_in => interval_in,
         dosage_in => dosage_in,
         create_user_in => create_user_in,
         create_time_in => create_time_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
   END ins;

    PROCEDURE ins (
      rows_in IN INTERV_PROTOCOLS_tc
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
   BEGIN
      IF rows_in.COUNT = 0
      THEN
         NULL;
      ELSE
         FORALL indx IN rows_in.FIRST .. rows_in.LAST
            SAVE EXCEPTIONS
            INSERT INTO INTERV_PROTOCOLS VALUES rows_in (indx) RETURNING ROWID BULK COLLECT INTO rows_out;
      END IF;
   EXCEPTION
     WHEN e_forall_error
     THEN
        -- In Oracle9i and above, SAVE EXCEPTIONS will direct control
        -- here if any error occurs. We can then save all the error
        -- information out to the error instance.
       IF NOT handle_error_in THEN RAISE;
       ELSE
          <<bulk_handler>>
          DECLARE
             l_err_instance_id NUMBER;
          BEGIN
             -- For each error, write to the log.
             FOR indx IN 1 .. SQL%BULK_EXCEPTIONS.COUNT
             LOOP
                pk_alert_exceptions.register_error (
                    error_name_in => 'FORALL-INSERT-FAILURE'
                   ,err_instance_id_out => l_err_instance_id
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'BINDING_ROW_' || indx
                  ,value_in => SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX
                  ,validate_in => FALSE
                );
                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ERROR_AT_ROW_' || indx
                  ,value_in => SQL%BULK_EXCEPTIONS (indx).ERROR_CODE
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_INTERV_PROTOCOLS _' || indx
                  ,value_in => rows_in(indx).ID_INTERV_PROTOCOLS
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_INTERVENTION _' || indx
                  ,value_in => rows_in(indx).ID_INTERVENTION
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'FLG_AVAILABLE _' || indx
                  ,value_in => rows_in(indx).FLG_AVAILABLE
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'RANK _' || indx
                  ,value_in => rows_in(indx).RANK
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ADW_LAST_UPDATE _' || indx
                  ,value_in => rows_in(indx).ADW_LAST_UPDATE
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'FLG_INTERV_TYPE _' || indx
                  ,value_in => rows_in(indx).FLG_INTERV_TYPE
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'NUM_TAKE _' || indx
                  ,value_in => rows_in(indx).NUM_TAKE
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'INTERVAL _' || indx
                  ,value_in => rows_in(indx).INTERVAL
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'DOSAGE _' || indx
                  ,value_in => rows_in(indx).DOSAGE
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'CREATE_USER _' || indx
                  ,value_in => rows_in(indx).CREATE_USER
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'CREATE_TIME _' || indx
                  ,value_in => rows_in(indx).CREATE_TIME
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'CREATE_INSTITUTION _' || indx
                  ,value_in => rows_in(indx).CREATE_INSTITUTION
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'UPDATE_USER _' || indx
                  ,value_in => rows_in(indx).UPDATE_USER
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'UPDATE_TIME _' || indx
                  ,value_in => rows_in(indx).UPDATE_TIME
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'UPDATE_INSTITUTION _' || indx
                  ,value_in => rows_in(indx).UPDATE_INSTITUTION
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.raise_error_instance( err_instance_id_in => l_err_instance_id );
             END LOOP;
          END bulk_handler;
        END IF;
     WHEN OTHERS
     THEN
       IF NOT handle_error_in THEN RAISE;
       ELSE
       pk_alert_exceptions.raise_error(
          error_name_in => 'FORALL-INSERT-FAILURE'
          ,name1_in => 'TABLE_NAME'
          ,value1_in => 'INTERV_PROTOCOLS'
          ,name2_in => 'ROW_COUNT'
          ,value2_in => rows_in.COUNT
           );
       END IF;
   END ins;

    PROCEDURE ins (
      rows_in IN INTERV_PROTOCOLS_tc
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
      rows_out TABLE_VARCHAR;
   BEGIN
      ins (
      rows_in => rows_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
   );
   END ins;


PROCEDURE upd (
      id_interv_protocols_in IN INTERV_PROTOCOLS.ID_INTERV_PROTOCOLS%TYPE,
      id_intervention_in IN INTERV_PROTOCOLS.ID_INTERVENTION%TYPE DEFAULT NULL,
      ID_INTERVENTION_nin IN BOOLEAN := TRUE,
      flg_available_in IN INTERV_PROTOCOLS.FLG_AVAILABLE%TYPE DEFAULT NULL,
      FLG_AVAILABLE_nin IN BOOLEAN := TRUE,
      rank_in IN INTERV_PROTOCOLS.RANK%TYPE DEFAULT NULL,
      RANK_nin IN BOOLEAN := TRUE,
      adw_last_update_in IN INTERV_PROTOCOLS.ADW_LAST_UPDATE%TYPE DEFAULT NULL,
      ADW_LAST_UPDATE_nin IN BOOLEAN := TRUE,
      flg_interv_type_in IN INTERV_PROTOCOLS.FLG_INTERV_TYPE%TYPE DEFAULT NULL,
      FLG_INTERV_TYPE_nin IN BOOLEAN := TRUE,
      num_take_in IN INTERV_PROTOCOLS.NUM_TAKE%TYPE DEFAULT NULL,
      NUM_TAKE_nin IN BOOLEAN := TRUE,
      interval_in IN INTERV_PROTOCOLS.INTERVAL%TYPE DEFAULT NULL,
      INTERVAL_nin IN BOOLEAN := TRUE,
      dosage_in IN INTERV_PROTOCOLS.DOSAGE%TYPE DEFAULT NULL,
      DOSAGE_nin IN BOOLEAN := TRUE,
      create_user_in IN INTERV_PROTOCOLS.CREATE_USER%TYPE DEFAULT NULL,
      CREATE_USER_nin IN BOOLEAN := TRUE,
      create_time_in IN INTERV_PROTOCOLS.CREATE_TIME%TYPE DEFAULT NULL,
      CREATE_TIME_nin IN BOOLEAN := TRUE,
      create_institution_in IN INTERV_PROTOCOLS.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      CREATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      update_user_in IN INTERV_PROTOCOLS.UPDATE_USER%TYPE DEFAULT NULL,
      UPDATE_USER_nin IN BOOLEAN := TRUE,
      update_time_in IN INTERV_PROTOCOLS.UPDATE_TIME%TYPE DEFAULT NULL,
      UPDATE_TIME_nin IN BOOLEAN := TRUE,
      update_institution_in IN INTERV_PROTOCOLS.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      UPDATE_INSTITUTION_nin IN BOOLEAN := TRUE,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      )
   IS
   l_rows_out TABLE_VARCHAR;
   l_ID_INTERVENTION_n NUMBER(1);
   l_FLG_AVAILABLE_n NUMBER(1);
   l_RANK_n NUMBER(1);
   l_ADW_LAST_UPDATE_n NUMBER(1);
   l_FLG_INTERV_TYPE_n NUMBER(1);
   l_NUM_TAKE_n NUMBER(1);
   l_INTERVAL_n NUMBER(1);
   l_DOSAGE_n NUMBER(1);
   l_CREATE_USER_n NUMBER(1);
   l_CREATE_TIME_n NUMBER(1);
   l_CREATE_INSTITUTION_n NUMBER(1);
   l_UPDATE_USER_n NUMBER(1);
   l_UPDATE_TIME_n NUMBER(1);
   l_UPDATE_INSTITUTION_n NUMBER(1);
   BEGIN

   l_ID_INTERVENTION_n := sys.diutil.bool_to_int(ID_INTERVENTION_nin);
   l_FLG_AVAILABLE_n := sys.diutil.bool_to_int(FLG_AVAILABLE_nin);
   l_RANK_n := sys.diutil.bool_to_int(RANK_nin);
   l_ADW_LAST_UPDATE_n := sys.diutil.bool_to_int(ADW_LAST_UPDATE_nin);
   l_FLG_INTERV_TYPE_n := sys.diutil.bool_to_int(FLG_INTERV_TYPE_nin);
   l_NUM_TAKE_n := sys.diutil.bool_to_int(NUM_TAKE_nin);
   l_INTERVAL_n := sys.diutil.bool_to_int(INTERVAL_nin);
   l_DOSAGE_n := sys.diutil.bool_to_int(DOSAGE_nin);
   l_CREATE_USER_n := sys.diutil.bool_to_int(CREATE_USER_nin);
   l_CREATE_TIME_n := sys.diutil.bool_to_int(CREATE_TIME_nin);
   l_CREATE_INSTITUTION_n := sys.diutil.bool_to_int(CREATE_INSTITUTION_nin);
   l_UPDATE_USER_n := sys.diutil.bool_to_int(UPDATE_USER_nin);
   l_UPDATE_TIME_n := sys.diutil.bool_to_int(UPDATE_TIME_nin);
   l_UPDATE_INSTITUTION_n := sys.diutil.bool_to_int(UPDATE_INSTITUTION_nin);


         UPDATE INTERV_PROTOCOLS SET
     ID_INTERVENTION = decode (l_ID_INTERVENTION_n,0,id_intervention_in, NVL (id_intervention_in, ID_INTERVENTION)),
     FLG_AVAILABLE = decode (l_FLG_AVAILABLE_n,0,flg_available_in, NVL (flg_available_in, FLG_AVAILABLE)),
     RANK = decode (l_RANK_n,0,rank_in, NVL (rank_in, RANK)),
     ADW_LAST_UPDATE = decode (l_ADW_LAST_UPDATE_n,0,adw_last_update_in, NVL (adw_last_update_in, ADW_LAST_UPDATE)),
     FLG_INTERV_TYPE = decode (l_FLG_INTERV_TYPE_n,0,flg_interv_type_in, NVL (flg_interv_type_in, FLG_INTERV_TYPE)),
     NUM_TAKE = decode (l_NUM_TAKE_n,0,num_take_in, NVL (num_take_in, NUM_TAKE)),
     INTERVAL = decode (l_INTERVAL_n,0,interval_in, NVL (interval_in, INTERVAL)),
     DOSAGE = decode (l_DOSAGE_n,0,dosage_in, NVL (dosage_in, DOSAGE)),
     CREATE_USER = decode (l_CREATE_USER_n,0,create_user_in, NVL (create_user_in, CREATE_USER)),
     CREATE_TIME = decode (l_CREATE_TIME_n,0,create_time_in, NVL (create_time_in, CREATE_TIME)),
     CREATE_INSTITUTION = decode (l_CREATE_INSTITUTION_n,0,create_institution_in, NVL (create_institution_in, CREATE_INSTITUTION)),
     UPDATE_USER = decode (l_UPDATE_USER_n,0,update_user_in, NVL (update_user_in, UPDATE_USER)),
     UPDATE_TIME = decode (l_UPDATE_TIME_n,0,update_time_in, NVL (update_time_in, UPDATE_TIME)),
     UPDATE_INSTITUTION = decode (l_UPDATE_INSTITUTION_n,0,update_institution_in, NVL (update_institution_in, UPDATE_INSTITUTION))
          WHERE
             ID_INTERV_PROTOCOLS = id_interv_protocols_in
         RETURNING ROWID BULK COLLECT INTO l_rows_out;


if(rows_out is null)
then
rows_out := table_varchar();
end if;

rows_out :=  rows_out MULTISET UNION DISTINCT l_rows_out;

   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF FALSE THEN NULL; -- Placeholder in case no unique indexes
           ELSE
              pk_alert_exceptions.raise_error (
                    error_name_in => 'DUPLICATE-VALUE'
                    ,name1_in => 'OWNER'
                    ,value1_in => l_owner
                    ,name2_in => 'CONSTRAINT_NAME'
                    ,value2_in => l_name
                    ,name3_in => 'TABLE_NAME'
                    ,value3_in => 'INTERV_PROTOCOLS');
           END IF;
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_PROTOCOLS');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_PROTOCOLS');
           IF l_name = 'IPS_INT_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_INTERVENTION'
               , value_in => id_intervention_in);
           END IF;
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END upd;


   PROCEDURE upd (
      id_interv_protocols_in IN INTERV_PROTOCOLS.ID_INTERV_PROTOCOLS%TYPE,
      id_intervention_in IN INTERV_PROTOCOLS.ID_INTERVENTION%TYPE DEFAULT NULL,
      ID_INTERVENTION_nin IN BOOLEAN := TRUE,
      flg_available_in IN INTERV_PROTOCOLS.FLG_AVAILABLE%TYPE DEFAULT NULL,
      FLG_AVAILABLE_nin IN BOOLEAN := TRUE,
      rank_in IN INTERV_PROTOCOLS.RANK%TYPE DEFAULT NULL,
      RANK_nin IN BOOLEAN := TRUE,
      adw_last_update_in IN INTERV_PROTOCOLS.ADW_LAST_UPDATE%TYPE DEFAULT NULL,
      ADW_LAST_UPDATE_nin IN BOOLEAN := TRUE,
      flg_interv_type_in IN INTERV_PROTOCOLS.FLG_INTERV_TYPE%TYPE DEFAULT NULL,
      FLG_INTERV_TYPE_nin IN BOOLEAN := TRUE,
      num_take_in IN INTERV_PROTOCOLS.NUM_TAKE%TYPE DEFAULT NULL,
      NUM_TAKE_nin IN BOOLEAN := TRUE,
      interval_in IN INTERV_PROTOCOLS.INTERVAL%TYPE DEFAULT NULL,
      INTERVAL_nin IN BOOLEAN := TRUE,
      dosage_in IN INTERV_PROTOCOLS.DOSAGE%TYPE DEFAULT NULL,
      DOSAGE_nin IN BOOLEAN := TRUE,
      create_user_in IN INTERV_PROTOCOLS.CREATE_USER%TYPE DEFAULT NULL,
      CREATE_USER_nin IN BOOLEAN := TRUE,
      create_time_in IN INTERV_PROTOCOLS.CREATE_TIME%TYPE DEFAULT NULL,
      CREATE_TIME_nin IN BOOLEAN := TRUE,
      create_institution_in IN INTERV_PROTOCOLS.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      CREATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      update_user_in IN INTERV_PROTOCOLS.UPDATE_USER%TYPE DEFAULT NULL,
      UPDATE_USER_nin IN BOOLEAN := TRUE,
      update_time_in IN INTERV_PROTOCOLS.UPDATE_TIME%TYPE DEFAULT NULL,
      UPDATE_TIME_nin IN BOOLEAN := TRUE,
      update_institution_in IN INTERV_PROTOCOLS.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      UPDATE_INSTITUTION_nin IN BOOLEAN := TRUE,
     handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
     upd (
      id_interv_protocols_in => id_interv_protocols_in,
      id_intervention_in => id_intervention_in,
      ID_INTERVENTION_nin => ID_INTERVENTION_nin,
      flg_available_in => flg_available_in,
      FLG_AVAILABLE_nin => FLG_AVAILABLE_nin,
      rank_in => rank_in,
      RANK_nin => RANK_nin,
      adw_last_update_in => adw_last_update_in,
      ADW_LAST_UPDATE_nin => ADW_LAST_UPDATE_nin,
      flg_interv_type_in => flg_interv_type_in,
      FLG_INTERV_TYPE_nin => FLG_INTERV_TYPE_nin,
      num_take_in => num_take_in,
      NUM_TAKE_nin => NUM_TAKE_nin,
      interval_in => interval_in,
      INTERVAL_nin => INTERVAL_nin,
      dosage_in => dosage_in,
      DOSAGE_nin => DOSAGE_nin,
      create_user_in => create_user_in,
      CREATE_USER_nin => CREATE_USER_nin,
      create_time_in => create_time_in,
      CREATE_TIME_nin => CREATE_TIME_nin,
      create_institution_in => create_institution_in,
      CREATE_INSTITUTION_nin => CREATE_INSTITUTION_nin,
      update_user_in => update_user_in,
      UPDATE_USER_nin => UPDATE_USER_nin,
      update_time_in => update_time_in,
      UPDATE_TIME_nin => UPDATE_TIME_nin,
      update_institution_in => update_institution_in,
      UPDATE_INSTITUTION_nin => UPDATE_INSTITUTION_nin,
     handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END upd;

PROCEDURE upd (
      id_intervention_in IN INTERV_PROTOCOLS.ID_INTERVENTION%TYPE DEFAULT NULL,
      ID_INTERVENTION_nin IN BOOLEAN := TRUE,
      flg_available_in IN INTERV_PROTOCOLS.FLG_AVAILABLE%TYPE DEFAULT NULL,
      FLG_AVAILABLE_nin IN BOOLEAN := TRUE,
      rank_in IN INTERV_PROTOCOLS.RANK%TYPE DEFAULT NULL,
      RANK_nin IN BOOLEAN := TRUE,
      adw_last_update_in IN INTERV_PROTOCOLS.ADW_LAST_UPDATE%TYPE DEFAULT NULL,
      ADW_LAST_UPDATE_nin IN BOOLEAN := TRUE,
      flg_interv_type_in IN INTERV_PROTOCOLS.FLG_INTERV_TYPE%TYPE DEFAULT NULL,
      FLG_INTERV_TYPE_nin IN BOOLEAN := TRUE,
      num_take_in IN INTERV_PROTOCOLS.NUM_TAKE%TYPE DEFAULT NULL,
      NUM_TAKE_nin IN BOOLEAN := TRUE,
      interval_in IN INTERV_PROTOCOLS.INTERVAL%TYPE DEFAULT NULL,
      INTERVAL_nin IN BOOLEAN := TRUE,
      dosage_in IN INTERV_PROTOCOLS.DOSAGE%TYPE DEFAULT NULL,
      DOSAGE_nin IN BOOLEAN := TRUE,
      create_user_in IN INTERV_PROTOCOLS.CREATE_USER%TYPE DEFAULT NULL,
      CREATE_USER_nin IN BOOLEAN := TRUE,
      create_time_in IN INTERV_PROTOCOLS.CREATE_TIME%TYPE DEFAULT NULL,
      CREATE_TIME_nin IN BOOLEAN := TRUE,
      create_institution_in IN INTERV_PROTOCOLS.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      CREATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      update_user_in IN INTERV_PROTOCOLS.UPDATE_USER%TYPE DEFAULT NULL,
      UPDATE_USER_nin IN BOOLEAN := TRUE,
      update_time_in IN INTERV_PROTOCOLS.UPDATE_TIME%TYPE DEFAULT NULL,
      UPDATE_TIME_nin IN BOOLEAN := TRUE,
      update_institution_in IN INTERV_PROTOCOLS.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      UPDATE_INSTITUTION_nin IN BOOLEAN := TRUE,
    where_in varchar2 DEFAULT NULL,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      )
   IS
   l_sql VARCHAR2(32767);
   l_rows_out TABLE_VARCHAR;
   l_ID_INTERVENTION_n NUMBER(1);
   l_FLG_AVAILABLE_n NUMBER(1);
   l_RANK_n NUMBER(1);
   l_ADW_LAST_UPDATE_n NUMBER(1);
   l_FLG_INTERV_TYPE_n NUMBER(1);
   l_NUM_TAKE_n NUMBER(1);
   l_INTERVAL_n NUMBER(1);
   l_DOSAGE_n NUMBER(1);
   l_CREATE_USER_n NUMBER(1);
   l_CREATE_TIME_n NUMBER(1);
   l_CREATE_INSTITUTION_n NUMBER(1);
   l_UPDATE_USER_n NUMBER(1);
   l_UPDATE_TIME_n NUMBER(1);
   l_UPDATE_INSTITUTION_n NUMBER(1);
      id_interv_protocols_in INTERV_PROTOCOLS.ID_INTERV_PROTOCOLS%TYPE;
   BEGIN



      l_ID_INTERVENTION_n := sys.diutil.bool_to_int(ID_INTERVENTION_nin);
      l_FLG_AVAILABLE_n := sys.diutil.bool_to_int(FLG_AVAILABLE_nin);
      l_RANK_n := sys.diutil.bool_to_int(RANK_nin);
      l_ADW_LAST_UPDATE_n := sys.diutil.bool_to_int(ADW_LAST_UPDATE_nin);
      l_FLG_INTERV_TYPE_n := sys.diutil.bool_to_int(FLG_INTERV_TYPE_nin);
      l_NUM_TAKE_n := sys.diutil.bool_to_int(NUM_TAKE_nin);
      l_INTERVAL_n := sys.diutil.bool_to_int(INTERVAL_nin);
      l_DOSAGE_n := sys.diutil.bool_to_int(DOSAGE_nin);
      l_CREATE_USER_n := sys.diutil.bool_to_int(CREATE_USER_nin);
      l_CREATE_TIME_n := sys.diutil.bool_to_int(CREATE_TIME_nin);
      l_CREATE_INSTITUTION_n := sys.diutil.bool_to_int(CREATE_INSTITUTION_nin);
      l_UPDATE_USER_n := sys.diutil.bool_to_int(UPDATE_USER_nin);
      l_UPDATE_TIME_n := sys.diutil.bool_to_int(UPDATE_TIME_nin);
      l_UPDATE_INSTITUTION_n := sys.diutil.bool_to_int(UPDATE_INSTITUTION_nin);



l_sql := 'UPDATE INTERV_PROTOCOLS SET '
     || ' ID_INTERVENTION = decode (' || l_ID_INTERVENTION_n || ',0,:id_intervention_in, NVL (:id_intervention_in, ID_INTERVENTION)) '|| ','
     || ' FLG_AVAILABLE = decode (' || l_FLG_AVAILABLE_n || ',0,:flg_available_in, NVL (:flg_available_in, FLG_AVAILABLE)) '|| ','
     || ' RANK = decode (' || l_RANK_n || ',0,:rank_in, NVL (:rank_in, RANK)) '|| ','
     || ' ADW_LAST_UPDATE = decode (' || l_ADW_LAST_UPDATE_n || ',0,:adw_last_update_in, NVL (:adw_last_update_in, ADW_LAST_UPDATE)) '|| ','
     || ' FLG_INTERV_TYPE = decode (' || l_FLG_INTERV_TYPE_n || ',0,:flg_interv_type_in, NVL (:flg_interv_type_in, FLG_INTERV_TYPE)) '|| ','
     || ' NUM_TAKE = decode (' || l_NUM_TAKE_n || ',0,:num_take_in, NVL (:num_take_in, NUM_TAKE)) '|| ','
     || ' INTERVAL = decode (' || l_INTERVAL_n || ',0,:interval_in, NVL (:interval_in, INTERVAL)) '|| ','
     || ' DOSAGE = decode (' || l_DOSAGE_n || ',0,:dosage_in, NVL (:dosage_in, DOSAGE)) '|| ','
     || ' CREATE_USER = decode (' || l_CREATE_USER_n || ',0,:create_user_in, NVL (:create_user_in, CREATE_USER)) '|| ','
     || ' CREATE_TIME = decode (' || l_CREATE_TIME_n || ',0,:create_time_in, NVL (:create_time_in, CREATE_TIME)) '|| ','
     || ' CREATE_INSTITUTION = decode (' || l_CREATE_INSTITUTION_n || ',0,:create_institution_in, NVL (:create_institution_in, CREATE_INSTITUTION)) '|| ','
     || ' UPDATE_USER = decode (' || l_UPDATE_USER_n || ',0,:update_user_in, NVL (:update_user_in, UPDATE_USER)) '|| ','
     || ' UPDATE_TIME = decode (' || l_UPDATE_TIME_n || ',0,:update_time_in, NVL (:update_time_in, UPDATE_TIME)) '|| ','
     || ' UPDATE_INSTITUTION = decode (' || l_UPDATE_INSTITUTION_n || ',0,:update_institution_in, NVL (:update_institution_in, UPDATE_INSTITUTION)) '
      || ' where ' || nvl(where_in,'(1=1)')
      || ' RETURNING ROWID BULK COLLECT INTO :l_rows_out';




execute immediate 'BEGIN ' || l_sql || '; END;' using in
     id_intervention_in,
     flg_available_in,
     rank_in,
     adw_last_update_in,
     flg_interv_type_in,
     num_take_in,
     interval_in,
     dosage_in,
     create_user_in,
     create_time_in,
     create_institution_in,
     update_user_in,
     update_time_in,
     update_institution_in,
    OUT l_rows_out;

if(rows_out is null)
then
rows_out := table_varchar();
end if;

rows_out :=  rows_out MULTISET UNION DISTINCT l_rows_out;

   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF FALSE THEN NULL; -- Placeholder in case no unique indexes
           ELSE
              pk_alert_exceptions.raise_error (
                    error_name_in => 'DUPLICATE-VALUE'
                    ,name1_in => 'OWNER'
                    ,value1_in => l_owner
                    ,name2_in => 'CONSTRAINT_NAME'
                    ,value2_in => l_name
                    ,name3_in => 'TABLE_NAME'
                    ,value3_in => 'INTERV_PROTOCOLS');
           END IF;
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_PROTOCOLS');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_PROTOCOLS');
           IF l_name = 'IPS_INT_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_INTERVENTION'
               , value_in => id_intervention_in);
           END IF;
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END upd;





PROCEDURE upd (
      id_intervention_in IN INTERV_PROTOCOLS.ID_INTERVENTION%TYPE DEFAULT NULL,
      ID_INTERVENTION_nin IN BOOLEAN := TRUE,
      flg_available_in IN INTERV_PROTOCOLS.FLG_AVAILABLE%TYPE DEFAULT NULL,
      FLG_AVAILABLE_nin IN BOOLEAN := TRUE,
      rank_in IN INTERV_PROTOCOLS.RANK%TYPE DEFAULT NULL,
      RANK_nin IN BOOLEAN := TRUE,
      adw_last_update_in IN INTERV_PROTOCOLS.ADW_LAST_UPDATE%TYPE DEFAULT NULL,
      ADW_LAST_UPDATE_nin IN BOOLEAN := TRUE,
      flg_interv_type_in IN INTERV_PROTOCOLS.FLG_INTERV_TYPE%TYPE DEFAULT NULL,
      FLG_INTERV_TYPE_nin IN BOOLEAN := TRUE,
      num_take_in IN INTERV_PROTOCOLS.NUM_TAKE%TYPE DEFAULT NULL,
      NUM_TAKE_nin IN BOOLEAN := TRUE,
      interval_in IN INTERV_PROTOCOLS.INTERVAL%TYPE DEFAULT NULL,
      INTERVAL_nin IN BOOLEAN := TRUE,
      dosage_in IN INTERV_PROTOCOLS.DOSAGE%TYPE DEFAULT NULL,
      DOSAGE_nin IN BOOLEAN := TRUE,
      create_user_in IN INTERV_PROTOCOLS.CREATE_USER%TYPE DEFAULT NULL,
      CREATE_USER_nin IN BOOLEAN := TRUE,
      create_time_in IN INTERV_PROTOCOLS.CREATE_TIME%TYPE DEFAULT NULL,
      CREATE_TIME_nin IN BOOLEAN := TRUE,
      create_institution_in IN INTERV_PROTOCOLS.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      CREATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      update_user_in IN INTERV_PROTOCOLS.UPDATE_USER%TYPE DEFAULT NULL,
      UPDATE_USER_nin IN BOOLEAN := TRUE,
      update_time_in IN INTERV_PROTOCOLS.UPDATE_TIME%TYPE DEFAULT NULL,
      UPDATE_TIME_nin IN BOOLEAN := TRUE,
      update_institution_in IN INTERV_PROTOCOLS.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      UPDATE_INSTITUTION_nin IN BOOLEAN := TRUE,
    where_in varchar2 DEFAULT NULL,
     handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
      upd (
      id_intervention_in => id_intervention_in,
      ID_INTERVENTION_nin => ID_INTERVENTION_nin,
      flg_available_in => flg_available_in,
      FLG_AVAILABLE_nin => FLG_AVAILABLE_nin,
      rank_in => rank_in,
      RANK_nin => RANK_nin,
      adw_last_update_in => adw_last_update_in,
      ADW_LAST_UPDATE_nin => ADW_LAST_UPDATE_nin,
      flg_interv_type_in => flg_interv_type_in,
      FLG_INTERV_TYPE_nin => FLG_INTERV_TYPE_nin,
      num_take_in => num_take_in,
      NUM_TAKE_nin => NUM_TAKE_nin,
      interval_in => interval_in,
      INTERVAL_nin => INTERVAL_nin,
      dosage_in => dosage_in,
      DOSAGE_nin => DOSAGE_nin,
      create_user_in => create_user_in,
      CREATE_USER_nin => CREATE_USER_nin,
      create_time_in => create_time_in,
      CREATE_TIME_nin => CREATE_TIME_nin,
      create_institution_in => create_institution_in,
      CREATE_INSTITUTION_nin => CREATE_INSTITUTION_nin,
      update_user_in => update_user_in,
      UPDATE_USER_nin => UPDATE_USER_nin,
      update_time_in => update_time_in,
      UPDATE_TIME_nin => UPDATE_TIME_nin,
      update_institution_in => update_institution_in,
      UPDATE_INSTITUTION_nin => UPDATE_INSTITUTION_nin,
    where_in => where_in,
     handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END upd;

   PROCEDURE upd (
      rec_in IN INTERV_PROTOCOLS%ROWTYPE,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      upd (
         id_interv_protocols_in => rec_in.ID_INTERV_PROTOCOLS,
         id_intervention_in => rec_in.ID_INTERVENTION,
         flg_available_in => rec_in.FLG_AVAILABLE,
         rank_in => rec_in.RANK,
         adw_last_update_in => rec_in.ADW_LAST_UPDATE,
         flg_interv_type_in => rec_in.FLG_INTERV_TYPE,
         num_take_in => rec_in.NUM_TAKE,
         interval_in => rec_in.INTERVAL,
         dosage_in => rec_in.DOSAGE,
         create_user_in => rec_in.CREATE_USER,
         create_time_in => rec_in.CREATE_TIME,
         create_institution_in => rec_in.CREATE_INSTITUTION,
         update_user_in => rec_in.UPDATE_USER,
         update_time_in => rec_in.UPDATE_TIME,
         update_institution_in => rec_in.UPDATE_INSTITUTION

        ,handle_error_in => handle_error_in
        , rows_out => rows_out
       );
   END upd;

   PROCEDURE upd (
      rec_in IN INTERV_PROTOCOLS%ROWTYPE,
     handle_error_in IN BOOLEAN := TRUE
      )
   IS
        rows_out TABLE_VARCHAR;
   BEGIN
      upd (
         id_interv_protocols_in => rec_in.ID_INTERV_PROTOCOLS,
         id_intervention_in => rec_in.ID_INTERVENTION,
         flg_available_in => rec_in.FLG_AVAILABLE,
         rank_in => rec_in.RANK,
         adw_last_update_in => rec_in.ADW_LAST_UPDATE,
         flg_interv_type_in => rec_in.FLG_INTERV_TYPE,
         num_take_in => rec_in.NUM_TAKE,
         interval_in => rec_in.INTERVAL,
         dosage_in => rec_in.DOSAGE,
         create_user_in => rec_in.CREATE_USER,
         create_time_in => rec_in.CREATE_TIME,
         create_institution_in => rec_in.CREATE_INSTITUTION,
         update_user_in => rec_in.UPDATE_USER,
         update_time_in => rec_in.UPDATE_TIME,
         update_institution_in => rec_in.UPDATE_INSTITUTION

        ,handle_error_in => handle_error_in
        , rows_out => rows_out
       );
   END upd;

   PROCEDURE upd_ins (
      id_interv_protocols_in IN INTERV_PROTOCOLS.ID_INTERV_PROTOCOLS%TYPE,
      id_intervention_in IN INTERV_PROTOCOLS.ID_INTERVENTION%TYPE DEFAULT NULL,
      flg_available_in IN INTERV_PROTOCOLS.FLG_AVAILABLE%TYPE DEFAULT NULL,
      rank_in IN INTERV_PROTOCOLS.RANK%TYPE DEFAULT NULL,
      adw_last_update_in IN INTERV_PROTOCOLS.ADW_LAST_UPDATE%TYPE DEFAULT NULL,
      flg_interv_type_in IN INTERV_PROTOCOLS.FLG_INTERV_TYPE%TYPE DEFAULT NULL,
      num_take_in IN INTERV_PROTOCOLS.NUM_TAKE%TYPE DEFAULT NULL,
      interval_in IN INTERV_PROTOCOLS.INTERVAL%TYPE DEFAULT NULL,
      dosage_in IN INTERV_PROTOCOLS.DOSAGE%TYPE DEFAULT NULL,
      create_user_in IN INTERV_PROTOCOLS.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN INTERV_PROTOCOLS.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN INTERV_PROTOCOLS.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN INTERV_PROTOCOLS.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN INTERV_PROTOCOLS.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN INTERV_PROTOCOLS.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      upd (
         id_interv_protocols_in => id_interv_protocols_in,
         id_intervention_in => id_intervention_in,
         flg_available_in => flg_available_in,
         rank_in => rank_in,
         adw_last_update_in => adw_last_update_in,
         flg_interv_type_in => flg_interv_type_in,
         num_take_in => num_take_in,
         interval_in => interval_in,
         dosage_in => dosage_in,
         create_user_in => create_user_in,
         create_time_in => create_time_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
       );
      IF SQL%ROWCOUNT = 0
      THEN
         ins (
            id_interv_protocols_in => id_interv_protocols_in,
            id_intervention_in => id_intervention_in,
            flg_available_in => flg_available_in,
            rank_in => rank_in,
            adw_last_update_in => adw_last_update_in,
            flg_interv_type_in => flg_interv_type_in,
            num_take_in => num_take_in,
            interval_in => interval_in,
            dosage_in => dosage_in,
            create_user_in => create_user_in,
            create_time_in => create_time_in,
            create_institution_in => create_institution_in,
            update_user_in => update_user_in,
            update_time_in => update_time_in,
            update_institution_in => update_institution_in
            ,handle_error_in => handle_error_in
            , rows_out => rows_out
         );
      END IF;
   END upd_ins;

   PROCEDURE upd_ins (
      id_interv_protocols_in IN INTERV_PROTOCOLS.ID_INTERV_PROTOCOLS%TYPE,
      id_intervention_in IN INTERV_PROTOCOLS.ID_INTERVENTION%TYPE DEFAULT NULL,
      flg_available_in IN INTERV_PROTOCOLS.FLG_AVAILABLE%TYPE DEFAULT NULL,
      rank_in IN INTERV_PROTOCOLS.RANK%TYPE DEFAULT NULL,
      adw_last_update_in IN INTERV_PROTOCOLS.ADW_LAST_UPDATE%TYPE DEFAULT NULL,
      flg_interv_type_in IN INTERV_PROTOCOLS.FLG_INTERV_TYPE%TYPE DEFAULT NULL,
      num_take_in IN INTERV_PROTOCOLS.NUM_TAKE%TYPE DEFAULT NULL,
      interval_in IN INTERV_PROTOCOLS.INTERVAL%TYPE DEFAULT NULL,
      dosage_in IN INTERV_PROTOCOLS.DOSAGE%TYPE DEFAULT NULL,
      create_user_in IN INTERV_PROTOCOLS.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN INTERV_PROTOCOLS.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN INTERV_PROTOCOLS.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN INTERV_PROTOCOLS.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN INTERV_PROTOCOLS.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN INTERV_PROTOCOLS.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
     handle_error_in IN BOOLEAN := TRUE
      )
   IS
        rows_out TABLE_VARCHAR;
   BEGIN
      upd_ins (
      id_interv_protocols_in,
      id_intervention_in,
      flg_available_in,
      rank_in,
      adw_last_update_in,
      flg_interv_type_in,
      num_take_in,
      interval_in,
      dosage_in,
      create_user_in,
      create_time_in,
      create_institution_in,
      update_user_in,
      update_time_in,
      update_institution_in,
     handle_error_in
     ,rows_out
      );
   END upd_ins;


   PROCEDURE upd (
      col_in IN INTERV_PROTOCOLS_tc,
      ignore_if_null_in IN BOOLEAN := TRUE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      )
   IS
      l_ID_INTERV_PROTOCOLS ID_INTERV_PROTOCOLS_cc;
      l_ID_INTERVENTION ID_INTERVENTION_cc;
      l_FLG_AVAILABLE FLG_AVAILABLE_cc;
      l_RANK RANK_cc;
      l_ADW_LAST_UPDATE ADW_LAST_UPDATE_cc;
      l_FLG_INTERV_TYPE FLG_INTERV_TYPE_cc;
      l_NUM_TAKE NUM_TAKE_cc;
      l_INTERVAL INTERVAL_cc;
      l_DOSAGE DOSAGE_cc;
      l_CREATE_USER CREATE_USER_cc;
      l_CREATE_TIME CREATE_TIME_cc;
      l_CREATE_INSTITUTION CREATE_INSTITUTION_cc;
      l_UPDATE_USER UPDATE_USER_cc;
      l_UPDATE_TIME UPDATE_TIME_cc;
      l_UPDATE_INSTITUTION UPDATE_INSTITUTION_cc;
   BEGIN
      FOR i IN col_in.FIRST .. col_in.LAST loop
         l_ID_INTERV_PROTOCOLS(i) := col_in(i).ID_INTERV_PROTOCOLS;
         l_ID_INTERVENTION(i) := col_in(i).ID_INTERVENTION;
         l_FLG_AVAILABLE(i) := col_in(i).FLG_AVAILABLE;
         l_RANK(i) := col_in(i).RANK;
         l_ADW_LAST_UPDATE(i) := col_in(i).ADW_LAST_UPDATE;
         l_FLG_INTERV_TYPE(i) := col_in(i).FLG_INTERV_TYPE;
         l_NUM_TAKE(i) := col_in(i).NUM_TAKE;
         l_INTERVAL(i) := col_in(i).INTERVAL;
         l_DOSAGE(i) := col_in(i).DOSAGE;
         l_CREATE_USER(i) := col_in(i).CREATE_USER;
         l_CREATE_TIME(i) := col_in(i).CREATE_TIME;
         l_CREATE_INSTITUTION(i) := col_in(i).CREATE_INSTITUTION;
         l_UPDATE_USER(i) := col_in(i).UPDATE_USER;
         l_UPDATE_TIME(i) := col_in(i).UPDATE_TIME;
         l_UPDATE_INSTITUTION(i) := col_in(i).UPDATE_INSTITUTION;
      END LOOP;
      IF NVL (ignore_if_null_in, FALSE)
      THEN
         -- Set any columns to their current values
         -- if incoming value is NULL.
         -- Put WHEN clause on column-level triggers!
         FORALL i IN col_in.FIRST .. col_in.LAST
            UPDATE INTERV_PROTOCOLS SET
               ID_INTERVENTION = NVL (l_ID_INTERVENTION(i), ID_INTERVENTION),
               FLG_AVAILABLE = NVL (l_FLG_AVAILABLE(i), FLG_AVAILABLE),
               RANK = NVL (l_RANK(i), RANK),
               ADW_LAST_UPDATE = NVL (l_ADW_LAST_UPDATE(i), ADW_LAST_UPDATE),
               FLG_INTERV_TYPE = NVL (l_FLG_INTERV_TYPE(i), FLG_INTERV_TYPE),
               NUM_TAKE = NVL (l_NUM_TAKE(i), NUM_TAKE),
               INTERVAL = NVL (l_INTERVAL(i), INTERVAL),
               DOSAGE = NVL (l_DOSAGE(i), DOSAGE),
               CREATE_USER = NVL (l_CREATE_USER(i), CREATE_USER),
               CREATE_TIME = NVL (l_CREATE_TIME(i), CREATE_TIME),
               CREATE_INSTITUTION = NVL (l_CREATE_INSTITUTION(i), CREATE_INSTITUTION),
               UPDATE_USER = NVL (l_UPDATE_USER(i), UPDATE_USER),
               UPDATE_TIME = NVL (l_UPDATE_TIME(i), UPDATE_TIME),
               UPDATE_INSTITUTION = NVL (l_UPDATE_INSTITUTION(i), UPDATE_INSTITUTION)
             WHERE
                ID_INTERV_PROTOCOLS = l_ID_INTERV_PROTOCOLS(i)
          returning rowid bulk collect into rows_out;
      ELSE
         FORALL i IN col_in.FIRST .. col_in.LAST
            UPDATE INTERV_PROTOCOLS SET
               ID_INTERVENTION = l_ID_INTERVENTION(i),
               FLG_AVAILABLE = l_FLG_AVAILABLE(i),
               RANK = l_RANK(i),
               ADW_LAST_UPDATE = l_ADW_LAST_UPDATE(i),
               FLG_INTERV_TYPE = l_FLG_INTERV_TYPE(i),
               NUM_TAKE = l_NUM_TAKE(i),
               INTERVAL = l_INTERVAL(i),
               DOSAGE = l_DOSAGE(i),
               CREATE_USER = l_CREATE_USER(i),
               CREATE_TIME = l_CREATE_TIME(i),
               CREATE_INSTITUTION = l_CREATE_INSTITUTION(i),
               UPDATE_USER = l_UPDATE_USER(i),
               UPDATE_TIME = l_UPDATE_TIME(i),
               UPDATE_INSTITUTION = l_UPDATE_INSTITUTION(i)
             WHERE
                ID_INTERV_PROTOCOLS = l_ID_INTERV_PROTOCOLS(i)
        returning rowid bulk collect into rows_out  ;
      END IF;
   END upd;


   PROCEDURE upd (
      col_in IN INTERV_PROTOCOLS_tc,
      ignore_if_null_in IN BOOLEAN := TRUE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
     rows_out TABLE_VARCHAR;
   BEGIN
      upd (
      col_in ,
      ignore_if_null_in
     ,handle_error_in
     , rows_out
      );
   END upd;

   FUNCTION dynupdstr (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL)

   RETURN VARCHAR2
   IS
   BEGIN
      RETURN
         'BEGIN UPDATE INTERV_PROTOCOLS
             SET ' || colname_in || ' = :value
           WHERE ' || NVL (where_in, '1=1') || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;' ;
   END dynupdstr;

   FUNCTION dynupdstr_no_rows_out (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL)

   RETURN VARCHAR2
   IS
   BEGIN
      RETURN
         'UPDATE INTERV_PROTOCOLS
             SET ' || colname_in || ' = :value
           WHERE ' || NVL (where_in, '1=1');
   END dynupdstr_no_rows_out;























  PROCEDURE increment_onecol (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL
      , increment_value_in IN NUMBER DEFAULT 1
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN UPDATE INTERV_PROTOCOLS set ' || colname_in || '=' || colname_in || ' + ' || nvl(increment_value_in,1) || ' WHERE ' || NVL (where_in, '1=1') || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
      USING OUT rows_out;
   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'DUPLICATE-VALUE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_PROTOCOLS');
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_PROTOCOLS');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_PROTOCOLS');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END increment_onecol;

   PROCEDURE increment_onecol (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL
     , increment_value_in IN NUMBER DEFAULT 1
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
   rows_out table_varchar;
   BEGIN
      EXECUTE IMMEDIATE 'UPDATE INTERV_PROTOCOLS set ' || colname_in || '=' || colname_in || ' + ' || nvl(increment_value_in,1) || ' WHERE ' || NVL (where_in, '1=1');
   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'DUPLICATE-VALUE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_PROTOCOLS');
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_PROTOCOLS');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_PROTOCOLS');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END increment_onecol;


   -- Delete functionality


   PROCEDURE del (
      id_interv_protocols_in IN INTERV_PROTOCOLS.ID_INTERV_PROTOCOLS%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM INTERV_PROTOCOLS
       WHERE
          ID_INTERV_PROTOCOLS = id_interv_protocols_in
       RETURNING ROWID BULK COLLECT INTO rows_out
         ;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_PROTOCOLS');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_PROTOCOLS');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del;




   PROCEDURE del (
      id_interv_protocols_in IN INTERV_PROTOCOLS.ID_INTERV_PROTOCOLS%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
rows_out TABLE_VARCHAR;
   BEGIN

del (
      id_interv_protocols_in => id_interv_protocols_in
     ,handle_error_in => handle_error_in
, rows_out => rows_out
      );

   END del;








   -- Delete all rows for primary key column ID_INTERV_PROTOCOLS
   PROCEDURE del_ID_INTERV_PROTOCOLS (
      id_interv_protocols_in IN INTERV_PROTOCOLS.ID_INTERV_PROTOCOLS%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
     )
   IS
   BEGIN
      DELETE FROM INTERV_PROTOCOLS
       WHERE ID_INTERV_PROTOCOLS = id_interv_protocols_in
      RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_PROTOCOLS');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_PROTOCOLS');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_ID_INTERV_PROTOCOLS;






   -- Delete all rows for primary key column ID_INTERV_PROTOCOLS
   PROCEDURE del_ID_INTERV_PROTOCOLS (
      id_interv_protocols_in IN INTERV_PROTOCOLS.ID_INTERV_PROTOCOLS%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_ID_INTERV_PROTOCOLS (
      id_interv_protocols_in => id_interv_protocols_in
     ,handle_error_in => handle_error_in
, rows_out => rows_out
     );
   END del_ID_INTERV_PROTOCOLS;















   PROCEDURE del_IPS_INT_FK (
      id_intervention_in IN INTERV_PROTOCOLS.ID_INTERVENTION%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM INTERV_PROTOCOLS
       WHERE
          ID_INTERVENTION = del_IPS_INT_FK.id_intervention_in
       RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_PROTOCOLS');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_PROTOCOLS');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_IPS_INT_FK;



PROCEDURE del_IPS_INT_FK (
      id_intervention_in IN INTERV_PROTOCOLS.ID_INTERVENTION%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_IPS_INT_FK (
      id_intervention_in => id_intervention_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END del_IPS_INT_FK;












   -- Deletions using dynamic SQL
   FUNCTION dyndelstr (where_in IN VARCHAR2) RETURN VARCHAR2
   IS
   BEGIN
      IF where_in IS NULL
      THEN
         RETURN 'DELETE FROM INTERV_PROTOCOLS';
      ELSE
         RETURN
            'DELETE FROM INTERV_PROTOCOLS WHERE ' || where_in;
      END IF;
   END dyndelstr;

   FUNCTION dyncoldelstr (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE)
      RETURN VARCHAR2
   IS
   BEGIN
      RETURN dyndelstr ( colname_in || ' = :value' );
   END;

   PROCEDURE del_by (
      where_clause_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyndelstr (where_clause_in);
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_PROTOCOLS');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_PROTOCOLS');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by;





   PROCEDURE del_by (
      where_clause_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN ' || dyndelstr (where_clause_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;' using OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_PROTOCOLS');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_PROTOCOLS');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by;





   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyncoldelstr (colname_in)
         USING colvalue_in;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_PROTOCOLS');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_PROTOCOLS');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;






   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr (colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
         USING IN colvalue_in, OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_PROTOCOLS');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_PROTOCOLS');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN DATE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyncoldelstr (colname_in)
         USING colvalue_in;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_PROTOCOLS');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_PROTOCOLS');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN DATE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr (colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
         USING IN colvalue_in, OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_PROTOCOLS');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_PROTOCOLS');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN TIMESTAMP WITH LOCAL TIME ZONE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyncoldelstr (colname_in)
         USING colvalue_in;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_PROTOCOLS');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_PROTOCOLS');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN TIMESTAMP WITH LOCAL TIME ZONE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr (colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
         USING IN colvalue_in, OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_PROTOCOLS');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_PROTOCOLS');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN NUMBER
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyncoldelstr (colname_in)
         USING colvalue_in;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_PROTOCOLS');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_PROTOCOLS');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;






   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN NUMBER
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
   BEGIN
     EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr (colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
         USING IN colvalue_in, OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'INTERV_PROTOCOLS');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'INTERV_PROTOCOLS');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   -- Initialize a record with default values for columns in the table.
   PROCEDURE initrec (
      interv_protocols_inout IN OUT INTERV_PROTOCOLS%ROWTYPE
   )
   IS
   BEGIN
      interv_protocols_inout.ID_INTERV_PROTOCOLS := NULL;
      interv_protocols_inout.ID_INTERVENTION := NULL;
      interv_protocols_inout.FLG_AVAILABLE := NULL;
      interv_protocols_inout.RANK := NULL;
      interv_protocols_inout.ADW_LAST_UPDATE := sysdate;
      interv_protocols_inout.FLG_INTERV_TYPE := NULL;
      interv_protocols_inout.NUM_TAKE := NULL;
      interv_protocols_inout.INTERVAL := NULL;
      interv_protocols_inout.DOSAGE := NULL;
      interv_protocols_inout.CREATE_USER := NULL;
      interv_protocols_inout.CREATE_TIME := NULL;
      interv_protocols_inout.CREATE_INSTITUTION := NULL;
      interv_protocols_inout.UPDATE_USER := NULL;
      interv_protocols_inout.UPDATE_TIME := NULL;
      interv_protocols_inout.UPDATE_INSTITUTION := NULL;
   END initrec;

   FUNCTION initrec RETURN INTERV_PROTOCOLS%ROWTYPE
   IS
      l_interv_protocols INTERV_PROTOCOLS%ROWTYPE;
   BEGIN
      l_interv_protocols.ADW_LAST_UPDATE := sysdate;
      RETURN l_interv_protocols;
   END initrec;


   FUNCTION get_data_rowid(
        rows_in IN TABLE_VARCHAR
        ) RETURN INTERV_PROTOCOLS_tc
   IS
        data INTERV_PROTOCOLS_tc;
   BEGIN
        select * bulk collect into data from INTERV_PROTOCOLS where rowid in (select /*+opt_estimate(table,t,scale_rows=0.0000001))*/ * from table(rows_in) t);
        return data;
        EXCEPTION
      WHEN OTHERS THEN
        pk_alert_exceptions.raise_error (
           error_name_in => 'get_data_rowid'
           );
   END get_data_rowid;


   FUNCTION get_data_rowid_pat(
        rows_in IN TABLE_VARCHAR
        ) RETURN INTERV_PROTOCOLS_tc
   is
        PRAGMA AUTONOMOUS_TRANSACTION;
        data INTERV_PROTOCOLS_tc;
   BEGIN
        data := get_data_rowid(rows_in);
        commit;
        return data;
        EXCEPTION
      WHEN OTHERS THEN
        pk_alert_exceptions.raise_error (
           error_name_in => 'get_data_rowid'
           );
        rollback;
    END get_data_rowid_pat;

BEGIN
   NULL;
END TS_INTERV_PROTOCOLS;
/
