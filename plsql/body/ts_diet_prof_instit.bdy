/*-- Last Change Revision: $Rev: 1144683 $*/
/*-- Last Change by: $Author: rita.lopes $*/
/*-- Date of last change: $Date: 2011-11-10 10:13:28 +0000 (qui, 10 nov 2011) $*/
CREATE OR REPLACE PACKAGE BODY TS_DIET_PROF_INSTIT
/*
| Generated by or retrieved from QCGU - DO NOT MODIFY!
| QCGU - "Get it right, do it fast" - www.ToadWorld.com
| QCGU Universal ID: {657BD35E-2E48-4CD4-B14E-41420B8433B3}
| Created On: November 2, 2011 10:6:51
| Created By: ALERT
*/
IS


   e_null_column_value EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_null_column_value, -1400);
   --
   e_existing_fky_reference EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_existing_fky_reference, -2266);
   --
   e_check_constraint_failure EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_check_constraint_failure, -2290);
   --
   e_no_parent_key EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_no_parent_key, -2291);
   --
   e_child_record_found EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_child_record_found, -2292);
   --
   e_forall_error EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_forall_error, -24381);
   --
   -- Defined for backward compatibilty.
   e_integ_constraint_failure EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_integ_constraint_failure, -2291);

    -- Private utilities
   PROCEDURE get_constraint_info (
      owner_out OUT ALL_CONSTRAINTS.OWNER%TYPE
     ,name_out OUT ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE)
   IS
      l_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
      dotloc PLS_INTEGER;
      leftloc PLS_INTEGER;
   BEGIN
      dotloc  := INSTR (l_errm,'.');
      leftloc := INSTR (l_errm,'(');
      owner_out := SUBSTR (l_errm, leftloc+1, dotloc-leftloc-1);
      name_out  := SUBSTR (l_errm, dotloc+1, INSTR (l_errm,')')-dotloc-1);
   END get_constraint_info;
   -- Public programs

   PROCEDURE ins (
      id_diet_prof_instit_in IN DIET_PROF_INSTIT.ID_DIET_PROF_INSTIT%TYPE
      ,
      desc_diet_in IN DIET_PROF_INSTIT.DESC_DIET%TYPE DEFAULT NULL,
      flg_status_in IN DIET_PROF_INSTIT.FLG_STATUS%TYPE DEFAULT NULL,
      food_plan_in IN DIET_PROF_INSTIT.FOOD_PLAN%TYPE DEFAULT NULL,
      flg_share_in IN DIET_PROF_INSTIT.FLG_SHARE%TYPE DEFAULT NULL,
      id_prof_create_in IN DIET_PROF_INSTIT.ID_PROF_CREATE%TYPE DEFAULT NULL,
      id_institution_in IN DIET_PROF_INSTIT.ID_INSTITUTION%TYPE DEFAULT NULL,
      notes_in IN DIET_PROF_INSTIT.NOTES%TYPE DEFAULT NULL,
      dt_creation_in IN DIET_PROF_INSTIT.DT_CREATION%TYPE DEFAULT NULL,
      id_prof_cancel_in IN DIET_PROF_INSTIT.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      dt_cancel_in IN DIET_PROF_INSTIT.DT_CANCEL%TYPE DEFAULT NULL,
      notes_cancel_in IN DIET_PROF_INSTIT.NOTES_CANCEL%TYPE DEFAULT NULL,
      id_diet_prof_parent_in IN DIET_PROF_INSTIT.ID_DIET_PROF_PARENT%TYPE DEFAULT NULL,
      create_user_in IN DIET_PROF_INSTIT.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN DIET_PROF_INSTIT.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN DIET_PROF_INSTIT.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN DIET_PROF_INSTIT.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN DIET_PROF_INSTIT.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN DIET_PROF_INSTIT.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      id_diet_type_in IN DIET_PROF_INSTIT.ID_DIET_TYPE%TYPE DEFAULT NULL,
      flg_help_in IN DIET_PROF_INSTIT.FLG_HELP%TYPE DEFAULT NULL,
      flg_institution_in IN DIET_PROF_INSTIT.FLG_INSTITUTION%TYPE DEFAULT NULL
     ,handle_error_in IN BOOLEAN := TRUE
      , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN

     INSERT INTO DIET_PROF_INSTIT (
         ID_DIET_PROF_INSTIT,
         DESC_DIET,
         FLG_STATUS,
         FOOD_PLAN,
         FLG_SHARE,
         ID_PROF_CREATE,
         ID_INSTITUTION,
         NOTES,
         DT_CREATION,
         ID_PROF_CANCEL,
         DT_CANCEL,
         NOTES_CANCEL,
         ID_DIET_PROF_PARENT,
         CREATE_USER,
         CREATE_TIME,
         CREATE_INSTITUTION,
         UPDATE_USER,
         UPDATE_TIME,
         UPDATE_INSTITUTION,
         ID_DIET_TYPE,
         FLG_HELP,
         FLG_INSTITUTION
         )
      VALUES (
         id_diet_prof_instit_in,
         desc_diet_in,
         flg_status_in,
         food_plan_in,
         flg_share_in,
         id_prof_create_in,
         id_institution_in,
         notes_in,
         dt_creation_in,
         id_prof_cancel_in,
         dt_cancel_in,
         notes_cancel_in,
         id_diet_prof_parent_in,
         create_user_in,
         create_time_in,
         create_institution_in,
         update_user_in,
         update_time_in,
         update_institution_in,
         id_diet_type_in,
         flg_help_in,
         flg_institution_in
         ) RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF FALSE THEN NULL; -- Placeholder in case no unique indexes
           ELSE
              pk_alert_exceptions.raise_error (
                    error_name_in => 'DUPLICATE-VALUE'
                    ,name1_in => 'OWNER'
                    ,value1_in => l_owner
                    ,name2_in => 'CONSTRAINT_NAME'
                    ,value2_in => l_name
                    ,name3_in => 'TABLE_NAME'
                    ,value3_in => 'DIET_PROF_INSTIT');
           END IF;
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'DIET_PROF_INSTIT');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'DIET_PROF_INSTIT');
           IF l_name = 'DPI_DIET_PARENT_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_DIET_PROF_PARENT'
               , value_in => id_diet_prof_parent_in);
           END IF;
           IF l_name = 'DPI_INST_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_INSTITUTION'
               , value_in => id_institution_in);
           END IF;
           IF l_name = 'DPI_PROFC_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_PROF_CANCEL'
               , value_in => id_prof_cancel_in);
           END IF;
           IF l_name = 'DPI_PROF_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_PROF_CREATE'
               , value_in => id_prof_create_in);
           END IF;
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END ins;

   PROCEDURE ins (
      id_diet_prof_instit_in IN DIET_PROF_INSTIT.ID_DIET_PROF_INSTIT%TYPE
      ,
      desc_diet_in IN DIET_PROF_INSTIT.DESC_DIET%TYPE DEFAULT NULL,
      flg_status_in IN DIET_PROF_INSTIT.FLG_STATUS%TYPE DEFAULT NULL,
      food_plan_in IN DIET_PROF_INSTIT.FOOD_PLAN%TYPE DEFAULT NULL,
      flg_share_in IN DIET_PROF_INSTIT.FLG_SHARE%TYPE DEFAULT NULL,
      id_prof_create_in IN DIET_PROF_INSTIT.ID_PROF_CREATE%TYPE DEFAULT NULL,
      id_institution_in IN DIET_PROF_INSTIT.ID_INSTITUTION%TYPE DEFAULT NULL,
      notes_in IN DIET_PROF_INSTIT.NOTES%TYPE DEFAULT NULL,
      dt_creation_in IN DIET_PROF_INSTIT.DT_CREATION%TYPE DEFAULT NULL,
      id_prof_cancel_in IN DIET_PROF_INSTIT.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      dt_cancel_in IN DIET_PROF_INSTIT.DT_CANCEL%TYPE DEFAULT NULL,
      notes_cancel_in IN DIET_PROF_INSTIT.NOTES_CANCEL%TYPE DEFAULT NULL,
      id_diet_prof_parent_in IN DIET_PROF_INSTIT.ID_DIET_PROF_PARENT%TYPE DEFAULT NULL,
      create_user_in IN DIET_PROF_INSTIT.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN DIET_PROF_INSTIT.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN DIET_PROF_INSTIT.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN DIET_PROF_INSTIT.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN DIET_PROF_INSTIT.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN DIET_PROF_INSTIT.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      id_diet_type_in IN DIET_PROF_INSTIT.ID_DIET_TYPE%TYPE DEFAULT NULL,
      flg_help_in IN DIET_PROF_INSTIT.FLG_HELP%TYPE DEFAULT NULL,
      flg_institution_in IN DIET_PROF_INSTIT.FLG_INSTITUTION%TYPE DEFAULT NULL
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN

     ins (
      id_diet_prof_instit_in => id_diet_prof_instit_in
      ,
      desc_diet_in => desc_diet_in,
      flg_status_in => flg_status_in,
      food_plan_in => food_plan_in,
      flg_share_in => flg_share_in,
      id_prof_create_in => id_prof_create_in,
      id_institution_in => id_institution_in,
      notes_in => notes_in,
      dt_creation_in => dt_creation_in,
      id_prof_cancel_in => id_prof_cancel_in,
      dt_cancel_in => dt_cancel_in,
      notes_cancel_in => notes_cancel_in,
      id_diet_prof_parent_in => id_diet_prof_parent_in,
      create_user_in => create_user_in,
      create_time_in => create_time_in,
      create_institution_in => create_institution_in,
      update_user_in => update_user_in,
      update_time_in => update_time_in,
      update_institution_in => update_institution_in,
      id_diet_type_in => id_diet_type_in,
      flg_help_in => flg_help_in,
      flg_institution_in => flg_institution_in
     ,handle_error_in => handle_error_in
      ,rows_out => rows_out
      );
   END ins;


   PROCEDURE ins (
      rec_in IN DIET_PROF_INSTIT%ROWTYPE
     ,gen_pky_in IN BOOLEAN DEFAULT FALSE
     ,sequence_in IN VARCHAR2 := NULL
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
      l_rec DIET_PROF_INSTIT%ROWTYPE := rec_in;
   BEGIN
      IF gen_pky_in THEN
         l_rec.ID_DIET_PROF_INSTIT := next_key (sequence_in);
      END IF;
      ins (
         id_diet_prof_instit_in => l_rec.ID_DIET_PROF_INSTIT
         ,
         desc_diet_in => l_rec.DESC_DIET,
         flg_status_in => l_rec.FLG_STATUS,
         food_plan_in => l_rec.FOOD_PLAN,
         flg_share_in => l_rec.FLG_SHARE,
         id_prof_create_in => l_rec.ID_PROF_CREATE,
         id_institution_in => l_rec.ID_INSTITUTION,
         notes_in => l_rec.NOTES,
         dt_creation_in => l_rec.DT_CREATION,
         id_prof_cancel_in => l_rec.ID_PROF_CANCEL,
         dt_cancel_in => l_rec.DT_CANCEL,
         notes_cancel_in => l_rec.NOTES_CANCEL,
         id_diet_prof_parent_in => l_rec.ID_DIET_PROF_PARENT,
         create_user_in => l_rec.CREATE_USER,
         create_time_in => l_rec.CREATE_TIME,
         create_institution_in => l_rec.CREATE_INSTITUTION,
         update_user_in => l_rec.UPDATE_USER,
         update_time_in => l_rec.UPDATE_TIME,
         update_institution_in => l_rec.UPDATE_INSTITUTION,
         id_diet_type_in => l_rec.ID_DIET_TYPE,
         flg_help_in => l_rec.FLG_HELP,
         flg_institution_in => l_rec.FLG_INSTITUTION
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
   END ins;

   PROCEDURE ins (
      rec_in IN DIET_PROF_INSTIT%ROWTYPE
     ,gen_pky_in IN BOOLEAN DEFAULT FALSE
     ,sequence_in IN VARCHAR2 := NULL
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
      rows_out TABLE_VARCHAR;
   BEGIN

  ins (
      rec_in => rec_in
     ,gen_pky_in => gen_pky_in
     ,sequence_in => sequence_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
   );

   END ins;

   FUNCTION next_key (sequence_in IN VARCHAR2 := NULL) RETURN DIET_PROF_INSTIT.ID_DIET_PROF_INSTIT%TYPE

   IS
     retval DIET_PROF_INSTIT.ID_DIET_PROF_INSTIT%TYPE;

   BEGIN
      IF sequence_in IS NULL
      THEN
         SELECT seq_DIET_PROF_INSTIT.NEXTVAL INTO retval FROM dual;
      ELSE
         EXECUTE IMMEDIATE
            'SELECT ' || sequence_in || '.NEXTVAL FROM dual'
            INTO retval;
      END IF;
      RETURN retval;
   EXCEPTION
      WHEN OTHERS THEN
        pk_alert_exceptions.raise_error (
           error_name_in => 'SEQUENCE-GENERATION-FAILURE'
           ,name1_in => 'SEQUENCE'
           ,value1_in => NVL (sequence_in, 'seq_DIET_PROF_INSTIT')
           );
   END next_key;

   PROCEDURE ins (
      desc_diet_in IN DIET_PROF_INSTIT.DESC_DIET%TYPE DEFAULT NULL,
      flg_status_in IN DIET_PROF_INSTIT.FLG_STATUS%TYPE DEFAULT NULL,
      food_plan_in IN DIET_PROF_INSTIT.FOOD_PLAN%TYPE DEFAULT NULL,
      flg_share_in IN DIET_PROF_INSTIT.FLG_SHARE%TYPE DEFAULT NULL,
      id_prof_create_in IN DIET_PROF_INSTIT.ID_PROF_CREATE%TYPE DEFAULT NULL,
      id_institution_in IN DIET_PROF_INSTIT.ID_INSTITUTION%TYPE DEFAULT NULL,
      notes_in IN DIET_PROF_INSTIT.NOTES%TYPE DEFAULT NULL,
      dt_creation_in IN DIET_PROF_INSTIT.DT_CREATION%TYPE DEFAULT NULL,
      id_prof_cancel_in IN DIET_PROF_INSTIT.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      dt_cancel_in IN DIET_PROF_INSTIT.DT_CANCEL%TYPE DEFAULT NULL,
      notes_cancel_in IN DIET_PROF_INSTIT.NOTES_CANCEL%TYPE DEFAULT NULL,
      id_diet_prof_parent_in IN DIET_PROF_INSTIT.ID_DIET_PROF_PARENT%TYPE DEFAULT NULL,
      create_user_in IN DIET_PROF_INSTIT.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN DIET_PROF_INSTIT.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN DIET_PROF_INSTIT.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN DIET_PROF_INSTIT.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN DIET_PROF_INSTIT.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN DIET_PROF_INSTIT.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      id_diet_type_in IN DIET_PROF_INSTIT.ID_DIET_TYPE%TYPE DEFAULT NULL,
      flg_help_in IN DIET_PROF_INSTIT.FLG_HELP%TYPE DEFAULT NULL,
      flg_institution_in IN DIET_PROF_INSTIT.FLG_INSTITUTION%TYPE DEFAULT NULL,
      id_diet_prof_instit_out IN OUT DIET_PROF_INSTIT.ID_DIET_PROF_INSTIT%TYPE
      ,handle_error_in IN BOOLEAN := TRUE
      , rows_out OUT TABLE_VARCHAR
   )
   IS
        l_pky DIET_PROF_INSTIT.ID_DIET_PROF_INSTIT%TYPE := next_key;
   BEGIN
      ins (
         id_diet_prof_instit_in => l_pky,
         desc_diet_in => desc_diet_in,
         flg_status_in => flg_status_in,
         food_plan_in => food_plan_in,
         flg_share_in => flg_share_in,
         id_prof_create_in => id_prof_create_in,
         id_institution_in => id_institution_in,
         notes_in => notes_in,
         dt_creation_in => dt_creation_in,
         id_prof_cancel_in => id_prof_cancel_in,
         dt_cancel_in => dt_cancel_in,
         notes_cancel_in => notes_cancel_in,
         id_diet_prof_parent_in => id_diet_prof_parent_in,
         create_user_in => create_user_in,
         create_time_in => create_time_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in,
         id_diet_type_in => id_diet_type_in,
         flg_help_in => flg_help_in,
         flg_institution_in => flg_institution_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
      id_diet_prof_instit_out := l_pky;
   END ins;

   PROCEDURE ins (
      desc_diet_in IN DIET_PROF_INSTIT.DESC_DIET%TYPE DEFAULT NULL,
      flg_status_in IN DIET_PROF_INSTIT.FLG_STATUS%TYPE DEFAULT NULL,
      food_plan_in IN DIET_PROF_INSTIT.FOOD_PLAN%TYPE DEFAULT NULL,
      flg_share_in IN DIET_PROF_INSTIT.FLG_SHARE%TYPE DEFAULT NULL,
      id_prof_create_in IN DIET_PROF_INSTIT.ID_PROF_CREATE%TYPE DEFAULT NULL,
      id_institution_in IN DIET_PROF_INSTIT.ID_INSTITUTION%TYPE DEFAULT NULL,
      notes_in IN DIET_PROF_INSTIT.NOTES%TYPE DEFAULT NULL,
      dt_creation_in IN DIET_PROF_INSTIT.DT_CREATION%TYPE DEFAULT NULL,
      id_prof_cancel_in IN DIET_PROF_INSTIT.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      dt_cancel_in IN DIET_PROF_INSTIT.DT_CANCEL%TYPE DEFAULT NULL,
      notes_cancel_in IN DIET_PROF_INSTIT.NOTES_CANCEL%TYPE DEFAULT NULL,
      id_diet_prof_parent_in IN DIET_PROF_INSTIT.ID_DIET_PROF_PARENT%TYPE DEFAULT NULL,
      create_user_in IN DIET_PROF_INSTIT.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN DIET_PROF_INSTIT.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN DIET_PROF_INSTIT.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN DIET_PROF_INSTIT.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN DIET_PROF_INSTIT.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN DIET_PROF_INSTIT.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      id_diet_type_in IN DIET_PROF_INSTIT.ID_DIET_TYPE%TYPE DEFAULT NULL,
      flg_help_in IN DIET_PROF_INSTIT.FLG_HELP%TYPE DEFAULT NULL,
      flg_institution_in IN DIET_PROF_INSTIT.FLG_INSTITUTION%TYPE DEFAULT NULL,
      id_diet_prof_instit_out IN OUT DIET_PROF_INSTIT.ID_DIET_PROF_INSTIT%TYPE
      ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
        rows_out TABLE_VARCHAR;
   BEGIN
      ins (
      desc_diet_in => desc_diet_in,
      flg_status_in => flg_status_in,
      food_plan_in => food_plan_in,
      flg_share_in => flg_share_in,
      id_prof_create_in => id_prof_create_in,
      id_institution_in => id_institution_in,
      notes_in => notes_in,
      dt_creation_in => dt_creation_in,
      id_prof_cancel_in => id_prof_cancel_in,
      dt_cancel_in => dt_cancel_in,
      notes_cancel_in => notes_cancel_in,
      id_diet_prof_parent_in => id_diet_prof_parent_in,
      create_user_in => create_user_in,
      create_time_in => create_time_in,
      create_institution_in => create_institution_in,
      update_user_in => update_user_in,
      update_time_in => update_time_in,
      update_institution_in => update_institution_in,
      id_diet_type_in => id_diet_type_in,
      flg_help_in => flg_help_in,
      flg_institution_in => flg_institution_in,
      id_diet_prof_instit_out => id_diet_prof_instit_out
      ,handle_error_in => handle_error_in
      , rows_out => rows_out
   );
   END ins;

   FUNCTION ins (
      desc_diet_in IN DIET_PROF_INSTIT.DESC_DIET%TYPE DEFAULT NULL,
      flg_status_in IN DIET_PROF_INSTIT.FLG_STATUS%TYPE DEFAULT NULL,
      food_plan_in IN DIET_PROF_INSTIT.FOOD_PLAN%TYPE DEFAULT NULL,
      flg_share_in IN DIET_PROF_INSTIT.FLG_SHARE%TYPE DEFAULT NULL,
      id_prof_create_in IN DIET_PROF_INSTIT.ID_PROF_CREATE%TYPE DEFAULT NULL,
      id_institution_in IN DIET_PROF_INSTIT.ID_INSTITUTION%TYPE DEFAULT NULL,
      notes_in IN DIET_PROF_INSTIT.NOTES%TYPE DEFAULT NULL,
      dt_creation_in IN DIET_PROF_INSTIT.DT_CREATION%TYPE DEFAULT NULL,
      id_prof_cancel_in IN DIET_PROF_INSTIT.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      dt_cancel_in IN DIET_PROF_INSTIT.DT_CANCEL%TYPE DEFAULT NULL,
      notes_cancel_in IN DIET_PROF_INSTIT.NOTES_CANCEL%TYPE DEFAULT NULL,
      id_diet_prof_parent_in IN DIET_PROF_INSTIT.ID_DIET_PROF_PARENT%TYPE DEFAULT NULL,
      create_user_in IN DIET_PROF_INSTIT.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN DIET_PROF_INSTIT.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN DIET_PROF_INSTIT.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN DIET_PROF_INSTIT.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN DIET_PROF_INSTIT.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN DIET_PROF_INSTIT.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      id_diet_type_in IN DIET_PROF_INSTIT.ID_DIET_TYPE%TYPE DEFAULT NULL,
      flg_help_in IN DIET_PROF_INSTIT.FLG_HELP%TYPE DEFAULT NULL,
      flg_institution_in IN DIET_PROF_INSTIT.FLG_INSTITUTION%TYPE DEFAULT NULL
      ,handle_error_in IN BOOLEAN := TRUE
      , rows_out OUT TABLE_VARCHAR
   )
      RETURN
         DIET_PROF_INSTIT.ID_DIET_PROF_INSTIT%TYPE
   IS
        l_pky DIET_PROF_INSTIT.ID_DIET_PROF_INSTIT%TYPE := next_key;
   BEGIN
      ins (
         id_diet_prof_instit_in => l_pky,
         desc_diet_in => desc_diet_in,
         flg_status_in => flg_status_in,
         food_plan_in => food_plan_in,
         flg_share_in => flg_share_in,
         id_prof_create_in => id_prof_create_in,
         id_institution_in => id_institution_in,
         notes_in => notes_in,
         dt_creation_in => dt_creation_in,
         id_prof_cancel_in => id_prof_cancel_in,
         dt_cancel_in => dt_cancel_in,
         notes_cancel_in => notes_cancel_in,
         id_diet_prof_parent_in => id_diet_prof_parent_in,
         create_user_in => create_user_in,
         create_time_in => create_time_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in,
         id_diet_type_in => id_diet_type_in,
         flg_help_in => flg_help_in,
         flg_institution_in => flg_institution_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
      RETURN l_pky;
   END ins;

   FUNCTION ins (
      desc_diet_in IN DIET_PROF_INSTIT.DESC_DIET%TYPE DEFAULT NULL,
      flg_status_in IN DIET_PROF_INSTIT.FLG_STATUS%TYPE DEFAULT NULL,
      food_plan_in IN DIET_PROF_INSTIT.FOOD_PLAN%TYPE DEFAULT NULL,
      flg_share_in IN DIET_PROF_INSTIT.FLG_SHARE%TYPE DEFAULT NULL,
      id_prof_create_in IN DIET_PROF_INSTIT.ID_PROF_CREATE%TYPE DEFAULT NULL,
      id_institution_in IN DIET_PROF_INSTIT.ID_INSTITUTION%TYPE DEFAULT NULL,
      notes_in IN DIET_PROF_INSTIT.NOTES%TYPE DEFAULT NULL,
      dt_creation_in IN DIET_PROF_INSTIT.DT_CREATION%TYPE DEFAULT NULL,
      id_prof_cancel_in IN DIET_PROF_INSTIT.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      dt_cancel_in IN DIET_PROF_INSTIT.DT_CANCEL%TYPE DEFAULT NULL,
      notes_cancel_in IN DIET_PROF_INSTIT.NOTES_CANCEL%TYPE DEFAULT NULL,
      id_diet_prof_parent_in IN DIET_PROF_INSTIT.ID_DIET_PROF_PARENT%TYPE DEFAULT NULL,
      create_user_in IN DIET_PROF_INSTIT.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN DIET_PROF_INSTIT.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN DIET_PROF_INSTIT.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN DIET_PROF_INSTIT.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN DIET_PROF_INSTIT.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN DIET_PROF_INSTIT.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      id_diet_type_in IN DIET_PROF_INSTIT.ID_DIET_TYPE%TYPE DEFAULT NULL,
      flg_help_in IN DIET_PROF_INSTIT.FLG_HELP%TYPE DEFAULT NULL,
      flg_institution_in IN DIET_PROF_INSTIT.FLG_INSTITUTION%TYPE DEFAULT NULL
      ,handle_error_in IN BOOLEAN := TRUE
   )
      RETURN
         DIET_PROF_INSTIT.ID_DIET_PROF_INSTIT%TYPE
   IS
        l_pky DIET_PROF_INSTIT.ID_DIET_PROF_INSTIT%TYPE := next_key;
    rows_out TABLE_VARCHAR;
   BEGIN
      ins (
         id_diet_prof_instit_in => l_pky,
         desc_diet_in => desc_diet_in,
         flg_status_in => flg_status_in,
         food_plan_in => food_plan_in,
         flg_share_in => flg_share_in,
         id_prof_create_in => id_prof_create_in,
         id_institution_in => id_institution_in,
         notes_in => notes_in,
         dt_creation_in => dt_creation_in,
         id_prof_cancel_in => id_prof_cancel_in,
         dt_cancel_in => dt_cancel_in,
         notes_cancel_in => notes_cancel_in,
         id_diet_prof_parent_in => id_diet_prof_parent_in,
         create_user_in => create_user_in,
         create_time_in => create_time_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in,
         id_diet_type_in => id_diet_type_in,
         flg_help_in => flg_help_in,
         flg_institution_in => flg_institution_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
      RETURN l_pky;
   END ins;

      PROCEDURE ins (
      desc_diet_in IN DIET_PROF_INSTIT.DESC_DIET%TYPE DEFAULT NULL,
      flg_status_in IN DIET_PROF_INSTIT.FLG_STATUS%TYPE DEFAULT NULL,
      food_plan_in IN DIET_PROF_INSTIT.FOOD_PLAN%TYPE DEFAULT NULL,
      flg_share_in IN DIET_PROF_INSTIT.FLG_SHARE%TYPE DEFAULT NULL,
      id_prof_create_in IN DIET_PROF_INSTIT.ID_PROF_CREATE%TYPE DEFAULT NULL,
      id_institution_in IN DIET_PROF_INSTIT.ID_INSTITUTION%TYPE DEFAULT NULL,
      notes_in IN DIET_PROF_INSTIT.NOTES%TYPE DEFAULT NULL,
      dt_creation_in IN DIET_PROF_INSTIT.DT_CREATION%TYPE DEFAULT NULL,
      id_prof_cancel_in IN DIET_PROF_INSTIT.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      dt_cancel_in IN DIET_PROF_INSTIT.DT_CANCEL%TYPE DEFAULT NULL,
      notes_cancel_in IN DIET_PROF_INSTIT.NOTES_CANCEL%TYPE DEFAULT NULL,
      id_diet_prof_parent_in IN DIET_PROF_INSTIT.ID_DIET_PROF_PARENT%TYPE DEFAULT NULL,
      create_user_in IN DIET_PROF_INSTIT.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN DIET_PROF_INSTIT.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN DIET_PROF_INSTIT.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN DIET_PROF_INSTIT.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN DIET_PROF_INSTIT.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN DIET_PROF_INSTIT.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      id_diet_type_in IN DIET_PROF_INSTIT.ID_DIET_TYPE%TYPE DEFAULT NULL,
      flg_help_in IN DIET_PROF_INSTIT.FLG_HELP%TYPE DEFAULT NULL,
      flg_institution_in IN DIET_PROF_INSTIT.FLG_INSTITUTION%TYPE DEFAULT NULL
      ,handle_error_in IN BOOLEAN := TRUE
      , rows_out OUT TABLE_VARCHAR
   )
   IS
        l_pky DIET_PROF_INSTIT.ID_DIET_PROF_INSTIT%TYPE := next_key;
   BEGIN
      ins (
         id_diet_prof_instit_in => l_pky,
         desc_diet_in => desc_diet_in,
         flg_status_in => flg_status_in,
         food_plan_in => food_plan_in,
         flg_share_in => flg_share_in,
         id_prof_create_in => id_prof_create_in,
         id_institution_in => id_institution_in,
         notes_in => notes_in,
         dt_creation_in => dt_creation_in,
         id_prof_cancel_in => id_prof_cancel_in,
         dt_cancel_in => dt_cancel_in,
         notes_cancel_in => notes_cancel_in,
         id_diet_prof_parent_in => id_diet_prof_parent_in,
         create_user_in => create_user_in,
         create_time_in => create_time_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in,
         id_diet_type_in => id_diet_type_in,
         flg_help_in => flg_help_in,
         flg_institution_in => flg_institution_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
   END ins;


     PROCEDURE ins (
      desc_diet_in IN DIET_PROF_INSTIT.DESC_DIET%TYPE DEFAULT NULL,
      flg_status_in IN DIET_PROF_INSTIT.FLG_STATUS%TYPE DEFAULT NULL,
      food_plan_in IN DIET_PROF_INSTIT.FOOD_PLAN%TYPE DEFAULT NULL,
      flg_share_in IN DIET_PROF_INSTIT.FLG_SHARE%TYPE DEFAULT NULL,
      id_prof_create_in IN DIET_PROF_INSTIT.ID_PROF_CREATE%TYPE DEFAULT NULL,
      id_institution_in IN DIET_PROF_INSTIT.ID_INSTITUTION%TYPE DEFAULT NULL,
      notes_in IN DIET_PROF_INSTIT.NOTES%TYPE DEFAULT NULL,
      dt_creation_in IN DIET_PROF_INSTIT.DT_CREATION%TYPE DEFAULT NULL,
      id_prof_cancel_in IN DIET_PROF_INSTIT.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      dt_cancel_in IN DIET_PROF_INSTIT.DT_CANCEL%TYPE DEFAULT NULL,
      notes_cancel_in IN DIET_PROF_INSTIT.NOTES_CANCEL%TYPE DEFAULT NULL,
      id_diet_prof_parent_in IN DIET_PROF_INSTIT.ID_DIET_PROF_PARENT%TYPE DEFAULT NULL,
      create_user_in IN DIET_PROF_INSTIT.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN DIET_PROF_INSTIT.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN DIET_PROF_INSTIT.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN DIET_PROF_INSTIT.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN DIET_PROF_INSTIT.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN DIET_PROF_INSTIT.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      id_diet_type_in IN DIET_PROF_INSTIT.ID_DIET_TYPE%TYPE DEFAULT NULL,
      flg_help_in IN DIET_PROF_INSTIT.FLG_HELP%TYPE DEFAULT NULL,
      flg_institution_in IN DIET_PROF_INSTIT.FLG_INSTITUTION%TYPE DEFAULT NULL
      ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
        l_pky DIET_PROF_INSTIT.ID_DIET_PROF_INSTIT%TYPE := next_key;
      rows_out TABLE_VARCHAR;
   BEGIN
      ins (
         id_diet_prof_instit_in => l_pky,
         desc_diet_in => desc_diet_in,
         flg_status_in => flg_status_in,
         food_plan_in => food_plan_in,
         flg_share_in => flg_share_in,
         id_prof_create_in => id_prof_create_in,
         id_institution_in => id_institution_in,
         notes_in => notes_in,
         dt_creation_in => dt_creation_in,
         id_prof_cancel_in => id_prof_cancel_in,
         dt_cancel_in => dt_cancel_in,
         notes_cancel_in => notes_cancel_in,
         id_diet_prof_parent_in => id_diet_prof_parent_in,
         create_user_in => create_user_in,
         create_time_in => create_time_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in,
         id_diet_type_in => id_diet_type_in,
         flg_help_in => flg_help_in,
         flg_institution_in => flg_institution_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
   END ins;

    PROCEDURE ins (
      rows_in IN DIET_PROF_INSTIT_tc
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
   BEGIN
      IF rows_in.COUNT = 0
      THEN
         NULL;
      ELSE
         FORALL indx IN rows_in.FIRST .. rows_in.LAST
            SAVE EXCEPTIONS
            INSERT INTO DIET_PROF_INSTIT VALUES rows_in (indx) RETURNING ROWID BULK COLLECT INTO rows_out;
      END IF;
   EXCEPTION
     WHEN e_forall_error
     THEN
        -- In Oracle9i and above, SAVE EXCEPTIONS will direct control
        -- here if any error occurs. We can then save all the error
        -- information out to the error instance.
       IF NOT handle_error_in THEN RAISE;
       ELSE
          <<bulk_handler>>
          DECLARE
             l_err_instance_id NUMBER;
          BEGIN
             -- For each error, write to the log.
             FOR indx IN 1 .. SQL%BULK_EXCEPTIONS.COUNT
             LOOP
                pk_alert_exceptions.register_error (
                    error_name_in => 'FORALL-INSERT-FAILURE'
                   ,err_instance_id_out => l_err_instance_id
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'BINDING_ROW_' || indx
                  ,value_in => SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX
                  ,validate_in => FALSE
                );
                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ERROR_AT_ROW_' || indx
                  ,value_in => SQL%BULK_EXCEPTIONS (indx).ERROR_CODE
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_DIET_PROF_INSTIT _' || indx
                  ,value_in => rows_in(indx).ID_DIET_PROF_INSTIT
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'DESC_DIET _' || indx
                  ,value_in => rows_in(indx).DESC_DIET
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'FLG_STATUS _' || indx
                  ,value_in => rows_in(indx).FLG_STATUS
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'FOOD_PLAN _' || indx
                  ,value_in => rows_in(indx).FOOD_PLAN
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'FLG_SHARE _' || indx
                  ,value_in => rows_in(indx).FLG_SHARE
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_PROF_CREATE _' || indx
                  ,value_in => rows_in(indx).ID_PROF_CREATE
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_INSTITUTION _' || indx
                  ,value_in => rows_in(indx).ID_INSTITUTION
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'NOTES _' || indx
                  ,value_in => rows_in(indx).NOTES
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'DT_CREATION _' || indx
                  ,value_in => rows_in(indx).DT_CREATION
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_PROF_CANCEL _' || indx
                  ,value_in => rows_in(indx).ID_PROF_CANCEL
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'DT_CANCEL _' || indx
                  ,value_in => rows_in(indx).DT_CANCEL
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'NOTES_CANCEL _' || indx
                  ,value_in => rows_in(indx).NOTES_CANCEL
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_DIET_PROF_PARENT _' || indx
                  ,value_in => rows_in(indx).ID_DIET_PROF_PARENT
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'CREATE_USER _' || indx
                  ,value_in => rows_in(indx).CREATE_USER
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'CREATE_TIME _' || indx
                  ,value_in => rows_in(indx).CREATE_TIME
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'CREATE_INSTITUTION _' || indx
                  ,value_in => rows_in(indx).CREATE_INSTITUTION
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'UPDATE_USER _' || indx
                  ,value_in => rows_in(indx).UPDATE_USER
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'UPDATE_TIME _' || indx
                  ,value_in => rows_in(indx).UPDATE_TIME
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'UPDATE_INSTITUTION _' || indx
                  ,value_in => rows_in(indx).UPDATE_INSTITUTION
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_DIET_TYPE _' || indx
                  ,value_in => rows_in(indx).ID_DIET_TYPE
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'FLG_HELP _' || indx
                  ,value_in => rows_in(indx).FLG_HELP
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'FLG_INSTITUTION _' || indx
                  ,value_in => rows_in(indx).FLG_INSTITUTION
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.raise_error_instance( err_instance_id_in => l_err_instance_id );
             END LOOP;
          END bulk_handler;
        END IF;
     WHEN OTHERS
     THEN
       IF NOT handle_error_in THEN RAISE;
       ELSE
       pk_alert_exceptions.raise_error(
          error_name_in => 'FORALL-INSERT-FAILURE'
          ,name1_in => 'TABLE_NAME'
          ,value1_in => 'DIET_PROF_INSTIT'
          ,name2_in => 'ROW_COUNT'
          ,value2_in => rows_in.COUNT
           );
       END IF;
   END ins;

    PROCEDURE ins (
      rows_in IN DIET_PROF_INSTIT_tc
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
      rows_out TABLE_VARCHAR;
   BEGIN
      ins (
      rows_in => rows_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
   );
   END ins;


PROCEDURE upd (
      id_diet_prof_instit_in IN DIET_PROF_INSTIT.ID_DIET_PROF_INSTIT%TYPE,
      desc_diet_in IN DIET_PROF_INSTIT.DESC_DIET%TYPE DEFAULT NULL,
      DESC_DIET_nin IN BOOLEAN := TRUE,
      flg_status_in IN DIET_PROF_INSTIT.FLG_STATUS%TYPE DEFAULT NULL,
      FLG_STATUS_nin IN BOOLEAN := TRUE,
      food_plan_in IN DIET_PROF_INSTIT.FOOD_PLAN%TYPE DEFAULT NULL,
      FOOD_PLAN_nin IN BOOLEAN := TRUE,
      flg_share_in IN DIET_PROF_INSTIT.FLG_SHARE%TYPE DEFAULT NULL,
      FLG_SHARE_nin IN BOOLEAN := TRUE,
      id_prof_create_in IN DIET_PROF_INSTIT.ID_PROF_CREATE%TYPE DEFAULT NULL,
      ID_PROF_CREATE_nin IN BOOLEAN := TRUE,
      id_institution_in IN DIET_PROF_INSTIT.ID_INSTITUTION%TYPE DEFAULT NULL,
      ID_INSTITUTION_nin IN BOOLEAN := TRUE,
      notes_in IN DIET_PROF_INSTIT.NOTES%TYPE DEFAULT NULL,
      NOTES_nin IN BOOLEAN := TRUE,
      dt_creation_in IN DIET_PROF_INSTIT.DT_CREATION%TYPE DEFAULT NULL,
      DT_CREATION_nin IN BOOLEAN := TRUE,
      id_prof_cancel_in IN DIET_PROF_INSTIT.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      ID_PROF_CANCEL_nin IN BOOLEAN := TRUE,
      dt_cancel_in IN DIET_PROF_INSTIT.DT_CANCEL%TYPE DEFAULT NULL,
      DT_CANCEL_nin IN BOOLEAN := TRUE,
      notes_cancel_in IN DIET_PROF_INSTIT.NOTES_CANCEL%TYPE DEFAULT NULL,
      NOTES_CANCEL_nin IN BOOLEAN := TRUE,
      id_diet_prof_parent_in IN DIET_PROF_INSTIT.ID_DIET_PROF_PARENT%TYPE DEFAULT NULL,
      ID_DIET_PROF_PARENT_nin IN BOOLEAN := TRUE,
      create_user_in IN DIET_PROF_INSTIT.CREATE_USER%TYPE DEFAULT NULL,
      CREATE_USER_nin IN BOOLEAN := TRUE,
      create_time_in IN DIET_PROF_INSTIT.CREATE_TIME%TYPE DEFAULT NULL,
      CREATE_TIME_nin IN BOOLEAN := TRUE,
      create_institution_in IN DIET_PROF_INSTIT.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      CREATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      update_user_in IN DIET_PROF_INSTIT.UPDATE_USER%TYPE DEFAULT NULL,
      UPDATE_USER_nin IN BOOLEAN := TRUE,
      update_time_in IN DIET_PROF_INSTIT.UPDATE_TIME%TYPE DEFAULT NULL,
      UPDATE_TIME_nin IN BOOLEAN := TRUE,
      update_institution_in IN DIET_PROF_INSTIT.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      UPDATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      id_diet_type_in IN DIET_PROF_INSTIT.ID_DIET_TYPE%TYPE DEFAULT NULL,
      ID_DIET_TYPE_nin IN BOOLEAN := TRUE,
      flg_help_in IN DIET_PROF_INSTIT.FLG_HELP%TYPE DEFAULT NULL,
      FLG_HELP_nin IN BOOLEAN := TRUE,
      flg_institution_in IN DIET_PROF_INSTIT.FLG_INSTITUTION%TYPE DEFAULT NULL,
      FLG_INSTITUTION_nin IN BOOLEAN := TRUE,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      )
   IS
   l_rows_out TABLE_VARCHAR;
   l_DESC_DIET_n NUMBER(1);
   l_FLG_STATUS_n NUMBER(1);
   l_FOOD_PLAN_n NUMBER(1);
   l_FLG_SHARE_n NUMBER(1);
   l_ID_PROF_CREATE_n NUMBER(1);
   l_ID_INSTITUTION_n NUMBER(1);
   l_NOTES_n NUMBER(1);
   l_DT_CREATION_n NUMBER(1);
   l_ID_PROF_CANCEL_n NUMBER(1);
   l_DT_CANCEL_n NUMBER(1);
   l_NOTES_CANCEL_n NUMBER(1);
   l_ID_DIET_PROF_PARENT_n NUMBER(1);
   l_CREATE_USER_n NUMBER(1);
   l_CREATE_TIME_n NUMBER(1);
   l_CREATE_INSTITUTION_n NUMBER(1);
   l_UPDATE_USER_n NUMBER(1);
   l_UPDATE_TIME_n NUMBER(1);
   l_UPDATE_INSTITUTION_n NUMBER(1);
   l_ID_DIET_TYPE_n NUMBER(1);
   l_FLG_HELP_n NUMBER(1);
   l_FLG_INSTITUTION_n NUMBER(1);
   BEGIN

   l_DESC_DIET_n := sys.diutil.bool_to_int(DESC_DIET_nin);
   l_FLG_STATUS_n := sys.diutil.bool_to_int(FLG_STATUS_nin);
   l_FOOD_PLAN_n := sys.diutil.bool_to_int(FOOD_PLAN_nin);
   l_FLG_SHARE_n := sys.diutil.bool_to_int(FLG_SHARE_nin);
   l_ID_PROF_CREATE_n := sys.diutil.bool_to_int(ID_PROF_CREATE_nin);
   l_ID_INSTITUTION_n := sys.diutil.bool_to_int(ID_INSTITUTION_nin);
   l_NOTES_n := sys.diutil.bool_to_int(NOTES_nin);
   l_DT_CREATION_n := sys.diutil.bool_to_int(DT_CREATION_nin);
   l_ID_PROF_CANCEL_n := sys.diutil.bool_to_int(ID_PROF_CANCEL_nin);
   l_DT_CANCEL_n := sys.diutil.bool_to_int(DT_CANCEL_nin);
   l_NOTES_CANCEL_n := sys.diutil.bool_to_int(NOTES_CANCEL_nin);
   l_ID_DIET_PROF_PARENT_n := sys.diutil.bool_to_int(ID_DIET_PROF_PARENT_nin);
   l_CREATE_USER_n := sys.diutil.bool_to_int(CREATE_USER_nin);
   l_CREATE_TIME_n := sys.diutil.bool_to_int(CREATE_TIME_nin);
   l_CREATE_INSTITUTION_n := sys.diutil.bool_to_int(CREATE_INSTITUTION_nin);
   l_UPDATE_USER_n := sys.diutil.bool_to_int(UPDATE_USER_nin);
   l_UPDATE_TIME_n := sys.diutil.bool_to_int(UPDATE_TIME_nin);
   l_UPDATE_INSTITUTION_n := sys.diutil.bool_to_int(UPDATE_INSTITUTION_nin);
   l_ID_DIET_TYPE_n := sys.diutil.bool_to_int(ID_DIET_TYPE_nin);
   l_FLG_HELP_n := sys.diutil.bool_to_int(FLG_HELP_nin);
   l_FLG_INSTITUTION_n := sys.diutil.bool_to_int(FLG_INSTITUTION_nin);


         UPDATE DIET_PROF_INSTIT SET
     DESC_DIET = decode (l_DESC_DIET_n,0,desc_diet_in, NVL (desc_diet_in, DESC_DIET)),
     FLG_STATUS = decode (l_FLG_STATUS_n,0,flg_status_in, NVL (flg_status_in, FLG_STATUS)),
     FOOD_PLAN = decode (l_FOOD_PLAN_n,0,food_plan_in, NVL (food_plan_in, FOOD_PLAN)),
     FLG_SHARE = decode (l_FLG_SHARE_n,0,flg_share_in, NVL (flg_share_in, FLG_SHARE)),
     ID_PROF_CREATE = decode (l_ID_PROF_CREATE_n,0,id_prof_create_in, NVL (id_prof_create_in, ID_PROF_CREATE)),
     ID_INSTITUTION = decode (l_ID_INSTITUTION_n,0,id_institution_in, NVL (id_institution_in, ID_INSTITUTION)),
     NOTES = decode (l_NOTES_n,0,notes_in, NVL (notes_in, NOTES)),
     DT_CREATION = decode (l_DT_CREATION_n,0,dt_creation_in, NVL (dt_creation_in, DT_CREATION)),
     ID_PROF_CANCEL = decode (l_ID_PROF_CANCEL_n,0,id_prof_cancel_in, NVL (id_prof_cancel_in, ID_PROF_CANCEL)),
     DT_CANCEL = decode (l_DT_CANCEL_n,0,dt_cancel_in, NVL (dt_cancel_in, DT_CANCEL)),
     NOTES_CANCEL = decode (l_NOTES_CANCEL_n,0,notes_cancel_in, NVL (notes_cancel_in, NOTES_CANCEL)),
     ID_DIET_PROF_PARENT = decode (l_ID_DIET_PROF_PARENT_n,0,id_diet_prof_parent_in, NVL (id_diet_prof_parent_in, ID_DIET_PROF_PARENT)),
     CREATE_USER = decode (l_CREATE_USER_n,0,create_user_in, NVL (create_user_in, CREATE_USER)),
     CREATE_TIME = decode (l_CREATE_TIME_n,0,create_time_in, NVL (create_time_in, CREATE_TIME)),
     CREATE_INSTITUTION = decode (l_CREATE_INSTITUTION_n,0,create_institution_in, NVL (create_institution_in, CREATE_INSTITUTION)),
     UPDATE_USER = decode (l_UPDATE_USER_n,0,update_user_in, NVL (update_user_in, UPDATE_USER)),
     UPDATE_TIME = decode (l_UPDATE_TIME_n,0,update_time_in, NVL (update_time_in, UPDATE_TIME)),
     UPDATE_INSTITUTION = decode (l_UPDATE_INSTITUTION_n,0,update_institution_in, NVL (update_institution_in, UPDATE_INSTITUTION)),
     ID_DIET_TYPE = decode (l_ID_DIET_TYPE_n,0,id_diet_type_in, NVL (id_diet_type_in, ID_DIET_TYPE)),
     FLG_HELP = decode (l_FLG_HELP_n,0,flg_help_in, NVL (flg_help_in, FLG_HELP)),
     FLG_INSTITUTION = decode (l_FLG_INSTITUTION_n,0,flg_institution_in, NVL (flg_institution_in, FLG_INSTITUTION))
          WHERE
             ID_DIET_PROF_INSTIT = id_diet_prof_instit_in
         RETURNING ROWID BULK COLLECT INTO l_rows_out;


if(rows_out is null)
then
rows_out := table_varchar();
end if;

rows_out :=  rows_out MULTISET UNION DISTINCT l_rows_out;

   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF FALSE THEN NULL; -- Placeholder in case no unique indexes
           ELSE
              pk_alert_exceptions.raise_error (
                    error_name_in => 'DUPLICATE-VALUE'
                    ,name1_in => 'OWNER'
                    ,value1_in => l_owner
                    ,name2_in => 'CONSTRAINT_NAME'
                    ,value2_in => l_name
                    ,name3_in => 'TABLE_NAME'
                    ,value3_in => 'DIET_PROF_INSTIT');
           END IF;
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'DIET_PROF_INSTIT');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'DIET_PROF_INSTIT');
           IF l_name = 'DPI_DIET_PARENT_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_DIET_PROF_PARENT'
               , value_in => id_diet_prof_parent_in);
           END IF;
           IF l_name = 'DPI_INST_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_INSTITUTION'
               , value_in => id_institution_in);
           END IF;
           IF l_name = 'DPI_PROFC_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_PROF_CANCEL'
               , value_in => id_prof_cancel_in);
           END IF;
           IF l_name = 'DPI_PROF_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_PROF_CREATE'
               , value_in => id_prof_create_in);
           END IF;
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END upd;


   PROCEDURE upd (
      id_diet_prof_instit_in IN DIET_PROF_INSTIT.ID_DIET_PROF_INSTIT%TYPE,
      desc_diet_in IN DIET_PROF_INSTIT.DESC_DIET%TYPE DEFAULT NULL,
      DESC_DIET_nin IN BOOLEAN := TRUE,
      flg_status_in IN DIET_PROF_INSTIT.FLG_STATUS%TYPE DEFAULT NULL,
      FLG_STATUS_nin IN BOOLEAN := TRUE,
      food_plan_in IN DIET_PROF_INSTIT.FOOD_PLAN%TYPE DEFAULT NULL,
      FOOD_PLAN_nin IN BOOLEAN := TRUE,
      flg_share_in IN DIET_PROF_INSTIT.FLG_SHARE%TYPE DEFAULT NULL,
      FLG_SHARE_nin IN BOOLEAN := TRUE,
      id_prof_create_in IN DIET_PROF_INSTIT.ID_PROF_CREATE%TYPE DEFAULT NULL,
      ID_PROF_CREATE_nin IN BOOLEAN := TRUE,
      id_institution_in IN DIET_PROF_INSTIT.ID_INSTITUTION%TYPE DEFAULT NULL,
      ID_INSTITUTION_nin IN BOOLEAN := TRUE,
      notes_in IN DIET_PROF_INSTIT.NOTES%TYPE DEFAULT NULL,
      NOTES_nin IN BOOLEAN := TRUE,
      dt_creation_in IN DIET_PROF_INSTIT.DT_CREATION%TYPE DEFAULT NULL,
      DT_CREATION_nin IN BOOLEAN := TRUE,
      id_prof_cancel_in IN DIET_PROF_INSTIT.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      ID_PROF_CANCEL_nin IN BOOLEAN := TRUE,
      dt_cancel_in IN DIET_PROF_INSTIT.DT_CANCEL%TYPE DEFAULT NULL,
      DT_CANCEL_nin IN BOOLEAN := TRUE,
      notes_cancel_in IN DIET_PROF_INSTIT.NOTES_CANCEL%TYPE DEFAULT NULL,
      NOTES_CANCEL_nin IN BOOLEAN := TRUE,
      id_diet_prof_parent_in IN DIET_PROF_INSTIT.ID_DIET_PROF_PARENT%TYPE DEFAULT NULL,
      ID_DIET_PROF_PARENT_nin IN BOOLEAN := TRUE,
      create_user_in IN DIET_PROF_INSTIT.CREATE_USER%TYPE DEFAULT NULL,
      CREATE_USER_nin IN BOOLEAN := TRUE,
      create_time_in IN DIET_PROF_INSTIT.CREATE_TIME%TYPE DEFAULT NULL,
      CREATE_TIME_nin IN BOOLEAN := TRUE,
      create_institution_in IN DIET_PROF_INSTIT.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      CREATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      update_user_in IN DIET_PROF_INSTIT.UPDATE_USER%TYPE DEFAULT NULL,
      UPDATE_USER_nin IN BOOLEAN := TRUE,
      update_time_in IN DIET_PROF_INSTIT.UPDATE_TIME%TYPE DEFAULT NULL,
      UPDATE_TIME_nin IN BOOLEAN := TRUE,
      update_institution_in IN DIET_PROF_INSTIT.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      UPDATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      id_diet_type_in IN DIET_PROF_INSTIT.ID_DIET_TYPE%TYPE DEFAULT NULL,
      ID_DIET_TYPE_nin IN BOOLEAN := TRUE,
      flg_help_in IN DIET_PROF_INSTIT.FLG_HELP%TYPE DEFAULT NULL,
      FLG_HELP_nin IN BOOLEAN := TRUE,
      flg_institution_in IN DIET_PROF_INSTIT.FLG_INSTITUTION%TYPE DEFAULT NULL,
      FLG_INSTITUTION_nin IN BOOLEAN := TRUE,
     handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
     upd (
      id_diet_prof_instit_in => id_diet_prof_instit_in,
      desc_diet_in => desc_diet_in,
      DESC_DIET_nin => DESC_DIET_nin,
      flg_status_in => flg_status_in,
      FLG_STATUS_nin => FLG_STATUS_nin,
      food_plan_in => food_plan_in,
      FOOD_PLAN_nin => FOOD_PLAN_nin,
      flg_share_in => flg_share_in,
      FLG_SHARE_nin => FLG_SHARE_nin,
      id_prof_create_in => id_prof_create_in,
      ID_PROF_CREATE_nin => ID_PROF_CREATE_nin,
      id_institution_in => id_institution_in,
      ID_INSTITUTION_nin => ID_INSTITUTION_nin,
      notes_in => notes_in,
      NOTES_nin => NOTES_nin,
      dt_creation_in => dt_creation_in,
      DT_CREATION_nin => DT_CREATION_nin,
      id_prof_cancel_in => id_prof_cancel_in,
      ID_PROF_CANCEL_nin => ID_PROF_CANCEL_nin,
      dt_cancel_in => dt_cancel_in,
      DT_CANCEL_nin => DT_CANCEL_nin,
      notes_cancel_in => notes_cancel_in,
      NOTES_CANCEL_nin => NOTES_CANCEL_nin,
      id_diet_prof_parent_in => id_diet_prof_parent_in,
      ID_DIET_PROF_PARENT_nin => ID_DIET_PROF_PARENT_nin,
      create_user_in => create_user_in,
      CREATE_USER_nin => CREATE_USER_nin,
      create_time_in => create_time_in,
      CREATE_TIME_nin => CREATE_TIME_nin,
      create_institution_in => create_institution_in,
      CREATE_INSTITUTION_nin => CREATE_INSTITUTION_nin,
      update_user_in => update_user_in,
      UPDATE_USER_nin => UPDATE_USER_nin,
      update_time_in => update_time_in,
      UPDATE_TIME_nin => UPDATE_TIME_nin,
      update_institution_in => update_institution_in,
      UPDATE_INSTITUTION_nin => UPDATE_INSTITUTION_nin,
      id_diet_type_in => id_diet_type_in,
      ID_DIET_TYPE_nin => ID_DIET_TYPE_nin,
      flg_help_in => flg_help_in,
      FLG_HELP_nin => FLG_HELP_nin,
      flg_institution_in => flg_institution_in,
      FLG_INSTITUTION_nin => FLG_INSTITUTION_nin,
     handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END upd;

PROCEDURE upd (
      desc_diet_in IN DIET_PROF_INSTIT.DESC_DIET%TYPE DEFAULT NULL,
      DESC_DIET_nin IN BOOLEAN := TRUE,
      flg_status_in IN DIET_PROF_INSTIT.FLG_STATUS%TYPE DEFAULT NULL,
      FLG_STATUS_nin IN BOOLEAN := TRUE,
      food_plan_in IN DIET_PROF_INSTIT.FOOD_PLAN%TYPE DEFAULT NULL,
      FOOD_PLAN_nin IN BOOLEAN := TRUE,
      flg_share_in IN DIET_PROF_INSTIT.FLG_SHARE%TYPE DEFAULT NULL,
      FLG_SHARE_nin IN BOOLEAN := TRUE,
      id_prof_create_in IN DIET_PROF_INSTIT.ID_PROF_CREATE%TYPE DEFAULT NULL,
      ID_PROF_CREATE_nin IN BOOLEAN := TRUE,
      id_institution_in IN DIET_PROF_INSTIT.ID_INSTITUTION%TYPE DEFAULT NULL,
      ID_INSTITUTION_nin IN BOOLEAN := TRUE,
      notes_in IN DIET_PROF_INSTIT.NOTES%TYPE DEFAULT NULL,
      NOTES_nin IN BOOLEAN := TRUE,
      dt_creation_in IN DIET_PROF_INSTIT.DT_CREATION%TYPE DEFAULT NULL,
      DT_CREATION_nin IN BOOLEAN := TRUE,
      id_prof_cancel_in IN DIET_PROF_INSTIT.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      ID_PROF_CANCEL_nin IN BOOLEAN := TRUE,
      dt_cancel_in IN DIET_PROF_INSTIT.DT_CANCEL%TYPE DEFAULT NULL,
      DT_CANCEL_nin IN BOOLEAN := TRUE,
      notes_cancel_in IN DIET_PROF_INSTIT.NOTES_CANCEL%TYPE DEFAULT NULL,
      NOTES_CANCEL_nin IN BOOLEAN := TRUE,
      id_diet_prof_parent_in IN DIET_PROF_INSTIT.ID_DIET_PROF_PARENT%TYPE DEFAULT NULL,
      ID_DIET_PROF_PARENT_nin IN BOOLEAN := TRUE,
      create_user_in IN DIET_PROF_INSTIT.CREATE_USER%TYPE DEFAULT NULL,
      CREATE_USER_nin IN BOOLEAN := TRUE,
      create_time_in IN DIET_PROF_INSTIT.CREATE_TIME%TYPE DEFAULT NULL,
      CREATE_TIME_nin IN BOOLEAN := TRUE,
      create_institution_in IN DIET_PROF_INSTIT.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      CREATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      update_user_in IN DIET_PROF_INSTIT.UPDATE_USER%TYPE DEFAULT NULL,
      UPDATE_USER_nin IN BOOLEAN := TRUE,
      update_time_in IN DIET_PROF_INSTIT.UPDATE_TIME%TYPE DEFAULT NULL,
      UPDATE_TIME_nin IN BOOLEAN := TRUE,
      update_institution_in IN DIET_PROF_INSTIT.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      UPDATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      id_diet_type_in IN DIET_PROF_INSTIT.ID_DIET_TYPE%TYPE DEFAULT NULL,
      ID_DIET_TYPE_nin IN BOOLEAN := TRUE,
      flg_help_in IN DIET_PROF_INSTIT.FLG_HELP%TYPE DEFAULT NULL,
      FLG_HELP_nin IN BOOLEAN := TRUE,
      flg_institution_in IN DIET_PROF_INSTIT.FLG_INSTITUTION%TYPE DEFAULT NULL,
      FLG_INSTITUTION_nin IN BOOLEAN := TRUE,
    where_in varchar2,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      )
   IS
   l_sql VARCHAR2(32767);
   l_rows_out TABLE_VARCHAR;
   l_DESC_DIET_n NUMBER(1);
   l_FLG_STATUS_n NUMBER(1);
   l_FOOD_PLAN_n NUMBER(1);
   l_FLG_SHARE_n NUMBER(1);
   l_ID_PROF_CREATE_n NUMBER(1);
   l_ID_INSTITUTION_n NUMBER(1);
   l_NOTES_n NUMBER(1);
   l_DT_CREATION_n NUMBER(1);
   l_ID_PROF_CANCEL_n NUMBER(1);
   l_DT_CANCEL_n NUMBER(1);
   l_NOTES_CANCEL_n NUMBER(1);
   l_ID_DIET_PROF_PARENT_n NUMBER(1);
   l_CREATE_USER_n NUMBER(1);
   l_CREATE_TIME_n NUMBER(1);
   l_CREATE_INSTITUTION_n NUMBER(1);
   l_UPDATE_USER_n NUMBER(1);
   l_UPDATE_TIME_n NUMBER(1);
   l_UPDATE_INSTITUTION_n NUMBER(1);
   l_ID_DIET_TYPE_n NUMBER(1);
   l_FLG_HELP_n NUMBER(1);
   l_FLG_INSTITUTION_n NUMBER(1);
      id_diet_prof_instit_in DIET_PROF_INSTIT.ID_DIET_PROF_INSTIT%TYPE;
   BEGIN



      l_DESC_DIET_n := sys.diutil.bool_to_int(DESC_DIET_nin);
      l_FLG_STATUS_n := sys.diutil.bool_to_int(FLG_STATUS_nin);
      l_FOOD_PLAN_n := sys.diutil.bool_to_int(FOOD_PLAN_nin);
      l_FLG_SHARE_n := sys.diutil.bool_to_int(FLG_SHARE_nin);
      l_ID_PROF_CREATE_n := sys.diutil.bool_to_int(ID_PROF_CREATE_nin);
      l_ID_INSTITUTION_n := sys.diutil.bool_to_int(ID_INSTITUTION_nin);
      l_NOTES_n := sys.diutil.bool_to_int(NOTES_nin);
      l_DT_CREATION_n := sys.diutil.bool_to_int(DT_CREATION_nin);
      l_ID_PROF_CANCEL_n := sys.diutil.bool_to_int(ID_PROF_CANCEL_nin);
      l_DT_CANCEL_n := sys.diutil.bool_to_int(DT_CANCEL_nin);
      l_NOTES_CANCEL_n := sys.diutil.bool_to_int(NOTES_CANCEL_nin);
      l_ID_DIET_PROF_PARENT_n := sys.diutil.bool_to_int(ID_DIET_PROF_PARENT_nin);
      l_CREATE_USER_n := sys.diutil.bool_to_int(CREATE_USER_nin);
      l_CREATE_TIME_n := sys.diutil.bool_to_int(CREATE_TIME_nin);
      l_CREATE_INSTITUTION_n := sys.diutil.bool_to_int(CREATE_INSTITUTION_nin);
      l_UPDATE_USER_n := sys.diutil.bool_to_int(UPDATE_USER_nin);
      l_UPDATE_TIME_n := sys.diutil.bool_to_int(UPDATE_TIME_nin);
      l_UPDATE_INSTITUTION_n := sys.diutil.bool_to_int(UPDATE_INSTITUTION_nin);
      l_ID_DIET_TYPE_n := sys.diutil.bool_to_int(ID_DIET_TYPE_nin);
      l_FLG_HELP_n := sys.diutil.bool_to_int(FLG_HELP_nin);
      l_FLG_INSTITUTION_n := sys.diutil.bool_to_int(FLG_INSTITUTION_nin);



l_sql := 'UPDATE DIET_PROF_INSTIT SET '
     || ' DESC_DIET = decode (' || l_DESC_DIET_n || ',0,:desc_diet_in, NVL (:desc_diet_in, DESC_DIET)) '|| ','
     || ' FLG_STATUS = decode (' || l_FLG_STATUS_n || ',0,:flg_status_in, NVL (:flg_status_in, FLG_STATUS)) '|| ','
     || ' FOOD_PLAN = decode (' || l_FOOD_PLAN_n || ',0,:food_plan_in, NVL (:food_plan_in, FOOD_PLAN)) '|| ','
     || ' FLG_SHARE = decode (' || l_FLG_SHARE_n || ',0,:flg_share_in, NVL (:flg_share_in, FLG_SHARE)) '|| ','
     || ' ID_PROF_CREATE = decode (' || l_ID_PROF_CREATE_n || ',0,:id_prof_create_in, NVL (:id_prof_create_in, ID_PROF_CREATE)) '|| ','
     || ' ID_INSTITUTION = decode (' || l_ID_INSTITUTION_n || ',0,:id_institution_in, NVL (:id_institution_in, ID_INSTITUTION)) '|| ','
     || ' NOTES = decode (' || l_NOTES_n || ',0,:notes_in, NVL (:notes_in, NOTES)) '|| ','
     || ' DT_CREATION = decode (' || l_DT_CREATION_n || ',0,:dt_creation_in, NVL (:dt_creation_in, DT_CREATION)) '|| ','
     || ' ID_PROF_CANCEL = decode (' || l_ID_PROF_CANCEL_n || ',0,:id_prof_cancel_in, NVL (:id_prof_cancel_in, ID_PROF_CANCEL)) '|| ','
     || ' DT_CANCEL = decode (' || l_DT_CANCEL_n || ',0,:dt_cancel_in, NVL (:dt_cancel_in, DT_CANCEL)) '|| ','
     || ' NOTES_CANCEL = decode (' || l_NOTES_CANCEL_n || ',0,:notes_cancel_in, NVL (:notes_cancel_in, NOTES_CANCEL)) '|| ','
     || ' ID_DIET_PROF_PARENT = decode (' || l_ID_DIET_PROF_PARENT_n || ',0,:id_diet_prof_parent_in, NVL (:id_diet_prof_parent_in, ID_DIET_PROF_PARENT)) '|| ','
     || ' CREATE_USER = decode (' || l_CREATE_USER_n || ',0,:create_user_in, NVL (:create_user_in, CREATE_USER)) '|| ','
     || ' CREATE_TIME = decode (' || l_CREATE_TIME_n || ',0,:create_time_in, NVL (:create_time_in, CREATE_TIME)) '|| ','
     || ' CREATE_INSTITUTION = decode (' || l_CREATE_INSTITUTION_n || ',0,:create_institution_in, NVL (:create_institution_in, CREATE_INSTITUTION)) '|| ','
     || ' UPDATE_USER = decode (' || l_UPDATE_USER_n || ',0,:update_user_in, NVL (:update_user_in, UPDATE_USER)) '|| ','
     || ' UPDATE_TIME = decode (' || l_UPDATE_TIME_n || ',0,:update_time_in, NVL (:update_time_in, UPDATE_TIME)) '|| ','
     || ' UPDATE_INSTITUTION = decode (' || l_UPDATE_INSTITUTION_n || ',0,:update_institution_in, NVL (:update_institution_in, UPDATE_INSTITUTION)) '|| ','
     || ' ID_DIET_TYPE = decode (' || l_ID_DIET_TYPE_n || ',0,:id_diet_type_in, NVL (:id_diet_type_in, ID_DIET_TYPE)) '|| ','
     || ' FLG_HELP = decode (' || l_FLG_HELP_n || ',0,:flg_help_in, NVL (:flg_help_in, FLG_HELP)) '|| ','
     || ' FLG_INSTITUTION = decode (' || l_FLG_INSTITUTION_n || ',0,:flg_institution_in, NVL (:flg_institution_in, FLG_INSTITUTION)) '
      || ' where ' || nvl(where_in,'(1=1)')
      || ' RETURNING ROWID BULK COLLECT INTO :l_rows_out';




execute immediate 'BEGIN ' || l_sql || '; END;' using in
     desc_diet_in,
     flg_status_in,
     food_plan_in,
     flg_share_in,
     id_prof_create_in,
     id_institution_in,
     notes_in,
     dt_creation_in,
     id_prof_cancel_in,
     dt_cancel_in,
     notes_cancel_in,
     id_diet_prof_parent_in,
     create_user_in,
     create_time_in,
     create_institution_in,
     update_user_in,
     update_time_in,
     update_institution_in,
     id_diet_type_in,
     flg_help_in,
     flg_institution_in,
    OUT l_rows_out;

if(rows_out is null)
then
rows_out := table_varchar();
end if;

rows_out :=  rows_out MULTISET UNION DISTINCT l_rows_out;

   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF FALSE THEN NULL; -- Placeholder in case no unique indexes
           ELSE
              pk_alert_exceptions.raise_error (
                    error_name_in => 'DUPLICATE-VALUE'
                    ,name1_in => 'OWNER'
                    ,value1_in => l_owner
                    ,name2_in => 'CONSTRAINT_NAME'
                    ,value2_in => l_name
                    ,name3_in => 'TABLE_NAME'
                    ,value3_in => 'DIET_PROF_INSTIT');
           END IF;
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'DIET_PROF_INSTIT');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'DIET_PROF_INSTIT');
           IF l_name = 'DPI_DIET_PARENT_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_DIET_PROF_PARENT'
               , value_in => id_diet_prof_parent_in);
           END IF;
           IF l_name = 'DPI_INST_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_INSTITUTION'
               , value_in => id_institution_in);
           END IF;
           IF l_name = 'DPI_PROFC_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_PROF_CANCEL'
               , value_in => id_prof_cancel_in);
           END IF;
           IF l_name = 'DPI_PROF_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_PROF_CREATE'
               , value_in => id_prof_create_in);
           END IF;
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END upd;





PROCEDURE upd (
      desc_diet_in IN DIET_PROF_INSTIT.DESC_DIET%TYPE DEFAULT NULL,
      DESC_DIET_nin IN BOOLEAN := TRUE,
      flg_status_in IN DIET_PROF_INSTIT.FLG_STATUS%TYPE DEFAULT NULL,
      FLG_STATUS_nin IN BOOLEAN := TRUE,
      food_plan_in IN DIET_PROF_INSTIT.FOOD_PLAN%TYPE DEFAULT NULL,
      FOOD_PLAN_nin IN BOOLEAN := TRUE,
      flg_share_in IN DIET_PROF_INSTIT.FLG_SHARE%TYPE DEFAULT NULL,
      FLG_SHARE_nin IN BOOLEAN := TRUE,
      id_prof_create_in IN DIET_PROF_INSTIT.ID_PROF_CREATE%TYPE DEFAULT NULL,
      ID_PROF_CREATE_nin IN BOOLEAN := TRUE,
      id_institution_in IN DIET_PROF_INSTIT.ID_INSTITUTION%TYPE DEFAULT NULL,
      ID_INSTITUTION_nin IN BOOLEAN := TRUE,
      notes_in IN DIET_PROF_INSTIT.NOTES%TYPE DEFAULT NULL,
      NOTES_nin IN BOOLEAN := TRUE,
      dt_creation_in IN DIET_PROF_INSTIT.DT_CREATION%TYPE DEFAULT NULL,
      DT_CREATION_nin IN BOOLEAN := TRUE,
      id_prof_cancel_in IN DIET_PROF_INSTIT.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      ID_PROF_CANCEL_nin IN BOOLEAN := TRUE,
      dt_cancel_in IN DIET_PROF_INSTIT.DT_CANCEL%TYPE DEFAULT NULL,
      DT_CANCEL_nin IN BOOLEAN := TRUE,
      notes_cancel_in IN DIET_PROF_INSTIT.NOTES_CANCEL%TYPE DEFAULT NULL,
      NOTES_CANCEL_nin IN BOOLEAN := TRUE,
      id_diet_prof_parent_in IN DIET_PROF_INSTIT.ID_DIET_PROF_PARENT%TYPE DEFAULT NULL,
      ID_DIET_PROF_PARENT_nin IN BOOLEAN := TRUE,
      create_user_in IN DIET_PROF_INSTIT.CREATE_USER%TYPE DEFAULT NULL,
      CREATE_USER_nin IN BOOLEAN := TRUE,
      create_time_in IN DIET_PROF_INSTIT.CREATE_TIME%TYPE DEFAULT NULL,
      CREATE_TIME_nin IN BOOLEAN := TRUE,
      create_institution_in IN DIET_PROF_INSTIT.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      CREATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      update_user_in IN DIET_PROF_INSTIT.UPDATE_USER%TYPE DEFAULT NULL,
      UPDATE_USER_nin IN BOOLEAN := TRUE,
      update_time_in IN DIET_PROF_INSTIT.UPDATE_TIME%TYPE DEFAULT NULL,
      UPDATE_TIME_nin IN BOOLEAN := TRUE,
      update_institution_in IN DIET_PROF_INSTIT.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      UPDATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      id_diet_type_in IN DIET_PROF_INSTIT.ID_DIET_TYPE%TYPE DEFAULT NULL,
      ID_DIET_TYPE_nin IN BOOLEAN := TRUE,
      flg_help_in IN DIET_PROF_INSTIT.FLG_HELP%TYPE DEFAULT NULL,
      FLG_HELP_nin IN BOOLEAN := TRUE,
      flg_institution_in IN DIET_PROF_INSTIT.FLG_INSTITUTION%TYPE DEFAULT NULL,
      FLG_INSTITUTION_nin IN BOOLEAN := TRUE,
    where_in varchar2,
     handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
      upd (
      desc_diet_in => desc_diet_in,
      DESC_DIET_nin => DESC_DIET_nin,
      flg_status_in => flg_status_in,
      FLG_STATUS_nin => FLG_STATUS_nin,
      food_plan_in => food_plan_in,
      FOOD_PLAN_nin => FOOD_PLAN_nin,
      flg_share_in => flg_share_in,
      FLG_SHARE_nin => FLG_SHARE_nin,
      id_prof_create_in => id_prof_create_in,
      ID_PROF_CREATE_nin => ID_PROF_CREATE_nin,
      id_institution_in => id_institution_in,
      ID_INSTITUTION_nin => ID_INSTITUTION_nin,
      notes_in => notes_in,
      NOTES_nin => NOTES_nin,
      dt_creation_in => dt_creation_in,
      DT_CREATION_nin => DT_CREATION_nin,
      id_prof_cancel_in => id_prof_cancel_in,
      ID_PROF_CANCEL_nin => ID_PROF_CANCEL_nin,
      dt_cancel_in => dt_cancel_in,
      DT_CANCEL_nin => DT_CANCEL_nin,
      notes_cancel_in => notes_cancel_in,
      NOTES_CANCEL_nin => NOTES_CANCEL_nin,
      id_diet_prof_parent_in => id_diet_prof_parent_in,
      ID_DIET_PROF_PARENT_nin => ID_DIET_PROF_PARENT_nin,
      create_user_in => create_user_in,
      CREATE_USER_nin => CREATE_USER_nin,
      create_time_in => create_time_in,
      CREATE_TIME_nin => CREATE_TIME_nin,
      create_institution_in => create_institution_in,
      CREATE_INSTITUTION_nin => CREATE_INSTITUTION_nin,
      update_user_in => update_user_in,
      UPDATE_USER_nin => UPDATE_USER_nin,
      update_time_in => update_time_in,
      UPDATE_TIME_nin => UPDATE_TIME_nin,
      update_institution_in => update_institution_in,
      UPDATE_INSTITUTION_nin => UPDATE_INSTITUTION_nin,
      id_diet_type_in => id_diet_type_in,
      ID_DIET_TYPE_nin => ID_DIET_TYPE_nin,
      flg_help_in => flg_help_in,
      FLG_HELP_nin => FLG_HELP_nin,
      flg_institution_in => flg_institution_in,
      FLG_INSTITUTION_nin => FLG_INSTITUTION_nin,
    where_in => where_in,
     handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END upd;

   PROCEDURE upd (
      rec_in IN DIET_PROF_INSTIT%ROWTYPE,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      upd (
         id_diet_prof_instit_in => rec_in.ID_DIET_PROF_INSTIT,
         desc_diet_in => rec_in.DESC_DIET,
         flg_status_in => rec_in.FLG_STATUS,
         food_plan_in => rec_in.FOOD_PLAN,
         flg_share_in => rec_in.FLG_SHARE,
         id_prof_create_in => rec_in.ID_PROF_CREATE,
         id_institution_in => rec_in.ID_INSTITUTION,
         notes_in => rec_in.NOTES,
         dt_creation_in => rec_in.DT_CREATION,
         id_prof_cancel_in => rec_in.ID_PROF_CANCEL,
         dt_cancel_in => rec_in.DT_CANCEL,
         notes_cancel_in => rec_in.NOTES_CANCEL,
         id_diet_prof_parent_in => rec_in.ID_DIET_PROF_PARENT,
         create_user_in => rec_in.CREATE_USER,
         create_time_in => rec_in.CREATE_TIME,
         create_institution_in => rec_in.CREATE_INSTITUTION,
         update_user_in => rec_in.UPDATE_USER,
         update_time_in => rec_in.UPDATE_TIME,
         update_institution_in => rec_in.UPDATE_INSTITUTION,
         id_diet_type_in => rec_in.ID_DIET_TYPE,
         flg_help_in => rec_in.FLG_HELP,
         flg_institution_in => rec_in.FLG_INSTITUTION

        ,handle_error_in => handle_error_in
        , rows_out => rows_out
       );
   END upd;

   PROCEDURE upd (
      rec_in IN DIET_PROF_INSTIT%ROWTYPE,
     handle_error_in IN BOOLEAN := TRUE
      )
   IS
        rows_out TABLE_VARCHAR;
   BEGIN
      upd (
         id_diet_prof_instit_in => rec_in.ID_DIET_PROF_INSTIT,
         desc_diet_in => rec_in.DESC_DIET,
         flg_status_in => rec_in.FLG_STATUS,
         food_plan_in => rec_in.FOOD_PLAN,
         flg_share_in => rec_in.FLG_SHARE,
         id_prof_create_in => rec_in.ID_PROF_CREATE,
         id_institution_in => rec_in.ID_INSTITUTION,
         notes_in => rec_in.NOTES,
         dt_creation_in => rec_in.DT_CREATION,
         id_prof_cancel_in => rec_in.ID_PROF_CANCEL,
         dt_cancel_in => rec_in.DT_CANCEL,
         notes_cancel_in => rec_in.NOTES_CANCEL,
         id_diet_prof_parent_in => rec_in.ID_DIET_PROF_PARENT,
         create_user_in => rec_in.CREATE_USER,
         create_time_in => rec_in.CREATE_TIME,
         create_institution_in => rec_in.CREATE_INSTITUTION,
         update_user_in => rec_in.UPDATE_USER,
         update_time_in => rec_in.UPDATE_TIME,
         update_institution_in => rec_in.UPDATE_INSTITUTION,
         id_diet_type_in => rec_in.ID_DIET_TYPE,
         flg_help_in => rec_in.FLG_HELP,
         flg_institution_in => rec_in.FLG_INSTITUTION

        ,handle_error_in => handle_error_in
        , rows_out => rows_out
       );
   END upd;

   PROCEDURE upd_ins (
      id_diet_prof_instit_in IN DIET_PROF_INSTIT.ID_DIET_PROF_INSTIT%TYPE,
      desc_diet_in IN DIET_PROF_INSTIT.DESC_DIET%TYPE DEFAULT NULL,
      flg_status_in IN DIET_PROF_INSTIT.FLG_STATUS%TYPE DEFAULT NULL,
      food_plan_in IN DIET_PROF_INSTIT.FOOD_PLAN%TYPE DEFAULT NULL,
      flg_share_in IN DIET_PROF_INSTIT.FLG_SHARE%TYPE DEFAULT NULL,
      id_prof_create_in IN DIET_PROF_INSTIT.ID_PROF_CREATE%TYPE DEFAULT NULL,
      id_institution_in IN DIET_PROF_INSTIT.ID_INSTITUTION%TYPE DEFAULT NULL,
      notes_in IN DIET_PROF_INSTIT.NOTES%TYPE DEFAULT NULL,
      dt_creation_in IN DIET_PROF_INSTIT.DT_CREATION%TYPE DEFAULT NULL,
      id_prof_cancel_in IN DIET_PROF_INSTIT.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      dt_cancel_in IN DIET_PROF_INSTIT.DT_CANCEL%TYPE DEFAULT NULL,
      notes_cancel_in IN DIET_PROF_INSTIT.NOTES_CANCEL%TYPE DEFAULT NULL,
      id_diet_prof_parent_in IN DIET_PROF_INSTIT.ID_DIET_PROF_PARENT%TYPE DEFAULT NULL,
      create_user_in IN DIET_PROF_INSTIT.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN DIET_PROF_INSTIT.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN DIET_PROF_INSTIT.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN DIET_PROF_INSTIT.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN DIET_PROF_INSTIT.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN DIET_PROF_INSTIT.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      id_diet_type_in IN DIET_PROF_INSTIT.ID_DIET_TYPE%TYPE DEFAULT NULL,
      flg_help_in IN DIET_PROF_INSTIT.FLG_HELP%TYPE DEFAULT NULL,
      flg_institution_in IN DIET_PROF_INSTIT.FLG_INSTITUTION%TYPE DEFAULT NULL,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      upd (
         id_diet_prof_instit_in => id_diet_prof_instit_in,
         desc_diet_in => desc_diet_in,
         flg_status_in => flg_status_in,
         food_plan_in => food_plan_in,
         flg_share_in => flg_share_in,
         id_prof_create_in => id_prof_create_in,
         id_institution_in => id_institution_in,
         notes_in => notes_in,
         dt_creation_in => dt_creation_in,
         id_prof_cancel_in => id_prof_cancel_in,
         dt_cancel_in => dt_cancel_in,
         notes_cancel_in => notes_cancel_in,
         id_diet_prof_parent_in => id_diet_prof_parent_in,
         create_user_in => create_user_in,
         create_time_in => create_time_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in,
         id_diet_type_in => id_diet_type_in,
         flg_help_in => flg_help_in,
         flg_institution_in => flg_institution_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
       );
      IF SQL%ROWCOUNT = 0
      THEN
         ins (
            id_diet_prof_instit_in => id_diet_prof_instit_in,
            desc_diet_in => desc_diet_in,
            flg_status_in => flg_status_in,
            food_plan_in => food_plan_in,
            flg_share_in => flg_share_in,
            id_prof_create_in => id_prof_create_in,
            id_institution_in => id_institution_in,
            notes_in => notes_in,
            dt_creation_in => dt_creation_in,
            id_prof_cancel_in => id_prof_cancel_in,
            dt_cancel_in => dt_cancel_in,
            notes_cancel_in => notes_cancel_in,
            id_diet_prof_parent_in => id_diet_prof_parent_in,
            create_user_in => create_user_in,
            create_time_in => create_time_in,
            create_institution_in => create_institution_in,
            update_user_in => update_user_in,
            update_time_in => update_time_in,
            update_institution_in => update_institution_in,
            id_diet_type_in => id_diet_type_in,
            flg_help_in => flg_help_in,
            flg_institution_in => flg_institution_in
            ,handle_error_in => handle_error_in
            , rows_out => rows_out
         );
      END IF;
   END upd_ins;

   PROCEDURE upd_ins (
      id_diet_prof_instit_in IN DIET_PROF_INSTIT.ID_DIET_PROF_INSTIT%TYPE,
      desc_diet_in IN DIET_PROF_INSTIT.DESC_DIET%TYPE DEFAULT NULL,
      flg_status_in IN DIET_PROF_INSTIT.FLG_STATUS%TYPE DEFAULT NULL,
      food_plan_in IN DIET_PROF_INSTIT.FOOD_PLAN%TYPE DEFAULT NULL,
      flg_share_in IN DIET_PROF_INSTIT.FLG_SHARE%TYPE DEFAULT NULL,
      id_prof_create_in IN DIET_PROF_INSTIT.ID_PROF_CREATE%TYPE DEFAULT NULL,
      id_institution_in IN DIET_PROF_INSTIT.ID_INSTITUTION%TYPE DEFAULT NULL,
      notes_in IN DIET_PROF_INSTIT.NOTES%TYPE DEFAULT NULL,
      dt_creation_in IN DIET_PROF_INSTIT.DT_CREATION%TYPE DEFAULT NULL,
      id_prof_cancel_in IN DIET_PROF_INSTIT.ID_PROF_CANCEL%TYPE DEFAULT NULL,
      dt_cancel_in IN DIET_PROF_INSTIT.DT_CANCEL%TYPE DEFAULT NULL,
      notes_cancel_in IN DIET_PROF_INSTIT.NOTES_CANCEL%TYPE DEFAULT NULL,
      id_diet_prof_parent_in IN DIET_PROF_INSTIT.ID_DIET_PROF_PARENT%TYPE DEFAULT NULL,
      create_user_in IN DIET_PROF_INSTIT.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN DIET_PROF_INSTIT.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN DIET_PROF_INSTIT.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN DIET_PROF_INSTIT.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN DIET_PROF_INSTIT.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN DIET_PROF_INSTIT.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      id_diet_type_in IN DIET_PROF_INSTIT.ID_DIET_TYPE%TYPE DEFAULT NULL,
      flg_help_in IN DIET_PROF_INSTIT.FLG_HELP%TYPE DEFAULT NULL,
      flg_institution_in IN DIET_PROF_INSTIT.FLG_INSTITUTION%TYPE DEFAULT NULL,
     handle_error_in IN BOOLEAN := TRUE
      )
   IS
        rows_out TABLE_VARCHAR;
   BEGIN
      upd_ins (
      id_diet_prof_instit_in,
      desc_diet_in,
      flg_status_in,
      food_plan_in,
      flg_share_in,
      id_prof_create_in,
      id_institution_in,
      notes_in,
      dt_creation_in,
      id_prof_cancel_in,
      dt_cancel_in,
      notes_cancel_in,
      id_diet_prof_parent_in,
      create_user_in,
      create_time_in,
      create_institution_in,
      update_user_in,
      update_time_in,
      update_institution_in,
      id_diet_type_in,
      flg_help_in,
      flg_institution_in,
     handle_error_in
     ,rows_out
      );
   END upd_ins;


   PROCEDURE upd (
      col_in IN DIET_PROF_INSTIT_tc,
      ignore_if_null_in IN BOOLEAN := TRUE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      )
   IS
      l_ID_DIET_PROF_INSTIT ID_DIET_PROF_INSTIT_cc;
      l_DESC_DIET DESC_DIET_cc;
      l_FLG_STATUS FLG_STATUS_cc;
      l_FOOD_PLAN FOOD_PLAN_cc;
      l_FLG_SHARE FLG_SHARE_cc;
      l_ID_PROF_CREATE ID_PROF_CREATE_cc;
      l_ID_INSTITUTION ID_INSTITUTION_cc;
      l_NOTES NOTES_cc;
      l_DT_CREATION DT_CREATION_cc;
      l_ID_PROF_CANCEL ID_PROF_CANCEL_cc;
      l_DT_CANCEL DT_CANCEL_cc;
      l_NOTES_CANCEL NOTES_CANCEL_cc;
      l_ID_DIET_PROF_PARENT ID_DIET_PROF_PARENT_cc;
      l_CREATE_USER CREATE_USER_cc;
      l_CREATE_TIME CREATE_TIME_cc;
      l_CREATE_INSTITUTION CREATE_INSTITUTION_cc;
      l_UPDATE_USER UPDATE_USER_cc;
      l_UPDATE_TIME UPDATE_TIME_cc;
      l_UPDATE_INSTITUTION UPDATE_INSTITUTION_cc;
      l_ID_DIET_TYPE ID_DIET_TYPE_cc;
      l_FLG_HELP FLG_HELP_cc;
      l_FLG_INSTITUTION FLG_INSTITUTION_cc;
   BEGIN
      FOR i IN col_in.FIRST .. col_in.LAST loop
         l_ID_DIET_PROF_INSTIT(i) := col_in(i).ID_DIET_PROF_INSTIT;
         l_DESC_DIET(i) := col_in(i).DESC_DIET;
         l_FLG_STATUS(i) := col_in(i).FLG_STATUS;
         l_FOOD_PLAN(i) := col_in(i).FOOD_PLAN;
         l_FLG_SHARE(i) := col_in(i).FLG_SHARE;
         l_ID_PROF_CREATE(i) := col_in(i).ID_PROF_CREATE;
         l_ID_INSTITUTION(i) := col_in(i).ID_INSTITUTION;
         l_NOTES(i) := col_in(i).NOTES;
         l_DT_CREATION(i) := col_in(i).DT_CREATION;
         l_ID_PROF_CANCEL(i) := col_in(i).ID_PROF_CANCEL;
         l_DT_CANCEL(i) := col_in(i).DT_CANCEL;
         l_NOTES_CANCEL(i) := col_in(i).NOTES_CANCEL;
         l_ID_DIET_PROF_PARENT(i) := col_in(i).ID_DIET_PROF_PARENT;
         l_CREATE_USER(i) := col_in(i).CREATE_USER;
         l_CREATE_TIME(i) := col_in(i).CREATE_TIME;
         l_CREATE_INSTITUTION(i) := col_in(i).CREATE_INSTITUTION;
         l_UPDATE_USER(i) := col_in(i).UPDATE_USER;
         l_UPDATE_TIME(i) := col_in(i).UPDATE_TIME;
         l_UPDATE_INSTITUTION(i) := col_in(i).UPDATE_INSTITUTION;
         l_ID_DIET_TYPE(i) := col_in(i).ID_DIET_TYPE;
         l_FLG_HELP(i) := col_in(i).FLG_HELP;
         l_FLG_INSTITUTION(i) := col_in(i).FLG_INSTITUTION;
      END LOOP;
      IF NVL (ignore_if_null_in, FALSE)
      THEN
         -- Set any columns to their current values
         -- if incoming value is NULL.
         -- Put WHEN clause on column-level triggers!
         FORALL i IN col_in.FIRST .. col_in.LAST
            UPDATE DIET_PROF_INSTIT SET
               DESC_DIET = NVL (l_DESC_DIET(i), DESC_DIET),
               FLG_STATUS = NVL (l_FLG_STATUS(i), FLG_STATUS),
               FOOD_PLAN = NVL (l_FOOD_PLAN(i), FOOD_PLAN),
               FLG_SHARE = NVL (l_FLG_SHARE(i), FLG_SHARE),
               ID_PROF_CREATE = NVL (l_ID_PROF_CREATE(i), ID_PROF_CREATE),
               ID_INSTITUTION = NVL (l_ID_INSTITUTION(i), ID_INSTITUTION),
               NOTES = NVL (l_NOTES(i), NOTES),
               DT_CREATION = NVL (l_DT_CREATION(i), DT_CREATION),
               ID_PROF_CANCEL = NVL (l_ID_PROF_CANCEL(i), ID_PROF_CANCEL),
               DT_CANCEL = NVL (l_DT_CANCEL(i), DT_CANCEL),
               NOTES_CANCEL = NVL (l_NOTES_CANCEL(i), NOTES_CANCEL),
               ID_DIET_PROF_PARENT = NVL (l_ID_DIET_PROF_PARENT(i), ID_DIET_PROF_PARENT),
               CREATE_USER = NVL (l_CREATE_USER(i), CREATE_USER),
               CREATE_TIME = NVL (l_CREATE_TIME(i), CREATE_TIME),
               CREATE_INSTITUTION = NVL (l_CREATE_INSTITUTION(i), CREATE_INSTITUTION),
               UPDATE_USER = NVL (l_UPDATE_USER(i), UPDATE_USER),
               UPDATE_TIME = NVL (l_UPDATE_TIME(i), UPDATE_TIME),
               UPDATE_INSTITUTION = NVL (l_UPDATE_INSTITUTION(i), UPDATE_INSTITUTION),
               ID_DIET_TYPE = NVL (l_ID_DIET_TYPE(i), ID_DIET_TYPE),
               FLG_HELP = NVL (l_FLG_HELP(i), FLG_HELP),
               FLG_INSTITUTION = NVL (l_FLG_INSTITUTION(i), FLG_INSTITUTION)
             WHERE
                ID_DIET_PROF_INSTIT = l_ID_DIET_PROF_INSTIT(i)
          returning rowid bulk collect into rows_out;
      ELSE
         FORALL i IN col_in.FIRST .. col_in.LAST
            UPDATE DIET_PROF_INSTIT SET
               DESC_DIET = l_DESC_DIET(i),
               FLG_STATUS = l_FLG_STATUS(i),
               FOOD_PLAN = l_FOOD_PLAN(i),
               FLG_SHARE = l_FLG_SHARE(i),
               ID_PROF_CREATE = l_ID_PROF_CREATE(i),
               ID_INSTITUTION = l_ID_INSTITUTION(i),
               NOTES = l_NOTES(i),
               DT_CREATION = l_DT_CREATION(i),
               ID_PROF_CANCEL = l_ID_PROF_CANCEL(i),
               DT_CANCEL = l_DT_CANCEL(i),
               NOTES_CANCEL = l_NOTES_CANCEL(i),
               ID_DIET_PROF_PARENT = l_ID_DIET_PROF_PARENT(i),
               CREATE_USER = l_CREATE_USER(i),
               CREATE_TIME = l_CREATE_TIME(i),
               CREATE_INSTITUTION = l_CREATE_INSTITUTION(i),
               UPDATE_USER = l_UPDATE_USER(i),
               UPDATE_TIME = l_UPDATE_TIME(i),
               UPDATE_INSTITUTION = l_UPDATE_INSTITUTION(i),
               ID_DIET_TYPE = l_ID_DIET_TYPE(i),
               FLG_HELP = l_FLG_HELP(i),
               FLG_INSTITUTION = l_FLG_INSTITUTION(i)
             WHERE
                ID_DIET_PROF_INSTIT = l_ID_DIET_PROF_INSTIT(i)
        returning rowid bulk collect into rows_out  ;
      END IF;
   END upd;


   PROCEDURE upd (
      col_in IN DIET_PROF_INSTIT_tc,
      ignore_if_null_in IN BOOLEAN := TRUE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
     rows_out TABLE_VARCHAR;
   BEGIN
      upd (
      col_in ,
      ignore_if_null_in
     ,handle_error_in
     , rows_out
      );
   END upd;

   FUNCTION dynupdstr (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL)

   RETURN VARCHAR2
   IS
   BEGIN
      RETURN
         'BEGIN UPDATE DIET_PROF_INSTIT
             SET ' || colname_in || ' = :value
           WHERE ' || NVL (where_in, '1=1') || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;' ;
   END dynupdstr;

   FUNCTION dynupdstr_no_rows_out (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL)

   RETURN VARCHAR2
   IS
   BEGIN
      RETURN
         'UPDATE DIET_PROF_INSTIT
             SET ' || colname_in || ' = :value
           WHERE ' || NVL (where_in, '1=1');
   END dynupdstr_no_rows_out;























  PROCEDURE increment_onecol (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL
      , increment_value_in IN NUMBER DEFAULT 1
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN UPDATE DIET_PROF_INSTIT set ' || colname_in || '=' || colname_in || ' + ' || nvl(increment_value_in,1) || ' WHERE ' || NVL (where_in, '1=1') || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
      USING OUT rows_out;
   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'DUPLICATE-VALUE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'DIET_PROF_INSTIT');
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'DIET_PROF_INSTIT');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'DIET_PROF_INSTIT');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END increment_onecol;

   PROCEDURE increment_onecol (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL
     , increment_value_in IN NUMBER DEFAULT 1
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
   rows_out table_varchar;
   BEGIN
      EXECUTE IMMEDIATE 'UPDATE DIET_PROF_INSTIT set ' || colname_in || '=' || colname_in || ' + ' || nvl(increment_value_in,1) || ' WHERE ' || NVL (where_in, '1=1');
   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'DUPLICATE-VALUE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'DIET_PROF_INSTIT');
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'DIET_PROF_INSTIT');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'DIET_PROF_INSTIT');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END increment_onecol;


   -- Delete functionality


   PROCEDURE del (
      id_diet_prof_instit_in IN DIET_PROF_INSTIT.ID_DIET_PROF_INSTIT%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM DIET_PROF_INSTIT
       WHERE
          ID_DIET_PROF_INSTIT = id_diet_prof_instit_in
       RETURNING ROWID BULK COLLECT INTO rows_out
         ;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'DIET_PROF_INSTIT');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'DIET_PROF_INSTIT');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del;




   PROCEDURE del (
      id_diet_prof_instit_in IN DIET_PROF_INSTIT.ID_DIET_PROF_INSTIT%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
rows_out TABLE_VARCHAR;
   BEGIN

del (
      id_diet_prof_instit_in => id_diet_prof_instit_in
     ,handle_error_in => handle_error_in
, rows_out => rows_out
      );

   END del;








   -- Delete all rows for primary key column ID_DIET_PROF_INSTIT
   PROCEDURE del_ID_DIET_PROF_INSTIT (
      id_diet_prof_instit_in IN DIET_PROF_INSTIT.ID_DIET_PROF_INSTIT%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
     )
   IS
   BEGIN
      DELETE FROM DIET_PROF_INSTIT
       WHERE ID_DIET_PROF_INSTIT = id_diet_prof_instit_in
      RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'DIET_PROF_INSTIT');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'DIET_PROF_INSTIT');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_ID_DIET_PROF_INSTIT;






   -- Delete all rows for primary key column ID_DIET_PROF_INSTIT
   PROCEDURE del_ID_DIET_PROF_INSTIT (
      id_diet_prof_instit_in IN DIET_PROF_INSTIT.ID_DIET_PROF_INSTIT%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_ID_DIET_PROF_INSTIT (
      id_diet_prof_instit_in => id_diet_prof_instit_in
     ,handle_error_in => handle_error_in
, rows_out => rows_out
     );
   END del_ID_DIET_PROF_INSTIT;















   PROCEDURE del_DPI_DIET_PARENT_FK (
      id_diet_prof_parent_in IN DIET_PROF_INSTIT.ID_DIET_PROF_PARENT%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM DIET_PROF_INSTIT
       WHERE
          ID_DIET_PROF_PARENT = del_DPI_DIET_PARENT_FK.id_diet_prof_parent_in
       RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'DIET_PROF_INSTIT');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'DIET_PROF_INSTIT');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_DPI_DIET_PARENT_FK;



PROCEDURE del_DPI_DIET_PARENT_FK (
      id_diet_prof_parent_in IN DIET_PROF_INSTIT.ID_DIET_PROF_PARENT%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_DPI_DIET_PARENT_FK (
      id_diet_prof_parent_in => id_diet_prof_parent_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END del_DPI_DIET_PARENT_FK;





   PROCEDURE del_DPI_INST_FK (
      id_institution_in IN DIET_PROF_INSTIT.ID_INSTITUTION%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM DIET_PROF_INSTIT
       WHERE
          ID_INSTITUTION = del_DPI_INST_FK.id_institution_in
       RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'DIET_PROF_INSTIT');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'DIET_PROF_INSTIT');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_DPI_INST_FK;



PROCEDURE del_DPI_INST_FK (
      id_institution_in IN DIET_PROF_INSTIT.ID_INSTITUTION%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_DPI_INST_FK (
      id_institution_in => id_institution_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END del_DPI_INST_FK;





   PROCEDURE del_DPI_PROFC_FK (
      id_prof_cancel_in IN DIET_PROF_INSTIT.ID_PROF_CANCEL%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM DIET_PROF_INSTIT
       WHERE
          ID_PROF_CANCEL = del_DPI_PROFC_FK.id_prof_cancel_in
       RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'DIET_PROF_INSTIT');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'DIET_PROF_INSTIT');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_DPI_PROFC_FK;



PROCEDURE del_DPI_PROFC_FK (
      id_prof_cancel_in IN DIET_PROF_INSTIT.ID_PROF_CANCEL%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_DPI_PROFC_FK (
      id_prof_cancel_in => id_prof_cancel_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END del_DPI_PROFC_FK;





   PROCEDURE del_DPI_PROF_FK (
      id_prof_create_in IN DIET_PROF_INSTIT.ID_PROF_CREATE%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM DIET_PROF_INSTIT
       WHERE
          ID_PROF_CREATE = del_DPI_PROF_FK.id_prof_create_in
       RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'DIET_PROF_INSTIT');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'DIET_PROF_INSTIT');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_DPI_PROF_FK;



PROCEDURE del_DPI_PROF_FK (
      id_prof_create_in IN DIET_PROF_INSTIT.ID_PROF_CREATE%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_DPI_PROF_FK (
      id_prof_create_in => id_prof_create_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END del_DPI_PROF_FK;












   -- Deletions using dynamic SQL
   FUNCTION dyndelstr (where_in IN VARCHAR2) RETURN VARCHAR2
   IS
   BEGIN
      IF where_in IS NULL
      THEN
         RETURN 'DELETE FROM DIET_PROF_INSTIT';
      ELSE
         RETURN
            'DELETE FROM DIET_PROF_INSTIT WHERE ' || where_in;
      END IF;
   END dyndelstr;

   FUNCTION dyncoldelstr (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE)
      RETURN VARCHAR2
   IS
   BEGIN
      RETURN dyndelstr ( colname_in || ' = :value' );
   END;

   PROCEDURE del_by (
      where_clause_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyndelstr (where_clause_in);
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'DIET_PROF_INSTIT');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'DIET_PROF_INSTIT');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by;





   PROCEDURE del_by (
      where_clause_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN ' || dyndelstr (where_clause_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;' using OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'DIET_PROF_INSTIT');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'DIET_PROF_INSTIT');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by;





   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyncoldelstr (colname_in)
         USING colvalue_in;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'DIET_PROF_INSTIT');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'DIET_PROF_INSTIT');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;






   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr (colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
         USING IN colvalue_in, OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'DIET_PROF_INSTIT');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'DIET_PROF_INSTIT');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN DATE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyncoldelstr (colname_in)
         USING colvalue_in;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'DIET_PROF_INSTIT');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'DIET_PROF_INSTIT');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN DATE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr (colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
         USING IN colvalue_in, OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'DIET_PROF_INSTIT');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'DIET_PROF_INSTIT');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN TIMESTAMP WITH LOCAL TIME ZONE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyncoldelstr (colname_in)
         USING colvalue_in;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'DIET_PROF_INSTIT');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'DIET_PROF_INSTIT');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN TIMESTAMP WITH LOCAL TIME ZONE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr (colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
         USING IN colvalue_in, OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'DIET_PROF_INSTIT');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'DIET_PROF_INSTIT');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN NUMBER
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyncoldelstr (colname_in)
         USING colvalue_in;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'DIET_PROF_INSTIT');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'DIET_PROF_INSTIT');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;






   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN NUMBER
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
   BEGIN
     EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr (colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
         USING IN colvalue_in, OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'DIET_PROF_INSTIT');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'DIET_PROF_INSTIT');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   -- Initialize a record with default values for columns in the table.
   PROCEDURE initrec (
      diet_prof_instit_inout IN OUT DIET_PROF_INSTIT%ROWTYPE
   )
   IS
   BEGIN
      diet_prof_instit_inout.ID_DIET_PROF_INSTIT := NULL;
      diet_prof_instit_inout.DESC_DIET := NULL;
      diet_prof_instit_inout.FLG_STATUS := NULL;
      diet_prof_instit_inout.FOOD_PLAN := NULL;
      diet_prof_instit_inout.FLG_SHARE := NULL;
      diet_prof_instit_inout.ID_PROF_CREATE := NULL;
      diet_prof_instit_inout.ID_INSTITUTION := NULL;
      diet_prof_instit_inout.NOTES := NULL;
      diet_prof_instit_inout.DT_CREATION := NULL;
      diet_prof_instit_inout.ID_PROF_CANCEL := NULL;
      diet_prof_instit_inout.DT_CANCEL := NULL;
      diet_prof_instit_inout.NOTES_CANCEL := NULL;
      diet_prof_instit_inout.ID_DIET_PROF_PARENT := NULL;
      diet_prof_instit_inout.CREATE_USER := NULL;
      diet_prof_instit_inout.CREATE_TIME := NULL;
      diet_prof_instit_inout.CREATE_INSTITUTION := NULL;
      diet_prof_instit_inout.UPDATE_USER := NULL;
      diet_prof_instit_inout.UPDATE_TIME := NULL;
      diet_prof_instit_inout.UPDATE_INSTITUTION := NULL;
      diet_prof_instit_inout.ID_DIET_TYPE := NULL;
      diet_prof_instit_inout.FLG_HELP := NULL;
      diet_prof_instit_inout.FLG_INSTITUTION := NULL;
   END initrec;

   FUNCTION initrec RETURN DIET_PROF_INSTIT%ROWTYPE
   IS
      l_diet_prof_instit DIET_PROF_INSTIT%ROWTYPE;
   BEGIN
      RETURN l_diet_prof_instit;
   END initrec;


   FUNCTION get_data_rowid(
        rows_in IN TABLE_VARCHAR
        ) RETURN DIET_PROF_INSTIT_tc
   IS
        data DIET_PROF_INSTIT_tc;
   BEGIN
        select * bulk collect into data from DIET_PROF_INSTIT where rowid in (select /*+opt_estimate(table,t,scale_rows=0.0000001))*/ * from table(rows_in) t);
        return data;
        EXCEPTION
      WHEN OTHERS THEN
        pk_alert_exceptions.raise_error (
           error_name_in => 'get_data_rowid'
           );
   END get_data_rowid;


   FUNCTION get_data_rowid_pat(
        rows_in IN TABLE_VARCHAR
        ) RETURN DIET_PROF_INSTIT_tc
   is
        PRAGMA AUTONOMOUS_TRANSACTION;
        data DIET_PROF_INSTIT_tc;
   BEGIN
        data := get_data_rowid(rows_in);
        commit;
        return data;
        EXCEPTION
      WHEN OTHERS THEN
        pk_alert_exceptions.raise_error (
           error_name_in => 'get_data_rowid'
           );
        rollback;
    END get_data_rowid_pat;

BEGIN
   NULL;
END TS_DIET_PROF_INSTIT;
/
