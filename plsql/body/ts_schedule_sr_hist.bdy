/*-- Last Change Revision: $Rev: 1861243 $*/
/*-- Last Change by: $Author: pedro.henriques $*/
/*-- Date of last change: $Date: 2018-08-30 11:38:02 +0100 (qui, 30 ago 2018) $*/
CREATE OR REPLACE PACKAGE BODY ts_schedule_sr_hist
/*
| Generated by or retrieved - DO NOT MODIFY!
| Created On: 2018-07-17 10:46:31
| Created By: ALERT
*/
 IS

    e_null_column_value EXCEPTION;
    PRAGMA EXCEPTION_INIT(e_null_column_value, -1400);

    e_existing_fky_reference EXCEPTION;
    PRAGMA EXCEPTION_INIT(e_existing_fky_reference, -2266);

    e_check_constraint_failure EXCEPTION;
    PRAGMA EXCEPTION_INIT(e_check_constraint_failure, -2290);

    e_no_parent_key EXCEPTION;
    PRAGMA EXCEPTION_INIT(e_no_parent_key, -2291);

    e_child_record_found EXCEPTION;
    PRAGMA EXCEPTION_INIT(e_child_record_found, -2292);

    e_forall_error EXCEPTION;
    PRAGMA EXCEPTION_INIT(e_forall_error, -24381);

    -- Defined for backward compatibilty.
    e_integ_constraint_failure EXCEPTION;
    PRAGMA EXCEPTION_INIT(e_integ_constraint_failure, -2291);

    PROCEDURE ins
    (
        id_schedule_sr_in      IN schedule_sr_hist.id_schedule_sr%TYPE DEFAULT NULL,
        id_sched_sr_parent_in  IN schedule_sr_hist.id_sched_sr_parent%TYPE DEFAULT NULL,
        id_schedule_in         IN schedule_sr_hist.id_schedule%TYPE DEFAULT NULL,
        id_episode_in          IN schedule_sr_hist.id_episode%TYPE DEFAULT NULL,
        id_patient_in          IN schedule_sr_hist.id_patient%TYPE DEFAULT NULL,
        duration_in            IN schedule_sr_hist.duration%TYPE DEFAULT NULL,
        id_diagnosis_in        IN schedule_sr_hist.id_diagnosis%TYPE DEFAULT NULL,
        id_speciality_in       IN schedule_sr_hist.id_speciality%TYPE DEFAULT NULL,
        flg_status_in          IN schedule_sr_hist.flg_status%TYPE DEFAULT NULL,
        flg_sched_in           IN schedule_sr_hist.flg_sched%TYPE DEFAULT NULL,
        id_dept_dest_in        IN schedule_sr_hist.id_dept_dest%TYPE DEFAULT NULL,
        prev_recovery_time_in  IN schedule_sr_hist.prev_recovery_time%TYPE DEFAULT NULL,
        id_sr_cancel_reason_in IN schedule_sr_hist.id_sr_cancel_reason%TYPE DEFAULT NULL,
        id_prof_cancel_in      IN schedule_sr_hist.id_prof_cancel%TYPE DEFAULT NULL,
        notes_cancel_in        IN schedule_sr_hist.notes_cancel%TYPE DEFAULT NULL,
        id_prof_reg_in         IN schedule_sr_hist.id_prof_reg%TYPE DEFAULT NULL,
        id_institution_in      IN schedule_sr_hist.id_institution%TYPE DEFAULT NULL,
        adw_last_update_in     IN schedule_sr_hist.adw_last_update%TYPE DEFAULT SYSDATE,
        dt_target_in           IN schedule_sr_hist.dt_target%TYPE DEFAULT NULL,
        dt_interv_preview_in   IN schedule_sr_hist.dt_interv_preview%TYPE DEFAULT NULL,
        dt_cancel_in           IN schedule_sr_hist.dt_cancel%TYPE DEFAULT NULL,
        create_user_in         IN schedule_sr_hist.create_user%TYPE DEFAULT NULL,
        create_time_in         IN schedule_sr_hist.create_time%TYPE DEFAULT NULL,
        create_institution_in  IN schedule_sr_hist.create_institution%TYPE DEFAULT NULL,
        update_user_in         IN schedule_sr_hist.update_user%TYPE DEFAULT NULL,
        update_time_in         IN schedule_sr_hist.update_time%TYPE DEFAULT NULL,
        update_institution_in  IN schedule_sr_hist.update_institution%TYPE DEFAULT NULL,
        icu_in                 IN schedule_sr_hist.icu%TYPE DEFAULT 'N',
        notes_in               IN schedule_sr_hist.notes%TYPE DEFAULT NULL,
        id_waiting_list_in     IN schedule_sr_hist.id_waiting_list%TYPE DEFAULT NULL,
        adm_needed_in          IN schedule_sr_hist.adm_needed%TYPE DEFAULT NULL,
        flg_temporary_in       IN schedule_sr_hist.flg_temporary%TYPE DEFAULT NULL,
        flg_dur_control_in     IN schedule_sr_hist.flg_dur_control%TYPE DEFAULT NULL,
        id_diag_inst_owner_in  IN schedule_sr_hist.id_diag_inst_owner%TYPE DEFAULT NULL,
        dt_update_in           IN schedule_sr_hist.dt_update%TYPE DEFAULT current_timestamp,
        id_prof_update_in      IN schedule_sr_hist.id_prof_update%TYPE DEFAULT NULL,
        need_global_anesth_in  IN schedule_sr_hist.need_global_anesth%TYPE DEFAULT NULL,
        need_local_anesth_in   IN schedule_sr_hist.need_local_anesth%TYPE DEFAULT NULL,
        icu_pos_in             IN schedule_sr_hist.icu_pos%TYPE DEFAULT NULL,
        handle_error_in        IN BOOLEAN := TRUE,
        rows_out               OUT table_varchar
    ) IS
    BEGIN
        INSERT INTO schedule_sr_hist
            (id_schedule_sr,
             id_sched_sr_parent,
             id_schedule,
             id_episode,
             id_patient,
             duration,
             id_diagnosis,
             id_speciality,
             flg_status,
             flg_sched,
             id_dept_dest,
             prev_recovery_time,
             id_sr_cancel_reason,
             id_prof_cancel,
             notes_cancel,
             id_prof_reg,
             id_institution,
             adw_last_update,
             dt_target,
             dt_interv_preview,
             dt_cancel,
             create_user,
             create_time,
             create_institution,
             update_user,
             update_time,
             update_institution,
             icu,
             notes,
             id_waiting_list,
             adm_needed,
             flg_temporary,
             flg_dur_control,
             id_diag_inst_owner,
             dt_update,
             id_prof_update,
             need_global_anesth,
             need_local_anesth,
             icu_pos)
        VALUES
            (id_schedule_sr_in,
             id_sched_sr_parent_in,
             id_schedule_in,
             id_episode_in,
             id_patient_in,
             duration_in,
             id_diagnosis_in,
             id_speciality_in,
             flg_status_in,
             flg_sched_in,
             id_dept_dest_in,
             prev_recovery_time_in,
             id_sr_cancel_reason_in,
             id_prof_cancel_in,
             notes_cancel_in,
             id_prof_reg_in,
             id_institution_in,
             adw_last_update_in,
             dt_target_in,
             dt_interv_preview_in,
             dt_cancel_in,
             create_user_in,
             create_time_in,
             create_institution_in,
             update_user_in,
             update_time_in,
             update_institution_in,
             icu_in,
             notes_in,
             id_waiting_list_in,
             adm_needed_in,
             flg_temporary_in,
             flg_dur_control_in,
             id_diag_inst_owner_in,
             dt_update_in,
             id_prof_update_in,
             need_global_anesth_in,
             need_local_anesth_in,
             icu_pos_in)
        RETURNING ROWID BULK COLLECT INTO rows_out;
    
    EXCEPTION
        WHEN e_null_column_value
             OR e_existing_fky_reference
             OR e_check_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found
             OR e_forall_error
             OR e_integ_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                pk_alertlog.log_info('DML error ignored: ' || SQLCODE || ': ' || SQLERRM);
            END IF;
        
    END ins;

    PROCEDURE ins
    (
        id_schedule_sr_in      IN schedule_sr_hist.id_schedule_sr%TYPE DEFAULT NULL,
        id_sched_sr_parent_in  IN schedule_sr_hist.id_sched_sr_parent%TYPE DEFAULT NULL,
        id_schedule_in         IN schedule_sr_hist.id_schedule%TYPE DEFAULT NULL,
        id_episode_in          IN schedule_sr_hist.id_episode%TYPE DEFAULT NULL,
        id_patient_in          IN schedule_sr_hist.id_patient%TYPE DEFAULT NULL,
        duration_in            IN schedule_sr_hist.duration%TYPE DEFAULT NULL,
        id_diagnosis_in        IN schedule_sr_hist.id_diagnosis%TYPE DEFAULT NULL,
        id_speciality_in       IN schedule_sr_hist.id_speciality%TYPE DEFAULT NULL,
        flg_status_in          IN schedule_sr_hist.flg_status%TYPE DEFAULT NULL,
        flg_sched_in           IN schedule_sr_hist.flg_sched%TYPE DEFAULT NULL,
        id_dept_dest_in        IN schedule_sr_hist.id_dept_dest%TYPE DEFAULT NULL,
        prev_recovery_time_in  IN schedule_sr_hist.prev_recovery_time%TYPE DEFAULT NULL,
        id_sr_cancel_reason_in IN schedule_sr_hist.id_sr_cancel_reason%TYPE DEFAULT NULL,
        id_prof_cancel_in      IN schedule_sr_hist.id_prof_cancel%TYPE DEFAULT NULL,
        notes_cancel_in        IN schedule_sr_hist.notes_cancel%TYPE DEFAULT NULL,
        id_prof_reg_in         IN schedule_sr_hist.id_prof_reg%TYPE DEFAULT NULL,
        id_institution_in      IN schedule_sr_hist.id_institution%TYPE DEFAULT NULL,
        adw_last_update_in     IN schedule_sr_hist.adw_last_update%TYPE DEFAULT SYSDATE,
        dt_target_in           IN schedule_sr_hist.dt_target%TYPE DEFAULT NULL,
        dt_interv_preview_in   IN schedule_sr_hist.dt_interv_preview%TYPE DEFAULT NULL,
        dt_cancel_in           IN schedule_sr_hist.dt_cancel%TYPE DEFAULT NULL,
        create_user_in         IN schedule_sr_hist.create_user%TYPE DEFAULT NULL,
        create_time_in         IN schedule_sr_hist.create_time%TYPE DEFAULT NULL,
        create_institution_in  IN schedule_sr_hist.create_institution%TYPE DEFAULT NULL,
        update_user_in         IN schedule_sr_hist.update_user%TYPE DEFAULT NULL,
        update_time_in         IN schedule_sr_hist.update_time%TYPE DEFAULT NULL,
        update_institution_in  IN schedule_sr_hist.update_institution%TYPE DEFAULT NULL,
        icu_in                 IN schedule_sr_hist.icu%TYPE DEFAULT 'N',
        notes_in               IN schedule_sr_hist.notes%TYPE DEFAULT NULL,
        id_waiting_list_in     IN schedule_sr_hist.id_waiting_list%TYPE DEFAULT NULL,
        adm_needed_in          IN schedule_sr_hist.adm_needed%TYPE DEFAULT NULL,
        flg_temporary_in       IN schedule_sr_hist.flg_temporary%TYPE DEFAULT NULL,
        flg_dur_control_in     IN schedule_sr_hist.flg_dur_control%TYPE DEFAULT NULL,
        id_diag_inst_owner_in  IN schedule_sr_hist.id_diag_inst_owner%TYPE DEFAULT NULL,
        dt_update_in           IN schedule_sr_hist.dt_update%TYPE DEFAULT current_timestamp,
        id_prof_update_in      IN schedule_sr_hist.id_prof_update%TYPE DEFAULT NULL,
        need_global_anesth_in  IN schedule_sr_hist.need_global_anesth%TYPE DEFAULT NULL,
        need_local_anesth_in   IN schedule_sr_hist.need_local_anesth%TYPE DEFAULT NULL,
        icu_pos_in             IN schedule_sr_hist.icu_pos%TYPE DEFAULT NULL,
        handle_error_in        IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        ins(id_schedule_sr_in      => id_schedule_sr_in,
            id_sched_sr_parent_in  => id_sched_sr_parent_in,
            id_schedule_in         => id_schedule_in,
            id_episode_in          => id_episode_in,
            id_patient_in          => id_patient_in,
            duration_in            => duration_in,
            id_diagnosis_in        => id_diagnosis_in,
            id_speciality_in       => id_speciality_in,
            flg_status_in          => flg_status_in,
            flg_sched_in           => flg_sched_in,
            id_dept_dest_in        => id_dept_dest_in,
            prev_recovery_time_in  => prev_recovery_time_in,
            id_sr_cancel_reason_in => id_sr_cancel_reason_in,
            id_prof_cancel_in      => id_prof_cancel_in,
            notes_cancel_in        => notes_cancel_in,
            id_prof_reg_in         => id_prof_reg_in,
            id_institution_in      => id_institution_in,
            adw_last_update_in     => adw_last_update_in,
            dt_target_in           => dt_target_in,
            dt_interv_preview_in   => dt_interv_preview_in,
            dt_cancel_in           => dt_cancel_in,
            create_user_in         => create_user_in,
            create_time_in         => create_time_in,
            create_institution_in  => create_institution_in,
            update_user_in         => update_user_in,
            update_time_in         => update_time_in,
            update_institution_in  => update_institution_in,
            icu_in                 => icu_in,
            notes_in               => notes_in,
            id_waiting_list_in     => id_waiting_list_in,
            adm_needed_in          => adm_needed_in,
            flg_temporary_in       => flg_temporary_in,
            flg_dur_control_in     => flg_dur_control_in,
            id_diag_inst_owner_in  => id_diag_inst_owner_in,
            dt_update_in           => dt_update_in,
            id_prof_update_in      => id_prof_update_in,
            need_global_anesth_in  => need_global_anesth_in,
            need_local_anesth_in   => need_local_anesth_in,
            icu_pos_in             => icu_pos_in,
            handle_error_in        => handle_error_in,
            rows_out               => rows_out);
    END ins;

    PROCEDURE ins
    (
        rec_in          IN schedule_sr_hist%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) IS
        l_rec schedule_sr_hist%ROWTYPE := rec_in;
    BEGIN
    
        ins(id_schedule_sr_in      => l_rec.id_schedule_sr,
            id_sched_sr_parent_in  => l_rec.id_sched_sr_parent,
            id_schedule_in         => l_rec.id_schedule,
            id_episode_in          => l_rec.id_episode,
            id_patient_in          => l_rec.id_patient,
            duration_in            => l_rec.duration,
            id_diagnosis_in        => l_rec.id_diagnosis,
            id_speciality_in       => l_rec.id_speciality,
            flg_status_in          => l_rec.flg_status,
            flg_sched_in           => l_rec.flg_sched,
            id_dept_dest_in        => l_rec.id_dept_dest,
            prev_recovery_time_in  => l_rec.prev_recovery_time,
            id_sr_cancel_reason_in => l_rec.id_sr_cancel_reason,
            id_prof_cancel_in      => l_rec.id_prof_cancel,
            notes_cancel_in        => l_rec.notes_cancel,
            id_prof_reg_in         => l_rec.id_prof_reg,
            id_institution_in      => l_rec.id_institution,
            adw_last_update_in     => l_rec.adw_last_update,
            dt_target_in           => l_rec.dt_target,
            dt_interv_preview_in   => l_rec.dt_interv_preview,
            dt_cancel_in           => l_rec.dt_cancel,
            create_user_in         => l_rec.create_user,
            create_time_in         => l_rec.create_time,
            create_institution_in  => l_rec.create_institution,
            update_user_in         => l_rec.update_user,
            update_time_in         => l_rec.update_time,
            update_institution_in  => l_rec.update_institution,
            icu_in                 => l_rec.icu,
            notes_in               => l_rec.notes,
            id_waiting_list_in     => l_rec.id_waiting_list,
            adm_needed_in          => l_rec.adm_needed,
            flg_temporary_in       => l_rec.flg_temporary,
            flg_dur_control_in     => l_rec.flg_dur_control,
            id_diag_inst_owner_in  => l_rec.id_diag_inst_owner,
            dt_update_in           => l_rec.dt_update,
            id_prof_update_in      => l_rec.id_prof_update,
            need_global_anesth_in  => l_rec.need_global_anesth,
            need_local_anesth_in   => l_rec.need_local_anesth,
            icu_pos_in             => l_rec.icu_pos,
            handle_error_in        => handle_error_in,
            rows_out               => rows_out);
    END ins;

    PROCEDURE ins
    (
        rec_in          IN schedule_sr_hist%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        ins(rec_in          => rec_in,
            gen_pky_in      => gen_pky_in,
            sequence_in     => sequence_in,
            handle_error_in => handle_error_in,
            rows_out        => rows_out);
    END ins;

    -- Insert a collection of rows using FORALL; all primary key values
    -- must have already been generated, or are handled in triggers
    PROCEDURE ins
    (
        rows_in         IN schedule_sr_hist_tc,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) IS
    BEGIN
        IF rows_in.count = 0
        THEN
            NULL;
        ELSE
            FORALL indx IN rows_in.first .. rows_in.last SAVE EXCEPTIONS
                INSERT INTO schedule_sr_hist
                VALUES rows_in
                    (indx)
                RETURNING ROWID BULK COLLECT INTO rows_out;
        END IF;
    
    EXCEPTION
        WHEN e_forall_error THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                FOR indx IN 1 .. SQL%bulk_exceptions.count
                LOOP
                    pk_alertlog.log_info('DML error ignored: ' || SQLCODE || ': ' || SQLERRM);
                END LOOP;
            END IF;
        WHEN OTHERS THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                pk_alertlog.log_info('DML error ignored: ' || SQLCODE || ': ' || SQLERRM);
            END IF;
        
    END ins;

    -- Insert a collection of rows using FORALL; all primary key values
    -- must have already been generated, or are handled in triggers
    PROCEDURE ins
    (
        rows_in         IN schedule_sr_hist_tc,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        ins(rows_in => rows_in, handle_error_in => handle_error_in, rows_out => rows_out);
    END ins;

    -- Update any/all columns by primary key. If you pass NULL, then
    -- the current column value is set to itself. If you need a more
    -- selected UPDATE then use one of the onecol procedures below.
    PROCEDURE upd
    (
        id_schedule_sr_in       IN schedule_sr_hist.id_schedule_sr%TYPE DEFAULT NULL,
        id_schedule_sr_nin      IN BOOLEAN := TRUE,
        id_sched_sr_parent_in   IN schedule_sr_hist.id_sched_sr_parent%TYPE DEFAULT NULL,
        id_sched_sr_parent_nin  IN BOOLEAN := TRUE,
        id_schedule_in          IN schedule_sr_hist.id_schedule%TYPE DEFAULT NULL,
        id_schedule_nin         IN BOOLEAN := TRUE,
        id_episode_in           IN schedule_sr_hist.id_episode%TYPE DEFAULT NULL,
        id_episode_nin          IN BOOLEAN := TRUE,
        id_patient_in           IN schedule_sr_hist.id_patient%TYPE DEFAULT NULL,
        id_patient_nin          IN BOOLEAN := TRUE,
        duration_in             IN schedule_sr_hist.duration%TYPE DEFAULT NULL,
        duration_nin            IN BOOLEAN := TRUE,
        id_diagnosis_in         IN schedule_sr_hist.id_diagnosis%TYPE DEFAULT NULL,
        id_diagnosis_nin        IN BOOLEAN := TRUE,
        id_speciality_in        IN schedule_sr_hist.id_speciality%TYPE DEFAULT NULL,
        id_speciality_nin       IN BOOLEAN := TRUE,
        flg_status_in           IN schedule_sr_hist.flg_status%TYPE DEFAULT NULL,
        flg_status_nin          IN BOOLEAN := TRUE,
        flg_sched_in            IN schedule_sr_hist.flg_sched%TYPE DEFAULT NULL,
        flg_sched_nin           IN BOOLEAN := TRUE,
        id_dept_dest_in         IN schedule_sr_hist.id_dept_dest%TYPE DEFAULT NULL,
        id_dept_dest_nin        IN BOOLEAN := TRUE,
        prev_recovery_time_in   IN schedule_sr_hist.prev_recovery_time%TYPE DEFAULT NULL,
        prev_recovery_time_nin  IN BOOLEAN := TRUE,
        id_sr_cancel_reason_in  IN schedule_sr_hist.id_sr_cancel_reason%TYPE DEFAULT NULL,
        id_sr_cancel_reason_nin IN BOOLEAN := TRUE,
        id_prof_cancel_in       IN schedule_sr_hist.id_prof_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_nin      IN BOOLEAN := TRUE,
        notes_cancel_in         IN schedule_sr_hist.notes_cancel%TYPE DEFAULT NULL,
        notes_cancel_nin        IN BOOLEAN := TRUE,
        id_prof_reg_in          IN schedule_sr_hist.id_prof_reg%TYPE DEFAULT NULL,
        id_prof_reg_nin         IN BOOLEAN := TRUE,
        id_institution_in       IN schedule_sr_hist.id_institution%TYPE DEFAULT NULL,
        id_institution_nin      IN BOOLEAN := TRUE,
        adw_last_update_in      IN schedule_sr_hist.adw_last_update%TYPE DEFAULT NULL,
        adw_last_update_nin     IN BOOLEAN := TRUE,
        dt_target_in            IN schedule_sr_hist.dt_target%TYPE DEFAULT NULL,
        dt_target_nin           IN BOOLEAN := TRUE,
        dt_interv_preview_in    IN schedule_sr_hist.dt_interv_preview%TYPE DEFAULT NULL,
        dt_interv_preview_nin   IN BOOLEAN := TRUE,
        dt_cancel_in            IN schedule_sr_hist.dt_cancel%TYPE DEFAULT NULL,
        dt_cancel_nin           IN BOOLEAN := TRUE,
        create_user_in          IN schedule_sr_hist.create_user%TYPE DEFAULT NULL,
        create_user_nin         IN BOOLEAN := TRUE,
        create_time_in          IN schedule_sr_hist.create_time%TYPE DEFAULT NULL,
        create_time_nin         IN BOOLEAN := TRUE,
        create_institution_in   IN schedule_sr_hist.create_institution%TYPE DEFAULT NULL,
        create_institution_nin  IN BOOLEAN := TRUE,
        update_user_in          IN schedule_sr_hist.update_user%TYPE DEFAULT NULL,
        update_user_nin         IN BOOLEAN := TRUE,
        update_time_in          IN schedule_sr_hist.update_time%TYPE DEFAULT NULL,
        update_time_nin         IN BOOLEAN := TRUE,
        update_institution_in   IN schedule_sr_hist.update_institution%TYPE DEFAULT NULL,
        update_institution_nin  IN BOOLEAN := TRUE,
        icu_in                  IN schedule_sr_hist.icu%TYPE DEFAULT NULL,
        icu_nin                 IN BOOLEAN := TRUE,
        notes_in                IN schedule_sr_hist.notes%TYPE DEFAULT NULL,
        notes_nin               IN BOOLEAN := TRUE,
        id_waiting_list_in      IN schedule_sr_hist.id_waiting_list%TYPE DEFAULT NULL,
        id_waiting_list_nin     IN BOOLEAN := TRUE,
        adm_needed_in           IN schedule_sr_hist.adm_needed%TYPE DEFAULT NULL,
        adm_needed_nin          IN BOOLEAN := TRUE,
        flg_temporary_in        IN schedule_sr_hist.flg_temporary%TYPE DEFAULT NULL,
        flg_temporary_nin       IN BOOLEAN := TRUE,
        flg_dur_control_in      IN schedule_sr_hist.flg_dur_control%TYPE DEFAULT NULL,
        flg_dur_control_nin     IN BOOLEAN := TRUE,
        id_diag_inst_owner_in   IN schedule_sr_hist.id_diag_inst_owner%TYPE DEFAULT NULL,
        id_diag_inst_owner_nin  IN BOOLEAN := TRUE,
        dt_update_in            IN schedule_sr_hist.dt_update%TYPE DEFAULT NULL,
        dt_update_nin           IN BOOLEAN := TRUE,
        id_prof_update_in       IN schedule_sr_hist.id_prof_update%TYPE DEFAULT NULL,
        id_prof_update_nin      IN BOOLEAN := TRUE,
        need_global_anesth_in   IN schedule_sr_hist.need_global_anesth%TYPE DEFAULT NULL,
        need_global_anesth_nin  IN BOOLEAN := TRUE,
        need_local_anesth_in    IN schedule_sr_hist.need_local_anesth%TYPE DEFAULT NULL,
        need_local_anesth_nin   IN BOOLEAN := TRUE,
        icu_pos_in              IN schedule_sr_hist.icu_pos%TYPE DEFAULT NULL,
        icu_pos_nin             IN BOOLEAN := TRUE,
        handle_error_in         IN BOOLEAN := TRUE,
        rows_out                IN OUT table_varchar
    ) IS
        l_rows_out              table_varchar;
        l_id_schedule_sr_n      NUMBER(1) := sys.diutil.bool_to_int(id_schedule_sr_nin);
        l_id_sched_sr_parent_n  NUMBER(1) := sys.diutil.bool_to_int(id_sched_sr_parent_nin);
        l_id_schedule_n         NUMBER(1) := sys.diutil.bool_to_int(id_schedule_nin);
        l_id_episode_n          NUMBER(1) := sys.diutil.bool_to_int(id_episode_nin);
        l_id_patient_n          NUMBER(1) := sys.diutil.bool_to_int(id_patient_nin);
        l_duration_n            NUMBER(1) := sys.diutil.bool_to_int(duration_nin);
        l_id_diagnosis_n        NUMBER(1) := sys.diutil.bool_to_int(id_diagnosis_nin);
        l_id_speciality_n       NUMBER(1) := sys.diutil.bool_to_int(id_speciality_nin);
        l_flg_status_n          NUMBER(1) := sys.diutil.bool_to_int(flg_status_nin);
        l_flg_sched_n           NUMBER(1) := sys.diutil.bool_to_int(flg_sched_nin);
        l_id_dept_dest_n        NUMBER(1) := sys.diutil.bool_to_int(id_dept_dest_nin);
        l_prev_recovery_time_n  NUMBER(1) := sys.diutil.bool_to_int(prev_recovery_time_nin);
        l_id_sr_cancel_reason_n NUMBER(1) := sys.diutil.bool_to_int(id_sr_cancel_reason_nin);
        l_id_prof_cancel_n      NUMBER(1) := sys.diutil.bool_to_int(id_prof_cancel_nin);
        l_notes_cancel_n        NUMBER(1) := sys.diutil.bool_to_int(notes_cancel_nin);
        l_id_prof_reg_n         NUMBER(1) := sys.diutil.bool_to_int(id_prof_reg_nin);
        l_id_institution_n      NUMBER(1) := sys.diutil.bool_to_int(id_institution_nin);
        l_adw_last_update_n     NUMBER(1) := sys.diutil.bool_to_int(adw_last_update_nin);
        l_dt_target_n           NUMBER(1) := sys.diutil.bool_to_int(dt_target_nin);
        l_dt_interv_preview_n   NUMBER(1) := sys.diutil.bool_to_int(dt_interv_preview_nin);
        l_dt_cancel_n           NUMBER(1) := sys.diutil.bool_to_int(dt_cancel_nin);
        l_create_user_n         NUMBER(1) := sys.diutil.bool_to_int(create_user_nin);
        l_create_time_n         NUMBER(1) := sys.diutil.bool_to_int(create_time_nin);
        l_create_institution_n  NUMBER(1) := sys.diutil.bool_to_int(create_institution_nin);
        l_update_user_n         NUMBER(1) := sys.diutil.bool_to_int(update_user_nin);
        l_update_time_n         NUMBER(1) := sys.diutil.bool_to_int(update_time_nin);
        l_update_institution_n  NUMBER(1) := sys.diutil.bool_to_int(update_institution_nin);
        l_icu_n                 NUMBER(1) := sys.diutil.bool_to_int(icu_nin);
        l_notes_n               NUMBER(1) := sys.diutil.bool_to_int(notes_nin);
        l_id_waiting_list_n     NUMBER(1) := sys.diutil.bool_to_int(id_waiting_list_nin);
        l_adm_needed_n          NUMBER(1) := sys.diutil.bool_to_int(adm_needed_nin);
        l_flg_temporary_n       NUMBER(1) := sys.diutil.bool_to_int(flg_temporary_nin);
        l_flg_dur_control_n     NUMBER(1) := sys.diutil.bool_to_int(flg_dur_control_nin);
        l_id_diag_inst_owner_n  NUMBER(1) := sys.diutil.bool_to_int(id_diag_inst_owner_nin);
        l_dt_update_n           NUMBER(1) := sys.diutil.bool_to_int(dt_update_nin);
        l_id_prof_update_n      NUMBER(1) := sys.diutil.bool_to_int(id_prof_update_nin);
        l_need_global_anesth_n  NUMBER(1) := sys.diutil.bool_to_int(need_global_anesth_nin);
        l_need_local_anesth_n   NUMBER(1) := sys.diutil.bool_to_int(need_local_anesth_nin);
        l_icu_pos_n             NUMBER(1) := sys.diutil.bool_to_int(icu_pos_nin);
    BEGIN
    
        UPDATE schedule_sr_hist
           SET id_schedule_sr      = decode(l_id_schedule_sr_n,
                                            0,
                                            id_schedule_sr_in,
                                            nvl(id_schedule_sr_in, id_schedule_sr)),
               id_sched_sr_parent  = decode(l_id_sched_sr_parent_n,
                                            0,
                                            id_sched_sr_parent_in,
                                            nvl(id_sched_sr_parent_in, id_sched_sr_parent)),
               id_schedule         = decode(l_id_schedule_n, 0, id_schedule_in, nvl(id_schedule_in, id_schedule)),
               id_episode          = decode(l_id_episode_n, 0, id_episode_in, nvl(id_episode_in, id_episode)),
               id_patient          = decode(l_id_patient_n, 0, id_patient_in, nvl(id_patient_in, id_patient)),
               duration            = decode(l_duration_n, 0, duration_in, nvl(duration_in, duration)),
               id_diagnosis        = decode(l_id_diagnosis_n, 0, id_diagnosis_in, nvl(id_diagnosis_in, id_diagnosis)),
               id_speciality       = decode(l_id_speciality_n, 0, id_speciality_in, nvl(id_speciality_in, id_speciality)),
               flg_status          = decode(l_flg_status_n, 0, flg_status_in, nvl(flg_status_in, flg_status)),
               flg_sched           = decode(l_flg_sched_n, 0, flg_sched_in, nvl(flg_sched_in, flg_sched)),
               id_dept_dest        = decode(l_id_dept_dest_n, 0, id_dept_dest_in, nvl(id_dept_dest_in, id_dept_dest)),
               prev_recovery_time  = decode(l_prev_recovery_time_n,
                                            0,
                                            prev_recovery_time_in,
                                            nvl(prev_recovery_time_in, prev_recovery_time)),
               id_sr_cancel_reason = decode(l_id_sr_cancel_reason_n,
                                            0,
                                            id_sr_cancel_reason_in,
                                            nvl(id_sr_cancel_reason_in, id_sr_cancel_reason)),
               id_prof_cancel      = decode(l_id_prof_cancel_n,
                                            0,
                                            id_prof_cancel_in,
                                            nvl(id_prof_cancel_in, id_prof_cancel)),
               notes_cancel        = decode(l_notes_cancel_n, 0, notes_cancel_in, nvl(notes_cancel_in, notes_cancel)),
               id_prof_reg         = decode(l_id_prof_reg_n, 0, id_prof_reg_in, nvl(id_prof_reg_in, id_prof_reg)),
               id_institution      = decode(l_id_institution_n,
                                            0,
                                            id_institution_in,
                                            nvl(id_institution_in, id_institution)),
               adw_last_update     = decode(l_adw_last_update_n,
                                            0,
                                            adw_last_update_in,
                                            nvl(adw_last_update_in, adw_last_update)),
               dt_target           = decode(l_dt_target_n, 0, dt_target_in, nvl(dt_target_in, dt_target)),
               dt_interv_preview   = decode(l_dt_interv_preview_n,
                                            0,
                                            dt_interv_preview_in,
                                            nvl(dt_interv_preview_in, dt_interv_preview)),
               dt_cancel           = decode(l_dt_cancel_n, 0, dt_cancel_in, nvl(dt_cancel_in, dt_cancel)),
               create_user         = decode(l_create_user_n, 0, create_user_in, nvl(create_user_in, create_user)),
               create_time         = decode(l_create_time_n, 0, create_time_in, nvl(create_time_in, create_time)),
               create_institution  = decode(l_create_institution_n,
                                            0,
                                            create_institution_in,
                                            nvl(create_institution_in, create_institution)),
               update_user         = decode(l_update_user_n, 0, update_user_in, nvl(update_user_in, update_user)),
               update_time         = decode(l_update_time_n, 0, update_time_in, nvl(update_time_in, update_time)),
               update_institution  = decode(l_update_institution_n,
                                            0,
                                            update_institution_in,
                                            nvl(update_institution_in, update_institution)),
               icu                 = decode(l_icu_n, 0, icu_in, nvl(icu_in, icu)),
               notes               = decode(l_notes_n, 0, notes_in, nvl(notes_in, notes)),
               id_waiting_list     = decode(l_id_waiting_list_n,
                                            0,
                                            id_waiting_list_in,
                                            nvl(id_waiting_list_in, id_waiting_list)),
               adm_needed          = decode(l_adm_needed_n, 0, adm_needed_in, nvl(adm_needed_in, adm_needed)),
               flg_temporary       = decode(l_flg_temporary_n, 0, flg_temporary_in, nvl(flg_temporary_in, flg_temporary)),
               flg_dur_control     = decode(l_flg_dur_control_n,
                                            0,
                                            flg_dur_control_in,
                                            nvl(flg_dur_control_in, flg_dur_control)),
               id_diag_inst_owner  = decode(l_id_diag_inst_owner_n,
                                            0,
                                            id_diag_inst_owner_in,
                                            nvl(id_diag_inst_owner_in, id_diag_inst_owner)),
               dt_update           = decode(l_dt_update_n, 0, dt_update_in, nvl(dt_update_in, dt_update)),
               id_prof_update      = decode(l_id_prof_update_n,
                                            0,
                                            id_prof_update_in,
                                            nvl(id_prof_update_in, id_prof_update)),
               need_global_anesth  = decode(l_need_global_anesth_n,
                                            0,
                                            need_global_anesth_in,
                                            nvl(need_global_anesth_in, need_global_anesth)),
               need_local_anesth   = decode(l_need_local_anesth_n,
                                            0,
                                            need_local_anesth_in,
                                            nvl(need_local_anesth_in, need_local_anesth)),
               icu_pos             = decode(l_icu_pos_n, 0, icu_pos_in, nvl(icu_pos_in, icu_pos))
        RETURNING ROWID BULK COLLECT INTO l_rows_out;
        IF (rows_out IS NULL)
        THEN
            rows_out := table_varchar();
        END IF;
        rows_out := rows_out MULTISET UNION DISTINCT l_rows_out;
    
    EXCEPTION
        WHEN e_null_column_value
             OR e_existing_fky_reference
             OR e_check_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found
             OR e_forall_error
             OR e_integ_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                pk_alertlog.log_info('DML error ignored: ' || SQLCODE || ': ' || SQLERRM);
            END IF;
        
    END upd;

    -- Update any/all columns by primary key. If you pass NULL, then
    -- the current column value is set to itself. If you need a more
    -- selected UPDATE then use one of the onecol procedures below.
    PROCEDURE upd
    (
        id_schedule_sr_in       IN schedule_sr_hist.id_schedule_sr%TYPE DEFAULT NULL,
        id_schedule_sr_nin      IN BOOLEAN := TRUE,
        id_sched_sr_parent_in   IN schedule_sr_hist.id_sched_sr_parent%TYPE DEFAULT NULL,
        id_sched_sr_parent_nin  IN BOOLEAN := TRUE,
        id_schedule_in          IN schedule_sr_hist.id_schedule%TYPE DEFAULT NULL,
        id_schedule_nin         IN BOOLEAN := TRUE,
        id_episode_in           IN schedule_sr_hist.id_episode%TYPE DEFAULT NULL,
        id_episode_nin          IN BOOLEAN := TRUE,
        id_patient_in           IN schedule_sr_hist.id_patient%TYPE DEFAULT NULL,
        id_patient_nin          IN BOOLEAN := TRUE,
        duration_in             IN schedule_sr_hist.duration%TYPE DEFAULT NULL,
        duration_nin            IN BOOLEAN := TRUE,
        id_diagnosis_in         IN schedule_sr_hist.id_diagnosis%TYPE DEFAULT NULL,
        id_diagnosis_nin        IN BOOLEAN := TRUE,
        id_speciality_in        IN schedule_sr_hist.id_speciality%TYPE DEFAULT NULL,
        id_speciality_nin       IN BOOLEAN := TRUE,
        flg_status_in           IN schedule_sr_hist.flg_status%TYPE DEFAULT NULL,
        flg_status_nin          IN BOOLEAN := TRUE,
        flg_sched_in            IN schedule_sr_hist.flg_sched%TYPE DEFAULT NULL,
        flg_sched_nin           IN BOOLEAN := TRUE,
        id_dept_dest_in         IN schedule_sr_hist.id_dept_dest%TYPE DEFAULT NULL,
        id_dept_dest_nin        IN BOOLEAN := TRUE,
        prev_recovery_time_in   IN schedule_sr_hist.prev_recovery_time%TYPE DEFAULT NULL,
        prev_recovery_time_nin  IN BOOLEAN := TRUE,
        id_sr_cancel_reason_in  IN schedule_sr_hist.id_sr_cancel_reason%TYPE DEFAULT NULL,
        id_sr_cancel_reason_nin IN BOOLEAN := TRUE,
        id_prof_cancel_in       IN schedule_sr_hist.id_prof_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_nin      IN BOOLEAN := TRUE,
        notes_cancel_in         IN schedule_sr_hist.notes_cancel%TYPE DEFAULT NULL,
        notes_cancel_nin        IN BOOLEAN := TRUE,
        id_prof_reg_in          IN schedule_sr_hist.id_prof_reg%TYPE DEFAULT NULL,
        id_prof_reg_nin         IN BOOLEAN := TRUE,
        id_institution_in       IN schedule_sr_hist.id_institution%TYPE DEFAULT NULL,
        id_institution_nin      IN BOOLEAN := TRUE,
        adw_last_update_in      IN schedule_sr_hist.adw_last_update%TYPE DEFAULT NULL,
        adw_last_update_nin     IN BOOLEAN := TRUE,
        dt_target_in            IN schedule_sr_hist.dt_target%TYPE DEFAULT NULL,
        dt_target_nin           IN BOOLEAN := TRUE,
        dt_interv_preview_in    IN schedule_sr_hist.dt_interv_preview%TYPE DEFAULT NULL,
        dt_interv_preview_nin   IN BOOLEAN := TRUE,
        dt_cancel_in            IN schedule_sr_hist.dt_cancel%TYPE DEFAULT NULL,
        dt_cancel_nin           IN BOOLEAN := TRUE,
        create_user_in          IN schedule_sr_hist.create_user%TYPE DEFAULT NULL,
        create_user_nin         IN BOOLEAN := TRUE,
        create_time_in          IN schedule_sr_hist.create_time%TYPE DEFAULT NULL,
        create_time_nin         IN BOOLEAN := TRUE,
        create_institution_in   IN schedule_sr_hist.create_institution%TYPE DEFAULT NULL,
        create_institution_nin  IN BOOLEAN := TRUE,
        update_user_in          IN schedule_sr_hist.update_user%TYPE DEFAULT NULL,
        update_user_nin         IN BOOLEAN := TRUE,
        update_time_in          IN schedule_sr_hist.update_time%TYPE DEFAULT NULL,
        update_time_nin         IN BOOLEAN := TRUE,
        update_institution_in   IN schedule_sr_hist.update_institution%TYPE DEFAULT NULL,
        update_institution_nin  IN BOOLEAN := TRUE,
        icu_in                  IN schedule_sr_hist.icu%TYPE DEFAULT NULL,
        icu_nin                 IN BOOLEAN := TRUE,
        notes_in                IN schedule_sr_hist.notes%TYPE DEFAULT NULL,
        notes_nin               IN BOOLEAN := TRUE,
        id_waiting_list_in      IN schedule_sr_hist.id_waiting_list%TYPE DEFAULT NULL,
        id_waiting_list_nin     IN BOOLEAN := TRUE,
        adm_needed_in           IN schedule_sr_hist.adm_needed%TYPE DEFAULT NULL,
        adm_needed_nin          IN BOOLEAN := TRUE,
        flg_temporary_in        IN schedule_sr_hist.flg_temporary%TYPE DEFAULT NULL,
        flg_temporary_nin       IN BOOLEAN := TRUE,
        flg_dur_control_in      IN schedule_sr_hist.flg_dur_control%TYPE DEFAULT NULL,
        flg_dur_control_nin     IN BOOLEAN := TRUE,
        id_diag_inst_owner_in   IN schedule_sr_hist.id_diag_inst_owner%TYPE DEFAULT NULL,
        id_diag_inst_owner_nin  IN BOOLEAN := TRUE,
        dt_update_in            IN schedule_sr_hist.dt_update%TYPE DEFAULT NULL,
        dt_update_nin           IN BOOLEAN := TRUE,
        id_prof_update_in       IN schedule_sr_hist.id_prof_update%TYPE DEFAULT NULL,
        id_prof_update_nin      IN BOOLEAN := TRUE,
        need_global_anesth_in   IN schedule_sr_hist.need_global_anesth%TYPE DEFAULT NULL,
        need_global_anesth_nin  IN BOOLEAN := TRUE,
        need_local_anesth_in    IN schedule_sr_hist.need_local_anesth%TYPE DEFAULT NULL,
        need_local_anesth_nin   IN BOOLEAN := TRUE,
        icu_pos_in              IN schedule_sr_hist.icu_pos%TYPE DEFAULT NULL,
        icu_pos_nin             IN BOOLEAN := TRUE,
        handle_error_in         IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        upd(id_schedule_sr_in       => id_schedule_sr_in,
            id_schedule_sr_nin      => id_schedule_sr_nin,
            id_sched_sr_parent_in   => id_sched_sr_parent_in,
            id_sched_sr_parent_nin  => id_sched_sr_parent_nin,
            id_schedule_in          => id_schedule_in,
            id_schedule_nin         => id_schedule_nin,
            id_episode_in           => id_episode_in,
            id_episode_nin          => id_episode_nin,
            id_patient_in           => id_patient_in,
            id_patient_nin          => id_patient_nin,
            duration_in             => duration_in,
            duration_nin            => duration_nin,
            id_diagnosis_in         => id_diagnosis_in,
            id_diagnosis_nin        => id_diagnosis_nin,
            id_speciality_in        => id_speciality_in,
            id_speciality_nin       => id_speciality_nin,
            flg_status_in           => flg_status_in,
            flg_status_nin          => flg_status_nin,
            flg_sched_in            => flg_sched_in,
            flg_sched_nin           => flg_sched_nin,
            id_dept_dest_in         => id_dept_dest_in,
            id_dept_dest_nin        => id_dept_dest_nin,
            prev_recovery_time_in   => prev_recovery_time_in,
            prev_recovery_time_nin  => prev_recovery_time_nin,
            id_sr_cancel_reason_in  => id_sr_cancel_reason_in,
            id_sr_cancel_reason_nin => id_sr_cancel_reason_nin,
            id_prof_cancel_in       => id_prof_cancel_in,
            id_prof_cancel_nin      => id_prof_cancel_nin,
            notes_cancel_in         => notes_cancel_in,
            notes_cancel_nin        => notes_cancel_nin,
            id_prof_reg_in          => id_prof_reg_in,
            id_prof_reg_nin         => id_prof_reg_nin,
            id_institution_in       => id_institution_in,
            id_institution_nin      => id_institution_nin,
            adw_last_update_in      => adw_last_update_in,
            adw_last_update_nin     => adw_last_update_nin,
            dt_target_in            => dt_target_in,
            dt_target_nin           => dt_target_nin,
            dt_interv_preview_in    => dt_interv_preview_in,
            dt_interv_preview_nin   => dt_interv_preview_nin,
            dt_cancel_in            => dt_cancel_in,
            dt_cancel_nin           => dt_cancel_nin,
            create_user_in          => create_user_in,
            create_user_nin         => create_user_nin,
            create_time_in          => create_time_in,
            create_time_nin         => create_time_nin,
            create_institution_in   => create_institution_in,
            create_institution_nin  => create_institution_nin,
            update_user_in          => update_user_in,
            update_user_nin         => update_user_nin,
            update_time_in          => update_time_in,
            update_time_nin         => update_time_nin,
            update_institution_in   => update_institution_in,
            update_institution_nin  => update_institution_nin,
            icu_in                  => icu_in,
            icu_nin                 => icu_nin,
            notes_in                => notes_in,
            notes_nin               => notes_nin,
            id_waiting_list_in      => id_waiting_list_in,
            id_waiting_list_nin     => id_waiting_list_nin,
            adm_needed_in           => adm_needed_in,
            adm_needed_nin          => adm_needed_nin,
            flg_temporary_in        => flg_temporary_in,
            flg_temporary_nin       => flg_temporary_nin,
            flg_dur_control_in      => flg_dur_control_in,
            flg_dur_control_nin     => flg_dur_control_nin,
            id_diag_inst_owner_in   => id_diag_inst_owner_in,
            id_diag_inst_owner_nin  => id_diag_inst_owner_nin,
            dt_update_in            => dt_update_in,
            dt_update_nin           => dt_update_nin,
            id_prof_update_in       => id_prof_update_in,
            id_prof_update_nin      => id_prof_update_nin,
            need_global_anesth_in   => need_global_anesth_in,
            need_global_anesth_nin  => need_global_anesth_nin,
            need_local_anesth_in    => need_local_anesth_in,
            need_local_anesth_nin   => need_local_anesth_nin,
            icu_pos_in              => icu_pos_in,
            icu_pos_nin             => icu_pos_nin,
            handle_error_in         => handle_error_in,
            rows_out                => rows_out);
    END upd;

    --Update any/all columns by dynamic WHERE
    -- If you pass NULL, then the current column value is set to itself
    PROCEDURE upd
    (
        id_schedule_sr_in       IN schedule_sr_hist.id_schedule_sr%TYPE DEFAULT NULL,
        id_schedule_sr_nin      IN BOOLEAN := TRUE,
        id_sched_sr_parent_in   IN schedule_sr_hist.id_sched_sr_parent%TYPE DEFAULT NULL,
        id_sched_sr_parent_nin  IN BOOLEAN := TRUE,
        id_schedule_in          IN schedule_sr_hist.id_schedule%TYPE DEFAULT NULL,
        id_schedule_nin         IN BOOLEAN := TRUE,
        id_episode_in           IN schedule_sr_hist.id_episode%TYPE DEFAULT NULL,
        id_episode_nin          IN BOOLEAN := TRUE,
        id_patient_in           IN schedule_sr_hist.id_patient%TYPE DEFAULT NULL,
        id_patient_nin          IN BOOLEAN := TRUE,
        duration_in             IN schedule_sr_hist.duration%TYPE DEFAULT NULL,
        duration_nin            IN BOOLEAN := TRUE,
        id_diagnosis_in         IN schedule_sr_hist.id_diagnosis%TYPE DEFAULT NULL,
        id_diagnosis_nin        IN BOOLEAN := TRUE,
        id_speciality_in        IN schedule_sr_hist.id_speciality%TYPE DEFAULT NULL,
        id_speciality_nin       IN BOOLEAN := TRUE,
        flg_status_in           IN schedule_sr_hist.flg_status%TYPE DEFAULT NULL,
        flg_status_nin          IN BOOLEAN := TRUE,
        flg_sched_in            IN schedule_sr_hist.flg_sched%TYPE DEFAULT NULL,
        flg_sched_nin           IN BOOLEAN := TRUE,
        id_dept_dest_in         IN schedule_sr_hist.id_dept_dest%TYPE DEFAULT NULL,
        id_dept_dest_nin        IN BOOLEAN := TRUE,
        prev_recovery_time_in   IN schedule_sr_hist.prev_recovery_time%TYPE DEFAULT NULL,
        prev_recovery_time_nin  IN BOOLEAN := TRUE,
        id_sr_cancel_reason_in  IN schedule_sr_hist.id_sr_cancel_reason%TYPE DEFAULT NULL,
        id_sr_cancel_reason_nin IN BOOLEAN := TRUE,
        id_prof_cancel_in       IN schedule_sr_hist.id_prof_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_nin      IN BOOLEAN := TRUE,
        notes_cancel_in         IN schedule_sr_hist.notes_cancel%TYPE DEFAULT NULL,
        notes_cancel_nin        IN BOOLEAN := TRUE,
        id_prof_reg_in          IN schedule_sr_hist.id_prof_reg%TYPE DEFAULT NULL,
        id_prof_reg_nin         IN BOOLEAN := TRUE,
        id_institution_in       IN schedule_sr_hist.id_institution%TYPE DEFAULT NULL,
        id_institution_nin      IN BOOLEAN := TRUE,
        adw_last_update_in      IN schedule_sr_hist.adw_last_update%TYPE DEFAULT NULL,
        adw_last_update_nin     IN BOOLEAN := TRUE,
        dt_target_in            IN schedule_sr_hist.dt_target%TYPE DEFAULT NULL,
        dt_target_nin           IN BOOLEAN := TRUE,
        dt_interv_preview_in    IN schedule_sr_hist.dt_interv_preview%TYPE DEFAULT NULL,
        dt_interv_preview_nin   IN BOOLEAN := TRUE,
        dt_cancel_in            IN schedule_sr_hist.dt_cancel%TYPE DEFAULT NULL,
        dt_cancel_nin           IN BOOLEAN := TRUE,
        create_user_in          IN schedule_sr_hist.create_user%TYPE DEFAULT NULL,
        create_user_nin         IN BOOLEAN := TRUE,
        create_time_in          IN schedule_sr_hist.create_time%TYPE DEFAULT NULL,
        create_time_nin         IN BOOLEAN := TRUE,
        create_institution_in   IN schedule_sr_hist.create_institution%TYPE DEFAULT NULL,
        create_institution_nin  IN BOOLEAN := TRUE,
        update_user_in          IN schedule_sr_hist.update_user%TYPE DEFAULT NULL,
        update_user_nin         IN BOOLEAN := TRUE,
        update_time_in          IN schedule_sr_hist.update_time%TYPE DEFAULT NULL,
        update_time_nin         IN BOOLEAN := TRUE,
        update_institution_in   IN schedule_sr_hist.update_institution%TYPE DEFAULT NULL,
        update_institution_nin  IN BOOLEAN := TRUE,
        icu_in                  IN schedule_sr_hist.icu%TYPE DEFAULT NULL,
        icu_nin                 IN BOOLEAN := TRUE,
        notes_in                IN schedule_sr_hist.notes%TYPE DEFAULT NULL,
        notes_nin               IN BOOLEAN := TRUE,
        id_waiting_list_in      IN schedule_sr_hist.id_waiting_list%TYPE DEFAULT NULL,
        id_waiting_list_nin     IN BOOLEAN := TRUE,
        adm_needed_in           IN schedule_sr_hist.adm_needed%TYPE DEFAULT NULL,
        adm_needed_nin          IN BOOLEAN := TRUE,
        flg_temporary_in        IN schedule_sr_hist.flg_temporary%TYPE DEFAULT NULL,
        flg_temporary_nin       IN BOOLEAN := TRUE,
        flg_dur_control_in      IN schedule_sr_hist.flg_dur_control%TYPE DEFAULT NULL,
        flg_dur_control_nin     IN BOOLEAN := TRUE,
        id_diag_inst_owner_in   IN schedule_sr_hist.id_diag_inst_owner%TYPE DEFAULT NULL,
        id_diag_inst_owner_nin  IN BOOLEAN := TRUE,
        dt_update_in            IN schedule_sr_hist.dt_update%TYPE DEFAULT NULL,
        dt_update_nin           IN BOOLEAN := TRUE,
        id_prof_update_in       IN schedule_sr_hist.id_prof_update%TYPE DEFAULT NULL,
        id_prof_update_nin      IN BOOLEAN := TRUE,
        need_global_anesth_in   IN schedule_sr_hist.need_global_anesth%TYPE DEFAULT NULL,
        need_global_anesth_nin  IN BOOLEAN := TRUE,
        need_local_anesth_in    IN schedule_sr_hist.need_local_anesth%TYPE DEFAULT NULL,
        need_local_anesth_nin   IN BOOLEAN := TRUE,
        icu_pos_in              IN schedule_sr_hist.icu_pos%TYPE DEFAULT NULL,
        icu_pos_nin             IN BOOLEAN := TRUE,
        where_in                IN VARCHAR2,
        handle_error_in         IN BOOLEAN := TRUE,
        rows_out                IN OUT table_varchar
    ) IS
        l_sql                   VARCHAR2(32767);
        l_rows_out              table_varchar;
        l_id_schedule_sr_n      NUMBER(1) := sys.diutil.bool_to_int(id_schedule_sr_nin);
        l_id_sched_sr_parent_n  NUMBER(1) := sys.diutil.bool_to_int(id_sched_sr_parent_nin);
        l_id_schedule_n         NUMBER(1) := sys.diutil.bool_to_int(id_schedule_nin);
        l_id_episode_n          NUMBER(1) := sys.diutil.bool_to_int(id_episode_nin);
        l_id_patient_n          NUMBER(1) := sys.diutil.bool_to_int(id_patient_nin);
        l_duration_n            NUMBER(1) := sys.diutil.bool_to_int(duration_nin);
        l_id_diagnosis_n        NUMBER(1) := sys.diutil.bool_to_int(id_diagnosis_nin);
        l_id_speciality_n       NUMBER(1) := sys.diutil.bool_to_int(id_speciality_nin);
        l_flg_status_n          NUMBER(1) := sys.diutil.bool_to_int(flg_status_nin);
        l_flg_sched_n           NUMBER(1) := sys.diutil.bool_to_int(flg_sched_nin);
        l_id_dept_dest_n        NUMBER(1) := sys.diutil.bool_to_int(id_dept_dest_nin);
        l_prev_recovery_time_n  NUMBER(1) := sys.diutil.bool_to_int(prev_recovery_time_nin);
        l_id_sr_cancel_reason_n NUMBER(1) := sys.diutil.bool_to_int(id_sr_cancel_reason_nin);
        l_id_prof_cancel_n      NUMBER(1) := sys.diutil.bool_to_int(id_prof_cancel_nin);
        l_notes_cancel_n        NUMBER(1) := sys.diutil.bool_to_int(notes_cancel_nin);
        l_id_prof_reg_n         NUMBER(1) := sys.diutil.bool_to_int(id_prof_reg_nin);
        l_id_institution_n      NUMBER(1) := sys.diutil.bool_to_int(id_institution_nin);
        l_adw_last_update_n     NUMBER(1) := sys.diutil.bool_to_int(adw_last_update_nin);
        l_dt_target_n           NUMBER(1) := sys.diutil.bool_to_int(dt_target_nin);
        l_dt_interv_preview_n   NUMBER(1) := sys.diutil.bool_to_int(dt_interv_preview_nin);
        l_dt_cancel_n           NUMBER(1) := sys.diutil.bool_to_int(dt_cancel_nin);
        l_create_user_n         NUMBER(1) := sys.diutil.bool_to_int(create_user_nin);
        l_create_time_n         NUMBER(1) := sys.diutil.bool_to_int(create_time_nin);
        l_create_institution_n  NUMBER(1) := sys.diutil.bool_to_int(create_institution_nin);
        l_update_user_n         NUMBER(1) := sys.diutil.bool_to_int(update_user_nin);
        l_update_time_n         NUMBER(1) := sys.diutil.bool_to_int(update_time_nin);
        l_update_institution_n  NUMBER(1) := sys.diutil.bool_to_int(update_institution_nin);
        l_icu_n                 NUMBER(1) := sys.diutil.bool_to_int(icu_nin);
        l_notes_n               NUMBER(1) := sys.diutil.bool_to_int(notes_nin);
        l_id_waiting_list_n     NUMBER(1) := sys.diutil.bool_to_int(id_waiting_list_nin);
        l_adm_needed_n          NUMBER(1) := sys.diutil.bool_to_int(adm_needed_nin);
        l_flg_temporary_n       NUMBER(1) := sys.diutil.bool_to_int(flg_temporary_nin);
        l_flg_dur_control_n     NUMBER(1) := sys.diutil.bool_to_int(flg_dur_control_nin);
        l_id_diag_inst_owner_n  NUMBER(1) := sys.diutil.bool_to_int(id_diag_inst_owner_nin);
        l_dt_update_n           NUMBER(1) := sys.diutil.bool_to_int(dt_update_nin);
        l_id_prof_update_n      NUMBER(1) := sys.diutil.bool_to_int(id_prof_update_nin);
        l_need_global_anesth_n  NUMBER(1) := sys.diutil.bool_to_int(need_global_anesth_nin);
        l_need_local_anesth_n   NUMBER(1) := sys.diutil.bool_to_int(need_local_anesth_nin);
        l_icu_pos_n             NUMBER(1) := sys.diutil.bool_to_int(icu_pos_nin);
    BEGIN
        l_id_schedule_sr_n      := sys.diutil.bool_to_int(id_schedule_sr_nin);
        l_id_sched_sr_parent_n  := sys.diutil.bool_to_int(id_sched_sr_parent_nin);
        l_id_schedule_n         := sys.diutil.bool_to_int(id_schedule_nin);
        l_id_episode_n          := sys.diutil.bool_to_int(id_episode_nin);
        l_id_patient_n          := sys.diutil.bool_to_int(id_patient_nin);
        l_duration_n            := sys.diutil.bool_to_int(duration_nin);
        l_id_diagnosis_n        := sys.diutil.bool_to_int(id_diagnosis_nin);
        l_id_speciality_n       := sys.diutil.bool_to_int(id_speciality_nin);
        l_flg_status_n          := sys.diutil.bool_to_int(flg_status_nin);
        l_flg_sched_n           := sys.diutil.bool_to_int(flg_sched_nin);
        l_id_dept_dest_n        := sys.diutil.bool_to_int(id_dept_dest_nin);
        l_prev_recovery_time_n  := sys.diutil.bool_to_int(prev_recovery_time_nin);
        l_id_sr_cancel_reason_n := sys.diutil.bool_to_int(id_sr_cancel_reason_nin);
        l_id_prof_cancel_n      := sys.diutil.bool_to_int(id_prof_cancel_nin);
        l_notes_cancel_n        := sys.diutil.bool_to_int(notes_cancel_nin);
        l_id_prof_reg_n         := sys.diutil.bool_to_int(id_prof_reg_nin);
        l_id_institution_n      := sys.diutil.bool_to_int(id_institution_nin);
        l_adw_last_update_n     := sys.diutil.bool_to_int(adw_last_update_nin);
        l_dt_target_n           := sys.diutil.bool_to_int(dt_target_nin);
        l_dt_interv_preview_n   := sys.diutil.bool_to_int(dt_interv_preview_nin);
        l_dt_cancel_n           := sys.diutil.bool_to_int(dt_cancel_nin);
        l_create_user_n         := sys.diutil.bool_to_int(create_user_nin);
        l_create_time_n         := sys.diutil.bool_to_int(create_time_nin);
        l_create_institution_n  := sys.diutil.bool_to_int(create_institution_nin);
        l_update_user_n         := sys.diutil.bool_to_int(update_user_nin);
        l_update_time_n         := sys.diutil.bool_to_int(update_time_nin);
        l_update_institution_n  := sys.diutil.bool_to_int(update_institution_nin);
        l_icu_n                 := sys.diutil.bool_to_int(icu_nin);
        l_notes_n               := sys.diutil.bool_to_int(notes_nin);
        l_id_waiting_list_n     := sys.diutil.bool_to_int(id_waiting_list_nin);
        l_adm_needed_n          := sys.diutil.bool_to_int(adm_needed_nin);
        l_flg_temporary_n       := sys.diutil.bool_to_int(flg_temporary_nin);
        l_flg_dur_control_n     := sys.diutil.bool_to_int(flg_dur_control_nin);
        l_id_diag_inst_owner_n  := sys.diutil.bool_to_int(id_diag_inst_owner_nin);
        l_dt_update_n           := sys.diutil.bool_to_int(dt_update_nin);
        l_id_prof_update_n      := sys.diutil.bool_to_int(id_prof_update_nin);
        l_need_global_anesth_n  := sys.diutil.bool_to_int(need_global_anesth_nin);
        l_need_local_anesth_n   := sys.diutil.bool_to_int(need_local_anesth_nin);
        l_icu_pos_n             := sys.diutil.bool_to_int(icu_pos_nin);
        l_sql                   := 'UPDATE schedule_sr_hist SET' || ' ID_SCHEDULE_SR = decode (' || l_id_schedule_sr_n ||
                                   ',0,:ID_SCHEDULE_SR_in, NVL (:ID_SCHEDULE_SR_in, ID_SCHEDULE_SR)) ' || ',' ||
                                   ' ID_SCHED_SR_PARENT = decode (' || l_id_sched_sr_parent_n ||
                                   ',0,:ID_SCHED_SR_PARENT_in, NVL (:ID_SCHED_SR_PARENT_in, ID_SCHED_SR_PARENT)) ' || ',' ||
                                   ' ID_SCHEDULE = decode (' || l_id_schedule_n ||
                                   ',0,:ID_SCHEDULE_in, NVL (:ID_SCHEDULE_in, ID_SCHEDULE)) ' || ',' ||
                                   ' ID_EPISODE = decode (' || l_id_episode_n ||
                                   ',0,:ID_EPISODE_in, NVL (:ID_EPISODE_in, ID_EPISODE)) ' || ',' ||
                                   ' ID_PATIENT = decode (' || l_id_patient_n ||
                                   ',0,:ID_PATIENT_in, NVL (:ID_PATIENT_in, ID_PATIENT)) ' || ',' ||
                                   ' DURATION = decode (' || l_duration_n ||
                                   ',0,:DURATION_in, NVL (:DURATION_in, DURATION)) ' || ',' ||
                                   ' ID_DIAGNOSIS = decode (' || l_id_diagnosis_n ||
                                   ',0,:ID_DIAGNOSIS_in, NVL (:ID_DIAGNOSIS_in, ID_DIAGNOSIS)) ' || ',' ||
                                   ' ID_SPECIALITY = decode (' || l_id_speciality_n ||
                                   ',0,:ID_SPECIALITY_in, NVL (:ID_SPECIALITY_in, ID_SPECIALITY)) ' || ',' ||
                                   ' FLG_STATUS = decode (' || l_flg_status_n ||
                                   ',0,:FLG_STATUS_in, NVL (:FLG_STATUS_in, FLG_STATUS)) ' || ',' ||
                                   ' FLG_SCHED = decode (' || l_flg_sched_n ||
                                   ',0,:FLG_SCHED_in, NVL (:FLG_SCHED_in, FLG_SCHED)) ' || ',' ||
                                   ' ID_DEPT_DEST = decode (' || l_id_dept_dest_n ||
                                   ',0,:ID_DEPT_DEST_in, NVL (:ID_DEPT_DEST_in, ID_DEPT_DEST)) ' || ',' ||
                                   ' PREV_RECOVERY_TIME = decode (' || l_prev_recovery_time_n ||
                                   ',0,:PREV_RECOVERY_TIME_in, NVL (:PREV_RECOVERY_TIME_in, PREV_RECOVERY_TIME)) ' || ',' ||
                                   ' ID_SR_CANCEL_REASON = decode (' || l_id_sr_cancel_reason_n ||
                                   ',0,:ID_SR_CANCEL_REASON_in, NVL (:ID_SR_CANCEL_REASON_in, ID_SR_CANCEL_REASON)) ' || ',' ||
                                   ' ID_PROF_CANCEL = decode (' || l_id_prof_cancel_n ||
                                   ',0,:ID_PROF_CANCEL_in, NVL (:ID_PROF_CANCEL_in, ID_PROF_CANCEL)) ' || ',' ||
                                   ' NOTES_CANCEL = decode (' || l_notes_cancel_n ||
                                   ',0,:NOTES_CANCEL_in, NVL (:NOTES_CANCEL_in, NOTES_CANCEL)) ' || ',' ||
                                   ' ID_PROF_REG = decode (' || l_id_prof_reg_n ||
                                   ',0,:ID_PROF_REG_in, NVL (:ID_PROF_REG_in, ID_PROF_REG)) ' || ',' ||
                                   ' ID_INSTITUTION = decode (' || l_id_institution_n ||
                                   ',0,:ID_INSTITUTION_in, NVL (:ID_INSTITUTION_in, ID_INSTITUTION)) ' || ',' ||
                                   ' ADW_LAST_UPDATE = decode (' || l_adw_last_update_n ||
                                   ',0,:ADW_LAST_UPDATE_in, NVL (:ADW_LAST_UPDATE_in, ADW_LAST_UPDATE)) ' || ',' ||
                                   ' DT_TARGET = decode (' || l_dt_target_n ||
                                   ',0,:DT_TARGET_in, NVL (:DT_TARGET_in, DT_TARGET)) ' || ',' ||
                                   ' DT_INTERV_PREVIEW = decode (' || l_dt_interv_preview_n ||
                                   ',0,:DT_INTERV_PREVIEW_in, NVL (:DT_INTERV_PREVIEW_in, DT_INTERV_PREVIEW)) ' || ',' ||
                                   ' DT_CANCEL = decode (' || l_dt_cancel_n ||
                                   ',0,:DT_CANCEL_in, NVL (:DT_CANCEL_in, DT_CANCEL)) ' || ',' ||
                                   ' CREATE_USER = decode (' || l_create_user_n ||
                                   ',0,:CREATE_USER_in, NVL (:CREATE_USER_in, CREATE_USER)) ' || ',' ||
                                   ' CREATE_TIME = decode (' || l_create_time_n ||
                                   ',0,:CREATE_TIME_in, NVL (:CREATE_TIME_in, CREATE_TIME)) ' || ',' ||
                                   ' CREATE_INSTITUTION = decode (' || l_create_institution_n ||
                                   ',0,:CREATE_INSTITUTION_in, NVL (:CREATE_INSTITUTION_in, CREATE_INSTITUTION)) ' || ',' ||
                                   ' UPDATE_USER = decode (' || l_update_user_n ||
                                   ',0,:UPDATE_USER_in, NVL (:UPDATE_USER_in, UPDATE_USER)) ' || ',' ||
                                   ' UPDATE_TIME = decode (' || l_update_time_n ||
                                   ',0,:UPDATE_TIME_in, NVL (:UPDATE_TIME_in, UPDATE_TIME)) ' || ',' ||
                                   ' UPDATE_INSTITUTION = decode (' || l_update_institution_n ||
                                   ',0,:UPDATE_INSTITUTION_in, NVL (:UPDATE_INSTITUTION_in, UPDATE_INSTITUTION)) ' || ',' ||
                                   ' ICU = decode (' || l_icu_n || ',0,:ICU_in, NVL (:ICU_in, ICU)) ' || ',' ||
                                   ' NOTES = decode (' || l_notes_n || ',0,:NOTES_in, NVL (:NOTES_in, NOTES)) ' || ',' ||
                                   ' ID_WAITING_LIST = decode (' || l_id_waiting_list_n ||
                                   ',0,:ID_WAITING_LIST_in, NVL (:ID_WAITING_LIST_in, ID_WAITING_LIST)) ' || ',' ||
                                   ' ADM_NEEDED = decode (' || l_adm_needed_n ||
                                   ',0,:ADM_NEEDED_in, NVL (:ADM_NEEDED_in, ADM_NEEDED)) ' || ',' ||
                                   ' FLG_TEMPORARY = decode (' || l_flg_temporary_n ||
                                   ',0,:FLG_TEMPORARY_in, NVL (:FLG_TEMPORARY_in, FLG_TEMPORARY)) ' || ',' ||
                                   ' FLG_DUR_CONTROL = decode (' || l_flg_dur_control_n ||
                                   ',0,:FLG_DUR_CONTROL_in, NVL (:FLG_DUR_CONTROL_in, FLG_DUR_CONTROL)) ' || ',' ||
                                   ' ID_DIAG_INST_OWNER = decode (' || l_id_diag_inst_owner_n ||
                                   ',0,:ID_DIAG_INST_OWNER_in, NVL (:ID_DIAG_INST_OWNER_in, ID_DIAG_INST_OWNER)) ' || ',' ||
                                   ' DT_UPDATE = decode (' || l_dt_update_n ||
                                   ',0,:DT_UPDATE_in, NVL (:DT_UPDATE_in, DT_UPDATE)) ' || ',' ||
                                   ' ID_PROF_UPDATE = decode (' || l_id_prof_update_n ||
                                   ',0,:ID_PROF_UPDATE_in, NVL (:ID_PROF_UPDATE_in, ID_PROF_UPDATE)) ' || ',' ||
                                   ' NEED_GLOBAL_ANESTH = decode (' || l_need_global_anesth_n ||
                                   ',0,:NEED_GLOBAL_ANESTH_in, NVL (:NEED_GLOBAL_ANESTH_in, NEED_GLOBAL_ANESTH)) ' || ',' ||
                                   ' NEED_LOCAL_ANESTH = decode (' || l_need_local_anesth_n ||
                                   ',0,:NEED_LOCAL_ANESTH_in, NVL (:NEED_LOCAL_ANESTH_in, NEED_LOCAL_ANESTH)) ' || ',' ||
                                   ' ICU_POS = decode (' || l_icu_pos_n ||
                                   ',0,:ICU_POS_in, NVL (:ICU_POS_in, ICU_POS)) ' || ' where ' ||
                                   nvl(where_in, '(1=1)') || ' RETURNING ROWID BULK COLLECT INTO :l_rows_out';
        EXECUTE IMMEDIATE 'BEGIN ' || l_sql || '; END;'
            USING IN id_schedule_sr_in, id_sched_sr_parent_in, id_schedule_in, id_episode_in, id_patient_in, duration_in, id_diagnosis_in, id_speciality_in, flg_status_in, flg_sched_in, id_dept_dest_in, prev_recovery_time_in, id_sr_cancel_reason_in, id_prof_cancel_in, notes_cancel_in, id_prof_reg_in, id_institution_in, adw_last_update_in, dt_target_in, dt_interv_preview_in, dt_cancel_in, create_user_in, create_time_in, create_institution_in, update_user_in, update_time_in, update_institution_in, icu_in, notes_in, id_waiting_list_in, adm_needed_in, flg_temporary_in, flg_dur_control_in, id_diag_inst_owner_in, dt_update_in, id_prof_update_in, need_global_anesth_in, need_local_anesth_in, icu_pos_in, OUT l_rows_out;
        IF (rows_out IS NULL)
        THEN
            rows_out := table_varchar();
        END IF;
        rows_out := rows_out MULTISET UNION DISTINCT l_rows_out;
    
    EXCEPTION
        WHEN e_null_column_value
             OR e_existing_fky_reference
             OR e_check_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found
             OR e_forall_error
             OR e_integ_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                pk_alertlog.log_info('DML error ignored: ' || SQLCODE || ': ' || SQLERRM);
            END IF;
        
    END upd;

    --Update any/all columns by dynamic WHERE
    -- If you pass NULL, then the current column value is set to itself
    PROCEDURE upd
    (
        id_schedule_sr_in       IN schedule_sr_hist.id_schedule_sr%TYPE DEFAULT NULL,
        id_schedule_sr_nin      IN BOOLEAN := TRUE,
        id_sched_sr_parent_in   IN schedule_sr_hist.id_sched_sr_parent%TYPE DEFAULT NULL,
        id_sched_sr_parent_nin  IN BOOLEAN := TRUE,
        id_schedule_in          IN schedule_sr_hist.id_schedule%TYPE DEFAULT NULL,
        id_schedule_nin         IN BOOLEAN := TRUE,
        id_episode_in           IN schedule_sr_hist.id_episode%TYPE DEFAULT NULL,
        id_episode_nin          IN BOOLEAN := TRUE,
        id_patient_in           IN schedule_sr_hist.id_patient%TYPE DEFAULT NULL,
        id_patient_nin          IN BOOLEAN := TRUE,
        duration_in             IN schedule_sr_hist.duration%TYPE DEFAULT NULL,
        duration_nin            IN BOOLEAN := TRUE,
        id_diagnosis_in         IN schedule_sr_hist.id_diagnosis%TYPE DEFAULT NULL,
        id_diagnosis_nin        IN BOOLEAN := TRUE,
        id_speciality_in        IN schedule_sr_hist.id_speciality%TYPE DEFAULT NULL,
        id_speciality_nin       IN BOOLEAN := TRUE,
        flg_status_in           IN schedule_sr_hist.flg_status%TYPE DEFAULT NULL,
        flg_status_nin          IN BOOLEAN := TRUE,
        flg_sched_in            IN schedule_sr_hist.flg_sched%TYPE DEFAULT NULL,
        flg_sched_nin           IN BOOLEAN := TRUE,
        id_dept_dest_in         IN schedule_sr_hist.id_dept_dest%TYPE DEFAULT NULL,
        id_dept_dest_nin        IN BOOLEAN := TRUE,
        prev_recovery_time_in   IN schedule_sr_hist.prev_recovery_time%TYPE DEFAULT NULL,
        prev_recovery_time_nin  IN BOOLEAN := TRUE,
        id_sr_cancel_reason_in  IN schedule_sr_hist.id_sr_cancel_reason%TYPE DEFAULT NULL,
        id_sr_cancel_reason_nin IN BOOLEAN := TRUE,
        id_prof_cancel_in       IN schedule_sr_hist.id_prof_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_nin      IN BOOLEAN := TRUE,
        notes_cancel_in         IN schedule_sr_hist.notes_cancel%TYPE DEFAULT NULL,
        notes_cancel_nin        IN BOOLEAN := TRUE,
        id_prof_reg_in          IN schedule_sr_hist.id_prof_reg%TYPE DEFAULT NULL,
        id_prof_reg_nin         IN BOOLEAN := TRUE,
        id_institution_in       IN schedule_sr_hist.id_institution%TYPE DEFAULT NULL,
        id_institution_nin      IN BOOLEAN := TRUE,
        adw_last_update_in      IN schedule_sr_hist.adw_last_update%TYPE DEFAULT NULL,
        adw_last_update_nin     IN BOOLEAN := TRUE,
        dt_target_in            IN schedule_sr_hist.dt_target%TYPE DEFAULT NULL,
        dt_target_nin           IN BOOLEAN := TRUE,
        dt_interv_preview_in    IN schedule_sr_hist.dt_interv_preview%TYPE DEFAULT NULL,
        dt_interv_preview_nin   IN BOOLEAN := TRUE,
        dt_cancel_in            IN schedule_sr_hist.dt_cancel%TYPE DEFAULT NULL,
        dt_cancel_nin           IN BOOLEAN := TRUE,
        create_user_in          IN schedule_sr_hist.create_user%TYPE DEFAULT NULL,
        create_user_nin         IN BOOLEAN := TRUE,
        create_time_in          IN schedule_sr_hist.create_time%TYPE DEFAULT NULL,
        create_time_nin         IN BOOLEAN := TRUE,
        create_institution_in   IN schedule_sr_hist.create_institution%TYPE DEFAULT NULL,
        create_institution_nin  IN BOOLEAN := TRUE,
        update_user_in          IN schedule_sr_hist.update_user%TYPE DEFAULT NULL,
        update_user_nin         IN BOOLEAN := TRUE,
        update_time_in          IN schedule_sr_hist.update_time%TYPE DEFAULT NULL,
        update_time_nin         IN BOOLEAN := TRUE,
        update_institution_in   IN schedule_sr_hist.update_institution%TYPE DEFAULT NULL,
        update_institution_nin  IN BOOLEAN := TRUE,
        icu_in                  IN schedule_sr_hist.icu%TYPE DEFAULT NULL,
        icu_nin                 IN BOOLEAN := TRUE,
        notes_in                IN schedule_sr_hist.notes%TYPE DEFAULT NULL,
        notes_nin               IN BOOLEAN := TRUE,
        id_waiting_list_in      IN schedule_sr_hist.id_waiting_list%TYPE DEFAULT NULL,
        id_waiting_list_nin     IN BOOLEAN := TRUE,
        adm_needed_in           IN schedule_sr_hist.adm_needed%TYPE DEFAULT NULL,
        adm_needed_nin          IN BOOLEAN := TRUE,
        flg_temporary_in        IN schedule_sr_hist.flg_temporary%TYPE DEFAULT NULL,
        flg_temporary_nin       IN BOOLEAN := TRUE,
        flg_dur_control_in      IN schedule_sr_hist.flg_dur_control%TYPE DEFAULT NULL,
        flg_dur_control_nin     IN BOOLEAN := TRUE,
        id_diag_inst_owner_in   IN schedule_sr_hist.id_diag_inst_owner%TYPE DEFAULT NULL,
        id_diag_inst_owner_nin  IN BOOLEAN := TRUE,
        dt_update_in            IN schedule_sr_hist.dt_update%TYPE DEFAULT NULL,
        dt_update_nin           IN BOOLEAN := TRUE,
        id_prof_update_in       IN schedule_sr_hist.id_prof_update%TYPE DEFAULT NULL,
        id_prof_update_nin      IN BOOLEAN := TRUE,
        need_global_anesth_in   IN schedule_sr_hist.need_global_anesth%TYPE DEFAULT NULL,
        need_global_anesth_nin  IN BOOLEAN := TRUE,
        need_local_anesth_in    IN schedule_sr_hist.need_local_anesth%TYPE DEFAULT NULL,
        need_local_anesth_nin   IN BOOLEAN := TRUE,
        icu_pos_in              IN schedule_sr_hist.icu_pos%TYPE DEFAULT NULL,
        icu_pos_nin             IN BOOLEAN := TRUE,
        where_in                IN VARCHAR2,
        handle_error_in         IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        upd(id_schedule_sr_in       => id_schedule_sr_in,
            id_schedule_sr_nin      => id_schedule_sr_nin,
            id_sched_sr_parent_in   => id_sched_sr_parent_in,
            id_sched_sr_parent_nin  => id_sched_sr_parent_nin,
            id_schedule_in          => id_schedule_in,
            id_schedule_nin         => id_schedule_nin,
            id_episode_in           => id_episode_in,
            id_episode_nin          => id_episode_nin,
            id_patient_in           => id_patient_in,
            id_patient_nin          => id_patient_nin,
            duration_in             => duration_in,
            duration_nin            => duration_nin,
            id_diagnosis_in         => id_diagnosis_in,
            id_diagnosis_nin        => id_diagnosis_nin,
            id_speciality_in        => id_speciality_in,
            id_speciality_nin       => id_speciality_nin,
            flg_status_in           => flg_status_in,
            flg_status_nin          => flg_status_nin,
            flg_sched_in            => flg_sched_in,
            flg_sched_nin           => flg_sched_nin,
            id_dept_dest_in         => id_dept_dest_in,
            id_dept_dest_nin        => id_dept_dest_nin,
            prev_recovery_time_in   => prev_recovery_time_in,
            prev_recovery_time_nin  => prev_recovery_time_nin,
            id_sr_cancel_reason_in  => id_sr_cancel_reason_in,
            id_sr_cancel_reason_nin => id_sr_cancel_reason_nin,
            id_prof_cancel_in       => id_prof_cancel_in,
            id_prof_cancel_nin      => id_prof_cancel_nin,
            notes_cancel_in         => notes_cancel_in,
            notes_cancel_nin        => notes_cancel_nin,
            id_prof_reg_in          => id_prof_reg_in,
            id_prof_reg_nin         => id_prof_reg_nin,
            id_institution_in       => id_institution_in,
            id_institution_nin      => id_institution_nin,
            adw_last_update_in      => adw_last_update_in,
            adw_last_update_nin     => adw_last_update_nin,
            dt_target_in            => dt_target_in,
            dt_target_nin           => dt_target_nin,
            dt_interv_preview_in    => dt_interv_preview_in,
            dt_interv_preview_nin   => dt_interv_preview_nin,
            dt_cancel_in            => dt_cancel_in,
            dt_cancel_nin           => dt_cancel_nin,
            create_user_in          => create_user_in,
            create_user_nin         => create_user_nin,
            create_time_in          => create_time_in,
            create_time_nin         => create_time_nin,
            create_institution_in   => create_institution_in,
            create_institution_nin  => create_institution_nin,
            update_user_in          => update_user_in,
            update_user_nin         => update_user_nin,
            update_time_in          => update_time_in,
            update_time_nin         => update_time_nin,
            update_institution_in   => update_institution_in,
            update_institution_nin  => update_institution_nin,
            icu_in                  => icu_in,
            icu_nin                 => icu_nin,
            notes_in                => notes_in,
            notes_nin               => notes_nin,
            id_waiting_list_in      => id_waiting_list_in,
            id_waiting_list_nin     => id_waiting_list_nin,
            adm_needed_in           => adm_needed_in,
            adm_needed_nin          => adm_needed_nin,
            flg_temporary_in        => flg_temporary_in,
            flg_temporary_nin       => flg_temporary_nin,
            flg_dur_control_in      => flg_dur_control_in,
            flg_dur_control_nin     => flg_dur_control_nin,
            id_diag_inst_owner_in   => id_diag_inst_owner_in,
            id_diag_inst_owner_nin  => id_diag_inst_owner_nin,
            dt_update_in            => dt_update_in,
            dt_update_nin           => dt_update_nin,
            id_prof_update_in       => id_prof_update_in,
            id_prof_update_nin      => id_prof_update_nin,
            need_global_anesth_in   => need_global_anesth_in,
            need_global_anesth_nin  => need_global_anesth_nin,
            need_local_anesth_in    => need_local_anesth_in,
            need_local_anesth_nin   => need_local_anesth_nin,
            icu_pos_in              => icu_pos_in,
            icu_pos_nin             => icu_pos_nin,
            where_in                => where_in,
            handle_error_in         => handle_error_in,
            rows_out                => rows_out);
    END upd;

    --Update/insert columns (with rows_out)
    PROCEDURE upd_ins
    (
        id_schedule_sr_in      IN schedule_sr_hist.id_schedule_sr%TYPE DEFAULT NULL,
        id_sched_sr_parent_in  IN schedule_sr_hist.id_sched_sr_parent%TYPE DEFAULT NULL,
        id_schedule_in         IN schedule_sr_hist.id_schedule%TYPE DEFAULT NULL,
        id_episode_in          IN schedule_sr_hist.id_episode%TYPE DEFAULT NULL,
        id_patient_in          IN schedule_sr_hist.id_patient%TYPE DEFAULT NULL,
        duration_in            IN schedule_sr_hist.duration%TYPE DEFAULT NULL,
        id_diagnosis_in        IN schedule_sr_hist.id_diagnosis%TYPE DEFAULT NULL,
        id_speciality_in       IN schedule_sr_hist.id_speciality%TYPE DEFAULT NULL,
        flg_status_in          IN schedule_sr_hist.flg_status%TYPE DEFAULT NULL,
        flg_sched_in           IN schedule_sr_hist.flg_sched%TYPE DEFAULT NULL,
        id_dept_dest_in        IN schedule_sr_hist.id_dept_dest%TYPE DEFAULT NULL,
        prev_recovery_time_in  IN schedule_sr_hist.prev_recovery_time%TYPE DEFAULT NULL,
        id_sr_cancel_reason_in IN schedule_sr_hist.id_sr_cancel_reason%TYPE DEFAULT NULL,
        id_prof_cancel_in      IN schedule_sr_hist.id_prof_cancel%TYPE DEFAULT NULL,
        notes_cancel_in        IN schedule_sr_hist.notes_cancel%TYPE DEFAULT NULL,
        id_prof_reg_in         IN schedule_sr_hist.id_prof_reg%TYPE DEFAULT NULL,
        id_institution_in      IN schedule_sr_hist.id_institution%TYPE DEFAULT NULL,
        adw_last_update_in     IN schedule_sr_hist.adw_last_update%TYPE DEFAULT NULL,
        dt_target_in           IN schedule_sr_hist.dt_target%TYPE DEFAULT NULL,
        dt_interv_preview_in   IN schedule_sr_hist.dt_interv_preview%TYPE DEFAULT NULL,
        dt_cancel_in           IN schedule_sr_hist.dt_cancel%TYPE DEFAULT NULL,
        create_user_in         IN schedule_sr_hist.create_user%TYPE DEFAULT NULL,
        create_time_in         IN schedule_sr_hist.create_time%TYPE DEFAULT NULL,
        create_institution_in  IN schedule_sr_hist.create_institution%TYPE DEFAULT NULL,
        update_user_in         IN schedule_sr_hist.update_user%TYPE DEFAULT NULL,
        update_time_in         IN schedule_sr_hist.update_time%TYPE DEFAULT NULL,
        update_institution_in  IN schedule_sr_hist.update_institution%TYPE DEFAULT NULL,
        icu_in                 IN schedule_sr_hist.icu%TYPE DEFAULT NULL,
        notes_in               IN schedule_sr_hist.notes%TYPE DEFAULT NULL,
        id_waiting_list_in     IN schedule_sr_hist.id_waiting_list%TYPE DEFAULT NULL,
        adm_needed_in          IN schedule_sr_hist.adm_needed%TYPE DEFAULT NULL,
        flg_temporary_in       IN schedule_sr_hist.flg_temporary%TYPE DEFAULT NULL,
        flg_dur_control_in     IN schedule_sr_hist.flg_dur_control%TYPE DEFAULT NULL,
        id_diag_inst_owner_in  IN schedule_sr_hist.id_diag_inst_owner%TYPE DEFAULT NULL,
        dt_update_in           IN schedule_sr_hist.dt_update%TYPE DEFAULT NULL,
        id_prof_update_in      IN schedule_sr_hist.id_prof_update%TYPE DEFAULT NULL,
        need_global_anesth_in  IN schedule_sr_hist.need_global_anesth%TYPE DEFAULT NULL,
        need_local_anesth_in   IN schedule_sr_hist.need_local_anesth%TYPE DEFAULT NULL,
        icu_pos_in             IN schedule_sr_hist.icu_pos%TYPE DEFAULT NULL,
        handle_error_in        IN BOOLEAN := TRUE,
        rows_out               IN OUT table_varchar
    ) IS
    BEGIN
        upd(id_schedule_sr_in      => id_schedule_sr_in,
            id_sched_sr_parent_in  => id_sched_sr_parent_in,
            id_schedule_in         => id_schedule_in,
            id_episode_in          => id_episode_in,
            id_patient_in          => id_patient_in,
            duration_in            => duration_in,
            id_diagnosis_in        => id_diagnosis_in,
            id_speciality_in       => id_speciality_in,
            flg_status_in          => flg_status_in,
            flg_sched_in           => flg_sched_in,
            id_dept_dest_in        => id_dept_dest_in,
            prev_recovery_time_in  => prev_recovery_time_in,
            id_sr_cancel_reason_in => id_sr_cancel_reason_in,
            id_prof_cancel_in      => id_prof_cancel_in,
            notes_cancel_in        => notes_cancel_in,
            id_prof_reg_in         => id_prof_reg_in,
            id_institution_in      => id_institution_in,
            adw_last_update_in     => adw_last_update_in,
            dt_target_in           => dt_target_in,
            dt_interv_preview_in   => dt_interv_preview_in,
            dt_cancel_in           => dt_cancel_in,
            create_user_in         => create_user_in,
            create_time_in         => create_time_in,
            create_institution_in  => create_institution_in,
            update_user_in         => update_user_in,
            update_time_in         => update_time_in,
            update_institution_in  => update_institution_in,
            icu_in                 => icu_in,
            notes_in               => notes_in,
            id_waiting_list_in     => id_waiting_list_in,
            adm_needed_in          => adm_needed_in,
            flg_temporary_in       => flg_temporary_in,
            flg_dur_control_in     => flg_dur_control_in,
            id_diag_inst_owner_in  => id_diag_inst_owner_in,
            dt_update_in           => dt_update_in,
            id_prof_update_in      => id_prof_update_in,
            need_global_anesth_in  => need_global_anesth_in,
            need_local_anesth_in   => need_local_anesth_in,
            icu_pos_in             => icu_pos_in,
            handle_error_in        => handle_error_in,
            rows_out               => rows_out);
        IF SQL%ROWCOUNT = 0
        THEN
            ins(id_schedule_sr_in      => id_schedule_sr_in,
                id_sched_sr_parent_in  => id_sched_sr_parent_in,
                id_schedule_in         => id_schedule_in,
                id_episode_in          => id_episode_in,
                id_patient_in          => id_patient_in,
                duration_in            => duration_in,
                id_diagnosis_in        => id_diagnosis_in,
                id_speciality_in       => id_speciality_in,
                flg_status_in          => flg_status_in,
                flg_sched_in           => flg_sched_in,
                id_dept_dest_in        => id_dept_dest_in,
                prev_recovery_time_in  => prev_recovery_time_in,
                id_sr_cancel_reason_in => id_sr_cancel_reason_in,
                id_prof_cancel_in      => id_prof_cancel_in,
                notes_cancel_in        => notes_cancel_in,
                id_prof_reg_in         => id_prof_reg_in,
                id_institution_in      => id_institution_in,
                adw_last_update_in     => adw_last_update_in,
                dt_target_in           => dt_target_in,
                dt_interv_preview_in   => dt_interv_preview_in,
                dt_cancel_in           => dt_cancel_in,
                create_user_in         => create_user_in,
                create_time_in         => create_time_in,
                create_institution_in  => create_institution_in,
                update_user_in         => update_user_in,
                update_time_in         => update_time_in,
                update_institution_in  => update_institution_in,
                icu_in                 => icu_in,
                notes_in               => notes_in,
                id_waiting_list_in     => id_waiting_list_in,
                adm_needed_in          => adm_needed_in,
                flg_temporary_in       => flg_temporary_in,
                flg_dur_control_in     => flg_dur_control_in,
                id_diag_inst_owner_in  => id_diag_inst_owner_in,
                dt_update_in           => dt_update_in,
                id_prof_update_in      => id_prof_update_in,
                need_global_anesth_in  => need_global_anesth_in,
                need_local_anesth_in   => need_local_anesth_in,
                icu_pos_in             => icu_pos_in,
                handle_error_in        => handle_error_in,
                rows_out               => rows_out);
        END IF;
    END upd_ins;

    --Update/insert columns (without rows_out)
    PROCEDURE upd_ins
    (
        id_schedule_sr_in      IN schedule_sr_hist.id_schedule_sr%TYPE DEFAULT NULL,
        id_sched_sr_parent_in  IN schedule_sr_hist.id_sched_sr_parent%TYPE DEFAULT NULL,
        id_schedule_in         IN schedule_sr_hist.id_schedule%TYPE DEFAULT NULL,
        id_episode_in          IN schedule_sr_hist.id_episode%TYPE DEFAULT NULL,
        id_patient_in          IN schedule_sr_hist.id_patient%TYPE DEFAULT NULL,
        duration_in            IN schedule_sr_hist.duration%TYPE DEFAULT NULL,
        id_diagnosis_in        IN schedule_sr_hist.id_diagnosis%TYPE DEFAULT NULL,
        id_speciality_in       IN schedule_sr_hist.id_speciality%TYPE DEFAULT NULL,
        flg_status_in          IN schedule_sr_hist.flg_status%TYPE DEFAULT NULL,
        flg_sched_in           IN schedule_sr_hist.flg_sched%TYPE DEFAULT NULL,
        id_dept_dest_in        IN schedule_sr_hist.id_dept_dest%TYPE DEFAULT NULL,
        prev_recovery_time_in  IN schedule_sr_hist.prev_recovery_time%TYPE DEFAULT NULL,
        id_sr_cancel_reason_in IN schedule_sr_hist.id_sr_cancel_reason%TYPE DEFAULT NULL,
        id_prof_cancel_in      IN schedule_sr_hist.id_prof_cancel%TYPE DEFAULT NULL,
        notes_cancel_in        IN schedule_sr_hist.notes_cancel%TYPE DEFAULT NULL,
        id_prof_reg_in         IN schedule_sr_hist.id_prof_reg%TYPE DEFAULT NULL,
        id_institution_in      IN schedule_sr_hist.id_institution%TYPE DEFAULT NULL,
        adw_last_update_in     IN schedule_sr_hist.adw_last_update%TYPE DEFAULT NULL,
        dt_target_in           IN schedule_sr_hist.dt_target%TYPE DEFAULT NULL,
        dt_interv_preview_in   IN schedule_sr_hist.dt_interv_preview%TYPE DEFAULT NULL,
        dt_cancel_in           IN schedule_sr_hist.dt_cancel%TYPE DEFAULT NULL,
        create_user_in         IN schedule_sr_hist.create_user%TYPE DEFAULT NULL,
        create_time_in         IN schedule_sr_hist.create_time%TYPE DEFAULT NULL,
        create_institution_in  IN schedule_sr_hist.create_institution%TYPE DEFAULT NULL,
        update_user_in         IN schedule_sr_hist.update_user%TYPE DEFAULT NULL,
        update_time_in         IN schedule_sr_hist.update_time%TYPE DEFAULT NULL,
        update_institution_in  IN schedule_sr_hist.update_institution%TYPE DEFAULT NULL,
        icu_in                 IN schedule_sr_hist.icu%TYPE DEFAULT NULL,
        notes_in               IN schedule_sr_hist.notes%TYPE DEFAULT NULL,
        id_waiting_list_in     IN schedule_sr_hist.id_waiting_list%TYPE DEFAULT NULL,
        adm_needed_in          IN schedule_sr_hist.adm_needed%TYPE DEFAULT NULL,
        flg_temporary_in       IN schedule_sr_hist.flg_temporary%TYPE DEFAULT NULL,
        flg_dur_control_in     IN schedule_sr_hist.flg_dur_control%TYPE DEFAULT NULL,
        id_diag_inst_owner_in  IN schedule_sr_hist.id_diag_inst_owner%TYPE DEFAULT NULL,
        dt_update_in           IN schedule_sr_hist.dt_update%TYPE DEFAULT NULL,
        id_prof_update_in      IN schedule_sr_hist.id_prof_update%TYPE DEFAULT NULL,
        need_global_anesth_in  IN schedule_sr_hist.need_global_anesth%TYPE DEFAULT NULL,
        need_local_anesth_in   IN schedule_sr_hist.need_local_anesth%TYPE DEFAULT NULL,
        icu_pos_in             IN schedule_sr_hist.icu_pos%TYPE DEFAULT NULL,
        handle_error_in        IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        upd_ins(id_schedule_sr_in,
                id_sched_sr_parent_in,
                id_schedule_in,
                id_episode_in,
                id_patient_in,
                duration_in,
                id_diagnosis_in,
                id_speciality_in,
                flg_status_in,
                flg_sched_in,
                id_dept_dest_in,
                prev_recovery_time_in,
                id_sr_cancel_reason_in,
                id_prof_cancel_in,
                notes_cancel_in,
                id_prof_reg_in,
                id_institution_in,
                adw_last_update_in,
                dt_target_in,
                dt_interv_preview_in,
                dt_cancel_in,
                create_user_in,
                create_time_in,
                create_institution_in,
                update_user_in,
                update_time_in,
                update_institution_in,
                icu_in,
                notes_in,
                id_waiting_list_in,
                adm_needed_in,
                flg_temporary_in,
                flg_dur_control_in,
                id_diag_inst_owner_in,
                dt_update_in,
                id_prof_update_in,
                need_global_anesth_in,
                need_local_anesth_in,
                icu_pos_in,
                handle_error_in,
                rows_out);
    END upd_ins;

    --Update by record (with rows_out)
    PROCEDURE upd
    (
        rec_in          IN schedule_sr_hist%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        IN OUT table_varchar
    ) IS
    BEGIN
        upd(id_schedule_sr_in      => rec_in.id_schedule_sr,
            id_sched_sr_parent_in  => rec_in.id_sched_sr_parent,
            id_schedule_in         => rec_in.id_schedule,
            id_episode_in          => rec_in.id_episode,
            id_patient_in          => rec_in.id_patient,
            duration_in            => rec_in.duration,
            id_diagnosis_in        => rec_in.id_diagnosis,
            id_speciality_in       => rec_in.id_speciality,
            flg_status_in          => rec_in.flg_status,
            flg_sched_in           => rec_in.flg_sched,
            id_dept_dest_in        => rec_in.id_dept_dest,
            prev_recovery_time_in  => rec_in.prev_recovery_time,
            id_sr_cancel_reason_in => rec_in.id_sr_cancel_reason,
            id_prof_cancel_in      => rec_in.id_prof_cancel,
            notes_cancel_in        => rec_in.notes_cancel,
            id_prof_reg_in         => rec_in.id_prof_reg,
            id_institution_in      => rec_in.id_institution,
            adw_last_update_in     => rec_in.adw_last_update,
            dt_target_in           => rec_in.dt_target,
            dt_interv_preview_in   => rec_in.dt_interv_preview,
            dt_cancel_in           => rec_in.dt_cancel,
            create_user_in         => rec_in.create_user,
            create_time_in         => rec_in.create_time,
            create_institution_in  => rec_in.create_institution,
            update_user_in         => rec_in.update_user,
            update_time_in         => rec_in.update_time,
            update_institution_in  => rec_in.update_institution,
            icu_in                 => rec_in.icu,
            notes_in               => rec_in.notes,
            id_waiting_list_in     => rec_in.id_waiting_list,
            adm_needed_in          => rec_in.adm_needed,
            flg_temporary_in       => rec_in.flg_temporary,
            flg_dur_control_in     => rec_in.flg_dur_control,
            id_diag_inst_owner_in  => rec_in.id_diag_inst_owner,
            dt_update_in           => rec_in.dt_update,
            id_prof_update_in      => rec_in.id_prof_update,
            need_global_anesth_in  => rec_in.need_global_anesth,
            need_local_anesth_in   => rec_in.need_local_anesth,
            icu_pos_in             => rec_in.icu_pos,
            handle_error_in        => handle_error_in,
            rows_out               => rows_out);
    END upd;

    --Update by record (without rows_out)
    PROCEDURE upd
    (
        rec_in          IN schedule_sr_hist%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        upd(id_schedule_sr_in      => rec_in.id_schedule_sr,
            id_sched_sr_parent_in  => rec_in.id_sched_sr_parent,
            id_schedule_in         => rec_in.id_schedule,
            id_episode_in          => rec_in.id_episode,
            id_patient_in          => rec_in.id_patient,
            duration_in            => rec_in.duration,
            id_diagnosis_in        => rec_in.id_diagnosis,
            id_speciality_in       => rec_in.id_speciality,
            flg_status_in          => rec_in.flg_status,
            flg_sched_in           => rec_in.flg_sched,
            id_dept_dest_in        => rec_in.id_dept_dest,
            prev_recovery_time_in  => rec_in.prev_recovery_time,
            id_sr_cancel_reason_in => rec_in.id_sr_cancel_reason,
            id_prof_cancel_in      => rec_in.id_prof_cancel,
            notes_cancel_in        => rec_in.notes_cancel,
            id_prof_reg_in         => rec_in.id_prof_reg,
            id_institution_in      => rec_in.id_institution,
            adw_last_update_in     => rec_in.adw_last_update,
            dt_target_in           => rec_in.dt_target,
            dt_interv_preview_in   => rec_in.dt_interv_preview,
            dt_cancel_in           => rec_in.dt_cancel,
            create_user_in         => rec_in.create_user,
            create_time_in         => rec_in.create_time,
            create_institution_in  => rec_in.create_institution,
            update_user_in         => rec_in.update_user,
            update_time_in         => rec_in.update_time,
            update_institution_in  => rec_in.update_institution,
            icu_in                 => rec_in.icu,
            notes_in               => rec_in.notes,
            id_waiting_list_in     => rec_in.id_waiting_list,
            adm_needed_in          => rec_in.adm_needed,
            flg_temporary_in       => rec_in.flg_temporary,
            flg_dur_control_in     => rec_in.flg_dur_control,
            id_diag_inst_owner_in  => rec_in.id_diag_inst_owner,
            dt_update_in           => rec_in.dt_update,
            id_prof_update_in      => rec_in.id_prof_update,
            need_global_anesth_in  => rec_in.need_global_anesth,
            need_local_anesth_in   => rec_in.need_local_anesth,
            icu_pos_in             => rec_in.icu_pos,
            handle_error_in        => handle_error_in,
            rows_out               => rows_out);
    END upd;

    --Update with collection (with rows_out)
    PROCEDURE upd
    (
        col_in            IN schedule_sr_hist_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          IN OUT table_varchar
    ) IS
        l_id_schedule_sr      id_schedule_sr_cc;
        l_id_sched_sr_parent  id_sched_sr_parent_cc;
        l_id_schedule         id_schedule_cc;
        l_id_episode          id_episode_cc;
        l_id_patient          id_patient_cc;
        l_duration            duration_cc;
        l_id_diagnosis        id_diagnosis_cc;
        l_id_speciality       id_speciality_cc;
        l_flg_status          flg_status_cc;
        l_flg_sched           flg_sched_cc;
        l_id_dept_dest        id_dept_dest_cc;
        l_prev_recovery_time  prev_recovery_time_cc;
        l_id_sr_cancel_reason id_sr_cancel_reason_cc;
        l_id_prof_cancel      id_prof_cancel_cc;
        l_notes_cancel        notes_cancel_cc;
        l_id_prof_reg         id_prof_reg_cc;
        l_id_institution      id_institution_cc;
        l_adw_last_update     adw_last_update_cc;
        l_dt_target           dt_target_cc;
        l_dt_interv_preview   dt_interv_preview_cc;
        l_dt_cancel           dt_cancel_cc;
        l_create_user         create_user_cc;
        l_create_time         create_time_cc;
        l_create_institution  create_institution_cc;
        l_update_user         update_user_cc;
        l_update_time         update_time_cc;
        l_update_institution  update_institution_cc;
        l_icu                 icu_cc;
        l_notes               notes_cc;
        l_id_waiting_list     id_waiting_list_cc;
        l_adm_needed          adm_needed_cc;
        l_flg_temporary       flg_temporary_cc;
        l_flg_dur_control     flg_dur_control_cc;
        l_id_diag_inst_owner  id_diag_inst_owner_cc;
        l_dt_update           dt_update_cc;
        l_id_prof_update      id_prof_update_cc;
        l_need_global_anesth  need_global_anesth_cc;
        l_need_local_anesth   need_local_anesth_cc;
        l_icu_pos             icu_pos_cc;
    BEGIN
        FOR i IN col_in.first .. col_in.last
        LOOP
            l_id_schedule_sr(i) := col_in(i).id_schedule_sr;
            l_id_sched_sr_parent(i) := col_in(i).id_sched_sr_parent;
            l_id_schedule(i) := col_in(i).id_schedule;
            l_id_episode(i) := col_in(i).id_episode;
            l_id_patient(i) := col_in(i).id_patient;
            l_duration(i) := col_in(i).duration;
            l_id_diagnosis(i) := col_in(i).id_diagnosis;
            l_id_speciality(i) := col_in(i).id_speciality;
            l_flg_status(i) := col_in(i).flg_status;
            l_flg_sched(i) := col_in(i).flg_sched;
            l_id_dept_dest(i) := col_in(i).id_dept_dest;
            l_prev_recovery_time(i) := col_in(i).prev_recovery_time;
            l_id_sr_cancel_reason(i) := col_in(i).id_sr_cancel_reason;
            l_id_prof_cancel(i) := col_in(i).id_prof_cancel;
            l_notes_cancel(i) := col_in(i).notes_cancel;
            l_id_prof_reg(i) := col_in(i).id_prof_reg;
            l_id_institution(i) := col_in(i).id_institution;
            l_adw_last_update(i) := col_in(i).adw_last_update;
            l_dt_target(i) := col_in(i).dt_target;
            l_dt_interv_preview(i) := col_in(i).dt_interv_preview;
            l_dt_cancel(i) := col_in(i).dt_cancel;
            l_create_user(i) := col_in(i).create_user;
            l_create_time(i) := col_in(i).create_time;
            l_create_institution(i) := col_in(i).create_institution;
            l_update_user(i) := col_in(i).update_user;
            l_update_time(i) := col_in(i).update_time;
            l_update_institution(i) := col_in(i).update_institution;
            l_icu(i) := col_in(i).icu;
            l_notes(i) := col_in(i).notes;
            l_id_waiting_list(i) := col_in(i).id_waiting_list;
            l_adm_needed(i) := col_in(i).adm_needed;
            l_flg_temporary(i) := col_in(i).flg_temporary;
            l_flg_dur_control(i) := col_in(i).flg_dur_control;
            l_id_diag_inst_owner(i) := col_in(i).id_diag_inst_owner;
            l_dt_update(i) := col_in(i).dt_update;
            l_id_prof_update(i) := col_in(i).id_prof_update;
            l_need_global_anesth(i) := col_in(i).need_global_anesth;
            l_need_local_anesth(i) := col_in(i).need_local_anesth;
            l_icu_pos(i) := col_in(i).icu_pos;
        END LOOP;
        IF nvl(ignore_if_null_in, FALSE)
        THEN
            -- Set any columns to their current values
            -- if incoming value is NULL.
            -- Put WHEN clause on column-level triggers!
            FORALL i IN col_in.first .. col_in.last
                UPDATE schedule_sr_hist
                   SET id_schedule_sr      = nvl(l_id_schedule_sr(i), id_schedule_sr),
                       id_sched_sr_parent  = nvl(l_id_sched_sr_parent(i), id_sched_sr_parent),
                       id_schedule         = nvl(l_id_schedule(i), id_schedule),
                       id_episode          = nvl(l_id_episode(i), id_episode),
                       id_patient          = nvl(l_id_patient(i), id_patient),
                       duration            = nvl(l_duration(i), duration),
                       id_diagnosis        = nvl(l_id_diagnosis(i), id_diagnosis),
                       id_speciality       = nvl(l_id_speciality(i), id_speciality),
                       flg_status          = nvl(l_flg_status(i), flg_status),
                       flg_sched           = nvl(l_flg_sched(i), flg_sched),
                       id_dept_dest        = nvl(l_id_dept_dest(i), id_dept_dest),
                       prev_recovery_time  = nvl(l_prev_recovery_time(i), prev_recovery_time),
                       id_sr_cancel_reason = nvl(l_id_sr_cancel_reason(i), id_sr_cancel_reason),
                       id_prof_cancel      = nvl(l_id_prof_cancel(i), id_prof_cancel),
                       notes_cancel        = nvl(l_notes_cancel(i), notes_cancel),
                       id_prof_reg         = nvl(l_id_prof_reg(i), id_prof_reg),
                       id_institution      = nvl(l_id_institution(i), id_institution),
                       adw_last_update     = nvl(l_adw_last_update(i), adw_last_update),
                       dt_target           = nvl(l_dt_target(i), dt_target),
                       dt_interv_preview   = nvl(l_dt_interv_preview(i), dt_interv_preview),
                       dt_cancel           = nvl(l_dt_cancel(i), dt_cancel),
                       create_user         = nvl(l_create_user(i), create_user),
                       create_time         = nvl(l_create_time(i), create_time),
                       create_institution  = nvl(l_create_institution(i), create_institution),
                       update_user         = nvl(l_update_user(i), update_user),
                       update_time         = nvl(l_update_time(i), update_time),
                       update_institution  = nvl(l_update_institution(i), update_institution),
                       icu                 = nvl(l_icu(i), icu),
                       notes               = nvl(l_notes(i), notes),
                       id_waiting_list     = nvl(l_id_waiting_list(i), id_waiting_list),
                       adm_needed          = nvl(l_adm_needed(i), adm_needed),
                       flg_temporary       = nvl(l_flg_temporary(i), flg_temporary),
                       flg_dur_control     = nvl(l_flg_dur_control(i), flg_dur_control),
                       id_diag_inst_owner  = nvl(l_id_diag_inst_owner(i), id_diag_inst_owner),
                       dt_update           = nvl(l_dt_update(i), dt_update),
                       id_prof_update      = nvl(l_id_prof_update(i), id_prof_update),
                       need_global_anesth  = nvl(l_need_global_anesth(i), need_global_anesth),
                       need_local_anesth   = nvl(l_need_local_anesth(i), need_local_anesth),
                       icu_pos             = nvl(l_icu_pos(i), icu_pos)
                RETURNING ROWID BULK COLLECT INTO rows_out;
        ELSE
            FORALL i IN col_in.first .. col_in.last
                UPDATE schedule_sr_hist
                   SET id_schedule_sr      = l_id_schedule_sr(i),
                       id_sched_sr_parent  = l_id_sched_sr_parent(i),
                       id_schedule         = l_id_schedule(i),
                       id_episode          = l_id_episode(i),
                       id_patient          = l_id_patient(i),
                       duration            = l_duration(i),
                       id_diagnosis        = l_id_diagnosis(i),
                       id_speciality       = l_id_speciality(i),
                       flg_status          = l_flg_status(i),
                       flg_sched           = l_flg_sched(i),
                       id_dept_dest        = l_id_dept_dest(i),
                       prev_recovery_time  = l_prev_recovery_time(i),
                       id_sr_cancel_reason = l_id_sr_cancel_reason(i),
                       id_prof_cancel      = l_id_prof_cancel(i),
                       notes_cancel        = l_notes_cancel(i),
                       id_prof_reg         = l_id_prof_reg(i),
                       id_institution      = l_id_institution(i),
                       adw_last_update     = l_adw_last_update(i),
                       dt_target           = l_dt_target(i),
                       dt_interv_preview   = l_dt_interv_preview(i),
                       dt_cancel           = l_dt_cancel(i),
                       create_user         = l_create_user(i),
                       create_time         = l_create_time(i),
                       create_institution  = l_create_institution(i),
                       update_user         = l_update_user(i),
                       update_time         = l_update_time(i),
                       update_institution  = l_update_institution(i),
                       icu                 = l_icu(i),
                       notes               = l_notes(i),
                       id_waiting_list     = l_id_waiting_list(i),
                       adm_needed          = l_adm_needed(i),
                       flg_temporary       = l_flg_temporary(i),
                       flg_dur_control     = l_flg_dur_control(i),
                       id_diag_inst_owner  = l_id_diag_inst_owner(i),
                       dt_update           = l_dt_update(i),
                       id_prof_update      = l_id_prof_update(i),
                       need_global_anesth  = l_need_global_anesth(i),
                       need_local_anesth   = l_need_local_anesth(i),
                       icu_pos             = l_icu_pos(i)
                RETURNING ROWID BULK COLLECT INTO rows_out;
        END IF;
    
    EXCEPTION
        WHEN e_forall_error THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                FOR indx IN 1 .. SQL%bulk_exceptions.count
                LOOP
                    pk_alertlog.log_info('DML error ignored: ' || SQLCODE || ': ' || SQLERRM);
                END LOOP;
            END IF;
        WHEN OTHERS THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                pk_alertlog.log_info('DML error ignored: ' || SQLCODE || ': ' || SQLERRM);
            END IF;
        
    END upd;

    --Update with collection (without rows_out)
    PROCEDURE upd
    (
        col_in            IN schedule_sr_hist_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        upd(col_in, ignore_if_null_in, handle_error_in, rows_out);
    END upd;

    --Dynamic update string
    FUNCTION dynupdstr
    (
        colname_in IN all_tab_columns.column_name%TYPE,
        where_in   IN VARCHAR2 := NULL
    ) RETURN VARCHAR2 IS
    BEGIN
        RETURN 'BEGIN UPDATE schedule_sr_hist
SET ' || colname_in || ' = :value
 WHERE ' || nvl(where_in, '1=1') || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;';
    END dynupdstr;

    -- Use Native Dynamic SQL increment a single NUMBER column
    -- for all rows specified by the dynamic WHERE clause (with rows_out)
    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    ) IS
    BEGIN
        EXECUTE IMMEDIATE 'BEGIN UPDATE schedule_sr_hist
SET ' || colname_in || '=' || colname_in || '+' || nvl(increment_value_in, 1) || ' WHERE ' ||
                          nvl(where_in, '1=1') || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
            USING OUT rows_out;
    END increment_onecol;

    -- Use Native Dynamic SQL increment a single NUMBER column
    -- for all rows specified by the dynamic WHERE clause (without rows_out)
    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        EXECUTE IMMEDIATE 'UPDATE schedule_sr_hist
SET ' || colname_in || '=' || colname_in || '+' || nvl(increment_value_in, 1) || ' WHERE ' ||
                          nvl(where_in, '1=1');
    END increment_onecol;

    -- Delete one row by primary key
    PROCEDURE del
    (
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) IS
    BEGIN
        DELETE FROM schedule_sr_hist
        RETURNING ROWID BULK COLLECT INTO rows_out;
    
    EXCEPTION
        WHEN e_null_column_value
             OR e_existing_fky_reference
             OR e_check_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found
             OR e_forall_error
             OR e_integ_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                pk_alertlog.log_info('DML error ignored: ' || SQLCODE || ': ' || SQLERRM);
            END IF;
        
    END del;

    -- Delete one row by primary key
    PROCEDURE del(handle_error_in IN BOOLEAN := TRUE) IS
        rows_out table_varchar;
    BEGIN
        del(handle_error_in => handle_error_in, rows_out => rows_out);
    END del;

    -- Delete all rows for this SRHI_SCHSR_FK foreign key value
    PROCEDURE del_srhi_schsr_fk
    (
        id_schedule_sr_in IN schedule_sr_hist.id_schedule_sr%TYPE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          OUT table_varchar
    ) IS
    BEGIN
        DELETE FROM schedule_sr_hist
         WHERE id_schedule_sr = id_schedule_sr_in
        RETURNING ROWID BULK COLLECT INTO rows_out;
    
    EXCEPTION
        WHEN e_null_column_value
             OR e_existing_fky_reference
             OR e_check_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found
             OR e_forall_error
             OR e_integ_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                pk_alertlog.log_info('DML error ignored: ' || SQLCODE || ': ' || SQLERRM);
            END IF;
        
    END del_srhi_schsr_fk;

    -- Delete all rows for this SRHI_SCHSR_FK foreign key value
    PROCEDURE del_srhi_schsr_fk
    (
        id_schedule_sr_in IN schedule_sr_hist.id_schedule_sr%TYPE,
        handle_error_in   IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        del_srhi_schsr_fk(id_schedule_sr_in => id_schedule_sr_in,
                          handle_error_in   => handle_error_in,
                          rows_out          => rows_out);
    END del_srhi_schsr_fk;

    -- Deletions using dynamic SQL
    FUNCTION dyndelstr(where_in IN VARCHAR2) RETURN VARCHAR2 IS
    BEGIN
        IF where_in IS NULL
        THEN
            RETURN 'DELETE FROM schedule_sr_hist';
        ELSE
            RETURN 'DELETE FROM schedule_sr_hist WHERE ' || where_in;
        END IF;
    END dyndelstr;
    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) IS
    BEGIN
        EXECUTE IMMEDIATE 'BEGIN ' || dyndelstr(where_clause_in) ||
                          ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
            USING OUT rows_out;
    
    EXCEPTION
        WHEN e_null_column_value
             OR e_existing_fky_reference
             OR e_check_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found
             OR e_forall_error
             OR e_integ_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                pk_alertlog.log_info('DML error ignored: ' || SQLCODE || ': ' || SQLERRM);
            END IF;
        
    END del_by;

    -- Delete all rows specified by dynamic WHERE clause
    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
    BEGIN
        EXECUTE IMMEDIATE dyndelstr(where_clause_in);
    
    EXCEPTION
        WHEN e_null_column_value
             OR e_existing_fky_reference
             OR e_check_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found
             OR e_forall_error
             OR e_integ_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                pk_alertlog.log_info('DML error ignored: ' || SQLCODE || ': ' || SQLERRM);
            END IF;
        
    END del_by;

    -- Initialize a record with default values for columns in the table (prc)
    PROCEDURE initrec(schedule_sr_hist_inout IN OUT schedule_sr_hist%ROWTYPE) IS
    BEGIN
        schedule_sr_hist_inout.id_schedule_sr      := NULL;
        schedule_sr_hist_inout.id_sched_sr_parent  := NULL;
        schedule_sr_hist_inout.id_schedule         := NULL;
        schedule_sr_hist_inout.id_episode          := NULL;
        schedule_sr_hist_inout.id_patient          := NULL;
        schedule_sr_hist_inout.duration            := NULL;
        schedule_sr_hist_inout.id_diagnosis        := NULL;
        schedule_sr_hist_inout.id_speciality       := NULL;
        schedule_sr_hist_inout.flg_status          := NULL;
        schedule_sr_hist_inout.flg_sched           := NULL;
        schedule_sr_hist_inout.id_dept_dest        := NULL;
        schedule_sr_hist_inout.prev_recovery_time  := NULL;
        schedule_sr_hist_inout.id_sr_cancel_reason := NULL;
        schedule_sr_hist_inout.id_prof_cancel      := NULL;
        schedule_sr_hist_inout.notes_cancel        := NULL;
        schedule_sr_hist_inout.id_prof_reg         := NULL;
        schedule_sr_hist_inout.id_institution      := NULL;
        schedule_sr_hist_inout.adw_last_update     := NULL;
        schedule_sr_hist_inout.dt_target           := NULL;
        schedule_sr_hist_inout.dt_interv_preview   := NULL;
        schedule_sr_hist_inout.dt_cancel           := NULL;
        schedule_sr_hist_inout.create_user         := NULL;
        schedule_sr_hist_inout.create_time         := NULL;
        schedule_sr_hist_inout.create_institution  := NULL;
        schedule_sr_hist_inout.update_user         := NULL;
        schedule_sr_hist_inout.update_time         := NULL;
        schedule_sr_hist_inout.update_institution  := NULL;
        schedule_sr_hist_inout.icu                 := NULL;
        schedule_sr_hist_inout.notes               := NULL;
        schedule_sr_hist_inout.id_waiting_list     := NULL;
        schedule_sr_hist_inout.adm_needed          := NULL;
        schedule_sr_hist_inout.flg_temporary       := NULL;
        schedule_sr_hist_inout.flg_dur_control     := NULL;
        schedule_sr_hist_inout.id_diag_inst_owner  := NULL;
        schedule_sr_hist_inout.dt_update           := NULL;
        schedule_sr_hist_inout.id_prof_update      := NULL;
        schedule_sr_hist_inout.need_global_anesth  := NULL;
        schedule_sr_hist_inout.need_local_anesth   := NULL;
        schedule_sr_hist_inout.icu_pos             := NULL;
    END initrec;

    -- Initialize a record with default values for columns in the table (fnc)
    FUNCTION initrec RETURN schedule_sr_hist%ROWTYPE IS
        l_schedule_sr_hist schedule_sr_hist%ROWTYPE;
    BEGIN
        RETURN l_schedule_sr_hist;
    END initrec;

    --get data from rowid
    FUNCTION get_data_rowid(rows_in IN table_varchar) RETURN schedule_sr_hist_tc IS
        data schedule_sr_hist_tc;
    BEGIN
        SELECT *
          BULK COLLECT
          INTO data
          FROM schedule_sr_hist
         WHERE ROWID IN (SELECT /*+opt_estimate(table,t,scale_rows=0.0000001))*/
                          *
                           FROM TABLE(rows_in) t);
        RETURN data;
    END get_data_rowid;

    --get data from rowid (pragma autonomous transacion)
    FUNCTION get_data_rowid_pat(rows_in IN table_varchar) RETURN schedule_sr_hist_tc IS
        data schedule_sr_hist_tc;
        PRAGMA AUTONOMOUS_TRANSACTION;
    BEGIN
        data := get_data_rowid(rows_in);
        COMMIT;
        RETURN data;
    
    EXCEPTION
        WHEN OTHERS THEN
            pk_alert_exceptions.raise_error(error_name_in => 'get_data_rowid_pat');
            ROLLBACK;
        
    END get_data_rowid_pat;

BEGIN
    NULL;
END ts_schedule_sr_hist;
/
