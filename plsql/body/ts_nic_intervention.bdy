/*-- Last Change Revision: $Rev: 1658098 $*/
/*-- Last Change by: $Author: ariel.machado $*/
/*-- Date of last change: $Date: 2014-11-10 10:07:57 +0000 (seg, 10 nov 2014) $*/
CREATE OR REPLACE PACKAGE BODY TS_NIC_INTERVENTION
/*
| Generated by or retrieved from QCGU - DO NOT MODIFY!
| QCGU - "Get it right, do it fast" - www.ToadWorld.com
| QCGU Universal ID: {7A44F965-7C77-42EE-9A04-269EF006213B}
| Created On: Outubro 21, 2013 9:30:10
| Created By: ALERT
*/
IS


   e_null_column_value EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_null_column_value, -1400);
   --
   e_existing_fky_reference EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_existing_fky_reference, -2266);
   --
   e_check_constraint_failure EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_check_constraint_failure, -2290);
   --
   e_no_parent_key EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_no_parent_key, -2291);
   --
   e_child_record_found EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_child_record_found, -2292);
   --
   e_forall_error EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_forall_error, -24381);
   --
   -- Defined for backward compatibilty.
   e_integ_constraint_failure EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_integ_constraint_failure, -2291);

    -- Private utilities
   PROCEDURE get_constraint_info (
      owner_out OUT ALL_CONSTRAINTS.OWNER%TYPE
     ,name_out OUT ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE)
   IS
      l_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
      dotloc PLS_INTEGER;
      leftloc PLS_INTEGER;
   BEGIN
      dotloc  := INSTR (l_errm,'.');
      leftloc := INSTR (l_errm,'(');
      owner_out := SUBSTR (l_errm, leftloc+1, dotloc-leftloc-1);
      name_out  := SUBSTR (l_errm, dotloc+1, INSTR (l_errm,')')-dotloc-1);
   END get_constraint_info;
   -- Public programs

   PROCEDURE ins (
      id_nic_intervention_in IN NIC_INTERVENTION.ID_NIC_INTERVENTION%TYPE
      ,
      id_terminology_version_in IN NIC_INTERVENTION.ID_TERMINOLOGY_VERSION%TYPE DEFAULT NULL,
      intervention_code_in IN NIC_INTERVENTION.INTERVENTION_CODE%TYPE DEFAULT NULL,
      references_in IN NIC_INTERVENTION.REFERENCES%TYPE DEFAULT NULL,
      id_inst_owner_in IN NIC_INTERVENTION.ID_INST_OWNER%TYPE DEFAULT NULL,
      id_concept_version_in IN NIC_INTERVENTION.ID_CONCEPT_VERSION%TYPE DEFAULT NULL,
      id_concept_term_in IN NIC_INTERVENTION.ID_CONCEPT_TERM%TYPE DEFAULT NULL,
      create_user_in IN NIC_INTERVENTION.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN NIC_INTERVENTION.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN NIC_INTERVENTION.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN NIC_INTERVENTION.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN NIC_INTERVENTION.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN NIC_INTERVENTION.UPDATE_INSTITUTION%TYPE DEFAULT NULL
     ,handle_error_in IN BOOLEAN := TRUE
      , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN

     INSERT INTO NIC_INTERVENTION (
         ID_NIC_INTERVENTION,
         ID_TERMINOLOGY_VERSION,
         INTERVENTION_CODE,
         REFERENCES,
         ID_INST_OWNER,
         ID_CONCEPT_VERSION,
         ID_CONCEPT_TERM,
         CREATE_USER,
         CREATE_TIME,
         CREATE_INSTITUTION,
         UPDATE_USER,
         UPDATE_TIME,
         UPDATE_INSTITUTION
         )
      VALUES (
         id_nic_intervention_in,
         id_terminology_version_in,
         intervention_code_in,
         references_in,
         id_inst_owner_in,
         id_concept_version_in,
         id_concept_term_in,
         create_user_in,
         create_time_in,
         create_institution_in,
         update_user_in,
         update_time_in,
         update_institution_in
         ) RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF FALSE THEN NULL; -- Placeholder in case no unique indexes
           ELSIF l_name = 'NIC_INTERVENTION_UK'
           THEN
              pk_alert_exceptions.register_error (
                 error_name_in => 'DUPLICATE-VALUE'
                ,err_instance_id_out => l_err_instance_id
                ,text_in => 'Violation of unique index "NIC_INTERVENTION_UK"'
                );
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_err_instance_id
                ,NAME_IN => 'OWNER'
                ,value_in => l_owner
                ,validate_in => FALSE
                );
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_err_instance_id
                ,NAME_IN => 'CONSTRAINT_NAME'
                ,value_in => l_name
                ,validate_in => FALSE
                );
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_err_instance_id
                ,NAME_IN => 'TABLE_NAME'
                ,value_in => 'NIC_INTERVENTION'
                ,validate_in => FALSE
                );
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_err_instance_id
                ,NAME_IN => 'ID_TERMINOLOGY_VERSION'
                ,value_in => id_terminology_version_in
                ,validate_in => FALSE
                );
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_err_instance_id
                ,NAME_IN => 'INTERVENTION_CODE'
                ,value_in => intervention_code_in
                ,validate_in => FALSE
                );
              pk_alert_exceptions.raise_error_instance (
                    err_instance_id_in => l_err_instance_id);
           ELSE
              pk_alert_exceptions.raise_error (
                    error_name_in => 'DUPLICATE-VALUE'
                    ,name1_in => 'OWNER'
                    ,value1_in => l_owner
                    ,name2_in => 'CONSTRAINT_NAME'
                    ,value2_in => l_name
                    ,name3_in => 'TABLE_NAME'
                    ,value3_in => 'NIC_INTERVENTION');
           END IF;
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'NIC_INTERVENTION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'NIC_INTERVENTION');
           IF l_name = 'NICI_CNCPTTR_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_CONCEPT_TERM'
               , value_in => id_concept_term_in);
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_INST_OWNER'
               , value_in => id_inst_owner_in);
           END IF;
           IF l_name = 'NICI_CNCPTV_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_CONCEPT_VERSION'
               , value_in => id_concept_version_in);
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_INST_OWNER'
               , value_in => id_inst_owner_in);
           END IF;
           IF l_name = 'NICI_NIC_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'INTERVENTION_CODE'
               , value_in => intervention_code_in);
           END IF;
           IF l_name = 'NICI_TERMV_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_TERMINOLOGY_VERSION'
               , value_in => id_terminology_version_in);
           END IF;
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END ins;

   PROCEDURE ins (
      id_nic_intervention_in IN NIC_INTERVENTION.ID_NIC_INTERVENTION%TYPE
      ,
      id_terminology_version_in IN NIC_INTERVENTION.ID_TERMINOLOGY_VERSION%TYPE DEFAULT NULL,
      intervention_code_in IN NIC_INTERVENTION.INTERVENTION_CODE%TYPE DEFAULT NULL,
      references_in IN NIC_INTERVENTION.REFERENCES%TYPE DEFAULT NULL,
      id_inst_owner_in IN NIC_INTERVENTION.ID_INST_OWNER%TYPE DEFAULT NULL,
      id_concept_version_in IN NIC_INTERVENTION.ID_CONCEPT_VERSION%TYPE DEFAULT NULL,
      id_concept_term_in IN NIC_INTERVENTION.ID_CONCEPT_TERM%TYPE DEFAULT NULL,
      create_user_in IN NIC_INTERVENTION.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN NIC_INTERVENTION.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN NIC_INTERVENTION.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN NIC_INTERVENTION.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN NIC_INTERVENTION.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN NIC_INTERVENTION.UPDATE_INSTITUTION%TYPE DEFAULT NULL
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN

     ins (
      id_nic_intervention_in => id_nic_intervention_in
      ,
      id_terminology_version_in => id_terminology_version_in,
      intervention_code_in => intervention_code_in,
      references_in => references_in,
      id_inst_owner_in => id_inst_owner_in,
      id_concept_version_in => id_concept_version_in,
      id_concept_term_in => id_concept_term_in,
      create_user_in => create_user_in,
      create_time_in => create_time_in,
      create_institution_in => create_institution_in,
      update_user_in => update_user_in,
      update_time_in => update_time_in,
      update_institution_in => update_institution_in
     ,handle_error_in => handle_error_in
      ,rows_out => rows_out
      );
   END ins;

   /*
   START Special logic for handling LOB columns....
   */

   PROCEDURE n_ins_clobs_in_chunks (
      id_nic_intervention_in IN NIC_INTERVENTION.ID_NIC_INTERVENTION%TYPE,
      id_terminology_version_in IN NIC_INTERVENTION.ID_TERMINOLOGY_VERSION%TYPE DEFAULT NULL,
      intervention_code_in IN NIC_INTERVENTION.INTERVENTION_CODE%TYPE DEFAULT NULL,
      id_inst_owner_in IN NIC_INTERVENTION.ID_INST_OWNER%TYPE DEFAULT NULL,
      id_concept_version_in IN NIC_INTERVENTION.ID_CONCEPT_VERSION%TYPE DEFAULT NULL,
      id_concept_term_in IN NIC_INTERVENTION.ID_CONCEPT_TERM%TYPE DEFAULT NULL,
      create_user_in IN NIC_INTERVENTION.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN NIC_INTERVENTION.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN NIC_INTERVENTION.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN NIC_INTERVENTION.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN NIC_INTERVENTION.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN NIC_INTERVENTION.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      handle_error_in IN BOOLEAN := TRUE,
      clob_columns_in IN varchar2_t,
      clob_pieces_in IN  varchar2_t
   ) IS
        l_REFERENCES clob;
      i                PLS_INTEGER;
      current_column varchar2(30) := '';
   BEGIN
      /* Be aware, PLSQL limitations on temporary clobs prevent multiple CLOB
         variables from pointing to the same temporary CLOB (it creates copies).
         Otherwise, the code could have been written a bit more compactly. */

      FOR i IN clob_columns_in.FIRST .. clob_columns_in.LAST
      LOOP
         /* Even when all clobs are null, DOA must send 1 row so skip it. */
         IF clob_columns_in (i) IS NOT NULL
         THEN
            IF current_column <> clob_columns_in (i) OR current_column IS NULL
            THEN
               current_column := LOWER (clob_columns_in (i));

               CASE current_column
                  WHEN 'references_in'
                  THEN
                     IF l_REFERENCES IS NULL
                     THEN
                       DBMS_LOB.createtemporary (
                         l_REFERENCES, TRUE, DBMS_LOB.CALL);
                     END IF;
               END CASE;
            END IF;

            CASE current_column
              WHEN 'references_in'
              THEN
                  DBMS_LOB.writeappend (l_REFERENCES
                                      , LENGTH (clob_pieces_in (i))
                                      , clob_pieces_in (i)
                                       );
            END CASE;
         END IF;
      END LOOP;

      ins (
         id_nic_intervention_in => id_nic_intervention_in,
         id_terminology_version_in => id_terminology_version_in,
         intervention_code_in => intervention_code_in,
         references_in => l_REFERENCES,
         id_inst_owner_in => id_inst_owner_in,
         id_concept_version_in => id_concept_version_in,
         id_concept_term_in => id_concept_term_in,
         create_user_in => create_user_in,
         create_time_in => create_time_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in,
         handle_error_in => handle_error_in
         );

   END n_ins_clobs_in_chunks;

   PROCEDURE n_upd_clobs_in_chunks (
      id_nic_intervention_in IN NIC_INTERVENTION.ID_NIC_INTERVENTION%TYPE,
      id_terminology_version_in IN NIC_INTERVENTION.ID_TERMINOLOGY_VERSION%TYPE DEFAULT NULL,
      intervention_code_in IN NIC_INTERVENTION.INTERVENTION_CODE%TYPE DEFAULT NULL,
      id_inst_owner_in IN NIC_INTERVENTION.ID_INST_OWNER%TYPE DEFAULT NULL,
      id_concept_version_in IN NIC_INTERVENTION.ID_CONCEPT_VERSION%TYPE DEFAULT NULL,
      id_concept_term_in IN NIC_INTERVENTION.ID_CONCEPT_TERM%TYPE DEFAULT NULL,
      create_user_in IN NIC_INTERVENTION.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN NIC_INTERVENTION.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN NIC_INTERVENTION.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN NIC_INTERVENTION.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN NIC_INTERVENTION.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN NIC_INTERVENTION.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      ignore_if_null_in IN BOOLEAN := TRUE,
      handle_error_in IN BOOLEAN := TRUE,
      clob_columns_in IN varchar2_t,
      clob_pieces_in IN  varchar2_t
      )
   IS
       l_REFERENCES clob;
      i                PLS_INTEGER;
      current_column varchar2(30) := '';
      l_rows PLS_INTEGER;
   BEGIN

      /* Be aware, PLSQL limitations on temporary clobs prevent multiple CLOB
         variables from pointing to the same temporary CLOB (it creates copies).
         Otherwise, the code could have been written a bit more compactly. */

      FOR i IN clob_columns_in.FIRST .. clob_columns_in.LAST
      LOOP
         /* Even when all clobs are null, DOA must send 1 row so skip it. */
         IF clob_columns_in (i) IS NOT NULL
         THEN
            IF current_column <> clob_columns_in (i) OR current_column IS NULL
            THEN
               current_column := LOWER (clob_columns_in (i));

               CASE current_column
                  WHEN 'references_in'
                  THEN
                    IF l_REFERENCES IS NULL
                    THEN
                      DBMS_LOB.createtemporary (
                         l_REFERENCES, TRUE, DBMS_LOB.CALL);
                    END IF;
               END CASE;
            END IF;

            CASE current_column
              WHEN 'references_in'
              THEN
                  DBMS_LOB.writeappend (l_REFERENCES
                                      , LENGTH (clob_pieces_in (i))
                                      , clob_pieces_in (i)
                                       );
            END CASE;
         END IF;
      END LOOP;

      upd (
         id_nic_intervention_in => id_nic_intervention_in,
         id_terminology_version_in => id_terminology_version_in,
         intervention_code_in => intervention_code_in,
         references_in => l_REFERENCES,
         id_inst_owner_in => id_inst_owner_in,
         id_concept_version_in => id_concept_version_in,
         id_concept_term_in => id_concept_term_in,
         create_user_in => create_user_in,
         create_time_in => create_time_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in,
         handle_error_in => handle_error_in
         );
   END n_upd_clobs_in_chunks;

   PROCEDURE n_upd_ins_clobs_in_chunks (
      id_nic_intervention_in IN NIC_INTERVENTION.ID_NIC_INTERVENTION%TYPE,
      id_terminology_version_in IN NIC_INTERVENTION.ID_TERMINOLOGY_VERSION%TYPE DEFAULT NULL,
      intervention_code_in IN NIC_INTERVENTION.INTERVENTION_CODE%TYPE DEFAULT NULL,
      id_inst_owner_in IN NIC_INTERVENTION.ID_INST_OWNER%TYPE DEFAULT NULL,
      id_concept_version_in IN NIC_INTERVENTION.ID_CONCEPT_VERSION%TYPE DEFAULT NULL,
      id_concept_term_in IN NIC_INTERVENTION.ID_CONCEPT_TERM%TYPE DEFAULT NULL,
      create_user_in IN NIC_INTERVENTION.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN NIC_INTERVENTION.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN NIC_INTERVENTION.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN NIC_INTERVENTION.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN NIC_INTERVENTION.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN NIC_INTERVENTION.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      ignore_if_null_in IN BOOLEAN DEFAULT TRUE,
      handle_error_in IN BOOLEAN DEFAULT TRUE,
      clob_columns_in IN varchar2_t,
      clob_pieces_in IN  varchar2_t
      )
   IS
   BEGIN
      n_upd_clobs_in_chunks (
         id_nic_intervention_in => id_nic_intervention_in,
         id_terminology_version_in => id_terminology_version_in,
         intervention_code_in => intervention_code_in,
         id_inst_owner_in => id_inst_owner_in,
         id_concept_version_in => id_concept_version_in,
         id_concept_term_in => id_concept_term_in,
         create_user_in => create_user_in,
         create_time_in => create_time_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in,
         clob_columns_in => clob_columns_in ,
         clob_pieces_in => clob_pieces_in ,
         ignore_if_null_in => ignore_if_null_in,
         handle_error_in => handle_error_in
       );

      IF SQL%ROWCOUNT = 0
      THEN
         n_ins_clobs_in_chunks (
            id_nic_intervention_in => id_nic_intervention_in,
            id_terminology_version_in => id_terminology_version_in,
            intervention_code_in => intervention_code_in,
            id_inst_owner_in => id_inst_owner_in,
            id_concept_version_in => id_concept_version_in,
            id_concept_term_in => id_concept_term_in,
            create_user_in => create_user_in,
            create_time_in => create_time_in,
            create_institution_in => create_institution_in,
            update_user_in => update_user_in,
            update_time_in => update_time_in,
            update_institution_in => update_institution_in,
            clob_columns_in => clob_columns_in ,
            clob_pieces_in => clob_pieces_in ,
            handle_error_in => handle_error_in
         );
      END IF;
   END n_upd_ins_clobs_in_chunks;

   /*
   END Special logic for handling LOB columns.
   */

   PROCEDURE ins (
      rec_in IN NIC_INTERVENTION%ROWTYPE
     ,gen_pky_in IN BOOLEAN DEFAULT FALSE
     ,sequence_in IN VARCHAR2 := NULL
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
      l_rec NIC_INTERVENTION%ROWTYPE := rec_in;
   BEGIN
      IF gen_pky_in THEN
         l_rec.ID_NIC_INTERVENTION := next_key (sequence_in);
      END IF;
      ins (
         id_nic_intervention_in => l_rec.ID_NIC_INTERVENTION
         ,
         id_terminology_version_in => l_rec.ID_TERMINOLOGY_VERSION,
         intervention_code_in => l_rec.INTERVENTION_CODE,
         references_in => l_rec.REFERENCES,
         id_inst_owner_in => l_rec.ID_INST_OWNER,
         id_concept_version_in => l_rec.ID_CONCEPT_VERSION,
         id_concept_term_in => l_rec.ID_CONCEPT_TERM,
         create_user_in => l_rec.CREATE_USER,
         create_time_in => l_rec.CREATE_TIME,
         create_institution_in => l_rec.CREATE_INSTITUTION,
         update_user_in => l_rec.UPDATE_USER,
         update_time_in => l_rec.UPDATE_TIME,
         update_institution_in => l_rec.UPDATE_INSTITUTION
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
   END ins;

   PROCEDURE ins (
      rec_in IN NIC_INTERVENTION%ROWTYPE
     ,gen_pky_in IN BOOLEAN DEFAULT FALSE
     ,sequence_in IN VARCHAR2 := NULL
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
      rows_out TABLE_VARCHAR;
   BEGIN

  ins (
      rec_in => rec_in
     ,gen_pky_in => gen_pky_in
     ,sequence_in => sequence_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
   );

   END ins;

   FUNCTION next_key (sequence_in IN VARCHAR2 := NULL) RETURN NIC_INTERVENTION.ID_NIC_INTERVENTION%TYPE

   IS
     retval NIC_INTERVENTION.ID_NIC_INTERVENTION%TYPE;

   BEGIN
      IF sequence_in IS NULL
      THEN
         SELECT seq_NIC_INTERVENTION.NEXTVAL INTO retval FROM dual;
      ELSE
         EXECUTE IMMEDIATE
            'SELECT ' || sequence_in || '.NEXTVAL FROM dual'
            INTO retval;
      END IF;
      RETURN retval;
   EXCEPTION
      WHEN OTHERS THEN
        pk_alert_exceptions.raise_error (
           error_name_in => 'SEQUENCE-GENERATION-FAILURE'
           ,name1_in => 'SEQUENCE'
           ,value1_in => NVL (sequence_in, 'seq_NIC_INTERVENTION')
           );
   END next_key;

   PROCEDURE ins (
      id_terminology_version_in IN NIC_INTERVENTION.ID_TERMINOLOGY_VERSION%TYPE DEFAULT NULL,
      intervention_code_in IN NIC_INTERVENTION.INTERVENTION_CODE%TYPE DEFAULT NULL,
      references_in IN NIC_INTERVENTION.REFERENCES%TYPE DEFAULT NULL,
      id_inst_owner_in IN NIC_INTERVENTION.ID_INST_OWNER%TYPE DEFAULT NULL,
      id_concept_version_in IN NIC_INTERVENTION.ID_CONCEPT_VERSION%TYPE DEFAULT NULL,
      id_concept_term_in IN NIC_INTERVENTION.ID_CONCEPT_TERM%TYPE DEFAULT NULL,
      create_user_in IN NIC_INTERVENTION.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN NIC_INTERVENTION.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN NIC_INTERVENTION.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN NIC_INTERVENTION.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN NIC_INTERVENTION.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN NIC_INTERVENTION.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      id_nic_intervention_out IN OUT NIC_INTERVENTION.ID_NIC_INTERVENTION%TYPE
      ,handle_error_in IN BOOLEAN := TRUE
      , rows_out OUT TABLE_VARCHAR
   )
   IS
        l_pky NIC_INTERVENTION.ID_NIC_INTERVENTION%TYPE := next_key;
   BEGIN
      ins (
         id_nic_intervention_in => l_pky,
         id_terminology_version_in => id_terminology_version_in,
         intervention_code_in => intervention_code_in,
         references_in => references_in,
         id_inst_owner_in => id_inst_owner_in,
         id_concept_version_in => id_concept_version_in,
         id_concept_term_in => id_concept_term_in,
         create_user_in => create_user_in,
         create_time_in => create_time_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
      id_nic_intervention_out := l_pky;
   END ins;

   PROCEDURE ins (
      id_terminology_version_in IN NIC_INTERVENTION.ID_TERMINOLOGY_VERSION%TYPE DEFAULT NULL,
      intervention_code_in IN NIC_INTERVENTION.INTERVENTION_CODE%TYPE DEFAULT NULL,
      references_in IN NIC_INTERVENTION.REFERENCES%TYPE DEFAULT NULL,
      id_inst_owner_in IN NIC_INTERVENTION.ID_INST_OWNER%TYPE DEFAULT NULL,
      id_concept_version_in IN NIC_INTERVENTION.ID_CONCEPT_VERSION%TYPE DEFAULT NULL,
      id_concept_term_in IN NIC_INTERVENTION.ID_CONCEPT_TERM%TYPE DEFAULT NULL,
      create_user_in IN NIC_INTERVENTION.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN NIC_INTERVENTION.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN NIC_INTERVENTION.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN NIC_INTERVENTION.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN NIC_INTERVENTION.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN NIC_INTERVENTION.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      id_nic_intervention_out IN OUT NIC_INTERVENTION.ID_NIC_INTERVENTION%TYPE
      ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
        rows_out TABLE_VARCHAR;
   BEGIN
      ins (
      id_terminology_version_in => id_terminology_version_in,
      intervention_code_in => intervention_code_in,
      references_in => references_in,
      id_inst_owner_in => id_inst_owner_in,
      id_concept_version_in => id_concept_version_in,
      id_concept_term_in => id_concept_term_in,
      create_user_in => create_user_in,
      create_time_in => create_time_in,
      create_institution_in => create_institution_in,
      update_user_in => update_user_in,
      update_time_in => update_time_in,
      update_institution_in => update_institution_in,
      id_nic_intervention_out => id_nic_intervention_out
      ,handle_error_in => handle_error_in
      , rows_out => rows_out
   );
   END ins;

   FUNCTION ins (
      id_terminology_version_in IN NIC_INTERVENTION.ID_TERMINOLOGY_VERSION%TYPE DEFAULT NULL,
      intervention_code_in IN NIC_INTERVENTION.INTERVENTION_CODE%TYPE DEFAULT NULL,
      references_in IN NIC_INTERVENTION.REFERENCES%TYPE DEFAULT NULL,
      id_inst_owner_in IN NIC_INTERVENTION.ID_INST_OWNER%TYPE DEFAULT NULL,
      id_concept_version_in IN NIC_INTERVENTION.ID_CONCEPT_VERSION%TYPE DEFAULT NULL,
      id_concept_term_in IN NIC_INTERVENTION.ID_CONCEPT_TERM%TYPE DEFAULT NULL,
      create_user_in IN NIC_INTERVENTION.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN NIC_INTERVENTION.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN NIC_INTERVENTION.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN NIC_INTERVENTION.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN NIC_INTERVENTION.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN NIC_INTERVENTION.UPDATE_INSTITUTION%TYPE DEFAULT NULL
      ,handle_error_in IN BOOLEAN := TRUE
      , rows_out OUT TABLE_VARCHAR
   )
      RETURN
         NIC_INTERVENTION.ID_NIC_INTERVENTION%TYPE
   IS
        l_pky NIC_INTERVENTION.ID_NIC_INTERVENTION%TYPE := next_key;
   BEGIN
      ins (
         id_nic_intervention_in => l_pky,
         id_terminology_version_in => id_terminology_version_in,
         intervention_code_in => intervention_code_in,
         references_in => references_in,
         id_inst_owner_in => id_inst_owner_in,
         id_concept_version_in => id_concept_version_in,
         id_concept_term_in => id_concept_term_in,
         create_user_in => create_user_in,
         create_time_in => create_time_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
      RETURN l_pky;
   END ins;

   FUNCTION ins (
      id_terminology_version_in IN NIC_INTERVENTION.ID_TERMINOLOGY_VERSION%TYPE DEFAULT NULL,
      intervention_code_in IN NIC_INTERVENTION.INTERVENTION_CODE%TYPE DEFAULT NULL,
      references_in IN NIC_INTERVENTION.REFERENCES%TYPE DEFAULT NULL,
      id_inst_owner_in IN NIC_INTERVENTION.ID_INST_OWNER%TYPE DEFAULT NULL,
      id_concept_version_in IN NIC_INTERVENTION.ID_CONCEPT_VERSION%TYPE DEFAULT NULL,
      id_concept_term_in IN NIC_INTERVENTION.ID_CONCEPT_TERM%TYPE DEFAULT NULL,
      create_user_in IN NIC_INTERVENTION.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN NIC_INTERVENTION.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN NIC_INTERVENTION.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN NIC_INTERVENTION.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN NIC_INTERVENTION.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN NIC_INTERVENTION.UPDATE_INSTITUTION%TYPE DEFAULT NULL
      ,handle_error_in IN BOOLEAN := TRUE
   )
      RETURN
         NIC_INTERVENTION.ID_NIC_INTERVENTION%TYPE
   IS
        l_pky NIC_INTERVENTION.ID_NIC_INTERVENTION%TYPE := next_key;
    rows_out TABLE_VARCHAR;
   BEGIN
      ins (
         id_nic_intervention_in => l_pky,
         id_terminology_version_in => id_terminology_version_in,
         intervention_code_in => intervention_code_in,
         references_in => references_in,
         id_inst_owner_in => id_inst_owner_in,
         id_concept_version_in => id_concept_version_in,
         id_concept_term_in => id_concept_term_in,
         create_user_in => create_user_in,
         create_time_in => create_time_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
      RETURN l_pky;
   END ins;

      PROCEDURE ins (
      id_terminology_version_in IN NIC_INTERVENTION.ID_TERMINOLOGY_VERSION%TYPE DEFAULT NULL,
      intervention_code_in IN NIC_INTERVENTION.INTERVENTION_CODE%TYPE DEFAULT NULL,
      references_in IN NIC_INTERVENTION.REFERENCES%TYPE DEFAULT NULL,
      id_inst_owner_in IN NIC_INTERVENTION.ID_INST_OWNER%TYPE DEFAULT NULL,
      id_concept_version_in IN NIC_INTERVENTION.ID_CONCEPT_VERSION%TYPE DEFAULT NULL,
      id_concept_term_in IN NIC_INTERVENTION.ID_CONCEPT_TERM%TYPE DEFAULT NULL,
      create_user_in IN NIC_INTERVENTION.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN NIC_INTERVENTION.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN NIC_INTERVENTION.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN NIC_INTERVENTION.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN NIC_INTERVENTION.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN NIC_INTERVENTION.UPDATE_INSTITUTION%TYPE DEFAULT NULL
      ,handle_error_in IN BOOLEAN := TRUE
      , rows_out OUT TABLE_VARCHAR
   )
   IS
        l_pky NIC_INTERVENTION.ID_NIC_INTERVENTION%TYPE := next_key;
   BEGIN
      ins (
         id_nic_intervention_in => l_pky,
         id_terminology_version_in => id_terminology_version_in,
         intervention_code_in => intervention_code_in,
         references_in => references_in,
         id_inst_owner_in => id_inst_owner_in,
         id_concept_version_in => id_concept_version_in,
         id_concept_term_in => id_concept_term_in,
         create_user_in => create_user_in,
         create_time_in => create_time_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
   END ins;


     PROCEDURE ins (
      id_terminology_version_in IN NIC_INTERVENTION.ID_TERMINOLOGY_VERSION%TYPE DEFAULT NULL,
      intervention_code_in IN NIC_INTERVENTION.INTERVENTION_CODE%TYPE DEFAULT NULL,
      references_in IN NIC_INTERVENTION.REFERENCES%TYPE DEFAULT NULL,
      id_inst_owner_in IN NIC_INTERVENTION.ID_INST_OWNER%TYPE DEFAULT NULL,
      id_concept_version_in IN NIC_INTERVENTION.ID_CONCEPT_VERSION%TYPE DEFAULT NULL,
      id_concept_term_in IN NIC_INTERVENTION.ID_CONCEPT_TERM%TYPE DEFAULT NULL,
      create_user_in IN NIC_INTERVENTION.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN NIC_INTERVENTION.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN NIC_INTERVENTION.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN NIC_INTERVENTION.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN NIC_INTERVENTION.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN NIC_INTERVENTION.UPDATE_INSTITUTION%TYPE DEFAULT NULL
      ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
        l_pky NIC_INTERVENTION.ID_NIC_INTERVENTION%TYPE := next_key;
      rows_out TABLE_VARCHAR;
   BEGIN
      ins (
         id_nic_intervention_in => l_pky,
         id_terminology_version_in => id_terminology_version_in,
         intervention_code_in => intervention_code_in,
         references_in => references_in,
         id_inst_owner_in => id_inst_owner_in,
         id_concept_version_in => id_concept_version_in,
         id_concept_term_in => id_concept_term_in,
         create_user_in => create_user_in,
         create_time_in => create_time_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
   END ins;

    PROCEDURE ins (
      rows_in IN NIC_INTERVENTION_tc
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
   BEGIN
      IF rows_in.COUNT = 0
      THEN
         NULL;
      ELSE
         FORALL indx IN rows_in.FIRST .. rows_in.LAST
            SAVE EXCEPTIONS
            INSERT INTO NIC_INTERVENTION VALUES rows_in (indx) RETURNING ROWID BULK COLLECT INTO rows_out;
      END IF;
   EXCEPTION
     WHEN e_forall_error
     THEN
        -- In Oracle9i and above, SAVE EXCEPTIONS will direct control
        -- here if any error occurs. We can then save all the error
        -- information out to the error instance.
       IF NOT handle_error_in THEN RAISE;
       ELSE
          <<bulk_handler>>
          DECLARE
             l_err_instance_id NUMBER;
          BEGIN
             -- For each error, write to the log.
             FOR indx IN 1 .. SQL%BULK_EXCEPTIONS.COUNT
             LOOP
                pk_alert_exceptions.register_error (
                    error_name_in => 'FORALL-INSERT-FAILURE'
                   ,err_instance_id_out => l_err_instance_id
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'BINDING_ROW_' || indx
                  ,value_in => SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX
                  ,validate_in => FALSE
                );
                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ERROR_AT_ROW_' || indx
                  ,value_in => SQLERRM (-SQL%BULK_EXCEPTIONS (indx).ERROR_CODE)
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_NIC_INTERVENTION _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).ID_NIC_INTERVENTION
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_TERMINOLOGY_VERSION _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).ID_TERMINOLOGY_VERSION
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'INTERVENTION_CODE _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).INTERVENTION_CODE
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_INST_OWNER _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).ID_INST_OWNER
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_CONCEPT_VERSION _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).ID_CONCEPT_VERSION
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_CONCEPT_TERM _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).ID_CONCEPT_TERM
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'CREATE_USER _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).CREATE_USER
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'CREATE_TIME _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).CREATE_TIME
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'CREATE_INSTITUTION _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).CREATE_INSTITUTION
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'UPDATE_USER _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).UPDATE_USER
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'UPDATE_TIME _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).UPDATE_TIME
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'UPDATE_INSTITUTION _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).UPDATE_INSTITUTION
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.raise_error_instance( err_instance_id_in => l_err_instance_id );
             END LOOP;
          END bulk_handler;
        END IF;
     WHEN OTHERS
     THEN
       IF NOT handle_error_in THEN RAISE;
       ELSE
       pk_alert_exceptions.raise_error(
          error_name_in => 'FORALL-INSERT-FAILURE'
          ,name1_in => 'TABLE_NAME'
          ,value1_in => 'NIC_INTERVENTION'
          ,name2_in => 'ROW_COUNT'
          ,value2_in => rows_in.COUNT
           );
       END IF;
   END ins;

    PROCEDURE ins (
      rows_in IN NIC_INTERVENTION_tc
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
      rows_out TABLE_VARCHAR;
   BEGIN
      ins (
      rows_in => rows_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
   );
   END ins;


PROCEDURE upd (
      id_nic_intervention_in IN NIC_INTERVENTION.ID_NIC_INTERVENTION%TYPE,
      id_terminology_version_in IN NIC_INTERVENTION.ID_TERMINOLOGY_VERSION%TYPE DEFAULT NULL,
      ID_TERMINOLOGY_VERSION_nin IN BOOLEAN := TRUE,
      intervention_code_in IN NIC_INTERVENTION.INTERVENTION_CODE%TYPE DEFAULT NULL,
      INTERVENTION_CODE_nin IN BOOLEAN := TRUE,
      references_in IN NIC_INTERVENTION.REFERENCES%TYPE DEFAULT NULL,
      REFERENCES_nin IN BOOLEAN := TRUE,
      id_inst_owner_in IN NIC_INTERVENTION.ID_INST_OWNER%TYPE DEFAULT NULL,
      ID_INST_OWNER_nin IN BOOLEAN := TRUE,
      id_concept_version_in IN NIC_INTERVENTION.ID_CONCEPT_VERSION%TYPE DEFAULT NULL,
      ID_CONCEPT_VERSION_nin IN BOOLEAN := TRUE,
      id_concept_term_in IN NIC_INTERVENTION.ID_CONCEPT_TERM%TYPE DEFAULT NULL,
      ID_CONCEPT_TERM_nin IN BOOLEAN := TRUE,
      create_user_in IN NIC_INTERVENTION.CREATE_USER%TYPE DEFAULT NULL,
      CREATE_USER_nin IN BOOLEAN := TRUE,
      create_time_in IN NIC_INTERVENTION.CREATE_TIME%TYPE DEFAULT NULL,
      CREATE_TIME_nin IN BOOLEAN := TRUE,
      create_institution_in IN NIC_INTERVENTION.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      CREATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      update_user_in IN NIC_INTERVENTION.UPDATE_USER%TYPE DEFAULT NULL,
      UPDATE_USER_nin IN BOOLEAN := TRUE,
      update_time_in IN NIC_INTERVENTION.UPDATE_TIME%TYPE DEFAULT NULL,
      UPDATE_TIME_nin IN BOOLEAN := TRUE,
      update_institution_in IN NIC_INTERVENTION.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      UPDATE_INSTITUTION_nin IN BOOLEAN := TRUE,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      )
   IS
   l_rows_out TABLE_VARCHAR;
   l_ID_TERMINOLOGY_VERSION_n NUMBER(1);
   l_INTERVENTION_CODE_n NUMBER(1);
   l_REFERENCES_n NUMBER(1);
   l_ID_INST_OWNER_n NUMBER(1);
   l_ID_CONCEPT_VERSION_n NUMBER(1);
   l_ID_CONCEPT_TERM_n NUMBER(1);
   l_CREATE_USER_n NUMBER(1);
   l_CREATE_TIME_n NUMBER(1);
   l_CREATE_INSTITUTION_n NUMBER(1);
   l_UPDATE_USER_n NUMBER(1);
   l_UPDATE_TIME_n NUMBER(1);
   l_UPDATE_INSTITUTION_n NUMBER(1);
   BEGIN

   l_ID_TERMINOLOGY_VERSION_n := sys.diutil.bool_to_int(ID_TERMINOLOGY_VERSION_nin);
   l_INTERVENTION_CODE_n := sys.diutil.bool_to_int(INTERVENTION_CODE_nin);
   l_REFERENCES_n := sys.diutil.bool_to_int(REFERENCES_nin);
   l_ID_INST_OWNER_n := sys.diutil.bool_to_int(ID_INST_OWNER_nin);
   l_ID_CONCEPT_VERSION_n := sys.diutil.bool_to_int(ID_CONCEPT_VERSION_nin);
   l_ID_CONCEPT_TERM_n := sys.diutil.bool_to_int(ID_CONCEPT_TERM_nin);
   l_CREATE_USER_n := sys.diutil.bool_to_int(CREATE_USER_nin);
   l_CREATE_TIME_n := sys.diutil.bool_to_int(CREATE_TIME_nin);
   l_CREATE_INSTITUTION_n := sys.diutil.bool_to_int(CREATE_INSTITUTION_nin);
   l_UPDATE_USER_n := sys.diutil.bool_to_int(UPDATE_USER_nin);
   l_UPDATE_TIME_n := sys.diutil.bool_to_int(UPDATE_TIME_nin);
   l_UPDATE_INSTITUTION_n := sys.diutil.bool_to_int(UPDATE_INSTITUTION_nin);


         UPDATE NIC_INTERVENTION SET
     ID_TERMINOLOGY_VERSION = decode (l_ID_TERMINOLOGY_VERSION_n,0,id_terminology_version_in, NVL (id_terminology_version_in, ID_TERMINOLOGY_VERSION)),
     INTERVENTION_CODE = decode (l_INTERVENTION_CODE_n,0,intervention_code_in, NVL (intervention_code_in, INTERVENTION_CODE)),
     REFERENCES = decode (l_REFERENCES_n,0,references_in, NVL (references_in, REFERENCES)),
     ID_INST_OWNER = decode (l_ID_INST_OWNER_n,0,id_inst_owner_in, NVL (id_inst_owner_in, ID_INST_OWNER)),
     ID_CONCEPT_VERSION = decode (l_ID_CONCEPT_VERSION_n,0,id_concept_version_in, NVL (id_concept_version_in, ID_CONCEPT_VERSION)),
     ID_CONCEPT_TERM = decode (l_ID_CONCEPT_TERM_n,0,id_concept_term_in, NVL (id_concept_term_in, ID_CONCEPT_TERM)),
     CREATE_USER = decode (l_CREATE_USER_n,0,create_user_in, NVL (create_user_in, CREATE_USER)),
     CREATE_TIME = decode (l_CREATE_TIME_n,0,create_time_in, NVL (create_time_in, CREATE_TIME)),
     CREATE_INSTITUTION = decode (l_CREATE_INSTITUTION_n,0,create_institution_in, NVL (create_institution_in, CREATE_INSTITUTION)),
     UPDATE_USER = decode (l_UPDATE_USER_n,0,update_user_in, NVL (update_user_in, UPDATE_USER)),
     UPDATE_TIME = decode (l_UPDATE_TIME_n,0,update_time_in, NVL (update_time_in, UPDATE_TIME)),
     UPDATE_INSTITUTION = decode (l_UPDATE_INSTITUTION_n,0,update_institution_in, NVL (update_institution_in, UPDATE_INSTITUTION))
          WHERE
             ID_NIC_INTERVENTION = id_nic_intervention_in
         RETURNING ROWID BULK COLLECT INTO l_rows_out;


if(rows_out is null)
then
rows_out := table_varchar();
end if;

rows_out :=  rows_out MULTISET UNION DISTINCT l_rows_out;

   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF FALSE THEN NULL; -- Placeholder in case no unique indexes
           ELSIF l_name = 'NIC_INTERVENTION_UK'
           THEN
              pk_alert_exceptions.register_error (
                 error_name_in => 'DUPLICATE-VALUE'
                ,err_instance_id_out => l_err_instance_id
                ,text_in => 'Violation of unique index "NIC_INTERVENTION_UK"'
                );
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_err_instance_id
                ,NAME_IN => 'OWNER'
                ,value_in => l_owner
                ,validate_in => FALSE
                );
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_err_instance_id
                ,NAME_IN => 'CONSTRAINT_NAME'
                ,value_in => l_name
                ,validate_in => FALSE
                );
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_err_instance_id
                ,NAME_IN => 'TABLE_NAME'
                ,value_in => 'NIC_INTERVENTION'
                ,validate_in => FALSE
                );
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_err_instance_id
                ,NAME_IN => 'ID_TERMINOLOGY_VERSION'
                ,value_in => id_terminology_version_in
                ,validate_in => FALSE
                );
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_err_instance_id
                ,NAME_IN => 'INTERVENTION_CODE'
                ,value_in => intervention_code_in
                ,validate_in => FALSE
                );
              pk_alert_exceptions.raise_error_instance (
                    err_instance_id_in => l_err_instance_id);
           ELSE
              pk_alert_exceptions.raise_error (
                    error_name_in => 'DUPLICATE-VALUE'
                    ,name1_in => 'OWNER'
                    ,value1_in => l_owner
                    ,name2_in => 'CONSTRAINT_NAME'
                    ,value2_in => l_name
                    ,name3_in => 'TABLE_NAME'
                    ,value3_in => 'NIC_INTERVENTION');
           END IF;
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'NIC_INTERVENTION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'NIC_INTERVENTION');
           IF l_name = 'NICI_CNCPTTR_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_CONCEPT_TERM'
               , value_in => id_concept_term_in);
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_INST_OWNER'
               , value_in => id_inst_owner_in);
           END IF;
           IF l_name = 'NICI_CNCPTV_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_CONCEPT_VERSION'
               , value_in => id_concept_version_in);
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_INST_OWNER'
               , value_in => id_inst_owner_in);
           END IF;
           IF l_name = 'NICI_NIC_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'INTERVENTION_CODE'
               , value_in => intervention_code_in);
           END IF;
           IF l_name = 'NICI_TERMV_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_TERMINOLOGY_VERSION'
               , value_in => id_terminology_version_in);
           END IF;
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END upd;


   PROCEDURE upd (
      id_nic_intervention_in IN NIC_INTERVENTION.ID_NIC_INTERVENTION%TYPE,
      id_terminology_version_in IN NIC_INTERVENTION.ID_TERMINOLOGY_VERSION%TYPE DEFAULT NULL,
      ID_TERMINOLOGY_VERSION_nin IN BOOLEAN := TRUE,
      intervention_code_in IN NIC_INTERVENTION.INTERVENTION_CODE%TYPE DEFAULT NULL,
      INTERVENTION_CODE_nin IN BOOLEAN := TRUE,
      references_in IN NIC_INTERVENTION.REFERENCES%TYPE DEFAULT NULL,
      REFERENCES_nin IN BOOLEAN := TRUE,
      id_inst_owner_in IN NIC_INTERVENTION.ID_INST_OWNER%TYPE DEFAULT NULL,
      ID_INST_OWNER_nin IN BOOLEAN := TRUE,
      id_concept_version_in IN NIC_INTERVENTION.ID_CONCEPT_VERSION%TYPE DEFAULT NULL,
      ID_CONCEPT_VERSION_nin IN BOOLEAN := TRUE,
      id_concept_term_in IN NIC_INTERVENTION.ID_CONCEPT_TERM%TYPE DEFAULT NULL,
      ID_CONCEPT_TERM_nin IN BOOLEAN := TRUE,
      create_user_in IN NIC_INTERVENTION.CREATE_USER%TYPE DEFAULT NULL,
      CREATE_USER_nin IN BOOLEAN := TRUE,
      create_time_in IN NIC_INTERVENTION.CREATE_TIME%TYPE DEFAULT NULL,
      CREATE_TIME_nin IN BOOLEAN := TRUE,
      create_institution_in IN NIC_INTERVENTION.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      CREATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      update_user_in IN NIC_INTERVENTION.UPDATE_USER%TYPE DEFAULT NULL,
      UPDATE_USER_nin IN BOOLEAN := TRUE,
      update_time_in IN NIC_INTERVENTION.UPDATE_TIME%TYPE DEFAULT NULL,
      UPDATE_TIME_nin IN BOOLEAN := TRUE,
      update_institution_in IN NIC_INTERVENTION.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      UPDATE_INSTITUTION_nin IN BOOLEAN := TRUE,
     handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
     upd (
      id_nic_intervention_in => id_nic_intervention_in,
      id_terminology_version_in => id_terminology_version_in,
      ID_TERMINOLOGY_VERSION_nin => ID_TERMINOLOGY_VERSION_nin,
      intervention_code_in => intervention_code_in,
      INTERVENTION_CODE_nin => INTERVENTION_CODE_nin,
      references_in => references_in,
      REFERENCES_nin => REFERENCES_nin,
      id_inst_owner_in => id_inst_owner_in,
      ID_INST_OWNER_nin => ID_INST_OWNER_nin,
      id_concept_version_in => id_concept_version_in,
      ID_CONCEPT_VERSION_nin => ID_CONCEPT_VERSION_nin,
      id_concept_term_in => id_concept_term_in,
      ID_CONCEPT_TERM_nin => ID_CONCEPT_TERM_nin,
      create_user_in => create_user_in,
      CREATE_USER_nin => CREATE_USER_nin,
      create_time_in => create_time_in,
      CREATE_TIME_nin => CREATE_TIME_nin,
      create_institution_in => create_institution_in,
      CREATE_INSTITUTION_nin => CREATE_INSTITUTION_nin,
      update_user_in => update_user_in,
      UPDATE_USER_nin => UPDATE_USER_nin,
      update_time_in => update_time_in,
      UPDATE_TIME_nin => UPDATE_TIME_nin,
      update_institution_in => update_institution_in,
      UPDATE_INSTITUTION_nin => UPDATE_INSTITUTION_nin,
     handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END upd;

PROCEDURE upd (
      id_terminology_version_in IN NIC_INTERVENTION.ID_TERMINOLOGY_VERSION%TYPE DEFAULT NULL,
      ID_TERMINOLOGY_VERSION_nin IN BOOLEAN := TRUE,
      intervention_code_in IN NIC_INTERVENTION.INTERVENTION_CODE%TYPE DEFAULT NULL,
      INTERVENTION_CODE_nin IN BOOLEAN := TRUE,
      references_in IN NIC_INTERVENTION.REFERENCES%TYPE DEFAULT NULL,
      REFERENCES_nin IN BOOLEAN := TRUE,
      id_inst_owner_in IN NIC_INTERVENTION.ID_INST_OWNER%TYPE DEFAULT NULL,
      ID_INST_OWNER_nin IN BOOLEAN := TRUE,
      id_concept_version_in IN NIC_INTERVENTION.ID_CONCEPT_VERSION%TYPE DEFAULT NULL,
      ID_CONCEPT_VERSION_nin IN BOOLEAN := TRUE,
      id_concept_term_in IN NIC_INTERVENTION.ID_CONCEPT_TERM%TYPE DEFAULT NULL,
      ID_CONCEPT_TERM_nin IN BOOLEAN := TRUE,
      create_user_in IN NIC_INTERVENTION.CREATE_USER%TYPE DEFAULT NULL,
      CREATE_USER_nin IN BOOLEAN := TRUE,
      create_time_in IN NIC_INTERVENTION.CREATE_TIME%TYPE DEFAULT NULL,
      CREATE_TIME_nin IN BOOLEAN := TRUE,
      create_institution_in IN NIC_INTERVENTION.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      CREATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      update_user_in IN NIC_INTERVENTION.UPDATE_USER%TYPE DEFAULT NULL,
      UPDATE_USER_nin IN BOOLEAN := TRUE,
      update_time_in IN NIC_INTERVENTION.UPDATE_TIME%TYPE DEFAULT NULL,
      UPDATE_TIME_nin IN BOOLEAN := TRUE,
      update_institution_in IN NIC_INTERVENTION.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      UPDATE_INSTITUTION_nin IN BOOLEAN := TRUE,
    where_in varchar2,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      )
   IS
   l_sql VARCHAR2(32767);
   l_rows_out TABLE_VARCHAR;
   l_ID_TERMINOLOGY_VERSION_n NUMBER(1);
   l_INTERVENTION_CODE_n NUMBER(1);
   l_REFERENCES_n NUMBER(1);
   l_ID_INST_OWNER_n NUMBER(1);
   l_ID_CONCEPT_VERSION_n NUMBER(1);
   l_ID_CONCEPT_TERM_n NUMBER(1);
   l_CREATE_USER_n NUMBER(1);
   l_CREATE_TIME_n NUMBER(1);
   l_CREATE_INSTITUTION_n NUMBER(1);
   l_UPDATE_USER_n NUMBER(1);
   l_UPDATE_TIME_n NUMBER(1);
   l_UPDATE_INSTITUTION_n NUMBER(1);
      id_nic_intervention_in NIC_INTERVENTION.ID_NIC_INTERVENTION%TYPE;
   BEGIN



      l_ID_TERMINOLOGY_VERSION_n := sys.diutil.bool_to_int(ID_TERMINOLOGY_VERSION_nin);
      l_INTERVENTION_CODE_n := sys.diutil.bool_to_int(INTERVENTION_CODE_nin);
      l_REFERENCES_n := sys.diutil.bool_to_int(REFERENCES_nin);
      l_ID_INST_OWNER_n := sys.diutil.bool_to_int(ID_INST_OWNER_nin);
      l_ID_CONCEPT_VERSION_n := sys.diutil.bool_to_int(ID_CONCEPT_VERSION_nin);
      l_ID_CONCEPT_TERM_n := sys.diutil.bool_to_int(ID_CONCEPT_TERM_nin);
      l_CREATE_USER_n := sys.diutil.bool_to_int(CREATE_USER_nin);
      l_CREATE_TIME_n := sys.diutil.bool_to_int(CREATE_TIME_nin);
      l_CREATE_INSTITUTION_n := sys.diutil.bool_to_int(CREATE_INSTITUTION_nin);
      l_UPDATE_USER_n := sys.diutil.bool_to_int(UPDATE_USER_nin);
      l_UPDATE_TIME_n := sys.diutil.bool_to_int(UPDATE_TIME_nin);
      l_UPDATE_INSTITUTION_n := sys.diutil.bool_to_int(UPDATE_INSTITUTION_nin);



l_sql := 'UPDATE NIC_INTERVENTION SET '
     || ' ID_TERMINOLOGY_VERSION = decode (' || l_ID_TERMINOLOGY_VERSION_n || ',0,:id_terminology_version_in, NVL (:id_terminology_version_in, ID_TERMINOLOGY_VERSION)) '|| ','
     || ' INTERVENTION_CODE = decode (' || l_INTERVENTION_CODE_n || ',0,:intervention_code_in, NVL (:intervention_code_in, INTERVENTION_CODE)) '|| ','
     || ' REFERENCES = decode (' || l_REFERENCES_n || ',0,:references_in, NVL (:references_in, REFERENCES)) '|| ','
     || ' ID_INST_OWNER = decode (' || l_ID_INST_OWNER_n || ',0,:id_inst_owner_in, NVL (:id_inst_owner_in, ID_INST_OWNER)) '|| ','
     || ' ID_CONCEPT_VERSION = decode (' || l_ID_CONCEPT_VERSION_n || ',0,:id_concept_version_in, NVL (:id_concept_version_in, ID_CONCEPT_VERSION)) '|| ','
     || ' ID_CONCEPT_TERM = decode (' || l_ID_CONCEPT_TERM_n || ',0,:id_concept_term_in, NVL (:id_concept_term_in, ID_CONCEPT_TERM)) '|| ','
     || ' CREATE_USER = decode (' || l_CREATE_USER_n || ',0,:create_user_in, NVL (:create_user_in, CREATE_USER)) '|| ','
     || ' CREATE_TIME = decode (' || l_CREATE_TIME_n || ',0,:create_time_in, NVL (:create_time_in, CREATE_TIME)) '|| ','
     || ' CREATE_INSTITUTION = decode (' || l_CREATE_INSTITUTION_n || ',0,:create_institution_in, NVL (:create_institution_in, CREATE_INSTITUTION)) '|| ','
     || ' UPDATE_USER = decode (' || l_UPDATE_USER_n || ',0,:update_user_in, NVL (:update_user_in, UPDATE_USER)) '|| ','
     || ' UPDATE_TIME = decode (' || l_UPDATE_TIME_n || ',0,:update_time_in, NVL (:update_time_in, UPDATE_TIME)) '|| ','
     || ' UPDATE_INSTITUTION = decode (' || l_UPDATE_INSTITUTION_n || ',0,:update_institution_in, NVL (:update_institution_in, UPDATE_INSTITUTION)) '
      || ' where ' || nvl(where_in,'(1=1)')
      || ' RETURNING ROWID BULK COLLECT INTO :l_rows_out';




execute immediate 'BEGIN ' || l_sql || '; END;' using in
     id_terminology_version_in,
     intervention_code_in,
     references_in,
     id_inst_owner_in,
     id_concept_version_in,
     id_concept_term_in,
     create_user_in,
     create_time_in,
     create_institution_in,
     update_user_in,
     update_time_in,
     update_institution_in,
    OUT l_rows_out;

if(rows_out is null)
then
rows_out := table_varchar();
end if;

rows_out :=  rows_out MULTISET UNION DISTINCT l_rows_out;

   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF FALSE THEN NULL; -- Placeholder in case no unique indexes
           ELSIF l_name = 'NIC_INTERVENTION_UK'
           THEN
              pk_alert_exceptions.register_error (
                 error_name_in => 'DUPLICATE-VALUE'
                ,err_instance_id_out => l_err_instance_id
                ,text_in => 'Violation of unique index "NIC_INTERVENTION_UK"'
                );
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_err_instance_id
                ,NAME_IN => 'OWNER'
                ,value_in => l_owner
                ,validate_in => FALSE
                );
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_err_instance_id
                ,NAME_IN => 'CONSTRAINT_NAME'
                ,value_in => l_name
                ,validate_in => FALSE
                );
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_err_instance_id
                ,NAME_IN => 'TABLE_NAME'
                ,value_in => 'NIC_INTERVENTION'
                ,validate_in => FALSE
                );
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_err_instance_id
                ,NAME_IN => 'ID_TERMINOLOGY_VERSION'
                ,value_in => id_terminology_version_in
                ,validate_in => FALSE
                );
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_err_instance_id
                ,NAME_IN => 'INTERVENTION_CODE'
                ,value_in => intervention_code_in
                ,validate_in => FALSE
                );
              pk_alert_exceptions.raise_error_instance (
                    err_instance_id_in => l_err_instance_id);
           ELSE
              pk_alert_exceptions.raise_error (
                    error_name_in => 'DUPLICATE-VALUE'
                    ,name1_in => 'OWNER'
                    ,value1_in => l_owner
                    ,name2_in => 'CONSTRAINT_NAME'
                    ,value2_in => l_name
                    ,name3_in => 'TABLE_NAME'
                    ,value3_in => 'NIC_INTERVENTION');
           END IF;
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'NIC_INTERVENTION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'NIC_INTERVENTION');
           IF l_name = 'NICI_CNCPTTR_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_CONCEPT_TERM'
               , value_in => id_concept_term_in);
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_INST_OWNER'
               , value_in => id_inst_owner_in);
           END IF;
           IF l_name = 'NICI_CNCPTV_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_CONCEPT_VERSION'
               , value_in => id_concept_version_in);
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_INST_OWNER'
               , value_in => id_inst_owner_in);
           END IF;
           IF l_name = 'NICI_NIC_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'INTERVENTION_CODE'
               , value_in => intervention_code_in);
           END IF;
           IF l_name = 'NICI_TERMV_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_TERMINOLOGY_VERSION'
               , value_in => id_terminology_version_in);
           END IF;
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END upd;





PROCEDURE upd (
      id_terminology_version_in IN NIC_INTERVENTION.ID_TERMINOLOGY_VERSION%TYPE DEFAULT NULL,
      ID_TERMINOLOGY_VERSION_nin IN BOOLEAN := TRUE,
      intervention_code_in IN NIC_INTERVENTION.INTERVENTION_CODE%TYPE DEFAULT NULL,
      INTERVENTION_CODE_nin IN BOOLEAN := TRUE,
      references_in IN NIC_INTERVENTION.REFERENCES%TYPE DEFAULT NULL,
      REFERENCES_nin IN BOOLEAN := TRUE,
      id_inst_owner_in IN NIC_INTERVENTION.ID_INST_OWNER%TYPE DEFAULT NULL,
      ID_INST_OWNER_nin IN BOOLEAN := TRUE,
      id_concept_version_in IN NIC_INTERVENTION.ID_CONCEPT_VERSION%TYPE DEFAULT NULL,
      ID_CONCEPT_VERSION_nin IN BOOLEAN := TRUE,
      id_concept_term_in IN NIC_INTERVENTION.ID_CONCEPT_TERM%TYPE DEFAULT NULL,
      ID_CONCEPT_TERM_nin IN BOOLEAN := TRUE,
      create_user_in IN NIC_INTERVENTION.CREATE_USER%TYPE DEFAULT NULL,
      CREATE_USER_nin IN BOOLEAN := TRUE,
      create_time_in IN NIC_INTERVENTION.CREATE_TIME%TYPE DEFAULT NULL,
      CREATE_TIME_nin IN BOOLEAN := TRUE,
      create_institution_in IN NIC_INTERVENTION.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      CREATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      update_user_in IN NIC_INTERVENTION.UPDATE_USER%TYPE DEFAULT NULL,
      UPDATE_USER_nin IN BOOLEAN := TRUE,
      update_time_in IN NIC_INTERVENTION.UPDATE_TIME%TYPE DEFAULT NULL,
      UPDATE_TIME_nin IN BOOLEAN := TRUE,
      update_institution_in IN NIC_INTERVENTION.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      UPDATE_INSTITUTION_nin IN BOOLEAN := TRUE,
    where_in varchar2,
     handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
      upd (
      id_terminology_version_in => id_terminology_version_in,
      ID_TERMINOLOGY_VERSION_nin => ID_TERMINOLOGY_VERSION_nin,
      intervention_code_in => intervention_code_in,
      INTERVENTION_CODE_nin => INTERVENTION_CODE_nin,
      references_in => references_in,
      REFERENCES_nin => REFERENCES_nin,
      id_inst_owner_in => id_inst_owner_in,
      ID_INST_OWNER_nin => ID_INST_OWNER_nin,
      id_concept_version_in => id_concept_version_in,
      ID_CONCEPT_VERSION_nin => ID_CONCEPT_VERSION_nin,
      id_concept_term_in => id_concept_term_in,
      ID_CONCEPT_TERM_nin => ID_CONCEPT_TERM_nin,
      create_user_in => create_user_in,
      CREATE_USER_nin => CREATE_USER_nin,
      create_time_in => create_time_in,
      CREATE_TIME_nin => CREATE_TIME_nin,
      create_institution_in => create_institution_in,
      CREATE_INSTITUTION_nin => CREATE_INSTITUTION_nin,
      update_user_in => update_user_in,
      UPDATE_USER_nin => UPDATE_USER_nin,
      update_time_in => update_time_in,
      UPDATE_TIME_nin => UPDATE_TIME_nin,
      update_institution_in => update_institution_in,
      UPDATE_INSTITUTION_nin => UPDATE_INSTITUTION_nin,
    where_in => where_in,
     handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END upd;

   PROCEDURE upd (
      rec_in IN NIC_INTERVENTION%ROWTYPE,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      upd (
         id_nic_intervention_in => rec_in.ID_NIC_INTERVENTION,
         id_terminology_version_in => rec_in.ID_TERMINOLOGY_VERSION,
         intervention_code_in => rec_in.INTERVENTION_CODE,
         references_in => rec_in.REFERENCES,
         id_inst_owner_in => rec_in.ID_INST_OWNER,
         id_concept_version_in => rec_in.ID_CONCEPT_VERSION,
         id_concept_term_in => rec_in.ID_CONCEPT_TERM,
         create_user_in => rec_in.CREATE_USER,
         create_time_in => rec_in.CREATE_TIME,
         create_institution_in => rec_in.CREATE_INSTITUTION,
         update_user_in => rec_in.UPDATE_USER,
         update_time_in => rec_in.UPDATE_TIME,
         update_institution_in => rec_in.UPDATE_INSTITUTION

        ,handle_error_in => handle_error_in
        , rows_out => rows_out
       );
   END upd;

   PROCEDURE upd (
      rec_in IN NIC_INTERVENTION%ROWTYPE,
     handle_error_in IN BOOLEAN := TRUE
      )
   IS
        rows_out TABLE_VARCHAR;
   BEGIN
      upd (
         id_nic_intervention_in => rec_in.ID_NIC_INTERVENTION,
         id_terminology_version_in => rec_in.ID_TERMINOLOGY_VERSION,
         intervention_code_in => rec_in.INTERVENTION_CODE,
         references_in => rec_in.REFERENCES,
         id_inst_owner_in => rec_in.ID_INST_OWNER,
         id_concept_version_in => rec_in.ID_CONCEPT_VERSION,
         id_concept_term_in => rec_in.ID_CONCEPT_TERM,
         create_user_in => rec_in.CREATE_USER,
         create_time_in => rec_in.CREATE_TIME,
         create_institution_in => rec_in.CREATE_INSTITUTION,
         update_user_in => rec_in.UPDATE_USER,
         update_time_in => rec_in.UPDATE_TIME,
         update_institution_in => rec_in.UPDATE_INSTITUTION

        ,handle_error_in => handle_error_in
        , rows_out => rows_out
       );
   END upd;

   PROCEDURE upd_ins (
      id_nic_intervention_in IN NIC_INTERVENTION.ID_NIC_INTERVENTION%TYPE,
      id_terminology_version_in IN NIC_INTERVENTION.ID_TERMINOLOGY_VERSION%TYPE DEFAULT NULL,
      intervention_code_in IN NIC_INTERVENTION.INTERVENTION_CODE%TYPE DEFAULT NULL,
      references_in IN NIC_INTERVENTION.REFERENCES%TYPE DEFAULT NULL,
      id_inst_owner_in IN NIC_INTERVENTION.ID_INST_OWNER%TYPE DEFAULT NULL,
      id_concept_version_in IN NIC_INTERVENTION.ID_CONCEPT_VERSION%TYPE DEFAULT NULL,
      id_concept_term_in IN NIC_INTERVENTION.ID_CONCEPT_TERM%TYPE DEFAULT NULL,
      create_user_in IN NIC_INTERVENTION.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN NIC_INTERVENTION.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN NIC_INTERVENTION.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN NIC_INTERVENTION.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN NIC_INTERVENTION.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN NIC_INTERVENTION.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      upd (
         id_nic_intervention_in => id_nic_intervention_in,
         id_terminology_version_in => id_terminology_version_in,
         intervention_code_in => intervention_code_in,
         references_in => references_in,
         id_inst_owner_in => id_inst_owner_in,
         id_concept_version_in => id_concept_version_in,
         id_concept_term_in => id_concept_term_in,
         create_user_in => create_user_in,
         create_time_in => create_time_in,
         create_institution_in => create_institution_in,
         update_user_in => update_user_in,
         update_time_in => update_time_in,
         update_institution_in => update_institution_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
       );
      IF SQL%ROWCOUNT = 0
      THEN
         ins (
            id_nic_intervention_in => id_nic_intervention_in,
            id_terminology_version_in => id_terminology_version_in,
            intervention_code_in => intervention_code_in,
            references_in => references_in,
            id_inst_owner_in => id_inst_owner_in,
            id_concept_version_in => id_concept_version_in,
            id_concept_term_in => id_concept_term_in,
            create_user_in => create_user_in,
            create_time_in => create_time_in,
            create_institution_in => create_institution_in,
            update_user_in => update_user_in,
            update_time_in => update_time_in,
            update_institution_in => update_institution_in
            ,handle_error_in => handle_error_in
            , rows_out => rows_out
         );
      END IF;
   END upd_ins;

   PROCEDURE upd_ins (
      id_nic_intervention_in IN NIC_INTERVENTION.ID_NIC_INTERVENTION%TYPE,
      id_terminology_version_in IN NIC_INTERVENTION.ID_TERMINOLOGY_VERSION%TYPE DEFAULT NULL,
      intervention_code_in IN NIC_INTERVENTION.INTERVENTION_CODE%TYPE DEFAULT NULL,
      references_in IN NIC_INTERVENTION.REFERENCES%TYPE DEFAULT NULL,
      id_inst_owner_in IN NIC_INTERVENTION.ID_INST_OWNER%TYPE DEFAULT NULL,
      id_concept_version_in IN NIC_INTERVENTION.ID_CONCEPT_VERSION%TYPE DEFAULT NULL,
      id_concept_term_in IN NIC_INTERVENTION.ID_CONCEPT_TERM%TYPE DEFAULT NULL,
      create_user_in IN NIC_INTERVENTION.CREATE_USER%TYPE DEFAULT NULL,
      create_time_in IN NIC_INTERVENTION.CREATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN NIC_INTERVENTION.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_user_in IN NIC_INTERVENTION.UPDATE_USER%TYPE DEFAULT NULL,
      update_time_in IN NIC_INTERVENTION.UPDATE_TIME%TYPE DEFAULT NULL,
      update_institution_in IN NIC_INTERVENTION.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
     handle_error_in IN BOOLEAN := TRUE
      )
   IS
        rows_out TABLE_VARCHAR;
   BEGIN
      upd_ins (
      id_nic_intervention_in,
      id_terminology_version_in,
      intervention_code_in,
      references_in,
      id_inst_owner_in,
      id_concept_version_in,
      id_concept_term_in,
      create_user_in,
      create_time_in,
      create_institution_in,
      update_user_in,
      update_time_in,
      update_institution_in,
     handle_error_in
     ,rows_out
      );
   END upd_ins;


   PROCEDURE upd (
      col_in IN NIC_INTERVENTION_tc,
      ignore_if_null_in IN BOOLEAN := TRUE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      )
   IS
      l_ID_NIC_INTERVENTION ID_NIC_INTERVENTION_cc;
      l_ID_TERMINOLOGY_VERSION ID_TERMINOLOGY_VERSION_cc;
      l_INTERVENTION_CODE INTERVENTION_CODE_cc;
      l_REFERENCES REFERENCES_cc;
      l_ID_INST_OWNER ID_INST_OWNER_cc;
      l_ID_CONCEPT_VERSION ID_CONCEPT_VERSION_cc;
      l_ID_CONCEPT_TERM ID_CONCEPT_TERM_cc;
      l_CREATE_USER CREATE_USER_cc;
      l_CREATE_TIME CREATE_TIME_cc;
      l_CREATE_INSTITUTION CREATE_INSTITUTION_cc;
      l_UPDATE_USER UPDATE_USER_cc;
      l_UPDATE_TIME UPDATE_TIME_cc;
      l_UPDATE_INSTITUTION UPDATE_INSTITUTION_cc;
   BEGIN
      FOR i IN col_in.FIRST .. col_in.LAST loop
         l_ID_NIC_INTERVENTION(i) := col_in(i).ID_NIC_INTERVENTION;
         l_ID_TERMINOLOGY_VERSION(i) := col_in(i).ID_TERMINOLOGY_VERSION;
         l_INTERVENTION_CODE(i) := col_in(i).INTERVENTION_CODE;
         l_REFERENCES(i) := col_in(i).REFERENCES;
         l_ID_INST_OWNER(i) := col_in(i).ID_INST_OWNER;
         l_ID_CONCEPT_VERSION(i) := col_in(i).ID_CONCEPT_VERSION;
         l_ID_CONCEPT_TERM(i) := col_in(i).ID_CONCEPT_TERM;
         l_CREATE_USER(i) := col_in(i).CREATE_USER;
         l_CREATE_TIME(i) := col_in(i).CREATE_TIME;
         l_CREATE_INSTITUTION(i) := col_in(i).CREATE_INSTITUTION;
         l_UPDATE_USER(i) := col_in(i).UPDATE_USER;
         l_UPDATE_TIME(i) := col_in(i).UPDATE_TIME;
         l_UPDATE_INSTITUTION(i) := col_in(i).UPDATE_INSTITUTION;
      END LOOP;
      IF NVL (ignore_if_null_in, FALSE)
      THEN
         -- Set any columns to their current values
         -- if incoming value is NULL.
         -- Put WHEN clause on column-level triggers!
         FORALL i IN col_in.FIRST .. col_in.LAST
            UPDATE NIC_INTERVENTION SET
               ID_TERMINOLOGY_VERSION = NVL (l_ID_TERMINOLOGY_VERSION(i), ID_TERMINOLOGY_VERSION),
               INTERVENTION_CODE = NVL (l_INTERVENTION_CODE(i), INTERVENTION_CODE),
               REFERENCES = NVL (l_REFERENCES(i), REFERENCES),
               ID_INST_OWNER = NVL (l_ID_INST_OWNER(i), ID_INST_OWNER),
               ID_CONCEPT_VERSION = NVL (l_ID_CONCEPT_VERSION(i), ID_CONCEPT_VERSION),
               ID_CONCEPT_TERM = NVL (l_ID_CONCEPT_TERM(i), ID_CONCEPT_TERM),
               CREATE_USER = NVL (l_CREATE_USER(i), CREATE_USER),
               CREATE_TIME = NVL (l_CREATE_TIME(i), CREATE_TIME),
               CREATE_INSTITUTION = NVL (l_CREATE_INSTITUTION(i), CREATE_INSTITUTION),
               UPDATE_USER = NVL (l_UPDATE_USER(i), UPDATE_USER),
               UPDATE_TIME = NVL (l_UPDATE_TIME(i), UPDATE_TIME),
               UPDATE_INSTITUTION = NVL (l_UPDATE_INSTITUTION(i), UPDATE_INSTITUTION)
             WHERE
                ID_NIC_INTERVENTION = l_ID_NIC_INTERVENTION(i)
          returning rowid bulk collect into rows_out;
      ELSE
         FORALL i IN col_in.FIRST .. col_in.LAST
            UPDATE NIC_INTERVENTION SET
               ID_TERMINOLOGY_VERSION = l_ID_TERMINOLOGY_VERSION(i),
               INTERVENTION_CODE = l_INTERVENTION_CODE(i),
               REFERENCES = l_REFERENCES(i),
               ID_INST_OWNER = l_ID_INST_OWNER(i),
               ID_CONCEPT_VERSION = l_ID_CONCEPT_VERSION(i),
               ID_CONCEPT_TERM = l_ID_CONCEPT_TERM(i),
               CREATE_USER = l_CREATE_USER(i),
               CREATE_TIME = l_CREATE_TIME(i),
               CREATE_INSTITUTION = l_CREATE_INSTITUTION(i),
               UPDATE_USER = l_UPDATE_USER(i),
               UPDATE_TIME = l_UPDATE_TIME(i),
               UPDATE_INSTITUTION = l_UPDATE_INSTITUTION(i)
             WHERE
                ID_NIC_INTERVENTION = l_ID_NIC_INTERVENTION(i)
        returning rowid bulk collect into rows_out  ;
      END IF;
   END upd;


   PROCEDURE upd (
      col_in IN NIC_INTERVENTION_tc,
      ignore_if_null_in IN BOOLEAN := TRUE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
     rows_out TABLE_VARCHAR;
   BEGIN
      upd (
      col_in ,
      ignore_if_null_in
     ,handle_error_in
     , rows_out
      );
   END upd;

   FUNCTION dynupdstr (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL)

   RETURN VARCHAR2
   IS
   BEGIN
      RETURN
         'BEGIN UPDATE NIC_INTERVENTION
             SET ' || colname_in || ' = :value
           WHERE ' || NVL (where_in, '1=1') || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;' ;
   END dynupdstr;

   FUNCTION dynupdstr_no_rows_out (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL)

   RETURN VARCHAR2
   IS
   BEGIN
      RETURN
         'UPDATE NIC_INTERVENTION
             SET ' || colname_in || ' = :value
           WHERE ' || NVL (where_in, '1=1');
   END dynupdstr_no_rows_out;























  PROCEDURE increment_onecol (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL
      , increment_value_in IN NUMBER DEFAULT 1
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN UPDATE NIC_INTERVENTION set ' || colname_in || '=' || colname_in || ' + ' || nvl(increment_value_in,1) || ' WHERE ' || NVL (where_in, '1=1') || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
      USING OUT rows_out;
   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'DUPLICATE-VALUE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'NIC_INTERVENTION');
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'NIC_INTERVENTION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'NIC_INTERVENTION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END increment_onecol;

   PROCEDURE increment_onecol (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL
     , increment_value_in IN NUMBER DEFAULT 1
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
   rows_out table_varchar;
   BEGIN
      EXECUTE IMMEDIATE 'UPDATE NIC_INTERVENTION set ' || colname_in || '=' || colname_in || ' + ' || nvl(increment_value_in,1) || ' WHERE ' || NVL (where_in, '1=1');
   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'DUPLICATE-VALUE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'NIC_INTERVENTION');
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'NIC_INTERVENTION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'NIC_INTERVENTION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END increment_onecol;


   -- Delete functionality


   PROCEDURE del (
      id_nic_intervention_in IN NIC_INTERVENTION.ID_NIC_INTERVENTION%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM NIC_INTERVENTION
       WHERE
          ID_NIC_INTERVENTION = id_nic_intervention_in
       RETURNING ROWID BULK COLLECT INTO rows_out
         ;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'NIC_INTERVENTION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'NIC_INTERVENTION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del;




   PROCEDURE del (
      id_nic_intervention_in IN NIC_INTERVENTION.ID_NIC_INTERVENTION%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
rows_out TABLE_VARCHAR;
   BEGIN

del (
      id_nic_intervention_in => id_nic_intervention_in
     ,handle_error_in => handle_error_in
, rows_out => rows_out
      );

   END del;








   -- Delete all rows for primary key column ID_NIC_INTERVENTION
   PROCEDURE del_ID_NIC_INTERVENTION (
      id_nic_intervention_in IN NIC_INTERVENTION.ID_NIC_INTERVENTION%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
     )
   IS
   BEGIN
      DELETE FROM NIC_INTERVENTION
       WHERE ID_NIC_INTERVENTION = id_nic_intervention_in
      RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'NIC_INTERVENTION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'NIC_INTERVENTION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_ID_NIC_INTERVENTION;






   -- Delete all rows for primary key column ID_NIC_INTERVENTION
   PROCEDURE del_ID_NIC_INTERVENTION (
      id_nic_intervention_in IN NIC_INTERVENTION.ID_NIC_INTERVENTION%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_ID_NIC_INTERVENTION (
      id_nic_intervention_in => id_nic_intervention_in
     ,handle_error_in => handle_error_in
, rows_out => rows_out
     );
   END del_ID_NIC_INTERVENTION;






   -- Delete for unique value of NIC_INTERVENTION_UK
   PROCEDURE del_NIC_INTERVENTION_UK (
      id_terminology_version_in IN NIC_INTERVENTION.ID_TERMINOLOGY_VERSION%TYPE,
      intervention_code_in IN NIC_INTERVENTION.INTERVENTION_CODE%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM NIC_INTERVENTION
       WHERE
          ID_TERMINOLOGY_VERSION = id_terminology_version_in AND
          INTERVENTION_CODE = intervention_code_in
      RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'NIC_INTERVENTION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'NIC_INTERVENTION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_NIC_INTERVENTION_UK;



   -- Delete for unique value of NIC_INTERVENTION_UK
   PROCEDURE del_NIC_INTERVENTION_UK (
      id_terminology_version_in IN NIC_INTERVENTION.ID_TERMINOLOGY_VERSION%TYPE,
      intervention_code_in IN NIC_INTERVENTION.INTERVENTION_CODE%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_NIC_INTERVENTION_UK (
      id_terminology_version_in =>  id_terminology_version_in,
      intervention_code_in =>  intervention_code_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END del_NIC_INTERVENTION_UK;






   PROCEDURE del_NICI_CNCPTTR_FK (
      id_concept_term_in IN NIC_INTERVENTION.ID_CONCEPT_TERM%TYPE,
      id_inst_owner_in IN NIC_INTERVENTION.ID_INST_OWNER%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM NIC_INTERVENTION
       WHERE
          ID_CONCEPT_TERM = del_NICI_CNCPTTR_FK.id_concept_term_in AND
          ID_INST_OWNER = del_NICI_CNCPTTR_FK.id_inst_owner_in
       RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'NIC_INTERVENTION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'NIC_INTERVENTION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_NICI_CNCPTTR_FK;



PROCEDURE del_NICI_CNCPTTR_FK (
      id_concept_term_in IN NIC_INTERVENTION.ID_CONCEPT_TERM%TYPE,
      id_inst_owner_in IN NIC_INTERVENTION.ID_INST_OWNER%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_NICI_CNCPTTR_FK (
      id_concept_term_in => id_concept_term_in,
      id_inst_owner_in => id_inst_owner_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END del_NICI_CNCPTTR_FK;





   PROCEDURE del_NICI_CNCPTV_FK (
      id_concept_version_in IN NIC_INTERVENTION.ID_CONCEPT_VERSION%TYPE,
      id_inst_owner_in IN NIC_INTERVENTION.ID_INST_OWNER%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM NIC_INTERVENTION
       WHERE
          ID_CONCEPT_VERSION = del_NICI_CNCPTV_FK.id_concept_version_in AND
          ID_INST_OWNER = del_NICI_CNCPTV_FK.id_inst_owner_in
       RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'NIC_INTERVENTION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'NIC_INTERVENTION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_NICI_CNCPTV_FK;



PROCEDURE del_NICI_CNCPTV_FK (
      id_concept_version_in IN NIC_INTERVENTION.ID_CONCEPT_VERSION%TYPE,
      id_inst_owner_in IN NIC_INTERVENTION.ID_INST_OWNER%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_NICI_CNCPTV_FK (
      id_concept_version_in => id_concept_version_in,
      id_inst_owner_in => id_inst_owner_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END del_NICI_CNCPTV_FK;





   PROCEDURE del_NICI_NIC_FK (
      intervention_code_in IN NIC_INTERVENTION.INTERVENTION_CODE%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM NIC_INTERVENTION
       WHERE
          INTERVENTION_CODE = del_NICI_NIC_FK.intervention_code_in
       RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'NIC_INTERVENTION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'NIC_INTERVENTION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_NICI_NIC_FK;



PROCEDURE del_NICI_NIC_FK (
      intervention_code_in IN NIC_INTERVENTION.INTERVENTION_CODE%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_NICI_NIC_FK (
      intervention_code_in => intervention_code_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END del_NICI_NIC_FK;





   PROCEDURE del_NICI_TERMV_FK (
      id_terminology_version_in IN NIC_INTERVENTION.ID_TERMINOLOGY_VERSION%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM NIC_INTERVENTION
       WHERE
          ID_TERMINOLOGY_VERSION = del_NICI_TERMV_FK.id_terminology_version_in
       RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'NIC_INTERVENTION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'NIC_INTERVENTION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_NICI_TERMV_FK;



PROCEDURE del_NICI_TERMV_FK (
      id_terminology_version_in IN NIC_INTERVENTION.ID_TERMINOLOGY_VERSION%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_NICI_TERMV_FK (
      id_terminology_version_in => id_terminology_version_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END del_NICI_TERMV_FK;












   -- Deletions using dynamic SQL
   FUNCTION dyndelstr (where_in IN VARCHAR2) RETURN VARCHAR2
   IS
   BEGIN
      IF where_in IS NULL
      THEN
         RETURN 'DELETE FROM NIC_INTERVENTION';
      ELSE
         RETURN
            'DELETE FROM NIC_INTERVENTION WHERE ' || where_in;
      END IF;
   END dyndelstr;

   FUNCTION dyncoldelstr (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE)
      RETURN VARCHAR2
   IS
   BEGIN
      RETURN dyndelstr ( colname_in || ' = :value' );
   END;

   PROCEDURE del_by (
      where_clause_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyndelstr (where_clause_in);
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'NIC_INTERVENTION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'NIC_INTERVENTION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by;





   PROCEDURE del_by (
      where_clause_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN ' || dyndelstr (where_clause_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;' using OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'NIC_INTERVENTION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'NIC_INTERVENTION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by;





   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyncoldelstr (colname_in)
         USING colvalue_in;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'NIC_INTERVENTION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'NIC_INTERVENTION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;






   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr (colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
         USING IN colvalue_in, OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'NIC_INTERVENTION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'NIC_INTERVENTION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN DATE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyncoldelstr (colname_in)
         USING colvalue_in;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'NIC_INTERVENTION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'NIC_INTERVENTION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN DATE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr (colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
         USING IN colvalue_in, OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'NIC_INTERVENTION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'NIC_INTERVENTION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN TIMESTAMP WITH LOCAL TIME ZONE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyncoldelstr (colname_in)
         USING colvalue_in;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'NIC_INTERVENTION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'NIC_INTERVENTION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN TIMESTAMP WITH LOCAL TIME ZONE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr (colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
         USING IN colvalue_in, OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'NIC_INTERVENTION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'NIC_INTERVENTION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN NUMBER
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyncoldelstr (colname_in)
         USING colvalue_in;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'NIC_INTERVENTION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'NIC_INTERVENTION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;






   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN NUMBER
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
   BEGIN
     EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr (colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
         USING IN colvalue_in, OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'NIC_INTERVENTION');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'NIC_INTERVENTION');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   -- Initialize a record with default values for columns in the table.
   PROCEDURE initrec (
      nic_intervention_inout IN OUT NIC_INTERVENTION%ROWTYPE
   )
   IS
   BEGIN
      nic_intervention_inout.ID_NIC_INTERVENTION := NULL;
      nic_intervention_inout.ID_TERMINOLOGY_VERSION := NULL;
      nic_intervention_inout.INTERVENTION_CODE := NULL;
      nic_intervention_inout.REFERENCES := NULL;
      nic_intervention_inout.ID_INST_OWNER := NULL;
      nic_intervention_inout.ID_CONCEPT_VERSION := NULL;
      nic_intervention_inout.ID_CONCEPT_TERM := NULL;
      nic_intervention_inout.CREATE_USER := NULL;
      nic_intervention_inout.CREATE_TIME := NULL;
      nic_intervention_inout.CREATE_INSTITUTION := NULL;
      nic_intervention_inout.UPDATE_USER := NULL;
      nic_intervention_inout.UPDATE_TIME := NULL;
      nic_intervention_inout.UPDATE_INSTITUTION := NULL;
   END initrec;

   FUNCTION initrec RETURN NIC_INTERVENTION%ROWTYPE
   IS
      l_nic_intervention NIC_INTERVENTION%ROWTYPE;
   BEGIN
      RETURN l_nic_intervention;
   END initrec;


   FUNCTION get_data_rowid(
        rows_in IN TABLE_VARCHAR
        ) RETURN NIC_INTERVENTION_tc
   IS
        data NIC_INTERVENTION_tc;
   BEGIN
        select * bulk collect into data from NIC_INTERVENTION where rowid in (select /*+opt_estimate(table,t,scale_rows=0.0000001))*/ * from table(rows_in) t);
        return data;
        EXCEPTION
      WHEN OTHERS THEN
        pk_alert_exceptions.raise_error (
           error_name_in => 'get_data_rowid'
           );
   END get_data_rowid;


   FUNCTION get_data_rowid_pat(
        rows_in IN TABLE_VARCHAR
        ) RETURN NIC_INTERVENTION_tc
   is
        PRAGMA AUTONOMOUS_TRANSACTION;
        data NIC_INTERVENTION_tc;
   BEGIN
        data := get_data_rowid(rows_in);
        commit;
        return data;
        EXCEPTION
      WHEN OTHERS THEN
        pk_alert_exceptions.raise_error (
           error_name_in => 'get_data_rowid'
           );
        rollback;
    END get_data_rowid_pat;

BEGIN
   NULL;
END TS_NIC_INTERVENTION;
/
