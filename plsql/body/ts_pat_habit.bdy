/*-- Last Change Revision: $Rev: 2028307 $*/
/*-- Last Change by: $Author: mario.fernandes $*/
/*-- Date of last change: $Date: 2022-08-02 18:44:59 +0100 (ter, 02 ago 2022) $*/

CREATE OR REPLACE PACKAGE BODY ts_pat_habit
/*
| Generated by or retrieved from QCGU - DO NOT MODIFY!
| QCGU - "Get it right, do it fast" - www.ToadWorld.com
| QCGU Universal ID: {657BD35E-2E48-4CD4-B14E-41420B8433B3}
| Created On: Março 29, 2011 11:0:25
| Created By: ALERT
*/
 IS

    e_null_column_value EXCEPTION;
    PRAGMA EXCEPTION_INIT(e_null_column_value, -1400);
    --
    e_existing_fky_reference EXCEPTION;
    PRAGMA EXCEPTION_INIT(e_existing_fky_reference, -2266);
    --
    e_check_constraint_failure EXCEPTION;
    PRAGMA EXCEPTION_INIT(e_check_constraint_failure, -2290);
    --
    e_no_parent_key EXCEPTION;
    PRAGMA EXCEPTION_INIT(e_no_parent_key, -2291);
    --
    e_child_record_found EXCEPTION;
    PRAGMA EXCEPTION_INIT(e_child_record_found, -2292);
    --
    e_forall_error EXCEPTION;
    PRAGMA EXCEPTION_INIT(e_forall_error, -24381);
    --
    -- Defined for backward compatibilty.
    e_integ_constraint_failure EXCEPTION;
    PRAGMA EXCEPTION_INIT(e_integ_constraint_failure, -2291);

    -- Private utilities
    PROCEDURE get_constraint_info
    (
        owner_out OUT all_constraints.owner%TYPE,
        name_out  OUT all_constraints.constraint_name%TYPE
    ) IS
        l_errm  VARCHAR2(2000) := dbms_utility.format_error_stack;
        dotloc  PLS_INTEGER;
        leftloc PLS_INTEGER;
    BEGIN
        dotloc    := instr(l_errm, '.');
        leftloc   := instr(l_errm, '(');
        owner_out := substr(l_errm, leftloc + 1, dotloc - leftloc - 1);
        name_out  := substr(l_errm, dotloc + 1, instr(l_errm, ')') - dotloc - 1);
    END get_constraint_info;
    -- Public programs

    PROCEDURE ins
    (
        id_pat_habit_in              IN pat_habit.id_pat_habit%TYPE,
        id_patient_in                IN pat_habit.id_patient%TYPE DEFAULT NULL,
        id_habit_in                  IN pat_habit.id_habit%TYPE DEFAULT NULL,
        flg_status_in                IN pat_habit.flg_status%TYPE DEFAULT NULL,
        id_prof_writes_in            IN pat_habit.id_prof_writes%TYPE DEFAULT NULL,
        notes_in                     IN pat_habit.notes%TYPE DEFAULT NULL,
        id_prof_cancel_in            IN pat_habit.id_prof_cancel%TYPE DEFAULT NULL,
        note_cancel_in               IN pat_habit.note_cancel%TYPE DEFAULT NULL,
        year_begin_in                IN pat_habit.year_begin%TYPE DEFAULT NULL,
        month_begin_in               IN pat_habit.month_begin%TYPE DEFAULT NULL,
        day_begin_in                 IN pat_habit.day_begin%TYPE DEFAULT NULL,
        year_end_in                  IN pat_habit.year_end%TYPE DEFAULT NULL,
        month_end_in                 IN pat_habit.month_end%TYPE DEFAULT NULL,
        day_end_in                   IN pat_habit.day_end%TYPE DEFAULT NULL,
        id_institution_in            IN pat_habit.id_institution%TYPE DEFAULT NULL,
        id_episode_in                IN pat_habit.id_episode%TYPE DEFAULT NULL,
        dt_pat_habit_tstz_in         IN pat_habit.dt_pat_habit_tstz%TYPE DEFAULT NULL,
        dt_cancel_tstz_in            IN pat_habit.dt_cancel_tstz%TYPE DEFAULT NULL,
        id_cancel_reason_in          IN pat_habit.id_cancel_reason%TYPE DEFAULT NULL,
        cancel_notes_in              IN pat_habit.cancel_notes%TYPE DEFAULT NULL,
        create_user_in               IN pat_habit.create_user%TYPE DEFAULT NULL,
        create_time_in               IN pat_habit.create_time%TYPE DEFAULT NULL,
        create_institution_in        IN pat_habit.create_institution%TYPE DEFAULT NULL,
        update_user_in               IN pat_habit.update_user%TYPE DEFAULT NULL,
        update_time_in               IN pat_habit.update_time%TYPE DEFAULT NULL,
        update_institution_in        IN pat_habit.update_institution%TYPE DEFAULT NULL,
        id_habit_characterization_in IN pat_habit.id_habit_characterization%TYPE DEFAULT NULL,
        handle_error_in              IN BOOLEAN := TRUE,
        rows_out                     OUT table_varchar
    ) IS
    BEGIN
    
        INSERT INTO pat_habit
            (id_pat_habit,
             id_patient,
             id_habit,
             flg_status,
             id_prof_writes,
             notes,
             id_prof_cancel,
             note_cancel,
             year_begin,
             month_begin,
             day_begin,
             year_end,
             month_end,
             day_end,
             id_institution,
             id_episode,
             dt_pat_habit_tstz,
             dt_cancel_tstz,
             id_cancel_reason,
             cancel_notes,
             create_user,
             create_time,
             create_institution,
             update_user,
             update_time,
             update_institution,
             id_habit_characterization)
        VALUES
            (id_pat_habit_in,
             id_patient_in,
             id_habit_in,
             flg_status_in,
             id_prof_writes_in,
             notes_in,
             id_prof_cancel_in,
             note_cancel_in,
             year_begin_in,
             month_begin_in,
             day_begin_in,
             year_end_in,
             month_end_in,
             day_end_in,
             id_institution_in,
             id_episode_in,
             dt_pat_habit_tstz_in,
             dt_cancel_tstz_in,
             id_cancel_reason_in,
             cancel_notes_in,
             create_user_in,
             create_time_in,
             create_institution_in,
             update_user_in,
             update_time_in,
             update_institution_in,
             id_habit_characterization_in)
        RETURNING ROWID BULK COLLECT INTO rows_out;
    EXCEPTION
        WHEN dup_val_on_index THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner           all_constraints.owner%TYPE;
                    l_name            all_constraints.constraint_name%TYPE;
                    l_err_instance_id PLS_INTEGER;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF FALSE
                    THEN
                        NULL; -- Placeholder in case no unique indexes
                    ELSE
                        pk_alert_exceptions.raise_error(error_name_in => 'DUPLICATE-VALUE',
                                                        name1_in      => 'OWNER',
                                                        value1_in     => l_owner,
                                                        name2_in      => 'CONSTRAINT_NAME',
                                                        value2_in     => l_name,
                                                        name3_in      => 'TABLE_NAME',
                                                        value3_in     => 'PAT_HABIT');
                    END IF;
                END;
            END IF;
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'PAT_HABIT');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'PAT_HABIT');
                    IF l_name = 'PH_CR_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_CANCEL_REASON',
                                                        value_in           => id_cancel_reason_in);
                    END IF;
                    IF l_name = 'PTNOT_EPIS_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_EPISODE',
                                                        value_in           => id_episode_in);
                    END IF;
                    IF l_name = 'PTNOT_HAT_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_HABIT',
                                                        value_in           => id_habit_in);
                    END IF;
                    IF l_name = 'PTNOT_INST_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_INSTITUTION',
                                                        value_in           => id_institution_in);
                    END IF;
                    IF l_name = 'PTNOT_PAT_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_PATIENT',
                                                        value_in           => id_patient_in);
                    END IF;
                    IF l_name = 'PTNOT_PROF_CANCEL_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_PROF_CANCEL',
                                                        value_in           => id_prof_cancel_in);
                    END IF;
                    IF l_name = 'PTNOT_PROF_WRITES_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_PROF_WRITES',
                                                        value_in           => id_prof_writes_in);
                    END IF;
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
        WHEN e_null_column_value THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    v_errm    VARCHAR2(2000) := dbms_utility.format_error_stack;
                    dot1loc   INTEGER;
                    dot2loc   INTEGER;
                    parenloc  INTEGER;
                    c_owner   all_constraints.owner%TYPE;
                    c_tabname all_tables.table_name%TYPE;
                    c_colname all_tab_columns.column_name%TYPE;
                BEGIN
                    dot1loc   := instr(v_errm, '.', 1, 1);
                    dot2loc   := instr(v_errm, '.', 1, 2);
                    parenloc  := instr(v_errm, '(');
                    c_owner   := substr(v_errm, parenloc + 1, dot1loc - parenloc - 1);
                    c_tabname := substr(v_errm, dot1loc + 1, dot2loc - dot1loc - 1);
                    c_colname := substr(v_errm, dot2loc + 1, instr(v_errm, ')') - dot2loc - 1);
                
                    pk_alert_exceptions.raise_error(error_name_in => 'COLUMN-CANNOT-BE-NULL',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => c_owner,
                                                    name2_in      => 'TABLE_NAME',
                                                    value2_in     => c_tabname,
                                                    name3_in      => 'COLUMN_NAME',
                                                    value3_in     => c_colname);
                END;
            END IF;
    END ins;

    PROCEDURE ins
    (
        id_pat_habit_in              IN pat_habit.id_pat_habit%TYPE,
        id_patient_in                IN pat_habit.id_patient%TYPE DEFAULT NULL,
        id_habit_in                  IN pat_habit.id_habit%TYPE DEFAULT NULL,
        flg_status_in                IN pat_habit.flg_status%TYPE DEFAULT NULL,
        id_prof_writes_in            IN pat_habit.id_prof_writes%TYPE DEFAULT NULL,
        notes_in                     IN pat_habit.notes%TYPE DEFAULT NULL,
        id_prof_cancel_in            IN pat_habit.id_prof_cancel%TYPE DEFAULT NULL,
        note_cancel_in               IN pat_habit.note_cancel%TYPE DEFAULT NULL,
        year_begin_in                IN pat_habit.year_begin%TYPE DEFAULT NULL,
        month_begin_in               IN pat_habit.month_begin%TYPE DEFAULT NULL,
        day_begin_in                 IN pat_habit.day_begin%TYPE DEFAULT NULL,
        year_end_in                  IN pat_habit.year_end%TYPE DEFAULT NULL,
        month_end_in                 IN pat_habit.month_end%TYPE DEFAULT NULL,
        day_end_in                   IN pat_habit.day_end%TYPE DEFAULT NULL,
        id_institution_in            IN pat_habit.id_institution%TYPE DEFAULT NULL,
        id_episode_in                IN pat_habit.id_episode%TYPE DEFAULT NULL,
        dt_pat_habit_tstz_in         IN pat_habit.dt_pat_habit_tstz%TYPE DEFAULT NULL,
        dt_cancel_tstz_in            IN pat_habit.dt_cancel_tstz%TYPE DEFAULT NULL,
        id_cancel_reason_in          IN pat_habit.id_cancel_reason%TYPE DEFAULT NULL,
        cancel_notes_in              IN pat_habit.cancel_notes%TYPE DEFAULT NULL,
        create_user_in               IN pat_habit.create_user%TYPE DEFAULT NULL,
        create_time_in               IN pat_habit.create_time%TYPE DEFAULT NULL,
        create_institution_in        IN pat_habit.create_institution%TYPE DEFAULT NULL,
        update_user_in               IN pat_habit.update_user%TYPE DEFAULT NULL,
        update_time_in               IN pat_habit.update_time%TYPE DEFAULT NULL,
        update_institution_in        IN pat_habit.update_institution%TYPE DEFAULT NULL,
        id_habit_characterization_in IN pat_habit.id_habit_characterization%TYPE DEFAULT NULL,
        handle_error_in              IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
    
        ins(id_pat_habit_in              => id_pat_habit_in,
            id_patient_in                => id_patient_in,
            id_habit_in                  => id_habit_in,
            flg_status_in                => flg_status_in,
            id_prof_writes_in            => id_prof_writes_in,
            notes_in                     => notes_in,
            id_prof_cancel_in            => id_prof_cancel_in,
            note_cancel_in               => note_cancel_in,
            year_begin_in                => year_begin_in,
            month_begin_in               => month_begin_in,
            day_begin_in                 => day_begin_in,
            year_end_in                  => year_end_in,
            month_end_in                 => month_end_in,
            day_end_in                   => day_end_in,
            id_institution_in            => id_institution_in,
            id_episode_in                => id_episode_in,
            dt_pat_habit_tstz_in         => dt_pat_habit_tstz_in,
            dt_cancel_tstz_in            => dt_cancel_tstz_in,
            id_cancel_reason_in          => id_cancel_reason_in,
            cancel_notes_in              => cancel_notes_in,
            create_user_in               => create_user_in,
            create_time_in               => create_time_in,
            create_institution_in        => create_institution_in,
            update_user_in               => update_user_in,
            update_time_in               => update_time_in,
            update_institution_in        => update_institution_in,
            id_habit_characterization_in => id_habit_characterization_in,
            handle_error_in              => handle_error_in,
            rows_out                     => rows_out);
    END ins;

    PROCEDURE ins
    (
        rec_in          IN pat_habit%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) IS
        l_rec pat_habit%ROWTYPE := rec_in;
    BEGIN
        IF gen_pky_in
        THEN
            l_rec.id_pat_habit := next_key(sequence_in);
        END IF;
        ins(id_pat_habit_in              => l_rec.id_pat_habit,
            id_patient_in                => l_rec.id_patient,
            id_habit_in                  => l_rec.id_habit,
            flg_status_in                => l_rec.flg_status,
            id_prof_writes_in            => l_rec.id_prof_writes,
            notes_in                     => l_rec.notes,
            id_prof_cancel_in            => l_rec.id_prof_cancel,
            note_cancel_in               => l_rec.note_cancel,
            year_begin_in                => l_rec.year_begin,
            month_begin_in               => l_rec.month_begin,
            day_begin_in                 => l_rec.day_begin,
            year_end_in                  => l_rec.year_end,
            month_end_in                 => l_rec.month_end,
            day_end_in                   => l_rec.day_end,
            id_institution_in            => l_rec.id_institution,
            id_episode_in                => l_rec.id_episode,
            dt_pat_habit_tstz_in         => l_rec.dt_pat_habit_tstz,
            dt_cancel_tstz_in            => l_rec.dt_cancel_tstz,
            id_cancel_reason_in          => l_rec.id_cancel_reason,
            cancel_notes_in              => l_rec.cancel_notes,
            create_user_in               => l_rec.create_user,
            create_time_in               => l_rec.create_time,
            create_institution_in        => l_rec.create_institution,
            update_user_in               => l_rec.update_user,
            update_time_in               => l_rec.update_time,
            update_institution_in        => l_rec.update_institution,
            id_habit_characterization_in => l_rec.id_habit_characterization,
            handle_error_in              => handle_error_in,
            rows_out                     => rows_out);
    END ins;

    PROCEDURE ins
    (
        rec_in          IN pat_habit%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
    
        ins(rec_in          => rec_in,
            gen_pky_in      => gen_pky_in,
            sequence_in     => sequence_in,
            handle_error_in => handle_error_in,
            rows_out        => rows_out);
    
    END ins;

    FUNCTION next_key(sequence_in IN VARCHAR2 := NULL) RETURN pat_habit.id_pat_habit%TYPE
    
     IS
        retval pat_habit.id_pat_habit%TYPE;
    
    BEGIN
        IF sequence_in IS NULL
        THEN
            SELECT seq_pat_habit.nextval
              INTO retval
              FROM dual;
        ELSE
            EXECUTE IMMEDIATE 'SELECT ' || sequence_in || '.NEXTVAL FROM dual'
                INTO retval;
        END IF;
        RETURN retval;
    EXCEPTION
        WHEN OTHERS THEN
            pk_alert_exceptions.raise_error(error_name_in => 'SEQUENCE-GENERATION-FAILURE',
                                            name1_in      => 'SEQUENCE',
                                            value1_in     => nvl(sequence_in, 'seq_PAT_HABIT'));
    END next_key;

    PROCEDURE ins
    (
        id_patient_in                IN pat_habit.id_patient%TYPE DEFAULT NULL,
        id_habit_in                  IN pat_habit.id_habit%TYPE DEFAULT NULL,
        flg_status_in                IN pat_habit.flg_status%TYPE DEFAULT NULL,
        id_prof_writes_in            IN pat_habit.id_prof_writes%TYPE DEFAULT NULL,
        notes_in                     IN pat_habit.notes%TYPE DEFAULT NULL,
        id_prof_cancel_in            IN pat_habit.id_prof_cancel%TYPE DEFAULT NULL,
        note_cancel_in               IN pat_habit.note_cancel%TYPE DEFAULT NULL,
        year_begin_in                IN pat_habit.year_begin%TYPE DEFAULT NULL,
        month_begin_in               IN pat_habit.month_begin%TYPE DEFAULT NULL,
        day_begin_in                 IN pat_habit.day_begin%TYPE DEFAULT NULL,
        year_end_in                  IN pat_habit.year_end%TYPE DEFAULT NULL,
        month_end_in                 IN pat_habit.month_end%TYPE DEFAULT NULL,
        day_end_in                   IN pat_habit.day_end%TYPE DEFAULT NULL,
        id_institution_in            IN pat_habit.id_institution%TYPE DEFAULT NULL,
        id_episode_in                IN pat_habit.id_episode%TYPE DEFAULT NULL,
        dt_pat_habit_tstz_in         IN pat_habit.dt_pat_habit_tstz%TYPE DEFAULT NULL,
        dt_cancel_tstz_in            IN pat_habit.dt_cancel_tstz%TYPE DEFAULT NULL,
        id_cancel_reason_in          IN pat_habit.id_cancel_reason%TYPE DEFAULT NULL,
        cancel_notes_in              IN pat_habit.cancel_notes%TYPE DEFAULT NULL,
        create_user_in               IN pat_habit.create_user%TYPE DEFAULT NULL,
        create_time_in               IN pat_habit.create_time%TYPE DEFAULT NULL,
        create_institution_in        IN pat_habit.create_institution%TYPE DEFAULT NULL,
        update_user_in               IN pat_habit.update_user%TYPE DEFAULT NULL,
        update_time_in               IN pat_habit.update_time%TYPE DEFAULT NULL,
        update_institution_in        IN pat_habit.update_institution%TYPE DEFAULT NULL,
        id_habit_characterization_in IN pat_habit.id_habit_characterization%TYPE DEFAULT NULL,
        id_pat_habit_out             IN OUT pat_habit.id_pat_habit%TYPE,
        handle_error_in              IN BOOLEAN := TRUE,
        rows_out                     OUT table_varchar
    ) IS
        l_pky pat_habit.id_pat_habit%TYPE := next_key;
    BEGIN
        ins(id_pat_habit_in              => l_pky,
            id_patient_in                => id_patient_in,
            id_habit_in                  => id_habit_in,
            flg_status_in                => flg_status_in,
            id_prof_writes_in            => id_prof_writes_in,
            notes_in                     => notes_in,
            id_prof_cancel_in            => id_prof_cancel_in,
            note_cancel_in               => note_cancel_in,
            year_begin_in                => year_begin_in,
            month_begin_in               => month_begin_in,
            day_begin_in                 => day_begin_in,
            year_end_in                  => year_end_in,
            month_end_in                 => month_end_in,
            day_end_in                   => day_end_in,
            id_institution_in            => id_institution_in,
            id_episode_in                => id_episode_in,
            dt_pat_habit_tstz_in         => dt_pat_habit_tstz_in,
            dt_cancel_tstz_in            => dt_cancel_tstz_in,
            id_cancel_reason_in          => id_cancel_reason_in,
            cancel_notes_in              => cancel_notes_in,
            create_user_in               => create_user_in,
            create_time_in               => create_time_in,
            create_institution_in        => create_institution_in,
            update_user_in               => update_user_in,
            update_time_in               => update_time_in,
            update_institution_in        => update_institution_in,
            id_habit_characterization_in => id_habit_characterization_in,
            handle_error_in              => handle_error_in,
            rows_out                     => rows_out);
        id_pat_habit_out := l_pky;
    END ins;

    PROCEDURE ins
    (
        id_patient_in                IN pat_habit.id_patient%TYPE DEFAULT NULL,
        id_habit_in                  IN pat_habit.id_habit%TYPE DEFAULT NULL,
        flg_status_in                IN pat_habit.flg_status%TYPE DEFAULT NULL,
        id_prof_writes_in            IN pat_habit.id_prof_writes%TYPE DEFAULT NULL,
        notes_in                     IN pat_habit.notes%TYPE DEFAULT NULL,
        id_prof_cancel_in            IN pat_habit.id_prof_cancel%TYPE DEFAULT NULL,
        note_cancel_in               IN pat_habit.note_cancel%TYPE DEFAULT NULL,
        year_begin_in                IN pat_habit.year_begin%TYPE DEFAULT NULL,
        month_begin_in               IN pat_habit.month_begin%TYPE DEFAULT NULL,
        day_begin_in                 IN pat_habit.day_begin%TYPE DEFAULT NULL,
        year_end_in                  IN pat_habit.year_end%TYPE DEFAULT NULL,
        month_end_in                 IN pat_habit.month_end%TYPE DEFAULT NULL,
        day_end_in                   IN pat_habit.day_end%TYPE DEFAULT NULL,
        id_institution_in            IN pat_habit.id_institution%TYPE DEFAULT NULL,
        id_episode_in                IN pat_habit.id_episode%TYPE DEFAULT NULL,
        dt_pat_habit_tstz_in         IN pat_habit.dt_pat_habit_tstz%TYPE DEFAULT NULL,
        dt_cancel_tstz_in            IN pat_habit.dt_cancel_tstz%TYPE DEFAULT NULL,
        id_cancel_reason_in          IN pat_habit.id_cancel_reason%TYPE DEFAULT NULL,
        cancel_notes_in              IN pat_habit.cancel_notes%TYPE DEFAULT NULL,
        create_user_in               IN pat_habit.create_user%TYPE DEFAULT NULL,
        create_time_in               IN pat_habit.create_time%TYPE DEFAULT NULL,
        create_institution_in        IN pat_habit.create_institution%TYPE DEFAULT NULL,
        update_user_in               IN pat_habit.update_user%TYPE DEFAULT NULL,
        update_time_in               IN pat_habit.update_time%TYPE DEFAULT NULL,
        update_institution_in        IN pat_habit.update_institution%TYPE DEFAULT NULL,
        id_habit_characterization_in IN pat_habit.id_habit_characterization%TYPE DEFAULT NULL,
        id_pat_habit_out             IN OUT pat_habit.id_pat_habit%TYPE,
        handle_error_in              IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        ins(id_patient_in                => id_patient_in,
            id_habit_in                  => id_habit_in,
            flg_status_in                => flg_status_in,
            id_prof_writes_in            => id_prof_writes_in,
            notes_in                     => notes_in,
            id_prof_cancel_in            => id_prof_cancel_in,
            note_cancel_in               => note_cancel_in,
            year_begin_in                => year_begin_in,
            month_begin_in               => month_begin_in,
            day_begin_in                 => day_begin_in,
            year_end_in                  => year_end_in,
            month_end_in                 => month_end_in,
            day_end_in                   => day_end_in,
            id_institution_in            => id_institution_in,
            id_episode_in                => id_episode_in,
            dt_pat_habit_tstz_in         => dt_pat_habit_tstz_in,
            dt_cancel_tstz_in            => dt_cancel_tstz_in,
            id_cancel_reason_in          => id_cancel_reason_in,
            cancel_notes_in              => cancel_notes_in,
            create_user_in               => create_user_in,
            create_time_in               => create_time_in,
            create_institution_in        => create_institution_in,
            update_user_in               => update_user_in,
            update_time_in               => update_time_in,
            update_institution_in        => update_institution_in,
            id_habit_characterization_in => id_habit_characterization_in,
            id_pat_habit_out             => id_pat_habit_out,
            handle_error_in              => handle_error_in,
            rows_out                     => rows_out);
    END ins;

    FUNCTION ins
    (
        id_patient_in                IN pat_habit.id_patient%TYPE DEFAULT NULL,
        id_habit_in                  IN pat_habit.id_habit%TYPE DEFAULT NULL,
        flg_status_in                IN pat_habit.flg_status%TYPE DEFAULT NULL,
        id_prof_writes_in            IN pat_habit.id_prof_writes%TYPE DEFAULT NULL,
        notes_in                     IN pat_habit.notes%TYPE DEFAULT NULL,
        id_prof_cancel_in            IN pat_habit.id_prof_cancel%TYPE DEFAULT NULL,
        note_cancel_in               IN pat_habit.note_cancel%TYPE DEFAULT NULL,
        year_begin_in                IN pat_habit.year_begin%TYPE DEFAULT NULL,
        month_begin_in               IN pat_habit.month_begin%TYPE DEFAULT NULL,
        day_begin_in                 IN pat_habit.day_begin%TYPE DEFAULT NULL,
        year_end_in                  IN pat_habit.year_end%TYPE DEFAULT NULL,
        month_end_in                 IN pat_habit.month_end%TYPE DEFAULT NULL,
        day_end_in                   IN pat_habit.day_end%TYPE DEFAULT NULL,
        id_institution_in            IN pat_habit.id_institution%TYPE DEFAULT NULL,
        id_episode_in                IN pat_habit.id_episode%TYPE DEFAULT NULL,
        dt_pat_habit_tstz_in         IN pat_habit.dt_pat_habit_tstz%TYPE DEFAULT NULL,
        dt_cancel_tstz_in            IN pat_habit.dt_cancel_tstz%TYPE DEFAULT NULL,
        id_cancel_reason_in          IN pat_habit.id_cancel_reason%TYPE DEFAULT NULL,
        cancel_notes_in              IN pat_habit.cancel_notes%TYPE DEFAULT NULL,
        create_user_in               IN pat_habit.create_user%TYPE DEFAULT NULL,
        create_time_in               IN pat_habit.create_time%TYPE DEFAULT NULL,
        create_institution_in        IN pat_habit.create_institution%TYPE DEFAULT NULL,
        update_user_in               IN pat_habit.update_user%TYPE DEFAULT NULL,
        update_time_in               IN pat_habit.update_time%TYPE DEFAULT NULL,
        update_institution_in        IN pat_habit.update_institution%TYPE DEFAULT NULL,
        id_habit_characterization_in IN pat_habit.id_habit_characterization%TYPE DEFAULT NULL,
        handle_error_in              IN BOOLEAN := TRUE,
        rows_out                     OUT table_varchar
    ) RETURN pat_habit.id_pat_habit%TYPE IS
        l_pky pat_habit.id_pat_habit%TYPE := next_key;
    BEGIN
        ins(id_pat_habit_in              => l_pky,
            id_patient_in                => id_patient_in,
            id_habit_in                  => id_habit_in,
            flg_status_in                => flg_status_in,
            id_prof_writes_in            => id_prof_writes_in,
            notes_in                     => notes_in,
            id_prof_cancel_in            => id_prof_cancel_in,
            note_cancel_in               => note_cancel_in,
            year_begin_in                => year_begin_in,
            month_begin_in               => month_begin_in,
            day_begin_in                 => day_begin_in,
            year_end_in                  => year_end_in,
            month_end_in                 => month_end_in,
            day_end_in                   => day_end_in,
            id_institution_in            => id_institution_in,
            id_episode_in                => id_episode_in,
            dt_pat_habit_tstz_in         => dt_pat_habit_tstz_in,
            dt_cancel_tstz_in            => dt_cancel_tstz_in,
            id_cancel_reason_in          => id_cancel_reason_in,
            cancel_notes_in              => cancel_notes_in,
            create_user_in               => create_user_in,
            create_time_in               => create_time_in,
            create_institution_in        => create_institution_in,
            update_user_in               => update_user_in,
            update_time_in               => update_time_in,
            update_institution_in        => update_institution_in,
            id_habit_characterization_in => id_habit_characterization_in,
            handle_error_in              => handle_error_in,
            rows_out                     => rows_out);
        RETURN l_pky;
    END ins;

    FUNCTION ins
    (
        id_patient_in                IN pat_habit.id_patient%TYPE DEFAULT NULL,
        id_habit_in                  IN pat_habit.id_habit%TYPE DEFAULT NULL,
        flg_status_in                IN pat_habit.flg_status%TYPE DEFAULT NULL,
        id_prof_writes_in            IN pat_habit.id_prof_writes%TYPE DEFAULT NULL,
        notes_in                     IN pat_habit.notes%TYPE DEFAULT NULL,
        id_prof_cancel_in            IN pat_habit.id_prof_cancel%TYPE DEFAULT NULL,
        note_cancel_in               IN pat_habit.note_cancel%TYPE DEFAULT NULL,
        year_begin_in                IN pat_habit.year_begin%TYPE DEFAULT NULL,
        month_begin_in               IN pat_habit.month_begin%TYPE DEFAULT NULL,
        day_begin_in                 IN pat_habit.day_begin%TYPE DEFAULT NULL,
        year_end_in                  IN pat_habit.year_end%TYPE DEFAULT NULL,
        month_end_in                 IN pat_habit.month_end%TYPE DEFAULT NULL,
        day_end_in                   IN pat_habit.day_end%TYPE DEFAULT NULL,
        id_institution_in            IN pat_habit.id_institution%TYPE DEFAULT NULL,
        id_episode_in                IN pat_habit.id_episode%TYPE DEFAULT NULL,
        dt_pat_habit_tstz_in         IN pat_habit.dt_pat_habit_tstz%TYPE DEFAULT NULL,
        dt_cancel_tstz_in            IN pat_habit.dt_cancel_tstz%TYPE DEFAULT NULL,
        id_cancel_reason_in          IN pat_habit.id_cancel_reason%TYPE DEFAULT NULL,
        cancel_notes_in              IN pat_habit.cancel_notes%TYPE DEFAULT NULL,
        create_user_in               IN pat_habit.create_user%TYPE DEFAULT NULL,
        create_time_in               IN pat_habit.create_time%TYPE DEFAULT NULL,
        create_institution_in        IN pat_habit.create_institution%TYPE DEFAULT NULL,
        update_user_in               IN pat_habit.update_user%TYPE DEFAULT NULL,
        update_time_in               IN pat_habit.update_time%TYPE DEFAULT NULL,
        update_institution_in        IN pat_habit.update_institution%TYPE DEFAULT NULL,
        id_habit_characterization_in IN pat_habit.id_habit_characterization%TYPE DEFAULT NULL,
        handle_error_in              IN BOOLEAN := TRUE
    ) RETURN pat_habit.id_pat_habit%TYPE IS
        l_pky    pat_habit.id_pat_habit%TYPE := next_key;
        rows_out table_varchar;
    BEGIN
        ins(id_pat_habit_in              => l_pky,
            id_patient_in                => id_patient_in,
            id_habit_in                  => id_habit_in,
            flg_status_in                => flg_status_in,
            id_prof_writes_in            => id_prof_writes_in,
            notes_in                     => notes_in,
            id_prof_cancel_in            => id_prof_cancel_in,
            note_cancel_in               => note_cancel_in,
            year_begin_in                => year_begin_in,
            month_begin_in               => month_begin_in,
            day_begin_in                 => day_begin_in,
            year_end_in                  => year_end_in,
            month_end_in                 => month_end_in,
            day_end_in                   => day_end_in,
            id_institution_in            => id_institution_in,
            id_episode_in                => id_episode_in,
            dt_pat_habit_tstz_in         => dt_pat_habit_tstz_in,
            dt_cancel_tstz_in            => dt_cancel_tstz_in,
            id_cancel_reason_in          => id_cancel_reason_in,
            cancel_notes_in              => cancel_notes_in,
            create_user_in               => create_user_in,
            create_time_in               => create_time_in,
            create_institution_in        => create_institution_in,
            update_user_in               => update_user_in,
            update_time_in               => update_time_in,
            update_institution_in        => update_institution_in,
            id_habit_characterization_in => id_habit_characterization_in,
            handle_error_in              => handle_error_in,
            rows_out                     => rows_out);
        RETURN l_pky;
    END ins;

    PROCEDURE ins
    (
        id_patient_in                IN pat_habit.id_patient%TYPE DEFAULT NULL,
        id_habit_in                  IN pat_habit.id_habit%TYPE DEFAULT NULL,
        flg_status_in                IN pat_habit.flg_status%TYPE DEFAULT NULL,
        id_prof_writes_in            IN pat_habit.id_prof_writes%TYPE DEFAULT NULL,
        notes_in                     IN pat_habit.notes%TYPE DEFAULT NULL,
        id_prof_cancel_in            IN pat_habit.id_prof_cancel%TYPE DEFAULT NULL,
        note_cancel_in               IN pat_habit.note_cancel%TYPE DEFAULT NULL,
        year_begin_in                IN pat_habit.year_begin%TYPE DEFAULT NULL,
        month_begin_in               IN pat_habit.month_begin%TYPE DEFAULT NULL,
        day_begin_in                 IN pat_habit.day_begin%TYPE DEFAULT NULL,
        year_end_in                  IN pat_habit.year_end%TYPE DEFAULT NULL,
        month_end_in                 IN pat_habit.month_end%TYPE DEFAULT NULL,
        day_end_in                   IN pat_habit.day_end%TYPE DEFAULT NULL,
        id_institution_in            IN pat_habit.id_institution%TYPE DEFAULT NULL,
        id_episode_in                IN pat_habit.id_episode%TYPE DEFAULT NULL,
        dt_pat_habit_tstz_in         IN pat_habit.dt_pat_habit_tstz%TYPE DEFAULT NULL,
        dt_cancel_tstz_in            IN pat_habit.dt_cancel_tstz%TYPE DEFAULT NULL,
        id_cancel_reason_in          IN pat_habit.id_cancel_reason%TYPE DEFAULT NULL,
        cancel_notes_in              IN pat_habit.cancel_notes%TYPE DEFAULT NULL,
        create_user_in               IN pat_habit.create_user%TYPE DEFAULT NULL,
        create_time_in               IN pat_habit.create_time%TYPE DEFAULT NULL,
        create_institution_in        IN pat_habit.create_institution%TYPE DEFAULT NULL,
        update_user_in               IN pat_habit.update_user%TYPE DEFAULT NULL,
        update_time_in               IN pat_habit.update_time%TYPE DEFAULT NULL,
        update_institution_in        IN pat_habit.update_institution%TYPE DEFAULT NULL,
        id_habit_characterization_in IN pat_habit.id_habit_characterization%TYPE DEFAULT NULL,
        handle_error_in              IN BOOLEAN := TRUE,
        rows_out                     OUT table_varchar
    ) IS
        l_pky pat_habit.id_pat_habit%TYPE := next_key;
    BEGIN
        ins(id_pat_habit_in              => l_pky,
            id_patient_in                => id_patient_in,
            id_habit_in                  => id_habit_in,
            flg_status_in                => flg_status_in,
            id_prof_writes_in            => id_prof_writes_in,
            notes_in                     => notes_in,
            id_prof_cancel_in            => id_prof_cancel_in,
            note_cancel_in               => note_cancel_in,
            year_begin_in                => year_begin_in,
            month_begin_in               => month_begin_in,
            day_begin_in                 => day_begin_in,
            year_end_in                  => year_end_in,
            month_end_in                 => month_end_in,
            day_end_in                   => day_end_in,
            id_institution_in            => id_institution_in,
            id_episode_in                => id_episode_in,
            dt_pat_habit_tstz_in         => dt_pat_habit_tstz_in,
            dt_cancel_tstz_in            => dt_cancel_tstz_in,
            id_cancel_reason_in          => id_cancel_reason_in,
            cancel_notes_in              => cancel_notes_in,
            create_user_in               => create_user_in,
            create_time_in               => create_time_in,
            create_institution_in        => create_institution_in,
            update_user_in               => update_user_in,
            update_time_in               => update_time_in,
            update_institution_in        => update_institution_in,
            id_habit_characterization_in => id_habit_characterization_in,
            handle_error_in              => handle_error_in,
            rows_out                     => rows_out);
    END ins;

    PROCEDURE ins
    (
        id_patient_in                IN pat_habit.id_patient%TYPE DEFAULT NULL,
        id_habit_in                  IN pat_habit.id_habit%TYPE DEFAULT NULL,
        flg_status_in                IN pat_habit.flg_status%TYPE DEFAULT NULL,
        id_prof_writes_in            IN pat_habit.id_prof_writes%TYPE DEFAULT NULL,
        notes_in                     IN pat_habit.notes%TYPE DEFAULT NULL,
        id_prof_cancel_in            IN pat_habit.id_prof_cancel%TYPE DEFAULT NULL,
        note_cancel_in               IN pat_habit.note_cancel%TYPE DEFAULT NULL,
        year_begin_in                IN pat_habit.year_begin%TYPE DEFAULT NULL,
        month_begin_in               IN pat_habit.month_begin%TYPE DEFAULT NULL,
        day_begin_in                 IN pat_habit.day_begin%TYPE DEFAULT NULL,
        year_end_in                  IN pat_habit.year_end%TYPE DEFAULT NULL,
        month_end_in                 IN pat_habit.month_end%TYPE DEFAULT NULL,
        day_end_in                   IN pat_habit.day_end%TYPE DEFAULT NULL,
        id_institution_in            IN pat_habit.id_institution%TYPE DEFAULT NULL,
        id_episode_in                IN pat_habit.id_episode%TYPE DEFAULT NULL,
        dt_pat_habit_tstz_in         IN pat_habit.dt_pat_habit_tstz%TYPE DEFAULT NULL,
        dt_cancel_tstz_in            IN pat_habit.dt_cancel_tstz%TYPE DEFAULT NULL,
        id_cancel_reason_in          IN pat_habit.id_cancel_reason%TYPE DEFAULT NULL,
        cancel_notes_in              IN pat_habit.cancel_notes%TYPE DEFAULT NULL,
        create_user_in               IN pat_habit.create_user%TYPE DEFAULT NULL,
        create_time_in               IN pat_habit.create_time%TYPE DEFAULT NULL,
        create_institution_in        IN pat_habit.create_institution%TYPE DEFAULT NULL,
        update_user_in               IN pat_habit.update_user%TYPE DEFAULT NULL,
        update_time_in               IN pat_habit.update_time%TYPE DEFAULT NULL,
        update_institution_in        IN pat_habit.update_institution%TYPE DEFAULT NULL,
        id_habit_characterization_in IN pat_habit.id_habit_characterization%TYPE DEFAULT NULL,
        handle_error_in              IN BOOLEAN := TRUE
    ) IS
        l_pky    pat_habit.id_pat_habit%TYPE := next_key;
        rows_out table_varchar;
    BEGIN
        ins(id_pat_habit_in              => l_pky,
            id_patient_in                => id_patient_in,
            id_habit_in                  => id_habit_in,
            flg_status_in                => flg_status_in,
            id_prof_writes_in            => id_prof_writes_in,
            notes_in                     => notes_in,
            id_prof_cancel_in            => id_prof_cancel_in,
            note_cancel_in               => note_cancel_in,
            year_begin_in                => year_begin_in,
            month_begin_in               => month_begin_in,
            day_begin_in                 => day_begin_in,
            year_end_in                  => year_end_in,
            month_end_in                 => month_end_in,
            day_end_in                   => day_end_in,
            id_institution_in            => id_institution_in,
            id_episode_in                => id_episode_in,
            dt_pat_habit_tstz_in         => dt_pat_habit_tstz_in,
            dt_cancel_tstz_in            => dt_cancel_tstz_in,
            id_cancel_reason_in          => id_cancel_reason_in,
            cancel_notes_in              => cancel_notes_in,
            create_user_in               => create_user_in,
            create_time_in               => create_time_in,
            create_institution_in        => create_institution_in,
            update_user_in               => update_user_in,
            update_time_in               => update_time_in,
            update_institution_in        => update_institution_in,
            id_habit_characterization_in => id_habit_characterization_in,
            handle_error_in              => handle_error_in,
            rows_out                     => rows_out);
    END ins;

    PROCEDURE ins
    (
        rows_in         IN pat_habit_tc,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) IS
    BEGIN
        IF rows_in.count = 0
        THEN
            NULL;
        ELSE
            FORALL indx IN rows_in.first .. rows_in.last SAVE EXCEPTIONS
                INSERT INTO pat_habit
                VALUES rows_in
                    (indx)
                RETURNING ROWID BULK COLLECT INTO rows_out;
        END IF;
    EXCEPTION
        WHEN e_forall_error THEN
            -- In Oracle9i and above, SAVE EXCEPTIONS will direct control
            -- here if any error occurs. We can then save all the error
            -- information out to the error instance.
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                <<bulk_handler>>
                DECLARE
                    l_err_instance_id NUMBER;
                BEGIN
                    -- For each error, write to the log.
                    FOR indx IN 1 .. SQL%bulk_exceptions.count
                    LOOP
                        pk_alert_exceptions.register_error(error_name_in       => 'FORALL-INSERT-FAILURE',
                                                           err_instance_id_out => l_err_instance_id);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'BINDING_ROW_' || indx,
                                                        value_in           => SQL%BULK_EXCEPTIONS(indx).error_index,
                                                        validate_in        => FALSE);
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'ERROR_AT_ROW_' || indx,
                                                        value_in           => SQL%BULK_EXCEPTIONS(indx).error_code,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'ID_PAT_HABIT _' || indx,
                                                        value_in           => rows_in(indx).id_pat_habit,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'ID_PATIENT _' || indx,
                                                        value_in           => rows_in(indx).id_patient,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'ID_HABIT _' || indx,
                                                        value_in           => rows_in(indx).id_habit,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'FLG_STATUS _' || indx,
                                                        value_in           => rows_in(indx).flg_status,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'ID_PROF_WRITES _' || indx,
                                                        value_in           => rows_in(indx).id_prof_writes,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'NOTES _' || indx,
                                                        value_in           => rows_in(indx).notes,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'ID_PROF_CANCEL _' || indx,
                                                        value_in           => rows_in(indx).id_prof_cancel,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'NOTE_CANCEL _' || indx,
                                                        value_in           => rows_in(indx).note_cancel,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'YEAR_BEGIN _' || indx,
                                                        value_in           => rows_in(indx).year_begin,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'MONTH_BEGIN _' || indx,
                                                        value_in           => rows_in(indx).month_begin,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'DAY_BEGIN _' || indx,
                                                        value_in           => rows_in(indx).day_begin,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'YEAR_END _' || indx,
                                                        value_in           => rows_in(indx).year_end,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'MONTH_END _' || indx,
                                                        value_in           => rows_in(indx).month_end,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'DAY_END _' || indx,
                                                        value_in           => rows_in(indx).day_end,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'ID_INSTITUTION _' || indx,
                                                        value_in           => rows_in(indx).id_institution,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'ID_EPISODE _' || indx,
                                                        value_in           => rows_in(indx).id_episode,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'DT_PAT_HABIT_TSTZ _' || indx,
                                                        value_in           => rows_in(indx).dt_pat_habit_tstz,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'DT_CANCEL_TSTZ _' || indx,
                                                        value_in           => rows_in(indx).dt_cancel_tstz,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'ID_CANCEL_REASON _' || indx,
                                                        value_in           => rows_in(indx).id_cancel_reason,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'CANCEL_NOTES _' || indx,
                                                        value_in           => rows_in(indx).cancel_notes,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'CREATE_USER _' || indx,
                                                        value_in           => rows_in(indx).create_user,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'CREATE_TIME _' || indx,
                                                        value_in           => rows_in(indx).create_time,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'CREATE_INSTITUTION _' || indx,
                                                        value_in           => rows_in(indx).create_institution,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'UPDATE_USER _' || indx,
                                                        value_in           => rows_in(indx).update_user,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'UPDATE_TIME _' || indx,
                                                        value_in           => rows_in(indx).update_time,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'UPDATE_INSTITUTION _' || indx,
                                                        value_in           => rows_in(indx).update_institution,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'ID_HABIT_CHARACTERIZATION _' || indx,
                                                        value_in           => rows_in(indx).id_habit_characterization,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_err_instance_id);
                    END LOOP;
                END bulk_handler;
            END IF;
        WHEN OTHERS THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                pk_alert_exceptions.raise_error(error_name_in => 'FORALL-INSERT-FAILURE',
                                                name1_in      => 'TABLE_NAME',
                                                value1_in     => 'PAT_HABIT',
                                                name2_in      => 'ROW_COUNT',
                                                value2_in     => rows_in.count);
            END IF;
    END ins;

    PROCEDURE ins
    (
        rows_in         IN pat_habit_tc,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        ins(rows_in => rows_in, handle_error_in => handle_error_in, rows_out => rows_out);
    END ins;

    PROCEDURE upd
    (
        id_pat_habit_in               IN pat_habit.id_pat_habit%TYPE,
        id_patient_in                 IN pat_habit.id_patient%TYPE DEFAULT NULL,
        id_patient_nin                IN BOOLEAN := TRUE,
        id_habit_in                   IN pat_habit.id_habit%TYPE DEFAULT NULL,
        id_habit_nin                  IN BOOLEAN := TRUE,
        flg_status_in                 IN pat_habit.flg_status%TYPE DEFAULT NULL,
        flg_status_nin                IN BOOLEAN := TRUE,
        id_prof_writes_in             IN pat_habit.id_prof_writes%TYPE DEFAULT NULL,
        id_prof_writes_nin            IN BOOLEAN := TRUE,
        notes_in                      IN pat_habit.notes%TYPE DEFAULT NULL,
        notes_nin                     IN BOOLEAN := TRUE,
        id_prof_cancel_in             IN pat_habit.id_prof_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_nin            IN BOOLEAN := TRUE,
        note_cancel_in                IN pat_habit.note_cancel%TYPE DEFAULT NULL,
        note_cancel_nin               IN BOOLEAN := TRUE,
        year_begin_in                 IN pat_habit.year_begin%TYPE DEFAULT NULL,
        year_begin_nin                IN BOOLEAN := TRUE,
        month_begin_in                IN pat_habit.month_begin%TYPE DEFAULT NULL,
        month_begin_nin               IN BOOLEAN := TRUE,
        day_begin_in                  IN pat_habit.day_begin%TYPE DEFAULT NULL,
        day_begin_nin                 IN BOOLEAN := TRUE,
        year_end_in                   IN pat_habit.year_end%TYPE DEFAULT NULL,
        year_end_nin                  IN BOOLEAN := TRUE,
        month_end_in                  IN pat_habit.month_end%TYPE DEFAULT NULL,
        month_end_nin                 IN BOOLEAN := TRUE,
        day_end_in                    IN pat_habit.day_end%TYPE DEFAULT NULL,
        day_end_nin                   IN BOOLEAN := TRUE,
        id_institution_in             IN pat_habit.id_institution%TYPE DEFAULT NULL,
        id_institution_nin            IN BOOLEAN := TRUE,
        id_episode_in                 IN pat_habit.id_episode%TYPE DEFAULT NULL,
        id_episode_nin                IN BOOLEAN := TRUE,
        dt_pat_habit_tstz_in          IN pat_habit.dt_pat_habit_tstz%TYPE DEFAULT NULL,
        dt_pat_habit_tstz_nin         IN BOOLEAN := TRUE,
        dt_cancel_tstz_in             IN pat_habit.dt_cancel_tstz%TYPE DEFAULT NULL,
        dt_cancel_tstz_nin            IN BOOLEAN := TRUE,
        id_cancel_reason_in           IN pat_habit.id_cancel_reason%TYPE DEFAULT NULL,
        id_cancel_reason_nin          IN BOOLEAN := TRUE,
        cancel_notes_in               IN pat_habit.cancel_notes%TYPE DEFAULT NULL,
        cancel_notes_nin              IN BOOLEAN := TRUE,
        create_user_in                IN pat_habit.create_user%TYPE DEFAULT NULL,
        create_user_nin               IN BOOLEAN := TRUE,
        create_time_in                IN pat_habit.create_time%TYPE DEFAULT NULL,
        create_time_nin               IN BOOLEAN := TRUE,
        create_institution_in         IN pat_habit.create_institution%TYPE DEFAULT NULL,
        create_institution_nin        IN BOOLEAN := TRUE,
        update_user_in                IN pat_habit.update_user%TYPE DEFAULT NULL,
        update_user_nin               IN BOOLEAN := TRUE,
        update_time_in                IN pat_habit.update_time%TYPE DEFAULT NULL,
        update_time_nin               IN BOOLEAN := TRUE,
        update_institution_in         IN pat_habit.update_institution%TYPE DEFAULT NULL,
        update_institution_nin        IN BOOLEAN := TRUE,
        id_habit_characterization_in  IN pat_habit.id_habit_characterization%TYPE DEFAULT NULL,
        id_habit_characterization_nin IN BOOLEAN := TRUE,
        handle_error_in               IN BOOLEAN := TRUE,
        rows_out                      IN OUT table_varchar
    ) IS
        l_rows_out                    table_varchar;
        l_id_patient_n                NUMBER(1);
        l_id_habit_n                  NUMBER(1);
        l_flg_status_n                NUMBER(1);
        l_id_prof_writes_n            NUMBER(1);
        l_notes_n                     NUMBER(1);
        l_id_prof_cancel_n            NUMBER(1);
        l_note_cancel_n               NUMBER(1);
        l_year_begin_n                NUMBER(1);
        l_month_begin_n               NUMBER(1);
        l_day_begin_n                 NUMBER(1);
        l_year_end_n                  NUMBER(1);
        l_month_end_n                 NUMBER(1);
        l_day_end_n                   NUMBER(1);
        l_id_institution_n            NUMBER(1);
        l_id_episode_n                NUMBER(1);
        l_dt_pat_habit_tstz_n         NUMBER(1);
        l_dt_cancel_tstz_n            NUMBER(1);
        l_id_cancel_reason_n          NUMBER(1);
        l_cancel_notes_n              NUMBER(1);
        l_create_user_n               NUMBER(1);
        l_create_time_n               NUMBER(1);
        l_create_institution_n        NUMBER(1);
        l_update_user_n               NUMBER(1);
        l_update_time_n               NUMBER(1);
        l_update_institution_n        NUMBER(1);
        l_id_habit_characterization_n NUMBER(1);
    BEGIN
    
        l_id_patient_n                := sys.diutil.bool_to_int(id_patient_nin);
        l_id_habit_n                  := sys.diutil.bool_to_int(id_habit_nin);
        l_flg_status_n                := sys.diutil.bool_to_int(flg_status_nin);
        l_id_prof_writes_n            := sys.diutil.bool_to_int(id_prof_writes_nin);
        l_notes_n                     := sys.diutil.bool_to_int(notes_nin);
        l_id_prof_cancel_n            := sys.diutil.bool_to_int(id_prof_cancel_nin);
        l_note_cancel_n               := sys.diutil.bool_to_int(note_cancel_nin);
        l_year_begin_n                := sys.diutil.bool_to_int(year_begin_nin);
        l_month_begin_n               := sys.diutil.bool_to_int(month_begin_nin);
        l_day_begin_n                 := sys.diutil.bool_to_int(day_begin_nin);
        l_year_end_n                  := sys.diutil.bool_to_int(year_end_nin);
        l_month_end_n                 := sys.diutil.bool_to_int(month_end_nin);
        l_day_end_n                   := sys.diutil.bool_to_int(day_end_nin);
        l_id_institution_n            := sys.diutil.bool_to_int(id_institution_nin);
        l_id_episode_n                := sys.diutil.bool_to_int(id_episode_nin);
        l_dt_pat_habit_tstz_n         := sys.diutil.bool_to_int(dt_pat_habit_tstz_nin);
        l_dt_cancel_tstz_n            := sys.diutil.bool_to_int(dt_cancel_tstz_nin);
        l_id_cancel_reason_n          := sys.diutil.bool_to_int(id_cancel_reason_nin);
        l_cancel_notes_n              := sys.diutil.bool_to_int(cancel_notes_nin);
        l_create_user_n               := sys.diutil.bool_to_int(create_user_nin);
        l_create_time_n               := sys.diutil.bool_to_int(create_time_nin);
        l_create_institution_n        := sys.diutil.bool_to_int(create_institution_nin);
        l_update_user_n               := sys.diutil.bool_to_int(update_user_nin);
        l_update_time_n               := sys.diutil.bool_to_int(update_time_nin);
        l_update_institution_n        := sys.diutil.bool_to_int(update_institution_nin);
        l_id_habit_characterization_n := sys.diutil.bool_to_int(id_habit_characterization_nin);
    
        UPDATE pat_habit
           SET id_patient                = decode(l_id_patient_n, 0, id_patient_in, nvl(id_patient_in, id_patient)),
               id_habit                  = decode(l_id_habit_n, 0, id_habit_in, nvl(id_habit_in, id_habit)),
               flg_status                = decode(l_flg_status_n, 0, flg_status_in, nvl(flg_status_in, flg_status)),
               id_prof_writes            = decode(l_id_prof_writes_n,
                                                  0,
                                                  id_prof_writes_in,
                                                  nvl(id_prof_writes_in, id_prof_writes)),
               notes                     = decode(l_notes_n, 0, notes_in, nvl(notes_in, notes)),
               id_prof_cancel            = decode(l_id_prof_cancel_n,
                                                  0,
                                                  id_prof_cancel_in,
                                                  nvl(id_prof_cancel_in, id_prof_cancel)),
               note_cancel               = decode(l_note_cancel_n, 0, note_cancel_in, nvl(note_cancel_in, note_cancel)),
               year_begin                = decode(l_year_begin_n, 0, year_begin_in, nvl(year_begin_in, year_begin)),
               month_begin               = decode(l_month_begin_n, 0, month_begin_in, nvl(month_begin_in, month_begin)),
               day_begin                 = decode(l_day_begin_n, 0, day_begin_in, nvl(day_begin_in, day_begin)),
               year_end                  = decode(l_year_end_n, 0, year_end_in, nvl(year_end_in, year_end)),
               month_end                 = decode(l_month_end_n, 0, month_end_in, nvl(month_end_in, month_end)),
               day_end                   = decode(l_day_end_n, 0, day_end_in, nvl(day_end_in, day_end)),
               id_institution            = decode(l_id_institution_n,
                                                  0,
                                                  id_institution_in,
                                                  nvl(id_institution_in, id_institution)),
               id_episode                = decode(l_id_episode_n, 0, id_episode_in, nvl(id_episode_in, id_episode)),
               dt_pat_habit_tstz         = decode(l_dt_pat_habit_tstz_n,
                                                  0,
                                                  dt_pat_habit_tstz_in,
                                                  nvl(dt_pat_habit_tstz_in, dt_pat_habit_tstz)),
               dt_cancel_tstz            = decode(l_dt_cancel_tstz_n,
                                                  0,
                                                  dt_cancel_tstz_in,
                                                  nvl(dt_cancel_tstz_in, dt_cancel_tstz)),
               id_cancel_reason          = decode(l_id_cancel_reason_n,
                                                  0,
                                                  id_cancel_reason_in,
                                                  nvl(id_cancel_reason_in, id_cancel_reason)),
               cancel_notes              = decode(l_cancel_notes_n,
                                                  0,
                                                  cancel_notes_in,
                                                  nvl(cancel_notes_in, cancel_notes)),
               create_user               = decode(l_create_user_n, 0, create_user_in, nvl(create_user_in, create_user)),
               create_time               = decode(l_create_time_n, 0, create_time_in, nvl(create_time_in, create_time)),
               create_institution        = decode(l_create_institution_n,
                                                  0,
                                                  create_institution_in,
                                                  nvl(create_institution_in, create_institution)),
               update_user               = decode(l_update_user_n, 0, update_user_in, nvl(update_user_in, update_user)),
               update_time               = decode(l_update_time_n, 0, update_time_in, nvl(update_time_in, update_time)),
               update_institution        = decode(l_update_institution_n,
                                                  0,
                                                  update_institution_in,
                                                  nvl(update_institution_in, update_institution)),
               id_habit_characterization = decode(l_id_habit_characterization_n,
                                                  0,
                                                  id_habit_characterization_in,
                                                  nvl(id_habit_characterization_in, id_habit_characterization))
         WHERE id_pat_habit = id_pat_habit_in
        RETURNING ROWID BULK COLLECT INTO l_rows_out;
    
        IF (rows_out IS NULL)
        THEN
            rows_out := table_varchar();
        END IF;
    
        rows_out := rows_out MULTISET UNION DISTINCT l_rows_out;
    
    EXCEPTION
        WHEN dup_val_on_index THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner           all_constraints.owner%TYPE;
                    l_name            all_constraints.constraint_name%TYPE;
                    l_err_instance_id PLS_INTEGER;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF FALSE
                    THEN
                        NULL; -- Placeholder in case no unique indexes
                    ELSE
                        pk_alert_exceptions.raise_error(error_name_in => 'DUPLICATE-VALUE',
                                                        name1_in      => 'OWNER',
                                                        value1_in     => l_owner,
                                                        name2_in      => 'CONSTRAINT_NAME',
                                                        value2_in     => l_name,
                                                        name3_in      => 'TABLE_NAME',
                                                        value3_in     => 'PAT_HABIT');
                    END IF;
                END;
            END IF;
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'PAT_HABIT');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'PAT_HABIT');
                    IF l_name = 'PH_CR_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_CANCEL_REASON',
                                                        value_in           => id_cancel_reason_in);
                    END IF;
                    IF l_name = 'PTNOT_EPIS_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_EPISODE',
                                                        value_in           => id_episode_in);
                    END IF;
                    IF l_name = 'PTNOT_HAT_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_HABIT',
                                                        value_in           => id_habit_in);
                    END IF;
                    IF l_name = 'PTNOT_INST_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_INSTITUTION',
                                                        value_in           => id_institution_in);
                    END IF;
                    IF l_name = 'PTNOT_PAT_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_PATIENT',
                                                        value_in           => id_patient_in);
                    END IF;
                    IF l_name = 'PTNOT_PROF_CANCEL_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_PROF_CANCEL',
                                                        value_in           => id_prof_cancel_in);
                    END IF;
                    IF l_name = 'PTNOT_PROF_WRITES_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_PROF_WRITES',
                                                        value_in           => id_prof_writes_in);
                    END IF;
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
        WHEN e_null_column_value THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    v_errm    VARCHAR2(2000) := dbms_utility.format_error_stack;
                    dot1loc   INTEGER;
                    dot2loc   INTEGER;
                    parenloc  INTEGER;
                    c_owner   all_constraints.owner%TYPE;
                    c_tabname all_tables.table_name%TYPE;
                    c_colname all_tab_columns.column_name%TYPE;
                BEGIN
                    dot1loc   := instr(v_errm, '.', 1, 1);
                    dot2loc   := instr(v_errm, '.', 1, 2);
                    parenloc  := instr(v_errm, '(');
                    c_owner   := substr(v_errm, parenloc + 1, dot1loc - parenloc - 1);
                    c_tabname := substr(v_errm, dot1loc + 1, dot2loc - dot1loc - 1);
                    c_colname := substr(v_errm, dot2loc + 1, instr(v_errm, ')') - dot2loc - 1);
                
                    pk_alert_exceptions.raise_error(error_name_in => 'COLUMN-CANNOT-BE-NULL',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => c_owner,
                                                    name2_in      => 'TABLE_NAME',
                                                    value2_in     => c_tabname,
                                                    name3_in      => 'COLUMN_NAME',
                                                    value3_in     => c_colname);
                END;
            END IF;
    END upd;

    PROCEDURE upd
    (
        id_pat_habit_in               IN pat_habit.id_pat_habit%TYPE,
        id_patient_in                 IN pat_habit.id_patient%TYPE DEFAULT NULL,
        id_patient_nin                IN BOOLEAN := TRUE,
        id_habit_in                   IN pat_habit.id_habit%TYPE DEFAULT NULL,
        id_habit_nin                  IN BOOLEAN := TRUE,
        flg_status_in                 IN pat_habit.flg_status%TYPE DEFAULT NULL,
        flg_status_nin                IN BOOLEAN := TRUE,
        id_prof_writes_in             IN pat_habit.id_prof_writes%TYPE DEFAULT NULL,
        id_prof_writes_nin            IN BOOLEAN := TRUE,
        notes_in                      IN pat_habit.notes%TYPE DEFAULT NULL,
        notes_nin                     IN BOOLEAN := TRUE,
        id_prof_cancel_in             IN pat_habit.id_prof_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_nin            IN BOOLEAN := TRUE,
        note_cancel_in                IN pat_habit.note_cancel%TYPE DEFAULT NULL,
        note_cancel_nin               IN BOOLEAN := TRUE,
        year_begin_in                 IN pat_habit.year_begin%TYPE DEFAULT NULL,
        year_begin_nin                IN BOOLEAN := TRUE,
        month_begin_in                IN pat_habit.month_begin%TYPE DEFAULT NULL,
        month_begin_nin               IN BOOLEAN := TRUE,
        day_begin_in                  IN pat_habit.day_begin%TYPE DEFAULT NULL,
        day_begin_nin                 IN BOOLEAN := TRUE,
        year_end_in                   IN pat_habit.year_end%TYPE DEFAULT NULL,
        year_end_nin                  IN BOOLEAN := TRUE,
        month_end_in                  IN pat_habit.month_end%TYPE DEFAULT NULL,
        month_end_nin                 IN BOOLEAN := TRUE,
        day_end_in                    IN pat_habit.day_end%TYPE DEFAULT NULL,
        day_end_nin                   IN BOOLEAN := TRUE,
        id_institution_in             IN pat_habit.id_institution%TYPE DEFAULT NULL,
        id_institution_nin            IN BOOLEAN := TRUE,
        id_episode_in                 IN pat_habit.id_episode%TYPE DEFAULT NULL,
        id_episode_nin                IN BOOLEAN := TRUE,
        dt_pat_habit_tstz_in          IN pat_habit.dt_pat_habit_tstz%TYPE DEFAULT NULL,
        dt_pat_habit_tstz_nin         IN BOOLEAN := TRUE,
        dt_cancel_tstz_in             IN pat_habit.dt_cancel_tstz%TYPE DEFAULT NULL,
        dt_cancel_tstz_nin            IN BOOLEAN := TRUE,
        id_cancel_reason_in           IN pat_habit.id_cancel_reason%TYPE DEFAULT NULL,
        id_cancel_reason_nin          IN BOOLEAN := TRUE,
        cancel_notes_in               IN pat_habit.cancel_notes%TYPE DEFAULT NULL,
        cancel_notes_nin              IN BOOLEAN := TRUE,
        create_user_in                IN pat_habit.create_user%TYPE DEFAULT NULL,
        create_user_nin               IN BOOLEAN := TRUE,
        create_time_in                IN pat_habit.create_time%TYPE DEFAULT NULL,
        create_time_nin               IN BOOLEAN := TRUE,
        create_institution_in         IN pat_habit.create_institution%TYPE DEFAULT NULL,
        create_institution_nin        IN BOOLEAN := TRUE,
        update_user_in                IN pat_habit.update_user%TYPE DEFAULT NULL,
        update_user_nin               IN BOOLEAN := TRUE,
        update_time_in                IN pat_habit.update_time%TYPE DEFAULT NULL,
        update_time_nin               IN BOOLEAN := TRUE,
        update_institution_in         IN pat_habit.update_institution%TYPE DEFAULT NULL,
        update_institution_nin        IN BOOLEAN := TRUE,
        id_habit_characterization_in  IN pat_habit.id_habit_characterization%TYPE DEFAULT NULL,
        id_habit_characterization_nin IN BOOLEAN := TRUE,
        handle_error_in               IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        upd(id_pat_habit_in               => id_pat_habit_in,
            id_patient_in                 => id_patient_in,
            id_patient_nin                => id_patient_nin,
            id_habit_in                   => id_habit_in,
            id_habit_nin                  => id_habit_nin,
            flg_status_in                 => flg_status_in,
            flg_status_nin                => flg_status_nin,
            id_prof_writes_in             => id_prof_writes_in,
            id_prof_writes_nin            => id_prof_writes_nin,
            notes_in                      => notes_in,
            notes_nin                     => notes_nin,
            id_prof_cancel_in             => id_prof_cancel_in,
            id_prof_cancel_nin            => id_prof_cancel_nin,
            note_cancel_in                => note_cancel_in,
            note_cancel_nin               => note_cancel_nin,
            year_begin_in                 => year_begin_in,
            year_begin_nin                => year_begin_nin,
            month_begin_in                => month_begin_in,
            month_begin_nin               => month_begin_nin,
            day_begin_in                  => day_begin_in,
            day_begin_nin                 => day_begin_nin,
            year_end_in                   => year_end_in,
            year_end_nin                  => year_end_nin,
            month_end_in                  => month_end_in,
            month_end_nin                 => month_end_nin,
            day_end_in                    => day_end_in,
            day_end_nin                   => day_end_nin,
            id_institution_in             => id_institution_in,
            id_institution_nin            => id_institution_nin,
            id_episode_in                 => id_episode_in,
            id_episode_nin                => id_episode_nin,
            dt_pat_habit_tstz_in          => dt_pat_habit_tstz_in,
            dt_pat_habit_tstz_nin         => dt_pat_habit_tstz_nin,
            dt_cancel_tstz_in             => dt_cancel_tstz_in,
            dt_cancel_tstz_nin            => dt_cancel_tstz_nin,
            id_cancel_reason_in           => id_cancel_reason_in,
            id_cancel_reason_nin          => id_cancel_reason_nin,
            cancel_notes_in               => cancel_notes_in,
            cancel_notes_nin              => cancel_notes_nin,
            create_user_in                => create_user_in,
            create_user_nin               => create_user_nin,
            create_time_in                => create_time_in,
            create_time_nin               => create_time_nin,
            create_institution_in         => create_institution_in,
            create_institution_nin        => create_institution_nin,
            update_user_in                => update_user_in,
            update_user_nin               => update_user_nin,
            update_time_in                => update_time_in,
            update_time_nin               => update_time_nin,
            update_institution_in         => update_institution_in,
            update_institution_nin        => update_institution_nin,
            id_habit_characterization_in  => id_habit_characterization_in,
            id_habit_characterization_nin => id_habit_characterization_nin,
            handle_error_in               => handle_error_in,
            rows_out                      => rows_out);
    END upd;

    PROCEDURE upd
    (
        id_patient_in                 IN pat_habit.id_patient%TYPE DEFAULT NULL,
        id_patient_nin                IN BOOLEAN := TRUE,
        id_habit_in                   IN pat_habit.id_habit%TYPE DEFAULT NULL,
        id_habit_nin                  IN BOOLEAN := TRUE,
        flg_status_in                 IN pat_habit.flg_status%TYPE DEFAULT NULL,
        flg_status_nin                IN BOOLEAN := TRUE,
        id_prof_writes_in             IN pat_habit.id_prof_writes%TYPE DEFAULT NULL,
        id_prof_writes_nin            IN BOOLEAN := TRUE,
        notes_in                      IN pat_habit.notes%TYPE DEFAULT NULL,
        notes_nin                     IN BOOLEAN := TRUE,
        id_prof_cancel_in             IN pat_habit.id_prof_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_nin            IN BOOLEAN := TRUE,
        note_cancel_in                IN pat_habit.note_cancel%TYPE DEFAULT NULL,
        note_cancel_nin               IN BOOLEAN := TRUE,
        year_begin_in                 IN pat_habit.year_begin%TYPE DEFAULT NULL,
        year_begin_nin                IN BOOLEAN := TRUE,
        month_begin_in                IN pat_habit.month_begin%TYPE DEFAULT NULL,
        month_begin_nin               IN BOOLEAN := TRUE,
        day_begin_in                  IN pat_habit.day_begin%TYPE DEFAULT NULL,
        day_begin_nin                 IN BOOLEAN := TRUE,
        year_end_in                   IN pat_habit.year_end%TYPE DEFAULT NULL,
        year_end_nin                  IN BOOLEAN := TRUE,
        month_end_in                  IN pat_habit.month_end%TYPE DEFAULT NULL,
        month_end_nin                 IN BOOLEAN := TRUE,
        day_end_in                    IN pat_habit.day_end%TYPE DEFAULT NULL,
        day_end_nin                   IN BOOLEAN := TRUE,
        id_institution_in             IN pat_habit.id_institution%TYPE DEFAULT NULL,
        id_institution_nin            IN BOOLEAN := TRUE,
        id_episode_in                 IN pat_habit.id_episode%TYPE DEFAULT NULL,
        id_episode_nin                IN BOOLEAN := TRUE,
        dt_pat_habit_tstz_in          IN pat_habit.dt_pat_habit_tstz%TYPE DEFAULT NULL,
        dt_pat_habit_tstz_nin         IN BOOLEAN := TRUE,
        dt_cancel_tstz_in             IN pat_habit.dt_cancel_tstz%TYPE DEFAULT NULL,
        dt_cancel_tstz_nin            IN BOOLEAN := TRUE,
        id_cancel_reason_in           IN pat_habit.id_cancel_reason%TYPE DEFAULT NULL,
        id_cancel_reason_nin          IN BOOLEAN := TRUE,
        cancel_notes_in               IN pat_habit.cancel_notes%TYPE DEFAULT NULL,
        cancel_notes_nin              IN BOOLEAN := TRUE,
        create_user_in                IN pat_habit.create_user%TYPE DEFAULT NULL,
        create_user_nin               IN BOOLEAN := TRUE,
        create_time_in                IN pat_habit.create_time%TYPE DEFAULT NULL,
        create_time_nin               IN BOOLEAN := TRUE,
        create_institution_in         IN pat_habit.create_institution%TYPE DEFAULT NULL,
        create_institution_nin        IN BOOLEAN := TRUE,
        update_user_in                IN pat_habit.update_user%TYPE DEFAULT NULL,
        update_user_nin               IN BOOLEAN := TRUE,
        update_time_in                IN pat_habit.update_time%TYPE DEFAULT NULL,
        update_time_nin               IN BOOLEAN := TRUE,
        update_institution_in         IN pat_habit.update_institution%TYPE DEFAULT NULL,
        update_institution_nin        IN BOOLEAN := TRUE,
        id_habit_characterization_in  IN pat_habit.id_habit_characterization%TYPE DEFAULT NULL,
        id_habit_characterization_nin IN BOOLEAN := TRUE,
        where_in                      VARCHAR2 DEFAULT NULL,
        handle_error_in               IN BOOLEAN := TRUE,
        rows_out                      IN OUT table_varchar
    ) IS
        l_sql                         VARCHAR2(32767);
        l_rows_out                    table_varchar;
        l_id_patient_n                NUMBER(1);
        l_id_habit_n                  NUMBER(1);
        l_flg_status_n                NUMBER(1);
        l_id_prof_writes_n            NUMBER(1);
        l_notes_n                     NUMBER(1);
        l_id_prof_cancel_n            NUMBER(1);
        l_note_cancel_n               NUMBER(1);
        l_year_begin_n                NUMBER(1);
        l_month_begin_n               NUMBER(1);
        l_day_begin_n                 NUMBER(1);
        l_year_end_n                  NUMBER(1);
        l_month_end_n                 NUMBER(1);
        l_day_end_n                   NUMBER(1);
        l_id_institution_n            NUMBER(1);
        l_id_episode_n                NUMBER(1);
        l_dt_pat_habit_tstz_n         NUMBER(1);
        l_dt_cancel_tstz_n            NUMBER(1);
        l_id_cancel_reason_n          NUMBER(1);
        l_cancel_notes_n              NUMBER(1);
        l_create_user_n               NUMBER(1);
        l_create_time_n               NUMBER(1);
        l_create_institution_n        NUMBER(1);
        l_update_user_n               NUMBER(1);
        l_update_time_n               NUMBER(1);
        l_update_institution_n        NUMBER(1);
        l_id_habit_characterization_n NUMBER(1);
        id_pat_habit_in               pat_habit.id_pat_habit%TYPE;
    BEGIN
    
        l_id_patient_n                := sys.diutil.bool_to_int(id_patient_nin);
        l_id_habit_n                  := sys.diutil.bool_to_int(id_habit_nin);
        l_flg_status_n                := sys.diutil.bool_to_int(flg_status_nin);
        l_id_prof_writes_n            := sys.diutil.bool_to_int(id_prof_writes_nin);
        l_notes_n                     := sys.diutil.bool_to_int(notes_nin);
        l_id_prof_cancel_n            := sys.diutil.bool_to_int(id_prof_cancel_nin);
        l_note_cancel_n               := sys.diutil.bool_to_int(note_cancel_nin);
        l_year_begin_n                := sys.diutil.bool_to_int(year_begin_nin);
        l_month_begin_n               := sys.diutil.bool_to_int(month_begin_nin);
        l_day_begin_n                 := sys.diutil.bool_to_int(day_begin_nin);
        l_year_end_n                  := sys.diutil.bool_to_int(year_end_nin);
        l_month_end_n                 := sys.diutil.bool_to_int(month_end_nin);
        l_day_end_n                   := sys.diutil.bool_to_int(day_end_nin);
        l_id_institution_n            := sys.diutil.bool_to_int(id_institution_nin);
        l_id_episode_n                := sys.diutil.bool_to_int(id_episode_nin);
        l_dt_pat_habit_tstz_n         := sys.diutil.bool_to_int(dt_pat_habit_tstz_nin);
        l_dt_cancel_tstz_n            := sys.diutil.bool_to_int(dt_cancel_tstz_nin);
        l_id_cancel_reason_n          := sys.diutil.bool_to_int(id_cancel_reason_nin);
        l_cancel_notes_n              := sys.diutil.bool_to_int(cancel_notes_nin);
        l_create_user_n               := sys.diutil.bool_to_int(create_user_nin);
        l_create_time_n               := sys.diutil.bool_to_int(create_time_nin);
        l_create_institution_n        := sys.diutil.bool_to_int(create_institution_nin);
        l_update_user_n               := sys.diutil.bool_to_int(update_user_nin);
        l_update_time_n               := sys.diutil.bool_to_int(update_time_nin);
        l_update_institution_n        := sys.diutil.bool_to_int(update_institution_nin);
        l_id_habit_characterization_n := sys.diutil.bool_to_int(id_habit_characterization_nin);
    
        l_sql := 'UPDATE PAT_HABIT SET ' || ' ID_PATIENT = decode (' || l_id_patient_n ||
                 ',0,:id_patient_in, NVL (:id_patient_in, ID_PATIENT)) ' || ',' || ' ID_HABIT = decode (' ||
                 l_id_habit_n || ',0,:id_habit_in, NVL (:id_habit_in, ID_HABIT)) ' || ',' || ' FLG_STATUS = decode (' ||
                 l_flg_status_n || ',0,:flg_status_in, NVL (:flg_status_in, FLG_STATUS)) ' || ',' ||
                 ' ID_PROF_WRITES = decode (' || l_id_prof_writes_n ||
                 ',0,:id_prof_writes_in, NVL (:id_prof_writes_in, ID_PROF_WRITES)) ' || ',' || ' NOTES = decode (' ||
                 l_notes_n || ',0,:notes_in, NVL (:notes_in, NOTES)) ' || ',' || ' ID_PROF_CANCEL = decode (' ||
                 l_id_prof_cancel_n || ',0,:id_prof_cancel_in, NVL (:id_prof_cancel_in, ID_PROF_CANCEL)) ' || ',' ||
                 ' NOTE_CANCEL = decode (' || l_note_cancel_n ||
                 ',0,:note_cancel_in, NVL (:note_cancel_in, NOTE_CANCEL)) ' || ',' || ' YEAR_BEGIN = decode (' ||
                 l_year_begin_n || ',0,:year_begin_in, NVL (:year_begin_in, YEAR_BEGIN)) ' || ',' ||
                 ' MONTH_BEGIN = decode (' || l_month_begin_n ||
                 ',0,:month_begin_in, NVL (:month_begin_in, MONTH_BEGIN)) ' || ',' || ' DAY_BEGIN = decode (' ||
                 l_day_begin_n || ',0,:day_begin_in, NVL (:day_begin_in, DAY_BEGIN)) ' || ',' || ' YEAR_END = decode (' ||
                 l_year_end_n || ',0,:year_end_in, NVL (:year_end_in, YEAR_END)) ' || ',' || ' MONTH_END = decode (' ||
                 l_month_end_n || ',0,:month_end_in, NVL (:month_end_in, MONTH_END)) ' || ',' || ' DAY_END = decode (' ||
                 l_day_end_n || ',0,:day_end_in, NVL (:day_end_in, DAY_END)) ' || ',' || ' ID_INSTITUTION = decode (' ||
                 l_id_institution_n || ',0,:id_institution_in, NVL (:id_institution_in, ID_INSTITUTION)) ' || ',' ||
                 ' ID_EPISODE = decode (' || l_id_episode_n || ',0,:id_episode_in, NVL (:id_episode_in, ID_EPISODE)) ' || ',' ||
                 ' DT_PAT_HABIT_TSTZ = decode (' || l_dt_pat_habit_tstz_n ||
                 ',0,:dt_pat_habit_tstz_in, NVL (:dt_pat_habit_tstz_in, DT_PAT_HABIT_TSTZ)) ' || ',' ||
                 ' DT_CANCEL_TSTZ = decode (' || l_dt_cancel_tstz_n ||
                 ',0,:dt_cancel_tstz_in, NVL (:dt_cancel_tstz_in, DT_CANCEL_TSTZ)) ' || ',' ||
                 ' ID_CANCEL_REASON = decode (' || l_id_cancel_reason_n ||
                 ',0,:id_cancel_reason_in, NVL (:id_cancel_reason_in, ID_CANCEL_REASON)) ' || ',' ||
                 ' CANCEL_NOTES = decode (' || l_cancel_notes_n ||
                 ',0,:cancel_notes_in, NVL (:cancel_notes_in, CANCEL_NOTES)) ' || ',' || ' CREATE_USER = decode (' ||
                 l_create_user_n || ',0,:create_user_in, NVL (:create_user_in, CREATE_USER)) ' || ',' ||
                 ' CREATE_TIME = decode (' || l_create_time_n ||
                 ',0,:create_time_in, NVL (:create_time_in, CREATE_TIME)) ' || ',' || ' CREATE_INSTITUTION = decode (' ||
                 l_create_institution_n ||
                 ',0,:create_institution_in, NVL (:create_institution_in, CREATE_INSTITUTION)) ' || ',' ||
                 ' UPDATE_USER = decode (' || l_update_user_n ||
                 ',0,:update_user_in, NVL (:update_user_in, UPDATE_USER)) ' || ',' || ' UPDATE_TIME = decode (' ||
                 l_update_time_n || ',0,:update_time_in, NVL (:update_time_in, UPDATE_TIME)) ' || ',' ||
                 ' UPDATE_INSTITUTION = decode (' || l_update_institution_n ||
                 ',0,:update_institution_in, NVL (:update_institution_in, UPDATE_INSTITUTION)) ' || ',' ||
                 ' ID_HABIT_CHARACTERIZATION = decode (' || l_id_habit_characterization_n ||
                 ',0,:id_habit_characterization_in, NVL (:id_habit_characterization_in, ID_HABIT_CHARACTERIZATION)) ' ||
                 ' where ' || nvl(where_in, '(1=1)') || ' RETURNING ROWID BULK COLLECT INTO :l_rows_out';
    
        EXECUTE IMMEDIATE 'BEGIN ' || l_sql || '; END;'
            USING IN id_patient_in, id_habit_in, flg_status_in, id_prof_writes_in, notes_in, id_prof_cancel_in, note_cancel_in, year_begin_in, month_begin_in, day_begin_in, year_end_in, month_end_in, day_end_in, id_institution_in, id_episode_in, dt_pat_habit_tstz_in, dt_cancel_tstz_in, id_cancel_reason_in, cancel_notes_in, create_user_in, create_time_in, create_institution_in, update_user_in, update_time_in, update_institution_in, id_habit_characterization_in, OUT l_rows_out;
    
        IF (rows_out IS NULL)
        THEN
            rows_out := table_varchar();
        END IF;
    
        rows_out := rows_out MULTISET UNION DISTINCT l_rows_out;
    
    EXCEPTION
        WHEN dup_val_on_index THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner           all_constraints.owner%TYPE;
                    l_name            all_constraints.constraint_name%TYPE;
                    l_err_instance_id PLS_INTEGER;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF FALSE
                    THEN
                        NULL; -- Placeholder in case no unique indexes
                    ELSE
                        pk_alert_exceptions.raise_error(error_name_in => 'DUPLICATE-VALUE',
                                                        name1_in      => 'OWNER',
                                                        value1_in     => l_owner,
                                                        name2_in      => 'CONSTRAINT_NAME',
                                                        value2_in     => l_name,
                                                        name3_in      => 'TABLE_NAME',
                                                        value3_in     => 'PAT_HABIT');
                    END IF;
                END;
            END IF;
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'PAT_HABIT');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'PAT_HABIT');
                    IF l_name = 'PH_CR_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_CANCEL_REASON',
                                                        value_in           => id_cancel_reason_in);
                    END IF;
                    IF l_name = 'PTNOT_EPIS_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_EPISODE',
                                                        value_in           => id_episode_in);
                    END IF;
                    IF l_name = 'PTNOT_HAT_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_HABIT',
                                                        value_in           => id_habit_in);
                    END IF;
                    IF l_name = 'PTNOT_INST_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_INSTITUTION',
                                                        value_in           => id_institution_in);
                    END IF;
                    IF l_name = 'PTNOT_PAT_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_PATIENT',
                                                        value_in           => id_patient_in);
                    END IF;
                    IF l_name = 'PTNOT_PROF_CANCEL_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_PROF_CANCEL',
                                                        value_in           => id_prof_cancel_in);
                    END IF;
                    IF l_name = 'PTNOT_PROF_WRITES_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_PROF_WRITES',
                                                        value_in           => id_prof_writes_in);
                    END IF;
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
        WHEN e_null_column_value THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    v_errm    VARCHAR2(2000) := dbms_utility.format_error_stack;
                    dot1loc   INTEGER;
                    dot2loc   INTEGER;
                    parenloc  INTEGER;
                    c_owner   all_constraints.owner%TYPE;
                    c_tabname all_tables.table_name%TYPE;
                    c_colname all_tab_columns.column_name%TYPE;
                BEGIN
                    dot1loc   := instr(v_errm, '.', 1, 1);
                    dot2loc   := instr(v_errm, '.', 1, 2);
                    parenloc  := instr(v_errm, '(');
                    c_owner   := substr(v_errm, parenloc + 1, dot1loc - parenloc - 1);
                    c_tabname := substr(v_errm, dot1loc + 1, dot2loc - dot1loc - 1);
                    c_colname := substr(v_errm, dot2loc + 1, instr(v_errm, ')') - dot2loc - 1);
                
                    pk_alert_exceptions.raise_error(error_name_in => 'COLUMN-CANNOT-BE-NULL',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => c_owner,
                                                    name2_in      => 'TABLE_NAME',
                                                    value2_in     => c_tabname,
                                                    name3_in      => 'COLUMN_NAME',
                                                    value3_in     => c_colname);
                END;
            END IF;
    END upd;

    PROCEDURE upd
    (
        id_patient_in                 IN pat_habit.id_patient%TYPE DEFAULT NULL,
        id_patient_nin                IN BOOLEAN := TRUE,
        id_habit_in                   IN pat_habit.id_habit%TYPE DEFAULT NULL,
        id_habit_nin                  IN BOOLEAN := TRUE,
        flg_status_in                 IN pat_habit.flg_status%TYPE DEFAULT NULL,
        flg_status_nin                IN BOOLEAN := TRUE,
        id_prof_writes_in             IN pat_habit.id_prof_writes%TYPE DEFAULT NULL,
        id_prof_writes_nin            IN BOOLEAN := TRUE,
        notes_in                      IN pat_habit.notes%TYPE DEFAULT NULL,
        notes_nin                     IN BOOLEAN := TRUE,
        id_prof_cancel_in             IN pat_habit.id_prof_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_nin            IN BOOLEAN := TRUE,
        note_cancel_in                IN pat_habit.note_cancel%TYPE DEFAULT NULL,
        note_cancel_nin               IN BOOLEAN := TRUE,
        year_begin_in                 IN pat_habit.year_begin%TYPE DEFAULT NULL,
        year_begin_nin                IN BOOLEAN := TRUE,
        month_begin_in                IN pat_habit.month_begin%TYPE DEFAULT NULL,
        month_begin_nin               IN BOOLEAN := TRUE,
        day_begin_in                  IN pat_habit.day_begin%TYPE DEFAULT NULL,
        day_begin_nin                 IN BOOLEAN := TRUE,
        year_end_in                   IN pat_habit.year_end%TYPE DEFAULT NULL,
        year_end_nin                  IN BOOLEAN := TRUE,
        month_end_in                  IN pat_habit.month_end%TYPE DEFAULT NULL,
        month_end_nin                 IN BOOLEAN := TRUE,
        day_end_in                    IN pat_habit.day_end%TYPE DEFAULT NULL,
        day_end_nin                   IN BOOLEAN := TRUE,
        id_institution_in             IN pat_habit.id_institution%TYPE DEFAULT NULL,
        id_institution_nin            IN BOOLEAN := TRUE,
        id_episode_in                 IN pat_habit.id_episode%TYPE DEFAULT NULL,
        id_episode_nin                IN BOOLEAN := TRUE,
        dt_pat_habit_tstz_in          IN pat_habit.dt_pat_habit_tstz%TYPE DEFAULT NULL,
        dt_pat_habit_tstz_nin         IN BOOLEAN := TRUE,
        dt_cancel_tstz_in             IN pat_habit.dt_cancel_tstz%TYPE DEFAULT NULL,
        dt_cancel_tstz_nin            IN BOOLEAN := TRUE,
        id_cancel_reason_in           IN pat_habit.id_cancel_reason%TYPE DEFAULT NULL,
        id_cancel_reason_nin          IN BOOLEAN := TRUE,
        cancel_notes_in               IN pat_habit.cancel_notes%TYPE DEFAULT NULL,
        cancel_notes_nin              IN BOOLEAN := TRUE,
        create_user_in                IN pat_habit.create_user%TYPE DEFAULT NULL,
        create_user_nin               IN BOOLEAN := TRUE,
        create_time_in                IN pat_habit.create_time%TYPE DEFAULT NULL,
        create_time_nin               IN BOOLEAN := TRUE,
        create_institution_in         IN pat_habit.create_institution%TYPE DEFAULT NULL,
        create_institution_nin        IN BOOLEAN := TRUE,
        update_user_in                IN pat_habit.update_user%TYPE DEFAULT NULL,
        update_user_nin               IN BOOLEAN := TRUE,
        update_time_in                IN pat_habit.update_time%TYPE DEFAULT NULL,
        update_time_nin               IN BOOLEAN := TRUE,
        update_institution_in         IN pat_habit.update_institution%TYPE DEFAULT NULL,
        update_institution_nin        IN BOOLEAN := TRUE,
        id_habit_characterization_in  IN pat_habit.id_habit_characterization%TYPE DEFAULT NULL,
        id_habit_characterization_nin IN BOOLEAN := TRUE,
        where_in                      VARCHAR2 DEFAULT NULL,
        handle_error_in               IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        upd(id_patient_in                 => id_patient_in,
            id_patient_nin                => id_patient_nin,
            id_habit_in                   => id_habit_in,
            id_habit_nin                  => id_habit_nin,
            flg_status_in                 => flg_status_in,
            flg_status_nin                => flg_status_nin,
            id_prof_writes_in             => id_prof_writes_in,
            id_prof_writes_nin            => id_prof_writes_nin,
            notes_in                      => notes_in,
            notes_nin                     => notes_nin,
            id_prof_cancel_in             => id_prof_cancel_in,
            id_prof_cancel_nin            => id_prof_cancel_nin,
            note_cancel_in                => note_cancel_in,
            note_cancel_nin               => note_cancel_nin,
            year_begin_in                 => year_begin_in,
            year_begin_nin                => year_begin_nin,
            month_begin_in                => month_begin_in,
            month_begin_nin               => month_begin_nin,
            day_begin_in                  => day_begin_in,
            day_begin_nin                 => day_begin_nin,
            year_end_in                   => year_end_in,
            year_end_nin                  => year_end_nin,
            month_end_in                  => month_end_in,
            month_end_nin                 => month_end_nin,
            day_end_in                    => day_end_in,
            day_end_nin                   => day_end_nin,
            id_institution_in             => id_institution_in,
            id_institution_nin            => id_institution_nin,
            id_episode_in                 => id_episode_in,
            id_episode_nin                => id_episode_nin,
            dt_pat_habit_tstz_in          => dt_pat_habit_tstz_in,
            dt_pat_habit_tstz_nin         => dt_pat_habit_tstz_nin,
            dt_cancel_tstz_in             => dt_cancel_tstz_in,
            dt_cancel_tstz_nin            => dt_cancel_tstz_nin,
            id_cancel_reason_in           => id_cancel_reason_in,
            id_cancel_reason_nin          => id_cancel_reason_nin,
            cancel_notes_in               => cancel_notes_in,
            cancel_notes_nin              => cancel_notes_nin,
            create_user_in                => create_user_in,
            create_user_nin               => create_user_nin,
            create_time_in                => create_time_in,
            create_time_nin               => create_time_nin,
            create_institution_in         => create_institution_in,
            create_institution_nin        => create_institution_nin,
            update_user_in                => update_user_in,
            update_user_nin               => update_user_nin,
            update_time_in                => update_time_in,
            update_time_nin               => update_time_nin,
            update_institution_in         => update_institution_in,
            update_institution_nin        => update_institution_nin,
            id_habit_characterization_in  => id_habit_characterization_in,
            id_habit_characterization_nin => id_habit_characterization_nin,
            where_in                      => where_in,
            handle_error_in               => handle_error_in,
            rows_out                      => rows_out);
    END upd;

    PROCEDURE upd
    (
        rec_in          IN pat_habit%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        IN OUT table_varchar
    ) IS
    BEGIN
        upd(id_pat_habit_in              => rec_in.id_pat_habit,
            id_patient_in                => rec_in.id_patient,
            id_habit_in                  => rec_in.id_habit,
            flg_status_in                => rec_in.flg_status,
            id_prof_writes_in            => rec_in.id_prof_writes,
            notes_in                     => rec_in.notes,
            id_prof_cancel_in            => rec_in.id_prof_cancel,
            note_cancel_in               => rec_in.note_cancel,
            year_begin_in                => rec_in.year_begin,
            month_begin_in               => rec_in.month_begin,
            day_begin_in                 => rec_in.day_begin,
            year_end_in                  => rec_in.year_end,
            month_end_in                 => rec_in.month_end,
            day_end_in                   => rec_in.day_end,
            id_institution_in            => rec_in.id_institution,
            id_episode_in                => rec_in.id_episode,
            dt_pat_habit_tstz_in         => rec_in.dt_pat_habit_tstz,
            dt_cancel_tstz_in            => rec_in.dt_cancel_tstz,
            id_cancel_reason_in          => rec_in.id_cancel_reason,
            cancel_notes_in              => rec_in.cancel_notes,
            create_user_in               => rec_in.create_user,
            create_time_in               => rec_in.create_time,
            create_institution_in        => rec_in.create_institution,
            update_user_in               => rec_in.update_user,
            update_time_in               => rec_in.update_time,
            update_institution_in        => rec_in.update_institution,
            id_habit_characterization_in => rec_in.id_habit_characterization
            
           ,
            handle_error_in => handle_error_in,
            rows_out        => rows_out);
    END upd;

    PROCEDURE upd
    (
        rec_in          IN pat_habit%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        upd(id_pat_habit_in              => rec_in.id_pat_habit,
            id_patient_in                => rec_in.id_patient,
            id_habit_in                  => rec_in.id_habit,
            flg_status_in                => rec_in.flg_status,
            id_prof_writes_in            => rec_in.id_prof_writes,
            notes_in                     => rec_in.notes,
            id_prof_cancel_in            => rec_in.id_prof_cancel,
            note_cancel_in               => rec_in.note_cancel,
            year_begin_in                => rec_in.year_begin,
            month_begin_in               => rec_in.month_begin,
            day_begin_in                 => rec_in.day_begin,
            year_end_in                  => rec_in.year_end,
            month_end_in                 => rec_in.month_end,
            day_end_in                   => rec_in.day_end,
            id_institution_in            => rec_in.id_institution,
            id_episode_in                => rec_in.id_episode,
            dt_pat_habit_tstz_in         => rec_in.dt_pat_habit_tstz,
            dt_cancel_tstz_in            => rec_in.dt_cancel_tstz,
            id_cancel_reason_in          => rec_in.id_cancel_reason,
            cancel_notes_in              => rec_in.cancel_notes,
            create_user_in               => rec_in.create_user,
            create_time_in               => rec_in.create_time,
            create_institution_in        => rec_in.create_institution,
            update_user_in               => rec_in.update_user,
            update_time_in               => rec_in.update_time,
            update_institution_in        => rec_in.update_institution,
            id_habit_characterization_in => rec_in.id_habit_characterization
            
           ,
            handle_error_in => handle_error_in,
            rows_out        => rows_out);
    END upd;

    PROCEDURE upd_ins
    (
        id_pat_habit_in              IN pat_habit.id_pat_habit%TYPE,
        id_patient_in                IN pat_habit.id_patient%TYPE DEFAULT NULL,
        id_habit_in                  IN pat_habit.id_habit%TYPE DEFAULT NULL,
        flg_status_in                IN pat_habit.flg_status%TYPE DEFAULT NULL,
        id_prof_writes_in            IN pat_habit.id_prof_writes%TYPE DEFAULT NULL,
        notes_in                     IN pat_habit.notes%TYPE DEFAULT NULL,
        id_prof_cancel_in            IN pat_habit.id_prof_cancel%TYPE DEFAULT NULL,
        note_cancel_in               IN pat_habit.note_cancel%TYPE DEFAULT NULL,
        year_begin_in                IN pat_habit.year_begin%TYPE DEFAULT NULL,
        month_begin_in               IN pat_habit.month_begin%TYPE DEFAULT NULL,
        day_begin_in                 IN pat_habit.day_begin%TYPE DEFAULT NULL,
        year_end_in                  IN pat_habit.year_end%TYPE DEFAULT NULL,
        month_end_in                 IN pat_habit.month_end%TYPE DEFAULT NULL,
        day_end_in                   IN pat_habit.day_end%TYPE DEFAULT NULL,
        id_institution_in            IN pat_habit.id_institution%TYPE DEFAULT NULL,
        id_episode_in                IN pat_habit.id_episode%TYPE DEFAULT NULL,
        dt_pat_habit_tstz_in         IN pat_habit.dt_pat_habit_tstz%TYPE DEFAULT NULL,
        dt_cancel_tstz_in            IN pat_habit.dt_cancel_tstz%TYPE DEFAULT NULL,
        id_cancel_reason_in          IN pat_habit.id_cancel_reason%TYPE DEFAULT NULL,
        cancel_notes_in              IN pat_habit.cancel_notes%TYPE DEFAULT NULL,
        create_user_in               IN pat_habit.create_user%TYPE DEFAULT NULL,
        create_time_in               IN pat_habit.create_time%TYPE DEFAULT NULL,
        create_institution_in        IN pat_habit.create_institution%TYPE DEFAULT NULL,
        update_user_in               IN pat_habit.update_user%TYPE DEFAULT NULL,
        update_time_in               IN pat_habit.update_time%TYPE DEFAULT NULL,
        update_institution_in        IN pat_habit.update_institution%TYPE DEFAULT NULL,
        id_habit_characterization_in IN pat_habit.id_habit_characterization%TYPE DEFAULT NULL,
        handle_error_in              IN BOOLEAN := TRUE,
        rows_out                     OUT table_varchar
    ) IS
    BEGIN
        upd(id_pat_habit_in              => id_pat_habit_in,
            id_patient_in                => id_patient_in,
            id_habit_in                  => id_habit_in,
            flg_status_in                => flg_status_in,
            id_prof_writes_in            => id_prof_writes_in,
            notes_in                     => notes_in,
            id_prof_cancel_in            => id_prof_cancel_in,
            note_cancel_in               => note_cancel_in,
            year_begin_in                => year_begin_in,
            month_begin_in               => month_begin_in,
            day_begin_in                 => day_begin_in,
            year_end_in                  => year_end_in,
            month_end_in                 => month_end_in,
            day_end_in                   => day_end_in,
            id_institution_in            => id_institution_in,
            id_episode_in                => id_episode_in,
            dt_pat_habit_tstz_in         => dt_pat_habit_tstz_in,
            dt_cancel_tstz_in            => dt_cancel_tstz_in,
            id_cancel_reason_in          => id_cancel_reason_in,
            cancel_notes_in              => cancel_notes_in,
            create_user_in               => create_user_in,
            create_time_in               => create_time_in,
            create_institution_in        => create_institution_in,
            update_user_in               => update_user_in,
            update_time_in               => update_time_in,
            update_institution_in        => update_institution_in,
            id_habit_characterization_in => id_habit_characterization_in,
            handle_error_in              => handle_error_in,
            rows_out                     => rows_out);
        IF SQL%ROWCOUNT = 0
        THEN
            ins(id_pat_habit_in              => id_pat_habit_in,
                id_patient_in                => id_patient_in,
                id_habit_in                  => id_habit_in,
                flg_status_in                => flg_status_in,
                id_prof_writes_in            => id_prof_writes_in,
                notes_in                     => notes_in,
                id_prof_cancel_in            => id_prof_cancel_in,
                note_cancel_in               => note_cancel_in,
                year_begin_in                => year_begin_in,
                month_begin_in               => month_begin_in,
                day_begin_in                 => day_begin_in,
                year_end_in                  => year_end_in,
                month_end_in                 => month_end_in,
                day_end_in                   => day_end_in,
                id_institution_in            => id_institution_in,
                id_episode_in                => id_episode_in,
                dt_pat_habit_tstz_in         => dt_pat_habit_tstz_in,
                dt_cancel_tstz_in            => dt_cancel_tstz_in,
                id_cancel_reason_in          => id_cancel_reason_in,
                cancel_notes_in              => cancel_notes_in,
                create_user_in               => create_user_in,
                create_time_in               => create_time_in,
                create_institution_in        => create_institution_in,
                update_user_in               => update_user_in,
                update_time_in               => update_time_in,
                update_institution_in        => update_institution_in,
                id_habit_characterization_in => id_habit_characterization_in,
                handle_error_in              => handle_error_in,
                rows_out                     => rows_out);
        END IF;
    END upd_ins;

    PROCEDURE upd_ins
    (
        id_pat_habit_in              IN pat_habit.id_pat_habit%TYPE,
        id_patient_in                IN pat_habit.id_patient%TYPE DEFAULT NULL,
        id_habit_in                  IN pat_habit.id_habit%TYPE DEFAULT NULL,
        flg_status_in                IN pat_habit.flg_status%TYPE DEFAULT NULL,
        id_prof_writes_in            IN pat_habit.id_prof_writes%TYPE DEFAULT NULL,
        notes_in                     IN pat_habit.notes%TYPE DEFAULT NULL,
        id_prof_cancel_in            IN pat_habit.id_prof_cancel%TYPE DEFAULT NULL,
        note_cancel_in               IN pat_habit.note_cancel%TYPE DEFAULT NULL,
        year_begin_in                IN pat_habit.year_begin%TYPE DEFAULT NULL,
        month_begin_in               IN pat_habit.month_begin%TYPE DEFAULT NULL,
        day_begin_in                 IN pat_habit.day_begin%TYPE DEFAULT NULL,
        year_end_in                  IN pat_habit.year_end%TYPE DEFAULT NULL,
        month_end_in                 IN pat_habit.month_end%TYPE DEFAULT NULL,
        day_end_in                   IN pat_habit.day_end%TYPE DEFAULT NULL,
        id_institution_in            IN pat_habit.id_institution%TYPE DEFAULT NULL,
        id_episode_in                IN pat_habit.id_episode%TYPE DEFAULT NULL,
        dt_pat_habit_tstz_in         IN pat_habit.dt_pat_habit_tstz%TYPE DEFAULT NULL,
        dt_cancel_tstz_in            IN pat_habit.dt_cancel_tstz%TYPE DEFAULT NULL,
        id_cancel_reason_in          IN pat_habit.id_cancel_reason%TYPE DEFAULT NULL,
        cancel_notes_in              IN pat_habit.cancel_notes%TYPE DEFAULT NULL,
        create_user_in               IN pat_habit.create_user%TYPE DEFAULT NULL,
        create_time_in               IN pat_habit.create_time%TYPE DEFAULT NULL,
        create_institution_in        IN pat_habit.create_institution%TYPE DEFAULT NULL,
        update_user_in               IN pat_habit.update_user%TYPE DEFAULT NULL,
        update_time_in               IN pat_habit.update_time%TYPE DEFAULT NULL,
        update_institution_in        IN pat_habit.update_institution%TYPE DEFAULT NULL,
        id_habit_characterization_in IN pat_habit.id_habit_characterization%TYPE DEFAULT NULL,
        handle_error_in              IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        upd_ins(id_pat_habit_in,
                id_patient_in,
                id_habit_in,
                flg_status_in,
                id_prof_writes_in,
                notes_in,
                id_prof_cancel_in,
                note_cancel_in,
                year_begin_in,
                month_begin_in,
                day_begin_in,
                year_end_in,
                month_end_in,
                day_end_in,
                id_institution_in,
                id_episode_in,
                dt_pat_habit_tstz_in,
                dt_cancel_tstz_in,
                id_cancel_reason_in,
                cancel_notes_in,
                create_user_in,
                create_time_in,
                create_institution_in,
                update_user_in,
                update_time_in,
                update_institution_in,
                id_habit_characterization_in,
                handle_error_in,
                rows_out);
    END upd_ins;

    PROCEDURE upd
    (
        col_in            IN pat_habit_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          IN OUT table_varchar
    ) IS
        l_id_pat_habit              id_pat_habit_cc;
        l_id_patient                id_patient_cc;
        l_id_habit                  id_habit_cc;
        l_flg_status                flg_status_cc;
        l_id_prof_writes            id_prof_writes_cc;
        l_notes                     notes_cc;
        l_id_prof_cancel            id_prof_cancel_cc;
        l_note_cancel               note_cancel_cc;
        l_year_begin                year_begin_cc;
        l_month_begin               month_begin_cc;
        l_day_begin                 day_begin_cc;
        l_year_end                  year_end_cc;
        l_month_end                 month_end_cc;
        l_day_end                   day_end_cc;
        l_id_institution            id_institution_cc;
        l_id_episode                id_episode_cc;
        l_dt_pat_habit_tstz         dt_pat_habit_tstz_cc;
        l_dt_cancel_tstz            dt_cancel_tstz_cc;
        l_id_cancel_reason          id_cancel_reason_cc;
        l_cancel_notes              cancel_notes_cc;
        l_create_user               create_user_cc;
        l_create_time               create_time_cc;
        l_create_institution        create_institution_cc;
        l_update_user               update_user_cc;
        l_update_time               update_time_cc;
        l_update_institution        update_institution_cc;
        l_id_habit_characterization id_habit_characterization_cc;
    BEGIN
        FOR i IN col_in.first .. col_in.last
        LOOP
            l_id_pat_habit(i) := col_in(i).id_pat_habit;
            l_id_patient(i) := col_in(i).id_patient;
            l_id_habit(i) := col_in(i).id_habit;
            l_flg_status(i) := col_in(i).flg_status;
            l_id_prof_writes(i) := col_in(i).id_prof_writes;
            l_notes(i) := col_in(i).notes;
            l_id_prof_cancel(i) := col_in(i).id_prof_cancel;
            l_note_cancel(i) := col_in(i).note_cancel;
            l_year_begin(i) := col_in(i).year_begin;
            l_month_begin(i) := col_in(i).month_begin;
            l_day_begin(i) := col_in(i).day_begin;
            l_year_end(i) := col_in(i).year_end;
            l_month_end(i) := col_in(i).month_end;
            l_day_end(i) := col_in(i).day_end;
            l_id_institution(i) := col_in(i).id_institution;
            l_id_episode(i) := col_in(i).id_episode;
            l_dt_pat_habit_tstz(i) := col_in(i).dt_pat_habit_tstz;
            l_dt_cancel_tstz(i) := col_in(i).dt_cancel_tstz;
            l_id_cancel_reason(i) := col_in(i).id_cancel_reason;
            l_cancel_notes(i) := col_in(i).cancel_notes;
            l_create_user(i) := col_in(i).create_user;
            l_create_time(i) := col_in(i).create_time;
            l_create_institution(i) := col_in(i).create_institution;
            l_update_user(i) := col_in(i).update_user;
            l_update_time(i) := col_in(i).update_time;
            l_update_institution(i) := col_in(i).update_institution;
            l_id_habit_characterization(i) := col_in(i).id_habit_characterization;
        END LOOP;
        IF nvl(ignore_if_null_in, FALSE)
        THEN
            -- Set any columns to their current values
            -- if incoming value is NULL.
            -- Put WHEN clause on column-level triggers!
            FORALL i IN col_in.first .. col_in.last
                UPDATE pat_habit
                   SET id_patient                = nvl(l_id_patient(i), id_patient),
                       id_habit                  = nvl(l_id_habit(i), id_habit),
                       flg_status                = nvl(l_flg_status(i), flg_status),
                       id_prof_writes            = nvl(l_id_prof_writes(i), id_prof_writes),
                       notes                     = nvl(l_notes(i), notes),
                       id_prof_cancel            = nvl(l_id_prof_cancel(i), id_prof_cancel),
                       note_cancel               = nvl(l_note_cancel(i), note_cancel),
                       year_begin                = nvl(l_year_begin(i), year_begin),
                       month_begin               = nvl(l_month_begin(i), month_begin),
                       day_begin                 = nvl(l_day_begin(i), day_begin),
                       year_end                  = nvl(l_year_end(i), year_end),
                       month_end                 = nvl(l_month_end(i), month_end),
                       day_end                   = nvl(l_day_end(i), day_end),
                       id_institution            = nvl(l_id_institution(i), id_institution),
                       id_episode                = nvl(l_id_episode(i), id_episode),
                       dt_pat_habit_tstz         = nvl(l_dt_pat_habit_tstz(i), dt_pat_habit_tstz),
                       dt_cancel_tstz            = nvl(l_dt_cancel_tstz(i), dt_cancel_tstz),
                       id_cancel_reason          = nvl(l_id_cancel_reason(i), id_cancel_reason),
                       cancel_notes              = nvl(l_cancel_notes(i), cancel_notes),
                       create_user               = nvl(l_create_user(i), create_user),
                       create_time               = nvl(l_create_time(i), create_time),
                       create_institution        = nvl(l_create_institution(i), create_institution),
                       update_user               = nvl(l_update_user(i), update_user),
                       update_time               = nvl(l_update_time(i), update_time),
                       update_institution        = nvl(l_update_institution(i), update_institution),
                       id_habit_characterization = nvl(l_id_habit_characterization(i), id_habit_characterization)
                 WHERE id_pat_habit = l_id_pat_habit(i)
                RETURNING ROWID BULK COLLECT INTO rows_out;
        ELSE
            FORALL i IN col_in.first .. col_in.last
                UPDATE pat_habit
                   SET id_patient                = l_id_patient(i),
                       id_habit                  = l_id_habit(i),
                       flg_status                = l_flg_status(i),
                       id_prof_writes            = l_id_prof_writes(i),
                       notes                     = l_notes(i),
                       id_prof_cancel            = l_id_prof_cancel(i),
                       note_cancel               = l_note_cancel(i),
                       year_begin                = l_year_begin(i),
                       month_begin               = l_month_begin(i),
                       day_begin                 = l_day_begin(i),
                       year_end                  = l_year_end(i),
                       month_end                 = l_month_end(i),
                       day_end                   = l_day_end(i),
                       id_institution            = l_id_institution(i),
                       id_episode                = l_id_episode(i),
                       dt_pat_habit_tstz         = l_dt_pat_habit_tstz(i),
                       dt_cancel_tstz            = l_dt_cancel_tstz(i),
                       id_cancel_reason          = l_id_cancel_reason(i),
                       cancel_notes              = l_cancel_notes(i),
                       create_user               = l_create_user(i),
                       create_time               = l_create_time(i),
                       create_institution        = l_create_institution(i),
                       update_user               = l_update_user(i),
                       update_time               = l_update_time(i),
                       update_institution        = l_update_institution(i),
                       id_habit_characterization = l_id_habit_characterization(i)
                 WHERE id_pat_habit = l_id_pat_habit(i)
                RETURNING ROWID BULK COLLECT INTO rows_out;
        END IF;
    END upd;

    PROCEDURE upd
    (
        col_in            IN pat_habit_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        upd(col_in, ignore_if_null_in, handle_error_in, rows_out);
    END upd;

    FUNCTION dynupdstr
    (
        colname_in IN all_tab_columns.column_name%TYPE,
        where_in   IN VARCHAR2 := NULL
    )
    
     RETURN VARCHAR2 IS
    BEGIN
        RETURN 'BEGIN UPDATE PAT_HABIT
             SET ' || colname_in || ' = :value
           WHERE ' || nvl(where_in, '1=1') || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;';
    END dynupdstr;

    FUNCTION dynupdstr_no_rows_out
    (
        colname_in IN all_tab_columns.column_name%TYPE,
        where_in   IN VARCHAR2 := NULL
    )
    
     RETURN VARCHAR2 IS
    BEGIN
        RETURN 'UPDATE PAT_HABIT
             SET ' || colname_in || ' = :value
           WHERE ' || nvl(where_in, '1=1');
    END dynupdstr_no_rows_out;

    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    ) IS
    BEGIN
        EXECUTE IMMEDIATE 'BEGIN UPDATE PAT_HABIT set ' || colname_in || '=' || colname_in || ' + ' ||
                          nvl(increment_value_in, 1) || ' WHERE ' || nvl(where_in, '1=1') ||
                          ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
            USING OUT rows_out;
    EXCEPTION
        WHEN dup_val_on_index THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner           all_constraints.owner%TYPE;
                    l_name            all_constraints.constraint_name%TYPE;
                    l_err_instance_id PLS_INTEGER;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'DUPLICATE-VALUE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'PAT_HABIT');
                END;
            END IF;
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'PAT_HABIT');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'PAT_HABIT');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
        WHEN e_null_column_value THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    v_errm    VARCHAR2(2000) := dbms_utility.format_error_stack;
                    dot1loc   INTEGER;
                    dot2loc   INTEGER;
                    parenloc  INTEGER;
                    c_owner   all_constraints.owner%TYPE;
                    c_tabname all_tables.table_name%TYPE;
                    c_colname all_tab_columns.column_name%TYPE;
                BEGIN
                    dot1loc   := instr(v_errm, '.', 1, 1);
                    dot2loc   := instr(v_errm, '.', 1, 2);
                    parenloc  := instr(v_errm, '(');
                    c_owner   := substr(v_errm, parenloc + 1, dot1loc - parenloc - 1);
                    c_tabname := substr(v_errm, dot1loc + 1, dot2loc - dot1loc - 1);
                    c_colname := substr(v_errm, dot2loc + 1, instr(v_errm, ')') - dot2loc - 1);
                
                    pk_alert_exceptions.raise_error(error_name_in => 'COLUMN-CANNOT-BE-NULL',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => c_owner,
                                                    name2_in      => 'TABLE_NAME',
                                                    value2_in     => c_tabname,
                                                    name3_in      => 'COLUMN_NAME',
                                                    value3_in     => c_colname);
                END;
            END IF;
    END increment_onecol;

    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        EXECUTE IMMEDIATE 'UPDATE PAT_HABIT set ' || colname_in || '=' || colname_in || ' + ' ||
                          nvl(increment_value_in, 1) || ' WHERE ' || nvl(where_in, '1=1');
    EXCEPTION
        WHEN dup_val_on_index THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner           all_constraints.owner%TYPE;
                    l_name            all_constraints.constraint_name%TYPE;
                    l_err_instance_id PLS_INTEGER;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'DUPLICATE-VALUE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'PAT_HABIT');
                END;
            END IF;
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'PAT_HABIT');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'PAT_HABIT');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
        WHEN e_null_column_value THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    v_errm    VARCHAR2(2000) := dbms_utility.format_error_stack;
                    dot1loc   INTEGER;
                    dot2loc   INTEGER;
                    parenloc  INTEGER;
                    c_owner   all_constraints.owner%TYPE;
                    c_tabname all_tables.table_name%TYPE;
                    c_colname all_tab_columns.column_name%TYPE;
                BEGIN
                    dot1loc   := instr(v_errm, '.', 1, 1);
                    dot2loc   := instr(v_errm, '.', 1, 2);
                    parenloc  := instr(v_errm, '(');
                    c_owner   := substr(v_errm, parenloc + 1, dot1loc - parenloc - 1);
                    c_tabname := substr(v_errm, dot1loc + 1, dot2loc - dot1loc - 1);
                    c_colname := substr(v_errm, dot2loc + 1, instr(v_errm, ')') - dot2loc - 1);
                
                    pk_alert_exceptions.raise_error(error_name_in => 'COLUMN-CANNOT-BE-NULL',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => c_owner,
                                                    name2_in      => 'TABLE_NAME',
                                                    value2_in     => c_tabname,
                                                    name3_in      => 'COLUMN_NAME',
                                                    value3_in     => c_colname);
                END;
            END IF;
    END increment_onecol;

    -- Delete functionality

    PROCEDURE del
    (
        id_pat_habit_in IN pat_habit.id_pat_habit%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) IS
    BEGIN
        DELETE FROM pat_habit
         WHERE id_pat_habit = id_pat_habit_in
        RETURNING ROWID BULK COLLECT INTO rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'PAT_HABIT');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'PAT_HABIT');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del;

    PROCEDURE del
    (
        id_pat_habit_in IN pat_habit.id_pat_habit%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
    
        del(id_pat_habit_in => id_pat_habit_in, handle_error_in => handle_error_in, rows_out => rows_out);
    
    END del;

    -- Delete all rows for primary key column ID_PAT_HABIT
    PROCEDURE del_id_pat_habit
    (
        id_pat_habit_in IN pat_habit.id_pat_habit%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) IS
    BEGIN
        DELETE FROM pat_habit
         WHERE id_pat_habit = id_pat_habit_in
        RETURNING ROWID BULK COLLECT INTO rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'PAT_HABIT');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'PAT_HABIT');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_id_pat_habit;

    -- Delete all rows for primary key column ID_PAT_HABIT
    PROCEDURE del_id_pat_habit
    (
        id_pat_habit_in IN pat_habit.id_pat_habit%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        del_id_pat_habit(id_pat_habit_in => id_pat_habit_in, handle_error_in => handle_error_in, rows_out => rows_out);
    END del_id_pat_habit;

    PROCEDURE del_ph_cr_fk
    (
        id_cancel_reason_in IN pat_habit.id_cancel_reason%TYPE,
        handle_error_in     IN BOOLEAN := TRUE,
        rows_out            OUT table_varchar
    ) IS
    BEGIN
        DELETE FROM pat_habit
         WHERE id_cancel_reason = del_ph_cr_fk.id_cancel_reason_in
        RETURNING ROWID BULK COLLECT INTO rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'PAT_HABIT');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'PAT_HABIT');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_ph_cr_fk;

    PROCEDURE del_ph_cr_fk
    (
        id_cancel_reason_in IN pat_habit.id_cancel_reason%TYPE,
        handle_error_in     IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        del_ph_cr_fk(id_cancel_reason_in => id_cancel_reason_in,
                     handle_error_in     => handle_error_in,
                     rows_out            => rows_out);
    END del_ph_cr_fk;

    PROCEDURE del_ptnot_epis_fk
    (
        id_episode_in   IN pat_habit.id_episode%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) IS
    BEGIN
        DELETE FROM pat_habit
         WHERE id_episode = del_ptnot_epis_fk.id_episode_in
        RETURNING ROWID BULK COLLECT INTO rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'PAT_HABIT');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'PAT_HABIT');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_ptnot_epis_fk;

    PROCEDURE del_ptnot_epis_fk
    (
        id_episode_in   IN pat_habit.id_episode%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        del_ptnot_epis_fk(id_episode_in => id_episode_in, handle_error_in => handle_error_in, rows_out => rows_out);
    END del_ptnot_epis_fk;

    PROCEDURE del_ptnot_hat_fk
    (
        id_habit_in     IN pat_habit.id_habit%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) IS
    BEGIN
        DELETE FROM pat_habit
         WHERE id_habit = del_ptnot_hat_fk.id_habit_in
        RETURNING ROWID BULK COLLECT INTO rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'PAT_HABIT');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'PAT_HABIT');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_ptnot_hat_fk;

    PROCEDURE del_ptnot_hat_fk
    (
        id_habit_in     IN pat_habit.id_habit%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        del_ptnot_hat_fk(id_habit_in => id_habit_in, handle_error_in => handle_error_in, rows_out => rows_out);
    END del_ptnot_hat_fk;

    PROCEDURE del_ptnot_inst_fk
    (
        id_institution_in IN pat_habit.id_institution%TYPE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          OUT table_varchar
    ) IS
    BEGIN
        DELETE FROM pat_habit
         WHERE id_institution = del_ptnot_inst_fk.id_institution_in
        RETURNING ROWID BULK COLLECT INTO rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'PAT_HABIT');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'PAT_HABIT');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_ptnot_inst_fk;

    PROCEDURE del_ptnot_inst_fk
    (
        id_institution_in IN pat_habit.id_institution%TYPE,
        handle_error_in   IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        del_ptnot_inst_fk(id_institution_in => id_institution_in,
                          handle_error_in   => handle_error_in,
                          rows_out          => rows_out);
    END del_ptnot_inst_fk;

    PROCEDURE del_ptnot_pat_fk
    (
        id_patient_in   IN pat_habit.id_patient%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) IS
    BEGIN
        DELETE FROM pat_habit
         WHERE id_patient = del_ptnot_pat_fk.id_patient_in
        RETURNING ROWID BULK COLLECT INTO rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'PAT_HABIT');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'PAT_HABIT');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_ptnot_pat_fk;

    PROCEDURE del_ptnot_pat_fk
    (
        id_patient_in   IN pat_habit.id_patient%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        del_ptnot_pat_fk(id_patient_in => id_patient_in, handle_error_in => handle_error_in, rows_out => rows_out);
    END del_ptnot_pat_fk;

    PROCEDURE del_ptnot_prof_cancel_fk
    (
        id_prof_cancel_in IN pat_habit.id_prof_cancel%TYPE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          OUT table_varchar
    ) IS
    BEGIN
        DELETE FROM pat_habit
         WHERE id_prof_cancel = del_ptnot_prof_cancel_fk.id_prof_cancel_in
        RETURNING ROWID BULK COLLECT INTO rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'PAT_HABIT');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'PAT_HABIT');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_ptnot_prof_cancel_fk;

    PROCEDURE del_ptnot_prof_cancel_fk
    (
        id_prof_cancel_in IN pat_habit.id_prof_cancel%TYPE,
        handle_error_in   IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        del_ptnot_prof_cancel_fk(id_prof_cancel_in => id_prof_cancel_in,
                                 handle_error_in   => handle_error_in,
                                 rows_out          => rows_out);
    END del_ptnot_prof_cancel_fk;

    PROCEDURE del_ptnot_prof_writes_fk
    (
        id_prof_writes_in IN pat_habit.id_prof_writes%TYPE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          OUT table_varchar
    ) IS
    BEGIN
        DELETE FROM pat_habit
         WHERE id_prof_writes = del_ptnot_prof_writes_fk.id_prof_writes_in
        RETURNING ROWID BULK COLLECT INTO rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'PAT_HABIT');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'PAT_HABIT');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_ptnot_prof_writes_fk;

    PROCEDURE del_ptnot_prof_writes_fk
    (
        id_prof_writes_in IN pat_habit.id_prof_writes%TYPE,
        handle_error_in   IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        del_ptnot_prof_writes_fk(id_prof_writes_in => id_prof_writes_in,
                                 handle_error_in   => handle_error_in,
                                 rows_out          => rows_out);
    END del_ptnot_prof_writes_fk;

    -- Deletions using dynamic SQL
    FUNCTION dyndelstr(where_in IN VARCHAR2) RETURN VARCHAR2 IS
    BEGIN
        IF where_in IS NULL
        THEN
            RETURN 'DELETE FROM PAT_HABIT';
        ELSE
            RETURN 'DELETE FROM PAT_HABIT WHERE ' || where_in;
        END IF;
    END dyndelstr;

    FUNCTION dyncoldelstr(colname_in IN all_tab_columns.column_name%TYPE) RETURN VARCHAR2 IS
    BEGIN
        RETURN dyndelstr(colname_in || ' = :value');
    END;

    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
    BEGIN
        EXECUTE IMMEDIATE dyndelstr(where_clause_in);
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'PAT_HABIT');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'PAT_HABIT');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_by;

    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) IS
    BEGIN
        EXECUTE IMMEDIATE 'BEGIN ' || dyndelstr(where_clause_in) ||
                          ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
            USING OUT rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'PAT_HABIT');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'PAT_HABIT');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_by;

    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
    BEGIN
        EXECUTE IMMEDIATE dyncoldelstr(colname_in)
            USING colvalue_in;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'PAT_HABIT');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'PAT_HABIT');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_by_col;

    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) IS
    BEGIN
        EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr(colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
            USING IN colvalue_in, OUT rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'PAT_HABIT');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'PAT_HABIT');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_by_col;

    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
    BEGIN
        EXECUTE IMMEDIATE dyncoldelstr(colname_in)
            USING colvalue_in;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'PAT_HABIT');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'PAT_HABIT');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_by_col;

    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) IS
    BEGIN
        EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr(colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
            USING IN colvalue_in, OUT rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'PAT_HABIT');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'PAT_HABIT');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_by_col;

    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
    BEGIN
        EXECUTE IMMEDIATE dyncoldelstr(colname_in)
            USING colvalue_in;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'PAT_HABIT');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'PAT_HABIT');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_by_col;

    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) IS
    BEGIN
        EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr(colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
            USING IN colvalue_in, OUT rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'PAT_HABIT');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'PAT_HABIT');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_by_col;

    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
    BEGIN
        EXECUTE IMMEDIATE dyncoldelstr(colname_in)
            USING colvalue_in;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'PAT_HABIT');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'PAT_HABIT');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_by_col;

    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) IS
    BEGIN
        EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr(colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
            USING IN colvalue_in, OUT rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'PAT_HABIT');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'PAT_HABIT');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_by_col;

    -- Initialize a record with default values for columns in the table.
    PROCEDURE initrec(pat_habit_inout IN OUT pat_habit%ROWTYPE) IS
    BEGIN
        pat_habit_inout.id_pat_habit              := NULL;
        pat_habit_inout.id_patient                := NULL;
        pat_habit_inout.id_habit                  := NULL;
        pat_habit_inout.flg_status                := NULL;
        pat_habit_inout.id_prof_writes            := NULL;
        pat_habit_inout.notes                     := NULL;
        pat_habit_inout.id_prof_cancel            := NULL;
        pat_habit_inout.note_cancel               := NULL;
        pat_habit_inout.year_begin                := NULL;
        pat_habit_inout.month_begin               := NULL;
        pat_habit_inout.day_begin                 := NULL;
        pat_habit_inout.year_end                  := NULL;
        pat_habit_inout.month_end                 := NULL;
        pat_habit_inout.day_end                   := NULL;
        pat_habit_inout.id_institution            := NULL;
        pat_habit_inout.id_episode                := NULL;
        pat_habit_inout.dt_pat_habit_tstz         := NULL;
        pat_habit_inout.dt_cancel_tstz            := NULL;
        pat_habit_inout.id_cancel_reason          := NULL;
        pat_habit_inout.cancel_notes              := NULL;
        pat_habit_inout.create_user               := NULL;
        pat_habit_inout.create_time               := NULL;
        pat_habit_inout.create_institution        := NULL;
        pat_habit_inout.update_user               := NULL;
        pat_habit_inout.update_time               := NULL;
        pat_habit_inout.update_institution        := NULL;
        pat_habit_inout.id_habit_characterization := NULL;
    END initrec;

    FUNCTION initrec RETURN pat_habit%ROWTYPE IS
        l_pat_habit pat_habit%ROWTYPE;
    BEGIN
        RETURN l_pat_habit;
    END initrec;

    FUNCTION get_data_rowid(rows_in IN table_varchar) RETURN pat_habit_tc IS
        data pat_habit_tc;
    BEGIN
        SELECT * BULK COLLECT
          INTO data
          FROM pat_habit
         WHERE ROWID IN (SELECT /*+opt_estimate(table,t,scale_rows=0.0000001))*/
                          *
                           FROM TABLE(rows_in) t);
        RETURN data;
    EXCEPTION
        WHEN OTHERS THEN
            pk_alert_exceptions.raise_error(error_name_in => 'get_data_rowid');
    END get_data_rowid;

    FUNCTION get_data_rowid_pat(rows_in IN table_varchar) RETURN pat_habit_tc IS
        PRAGMA AUTONOMOUS_TRANSACTION;
        data pat_habit_tc;
    BEGIN
        data := get_data_rowid(rows_in);
        COMMIT;
        RETURN data;
    EXCEPTION
        WHEN OTHERS THEN
            pk_alert_exceptions.raise_error(error_name_in => 'get_data_rowid');
            ROLLBACK;
    END get_data_rowid_pat;

BEGIN
    NULL;
END ts_pat_habit;
/
