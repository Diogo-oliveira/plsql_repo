/*-- Last Change Revision: $Rev: 1877368 $*/
/*-- Last Change by: $Author: adriano.ferreira $*/
/*-- Date of last change: $Date: 2018-11-12 15:39:19 +0000 (seg, 12 nov 2018) $*/
CREATE OR REPLACE PACKAGE BODY TS_PAT_NECESSITY_HIST
/*
| Generated by or retrieved from QCGU - DO NOT MODIFY!
| QCGU - "Get it right, do it fast" - www.ToadWorld.com
| QCGU Universal ID: {7A44F965-7C77-42EE-9A04-269EF006213B}
| Created On: Setembro 6, 2013 16:50:39
| Created By: ALERT_ADTCOD
*/
IS


   e_null_column_value EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_null_column_value, -1400);
   --
   e_existing_fky_reference EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_existing_fky_reference, -2266);
   --
   e_check_constraint_failure EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_check_constraint_failure, -2290);
   --
   e_no_parent_key EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_no_parent_key, -2291);
   --
   e_child_record_found EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_child_record_found, -2292);
   --
   e_forall_error EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_forall_error, -24381);
   --
   -- Defined for backward compatibilty.
   e_integ_constraint_failure EXCEPTION;
   PRAGMA EXCEPTION_INIT (e_integ_constraint_failure, -2291);

    -- Private utilities
   PROCEDURE get_constraint_info (
      owner_out OUT ALL_CONSTRAINTS.OWNER%TYPE
     ,name_out OUT ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE)
   IS
      l_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
      dotloc PLS_INTEGER;
      leftloc PLS_INTEGER;
   BEGIN
      dotloc  := INSTR (l_errm,'.');
      leftloc := INSTR (l_errm,'(');
      owner_out := SUBSTR (l_errm, leftloc+1, dotloc-leftloc-1);
      name_out  := SUBSTR (l_errm, dotloc+1, INSTR (l_errm,')')-dotloc-1);
   END get_constraint_info;
   -- Public programs

   PROCEDURE ins (
      id_pat_necessity_hist_in IN PAT_NECESSITY_HIST.ID_PAT_NECESSITY_HIST%TYPE
      ,
      dt_register_in IN PAT_NECESSITY_HIST.DT_REGISTER%TYPE DEFAULT NULL,
      id_pat_necessity_in IN PAT_NECESSITY_HIST.ID_PAT_NECESSITY%TYPE DEFAULT NULL,
      id_patient_in IN PAT_NECESSITY_HIST.ID_PATIENT%TYPE DEFAULT NULL,
      id_necessity_in IN PAT_NECESSITY_HIST.ID_NECESSITY%TYPE DEFAULT NULL,
      flg_status_in IN PAT_NECESSITY_HIST.FLG_STATUS%TYPE DEFAULT NULL,
      id_episode_in IN PAT_NECESSITY_HIST.ID_EPISODE%TYPE DEFAULT NULL,
      id_institution_in IN PAT_NECESSITY_HIST.ID_INSTITUTION%TYPE DEFAULT NULL,
      record_status_in IN PAT_NECESSITY_HIST.RECORD_STATUS%TYPE DEFAULT NULL,
      import_code_in IN PAT_NECESSITY_HIST.IMPORT_CODE%TYPE DEFAULT NULL,
      create_user_in IN PAT_NECESSITY_HIST.CREATE_USER%TYPE DEFAULT NULL,
      update_user_in IN PAT_NECESSITY_HIST.UPDATE_USER%TYPE DEFAULT NULL,
      create_time_in IN PAT_NECESSITY_HIST.CREATE_TIME%TYPE DEFAULT NULL,
      update_time_in IN PAT_NECESSITY_HIST.UPDATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN PAT_NECESSITY_HIST.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_institution_in IN PAT_NECESSITY_HIST.UPDATE_INSTITUTION%TYPE DEFAULT NULL
     ,handle_error_in IN BOOLEAN := TRUE
      , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN

     INSERT INTO PAT_NECESSITY_HIST (
         ID_PAT_NECESSITY_HIST,
         DT_REGISTER,
         ID_PAT_NECESSITY,
         ID_PATIENT,
         ID_NECESSITY,
         FLG_STATUS,
         ID_EPISODE,
         ID_INSTITUTION,
         RECORD_STATUS,
         IMPORT_CODE,
         CREATE_USER,
         UPDATE_USER,
         CREATE_TIME,
         UPDATE_TIME,
         CREATE_INSTITUTION,
         UPDATE_INSTITUTION
         )
      VALUES (
         id_pat_necessity_hist_in,
         dt_register_in,
         id_pat_necessity_in,
         id_patient_in,
         id_necessity_in,
         flg_status_in,
         id_episode_in,
         id_institution_in,
         record_status_in,
         import_code_in,
         create_user_in,
         update_user_in,
         create_time_in,
         update_time_in,
         create_institution_in,
         update_institution_in
         ) RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF FALSE THEN NULL; -- Placeholder in case no unique indexes
           ELSE
              pk_alert_exceptions.raise_error (
                    error_name_in => 'DUPLICATE-VALUE'
                    ,name1_in => 'OWNER'
                    ,value1_in => l_owner
                    ,name2_in => 'CONSTRAINT_NAME'
                    ,value2_in => l_name
                    ,name3_in => 'TABLE_NAME'
                    ,value3_in => 'PAT_NECESSITY_HIST');
           END IF;
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PAT_NECESSITY_HIST');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PAT_NECESSITY_HIST');
           IF l_name = 'PATNH_EPIS_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_EPISODE'
               , value_in => id_episode_in);
           END IF;
           IF l_name = 'PATNH_INST_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_INSTITUTION'
               , value_in => id_institution_in);
           END IF;
           IF l_name = 'PATNH_NEC_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_NECESSITY'
               , value_in => id_necessity_in);
           END IF;
           IF l_name = 'PATNH_PAT_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_PATIENT'
               , value_in => id_patient_in);
           END IF;
           IF l_name = 'PATNH_PATN_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_PAT_NECESSITY'
               , value_in => id_pat_necessity_in);
           END IF;
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END ins;

   PROCEDURE ins (
      id_pat_necessity_hist_in IN PAT_NECESSITY_HIST.ID_PAT_NECESSITY_HIST%TYPE
      ,
      dt_register_in IN PAT_NECESSITY_HIST.DT_REGISTER%TYPE DEFAULT NULL,
      id_pat_necessity_in IN PAT_NECESSITY_HIST.ID_PAT_NECESSITY%TYPE DEFAULT NULL,
      id_patient_in IN PAT_NECESSITY_HIST.ID_PATIENT%TYPE DEFAULT NULL,
      id_necessity_in IN PAT_NECESSITY_HIST.ID_NECESSITY%TYPE DEFAULT NULL,
      flg_status_in IN PAT_NECESSITY_HIST.FLG_STATUS%TYPE DEFAULT NULL,
      id_episode_in IN PAT_NECESSITY_HIST.ID_EPISODE%TYPE DEFAULT NULL,
      id_institution_in IN PAT_NECESSITY_HIST.ID_INSTITUTION%TYPE DEFAULT NULL,
      record_status_in IN PAT_NECESSITY_HIST.RECORD_STATUS%TYPE DEFAULT NULL,
      import_code_in IN PAT_NECESSITY_HIST.IMPORT_CODE%TYPE DEFAULT NULL,
      create_user_in IN PAT_NECESSITY_HIST.CREATE_USER%TYPE DEFAULT NULL,
      update_user_in IN PAT_NECESSITY_HIST.UPDATE_USER%TYPE DEFAULT NULL,
      create_time_in IN PAT_NECESSITY_HIST.CREATE_TIME%TYPE DEFAULT NULL,
      update_time_in IN PAT_NECESSITY_HIST.UPDATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN PAT_NECESSITY_HIST.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_institution_in IN PAT_NECESSITY_HIST.UPDATE_INSTITUTION%TYPE DEFAULT NULL
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN

     ins (
      id_pat_necessity_hist_in => id_pat_necessity_hist_in
      ,
      dt_register_in => dt_register_in,
      id_pat_necessity_in => id_pat_necessity_in,
      id_patient_in => id_patient_in,
      id_necessity_in => id_necessity_in,
      flg_status_in => flg_status_in,
      id_episode_in => id_episode_in,
      id_institution_in => id_institution_in,
      record_status_in => record_status_in,
      import_code_in => import_code_in,
      create_user_in => create_user_in,
      update_user_in => update_user_in,
      create_time_in => create_time_in,
      update_time_in => update_time_in,
      create_institution_in => create_institution_in,
      update_institution_in => update_institution_in
     ,handle_error_in => handle_error_in
      ,rows_out => rows_out
      );
   END ins;


   PROCEDURE ins (
      rec_in IN PAT_NECESSITY_HIST%ROWTYPE
     ,gen_pky_in IN BOOLEAN DEFAULT FALSE
     ,sequence_in IN VARCHAR2 := NULL
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
      l_rec PAT_NECESSITY_HIST%ROWTYPE := rec_in;
   BEGIN
      IF gen_pky_in THEN
         l_rec.ID_PAT_NECESSITY_HIST := next_key (sequence_in);
      END IF;
      ins (
         id_pat_necessity_hist_in => l_rec.ID_PAT_NECESSITY_HIST
         ,
         dt_register_in => l_rec.DT_REGISTER,
         id_pat_necessity_in => l_rec.ID_PAT_NECESSITY,
         id_patient_in => l_rec.ID_PATIENT,
         id_necessity_in => l_rec.ID_NECESSITY,
         flg_status_in => l_rec.FLG_STATUS,
         id_episode_in => l_rec.ID_EPISODE,
         id_institution_in => l_rec.ID_INSTITUTION,
         record_status_in => l_rec.RECORD_STATUS,
         import_code_in => l_rec.IMPORT_CODE,
         create_user_in => l_rec.CREATE_USER,
         update_user_in => l_rec.UPDATE_USER,
         create_time_in => l_rec.CREATE_TIME,
         update_time_in => l_rec.UPDATE_TIME,
         create_institution_in => l_rec.CREATE_INSTITUTION,
         update_institution_in => l_rec.UPDATE_INSTITUTION
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
   END ins;

   PROCEDURE ins (
      rec_in IN PAT_NECESSITY_HIST%ROWTYPE
     ,gen_pky_in IN BOOLEAN DEFAULT FALSE
     ,sequence_in IN VARCHAR2 := NULL
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
      rows_out TABLE_VARCHAR;
   BEGIN

  ins (
      rec_in => rec_in
     ,gen_pky_in => gen_pky_in
     ,sequence_in => sequence_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
   );

   END ins;

   FUNCTION next_key (sequence_in IN VARCHAR2 := NULL) RETURN PAT_NECESSITY_HIST.ID_PAT_NECESSITY_HIST%TYPE

   IS
     retval PAT_NECESSITY_HIST.ID_PAT_NECESSITY_HIST%TYPE;

   BEGIN
      IF sequence_in IS NULL
      THEN
         SELECT seq_PAT_NECESSITY_HIST.NEXTVAL INTO retval FROM dual;
      ELSE
         EXECUTE IMMEDIATE
            'SELECT ' || sequence_in || '.NEXTVAL FROM dual'
            INTO retval;
      END IF;
      RETURN retval;
   EXCEPTION
      WHEN OTHERS THEN
        pk_alert_exceptions.raise_error (
           error_name_in => 'SEQUENCE-GENERATION-FAILURE'
           ,name1_in => 'SEQUENCE'
           ,value1_in => NVL (sequence_in, 'seq_PAT_NECESSITY_HIST')
           );
   END next_key;

   PROCEDURE ins (
      dt_register_in IN PAT_NECESSITY_HIST.DT_REGISTER%TYPE DEFAULT NULL,
      id_pat_necessity_in IN PAT_NECESSITY_HIST.ID_PAT_NECESSITY%TYPE DEFAULT NULL,
      id_patient_in IN PAT_NECESSITY_HIST.ID_PATIENT%TYPE DEFAULT NULL,
      id_necessity_in IN PAT_NECESSITY_HIST.ID_NECESSITY%TYPE DEFAULT NULL,
      flg_status_in IN PAT_NECESSITY_HIST.FLG_STATUS%TYPE DEFAULT NULL,
      id_episode_in IN PAT_NECESSITY_HIST.ID_EPISODE%TYPE DEFAULT NULL,
      id_institution_in IN PAT_NECESSITY_HIST.ID_INSTITUTION%TYPE DEFAULT NULL,
      record_status_in IN PAT_NECESSITY_HIST.RECORD_STATUS%TYPE DEFAULT NULL,
      import_code_in IN PAT_NECESSITY_HIST.IMPORT_CODE%TYPE DEFAULT NULL,
      create_user_in IN PAT_NECESSITY_HIST.CREATE_USER%TYPE DEFAULT NULL,
      update_user_in IN PAT_NECESSITY_HIST.UPDATE_USER%TYPE DEFAULT NULL,
      create_time_in IN PAT_NECESSITY_HIST.CREATE_TIME%TYPE DEFAULT NULL,
      update_time_in IN PAT_NECESSITY_HIST.UPDATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN PAT_NECESSITY_HIST.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_institution_in IN PAT_NECESSITY_HIST.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      id_pat_necessity_hist_out IN OUT PAT_NECESSITY_HIST.ID_PAT_NECESSITY_HIST%TYPE
      ,handle_error_in IN BOOLEAN := TRUE
      , rows_out OUT TABLE_VARCHAR
   )
   IS
        l_pky PAT_NECESSITY_HIST.ID_PAT_NECESSITY_HIST%TYPE := next_key;
   BEGIN
      ins (
         id_pat_necessity_hist_in => l_pky,
         dt_register_in => dt_register_in,
         id_pat_necessity_in => id_pat_necessity_in,
         id_patient_in => id_patient_in,
         id_necessity_in => id_necessity_in,
         flg_status_in => flg_status_in,
         id_episode_in => id_episode_in,
         id_institution_in => id_institution_in,
         record_status_in => record_status_in,
         import_code_in => import_code_in,
         create_user_in => create_user_in,
         update_user_in => update_user_in,
         create_time_in => create_time_in,
         update_time_in => update_time_in,
         create_institution_in => create_institution_in,
         update_institution_in => update_institution_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
      id_pat_necessity_hist_out := l_pky;
   END ins;

   PROCEDURE ins (
      dt_register_in IN PAT_NECESSITY_HIST.DT_REGISTER%TYPE DEFAULT NULL,
      id_pat_necessity_in IN PAT_NECESSITY_HIST.ID_PAT_NECESSITY%TYPE DEFAULT NULL,
      id_patient_in IN PAT_NECESSITY_HIST.ID_PATIENT%TYPE DEFAULT NULL,
      id_necessity_in IN PAT_NECESSITY_HIST.ID_NECESSITY%TYPE DEFAULT NULL,
      flg_status_in IN PAT_NECESSITY_HIST.FLG_STATUS%TYPE DEFAULT NULL,
      id_episode_in IN PAT_NECESSITY_HIST.ID_EPISODE%TYPE DEFAULT NULL,
      id_institution_in IN PAT_NECESSITY_HIST.ID_INSTITUTION%TYPE DEFAULT NULL,
      record_status_in IN PAT_NECESSITY_HIST.RECORD_STATUS%TYPE DEFAULT NULL,
      import_code_in IN PAT_NECESSITY_HIST.IMPORT_CODE%TYPE DEFAULT NULL,
      create_user_in IN PAT_NECESSITY_HIST.CREATE_USER%TYPE DEFAULT NULL,
      update_user_in IN PAT_NECESSITY_HIST.UPDATE_USER%TYPE DEFAULT NULL,
      create_time_in IN PAT_NECESSITY_HIST.CREATE_TIME%TYPE DEFAULT NULL,
      update_time_in IN PAT_NECESSITY_HIST.UPDATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN PAT_NECESSITY_HIST.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_institution_in IN PAT_NECESSITY_HIST.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      id_pat_necessity_hist_out IN OUT PAT_NECESSITY_HIST.ID_PAT_NECESSITY_HIST%TYPE
      ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
        rows_out TABLE_VARCHAR;
   BEGIN
      ins (
      dt_register_in => dt_register_in,
      id_pat_necessity_in => id_pat_necessity_in,
      id_patient_in => id_patient_in,
      id_necessity_in => id_necessity_in,
      flg_status_in => flg_status_in,
      id_episode_in => id_episode_in,
      id_institution_in => id_institution_in,
      record_status_in => record_status_in,
      import_code_in => import_code_in,
      create_user_in => create_user_in,
      update_user_in => update_user_in,
      create_time_in => create_time_in,
      update_time_in => update_time_in,
      create_institution_in => create_institution_in,
      update_institution_in => update_institution_in,
      id_pat_necessity_hist_out => id_pat_necessity_hist_out
      ,handle_error_in => handle_error_in
      , rows_out => rows_out
   );
   END ins;

   FUNCTION ins (
      dt_register_in IN PAT_NECESSITY_HIST.DT_REGISTER%TYPE DEFAULT NULL,
      id_pat_necessity_in IN PAT_NECESSITY_HIST.ID_PAT_NECESSITY%TYPE DEFAULT NULL,
      id_patient_in IN PAT_NECESSITY_HIST.ID_PATIENT%TYPE DEFAULT NULL,
      id_necessity_in IN PAT_NECESSITY_HIST.ID_NECESSITY%TYPE DEFAULT NULL,
      flg_status_in IN PAT_NECESSITY_HIST.FLG_STATUS%TYPE DEFAULT NULL,
      id_episode_in IN PAT_NECESSITY_HIST.ID_EPISODE%TYPE DEFAULT NULL,
      id_institution_in IN PAT_NECESSITY_HIST.ID_INSTITUTION%TYPE DEFAULT NULL,
      record_status_in IN PAT_NECESSITY_HIST.RECORD_STATUS%TYPE DEFAULT NULL,
      import_code_in IN PAT_NECESSITY_HIST.IMPORT_CODE%TYPE DEFAULT NULL,
      create_user_in IN PAT_NECESSITY_HIST.CREATE_USER%TYPE DEFAULT NULL,
      update_user_in IN PAT_NECESSITY_HIST.UPDATE_USER%TYPE DEFAULT NULL,
      create_time_in IN PAT_NECESSITY_HIST.CREATE_TIME%TYPE DEFAULT NULL,
      update_time_in IN PAT_NECESSITY_HIST.UPDATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN PAT_NECESSITY_HIST.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_institution_in IN PAT_NECESSITY_HIST.UPDATE_INSTITUTION%TYPE DEFAULT NULL
      ,handle_error_in IN BOOLEAN := TRUE
      , rows_out OUT TABLE_VARCHAR
   )
      RETURN
         PAT_NECESSITY_HIST.ID_PAT_NECESSITY_HIST%TYPE
   IS
        l_pky PAT_NECESSITY_HIST.ID_PAT_NECESSITY_HIST%TYPE := next_key;
   BEGIN
      ins (
         id_pat_necessity_hist_in => l_pky,
         dt_register_in => dt_register_in,
         id_pat_necessity_in => id_pat_necessity_in,
         id_patient_in => id_patient_in,
         id_necessity_in => id_necessity_in,
         flg_status_in => flg_status_in,
         id_episode_in => id_episode_in,
         id_institution_in => id_institution_in,
         record_status_in => record_status_in,
         import_code_in => import_code_in,
         create_user_in => create_user_in,
         update_user_in => update_user_in,
         create_time_in => create_time_in,
         update_time_in => update_time_in,
         create_institution_in => create_institution_in,
         update_institution_in => update_institution_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
      RETURN l_pky;
   END ins;

   FUNCTION ins (
      dt_register_in IN PAT_NECESSITY_HIST.DT_REGISTER%TYPE DEFAULT NULL,
      id_pat_necessity_in IN PAT_NECESSITY_HIST.ID_PAT_NECESSITY%TYPE DEFAULT NULL,
      id_patient_in IN PAT_NECESSITY_HIST.ID_PATIENT%TYPE DEFAULT NULL,
      id_necessity_in IN PAT_NECESSITY_HIST.ID_NECESSITY%TYPE DEFAULT NULL,
      flg_status_in IN PAT_NECESSITY_HIST.FLG_STATUS%TYPE DEFAULT NULL,
      id_episode_in IN PAT_NECESSITY_HIST.ID_EPISODE%TYPE DEFAULT NULL,
      id_institution_in IN PAT_NECESSITY_HIST.ID_INSTITUTION%TYPE DEFAULT NULL,
      record_status_in IN PAT_NECESSITY_HIST.RECORD_STATUS%TYPE DEFAULT NULL,
      import_code_in IN PAT_NECESSITY_HIST.IMPORT_CODE%TYPE DEFAULT NULL,
      create_user_in IN PAT_NECESSITY_HIST.CREATE_USER%TYPE DEFAULT NULL,
      update_user_in IN PAT_NECESSITY_HIST.UPDATE_USER%TYPE DEFAULT NULL,
      create_time_in IN PAT_NECESSITY_HIST.CREATE_TIME%TYPE DEFAULT NULL,
      update_time_in IN PAT_NECESSITY_HIST.UPDATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN PAT_NECESSITY_HIST.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_institution_in IN PAT_NECESSITY_HIST.UPDATE_INSTITUTION%TYPE DEFAULT NULL
      ,handle_error_in IN BOOLEAN := TRUE
   )
      RETURN
         PAT_NECESSITY_HIST.ID_PAT_NECESSITY_HIST%TYPE
   IS
        l_pky PAT_NECESSITY_HIST.ID_PAT_NECESSITY_HIST%TYPE := next_key;
    rows_out TABLE_VARCHAR;
   BEGIN
      ins (
         id_pat_necessity_hist_in => l_pky,
         dt_register_in => dt_register_in,
         id_pat_necessity_in => id_pat_necessity_in,
         id_patient_in => id_patient_in,
         id_necessity_in => id_necessity_in,
         flg_status_in => flg_status_in,
         id_episode_in => id_episode_in,
         id_institution_in => id_institution_in,
         record_status_in => record_status_in,
         import_code_in => import_code_in,
         create_user_in => create_user_in,
         update_user_in => update_user_in,
         create_time_in => create_time_in,
         update_time_in => update_time_in,
         create_institution_in => create_institution_in,
         update_institution_in => update_institution_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
      RETURN l_pky;
   END ins;

      PROCEDURE ins (
      dt_register_in IN PAT_NECESSITY_HIST.DT_REGISTER%TYPE DEFAULT NULL,
      id_pat_necessity_in IN PAT_NECESSITY_HIST.ID_PAT_NECESSITY%TYPE DEFAULT NULL,
      id_patient_in IN PAT_NECESSITY_HIST.ID_PATIENT%TYPE DEFAULT NULL,
      id_necessity_in IN PAT_NECESSITY_HIST.ID_NECESSITY%TYPE DEFAULT NULL,
      flg_status_in IN PAT_NECESSITY_HIST.FLG_STATUS%TYPE DEFAULT NULL,
      id_episode_in IN PAT_NECESSITY_HIST.ID_EPISODE%TYPE DEFAULT NULL,
      id_institution_in IN PAT_NECESSITY_HIST.ID_INSTITUTION%TYPE DEFAULT NULL,
      record_status_in IN PAT_NECESSITY_HIST.RECORD_STATUS%TYPE DEFAULT NULL,
      import_code_in IN PAT_NECESSITY_HIST.IMPORT_CODE%TYPE DEFAULT NULL,
      create_user_in IN PAT_NECESSITY_HIST.CREATE_USER%TYPE DEFAULT NULL,
      update_user_in IN PAT_NECESSITY_HIST.UPDATE_USER%TYPE DEFAULT NULL,
      create_time_in IN PAT_NECESSITY_HIST.CREATE_TIME%TYPE DEFAULT NULL,
      update_time_in IN PAT_NECESSITY_HIST.UPDATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN PAT_NECESSITY_HIST.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_institution_in IN PAT_NECESSITY_HIST.UPDATE_INSTITUTION%TYPE DEFAULT NULL
      ,handle_error_in IN BOOLEAN := TRUE
      , rows_out OUT TABLE_VARCHAR
   )
   IS
        l_pky PAT_NECESSITY_HIST.ID_PAT_NECESSITY_HIST%TYPE := next_key;
   BEGIN
      ins (
         id_pat_necessity_hist_in => l_pky,
         dt_register_in => dt_register_in,
         id_pat_necessity_in => id_pat_necessity_in,
         id_patient_in => id_patient_in,
         id_necessity_in => id_necessity_in,
         flg_status_in => flg_status_in,
         id_episode_in => id_episode_in,
         id_institution_in => id_institution_in,
         record_status_in => record_status_in,
         import_code_in => import_code_in,
         create_user_in => create_user_in,
         update_user_in => update_user_in,
         create_time_in => create_time_in,
         update_time_in => update_time_in,
         create_institution_in => create_institution_in,
         update_institution_in => update_institution_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
   END ins;


     PROCEDURE ins (
      dt_register_in IN PAT_NECESSITY_HIST.DT_REGISTER%TYPE DEFAULT NULL,
      id_pat_necessity_in IN PAT_NECESSITY_HIST.ID_PAT_NECESSITY%TYPE DEFAULT NULL,
      id_patient_in IN PAT_NECESSITY_HIST.ID_PATIENT%TYPE DEFAULT NULL,
      id_necessity_in IN PAT_NECESSITY_HIST.ID_NECESSITY%TYPE DEFAULT NULL,
      flg_status_in IN PAT_NECESSITY_HIST.FLG_STATUS%TYPE DEFAULT NULL,
      id_episode_in IN PAT_NECESSITY_HIST.ID_EPISODE%TYPE DEFAULT NULL,
      id_institution_in IN PAT_NECESSITY_HIST.ID_INSTITUTION%TYPE DEFAULT NULL,
      record_status_in IN PAT_NECESSITY_HIST.RECORD_STATUS%TYPE DEFAULT NULL,
      import_code_in IN PAT_NECESSITY_HIST.IMPORT_CODE%TYPE DEFAULT NULL,
      create_user_in IN PAT_NECESSITY_HIST.CREATE_USER%TYPE DEFAULT NULL,
      update_user_in IN PAT_NECESSITY_HIST.UPDATE_USER%TYPE DEFAULT NULL,
      create_time_in IN PAT_NECESSITY_HIST.CREATE_TIME%TYPE DEFAULT NULL,
      update_time_in IN PAT_NECESSITY_HIST.UPDATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN PAT_NECESSITY_HIST.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_institution_in IN PAT_NECESSITY_HIST.UPDATE_INSTITUTION%TYPE DEFAULT NULL
      ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
        l_pky PAT_NECESSITY_HIST.ID_PAT_NECESSITY_HIST%TYPE := next_key;
      rows_out TABLE_VARCHAR;
   BEGIN
      ins (
         id_pat_necessity_hist_in => l_pky,
         dt_register_in => dt_register_in,
         id_pat_necessity_in => id_pat_necessity_in,
         id_patient_in => id_patient_in,
         id_necessity_in => id_necessity_in,
         flg_status_in => flg_status_in,
         id_episode_in => id_episode_in,
         id_institution_in => id_institution_in,
         record_status_in => record_status_in,
         import_code_in => import_code_in,
         create_user_in => create_user_in,
         update_user_in => update_user_in,
         create_time_in => create_time_in,
         update_time_in => update_time_in,
         create_institution_in => create_institution_in,
         update_institution_in => update_institution_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
         );
   END ins;

    PROCEDURE ins (
      rows_in IN PAT_NECESSITY_HIST_tc
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
   BEGIN
      IF rows_in.COUNT = 0
      THEN
         NULL;
      ELSE
         FORALL indx IN rows_in.FIRST .. rows_in.LAST
            SAVE EXCEPTIONS
            INSERT INTO PAT_NECESSITY_HIST VALUES rows_in (indx) RETURNING ROWID BULK COLLECT INTO rows_out;
      END IF;
   EXCEPTION
     WHEN e_forall_error
     THEN
        -- In Oracle9i and above, SAVE EXCEPTIONS will direct control
        -- here if any error occurs. We can then save all the error
        -- information out to the error instance.
       IF NOT handle_error_in THEN RAISE;
       ELSE
          <<bulk_handler>>
          DECLARE
             l_err_instance_id NUMBER;
          BEGIN
             -- For each error, write to the log.
             FOR indx IN 1 .. SQL%BULK_EXCEPTIONS.COUNT
             LOOP
                pk_alert_exceptions.register_error (
                    error_name_in => 'FORALL-INSERT-FAILURE'
                   ,err_instance_id_out => l_err_instance_id
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'BINDING_ROW_' || indx
                  ,value_in => SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX
                  ,validate_in => FALSE
                );
                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ERROR_AT_ROW_' || indx
                  ,value_in => SQLERRM (-SQL%BULK_EXCEPTIONS (indx).ERROR_CODE)
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_PAT_NECESSITY_HIST _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).ID_PAT_NECESSITY_HIST
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'DT_REGISTER _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).DT_REGISTER
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_PAT_NECESSITY _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).ID_PAT_NECESSITY
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_PATIENT _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).ID_PATIENT
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_NECESSITY _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).ID_NECESSITY
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'FLG_STATUS _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).FLG_STATUS
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_EPISODE _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).ID_EPISODE
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'ID_INSTITUTION _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).ID_INSTITUTION
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'RECORD_STATUS _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).RECORD_STATUS
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'IMPORT_CODE _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).IMPORT_CODE
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'CREATE_USER _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).CREATE_USER
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'UPDATE_USER _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).UPDATE_USER
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'CREATE_TIME _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).CREATE_TIME
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'UPDATE_TIME _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).UPDATE_TIME
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'CREATE_INSTITUTION _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).CREATE_INSTITUTION
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.add_context (
                   err_instance_id_in => l_err_instance_id
                  ,NAME_IN => 'UPDATE_INSTITUTION _' || indx
                  ,value_in => rows_in(SQL%BULK_EXCEPTIONS (indx).ERROR_INDEX).UPDATE_INSTITUTION
                  ,validate_in => FALSE
                );

                pk_alert_exceptions.raise_error_instance( err_instance_id_in => l_err_instance_id );
             END LOOP;
          END bulk_handler;
        END IF;
     WHEN OTHERS
     THEN
       IF NOT handle_error_in THEN RAISE;
       ELSE
       pk_alert_exceptions.raise_error(
          error_name_in => 'FORALL-INSERT-FAILURE'
          ,name1_in => 'TABLE_NAME'
          ,value1_in => 'PAT_NECESSITY_HIST'
          ,name2_in => 'ROW_COUNT'
          ,value2_in => rows_in.COUNT
           );
       END IF;
   END ins;

    PROCEDURE ins (
      rows_in IN PAT_NECESSITY_HIST_tc
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
      rows_out TABLE_VARCHAR;
   BEGIN
      ins (
      rows_in => rows_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
   );
   END ins;


PROCEDURE upd (
      id_pat_necessity_hist_in IN PAT_NECESSITY_HIST.ID_PAT_NECESSITY_HIST%TYPE,
      dt_register_in IN PAT_NECESSITY_HIST.DT_REGISTER%TYPE DEFAULT NULL,
      DT_REGISTER_nin IN BOOLEAN := TRUE,
      id_pat_necessity_in IN PAT_NECESSITY_HIST.ID_PAT_NECESSITY%TYPE DEFAULT NULL,
      ID_PAT_NECESSITY_nin IN BOOLEAN := TRUE,
      id_patient_in IN PAT_NECESSITY_HIST.ID_PATIENT%TYPE DEFAULT NULL,
      ID_PATIENT_nin IN BOOLEAN := TRUE,
      id_necessity_in IN PAT_NECESSITY_HIST.ID_NECESSITY%TYPE DEFAULT NULL,
      ID_NECESSITY_nin IN BOOLEAN := TRUE,
      flg_status_in IN PAT_NECESSITY_HIST.FLG_STATUS%TYPE DEFAULT NULL,
      FLG_STATUS_nin IN BOOLEAN := TRUE,
      id_episode_in IN PAT_NECESSITY_HIST.ID_EPISODE%TYPE DEFAULT NULL,
      ID_EPISODE_nin IN BOOLEAN := TRUE,
      id_institution_in IN PAT_NECESSITY_HIST.ID_INSTITUTION%TYPE DEFAULT NULL,
      ID_INSTITUTION_nin IN BOOLEAN := TRUE,
      record_status_in IN PAT_NECESSITY_HIST.RECORD_STATUS%TYPE DEFAULT NULL,
      RECORD_STATUS_nin IN BOOLEAN := TRUE,
      import_code_in IN PAT_NECESSITY_HIST.IMPORT_CODE%TYPE DEFAULT NULL,
      IMPORT_CODE_nin IN BOOLEAN := TRUE,
      create_user_in IN PAT_NECESSITY_HIST.CREATE_USER%TYPE DEFAULT NULL,
      CREATE_USER_nin IN BOOLEAN := TRUE,
      update_user_in IN PAT_NECESSITY_HIST.UPDATE_USER%TYPE DEFAULT NULL,
      UPDATE_USER_nin IN BOOLEAN := TRUE,
      create_time_in IN PAT_NECESSITY_HIST.CREATE_TIME%TYPE DEFAULT NULL,
      CREATE_TIME_nin IN BOOLEAN := TRUE,
      update_time_in IN PAT_NECESSITY_HIST.UPDATE_TIME%TYPE DEFAULT NULL,
      UPDATE_TIME_nin IN BOOLEAN := TRUE,
      create_institution_in IN PAT_NECESSITY_HIST.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      CREATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      update_institution_in IN PAT_NECESSITY_HIST.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      UPDATE_INSTITUTION_nin IN BOOLEAN := TRUE,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      )
   IS
   l_rows_out TABLE_VARCHAR;
   l_DT_REGISTER_n NUMBER(1);
   l_ID_PAT_NECESSITY_n NUMBER(1);
   l_ID_PATIENT_n NUMBER(1);
   l_ID_NECESSITY_n NUMBER(1);
   l_FLG_STATUS_n NUMBER(1);
   l_ID_EPISODE_n NUMBER(1);
   l_ID_INSTITUTION_n NUMBER(1);
   l_RECORD_STATUS_n NUMBER(1);
   l_IMPORT_CODE_n NUMBER(1);
   l_CREATE_USER_n NUMBER(1);
   l_UPDATE_USER_n NUMBER(1);
   l_CREATE_TIME_n NUMBER(1);
   l_UPDATE_TIME_n NUMBER(1);
   l_CREATE_INSTITUTION_n NUMBER(1);
   l_UPDATE_INSTITUTION_n NUMBER(1);
   BEGIN

   l_DT_REGISTER_n := sys.diutil.bool_to_int(DT_REGISTER_nin);
   l_ID_PAT_NECESSITY_n := sys.diutil.bool_to_int(ID_PAT_NECESSITY_nin);
   l_ID_PATIENT_n := sys.diutil.bool_to_int(ID_PATIENT_nin);
   l_ID_NECESSITY_n := sys.diutil.bool_to_int(ID_NECESSITY_nin);
   l_FLG_STATUS_n := sys.diutil.bool_to_int(FLG_STATUS_nin);
   l_ID_EPISODE_n := sys.diutil.bool_to_int(ID_EPISODE_nin);
   l_ID_INSTITUTION_n := sys.diutil.bool_to_int(ID_INSTITUTION_nin);
   l_RECORD_STATUS_n := sys.diutil.bool_to_int(RECORD_STATUS_nin);
   l_IMPORT_CODE_n := sys.diutil.bool_to_int(IMPORT_CODE_nin);
   l_CREATE_USER_n := sys.diutil.bool_to_int(CREATE_USER_nin);
   l_UPDATE_USER_n := sys.diutil.bool_to_int(UPDATE_USER_nin);
   l_CREATE_TIME_n := sys.diutil.bool_to_int(CREATE_TIME_nin);
   l_UPDATE_TIME_n := sys.diutil.bool_to_int(UPDATE_TIME_nin);
   l_CREATE_INSTITUTION_n := sys.diutil.bool_to_int(CREATE_INSTITUTION_nin);
   l_UPDATE_INSTITUTION_n := sys.diutil.bool_to_int(UPDATE_INSTITUTION_nin);


         UPDATE PAT_NECESSITY_HIST SET
     DT_REGISTER = decode (l_DT_REGISTER_n,0,dt_register_in, NVL (dt_register_in, DT_REGISTER)),
     ID_PAT_NECESSITY = decode (l_ID_PAT_NECESSITY_n,0,id_pat_necessity_in, NVL (id_pat_necessity_in, ID_PAT_NECESSITY)),
     ID_PATIENT = decode (l_ID_PATIENT_n,0,id_patient_in, NVL (id_patient_in, ID_PATIENT)),
     ID_NECESSITY = decode (l_ID_NECESSITY_n,0,id_necessity_in, NVL (id_necessity_in, ID_NECESSITY)),
     FLG_STATUS = decode (l_FLG_STATUS_n,0,flg_status_in, NVL (flg_status_in, FLG_STATUS)),
     ID_EPISODE = decode (l_ID_EPISODE_n,0,id_episode_in, NVL (id_episode_in, ID_EPISODE)),
     ID_INSTITUTION = decode (l_ID_INSTITUTION_n,0,id_institution_in, NVL (id_institution_in, ID_INSTITUTION)),
     RECORD_STATUS = decode (l_RECORD_STATUS_n,0,record_status_in, NVL (record_status_in, RECORD_STATUS)),
     IMPORT_CODE = decode (l_IMPORT_CODE_n,0,import_code_in, NVL (import_code_in, IMPORT_CODE)),
     CREATE_USER = decode (l_CREATE_USER_n,0,create_user_in, NVL (create_user_in, CREATE_USER)),
     UPDATE_USER = decode (l_UPDATE_USER_n,0,update_user_in, NVL (update_user_in, UPDATE_USER)),
     CREATE_TIME = decode (l_CREATE_TIME_n,0,create_time_in, NVL (create_time_in, CREATE_TIME)),
     UPDATE_TIME = decode (l_UPDATE_TIME_n,0,update_time_in, NVL (update_time_in, UPDATE_TIME)),
     CREATE_INSTITUTION = decode (l_CREATE_INSTITUTION_n,0,create_institution_in, NVL (create_institution_in, CREATE_INSTITUTION)),
     UPDATE_INSTITUTION = decode (l_UPDATE_INSTITUTION_n,0,update_institution_in, NVL (update_institution_in, UPDATE_INSTITUTION))
          WHERE
             ID_PAT_NECESSITY_HIST = id_pat_necessity_hist_in
         RETURNING ROWID BULK COLLECT INTO l_rows_out;


if(rows_out is null)
then
rows_out := table_varchar();
end if;

rows_out :=  rows_out MULTISET UNION DISTINCT l_rows_out;

   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF FALSE THEN NULL; -- Placeholder in case no unique indexes
           ELSE
              pk_alert_exceptions.raise_error (
                    error_name_in => 'DUPLICATE-VALUE'
                    ,name1_in => 'OWNER'
                    ,value1_in => l_owner
                    ,name2_in => 'CONSTRAINT_NAME'
                    ,value2_in => l_name
                    ,name3_in => 'TABLE_NAME'
                    ,value3_in => 'PAT_NECESSITY_HIST');
           END IF;
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PAT_NECESSITY_HIST');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PAT_NECESSITY_HIST');
           IF l_name = 'PATNH_EPIS_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_EPISODE'
               , value_in => id_episode_in);
           END IF;
           IF l_name = 'PATNH_INST_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_INSTITUTION'
               , value_in => id_institution_in);
           END IF;
           IF l_name = 'PATNH_NEC_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_NECESSITY'
               , value_in => id_necessity_in);
           END IF;
           IF l_name = 'PATNH_PAT_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_PATIENT'
               , value_in => id_patient_in);
           END IF;
           IF l_name = 'PATNH_PATN_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_PAT_NECESSITY'
               , value_in => id_pat_necessity_in);
           END IF;
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END upd;


   PROCEDURE upd (
      id_pat_necessity_hist_in IN PAT_NECESSITY_HIST.ID_PAT_NECESSITY_HIST%TYPE,
      dt_register_in IN PAT_NECESSITY_HIST.DT_REGISTER%TYPE DEFAULT NULL,
      DT_REGISTER_nin IN BOOLEAN := TRUE,
      id_pat_necessity_in IN PAT_NECESSITY_HIST.ID_PAT_NECESSITY%TYPE DEFAULT NULL,
      ID_PAT_NECESSITY_nin IN BOOLEAN := TRUE,
      id_patient_in IN PAT_NECESSITY_HIST.ID_PATIENT%TYPE DEFAULT NULL,
      ID_PATIENT_nin IN BOOLEAN := TRUE,
      id_necessity_in IN PAT_NECESSITY_HIST.ID_NECESSITY%TYPE DEFAULT NULL,
      ID_NECESSITY_nin IN BOOLEAN := TRUE,
      flg_status_in IN PAT_NECESSITY_HIST.FLG_STATUS%TYPE DEFAULT NULL,
      FLG_STATUS_nin IN BOOLEAN := TRUE,
      id_episode_in IN PAT_NECESSITY_HIST.ID_EPISODE%TYPE DEFAULT NULL,
      ID_EPISODE_nin IN BOOLEAN := TRUE,
      id_institution_in IN PAT_NECESSITY_HIST.ID_INSTITUTION%TYPE DEFAULT NULL,
      ID_INSTITUTION_nin IN BOOLEAN := TRUE,
      record_status_in IN PAT_NECESSITY_HIST.RECORD_STATUS%TYPE DEFAULT NULL,
      RECORD_STATUS_nin IN BOOLEAN := TRUE,
      import_code_in IN PAT_NECESSITY_HIST.IMPORT_CODE%TYPE DEFAULT NULL,
      IMPORT_CODE_nin IN BOOLEAN := TRUE,
      create_user_in IN PAT_NECESSITY_HIST.CREATE_USER%TYPE DEFAULT NULL,
      CREATE_USER_nin IN BOOLEAN := TRUE,
      update_user_in IN PAT_NECESSITY_HIST.UPDATE_USER%TYPE DEFAULT NULL,
      UPDATE_USER_nin IN BOOLEAN := TRUE,
      create_time_in IN PAT_NECESSITY_HIST.CREATE_TIME%TYPE DEFAULT NULL,
      CREATE_TIME_nin IN BOOLEAN := TRUE,
      update_time_in IN PAT_NECESSITY_HIST.UPDATE_TIME%TYPE DEFAULT NULL,
      UPDATE_TIME_nin IN BOOLEAN := TRUE,
      create_institution_in IN PAT_NECESSITY_HIST.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      CREATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      update_institution_in IN PAT_NECESSITY_HIST.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      UPDATE_INSTITUTION_nin IN BOOLEAN := TRUE,
     handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
     upd (
      id_pat_necessity_hist_in => id_pat_necessity_hist_in,
      dt_register_in => dt_register_in,
      DT_REGISTER_nin => DT_REGISTER_nin,
      id_pat_necessity_in => id_pat_necessity_in,
      ID_PAT_NECESSITY_nin => ID_PAT_NECESSITY_nin,
      id_patient_in => id_patient_in,
      ID_PATIENT_nin => ID_PATIENT_nin,
      id_necessity_in => id_necessity_in,
      ID_NECESSITY_nin => ID_NECESSITY_nin,
      flg_status_in => flg_status_in,
      FLG_STATUS_nin => FLG_STATUS_nin,
      id_episode_in => id_episode_in,
      ID_EPISODE_nin => ID_EPISODE_nin,
      id_institution_in => id_institution_in,
      ID_INSTITUTION_nin => ID_INSTITUTION_nin,
      record_status_in => record_status_in,
      RECORD_STATUS_nin => RECORD_STATUS_nin,
      import_code_in => import_code_in,
      IMPORT_CODE_nin => IMPORT_CODE_nin,
      create_user_in => create_user_in,
      CREATE_USER_nin => CREATE_USER_nin,
      update_user_in => update_user_in,
      UPDATE_USER_nin => UPDATE_USER_nin,
      create_time_in => create_time_in,
      CREATE_TIME_nin => CREATE_TIME_nin,
      update_time_in => update_time_in,
      UPDATE_TIME_nin => UPDATE_TIME_nin,
      create_institution_in => create_institution_in,
      CREATE_INSTITUTION_nin => CREATE_INSTITUTION_nin,
      update_institution_in => update_institution_in,
      UPDATE_INSTITUTION_nin => UPDATE_INSTITUTION_nin,
     handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END upd;

PROCEDURE upd (
      dt_register_in IN PAT_NECESSITY_HIST.DT_REGISTER%TYPE DEFAULT NULL,
      DT_REGISTER_nin IN BOOLEAN := TRUE,
      id_pat_necessity_in IN PAT_NECESSITY_HIST.ID_PAT_NECESSITY%TYPE DEFAULT NULL,
      ID_PAT_NECESSITY_nin IN BOOLEAN := TRUE,
      id_patient_in IN PAT_NECESSITY_HIST.ID_PATIENT%TYPE DEFAULT NULL,
      ID_PATIENT_nin IN BOOLEAN := TRUE,
      id_necessity_in IN PAT_NECESSITY_HIST.ID_NECESSITY%TYPE DEFAULT NULL,
      ID_NECESSITY_nin IN BOOLEAN := TRUE,
      flg_status_in IN PAT_NECESSITY_HIST.FLG_STATUS%TYPE DEFAULT NULL,
      FLG_STATUS_nin IN BOOLEAN := TRUE,
      id_episode_in IN PAT_NECESSITY_HIST.ID_EPISODE%TYPE DEFAULT NULL,
      ID_EPISODE_nin IN BOOLEAN := TRUE,
      id_institution_in IN PAT_NECESSITY_HIST.ID_INSTITUTION%TYPE DEFAULT NULL,
      ID_INSTITUTION_nin IN BOOLEAN := TRUE,
      record_status_in IN PAT_NECESSITY_HIST.RECORD_STATUS%TYPE DEFAULT NULL,
      RECORD_STATUS_nin IN BOOLEAN := TRUE,
      import_code_in IN PAT_NECESSITY_HIST.IMPORT_CODE%TYPE DEFAULT NULL,
      IMPORT_CODE_nin IN BOOLEAN := TRUE,
      create_user_in IN PAT_NECESSITY_HIST.CREATE_USER%TYPE DEFAULT NULL,
      CREATE_USER_nin IN BOOLEAN := TRUE,
      update_user_in IN PAT_NECESSITY_HIST.UPDATE_USER%TYPE DEFAULT NULL,
      UPDATE_USER_nin IN BOOLEAN := TRUE,
      create_time_in IN PAT_NECESSITY_HIST.CREATE_TIME%TYPE DEFAULT NULL,
      CREATE_TIME_nin IN BOOLEAN := TRUE,
      update_time_in IN PAT_NECESSITY_HIST.UPDATE_TIME%TYPE DEFAULT NULL,
      UPDATE_TIME_nin IN BOOLEAN := TRUE,
      create_institution_in IN PAT_NECESSITY_HIST.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      CREATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      update_institution_in IN PAT_NECESSITY_HIST.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      UPDATE_INSTITUTION_nin IN BOOLEAN := TRUE,
    where_in varchar2,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      )
   IS
   l_sql VARCHAR2(32767);
   l_rows_out TABLE_VARCHAR;
   l_DT_REGISTER_n NUMBER(1);
   l_ID_PAT_NECESSITY_n NUMBER(1);
   l_ID_PATIENT_n NUMBER(1);
   l_ID_NECESSITY_n NUMBER(1);
   l_FLG_STATUS_n NUMBER(1);
   l_ID_EPISODE_n NUMBER(1);
   l_ID_INSTITUTION_n NUMBER(1);
   l_RECORD_STATUS_n NUMBER(1);
   l_IMPORT_CODE_n NUMBER(1);
   l_CREATE_USER_n NUMBER(1);
   l_UPDATE_USER_n NUMBER(1);
   l_CREATE_TIME_n NUMBER(1);
   l_UPDATE_TIME_n NUMBER(1);
   l_CREATE_INSTITUTION_n NUMBER(1);
   l_UPDATE_INSTITUTION_n NUMBER(1);
      id_pat_necessity_hist_in PAT_NECESSITY_HIST.ID_PAT_NECESSITY_HIST%TYPE;
   BEGIN



      l_DT_REGISTER_n := sys.diutil.bool_to_int(DT_REGISTER_nin);
      l_ID_PAT_NECESSITY_n := sys.diutil.bool_to_int(ID_PAT_NECESSITY_nin);
      l_ID_PATIENT_n := sys.diutil.bool_to_int(ID_PATIENT_nin);
      l_ID_NECESSITY_n := sys.diutil.bool_to_int(ID_NECESSITY_nin);
      l_FLG_STATUS_n := sys.diutil.bool_to_int(FLG_STATUS_nin);
      l_ID_EPISODE_n := sys.diutil.bool_to_int(ID_EPISODE_nin);
      l_ID_INSTITUTION_n := sys.diutil.bool_to_int(ID_INSTITUTION_nin);
      l_RECORD_STATUS_n := sys.diutil.bool_to_int(RECORD_STATUS_nin);
      l_IMPORT_CODE_n := sys.diutil.bool_to_int(IMPORT_CODE_nin);
      l_CREATE_USER_n := sys.diutil.bool_to_int(CREATE_USER_nin);
      l_UPDATE_USER_n := sys.diutil.bool_to_int(UPDATE_USER_nin);
      l_CREATE_TIME_n := sys.diutil.bool_to_int(CREATE_TIME_nin);
      l_UPDATE_TIME_n := sys.diutil.bool_to_int(UPDATE_TIME_nin);
      l_CREATE_INSTITUTION_n := sys.diutil.bool_to_int(CREATE_INSTITUTION_nin);
      l_UPDATE_INSTITUTION_n := sys.diutil.bool_to_int(UPDATE_INSTITUTION_nin);



l_sql := 'UPDATE PAT_NECESSITY_HIST SET '
     || ' DT_REGISTER = decode (' || l_DT_REGISTER_n || ',0,:dt_register_in, NVL (:dt_register_in, DT_REGISTER)) '|| ','
     || ' ID_PAT_NECESSITY = decode (' || l_ID_PAT_NECESSITY_n || ',0,:id_pat_necessity_in, NVL (:id_pat_necessity_in, ID_PAT_NECESSITY)) '|| ','
     || ' ID_PATIENT = decode (' || l_ID_PATIENT_n || ',0,:id_patient_in, NVL (:id_patient_in, ID_PATIENT)) '|| ','
     || ' ID_NECESSITY = decode (' || l_ID_NECESSITY_n || ',0,:id_necessity_in, NVL (:id_necessity_in, ID_NECESSITY)) '|| ','
     || ' FLG_STATUS = decode (' || l_FLG_STATUS_n || ',0,:flg_status_in, NVL (:flg_status_in, FLG_STATUS)) '|| ','
     || ' ID_EPISODE = decode (' || l_ID_EPISODE_n || ',0,:id_episode_in, NVL (:id_episode_in, ID_EPISODE)) '|| ','
     || ' ID_INSTITUTION = decode (' || l_ID_INSTITUTION_n || ',0,:id_institution_in, NVL (:id_institution_in, ID_INSTITUTION)) '|| ','
     || ' RECORD_STATUS = decode (' || l_RECORD_STATUS_n || ',0,:record_status_in, NVL (:record_status_in, RECORD_STATUS)) '|| ','
     || ' IMPORT_CODE = decode (' || l_IMPORT_CODE_n || ',0,:import_code_in, NVL (:import_code_in, IMPORT_CODE)) '|| ','
     || ' CREATE_USER = decode (' || l_CREATE_USER_n || ',0,:create_user_in, NVL (:create_user_in, CREATE_USER)) '|| ','
     || ' UPDATE_USER = decode (' || l_UPDATE_USER_n || ',0,:update_user_in, NVL (:update_user_in, UPDATE_USER)) '|| ','
     || ' CREATE_TIME = decode (' || l_CREATE_TIME_n || ',0,:create_time_in, NVL (:create_time_in, CREATE_TIME)) '|| ','
     || ' UPDATE_TIME = decode (' || l_UPDATE_TIME_n || ',0,:update_time_in, NVL (:update_time_in, UPDATE_TIME)) '|| ','
     || ' CREATE_INSTITUTION = decode (' || l_CREATE_INSTITUTION_n || ',0,:create_institution_in, NVL (:create_institution_in, CREATE_INSTITUTION)) '|| ','
     || ' UPDATE_INSTITUTION = decode (' || l_UPDATE_INSTITUTION_n || ',0,:update_institution_in, NVL (:update_institution_in, UPDATE_INSTITUTION)) '
      || ' where ' || nvl(where_in,'(1=1)')
      || ' RETURNING ROWID BULK COLLECT INTO :l_rows_out';




execute immediate 'BEGIN ' || l_sql || '; END;' using in
     dt_register_in,
     id_pat_necessity_in,
     id_patient_in,
     id_necessity_in,
     flg_status_in,
     id_episode_in,
     id_institution_in,
     record_status_in,
     import_code_in,
     create_user_in,
     update_user_in,
     create_time_in,
     update_time_in,
     create_institution_in,
     update_institution_in,
    OUT l_rows_out;

if(rows_out is null)
then
rows_out := table_varchar();
end if;

rows_out :=  rows_out MULTISET UNION DISTINCT l_rows_out;

   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF FALSE THEN NULL; -- Placeholder in case no unique indexes
           ELSE
              pk_alert_exceptions.raise_error (
                    error_name_in => 'DUPLICATE-VALUE'
                    ,name1_in => 'OWNER'
                    ,value1_in => l_owner
                    ,name2_in => 'CONSTRAINT_NAME'
                    ,value2_in => l_name
                    ,name3_in => 'TABLE_NAME'
                    ,value3_in => 'PAT_NECESSITY_HIST');
           END IF;
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PAT_NECESSITY_HIST');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PAT_NECESSITY_HIST');
           IF l_name = 'PATNH_EPIS_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_EPISODE'
               , value_in => id_episode_in);
           END IF;
           IF l_name = 'PATNH_INST_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_INSTITUTION'
               , value_in => id_institution_in);
           END IF;
           IF l_name = 'PATNH_NEC_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_NECESSITY'
               , value_in => id_necessity_in);
           END IF;
           IF l_name = 'PATNH_PAT_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_PATIENT'
               , value_in => id_patient_in);
           END IF;
           IF l_name = 'PATNH_PATN_FK'
           THEN
              -- Add a context value for each column
              pk_alert_exceptions.add_context (
                 err_instance_id_in => l_id
               , name_in => 'ID_PAT_NECESSITY'
               , value_in => id_pat_necessity_in);
           END IF;
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END upd;





PROCEDURE upd (
      dt_register_in IN PAT_NECESSITY_HIST.DT_REGISTER%TYPE DEFAULT NULL,
      DT_REGISTER_nin IN BOOLEAN := TRUE,
      id_pat_necessity_in IN PAT_NECESSITY_HIST.ID_PAT_NECESSITY%TYPE DEFAULT NULL,
      ID_PAT_NECESSITY_nin IN BOOLEAN := TRUE,
      id_patient_in IN PAT_NECESSITY_HIST.ID_PATIENT%TYPE DEFAULT NULL,
      ID_PATIENT_nin IN BOOLEAN := TRUE,
      id_necessity_in IN PAT_NECESSITY_HIST.ID_NECESSITY%TYPE DEFAULT NULL,
      ID_NECESSITY_nin IN BOOLEAN := TRUE,
      flg_status_in IN PAT_NECESSITY_HIST.FLG_STATUS%TYPE DEFAULT NULL,
      FLG_STATUS_nin IN BOOLEAN := TRUE,
      id_episode_in IN PAT_NECESSITY_HIST.ID_EPISODE%TYPE DEFAULT NULL,
      ID_EPISODE_nin IN BOOLEAN := TRUE,
      id_institution_in IN PAT_NECESSITY_HIST.ID_INSTITUTION%TYPE DEFAULT NULL,
      ID_INSTITUTION_nin IN BOOLEAN := TRUE,
      record_status_in IN PAT_NECESSITY_HIST.RECORD_STATUS%TYPE DEFAULT NULL,
      RECORD_STATUS_nin IN BOOLEAN := TRUE,
      import_code_in IN PAT_NECESSITY_HIST.IMPORT_CODE%TYPE DEFAULT NULL,
      IMPORT_CODE_nin IN BOOLEAN := TRUE,
      create_user_in IN PAT_NECESSITY_HIST.CREATE_USER%TYPE DEFAULT NULL,
      CREATE_USER_nin IN BOOLEAN := TRUE,
      update_user_in IN PAT_NECESSITY_HIST.UPDATE_USER%TYPE DEFAULT NULL,
      UPDATE_USER_nin IN BOOLEAN := TRUE,
      create_time_in IN PAT_NECESSITY_HIST.CREATE_TIME%TYPE DEFAULT NULL,
      CREATE_TIME_nin IN BOOLEAN := TRUE,
      update_time_in IN PAT_NECESSITY_HIST.UPDATE_TIME%TYPE DEFAULT NULL,
      UPDATE_TIME_nin IN BOOLEAN := TRUE,
      create_institution_in IN PAT_NECESSITY_HIST.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      CREATE_INSTITUTION_nin IN BOOLEAN := TRUE,
      update_institution_in IN PAT_NECESSITY_HIST.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
      UPDATE_INSTITUTION_nin IN BOOLEAN := TRUE,
    where_in varchar2,
     handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
      upd (
      dt_register_in => dt_register_in,
      DT_REGISTER_nin => DT_REGISTER_nin,
      id_pat_necessity_in => id_pat_necessity_in,
      ID_PAT_NECESSITY_nin => ID_PAT_NECESSITY_nin,
      id_patient_in => id_patient_in,
      ID_PATIENT_nin => ID_PATIENT_nin,
      id_necessity_in => id_necessity_in,
      ID_NECESSITY_nin => ID_NECESSITY_nin,
      flg_status_in => flg_status_in,
      FLG_STATUS_nin => FLG_STATUS_nin,
      id_episode_in => id_episode_in,
      ID_EPISODE_nin => ID_EPISODE_nin,
      id_institution_in => id_institution_in,
      ID_INSTITUTION_nin => ID_INSTITUTION_nin,
      record_status_in => record_status_in,
      RECORD_STATUS_nin => RECORD_STATUS_nin,
      import_code_in => import_code_in,
      IMPORT_CODE_nin => IMPORT_CODE_nin,
      create_user_in => create_user_in,
      CREATE_USER_nin => CREATE_USER_nin,
      update_user_in => update_user_in,
      UPDATE_USER_nin => UPDATE_USER_nin,
      create_time_in => create_time_in,
      CREATE_TIME_nin => CREATE_TIME_nin,
      update_time_in => update_time_in,
      UPDATE_TIME_nin => UPDATE_TIME_nin,
      create_institution_in => create_institution_in,
      CREATE_INSTITUTION_nin => CREATE_INSTITUTION_nin,
      update_institution_in => update_institution_in,
      UPDATE_INSTITUTION_nin => UPDATE_INSTITUTION_nin,
    where_in => where_in,
     handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END upd;

   PROCEDURE upd (
      rec_in IN PAT_NECESSITY_HIST%ROWTYPE,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      upd (
         id_pat_necessity_hist_in => rec_in.ID_PAT_NECESSITY_HIST,
         dt_register_in => rec_in.DT_REGISTER,
         id_pat_necessity_in => rec_in.ID_PAT_NECESSITY,
         id_patient_in => rec_in.ID_PATIENT,
         id_necessity_in => rec_in.ID_NECESSITY,
         flg_status_in => rec_in.FLG_STATUS,
         id_episode_in => rec_in.ID_EPISODE,
         id_institution_in => rec_in.ID_INSTITUTION,
         record_status_in => rec_in.RECORD_STATUS,
         import_code_in => rec_in.IMPORT_CODE,
         create_user_in => rec_in.CREATE_USER,
         update_user_in => rec_in.UPDATE_USER,
         create_time_in => rec_in.CREATE_TIME,
         update_time_in => rec_in.UPDATE_TIME,
         create_institution_in => rec_in.CREATE_INSTITUTION,
         update_institution_in => rec_in.UPDATE_INSTITUTION

        ,handle_error_in => handle_error_in
        , rows_out => rows_out
       );
   END upd;

   PROCEDURE upd (
      rec_in IN PAT_NECESSITY_HIST%ROWTYPE,
     handle_error_in IN BOOLEAN := TRUE
      )
   IS
        rows_out TABLE_VARCHAR;
   BEGIN
      upd (
         id_pat_necessity_hist_in => rec_in.ID_PAT_NECESSITY_HIST,
         dt_register_in => rec_in.DT_REGISTER,
         id_pat_necessity_in => rec_in.ID_PAT_NECESSITY,
         id_patient_in => rec_in.ID_PATIENT,
         id_necessity_in => rec_in.ID_NECESSITY,
         flg_status_in => rec_in.FLG_STATUS,
         id_episode_in => rec_in.ID_EPISODE,
         id_institution_in => rec_in.ID_INSTITUTION,
         record_status_in => rec_in.RECORD_STATUS,
         import_code_in => rec_in.IMPORT_CODE,
         create_user_in => rec_in.CREATE_USER,
         update_user_in => rec_in.UPDATE_USER,
         create_time_in => rec_in.CREATE_TIME,
         update_time_in => rec_in.UPDATE_TIME,
         create_institution_in => rec_in.CREATE_INSTITUTION,
         update_institution_in => rec_in.UPDATE_INSTITUTION

        ,handle_error_in => handle_error_in
        , rows_out => rows_out
       );
   END upd;

   PROCEDURE upd_ins (
      id_pat_necessity_hist_in IN PAT_NECESSITY_HIST.ID_PAT_NECESSITY_HIST%TYPE,
      dt_register_in IN PAT_NECESSITY_HIST.DT_REGISTER%TYPE DEFAULT NULL,
      id_pat_necessity_in IN PAT_NECESSITY_HIST.ID_PAT_NECESSITY%TYPE DEFAULT NULL,
      id_patient_in IN PAT_NECESSITY_HIST.ID_PATIENT%TYPE DEFAULT NULL,
      id_necessity_in IN PAT_NECESSITY_HIST.ID_NECESSITY%TYPE DEFAULT NULL,
      flg_status_in IN PAT_NECESSITY_HIST.FLG_STATUS%TYPE DEFAULT NULL,
      id_episode_in IN PAT_NECESSITY_HIST.ID_EPISODE%TYPE DEFAULT NULL,
      id_institution_in IN PAT_NECESSITY_HIST.ID_INSTITUTION%TYPE DEFAULT NULL,
      record_status_in IN PAT_NECESSITY_HIST.RECORD_STATUS%TYPE DEFAULT NULL,
      import_code_in IN PAT_NECESSITY_HIST.IMPORT_CODE%TYPE DEFAULT NULL,
      create_user_in IN PAT_NECESSITY_HIST.CREATE_USER%TYPE DEFAULT NULL,
      update_user_in IN PAT_NECESSITY_HIST.UPDATE_USER%TYPE DEFAULT NULL,
      create_time_in IN PAT_NECESSITY_HIST.CREATE_TIME%TYPE DEFAULT NULL,
      update_time_in IN PAT_NECESSITY_HIST.UPDATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN PAT_NECESSITY_HIST.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_institution_in IN PAT_NECESSITY_HIST.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
     handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      upd (
         id_pat_necessity_hist_in => id_pat_necessity_hist_in,
         dt_register_in => dt_register_in,
         id_pat_necessity_in => id_pat_necessity_in,
         id_patient_in => id_patient_in,
         id_necessity_in => id_necessity_in,
         flg_status_in => flg_status_in,
         id_episode_in => id_episode_in,
         id_institution_in => id_institution_in,
         record_status_in => record_status_in,
         import_code_in => import_code_in,
         create_user_in => create_user_in,
         update_user_in => update_user_in,
         create_time_in => create_time_in,
         update_time_in => update_time_in,
         create_institution_in => create_institution_in,
         update_institution_in => update_institution_in
         ,handle_error_in => handle_error_in
         , rows_out => rows_out
       );
      IF SQL%ROWCOUNT = 0
      THEN
         ins (
            id_pat_necessity_hist_in => id_pat_necessity_hist_in,
            dt_register_in => dt_register_in,
            id_pat_necessity_in => id_pat_necessity_in,
            id_patient_in => id_patient_in,
            id_necessity_in => id_necessity_in,
            flg_status_in => flg_status_in,
            id_episode_in => id_episode_in,
            id_institution_in => id_institution_in,
            record_status_in => record_status_in,
            import_code_in => import_code_in,
            create_user_in => create_user_in,
            update_user_in => update_user_in,
            create_time_in => create_time_in,
            update_time_in => update_time_in,
            create_institution_in => create_institution_in,
            update_institution_in => update_institution_in
            ,handle_error_in => handle_error_in
            , rows_out => rows_out
         );
      END IF;
   END upd_ins;

   PROCEDURE upd_ins (
      id_pat_necessity_hist_in IN PAT_NECESSITY_HIST.ID_PAT_NECESSITY_HIST%TYPE,
      dt_register_in IN PAT_NECESSITY_HIST.DT_REGISTER%TYPE DEFAULT NULL,
      id_pat_necessity_in IN PAT_NECESSITY_HIST.ID_PAT_NECESSITY%TYPE DEFAULT NULL,
      id_patient_in IN PAT_NECESSITY_HIST.ID_PATIENT%TYPE DEFAULT NULL,
      id_necessity_in IN PAT_NECESSITY_HIST.ID_NECESSITY%TYPE DEFAULT NULL,
      flg_status_in IN PAT_NECESSITY_HIST.FLG_STATUS%TYPE DEFAULT NULL,
      id_episode_in IN PAT_NECESSITY_HIST.ID_EPISODE%TYPE DEFAULT NULL,
      id_institution_in IN PAT_NECESSITY_HIST.ID_INSTITUTION%TYPE DEFAULT NULL,
      record_status_in IN PAT_NECESSITY_HIST.RECORD_STATUS%TYPE DEFAULT NULL,
      import_code_in IN PAT_NECESSITY_HIST.IMPORT_CODE%TYPE DEFAULT NULL,
      create_user_in IN PAT_NECESSITY_HIST.CREATE_USER%TYPE DEFAULT NULL,
      update_user_in IN PAT_NECESSITY_HIST.UPDATE_USER%TYPE DEFAULT NULL,
      create_time_in IN PAT_NECESSITY_HIST.CREATE_TIME%TYPE DEFAULT NULL,
      update_time_in IN PAT_NECESSITY_HIST.UPDATE_TIME%TYPE DEFAULT NULL,
      create_institution_in IN PAT_NECESSITY_HIST.CREATE_INSTITUTION%TYPE DEFAULT NULL,
      update_institution_in IN PAT_NECESSITY_HIST.UPDATE_INSTITUTION%TYPE DEFAULT NULL,
     handle_error_in IN BOOLEAN := TRUE
      )
   IS
        rows_out TABLE_VARCHAR;
   BEGIN
      upd_ins (
      id_pat_necessity_hist_in,
      dt_register_in,
      id_pat_necessity_in,
      id_patient_in,
      id_necessity_in,
      flg_status_in,
      id_episode_in,
      id_institution_in,
      record_status_in,
      import_code_in,
      create_user_in,
      update_user_in,
      create_time_in,
      update_time_in,
      create_institution_in,
      update_institution_in,
     handle_error_in
     ,rows_out
      );
   END upd_ins;


   PROCEDURE upd (
      col_in IN PAT_NECESSITY_HIST_tc,
      ignore_if_null_in IN BOOLEAN := TRUE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out IN OUT TABLE_VARCHAR
      )
   IS
      l_ID_PAT_NECESSITY_HIST ID_PAT_NECESSITY_HIST_cc;
      l_DT_REGISTER DT_REGISTER_cc;
      l_ID_PAT_NECESSITY ID_PAT_NECESSITY_cc;
      l_ID_PATIENT ID_PATIENT_cc;
      l_ID_NECESSITY ID_NECESSITY_cc;
      l_FLG_STATUS FLG_STATUS_cc;
      l_ID_EPISODE ID_EPISODE_cc;
      l_ID_INSTITUTION ID_INSTITUTION_cc;
      l_RECORD_STATUS RECORD_STATUS_cc;
      l_IMPORT_CODE IMPORT_CODE_cc;
      l_CREATE_USER CREATE_USER_cc;
      l_UPDATE_USER UPDATE_USER_cc;
      l_CREATE_TIME CREATE_TIME_cc;
      l_UPDATE_TIME UPDATE_TIME_cc;
      l_CREATE_INSTITUTION CREATE_INSTITUTION_cc;
      l_UPDATE_INSTITUTION UPDATE_INSTITUTION_cc;
   BEGIN
      FOR i IN col_in.FIRST .. col_in.LAST loop
         l_ID_PAT_NECESSITY_HIST(i) := col_in(i).ID_PAT_NECESSITY_HIST;
         l_DT_REGISTER(i) := col_in(i).DT_REGISTER;
         l_ID_PAT_NECESSITY(i) := col_in(i).ID_PAT_NECESSITY;
         l_ID_PATIENT(i) := col_in(i).ID_PATIENT;
         l_ID_NECESSITY(i) := col_in(i).ID_NECESSITY;
         l_FLG_STATUS(i) := col_in(i).FLG_STATUS;
         l_ID_EPISODE(i) := col_in(i).ID_EPISODE;
         l_ID_INSTITUTION(i) := col_in(i).ID_INSTITUTION;
         l_RECORD_STATUS(i) := col_in(i).RECORD_STATUS;
         l_IMPORT_CODE(i) := col_in(i).IMPORT_CODE;
         l_CREATE_USER(i) := col_in(i).CREATE_USER;
         l_UPDATE_USER(i) := col_in(i).UPDATE_USER;
         l_CREATE_TIME(i) := col_in(i).CREATE_TIME;
         l_UPDATE_TIME(i) := col_in(i).UPDATE_TIME;
         l_CREATE_INSTITUTION(i) := col_in(i).CREATE_INSTITUTION;
         l_UPDATE_INSTITUTION(i) := col_in(i).UPDATE_INSTITUTION;
      END LOOP;
      IF NVL (ignore_if_null_in, FALSE)
      THEN
         -- Set any columns to their current values
         -- if incoming value is NULL.
         -- Put WHEN clause on column-level triggers!
         FORALL i IN col_in.FIRST .. col_in.LAST
            UPDATE PAT_NECESSITY_HIST SET
               DT_REGISTER = NVL (l_DT_REGISTER(i), DT_REGISTER),
               ID_PAT_NECESSITY = NVL (l_ID_PAT_NECESSITY(i), ID_PAT_NECESSITY),
               ID_PATIENT = NVL (l_ID_PATIENT(i), ID_PATIENT),
               ID_NECESSITY = NVL (l_ID_NECESSITY(i), ID_NECESSITY),
               FLG_STATUS = NVL (l_FLG_STATUS(i), FLG_STATUS),
               ID_EPISODE = NVL (l_ID_EPISODE(i), ID_EPISODE),
               ID_INSTITUTION = NVL (l_ID_INSTITUTION(i), ID_INSTITUTION),
               RECORD_STATUS = NVL (l_RECORD_STATUS(i), RECORD_STATUS),
               IMPORT_CODE = NVL (l_IMPORT_CODE(i), IMPORT_CODE),
               CREATE_USER = NVL (l_CREATE_USER(i), CREATE_USER),
               UPDATE_USER = NVL (l_UPDATE_USER(i), UPDATE_USER),
               CREATE_TIME = NVL (l_CREATE_TIME(i), CREATE_TIME),
               UPDATE_TIME = NVL (l_UPDATE_TIME(i), UPDATE_TIME),
               CREATE_INSTITUTION = NVL (l_CREATE_INSTITUTION(i), CREATE_INSTITUTION),
               UPDATE_INSTITUTION = NVL (l_UPDATE_INSTITUTION(i), UPDATE_INSTITUTION)
             WHERE
                ID_PAT_NECESSITY_HIST = l_ID_PAT_NECESSITY_HIST(i)
          returning rowid bulk collect into rows_out;
      ELSE
         FORALL i IN col_in.FIRST .. col_in.LAST
            UPDATE PAT_NECESSITY_HIST SET
               DT_REGISTER = l_DT_REGISTER(i),
               ID_PAT_NECESSITY = l_ID_PAT_NECESSITY(i),
               ID_PATIENT = l_ID_PATIENT(i),
               ID_NECESSITY = l_ID_NECESSITY(i),
               FLG_STATUS = l_FLG_STATUS(i),
               ID_EPISODE = l_ID_EPISODE(i),
               ID_INSTITUTION = l_ID_INSTITUTION(i),
               RECORD_STATUS = l_RECORD_STATUS(i),
               IMPORT_CODE = l_IMPORT_CODE(i),
               CREATE_USER = l_CREATE_USER(i),
               UPDATE_USER = l_UPDATE_USER(i),
               CREATE_TIME = l_CREATE_TIME(i),
               UPDATE_TIME = l_UPDATE_TIME(i),
               CREATE_INSTITUTION = l_CREATE_INSTITUTION(i),
               UPDATE_INSTITUTION = l_UPDATE_INSTITUTION(i)
             WHERE
                ID_PAT_NECESSITY_HIST = l_ID_PAT_NECESSITY_HIST(i)
        returning rowid bulk collect into rows_out  ;
      END IF;
   END upd;


   PROCEDURE upd (
      col_in IN PAT_NECESSITY_HIST_tc,
      ignore_if_null_in IN BOOLEAN := TRUE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
     rows_out TABLE_VARCHAR;
   BEGIN
      upd (
      col_in ,
      ignore_if_null_in
     ,handle_error_in
     , rows_out
      );
   END upd;

   FUNCTION dynupdstr (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL)

   RETURN VARCHAR2
   IS
   BEGIN
      RETURN
         'BEGIN UPDATE PAT_NECESSITY_HIST
             SET ' || colname_in || ' = :value
           WHERE ' || NVL (where_in, '1=1') || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;' ;
   END dynupdstr;

   FUNCTION dynupdstr_no_rows_out (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL)

   RETURN VARCHAR2
   IS
   BEGIN
      RETURN
         'UPDATE PAT_NECESSITY_HIST
             SET ' || colname_in || ' = :value
           WHERE ' || NVL (where_in, '1=1');
   END dynupdstr_no_rows_out;























  PROCEDURE increment_onecol (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL
      , increment_value_in IN NUMBER DEFAULT 1
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN UPDATE PAT_NECESSITY_HIST set ' || colname_in || '=' || colname_in || ' + ' || nvl(increment_value_in,1) || ' WHERE ' || NVL (where_in, '1=1') || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
      USING OUT rows_out;
   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'DUPLICATE-VALUE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PAT_NECESSITY_HIST');
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PAT_NECESSITY_HIST');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PAT_NECESSITY_HIST');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END increment_onecol;

   PROCEDURE increment_onecol (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE,
      where_in IN VARCHAR2 := NULL
     , increment_value_in IN NUMBER DEFAULT 1
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
   rows_out table_varchar;
   BEGIN
      EXECUTE IMMEDIATE 'UPDATE PAT_NECESSITY_HIST set ' || colname_in || '=' || colname_in || ' + ' || nvl(increment_value_in,1) || ' WHERE ' || NVL (where_in, '1=1');
   EXCEPTION
      WHEN DUP_VAL_ON_INDEX
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_err_instance_id PLS_INTEGER;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'DUPLICATE-VALUE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PAT_NECESSITY_HIST');
        END;
        END IF;
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PAT_NECESSITY_HIST');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PAT_NECESSITY_HIST');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
      WHEN e_null_column_value
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           v_errm VARCHAR2(2000) := DBMS_UTILITY.FORMAT_ERROR_STACK;
           dot1loc INTEGER;
           dot2loc INTEGER;
           parenloc INTEGER;
           c_owner ALL_CONSTRAINTS.OWNER%TYPE;
           c_tabname ALL_TABLES.TABLE_NAME%TYPE;
           c_colname ALL_TAB_COLUMNS.COLUMN_NAME%TYPE;
        BEGIN
           dot1loc := INSTR (v_errm, '.', 1, 1);
           dot2loc := INSTR (v_errm, '.', 1, 2);
           parenloc := INSTR (v_errm, '(');
           c_owner :=SUBSTR (v_errm, parenloc+1, dot1loc-parenloc-1);
           c_tabname := SUBSTR (v_errm, dot1loc+1, dot2loc-dot1loc-1);
           c_colname := SUBSTR (v_errm, dot2loc+1, INSTR (v_errm,')')-dot2loc-1);

           pk_alert_exceptions.raise_error (
                error_name_in => 'COLUMN-CANNOT-BE-NULL'
               ,name1_in => 'OWNER'
               ,value1_in => c_owner
               ,name2_in => 'TABLE_NAME'
               ,value2_in => c_tabname
               ,name3_in => 'COLUMN_NAME'
               ,value3_in => c_colname);
        END;
        END IF;
   END increment_onecol;


   -- Delete functionality


   PROCEDURE del (
      id_pat_necessity_hist_in IN PAT_NECESSITY_HIST.ID_PAT_NECESSITY_HIST%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM PAT_NECESSITY_HIST
       WHERE
          ID_PAT_NECESSITY_HIST = id_pat_necessity_hist_in
       RETURNING ROWID BULK COLLECT INTO rows_out
         ;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PAT_NECESSITY_HIST');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PAT_NECESSITY_HIST');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del;




   PROCEDURE del (
      id_pat_necessity_hist_in IN PAT_NECESSITY_HIST.ID_PAT_NECESSITY_HIST%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
rows_out TABLE_VARCHAR;
   BEGIN

del (
      id_pat_necessity_hist_in => id_pat_necessity_hist_in
     ,handle_error_in => handle_error_in
, rows_out => rows_out
      );

   END del;








   -- Delete all rows for primary key column ID_PAT_NECESSITY_HIST
   PROCEDURE del_ID_PAT_NECESSITY_HIST (
      id_pat_necessity_hist_in IN PAT_NECESSITY_HIST.ID_PAT_NECESSITY_HIST%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
     )
   IS
   BEGIN
      DELETE FROM PAT_NECESSITY_HIST
       WHERE ID_PAT_NECESSITY_HIST = id_pat_necessity_hist_in
      RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PAT_NECESSITY_HIST');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PAT_NECESSITY_HIST');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_ID_PAT_NECESSITY_HIST;






   -- Delete all rows for primary key column ID_PAT_NECESSITY_HIST
   PROCEDURE del_ID_PAT_NECESSITY_HIST (
      id_pat_necessity_hist_in IN PAT_NECESSITY_HIST.ID_PAT_NECESSITY_HIST%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_ID_PAT_NECESSITY_HIST (
      id_pat_necessity_hist_in => id_pat_necessity_hist_in
     ,handle_error_in => handle_error_in
, rows_out => rows_out
     );
   END del_ID_PAT_NECESSITY_HIST;















   PROCEDURE del_PATNH_EPIS_FK (
      id_episode_in IN PAT_NECESSITY_HIST.ID_EPISODE%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM PAT_NECESSITY_HIST
       WHERE
          ID_EPISODE = del_PATNH_EPIS_FK.id_episode_in
       RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PAT_NECESSITY_HIST');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PAT_NECESSITY_HIST');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_PATNH_EPIS_FK;



PROCEDURE del_PATNH_EPIS_FK (
      id_episode_in IN PAT_NECESSITY_HIST.ID_EPISODE%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_PATNH_EPIS_FK (
      id_episode_in => id_episode_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END del_PATNH_EPIS_FK;





   PROCEDURE del_PATNH_INST_FK (
      id_institution_in IN PAT_NECESSITY_HIST.ID_INSTITUTION%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM PAT_NECESSITY_HIST
       WHERE
          ID_INSTITUTION = del_PATNH_INST_FK.id_institution_in
       RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PAT_NECESSITY_HIST');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PAT_NECESSITY_HIST');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_PATNH_INST_FK;



PROCEDURE del_PATNH_INST_FK (
      id_institution_in IN PAT_NECESSITY_HIST.ID_INSTITUTION%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_PATNH_INST_FK (
      id_institution_in => id_institution_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END del_PATNH_INST_FK;





   PROCEDURE del_PATNH_NEC_FK (
      id_necessity_in IN PAT_NECESSITY_HIST.ID_NECESSITY%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM PAT_NECESSITY_HIST
       WHERE
          ID_NECESSITY = del_PATNH_NEC_FK.id_necessity_in
       RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PAT_NECESSITY_HIST');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PAT_NECESSITY_HIST');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_PATNH_NEC_FK;



PROCEDURE del_PATNH_NEC_FK (
      id_necessity_in IN PAT_NECESSITY_HIST.ID_NECESSITY%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_PATNH_NEC_FK (
      id_necessity_in => id_necessity_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END del_PATNH_NEC_FK;





   PROCEDURE del_PATNH_PAT_FK (
      id_patient_in IN PAT_NECESSITY_HIST.ID_PATIENT%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM PAT_NECESSITY_HIST
       WHERE
          ID_PATIENT = del_PATNH_PAT_FK.id_patient_in
       RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PAT_NECESSITY_HIST');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PAT_NECESSITY_HIST');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_PATNH_PAT_FK;



PROCEDURE del_PATNH_PAT_FK (
      id_patient_in IN PAT_NECESSITY_HIST.ID_PATIENT%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_PATNH_PAT_FK (
      id_patient_in => id_patient_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END del_PATNH_PAT_FK;





   PROCEDURE del_PATNH_PATN_FK (
      id_pat_necessity_in IN PAT_NECESSITY_HIST.ID_PAT_NECESSITY%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      DELETE FROM PAT_NECESSITY_HIST
       WHERE
          ID_PAT_NECESSITY = del_PATNH_PATN_FK.id_pat_necessity_in
       RETURNING ROWID BULK COLLECT INTO rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PAT_NECESSITY_HIST');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PAT_NECESSITY_HIST');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_PATNH_PATN_FK;



PROCEDURE del_PATNH_PATN_FK (
      id_pat_necessity_in IN PAT_NECESSITY_HIST.ID_PAT_NECESSITY%TYPE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   rows_out TABLE_VARCHAR;
   BEGIN
del_PATNH_PATN_FK (
      id_pat_necessity_in => id_pat_necessity_in
     ,handle_error_in => handle_error_in
     , rows_out => rows_out
      );
   END del_PATNH_PATN_FK;












   -- Deletions using dynamic SQL
   FUNCTION dyndelstr (where_in IN VARCHAR2) RETURN VARCHAR2
   IS
   BEGIN
      IF where_in IS NULL
      THEN
         RETURN 'DELETE FROM PAT_NECESSITY_HIST';
      ELSE
         RETURN
            'DELETE FROM PAT_NECESSITY_HIST WHERE ' || where_in;
      END IF;
   END dyndelstr;

   FUNCTION dyncoldelstr (
      colname_in IN ALL_TAB_COLUMNS.COLUMN_NAME%TYPE)
      RETURN VARCHAR2
   IS
   BEGIN
      RETURN dyndelstr ( colname_in || ' = :value' );
   END;

   PROCEDURE del_by (
      where_clause_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyndelstr (where_clause_in);
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PAT_NECESSITY_HIST');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PAT_NECESSITY_HIST');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by;





   PROCEDURE del_by (
      where_clause_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN ' || dyndelstr (where_clause_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;' using OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PAT_NECESSITY_HIST');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PAT_NECESSITY_HIST');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by;





   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyncoldelstr (colname_in)
         USING colvalue_in;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PAT_NECESSITY_HIST');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PAT_NECESSITY_HIST');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;






   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN VARCHAR2
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr (colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
         USING IN colvalue_in, OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PAT_NECESSITY_HIST');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PAT_NECESSITY_HIST');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN DATE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyncoldelstr (colname_in)
         USING colvalue_in;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PAT_NECESSITY_HIST');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PAT_NECESSITY_HIST');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN DATE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr (colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
         USING IN colvalue_in, OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PAT_NECESSITY_HIST');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PAT_NECESSITY_HIST');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN TIMESTAMP WITH LOCAL TIME ZONE
     ,handle_error_in IN BOOLEAN := TRUE
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyncoldelstr (colname_in)
         USING colvalue_in;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PAT_NECESSITY_HIST');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PAT_NECESSITY_HIST');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN TIMESTAMP WITH LOCAL TIME ZONE
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
      )
   IS
   BEGIN
      EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr (colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
         USING IN colvalue_in, OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PAT_NECESSITY_HIST');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PAT_NECESSITY_HIST');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN NUMBER
     ,handle_error_in IN BOOLEAN := TRUE
   )
   IS
   BEGIN
      EXECUTE IMMEDIATE dyncoldelstr (colname_in)
         USING colvalue_in;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PAT_NECESSITY_HIST');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PAT_NECESSITY_HIST');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;






   PROCEDURE del_by_col (
      colname_in IN VARCHAR2,
      colvalue_in IN NUMBER
     ,handle_error_in IN BOOLEAN := TRUE
     , rows_out OUT TABLE_VARCHAR
   )
   IS
   BEGIN
     EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr (colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
         USING IN colvalue_in, OUT rows_out;
   EXCEPTION
      WHEN e_check_constraint_failure
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
        BEGIN
           get_constraint_info (l_owner, l_name);
           pk_alert_exceptions.raise_error (
              error_name_in => 'CHECK-CONSTRAINT-FAILURE'
             ,name1_in => 'OWNER'
             ,value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME'
             ,value2_in => l_name
             ,name3_in => 'TABLE_NAME'
             ,value3_in => 'PAT_NECESSITY_HIST');
        END;
        END IF;
      WHEN e_integ_constraint_failure OR e_no_parent_key OR e_child_record_found
      THEN
        IF NOT handle_error_in THEN RAISE;
        ELSE
        DECLARE
           l_owner ALL_CONSTRAINTS.OWNER%TYPE;
           l_name ALL_CONSTRAINTS.CONSTRAINT_NAME%TYPE;
           l_id PLS_INTEGER;
           l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
        BEGIN
           get_constraint_info (l_owner, l_name);
           IF SQLCODE = -2292 -- Child record found
           THEN
              l_err_name := 'CHILD-RECORD-FOUND' ;
           END IF;
           pk_alert_exceptions.register_error (
              error_name_in => l_err_name
             ,err_instance_id_out => l_id
             ,name1_in => 'OWNER', value1_in => l_owner
             ,name2_in => 'CONSTRAINT_NAME', value2_in => l_name
             ,name3_in => 'TABLE_NAME', value3_in => 'PAT_NECESSITY_HIST');
           pk_alert_exceptions.raise_error_instance (err_instance_id_in => l_id);
        END;
        END IF;
   END del_by_col;







   -- Initialize a record with default values for columns in the table.
   PROCEDURE initrec (
      pat_necessity_hist_inout IN OUT PAT_NECESSITY_HIST%ROWTYPE
   )
   IS
   BEGIN
      pat_necessity_hist_inout.ID_PAT_NECESSITY_HIST := NULL;
      pat_necessity_hist_inout.DT_REGISTER := NULL;
      pat_necessity_hist_inout.ID_PAT_NECESSITY := NULL;
      pat_necessity_hist_inout.ID_PATIENT := NULL;
      pat_necessity_hist_inout.ID_NECESSITY := NULL;
      pat_necessity_hist_inout.FLG_STATUS := NULL;
      pat_necessity_hist_inout.ID_EPISODE := NULL;
      pat_necessity_hist_inout.ID_INSTITUTION := NULL;
      pat_necessity_hist_inout.RECORD_STATUS := NULL;
      pat_necessity_hist_inout.IMPORT_CODE := NULL;
      pat_necessity_hist_inout.CREATE_USER := NULL;
      pat_necessity_hist_inout.UPDATE_USER := NULL;
      pat_necessity_hist_inout.CREATE_TIME := NULL;
      pat_necessity_hist_inout.UPDATE_TIME := NULL;
      pat_necessity_hist_inout.CREATE_INSTITUTION := NULL;
      pat_necessity_hist_inout.UPDATE_INSTITUTION := NULL;
   END initrec;

   FUNCTION initrec RETURN PAT_NECESSITY_HIST%ROWTYPE
   IS
      l_pat_necessity_hist PAT_NECESSITY_HIST%ROWTYPE;
   BEGIN
      RETURN l_pat_necessity_hist;
   END initrec;


   FUNCTION get_data_rowid(
        rows_in IN TABLE_VARCHAR
        ) RETURN PAT_NECESSITY_HIST_tc
   IS
        data PAT_NECESSITY_HIST_tc;
   BEGIN
        select * bulk collect into data from PAT_NECESSITY_HIST where rowid in (select /*+opt_estimate(table,t,scale_rows=0.0000001))*/ * from table(rows_in) t);
        return data;
        EXCEPTION
      WHEN OTHERS THEN
        pk_alert_exceptions.raise_error (
           error_name_in => 'get_data_rowid'
           );
   END get_data_rowid;


   FUNCTION get_data_rowid_pat(
        rows_in IN TABLE_VARCHAR
        ) RETURN PAT_NECESSITY_HIST_tc
   is
        PRAGMA AUTONOMOUS_TRANSACTION;
        data PAT_NECESSITY_HIST_tc;
   BEGIN
        data := get_data_rowid(rows_in);
        commit;
        return data;
        EXCEPTION
      WHEN OTHERS THEN
        pk_alert_exceptions.raise_error (
           error_name_in => 'get_data_rowid'
           );
        rollback;
    END get_data_rowid_pat;

BEGIN
   NULL;
END TS_PAT_NECESSITY_HIST;
/
