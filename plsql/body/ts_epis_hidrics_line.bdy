/*-- Last Change Revision: $Rev: 1028800 $*/
/*-- Last Change by: $Author: sofia.mendes $*/
/*-- Date of last change: $Date: 2011-06-17 08:10:51 +0100 (sex, 17 jun 2011) $*/
CREATE OR REPLACE PACKAGE BODY ts_epis_hidrics_line
/*
| Generated by or retrieved from QCGU - DO NOT MODIFY!
| QCGU - "Get it right, do it fast" - www.ToadWorld.com
| QCGU Universal ID: {657BD35E-2E48-4CD4-B14E-41420B8433B3}
| Created On: Junho 15, 2011 9:47:40
| Created By: ALERT
*/
 IS

    e_null_column_value EXCEPTION;
    PRAGMA EXCEPTION_INIT(e_null_column_value, -1400);
    --
    e_existing_fky_reference EXCEPTION;
    PRAGMA EXCEPTION_INIT(e_existing_fky_reference, -2266);
    --
    e_check_constraint_failure EXCEPTION;
    PRAGMA EXCEPTION_INIT(e_check_constraint_failure, -2290);
    --
    e_no_parent_key EXCEPTION;
    PRAGMA EXCEPTION_INIT(e_no_parent_key, -2291);
    --
    e_child_record_found EXCEPTION;
    PRAGMA EXCEPTION_INIT(e_child_record_found, -2292);
    --
    e_forall_error EXCEPTION;
    PRAGMA EXCEPTION_INIT(e_forall_error, -24381);
    --
    -- Defined for backward compatibilty.
    e_integ_constraint_failure EXCEPTION;
    PRAGMA EXCEPTION_INIT(e_integ_constraint_failure, -2291);

    -- Private utilities
    PROCEDURE get_constraint_info
    (
        owner_out OUT all_constraints.owner%TYPE,
        name_out  OUT all_constraints.constraint_name%TYPE
    ) IS
        l_errm  VARCHAR2(2000) := dbms_utility.format_error_stack;
        dotloc  PLS_INTEGER;
        leftloc PLS_INTEGER;
    BEGIN
        dotloc    := instr(l_errm, '.');
        leftloc   := instr(l_errm, '(');
        owner_out := substr(l_errm, leftloc + 1, dotloc - leftloc - 1);
        name_out  := substr(l_errm, dotloc + 1, instr(l_errm, ')') - dotloc - 1);
    END get_constraint_info;
    -- Public programs

    PROCEDURE ins
    (
        id_epis_hidrics_line_in   IN epis_hidrics_line.id_epis_hidrics_line%TYPE,
        id_epis_hidrics_in        IN epis_hidrics_line.id_epis_hidrics%TYPE DEFAULT NULL,
        id_hidrics_in             IN epis_hidrics_line.id_hidrics%TYPE DEFAULT NULL,
        id_way_in                 IN epis_hidrics_line.id_way%TYPE DEFAULT NULL,
        id_hidrics_location_in    IN epis_hidrics_line.id_hidrics_location%TYPE DEFAULT NULL,
        flg_status_in             IN epis_hidrics_line.flg_status%TYPE DEFAULT NULL,
        dt_creation_in            IN epis_hidrics_line.dt_creation%TYPE DEFAULT NULL,
        id_prof_cancel_in         IN epis_hidrics_line.id_prof_cancel%TYPE DEFAULT NULL,
        id_cancel_reason_in       IN epis_hidrics_line.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in           IN epis_hidrics_line.notes_cancel%TYPE DEFAULT NULL,
        dt_cancel_in              IN epis_hidrics_line.dt_cancel%TYPE DEFAULT NULL,
        create_user_in            IN epis_hidrics_line.create_user%TYPE DEFAULT NULL,
        create_time_in            IN epis_hidrics_line.create_time%TYPE DEFAULT NULL,
        create_institution_in     IN epis_hidrics_line.create_institution%TYPE DEFAULT NULL,
        update_user_in            IN epis_hidrics_line.update_user%TYPE DEFAULT NULL,
        update_time_in            IN epis_hidrics_line.update_time%TYPE DEFAULT NULL,
        update_institution_in     IN epis_hidrics_line.update_institution%TYPE DEFAULT NULL,
        id_prof_last_change_in    IN epis_hidrics_line.id_prof_last_change%TYPE DEFAULT NULL,
        dt_epis_hidrics_line_in   IN epis_hidrics_line.dt_epis_hidrics_line%TYPE DEFAULT NULL,
        flg_action_in             IN epis_hidrics_line.flg_action%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_way_in   IN epis_hidrics_line.id_epis_hid_ftxt_way%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_fluid_in IN epis_hidrics_line.id_epis_hid_ftxt_fluid%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_loc_in   IN epis_hidrics_line.id_epis_hid_ftxt_loc%TYPE DEFAULT NULL,
        handle_error_in           IN BOOLEAN := TRUE,
        rows_out                  OUT table_varchar
    ) IS
    BEGIN
    
        INSERT INTO epis_hidrics_line
            (id_epis_hidrics_line,
             id_epis_hidrics,
             id_hidrics,
             id_way,
             id_hidrics_location,
             flg_status,
             dt_creation,
             id_prof_cancel,
             id_cancel_reason,
             notes_cancel,
             dt_cancel,
             create_user,
             create_time,
             create_institution,
             update_user,
             update_time,
             update_institution,
             id_prof_last_change,
             dt_epis_hidrics_line,
             flg_action,
             id_epis_hid_ftxt_way,
             id_epis_hid_ftxt_fluid,
             id_epis_hid_ftxt_loc)
        VALUES
            (id_epis_hidrics_line_in,
             id_epis_hidrics_in,
             id_hidrics_in,
             id_way_in,
             id_hidrics_location_in,
             flg_status_in,
             dt_creation_in,
             id_prof_cancel_in,
             id_cancel_reason_in,
             notes_cancel_in,
             dt_cancel_in,
             create_user_in,
             create_time_in,
             create_institution_in,
             update_user_in,
             update_time_in,
             update_institution_in,
             id_prof_last_change_in,
             dt_epis_hidrics_line_in,
             flg_action_in,
             id_epis_hid_ftxt_way_in,
             id_epis_hid_ftxt_fluid_in,
             id_epis_hid_ftxt_loc_in)
        RETURNING ROWID BULK COLLECT INTO rows_out;
    EXCEPTION
        WHEN dup_val_on_index THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner           all_constraints.owner%TYPE;
                    l_name            all_constraints.constraint_name%TYPE;
                    l_err_instance_id PLS_INTEGER;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF FALSE
                    THEN
                        NULL; -- Placeholder in case no unique indexes
                    ELSE
                        pk_alert_exceptions.raise_error(error_name_in => 'DUPLICATE-VALUE',
                                                        name1_in      => 'OWNER',
                                                        value1_in     => l_owner,
                                                        name2_in      => 'CONSTRAINT_NAME',
                                                        value2_in     => l_name,
                                                        name3_in      => 'TABLE_NAME',
                                                        value3_in     => 'EPIS_HIDRICS_LINE');
                    END IF;
                END;
            END IF;
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'EPIS_HIDRICS_LINE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'EPIS_HIDRICS_LINE');
                    IF l_name = 'EHL_CR_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_CANCEL_REASON',
                                                        value_in           => id_cancel_reason_in);
                    END IF;
                    IF l_name = 'EHL_EHID_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_EPIS_HIDRICS',
                                                        value_in           => id_epis_hidrics_in);
                    END IF;
                    IF l_name = 'EHL_FTFLUID_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_EPIS_HID_FTXT_FLUID',
                                                        value_in           => id_epis_hid_ftxt_fluid_in);
                    END IF;
                    IF l_name = 'EHL_FTLOC_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_EPIS_HID_FTXT_LOC',
                                                        value_in           => id_epis_hid_ftxt_loc_in);
                    END IF;
                    IF l_name = 'EHL_FTWAY_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_EPIS_HID_FTXT_WAY',
                                                        value_in           => id_epis_hid_ftxt_way_in);
                    END IF;
                    IF l_name = 'EHL_HID_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_HIDRICS',
                                                        value_in           => id_hidrics_in);
                    END IF;
                    IF l_name = 'EHL_HID_LOC_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_HIDRICS_LOCATION',
                                                        value_in           => id_hidrics_location_in);
                    END IF;
                    IF l_name = 'EHL_HID_WAY_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_WAY',
                                                        value_in           => id_way_in);
                    END IF;
                    IF l_name = 'EHL_PROF_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_PROF_CANCEL',
                                                        value_in           => id_prof_cancel_in);
                    END IF;
                    IF l_name = 'EHL_PROF_FK2'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_PROF_LAST_CHANGE',
                                                        value_in           => id_prof_last_change_in);
                    END IF;
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
        WHEN e_null_column_value THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    v_errm    VARCHAR2(2000) := dbms_utility.format_error_stack;
                    dot1loc   INTEGER;
                    dot2loc   INTEGER;
                    parenloc  INTEGER;
                    c_owner   all_constraints.owner%TYPE;
                    c_tabname all_tables.table_name%TYPE;
                    c_colname all_tab_columns.column_name%TYPE;
                BEGIN
                    dot1loc   := instr(v_errm, '.', 1, 1);
                    dot2loc   := instr(v_errm, '.', 1, 2);
                    parenloc  := instr(v_errm, '(');
                    c_owner   := substr(v_errm, parenloc + 1, dot1loc - parenloc - 1);
                    c_tabname := substr(v_errm, dot1loc + 1, dot2loc - dot1loc - 1);
                    c_colname := substr(v_errm, dot2loc + 1, instr(v_errm, ')') - dot2loc - 1);
                
                    pk_alert_exceptions.raise_error(error_name_in => 'COLUMN-CANNOT-BE-NULL',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => c_owner,
                                                    name2_in      => 'TABLE_NAME',
                                                    value2_in     => c_tabname,
                                                    name3_in      => 'COLUMN_NAME',
                                                    value3_in     => c_colname);
                END;
            END IF;
    END ins;

    PROCEDURE ins
    (
        id_epis_hidrics_line_in   IN epis_hidrics_line.id_epis_hidrics_line%TYPE,
        id_epis_hidrics_in        IN epis_hidrics_line.id_epis_hidrics%TYPE DEFAULT NULL,
        id_hidrics_in             IN epis_hidrics_line.id_hidrics%TYPE DEFAULT NULL,
        id_way_in                 IN epis_hidrics_line.id_way%TYPE DEFAULT NULL,
        id_hidrics_location_in    IN epis_hidrics_line.id_hidrics_location%TYPE DEFAULT NULL,
        flg_status_in             IN epis_hidrics_line.flg_status%TYPE DEFAULT NULL,
        dt_creation_in            IN epis_hidrics_line.dt_creation%TYPE DEFAULT NULL,
        id_prof_cancel_in         IN epis_hidrics_line.id_prof_cancel%TYPE DEFAULT NULL,
        id_cancel_reason_in       IN epis_hidrics_line.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in           IN epis_hidrics_line.notes_cancel%TYPE DEFAULT NULL,
        dt_cancel_in              IN epis_hidrics_line.dt_cancel%TYPE DEFAULT NULL,
        create_user_in            IN epis_hidrics_line.create_user%TYPE DEFAULT NULL,
        create_time_in            IN epis_hidrics_line.create_time%TYPE DEFAULT NULL,
        create_institution_in     IN epis_hidrics_line.create_institution%TYPE DEFAULT NULL,
        update_user_in            IN epis_hidrics_line.update_user%TYPE DEFAULT NULL,
        update_time_in            IN epis_hidrics_line.update_time%TYPE DEFAULT NULL,
        update_institution_in     IN epis_hidrics_line.update_institution%TYPE DEFAULT NULL,
        id_prof_last_change_in    IN epis_hidrics_line.id_prof_last_change%TYPE DEFAULT NULL,
        dt_epis_hidrics_line_in   IN epis_hidrics_line.dt_epis_hidrics_line%TYPE DEFAULT NULL,
        flg_action_in             IN epis_hidrics_line.flg_action%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_way_in   IN epis_hidrics_line.id_epis_hid_ftxt_way%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_fluid_in IN epis_hidrics_line.id_epis_hid_ftxt_fluid%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_loc_in   IN epis_hidrics_line.id_epis_hid_ftxt_loc%TYPE DEFAULT NULL,
        handle_error_in           IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
    
        ins(id_epis_hidrics_line_in   => id_epis_hidrics_line_in,
            id_epis_hidrics_in        => id_epis_hidrics_in,
            id_hidrics_in             => id_hidrics_in,
            id_way_in                 => id_way_in,
            id_hidrics_location_in    => id_hidrics_location_in,
            flg_status_in             => flg_status_in,
            dt_creation_in            => dt_creation_in,
            id_prof_cancel_in         => id_prof_cancel_in,
            id_cancel_reason_in       => id_cancel_reason_in,
            notes_cancel_in           => notes_cancel_in,
            dt_cancel_in              => dt_cancel_in,
            create_user_in            => create_user_in,
            create_time_in            => create_time_in,
            create_institution_in     => create_institution_in,
            update_user_in            => update_user_in,
            update_time_in            => update_time_in,
            update_institution_in     => update_institution_in,
            id_prof_last_change_in    => id_prof_last_change_in,
            dt_epis_hidrics_line_in   => dt_epis_hidrics_line_in,
            flg_action_in             => flg_action_in,
            id_epis_hid_ftxt_way_in   => id_epis_hid_ftxt_way_in,
            id_epis_hid_ftxt_fluid_in => id_epis_hid_ftxt_fluid_in,
            id_epis_hid_ftxt_loc_in   => id_epis_hid_ftxt_loc_in,
            handle_error_in           => handle_error_in,
            rows_out                  => rows_out);
    END ins;

    PROCEDURE ins
    (
        rec_in          IN epis_hidrics_line%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) IS
        l_rec epis_hidrics_line%ROWTYPE := rec_in;
    BEGIN
        IF gen_pky_in
        THEN
            l_rec.id_epis_hidrics_line := next_key(sequence_in);
        END IF;
        ins(id_epis_hidrics_line_in   => l_rec.id_epis_hidrics_line,
            id_epis_hidrics_in        => l_rec.id_epis_hidrics,
            id_hidrics_in             => l_rec.id_hidrics,
            id_way_in                 => l_rec.id_way,
            id_hidrics_location_in    => l_rec.id_hidrics_location,
            flg_status_in             => l_rec.flg_status,
            dt_creation_in            => l_rec.dt_creation,
            id_prof_cancel_in         => l_rec.id_prof_cancel,
            id_cancel_reason_in       => l_rec.id_cancel_reason,
            notes_cancel_in           => l_rec.notes_cancel,
            dt_cancel_in              => l_rec.dt_cancel,
            create_user_in            => l_rec.create_user,
            create_time_in            => l_rec.create_time,
            create_institution_in     => l_rec.create_institution,
            update_user_in            => l_rec.update_user,
            update_time_in            => l_rec.update_time,
            update_institution_in     => l_rec.update_institution,
            id_prof_last_change_in    => l_rec.id_prof_last_change,
            dt_epis_hidrics_line_in   => l_rec.dt_epis_hidrics_line,
            flg_action_in             => l_rec.flg_action,
            id_epis_hid_ftxt_way_in   => l_rec.id_epis_hid_ftxt_way,
            id_epis_hid_ftxt_fluid_in => l_rec.id_epis_hid_ftxt_fluid,
            id_epis_hid_ftxt_loc_in   => l_rec.id_epis_hid_ftxt_loc,
            handle_error_in           => handle_error_in,
            rows_out                  => rows_out);
    END ins;

    PROCEDURE ins
    (
        rec_in          IN epis_hidrics_line%ROWTYPE,
        gen_pky_in      IN BOOLEAN DEFAULT FALSE,
        sequence_in     IN VARCHAR2 := NULL,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
    
        ins(rec_in          => rec_in,
            gen_pky_in      => gen_pky_in,
            sequence_in     => sequence_in,
            handle_error_in => handle_error_in,
            rows_out        => rows_out);
    
    END ins;

    FUNCTION next_key(sequence_in IN VARCHAR2 := NULL) RETURN epis_hidrics_line.id_epis_hidrics_line%TYPE
    
     IS
        retval epis_hidrics_line.id_epis_hidrics_line%TYPE;
    
    BEGIN
        IF sequence_in IS NULL
        THEN
            SELECT seq_epis_hidrics_line.nextval
              INTO retval
              FROM dual;
        ELSE
            EXECUTE IMMEDIATE 'SELECT ' || sequence_in || '.NEXTVAL FROM dual'
                INTO retval;
        END IF;
        RETURN retval;
    EXCEPTION
        WHEN OTHERS THEN
            pk_alert_exceptions.raise_error(error_name_in => 'SEQUENCE-GENERATION-FAILURE',
                                            name1_in      => 'SEQUENCE',
                                            value1_in     => nvl(sequence_in, 'seq_EPIS_HIDRICS_LINE'));
    END next_key;

    PROCEDURE ins
    (
        id_epis_hidrics_in        IN epis_hidrics_line.id_epis_hidrics%TYPE DEFAULT NULL,
        id_hidrics_in             IN epis_hidrics_line.id_hidrics%TYPE DEFAULT NULL,
        id_way_in                 IN epis_hidrics_line.id_way%TYPE DEFAULT NULL,
        id_hidrics_location_in    IN epis_hidrics_line.id_hidrics_location%TYPE DEFAULT NULL,
        flg_status_in             IN epis_hidrics_line.flg_status%TYPE DEFAULT NULL,
        dt_creation_in            IN epis_hidrics_line.dt_creation%TYPE DEFAULT NULL,
        id_prof_cancel_in         IN epis_hidrics_line.id_prof_cancel%TYPE DEFAULT NULL,
        id_cancel_reason_in       IN epis_hidrics_line.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in           IN epis_hidrics_line.notes_cancel%TYPE DEFAULT NULL,
        dt_cancel_in              IN epis_hidrics_line.dt_cancel%TYPE DEFAULT NULL,
        create_user_in            IN epis_hidrics_line.create_user%TYPE DEFAULT NULL,
        create_time_in            IN epis_hidrics_line.create_time%TYPE DEFAULT NULL,
        create_institution_in     IN epis_hidrics_line.create_institution%TYPE DEFAULT NULL,
        update_user_in            IN epis_hidrics_line.update_user%TYPE DEFAULT NULL,
        update_time_in            IN epis_hidrics_line.update_time%TYPE DEFAULT NULL,
        update_institution_in     IN epis_hidrics_line.update_institution%TYPE DEFAULT NULL,
        id_prof_last_change_in    IN epis_hidrics_line.id_prof_last_change%TYPE DEFAULT NULL,
        dt_epis_hidrics_line_in   IN epis_hidrics_line.dt_epis_hidrics_line%TYPE DEFAULT NULL,
        flg_action_in             IN epis_hidrics_line.flg_action%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_way_in   IN epis_hidrics_line.id_epis_hid_ftxt_way%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_fluid_in IN epis_hidrics_line.id_epis_hid_ftxt_fluid%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_loc_in   IN epis_hidrics_line.id_epis_hid_ftxt_loc%TYPE DEFAULT NULL,
        id_epis_hidrics_line_out  IN OUT epis_hidrics_line.id_epis_hidrics_line%TYPE,
        handle_error_in           IN BOOLEAN := TRUE,
        rows_out                  OUT table_varchar
    ) IS
        l_pky epis_hidrics_line.id_epis_hidrics_line%TYPE := next_key;
    BEGIN
        ins(id_epis_hidrics_line_in   => l_pky,
            id_epis_hidrics_in        => id_epis_hidrics_in,
            id_hidrics_in             => id_hidrics_in,
            id_way_in                 => id_way_in,
            id_hidrics_location_in    => id_hidrics_location_in,
            flg_status_in             => flg_status_in,
            dt_creation_in            => dt_creation_in,
            id_prof_cancel_in         => id_prof_cancel_in,
            id_cancel_reason_in       => id_cancel_reason_in,
            notes_cancel_in           => notes_cancel_in,
            dt_cancel_in              => dt_cancel_in,
            create_user_in            => create_user_in,
            create_time_in            => create_time_in,
            create_institution_in     => create_institution_in,
            update_user_in            => update_user_in,
            update_time_in            => update_time_in,
            update_institution_in     => update_institution_in,
            id_prof_last_change_in    => id_prof_last_change_in,
            dt_epis_hidrics_line_in   => dt_epis_hidrics_line_in,
            flg_action_in             => flg_action_in,
            id_epis_hid_ftxt_way_in   => id_epis_hid_ftxt_way_in,
            id_epis_hid_ftxt_fluid_in => id_epis_hid_ftxt_fluid_in,
            id_epis_hid_ftxt_loc_in   => id_epis_hid_ftxt_loc_in,
            handle_error_in           => handle_error_in,
            rows_out                  => rows_out);
        id_epis_hidrics_line_out := l_pky;
    END ins;

    PROCEDURE ins
    (
        id_epis_hidrics_in        IN epis_hidrics_line.id_epis_hidrics%TYPE DEFAULT NULL,
        id_hidrics_in             IN epis_hidrics_line.id_hidrics%TYPE DEFAULT NULL,
        id_way_in                 IN epis_hidrics_line.id_way%TYPE DEFAULT NULL,
        id_hidrics_location_in    IN epis_hidrics_line.id_hidrics_location%TYPE DEFAULT NULL,
        flg_status_in             IN epis_hidrics_line.flg_status%TYPE DEFAULT NULL,
        dt_creation_in            IN epis_hidrics_line.dt_creation%TYPE DEFAULT NULL,
        id_prof_cancel_in         IN epis_hidrics_line.id_prof_cancel%TYPE DEFAULT NULL,
        id_cancel_reason_in       IN epis_hidrics_line.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in           IN epis_hidrics_line.notes_cancel%TYPE DEFAULT NULL,
        dt_cancel_in              IN epis_hidrics_line.dt_cancel%TYPE DEFAULT NULL,
        create_user_in            IN epis_hidrics_line.create_user%TYPE DEFAULT NULL,
        create_time_in            IN epis_hidrics_line.create_time%TYPE DEFAULT NULL,
        create_institution_in     IN epis_hidrics_line.create_institution%TYPE DEFAULT NULL,
        update_user_in            IN epis_hidrics_line.update_user%TYPE DEFAULT NULL,
        update_time_in            IN epis_hidrics_line.update_time%TYPE DEFAULT NULL,
        update_institution_in     IN epis_hidrics_line.update_institution%TYPE DEFAULT NULL,
        id_prof_last_change_in    IN epis_hidrics_line.id_prof_last_change%TYPE DEFAULT NULL,
        dt_epis_hidrics_line_in   IN epis_hidrics_line.dt_epis_hidrics_line%TYPE DEFAULT NULL,
        flg_action_in             IN epis_hidrics_line.flg_action%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_way_in   IN epis_hidrics_line.id_epis_hid_ftxt_way%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_fluid_in IN epis_hidrics_line.id_epis_hid_ftxt_fluid%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_loc_in   IN epis_hidrics_line.id_epis_hid_ftxt_loc%TYPE DEFAULT NULL,
        id_epis_hidrics_line_out  IN OUT epis_hidrics_line.id_epis_hidrics_line%TYPE,
        handle_error_in           IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        ins(id_epis_hidrics_in        => id_epis_hidrics_in,
            id_hidrics_in             => id_hidrics_in,
            id_way_in                 => id_way_in,
            id_hidrics_location_in    => id_hidrics_location_in,
            flg_status_in             => flg_status_in,
            dt_creation_in            => dt_creation_in,
            id_prof_cancel_in         => id_prof_cancel_in,
            id_cancel_reason_in       => id_cancel_reason_in,
            notes_cancel_in           => notes_cancel_in,
            dt_cancel_in              => dt_cancel_in,
            create_user_in            => create_user_in,
            create_time_in            => create_time_in,
            create_institution_in     => create_institution_in,
            update_user_in            => update_user_in,
            update_time_in            => update_time_in,
            update_institution_in     => update_institution_in,
            id_prof_last_change_in    => id_prof_last_change_in,
            dt_epis_hidrics_line_in   => dt_epis_hidrics_line_in,
            flg_action_in             => flg_action_in,
            id_epis_hid_ftxt_way_in   => id_epis_hid_ftxt_way_in,
            id_epis_hid_ftxt_fluid_in => id_epis_hid_ftxt_fluid_in,
            id_epis_hid_ftxt_loc_in   => id_epis_hid_ftxt_loc_in,
            id_epis_hidrics_line_out  => id_epis_hidrics_line_out,
            handle_error_in           => handle_error_in,
            rows_out                  => rows_out);
    END ins;

    FUNCTION ins
    (
        id_epis_hidrics_in        IN epis_hidrics_line.id_epis_hidrics%TYPE DEFAULT NULL,
        id_hidrics_in             IN epis_hidrics_line.id_hidrics%TYPE DEFAULT NULL,
        id_way_in                 IN epis_hidrics_line.id_way%TYPE DEFAULT NULL,
        id_hidrics_location_in    IN epis_hidrics_line.id_hidrics_location%TYPE DEFAULT NULL,
        flg_status_in             IN epis_hidrics_line.flg_status%TYPE DEFAULT NULL,
        dt_creation_in            IN epis_hidrics_line.dt_creation%TYPE DEFAULT NULL,
        id_prof_cancel_in         IN epis_hidrics_line.id_prof_cancel%TYPE DEFAULT NULL,
        id_cancel_reason_in       IN epis_hidrics_line.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in           IN epis_hidrics_line.notes_cancel%TYPE DEFAULT NULL,
        dt_cancel_in              IN epis_hidrics_line.dt_cancel%TYPE DEFAULT NULL,
        create_user_in            IN epis_hidrics_line.create_user%TYPE DEFAULT NULL,
        create_time_in            IN epis_hidrics_line.create_time%TYPE DEFAULT NULL,
        create_institution_in     IN epis_hidrics_line.create_institution%TYPE DEFAULT NULL,
        update_user_in            IN epis_hidrics_line.update_user%TYPE DEFAULT NULL,
        update_time_in            IN epis_hidrics_line.update_time%TYPE DEFAULT NULL,
        update_institution_in     IN epis_hidrics_line.update_institution%TYPE DEFAULT NULL,
        id_prof_last_change_in    IN epis_hidrics_line.id_prof_last_change%TYPE DEFAULT NULL,
        dt_epis_hidrics_line_in   IN epis_hidrics_line.dt_epis_hidrics_line%TYPE DEFAULT NULL,
        flg_action_in             IN epis_hidrics_line.flg_action%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_way_in   IN epis_hidrics_line.id_epis_hid_ftxt_way%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_fluid_in IN epis_hidrics_line.id_epis_hid_ftxt_fluid%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_loc_in   IN epis_hidrics_line.id_epis_hid_ftxt_loc%TYPE DEFAULT NULL,
        handle_error_in           IN BOOLEAN := TRUE,
        rows_out                  OUT table_varchar
    ) RETURN epis_hidrics_line.id_epis_hidrics_line%TYPE IS
        l_pky epis_hidrics_line.id_epis_hidrics_line%TYPE := next_key;
    BEGIN
        ins(id_epis_hidrics_line_in   => l_pky,
            id_epis_hidrics_in        => id_epis_hidrics_in,
            id_hidrics_in             => id_hidrics_in,
            id_way_in                 => id_way_in,
            id_hidrics_location_in    => id_hidrics_location_in,
            flg_status_in             => flg_status_in,
            dt_creation_in            => dt_creation_in,
            id_prof_cancel_in         => id_prof_cancel_in,
            id_cancel_reason_in       => id_cancel_reason_in,
            notes_cancel_in           => notes_cancel_in,
            dt_cancel_in              => dt_cancel_in,
            create_user_in            => create_user_in,
            create_time_in            => create_time_in,
            create_institution_in     => create_institution_in,
            update_user_in            => update_user_in,
            update_time_in            => update_time_in,
            update_institution_in     => update_institution_in,
            id_prof_last_change_in    => id_prof_last_change_in,
            dt_epis_hidrics_line_in   => dt_epis_hidrics_line_in,
            flg_action_in             => flg_action_in,
            id_epis_hid_ftxt_way_in   => id_epis_hid_ftxt_way_in,
            id_epis_hid_ftxt_fluid_in => id_epis_hid_ftxt_fluid_in,
            id_epis_hid_ftxt_loc_in   => id_epis_hid_ftxt_loc_in,
            handle_error_in           => handle_error_in,
            rows_out                  => rows_out);
        RETURN l_pky;
    END ins;

    FUNCTION ins
    (
        id_epis_hidrics_in        IN epis_hidrics_line.id_epis_hidrics%TYPE DEFAULT NULL,
        id_hidrics_in             IN epis_hidrics_line.id_hidrics%TYPE DEFAULT NULL,
        id_way_in                 IN epis_hidrics_line.id_way%TYPE DEFAULT NULL,
        id_hidrics_location_in    IN epis_hidrics_line.id_hidrics_location%TYPE DEFAULT NULL,
        flg_status_in             IN epis_hidrics_line.flg_status%TYPE DEFAULT NULL,
        dt_creation_in            IN epis_hidrics_line.dt_creation%TYPE DEFAULT NULL,
        id_prof_cancel_in         IN epis_hidrics_line.id_prof_cancel%TYPE DEFAULT NULL,
        id_cancel_reason_in       IN epis_hidrics_line.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in           IN epis_hidrics_line.notes_cancel%TYPE DEFAULT NULL,
        dt_cancel_in              IN epis_hidrics_line.dt_cancel%TYPE DEFAULT NULL,
        create_user_in            IN epis_hidrics_line.create_user%TYPE DEFAULT NULL,
        create_time_in            IN epis_hidrics_line.create_time%TYPE DEFAULT NULL,
        create_institution_in     IN epis_hidrics_line.create_institution%TYPE DEFAULT NULL,
        update_user_in            IN epis_hidrics_line.update_user%TYPE DEFAULT NULL,
        update_time_in            IN epis_hidrics_line.update_time%TYPE DEFAULT NULL,
        update_institution_in     IN epis_hidrics_line.update_institution%TYPE DEFAULT NULL,
        id_prof_last_change_in    IN epis_hidrics_line.id_prof_last_change%TYPE DEFAULT NULL,
        dt_epis_hidrics_line_in   IN epis_hidrics_line.dt_epis_hidrics_line%TYPE DEFAULT NULL,
        flg_action_in             IN epis_hidrics_line.flg_action%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_way_in   IN epis_hidrics_line.id_epis_hid_ftxt_way%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_fluid_in IN epis_hidrics_line.id_epis_hid_ftxt_fluid%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_loc_in   IN epis_hidrics_line.id_epis_hid_ftxt_loc%TYPE DEFAULT NULL,
        handle_error_in           IN BOOLEAN := TRUE
    ) RETURN epis_hidrics_line.id_epis_hidrics_line%TYPE IS
        l_pky    epis_hidrics_line.id_epis_hidrics_line%TYPE := next_key;
        rows_out table_varchar;
    BEGIN
        ins(id_epis_hidrics_line_in   => l_pky,
            id_epis_hidrics_in        => id_epis_hidrics_in,
            id_hidrics_in             => id_hidrics_in,
            id_way_in                 => id_way_in,
            id_hidrics_location_in    => id_hidrics_location_in,
            flg_status_in             => flg_status_in,
            dt_creation_in            => dt_creation_in,
            id_prof_cancel_in         => id_prof_cancel_in,
            id_cancel_reason_in       => id_cancel_reason_in,
            notes_cancel_in           => notes_cancel_in,
            dt_cancel_in              => dt_cancel_in,
            create_user_in            => create_user_in,
            create_time_in            => create_time_in,
            create_institution_in     => create_institution_in,
            update_user_in            => update_user_in,
            update_time_in            => update_time_in,
            update_institution_in     => update_institution_in,
            id_prof_last_change_in    => id_prof_last_change_in,
            dt_epis_hidrics_line_in   => dt_epis_hidrics_line_in,
            flg_action_in             => flg_action_in,
            id_epis_hid_ftxt_way_in   => id_epis_hid_ftxt_way_in,
            id_epis_hid_ftxt_fluid_in => id_epis_hid_ftxt_fluid_in,
            id_epis_hid_ftxt_loc_in   => id_epis_hid_ftxt_loc_in,
            handle_error_in           => handle_error_in,
            rows_out                  => rows_out);
        RETURN l_pky;
    END ins;

    PROCEDURE ins
    (
        id_epis_hidrics_in        IN epis_hidrics_line.id_epis_hidrics%TYPE DEFAULT NULL,
        id_hidrics_in             IN epis_hidrics_line.id_hidrics%TYPE DEFAULT NULL,
        id_way_in                 IN epis_hidrics_line.id_way%TYPE DEFAULT NULL,
        id_hidrics_location_in    IN epis_hidrics_line.id_hidrics_location%TYPE DEFAULT NULL,
        flg_status_in             IN epis_hidrics_line.flg_status%TYPE DEFAULT NULL,
        dt_creation_in            IN epis_hidrics_line.dt_creation%TYPE DEFAULT NULL,
        id_prof_cancel_in         IN epis_hidrics_line.id_prof_cancel%TYPE DEFAULT NULL,
        id_cancel_reason_in       IN epis_hidrics_line.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in           IN epis_hidrics_line.notes_cancel%TYPE DEFAULT NULL,
        dt_cancel_in              IN epis_hidrics_line.dt_cancel%TYPE DEFAULT NULL,
        create_user_in            IN epis_hidrics_line.create_user%TYPE DEFAULT NULL,
        create_time_in            IN epis_hidrics_line.create_time%TYPE DEFAULT NULL,
        create_institution_in     IN epis_hidrics_line.create_institution%TYPE DEFAULT NULL,
        update_user_in            IN epis_hidrics_line.update_user%TYPE DEFAULT NULL,
        update_time_in            IN epis_hidrics_line.update_time%TYPE DEFAULT NULL,
        update_institution_in     IN epis_hidrics_line.update_institution%TYPE DEFAULT NULL,
        id_prof_last_change_in    IN epis_hidrics_line.id_prof_last_change%TYPE DEFAULT NULL,
        dt_epis_hidrics_line_in   IN epis_hidrics_line.dt_epis_hidrics_line%TYPE DEFAULT NULL,
        flg_action_in             IN epis_hidrics_line.flg_action%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_way_in   IN epis_hidrics_line.id_epis_hid_ftxt_way%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_fluid_in IN epis_hidrics_line.id_epis_hid_ftxt_fluid%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_loc_in   IN epis_hidrics_line.id_epis_hid_ftxt_loc%TYPE DEFAULT NULL,
        handle_error_in           IN BOOLEAN := TRUE,
        rows_out                  OUT table_varchar
    ) IS
        l_pky epis_hidrics_line.id_epis_hidrics_line%TYPE := next_key;
    BEGIN
        ins(id_epis_hidrics_line_in   => l_pky,
            id_epis_hidrics_in        => id_epis_hidrics_in,
            id_hidrics_in             => id_hidrics_in,
            id_way_in                 => id_way_in,
            id_hidrics_location_in    => id_hidrics_location_in,
            flg_status_in             => flg_status_in,
            dt_creation_in            => dt_creation_in,
            id_prof_cancel_in         => id_prof_cancel_in,
            id_cancel_reason_in       => id_cancel_reason_in,
            notes_cancel_in           => notes_cancel_in,
            dt_cancel_in              => dt_cancel_in,
            create_user_in            => create_user_in,
            create_time_in            => create_time_in,
            create_institution_in     => create_institution_in,
            update_user_in            => update_user_in,
            update_time_in            => update_time_in,
            update_institution_in     => update_institution_in,
            id_prof_last_change_in    => id_prof_last_change_in,
            dt_epis_hidrics_line_in   => dt_epis_hidrics_line_in,
            flg_action_in             => flg_action_in,
            id_epis_hid_ftxt_way_in   => id_epis_hid_ftxt_way_in,
            id_epis_hid_ftxt_fluid_in => id_epis_hid_ftxt_fluid_in,
            id_epis_hid_ftxt_loc_in   => id_epis_hid_ftxt_loc_in,
            handle_error_in           => handle_error_in,
            rows_out                  => rows_out);
    END ins;

    PROCEDURE ins
    (
        id_epis_hidrics_in        IN epis_hidrics_line.id_epis_hidrics%TYPE DEFAULT NULL,
        id_hidrics_in             IN epis_hidrics_line.id_hidrics%TYPE DEFAULT NULL,
        id_way_in                 IN epis_hidrics_line.id_way%TYPE DEFAULT NULL,
        id_hidrics_location_in    IN epis_hidrics_line.id_hidrics_location%TYPE DEFAULT NULL,
        flg_status_in             IN epis_hidrics_line.flg_status%TYPE DEFAULT NULL,
        dt_creation_in            IN epis_hidrics_line.dt_creation%TYPE DEFAULT NULL,
        id_prof_cancel_in         IN epis_hidrics_line.id_prof_cancel%TYPE DEFAULT NULL,
        id_cancel_reason_in       IN epis_hidrics_line.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in           IN epis_hidrics_line.notes_cancel%TYPE DEFAULT NULL,
        dt_cancel_in              IN epis_hidrics_line.dt_cancel%TYPE DEFAULT NULL,
        create_user_in            IN epis_hidrics_line.create_user%TYPE DEFAULT NULL,
        create_time_in            IN epis_hidrics_line.create_time%TYPE DEFAULT NULL,
        create_institution_in     IN epis_hidrics_line.create_institution%TYPE DEFAULT NULL,
        update_user_in            IN epis_hidrics_line.update_user%TYPE DEFAULT NULL,
        update_time_in            IN epis_hidrics_line.update_time%TYPE DEFAULT NULL,
        update_institution_in     IN epis_hidrics_line.update_institution%TYPE DEFAULT NULL,
        id_prof_last_change_in    IN epis_hidrics_line.id_prof_last_change%TYPE DEFAULT NULL,
        dt_epis_hidrics_line_in   IN epis_hidrics_line.dt_epis_hidrics_line%TYPE DEFAULT NULL,
        flg_action_in             IN epis_hidrics_line.flg_action%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_way_in   IN epis_hidrics_line.id_epis_hid_ftxt_way%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_fluid_in IN epis_hidrics_line.id_epis_hid_ftxt_fluid%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_loc_in   IN epis_hidrics_line.id_epis_hid_ftxt_loc%TYPE DEFAULT NULL,
        handle_error_in           IN BOOLEAN := TRUE
    ) IS
        l_pky    epis_hidrics_line.id_epis_hidrics_line%TYPE := next_key;
        rows_out table_varchar;
    BEGIN
        ins(id_epis_hidrics_line_in   => l_pky,
            id_epis_hidrics_in        => id_epis_hidrics_in,
            id_hidrics_in             => id_hidrics_in,
            id_way_in                 => id_way_in,
            id_hidrics_location_in    => id_hidrics_location_in,
            flg_status_in             => flg_status_in,
            dt_creation_in            => dt_creation_in,
            id_prof_cancel_in         => id_prof_cancel_in,
            id_cancel_reason_in       => id_cancel_reason_in,
            notes_cancel_in           => notes_cancel_in,
            dt_cancel_in              => dt_cancel_in,
            create_user_in            => create_user_in,
            create_time_in            => create_time_in,
            create_institution_in     => create_institution_in,
            update_user_in            => update_user_in,
            update_time_in            => update_time_in,
            update_institution_in     => update_institution_in,
            id_prof_last_change_in    => id_prof_last_change_in,
            dt_epis_hidrics_line_in   => dt_epis_hidrics_line_in,
            flg_action_in             => flg_action_in,
            id_epis_hid_ftxt_way_in   => id_epis_hid_ftxt_way_in,
            id_epis_hid_ftxt_fluid_in => id_epis_hid_ftxt_fluid_in,
            id_epis_hid_ftxt_loc_in   => id_epis_hid_ftxt_loc_in,
            handle_error_in           => handle_error_in,
            rows_out                  => rows_out);
    END ins;

    PROCEDURE ins
    (
        rows_in         IN epis_hidrics_line_tc,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) IS
    BEGIN
        IF rows_in.count = 0
        THEN
            NULL;
        ELSE
            FORALL indx IN rows_in.first .. rows_in.last SAVE EXCEPTIONS
                INSERT INTO epis_hidrics_line
                VALUES rows_in
                    (indx)
                RETURNING ROWID BULK COLLECT INTO rows_out;
        END IF;
    EXCEPTION
        WHEN e_forall_error THEN
            -- In Oracle9i and above, SAVE EXCEPTIONS will direct control
            -- here if any error occurs. We can then save all the error
            -- information out to the error instance.
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                <<bulk_handler>>
                DECLARE
                    l_err_instance_id NUMBER;
                BEGIN
                    -- For each error, write to the log.
                    FOR indx IN 1 .. SQL%bulk_exceptions.count
                    LOOP
                        pk_alert_exceptions.register_error(error_name_in       => 'FORALL-INSERT-FAILURE',
                                                           err_instance_id_out => l_err_instance_id);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'BINDING_ROW_' || indx,
                                                        value_in           => SQL%BULK_EXCEPTIONS(indx).error_index,
                                                        validate_in        => FALSE);
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'ERROR_AT_ROW_' || indx,
                                                        value_in           => SQL%BULK_EXCEPTIONS(indx).error_code,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'ID_EPIS_HIDRICS_LINE _' || indx,
                                                        value_in           => rows_in(indx).id_epis_hidrics_line,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'ID_EPIS_HIDRICS _' || indx,
                                                        value_in           => rows_in(indx).id_epis_hidrics,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'ID_HIDRICS _' || indx,
                                                        value_in           => rows_in(indx).id_hidrics,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'ID_WAY _' || indx,
                                                        value_in           => rows_in(indx).id_way,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'ID_HIDRICS_LOCATION _' || indx,
                                                        value_in           => rows_in(indx).id_hidrics_location,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'FLG_STATUS _' || indx,
                                                        value_in           => rows_in(indx).flg_status,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'DT_CREATION _' || indx,
                                                        value_in           => rows_in(indx).dt_creation,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'ID_PROF_CANCEL _' || indx,
                                                        value_in           => rows_in(indx).id_prof_cancel,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'ID_CANCEL_REASON _' || indx,
                                                        value_in           => rows_in(indx).id_cancel_reason,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'NOTES_CANCEL _' || indx,
                                                        value_in           => rows_in(indx).notes_cancel,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'DT_CANCEL _' || indx,
                                                        value_in           => rows_in(indx).dt_cancel,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'CREATE_USER _' || indx,
                                                        value_in           => rows_in(indx).create_user,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'CREATE_TIME _' || indx,
                                                        value_in           => rows_in(indx).create_time,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'CREATE_INSTITUTION _' || indx,
                                                        value_in           => rows_in(indx).create_institution,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'UPDATE_USER _' || indx,
                                                        value_in           => rows_in(indx).update_user,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'UPDATE_TIME _' || indx,
                                                        value_in           => rows_in(indx).update_time,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'UPDATE_INSTITUTION _' || indx,
                                                        value_in           => rows_in(indx).update_institution,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'ID_PROF_LAST_CHANGE _' || indx,
                                                        value_in           => rows_in(indx).id_prof_last_change,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'DT_EPIS_HIDRICS_LINE _' || indx,
                                                        value_in           => rows_in(indx).dt_epis_hidrics_line,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'FLG_ACTION _' || indx,
                                                        value_in           => rows_in(indx).flg_action,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'ID_EPIS_HID_FTXT_WAY _' || indx,
                                                        value_in           => rows_in(indx).id_epis_hid_ftxt_way,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'ID_EPIS_HID_FTXT_FLUID _' || indx,
                                                        value_in           => rows_in(indx).id_epis_hid_ftxt_fluid,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.add_context(err_instance_id_in => l_err_instance_id,
                                                        name_in            => 'ID_EPIS_HID_FTXT_LOC _' || indx,
                                                        value_in           => rows_in(indx).id_epis_hid_ftxt_loc,
                                                        validate_in        => FALSE);
                    
                        pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_err_instance_id);
                    END LOOP;
                END bulk_handler;
            END IF;
        WHEN OTHERS THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                pk_alert_exceptions.raise_error(error_name_in => 'FORALL-INSERT-FAILURE',
                                                name1_in      => 'TABLE_NAME',
                                                value1_in     => 'EPIS_HIDRICS_LINE',
                                                name2_in      => 'ROW_COUNT',
                                                value2_in     => rows_in.count);
            END IF;
    END ins;

    PROCEDURE ins
    (
        rows_in         IN epis_hidrics_line_tc,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        ins(rows_in => rows_in, handle_error_in => handle_error_in, rows_out => rows_out);
    END ins;

    PROCEDURE upd
    (
        id_epis_hidrics_line_in    IN epis_hidrics_line.id_epis_hidrics_line%TYPE,
        id_epis_hidrics_in         IN epis_hidrics_line.id_epis_hidrics%TYPE DEFAULT NULL,
        id_epis_hidrics_nin        IN BOOLEAN := TRUE,
        id_hidrics_in              IN epis_hidrics_line.id_hidrics%TYPE DEFAULT NULL,
        id_hidrics_nin             IN BOOLEAN := TRUE,
        id_way_in                  IN epis_hidrics_line.id_way%TYPE DEFAULT NULL,
        id_way_nin                 IN BOOLEAN := TRUE,
        id_hidrics_location_in     IN epis_hidrics_line.id_hidrics_location%TYPE DEFAULT NULL,
        id_hidrics_location_nin    IN BOOLEAN := TRUE,
        flg_status_in              IN epis_hidrics_line.flg_status%TYPE DEFAULT NULL,
        flg_status_nin             IN BOOLEAN := TRUE,
        dt_creation_in             IN epis_hidrics_line.dt_creation%TYPE DEFAULT NULL,
        dt_creation_nin            IN BOOLEAN := TRUE,
        id_prof_cancel_in          IN epis_hidrics_line.id_prof_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_nin         IN BOOLEAN := TRUE,
        id_cancel_reason_in        IN epis_hidrics_line.id_cancel_reason%TYPE DEFAULT NULL,
        id_cancel_reason_nin       IN BOOLEAN := TRUE,
        notes_cancel_in            IN epis_hidrics_line.notes_cancel%TYPE DEFAULT NULL,
        notes_cancel_nin           IN BOOLEAN := TRUE,
        dt_cancel_in               IN epis_hidrics_line.dt_cancel%TYPE DEFAULT NULL,
        dt_cancel_nin              IN BOOLEAN := TRUE,
        create_user_in             IN epis_hidrics_line.create_user%TYPE DEFAULT NULL,
        create_user_nin            IN BOOLEAN := TRUE,
        create_time_in             IN epis_hidrics_line.create_time%TYPE DEFAULT NULL,
        create_time_nin            IN BOOLEAN := TRUE,
        create_institution_in      IN epis_hidrics_line.create_institution%TYPE DEFAULT NULL,
        create_institution_nin     IN BOOLEAN := TRUE,
        update_user_in             IN epis_hidrics_line.update_user%TYPE DEFAULT NULL,
        update_user_nin            IN BOOLEAN := TRUE,
        update_time_in             IN epis_hidrics_line.update_time%TYPE DEFAULT NULL,
        update_time_nin            IN BOOLEAN := TRUE,
        update_institution_in      IN epis_hidrics_line.update_institution%TYPE DEFAULT NULL,
        update_institution_nin     IN BOOLEAN := TRUE,
        id_prof_last_change_in     IN epis_hidrics_line.id_prof_last_change%TYPE DEFAULT NULL,
        id_prof_last_change_nin    IN BOOLEAN := TRUE,
        dt_epis_hidrics_line_in    IN epis_hidrics_line.dt_epis_hidrics_line%TYPE DEFAULT NULL,
        dt_epis_hidrics_line_nin   IN BOOLEAN := TRUE,
        flg_action_in              IN epis_hidrics_line.flg_action%TYPE DEFAULT NULL,
        flg_action_nin             IN BOOLEAN := TRUE,
        id_epis_hid_ftxt_way_in    IN epis_hidrics_line.id_epis_hid_ftxt_way%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_way_nin   IN BOOLEAN := TRUE,
        id_epis_hid_ftxt_fluid_in  IN epis_hidrics_line.id_epis_hid_ftxt_fluid%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_fluid_nin IN BOOLEAN := TRUE,
        id_epis_hid_ftxt_loc_in    IN epis_hidrics_line.id_epis_hid_ftxt_loc%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_loc_nin   IN BOOLEAN := TRUE,
        handle_error_in            IN BOOLEAN := TRUE,
        rows_out                   IN OUT table_varchar
    ) IS
        l_rows_out                 table_varchar;
        l_id_epis_hidrics_n        NUMBER(1);
        l_id_hidrics_n             NUMBER(1);
        l_id_way_n                 NUMBER(1);
        l_id_hidrics_location_n    NUMBER(1);
        l_flg_status_n             NUMBER(1);
        l_dt_creation_n            NUMBER(1);
        l_id_prof_cancel_n         NUMBER(1);
        l_id_cancel_reason_n       NUMBER(1);
        l_notes_cancel_n           NUMBER(1);
        l_dt_cancel_n              NUMBER(1);
        l_create_user_n            NUMBER(1);
        l_create_time_n            NUMBER(1);
        l_create_institution_n     NUMBER(1);
        l_update_user_n            NUMBER(1);
        l_update_time_n            NUMBER(1);
        l_update_institution_n     NUMBER(1);
        l_id_prof_last_change_n    NUMBER(1);
        l_dt_epis_hidrics_line_n   NUMBER(1);
        l_flg_action_n             NUMBER(1);
        l_id_epis_hid_ftxt_way_n   NUMBER(1);
        l_id_epis_hid_ftxt_fluid_n NUMBER(1);
        l_id_epis_hid_ftxt_loc_n   NUMBER(1);
    BEGIN
    
        l_id_epis_hidrics_n        := sys.diutil.bool_to_int(id_epis_hidrics_nin);
        l_id_hidrics_n             := sys.diutil.bool_to_int(id_hidrics_nin);
        l_id_way_n                 := sys.diutil.bool_to_int(id_way_nin);
        l_id_hidrics_location_n    := sys.diutil.bool_to_int(id_hidrics_location_nin);
        l_flg_status_n             := sys.diutil.bool_to_int(flg_status_nin);
        l_dt_creation_n            := sys.diutil.bool_to_int(dt_creation_nin);
        l_id_prof_cancel_n         := sys.diutil.bool_to_int(id_prof_cancel_nin);
        l_id_cancel_reason_n       := sys.diutil.bool_to_int(id_cancel_reason_nin);
        l_notes_cancel_n           := sys.diutil.bool_to_int(notes_cancel_nin);
        l_dt_cancel_n              := sys.diutil.bool_to_int(dt_cancel_nin);
        l_create_user_n            := sys.diutil.bool_to_int(create_user_nin);
        l_create_time_n            := sys.diutil.bool_to_int(create_time_nin);
        l_create_institution_n     := sys.diutil.bool_to_int(create_institution_nin);
        l_update_user_n            := sys.diutil.bool_to_int(update_user_nin);
        l_update_time_n            := sys.diutil.bool_to_int(update_time_nin);
        l_update_institution_n     := sys.diutil.bool_to_int(update_institution_nin);
        l_id_prof_last_change_n    := sys.diutil.bool_to_int(id_prof_last_change_nin);
        l_dt_epis_hidrics_line_n   := sys.diutil.bool_to_int(dt_epis_hidrics_line_nin);
        l_flg_action_n             := sys.diutil.bool_to_int(flg_action_nin);
        l_id_epis_hid_ftxt_way_n   := sys.diutil.bool_to_int(id_epis_hid_ftxt_way_nin);
        l_id_epis_hid_ftxt_fluid_n := sys.diutil.bool_to_int(id_epis_hid_ftxt_fluid_nin);
        l_id_epis_hid_ftxt_loc_n   := sys.diutil.bool_to_int(id_epis_hid_ftxt_loc_nin);
    
        UPDATE epis_hidrics_line
           SET id_epis_hidrics        = decode(l_id_epis_hidrics_n,
                                               0,
                                               id_epis_hidrics_in,
                                               nvl(id_epis_hidrics_in, id_epis_hidrics)),
               id_hidrics             = decode(l_id_hidrics_n, 0, id_hidrics_in, nvl(id_hidrics_in, id_hidrics)),
               id_way                 = decode(l_id_way_n, 0, id_way_in, nvl(id_way_in, id_way)),
               id_hidrics_location    = decode(l_id_hidrics_location_n,
                                               0,
                                               id_hidrics_location_in,
                                               nvl(id_hidrics_location_in, id_hidrics_location)),
               flg_status             = decode(l_flg_status_n, 0, flg_status_in, nvl(flg_status_in, flg_status)),
               dt_creation            = decode(l_dt_creation_n, 0, dt_creation_in, nvl(dt_creation_in, dt_creation)),
               id_prof_cancel         = decode(l_id_prof_cancel_n,
                                               0,
                                               id_prof_cancel_in,
                                               nvl(id_prof_cancel_in, id_prof_cancel)),
               id_cancel_reason       = decode(l_id_cancel_reason_n,
                                               0,
                                               id_cancel_reason_in,
                                               nvl(id_cancel_reason_in, id_cancel_reason)),
               notes_cancel           = decode(l_notes_cancel_n, 0, notes_cancel_in, nvl(notes_cancel_in, notes_cancel)),
               dt_cancel              = decode(l_dt_cancel_n, 0, dt_cancel_in, nvl(dt_cancel_in, dt_cancel)),
               create_user            = decode(l_create_user_n, 0, create_user_in, nvl(create_user_in, create_user)),
               create_time            = decode(l_create_time_n, 0, create_time_in, nvl(create_time_in, create_time)),
               create_institution     = decode(l_create_institution_n,
                                               0,
                                               create_institution_in,
                                               nvl(create_institution_in, create_institution)),
               update_user            = decode(l_update_user_n, 0, update_user_in, nvl(update_user_in, update_user)),
               update_time            = decode(l_update_time_n, 0, update_time_in, nvl(update_time_in, update_time)),
               update_institution     = decode(l_update_institution_n,
                                               0,
                                               update_institution_in,
                                               nvl(update_institution_in, update_institution)),
               id_prof_last_change    = decode(l_id_prof_last_change_n,
                                               0,
                                               id_prof_last_change_in,
                                               nvl(id_prof_last_change_in, id_prof_last_change)),
               dt_epis_hidrics_line   = decode(l_dt_epis_hidrics_line_n,
                                               0,
                                               dt_epis_hidrics_line_in,
                                               nvl(dt_epis_hidrics_line_in, dt_epis_hidrics_line)),
               flg_action             = decode(l_flg_action_n, 0, flg_action_in, nvl(flg_action_in, flg_action)),
               id_epis_hid_ftxt_way   = decode(l_id_epis_hid_ftxt_way_n,
                                               0,
                                               id_epis_hid_ftxt_way_in,
                                               nvl(id_epis_hid_ftxt_way_in, id_epis_hid_ftxt_way)),
               id_epis_hid_ftxt_fluid = decode(l_id_epis_hid_ftxt_fluid_n,
                                               0,
                                               id_epis_hid_ftxt_fluid_in,
                                               nvl(id_epis_hid_ftxt_fluid_in, id_epis_hid_ftxt_fluid)),
               id_epis_hid_ftxt_loc   = decode(l_id_epis_hid_ftxt_loc_n,
                                               0,
                                               id_epis_hid_ftxt_loc_in,
                                               nvl(id_epis_hid_ftxt_loc_in, id_epis_hid_ftxt_loc))
         WHERE id_epis_hidrics_line = id_epis_hidrics_line_in
        RETURNING ROWID BULK COLLECT INTO l_rows_out;
    
        IF (rows_out IS NULL)
        THEN
            rows_out := table_varchar();
        END IF;
    
        rows_out := rows_out MULTISET UNION DISTINCT l_rows_out;
    
    EXCEPTION
        WHEN dup_val_on_index THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner           all_constraints.owner%TYPE;
                    l_name            all_constraints.constraint_name%TYPE;
                    l_err_instance_id PLS_INTEGER;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF FALSE
                    THEN
                        NULL; -- Placeholder in case no unique indexes
                    ELSE
                        pk_alert_exceptions.raise_error(error_name_in => 'DUPLICATE-VALUE',
                                                        name1_in      => 'OWNER',
                                                        value1_in     => l_owner,
                                                        name2_in      => 'CONSTRAINT_NAME',
                                                        value2_in     => l_name,
                                                        name3_in      => 'TABLE_NAME',
                                                        value3_in     => 'EPIS_HIDRICS_LINE');
                    END IF;
                END;
            END IF;
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'EPIS_HIDRICS_LINE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'EPIS_HIDRICS_LINE');
                    IF l_name = 'EHL_CR_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_CANCEL_REASON',
                                                        value_in           => id_cancel_reason_in);
                    END IF;
                    IF l_name = 'EHL_EHID_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_EPIS_HIDRICS',
                                                        value_in           => id_epis_hidrics_in);
                    END IF;
                    IF l_name = 'EHL_FTFLUID_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_EPIS_HID_FTXT_FLUID',
                                                        value_in           => id_epis_hid_ftxt_fluid_in);
                    END IF;
                    IF l_name = 'EHL_FTLOC_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_EPIS_HID_FTXT_LOC',
                                                        value_in           => id_epis_hid_ftxt_loc_in);
                    END IF;
                    IF l_name = 'EHL_FTWAY_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_EPIS_HID_FTXT_WAY',
                                                        value_in           => id_epis_hid_ftxt_way_in);
                    END IF;
                    IF l_name = 'EHL_HID_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_HIDRICS',
                                                        value_in           => id_hidrics_in);
                    END IF;
                    IF l_name = 'EHL_HID_LOC_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_HIDRICS_LOCATION',
                                                        value_in           => id_hidrics_location_in);
                    END IF;
                    IF l_name = 'EHL_HID_WAY_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_WAY',
                                                        value_in           => id_way_in);
                    END IF;
                    IF l_name = 'EHL_PROF_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_PROF_CANCEL',
                                                        value_in           => id_prof_cancel_in);
                    END IF;
                    IF l_name = 'EHL_PROF_FK2'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_PROF_LAST_CHANGE',
                                                        value_in           => id_prof_last_change_in);
                    END IF;
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
        WHEN e_null_column_value THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    v_errm    VARCHAR2(2000) := dbms_utility.format_error_stack;
                    dot1loc   INTEGER;
                    dot2loc   INTEGER;
                    parenloc  INTEGER;
                    c_owner   all_constraints.owner%TYPE;
                    c_tabname all_tables.table_name%TYPE;
                    c_colname all_tab_columns.column_name%TYPE;
                BEGIN
                    dot1loc   := instr(v_errm, '.', 1, 1);
                    dot2loc   := instr(v_errm, '.', 1, 2);
                    parenloc  := instr(v_errm, '(');
                    c_owner   := substr(v_errm, parenloc + 1, dot1loc - parenloc - 1);
                    c_tabname := substr(v_errm, dot1loc + 1, dot2loc - dot1loc - 1);
                    c_colname := substr(v_errm, dot2loc + 1, instr(v_errm, ')') - dot2loc - 1);
                
                    pk_alert_exceptions.raise_error(error_name_in => 'COLUMN-CANNOT-BE-NULL',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => c_owner,
                                                    name2_in      => 'TABLE_NAME',
                                                    value2_in     => c_tabname,
                                                    name3_in      => 'COLUMN_NAME',
                                                    value3_in     => c_colname);
                END;
            END IF;
    END upd;

    PROCEDURE upd
    (
        id_epis_hidrics_line_in    IN epis_hidrics_line.id_epis_hidrics_line%TYPE,
        id_epis_hidrics_in         IN epis_hidrics_line.id_epis_hidrics%TYPE DEFAULT NULL,
        id_epis_hidrics_nin        IN BOOLEAN := TRUE,
        id_hidrics_in              IN epis_hidrics_line.id_hidrics%TYPE DEFAULT NULL,
        id_hidrics_nin             IN BOOLEAN := TRUE,
        id_way_in                  IN epis_hidrics_line.id_way%TYPE DEFAULT NULL,
        id_way_nin                 IN BOOLEAN := TRUE,
        id_hidrics_location_in     IN epis_hidrics_line.id_hidrics_location%TYPE DEFAULT NULL,
        id_hidrics_location_nin    IN BOOLEAN := TRUE,
        flg_status_in              IN epis_hidrics_line.flg_status%TYPE DEFAULT NULL,
        flg_status_nin             IN BOOLEAN := TRUE,
        dt_creation_in             IN epis_hidrics_line.dt_creation%TYPE DEFAULT NULL,
        dt_creation_nin            IN BOOLEAN := TRUE,
        id_prof_cancel_in          IN epis_hidrics_line.id_prof_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_nin         IN BOOLEAN := TRUE,
        id_cancel_reason_in        IN epis_hidrics_line.id_cancel_reason%TYPE DEFAULT NULL,
        id_cancel_reason_nin       IN BOOLEAN := TRUE,
        notes_cancel_in            IN epis_hidrics_line.notes_cancel%TYPE DEFAULT NULL,
        notes_cancel_nin           IN BOOLEAN := TRUE,
        dt_cancel_in               IN epis_hidrics_line.dt_cancel%TYPE DEFAULT NULL,
        dt_cancel_nin              IN BOOLEAN := TRUE,
        create_user_in             IN epis_hidrics_line.create_user%TYPE DEFAULT NULL,
        create_user_nin            IN BOOLEAN := TRUE,
        create_time_in             IN epis_hidrics_line.create_time%TYPE DEFAULT NULL,
        create_time_nin            IN BOOLEAN := TRUE,
        create_institution_in      IN epis_hidrics_line.create_institution%TYPE DEFAULT NULL,
        create_institution_nin     IN BOOLEAN := TRUE,
        update_user_in             IN epis_hidrics_line.update_user%TYPE DEFAULT NULL,
        update_user_nin            IN BOOLEAN := TRUE,
        update_time_in             IN epis_hidrics_line.update_time%TYPE DEFAULT NULL,
        update_time_nin            IN BOOLEAN := TRUE,
        update_institution_in      IN epis_hidrics_line.update_institution%TYPE DEFAULT NULL,
        update_institution_nin     IN BOOLEAN := TRUE,
        id_prof_last_change_in     IN epis_hidrics_line.id_prof_last_change%TYPE DEFAULT NULL,
        id_prof_last_change_nin    IN BOOLEAN := TRUE,
        dt_epis_hidrics_line_in    IN epis_hidrics_line.dt_epis_hidrics_line%TYPE DEFAULT NULL,
        dt_epis_hidrics_line_nin   IN BOOLEAN := TRUE,
        flg_action_in              IN epis_hidrics_line.flg_action%TYPE DEFAULT NULL,
        flg_action_nin             IN BOOLEAN := TRUE,
        id_epis_hid_ftxt_way_in    IN epis_hidrics_line.id_epis_hid_ftxt_way%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_way_nin   IN BOOLEAN := TRUE,
        id_epis_hid_ftxt_fluid_in  IN epis_hidrics_line.id_epis_hid_ftxt_fluid%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_fluid_nin IN BOOLEAN := TRUE,
        id_epis_hid_ftxt_loc_in    IN epis_hidrics_line.id_epis_hid_ftxt_loc%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_loc_nin   IN BOOLEAN := TRUE,
        handle_error_in            IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        upd(id_epis_hidrics_line_in    => id_epis_hidrics_line_in,
            id_epis_hidrics_in         => id_epis_hidrics_in,
            id_epis_hidrics_nin        => id_epis_hidrics_nin,
            id_hidrics_in              => id_hidrics_in,
            id_hidrics_nin             => id_hidrics_nin,
            id_way_in                  => id_way_in,
            id_way_nin                 => id_way_nin,
            id_hidrics_location_in     => id_hidrics_location_in,
            id_hidrics_location_nin    => id_hidrics_location_nin,
            flg_status_in              => flg_status_in,
            flg_status_nin             => flg_status_nin,
            dt_creation_in             => dt_creation_in,
            dt_creation_nin            => dt_creation_nin,
            id_prof_cancel_in          => id_prof_cancel_in,
            id_prof_cancel_nin         => id_prof_cancel_nin,
            id_cancel_reason_in        => id_cancel_reason_in,
            id_cancel_reason_nin       => id_cancel_reason_nin,
            notes_cancel_in            => notes_cancel_in,
            notes_cancel_nin           => notes_cancel_nin,
            dt_cancel_in               => dt_cancel_in,
            dt_cancel_nin              => dt_cancel_nin,
            create_user_in             => create_user_in,
            create_user_nin            => create_user_nin,
            create_time_in             => create_time_in,
            create_time_nin            => create_time_nin,
            create_institution_in      => create_institution_in,
            create_institution_nin     => create_institution_nin,
            update_user_in             => update_user_in,
            update_user_nin            => update_user_nin,
            update_time_in             => update_time_in,
            update_time_nin            => update_time_nin,
            update_institution_in      => update_institution_in,
            update_institution_nin     => update_institution_nin,
            id_prof_last_change_in     => id_prof_last_change_in,
            id_prof_last_change_nin    => id_prof_last_change_nin,
            dt_epis_hidrics_line_in    => dt_epis_hidrics_line_in,
            dt_epis_hidrics_line_nin   => dt_epis_hidrics_line_nin,
            flg_action_in              => flg_action_in,
            flg_action_nin             => flg_action_nin,
            id_epis_hid_ftxt_way_in    => id_epis_hid_ftxt_way_in,
            id_epis_hid_ftxt_way_nin   => id_epis_hid_ftxt_way_nin,
            id_epis_hid_ftxt_fluid_in  => id_epis_hid_ftxt_fluid_in,
            id_epis_hid_ftxt_fluid_nin => id_epis_hid_ftxt_fluid_nin,
            id_epis_hid_ftxt_loc_in    => id_epis_hid_ftxt_loc_in,
            id_epis_hid_ftxt_loc_nin   => id_epis_hid_ftxt_loc_nin,
            handle_error_in            => handle_error_in,
            rows_out                   => rows_out);
    END upd;

    PROCEDURE upd
    (
        id_epis_hidrics_in         IN epis_hidrics_line.id_epis_hidrics%TYPE DEFAULT NULL,
        id_epis_hidrics_nin        IN BOOLEAN := TRUE,
        id_hidrics_in              IN epis_hidrics_line.id_hidrics%TYPE DEFAULT NULL,
        id_hidrics_nin             IN BOOLEAN := TRUE,
        id_way_in                  IN epis_hidrics_line.id_way%TYPE DEFAULT NULL,
        id_way_nin                 IN BOOLEAN := TRUE,
        id_hidrics_location_in     IN epis_hidrics_line.id_hidrics_location%TYPE DEFAULT NULL,
        id_hidrics_location_nin    IN BOOLEAN := TRUE,
        flg_status_in              IN epis_hidrics_line.flg_status%TYPE DEFAULT NULL,
        flg_status_nin             IN BOOLEAN := TRUE,
        dt_creation_in             IN epis_hidrics_line.dt_creation%TYPE DEFAULT NULL,
        dt_creation_nin            IN BOOLEAN := TRUE,
        id_prof_cancel_in          IN epis_hidrics_line.id_prof_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_nin         IN BOOLEAN := TRUE,
        id_cancel_reason_in        IN epis_hidrics_line.id_cancel_reason%TYPE DEFAULT NULL,
        id_cancel_reason_nin       IN BOOLEAN := TRUE,
        notes_cancel_in            IN epis_hidrics_line.notes_cancel%TYPE DEFAULT NULL,
        notes_cancel_nin           IN BOOLEAN := TRUE,
        dt_cancel_in               IN epis_hidrics_line.dt_cancel%TYPE DEFAULT NULL,
        dt_cancel_nin              IN BOOLEAN := TRUE,
        create_user_in             IN epis_hidrics_line.create_user%TYPE DEFAULT NULL,
        create_user_nin            IN BOOLEAN := TRUE,
        create_time_in             IN epis_hidrics_line.create_time%TYPE DEFAULT NULL,
        create_time_nin            IN BOOLEAN := TRUE,
        create_institution_in      IN epis_hidrics_line.create_institution%TYPE DEFAULT NULL,
        create_institution_nin     IN BOOLEAN := TRUE,
        update_user_in             IN epis_hidrics_line.update_user%TYPE DEFAULT NULL,
        update_user_nin            IN BOOLEAN := TRUE,
        update_time_in             IN epis_hidrics_line.update_time%TYPE DEFAULT NULL,
        update_time_nin            IN BOOLEAN := TRUE,
        update_institution_in      IN epis_hidrics_line.update_institution%TYPE DEFAULT NULL,
        update_institution_nin     IN BOOLEAN := TRUE,
        id_prof_last_change_in     IN epis_hidrics_line.id_prof_last_change%TYPE DEFAULT NULL,
        id_prof_last_change_nin    IN BOOLEAN := TRUE,
        dt_epis_hidrics_line_in    IN epis_hidrics_line.dt_epis_hidrics_line%TYPE DEFAULT NULL,
        dt_epis_hidrics_line_nin   IN BOOLEAN := TRUE,
        flg_action_in              IN epis_hidrics_line.flg_action%TYPE DEFAULT NULL,
        flg_action_nin             IN BOOLEAN := TRUE,
        id_epis_hid_ftxt_way_in    IN epis_hidrics_line.id_epis_hid_ftxt_way%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_way_nin   IN BOOLEAN := TRUE,
        id_epis_hid_ftxt_fluid_in  IN epis_hidrics_line.id_epis_hid_ftxt_fluid%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_fluid_nin IN BOOLEAN := TRUE,
        id_epis_hid_ftxt_loc_in    IN epis_hidrics_line.id_epis_hid_ftxt_loc%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_loc_nin   IN BOOLEAN := TRUE,
        where_in                   VARCHAR2 DEFAULT NULL,
        handle_error_in            IN BOOLEAN := TRUE,
        rows_out                   IN OUT table_varchar
    ) IS
        l_sql                      VARCHAR2(32767);
        l_rows_out                 table_varchar;
        l_id_epis_hidrics_n        NUMBER(1);
        l_id_hidrics_n             NUMBER(1);
        l_id_way_n                 NUMBER(1);
        l_id_hidrics_location_n    NUMBER(1);
        l_flg_status_n             NUMBER(1);
        l_dt_creation_n            NUMBER(1);
        l_id_prof_cancel_n         NUMBER(1);
        l_id_cancel_reason_n       NUMBER(1);
        l_notes_cancel_n           NUMBER(1);
        l_dt_cancel_n              NUMBER(1);
        l_create_user_n            NUMBER(1);
        l_create_time_n            NUMBER(1);
        l_create_institution_n     NUMBER(1);
        l_update_user_n            NUMBER(1);
        l_update_time_n            NUMBER(1);
        l_update_institution_n     NUMBER(1);
        l_id_prof_last_change_n    NUMBER(1);
        l_dt_epis_hidrics_line_n   NUMBER(1);
        l_flg_action_n             NUMBER(1);
        l_id_epis_hid_ftxt_way_n   NUMBER(1);
        l_id_epis_hid_ftxt_fluid_n NUMBER(1);
        l_id_epis_hid_ftxt_loc_n   NUMBER(1);
        id_epis_hidrics_line_in    epis_hidrics_line.id_epis_hidrics_line%TYPE;
    BEGIN
    
        l_id_epis_hidrics_n        := sys.diutil.bool_to_int(id_epis_hidrics_nin);
        l_id_hidrics_n             := sys.diutil.bool_to_int(id_hidrics_nin);
        l_id_way_n                 := sys.diutil.bool_to_int(id_way_nin);
        l_id_hidrics_location_n    := sys.diutil.bool_to_int(id_hidrics_location_nin);
        l_flg_status_n             := sys.diutil.bool_to_int(flg_status_nin);
        l_dt_creation_n            := sys.diutil.bool_to_int(dt_creation_nin);
        l_id_prof_cancel_n         := sys.diutil.bool_to_int(id_prof_cancel_nin);
        l_id_cancel_reason_n       := sys.diutil.bool_to_int(id_cancel_reason_nin);
        l_notes_cancel_n           := sys.diutil.bool_to_int(notes_cancel_nin);
        l_dt_cancel_n              := sys.diutil.bool_to_int(dt_cancel_nin);
        l_create_user_n            := sys.diutil.bool_to_int(create_user_nin);
        l_create_time_n            := sys.diutil.bool_to_int(create_time_nin);
        l_create_institution_n     := sys.diutil.bool_to_int(create_institution_nin);
        l_update_user_n            := sys.diutil.bool_to_int(update_user_nin);
        l_update_time_n            := sys.diutil.bool_to_int(update_time_nin);
        l_update_institution_n     := sys.diutil.bool_to_int(update_institution_nin);
        l_id_prof_last_change_n    := sys.diutil.bool_to_int(id_prof_last_change_nin);
        l_dt_epis_hidrics_line_n   := sys.diutil.bool_to_int(dt_epis_hidrics_line_nin);
        l_flg_action_n             := sys.diutil.bool_to_int(flg_action_nin);
        l_id_epis_hid_ftxt_way_n   := sys.diutil.bool_to_int(id_epis_hid_ftxt_way_nin);
        l_id_epis_hid_ftxt_fluid_n := sys.diutil.bool_to_int(id_epis_hid_ftxt_fluid_nin);
        l_id_epis_hid_ftxt_loc_n   := sys.diutil.bool_to_int(id_epis_hid_ftxt_loc_nin);
    
        l_sql := 'UPDATE EPIS_HIDRICS_LINE SET ' || ' ID_EPIS_HIDRICS = decode (' || l_id_epis_hidrics_n ||
                 ',0,:id_epis_hidrics_in, NVL (:id_epis_hidrics_in, ID_EPIS_HIDRICS)) ' || ',' ||
                 ' ID_HIDRICS = decode (' || l_id_hidrics_n || ',0,:id_hidrics_in, NVL (:id_hidrics_in, ID_HIDRICS)) ' || ',' ||
                 ' ID_WAY = decode (' || l_id_way_n || ',0,:id_way_in, NVL (:id_way_in, ID_WAY)) ' || ',' ||
                 ' ID_HIDRICS_LOCATION = decode (' || l_id_hidrics_location_n ||
                 ',0,:id_hidrics_location_in, NVL (:id_hidrics_location_in, ID_HIDRICS_LOCATION)) ' || ',' ||
                 ' FLG_STATUS = decode (' || l_flg_status_n || ',0,:flg_status_in, NVL (:flg_status_in, FLG_STATUS)) ' || ',' ||
                 ' DT_CREATION = decode (' || l_dt_creation_n ||
                 ',0,:dt_creation_in, NVL (:dt_creation_in, DT_CREATION)) ' || ',' || ' ID_PROF_CANCEL = decode (' ||
                 l_id_prof_cancel_n || ',0,:id_prof_cancel_in, NVL (:id_prof_cancel_in, ID_PROF_CANCEL)) ' || ',' ||
                 ' ID_CANCEL_REASON = decode (' || l_id_cancel_reason_n ||
                 ',0,:id_cancel_reason_in, NVL (:id_cancel_reason_in, ID_CANCEL_REASON)) ' || ',' ||
                 ' NOTES_CANCEL = decode (' || l_notes_cancel_n ||
                 ',0,:notes_cancel_in, NVL (:notes_cancel_in, NOTES_CANCEL)) ' || ',' || ' DT_CANCEL = decode (' ||
                 l_dt_cancel_n || ',0,:dt_cancel_in, NVL (:dt_cancel_in, DT_CANCEL)) ' || ',' ||
                 ' CREATE_USER = decode (' || l_create_user_n ||
                 ',0,:create_user_in, NVL (:create_user_in, CREATE_USER)) ' || ',' || ' CREATE_TIME = decode (' ||
                 l_create_time_n || ',0,:create_time_in, NVL (:create_time_in, CREATE_TIME)) ' || ',' ||
                 ' CREATE_INSTITUTION = decode (' || l_create_institution_n ||
                 ',0,:create_institution_in, NVL (:create_institution_in, CREATE_INSTITUTION)) ' || ',' ||
                 ' UPDATE_USER = decode (' || l_update_user_n ||
                 ',0,:update_user_in, NVL (:update_user_in, UPDATE_USER)) ' || ',' || ' UPDATE_TIME = decode (' ||
                 l_update_time_n || ',0,:update_time_in, NVL (:update_time_in, UPDATE_TIME)) ' || ',' ||
                 ' UPDATE_INSTITUTION = decode (' || l_update_institution_n ||
                 ',0,:update_institution_in, NVL (:update_institution_in, UPDATE_INSTITUTION)) ' || ',' ||
                 ' ID_PROF_LAST_CHANGE = decode (' || l_id_prof_last_change_n ||
                 ',0,:id_prof_last_change_in, NVL (:id_prof_last_change_in, ID_PROF_LAST_CHANGE)) ' || ',' ||
                 ' DT_EPIS_HIDRICS_LINE = decode (' || l_dt_epis_hidrics_line_n ||
                 ',0,:dt_epis_hidrics_line_in, NVL (:dt_epis_hidrics_line_in, DT_EPIS_HIDRICS_LINE)) ' || ',' ||
                 ' FLG_ACTION = decode (' || l_flg_action_n || ',0,:flg_action_in, NVL (:flg_action_in, FLG_ACTION)) ' || ',' ||
                 ' ID_EPIS_HID_FTXT_WAY = decode (' || l_id_epis_hid_ftxt_way_n ||
                 ',0,:id_epis_hid_ftxt_way_in, NVL (:id_epis_hid_ftxt_way_in, ID_EPIS_HID_FTXT_WAY)) ' || ',' ||
                 ' ID_EPIS_HID_FTXT_FLUID = decode (' || l_id_epis_hid_ftxt_fluid_n ||
                 ',0,:id_epis_hid_ftxt_fluid_in, NVL (:id_epis_hid_ftxt_fluid_in, ID_EPIS_HID_FTXT_FLUID)) ' || ',' ||
                 ' ID_EPIS_HID_FTXT_LOC = decode (' || l_id_epis_hid_ftxt_loc_n ||
                 ',0,:id_epis_hid_ftxt_loc_in, NVL (:id_epis_hid_ftxt_loc_in, ID_EPIS_HID_FTXT_LOC)) ' || ' where ' ||
                 nvl(where_in, '(1=1)') || ' RETURNING ROWID BULK COLLECT INTO :l_rows_out';
    
        EXECUTE IMMEDIATE 'BEGIN ' || l_sql || '; END;'
            USING IN id_epis_hidrics_in, id_hidrics_in, id_way_in, id_hidrics_location_in, flg_status_in, dt_creation_in, id_prof_cancel_in, id_cancel_reason_in, notes_cancel_in, dt_cancel_in, create_user_in, create_time_in, create_institution_in, update_user_in, update_time_in, update_institution_in, id_prof_last_change_in, dt_epis_hidrics_line_in, flg_action_in, id_epis_hid_ftxt_way_in, id_epis_hid_ftxt_fluid_in, id_epis_hid_ftxt_loc_in, OUT l_rows_out;
    
        IF (rows_out IS NULL)
        THEN
            rows_out := table_varchar();
        END IF;
    
        rows_out := rows_out MULTISET UNION DISTINCT l_rows_out;
    
    EXCEPTION
        WHEN dup_val_on_index THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner           all_constraints.owner%TYPE;
                    l_name            all_constraints.constraint_name%TYPE;
                    l_err_instance_id PLS_INTEGER;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF FALSE
                    THEN
                        NULL; -- Placeholder in case no unique indexes
                    ELSE
                        pk_alert_exceptions.raise_error(error_name_in => 'DUPLICATE-VALUE',
                                                        name1_in      => 'OWNER',
                                                        value1_in     => l_owner,
                                                        name2_in      => 'CONSTRAINT_NAME',
                                                        value2_in     => l_name,
                                                        name3_in      => 'TABLE_NAME',
                                                        value3_in     => 'EPIS_HIDRICS_LINE');
                    END IF;
                END;
            END IF;
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'EPIS_HIDRICS_LINE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'EPIS_HIDRICS_LINE');
                    IF l_name = 'EHL_CR_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_CANCEL_REASON',
                                                        value_in           => id_cancel_reason_in);
                    END IF;
                    IF l_name = 'EHL_EHID_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_EPIS_HIDRICS',
                                                        value_in           => id_epis_hidrics_in);
                    END IF;
                    IF l_name = 'EHL_FTFLUID_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_EPIS_HID_FTXT_FLUID',
                                                        value_in           => id_epis_hid_ftxt_fluid_in);
                    END IF;
                    IF l_name = 'EHL_FTLOC_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_EPIS_HID_FTXT_LOC',
                                                        value_in           => id_epis_hid_ftxt_loc_in);
                    END IF;
                    IF l_name = 'EHL_FTWAY_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_EPIS_HID_FTXT_WAY',
                                                        value_in           => id_epis_hid_ftxt_way_in);
                    END IF;
                    IF l_name = 'EHL_HID_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_HIDRICS',
                                                        value_in           => id_hidrics_in);
                    END IF;
                    IF l_name = 'EHL_HID_LOC_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_HIDRICS_LOCATION',
                                                        value_in           => id_hidrics_location_in);
                    END IF;
                    IF l_name = 'EHL_HID_WAY_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_WAY',
                                                        value_in           => id_way_in);
                    END IF;
                    IF l_name = 'EHL_PROF_FK'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_PROF_CANCEL',
                                                        value_in           => id_prof_cancel_in);
                    END IF;
                    IF l_name = 'EHL_PROF_FK2'
                    THEN
                        -- Add a context value for each column
                        pk_alert_exceptions.add_context(err_instance_id_in => l_id,
                                                        name_in            => 'ID_PROF_LAST_CHANGE',
                                                        value_in           => id_prof_last_change_in);
                    END IF;
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
        WHEN e_null_column_value THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    v_errm    VARCHAR2(2000) := dbms_utility.format_error_stack;
                    dot1loc   INTEGER;
                    dot2loc   INTEGER;
                    parenloc  INTEGER;
                    c_owner   all_constraints.owner%TYPE;
                    c_tabname all_tables.table_name%TYPE;
                    c_colname all_tab_columns.column_name%TYPE;
                BEGIN
                    dot1loc   := instr(v_errm, '.', 1, 1);
                    dot2loc   := instr(v_errm, '.', 1, 2);
                    parenloc  := instr(v_errm, '(');
                    c_owner   := substr(v_errm, parenloc + 1, dot1loc - parenloc - 1);
                    c_tabname := substr(v_errm, dot1loc + 1, dot2loc - dot1loc - 1);
                    c_colname := substr(v_errm, dot2loc + 1, instr(v_errm, ')') - dot2loc - 1);
                
                    pk_alert_exceptions.raise_error(error_name_in => 'COLUMN-CANNOT-BE-NULL',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => c_owner,
                                                    name2_in      => 'TABLE_NAME',
                                                    value2_in     => c_tabname,
                                                    name3_in      => 'COLUMN_NAME',
                                                    value3_in     => c_colname);
                END;
            END IF;
    END upd;

    PROCEDURE upd
    (
        id_epis_hidrics_in         IN epis_hidrics_line.id_epis_hidrics%TYPE DEFAULT NULL,
        id_epis_hidrics_nin        IN BOOLEAN := TRUE,
        id_hidrics_in              IN epis_hidrics_line.id_hidrics%TYPE DEFAULT NULL,
        id_hidrics_nin             IN BOOLEAN := TRUE,
        id_way_in                  IN epis_hidrics_line.id_way%TYPE DEFAULT NULL,
        id_way_nin                 IN BOOLEAN := TRUE,
        id_hidrics_location_in     IN epis_hidrics_line.id_hidrics_location%TYPE DEFAULT NULL,
        id_hidrics_location_nin    IN BOOLEAN := TRUE,
        flg_status_in              IN epis_hidrics_line.flg_status%TYPE DEFAULT NULL,
        flg_status_nin             IN BOOLEAN := TRUE,
        dt_creation_in             IN epis_hidrics_line.dt_creation%TYPE DEFAULT NULL,
        dt_creation_nin            IN BOOLEAN := TRUE,
        id_prof_cancel_in          IN epis_hidrics_line.id_prof_cancel%TYPE DEFAULT NULL,
        id_prof_cancel_nin         IN BOOLEAN := TRUE,
        id_cancel_reason_in        IN epis_hidrics_line.id_cancel_reason%TYPE DEFAULT NULL,
        id_cancel_reason_nin       IN BOOLEAN := TRUE,
        notes_cancel_in            IN epis_hidrics_line.notes_cancel%TYPE DEFAULT NULL,
        notes_cancel_nin           IN BOOLEAN := TRUE,
        dt_cancel_in               IN epis_hidrics_line.dt_cancel%TYPE DEFAULT NULL,
        dt_cancel_nin              IN BOOLEAN := TRUE,
        create_user_in             IN epis_hidrics_line.create_user%TYPE DEFAULT NULL,
        create_user_nin            IN BOOLEAN := TRUE,
        create_time_in             IN epis_hidrics_line.create_time%TYPE DEFAULT NULL,
        create_time_nin            IN BOOLEAN := TRUE,
        create_institution_in      IN epis_hidrics_line.create_institution%TYPE DEFAULT NULL,
        create_institution_nin     IN BOOLEAN := TRUE,
        update_user_in             IN epis_hidrics_line.update_user%TYPE DEFAULT NULL,
        update_user_nin            IN BOOLEAN := TRUE,
        update_time_in             IN epis_hidrics_line.update_time%TYPE DEFAULT NULL,
        update_time_nin            IN BOOLEAN := TRUE,
        update_institution_in      IN epis_hidrics_line.update_institution%TYPE DEFAULT NULL,
        update_institution_nin     IN BOOLEAN := TRUE,
        id_prof_last_change_in     IN epis_hidrics_line.id_prof_last_change%TYPE DEFAULT NULL,
        id_prof_last_change_nin    IN BOOLEAN := TRUE,
        dt_epis_hidrics_line_in    IN epis_hidrics_line.dt_epis_hidrics_line%TYPE DEFAULT NULL,
        dt_epis_hidrics_line_nin   IN BOOLEAN := TRUE,
        flg_action_in              IN epis_hidrics_line.flg_action%TYPE DEFAULT NULL,
        flg_action_nin             IN BOOLEAN := TRUE,
        id_epis_hid_ftxt_way_in    IN epis_hidrics_line.id_epis_hid_ftxt_way%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_way_nin   IN BOOLEAN := TRUE,
        id_epis_hid_ftxt_fluid_in  IN epis_hidrics_line.id_epis_hid_ftxt_fluid%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_fluid_nin IN BOOLEAN := TRUE,
        id_epis_hid_ftxt_loc_in    IN epis_hidrics_line.id_epis_hid_ftxt_loc%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_loc_nin   IN BOOLEAN := TRUE,
        where_in                   VARCHAR2 DEFAULT NULL,
        handle_error_in            IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        upd(id_epis_hidrics_in         => id_epis_hidrics_in,
            id_epis_hidrics_nin        => id_epis_hidrics_nin,
            id_hidrics_in              => id_hidrics_in,
            id_hidrics_nin             => id_hidrics_nin,
            id_way_in                  => id_way_in,
            id_way_nin                 => id_way_nin,
            id_hidrics_location_in     => id_hidrics_location_in,
            id_hidrics_location_nin    => id_hidrics_location_nin,
            flg_status_in              => flg_status_in,
            flg_status_nin             => flg_status_nin,
            dt_creation_in             => dt_creation_in,
            dt_creation_nin            => dt_creation_nin,
            id_prof_cancel_in          => id_prof_cancel_in,
            id_prof_cancel_nin         => id_prof_cancel_nin,
            id_cancel_reason_in        => id_cancel_reason_in,
            id_cancel_reason_nin       => id_cancel_reason_nin,
            notes_cancel_in            => notes_cancel_in,
            notes_cancel_nin           => notes_cancel_nin,
            dt_cancel_in               => dt_cancel_in,
            dt_cancel_nin              => dt_cancel_nin,
            create_user_in             => create_user_in,
            create_user_nin            => create_user_nin,
            create_time_in             => create_time_in,
            create_time_nin            => create_time_nin,
            create_institution_in      => create_institution_in,
            create_institution_nin     => create_institution_nin,
            update_user_in             => update_user_in,
            update_user_nin            => update_user_nin,
            update_time_in             => update_time_in,
            update_time_nin            => update_time_nin,
            update_institution_in      => update_institution_in,
            update_institution_nin     => update_institution_nin,
            id_prof_last_change_in     => id_prof_last_change_in,
            id_prof_last_change_nin    => id_prof_last_change_nin,
            dt_epis_hidrics_line_in    => dt_epis_hidrics_line_in,
            dt_epis_hidrics_line_nin   => dt_epis_hidrics_line_nin,
            flg_action_in              => flg_action_in,
            flg_action_nin             => flg_action_nin,
            id_epis_hid_ftxt_way_in    => id_epis_hid_ftxt_way_in,
            id_epis_hid_ftxt_way_nin   => id_epis_hid_ftxt_way_nin,
            id_epis_hid_ftxt_fluid_in  => id_epis_hid_ftxt_fluid_in,
            id_epis_hid_ftxt_fluid_nin => id_epis_hid_ftxt_fluid_nin,
            id_epis_hid_ftxt_loc_in    => id_epis_hid_ftxt_loc_in,
            id_epis_hid_ftxt_loc_nin   => id_epis_hid_ftxt_loc_nin,
            where_in                   => where_in,
            handle_error_in            => handle_error_in,
            rows_out                   => rows_out);
    END upd;

    PROCEDURE upd
    (
        rec_in          IN epis_hidrics_line%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        IN OUT table_varchar
    ) IS
    BEGIN
        upd(id_epis_hidrics_line_in   => rec_in.id_epis_hidrics_line,
            id_epis_hidrics_in        => rec_in.id_epis_hidrics,
            id_hidrics_in             => rec_in.id_hidrics,
            id_way_in                 => rec_in.id_way,
            id_hidrics_location_in    => rec_in.id_hidrics_location,
            flg_status_in             => rec_in.flg_status,
            dt_creation_in            => rec_in.dt_creation,
            id_prof_cancel_in         => rec_in.id_prof_cancel,
            id_cancel_reason_in       => rec_in.id_cancel_reason,
            notes_cancel_in           => rec_in.notes_cancel,
            dt_cancel_in              => rec_in.dt_cancel,
            create_user_in            => rec_in.create_user,
            create_time_in            => rec_in.create_time,
            create_institution_in     => rec_in.create_institution,
            update_user_in            => rec_in.update_user,
            update_time_in            => rec_in.update_time,
            update_institution_in     => rec_in.update_institution,
            id_prof_last_change_in    => rec_in.id_prof_last_change,
            dt_epis_hidrics_line_in   => rec_in.dt_epis_hidrics_line,
            flg_action_in             => rec_in.flg_action,
            id_epis_hid_ftxt_way_in   => rec_in.id_epis_hid_ftxt_way,
            id_epis_hid_ftxt_fluid_in => rec_in.id_epis_hid_ftxt_fluid,
            id_epis_hid_ftxt_loc_in   => rec_in.id_epis_hid_ftxt_loc
            
           ,
            handle_error_in => handle_error_in,
            rows_out        => rows_out);
    END upd;

    PROCEDURE upd
    (
        rec_in          IN epis_hidrics_line%ROWTYPE,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        upd(id_epis_hidrics_line_in   => rec_in.id_epis_hidrics_line,
            id_epis_hidrics_in        => rec_in.id_epis_hidrics,
            id_hidrics_in             => rec_in.id_hidrics,
            id_way_in                 => rec_in.id_way,
            id_hidrics_location_in    => rec_in.id_hidrics_location,
            flg_status_in             => rec_in.flg_status,
            dt_creation_in            => rec_in.dt_creation,
            id_prof_cancel_in         => rec_in.id_prof_cancel,
            id_cancel_reason_in       => rec_in.id_cancel_reason,
            notes_cancel_in           => rec_in.notes_cancel,
            dt_cancel_in              => rec_in.dt_cancel,
            create_user_in            => rec_in.create_user,
            create_time_in            => rec_in.create_time,
            create_institution_in     => rec_in.create_institution,
            update_user_in            => rec_in.update_user,
            update_time_in            => rec_in.update_time,
            update_institution_in     => rec_in.update_institution,
            id_prof_last_change_in    => rec_in.id_prof_last_change,
            dt_epis_hidrics_line_in   => rec_in.dt_epis_hidrics_line,
            flg_action_in             => rec_in.flg_action,
            id_epis_hid_ftxt_way_in   => rec_in.id_epis_hid_ftxt_way,
            id_epis_hid_ftxt_fluid_in => rec_in.id_epis_hid_ftxt_fluid,
            id_epis_hid_ftxt_loc_in   => rec_in.id_epis_hid_ftxt_loc
            
           ,
            handle_error_in => handle_error_in,
            rows_out        => rows_out);
    END upd;

    PROCEDURE upd_ins
    (
        id_epis_hidrics_line_in   IN epis_hidrics_line.id_epis_hidrics_line%TYPE,
        id_epis_hidrics_in        IN epis_hidrics_line.id_epis_hidrics%TYPE DEFAULT NULL,
        id_hidrics_in             IN epis_hidrics_line.id_hidrics%TYPE DEFAULT NULL,
        id_way_in                 IN epis_hidrics_line.id_way%TYPE DEFAULT NULL,
        id_hidrics_location_in    IN epis_hidrics_line.id_hidrics_location%TYPE DEFAULT NULL,
        flg_status_in             IN epis_hidrics_line.flg_status%TYPE DEFAULT NULL,
        dt_creation_in            IN epis_hidrics_line.dt_creation%TYPE DEFAULT NULL,
        id_prof_cancel_in         IN epis_hidrics_line.id_prof_cancel%TYPE DEFAULT NULL,
        id_cancel_reason_in       IN epis_hidrics_line.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in           IN epis_hidrics_line.notes_cancel%TYPE DEFAULT NULL,
        dt_cancel_in              IN epis_hidrics_line.dt_cancel%TYPE DEFAULT NULL,
        create_user_in            IN epis_hidrics_line.create_user%TYPE DEFAULT NULL,
        create_time_in            IN epis_hidrics_line.create_time%TYPE DEFAULT NULL,
        create_institution_in     IN epis_hidrics_line.create_institution%TYPE DEFAULT NULL,
        update_user_in            IN epis_hidrics_line.update_user%TYPE DEFAULT NULL,
        update_time_in            IN epis_hidrics_line.update_time%TYPE DEFAULT NULL,
        update_institution_in     IN epis_hidrics_line.update_institution%TYPE DEFAULT NULL,
        id_prof_last_change_in    IN epis_hidrics_line.id_prof_last_change%TYPE DEFAULT NULL,
        dt_epis_hidrics_line_in   IN epis_hidrics_line.dt_epis_hidrics_line%TYPE DEFAULT NULL,
        flg_action_in             IN epis_hidrics_line.flg_action%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_way_in   IN epis_hidrics_line.id_epis_hid_ftxt_way%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_fluid_in IN epis_hidrics_line.id_epis_hid_ftxt_fluid%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_loc_in   IN epis_hidrics_line.id_epis_hid_ftxt_loc%TYPE DEFAULT NULL,
        handle_error_in           IN BOOLEAN := TRUE,
        rows_out                  OUT table_varchar
    ) IS
    BEGIN
        upd(id_epis_hidrics_line_in   => id_epis_hidrics_line_in,
            id_epis_hidrics_in        => id_epis_hidrics_in,
            id_hidrics_in             => id_hidrics_in,
            id_way_in                 => id_way_in,
            id_hidrics_location_in    => id_hidrics_location_in,
            flg_status_in             => flg_status_in,
            dt_creation_in            => dt_creation_in,
            id_prof_cancel_in         => id_prof_cancel_in,
            id_cancel_reason_in       => id_cancel_reason_in,
            notes_cancel_in           => notes_cancel_in,
            dt_cancel_in              => dt_cancel_in,
            create_user_in            => create_user_in,
            create_time_in            => create_time_in,
            create_institution_in     => create_institution_in,
            update_user_in            => update_user_in,
            update_time_in            => update_time_in,
            update_institution_in     => update_institution_in,
            id_prof_last_change_in    => id_prof_last_change_in,
            dt_epis_hidrics_line_in   => dt_epis_hidrics_line_in,
            flg_action_in             => flg_action_in,
            id_epis_hid_ftxt_way_in   => id_epis_hid_ftxt_way_in,
            id_epis_hid_ftxt_fluid_in => id_epis_hid_ftxt_fluid_in,
            id_epis_hid_ftxt_loc_in   => id_epis_hid_ftxt_loc_in,
            handle_error_in           => handle_error_in,
            rows_out                  => rows_out);
        IF SQL%ROWCOUNT = 0
        THEN
            ins(id_epis_hidrics_line_in   => id_epis_hidrics_line_in,
                id_epis_hidrics_in        => id_epis_hidrics_in,
                id_hidrics_in             => id_hidrics_in,
                id_way_in                 => id_way_in,
                id_hidrics_location_in    => id_hidrics_location_in,
                flg_status_in             => flg_status_in,
                dt_creation_in            => dt_creation_in,
                id_prof_cancel_in         => id_prof_cancel_in,
                id_cancel_reason_in       => id_cancel_reason_in,
                notes_cancel_in           => notes_cancel_in,
                dt_cancel_in              => dt_cancel_in,
                create_user_in            => create_user_in,
                create_time_in            => create_time_in,
                create_institution_in     => create_institution_in,
                update_user_in            => update_user_in,
                update_time_in            => update_time_in,
                update_institution_in     => update_institution_in,
                id_prof_last_change_in    => id_prof_last_change_in,
                dt_epis_hidrics_line_in   => dt_epis_hidrics_line_in,
                flg_action_in             => flg_action_in,
                id_epis_hid_ftxt_way_in   => id_epis_hid_ftxt_way_in,
                id_epis_hid_ftxt_fluid_in => id_epis_hid_ftxt_fluid_in,
                id_epis_hid_ftxt_loc_in   => id_epis_hid_ftxt_loc_in,
                handle_error_in           => handle_error_in,
                rows_out                  => rows_out);
        END IF;
    END upd_ins;

    PROCEDURE upd_ins
    (
        id_epis_hidrics_line_in   IN epis_hidrics_line.id_epis_hidrics_line%TYPE,
        id_epis_hidrics_in        IN epis_hidrics_line.id_epis_hidrics%TYPE DEFAULT NULL,
        id_hidrics_in             IN epis_hidrics_line.id_hidrics%TYPE DEFAULT NULL,
        id_way_in                 IN epis_hidrics_line.id_way%TYPE DEFAULT NULL,
        id_hidrics_location_in    IN epis_hidrics_line.id_hidrics_location%TYPE DEFAULT NULL,
        flg_status_in             IN epis_hidrics_line.flg_status%TYPE DEFAULT NULL,
        dt_creation_in            IN epis_hidrics_line.dt_creation%TYPE DEFAULT NULL,
        id_prof_cancel_in         IN epis_hidrics_line.id_prof_cancel%TYPE DEFAULT NULL,
        id_cancel_reason_in       IN epis_hidrics_line.id_cancel_reason%TYPE DEFAULT NULL,
        notes_cancel_in           IN epis_hidrics_line.notes_cancel%TYPE DEFAULT NULL,
        dt_cancel_in              IN epis_hidrics_line.dt_cancel%TYPE DEFAULT NULL,
        create_user_in            IN epis_hidrics_line.create_user%TYPE DEFAULT NULL,
        create_time_in            IN epis_hidrics_line.create_time%TYPE DEFAULT NULL,
        create_institution_in     IN epis_hidrics_line.create_institution%TYPE DEFAULT NULL,
        update_user_in            IN epis_hidrics_line.update_user%TYPE DEFAULT NULL,
        update_time_in            IN epis_hidrics_line.update_time%TYPE DEFAULT NULL,
        update_institution_in     IN epis_hidrics_line.update_institution%TYPE DEFAULT NULL,
        id_prof_last_change_in    IN epis_hidrics_line.id_prof_last_change%TYPE DEFAULT NULL,
        dt_epis_hidrics_line_in   IN epis_hidrics_line.dt_epis_hidrics_line%TYPE DEFAULT NULL,
        flg_action_in             IN epis_hidrics_line.flg_action%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_way_in   IN epis_hidrics_line.id_epis_hid_ftxt_way%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_fluid_in IN epis_hidrics_line.id_epis_hid_ftxt_fluid%TYPE DEFAULT NULL,
        id_epis_hid_ftxt_loc_in   IN epis_hidrics_line.id_epis_hid_ftxt_loc%TYPE DEFAULT NULL,
        handle_error_in           IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        upd_ins(id_epis_hidrics_line_in,
                id_epis_hidrics_in,
                id_hidrics_in,
                id_way_in,
                id_hidrics_location_in,
                flg_status_in,
                dt_creation_in,
                id_prof_cancel_in,
                id_cancel_reason_in,
                notes_cancel_in,
                dt_cancel_in,
                create_user_in,
                create_time_in,
                create_institution_in,
                update_user_in,
                update_time_in,
                update_institution_in,
                id_prof_last_change_in,
                dt_epis_hidrics_line_in,
                flg_action_in,
                id_epis_hid_ftxt_way_in,
                id_epis_hid_ftxt_fluid_in,
                id_epis_hid_ftxt_loc_in,
                handle_error_in,
                rows_out);
    END upd_ins;

    PROCEDURE upd
    (
        col_in            IN epis_hidrics_line_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          IN OUT table_varchar
    ) IS
        l_id_epis_hidrics_line   id_epis_hidrics_line_cc;
        l_id_epis_hidrics        id_epis_hidrics_cc;
        l_id_hidrics             id_hidrics_cc;
        l_id_way                 id_way_cc;
        l_id_hidrics_location    id_hidrics_location_cc;
        l_flg_status             flg_status_cc;
        l_dt_creation            dt_creation_cc;
        l_id_prof_cancel         id_prof_cancel_cc;
        l_id_cancel_reason       id_cancel_reason_cc;
        l_notes_cancel           notes_cancel_cc;
        l_dt_cancel              dt_cancel_cc;
        l_create_user            create_user_cc;
        l_create_time            create_time_cc;
        l_create_institution     create_institution_cc;
        l_update_user            update_user_cc;
        l_update_time            update_time_cc;
        l_update_institution     update_institution_cc;
        l_id_prof_last_change    id_prof_last_change_cc;
        l_dt_epis_hidrics_line   dt_epis_hidrics_line_cc;
        l_flg_action             flg_action_cc;
        l_id_epis_hid_ftxt_way   id_epis_hid_ftxt_way_cc;
        l_id_epis_hid_ftxt_fluid id_epis_hid_ftxt_fluid_cc;
        l_id_epis_hid_ftxt_loc   id_epis_hid_ftxt_loc_cc;
    BEGIN
        FOR i IN col_in.first .. col_in.last
        LOOP
            l_id_epis_hidrics_line(i) := col_in(i).id_epis_hidrics_line;
            l_id_epis_hidrics(i) := col_in(i).id_epis_hidrics;
            l_id_hidrics(i) := col_in(i).id_hidrics;
            l_id_way(i) := col_in(i).id_way;
            l_id_hidrics_location(i) := col_in(i).id_hidrics_location;
            l_flg_status(i) := col_in(i).flg_status;
            l_dt_creation(i) := col_in(i).dt_creation;
            l_id_prof_cancel(i) := col_in(i).id_prof_cancel;
            l_id_cancel_reason(i) := col_in(i).id_cancel_reason;
            l_notes_cancel(i) := col_in(i).notes_cancel;
            l_dt_cancel(i) := col_in(i).dt_cancel;
            l_create_user(i) := col_in(i).create_user;
            l_create_time(i) := col_in(i).create_time;
            l_create_institution(i) := col_in(i).create_institution;
            l_update_user(i) := col_in(i).update_user;
            l_update_time(i) := col_in(i).update_time;
            l_update_institution(i) := col_in(i).update_institution;
            l_id_prof_last_change(i) := col_in(i).id_prof_last_change;
            l_dt_epis_hidrics_line(i) := col_in(i).dt_epis_hidrics_line;
            l_flg_action(i) := col_in(i).flg_action;
            l_id_epis_hid_ftxt_way(i) := col_in(i).id_epis_hid_ftxt_way;
            l_id_epis_hid_ftxt_fluid(i) := col_in(i).id_epis_hid_ftxt_fluid;
            l_id_epis_hid_ftxt_loc(i) := col_in(i).id_epis_hid_ftxt_loc;
        END LOOP;
        IF nvl(ignore_if_null_in, FALSE)
        THEN
            -- Set any columns to their current values
            -- if incoming value is NULL.
            -- Put WHEN clause on column-level triggers!
            FORALL i IN col_in.first .. col_in.last
                UPDATE epis_hidrics_line
                   SET id_epis_hidrics        = nvl(l_id_epis_hidrics(i), id_epis_hidrics),
                       id_hidrics             = nvl(l_id_hidrics(i), id_hidrics),
                       id_way                 = nvl(l_id_way(i), id_way),
                       id_hidrics_location    = nvl(l_id_hidrics_location(i), id_hidrics_location),
                       flg_status             = nvl(l_flg_status(i), flg_status),
                       dt_creation            = nvl(l_dt_creation(i), dt_creation),
                       id_prof_cancel         = nvl(l_id_prof_cancel(i), id_prof_cancel),
                       id_cancel_reason       = nvl(l_id_cancel_reason(i), id_cancel_reason),
                       notes_cancel           = nvl(l_notes_cancel(i), notes_cancel),
                       dt_cancel              = nvl(l_dt_cancel(i), dt_cancel),
                       create_user            = nvl(l_create_user(i), create_user),
                       create_time            = nvl(l_create_time(i), create_time),
                       create_institution     = nvl(l_create_institution(i), create_institution),
                       update_user            = nvl(l_update_user(i), update_user),
                       update_time            = nvl(l_update_time(i), update_time),
                       update_institution     = nvl(l_update_institution(i), update_institution),
                       id_prof_last_change    = nvl(l_id_prof_last_change(i), id_prof_last_change),
                       dt_epis_hidrics_line   = nvl(l_dt_epis_hidrics_line(i), dt_epis_hidrics_line),
                       flg_action             = nvl(l_flg_action(i), flg_action),
                       id_epis_hid_ftxt_way   = nvl(l_id_epis_hid_ftxt_way(i), id_epis_hid_ftxt_way),
                       id_epis_hid_ftxt_fluid = nvl(l_id_epis_hid_ftxt_fluid(i), id_epis_hid_ftxt_fluid),
                       id_epis_hid_ftxt_loc   = nvl(l_id_epis_hid_ftxt_loc(i), id_epis_hid_ftxt_loc)
                 WHERE id_epis_hidrics_line = l_id_epis_hidrics_line(i)
                RETURNING ROWID BULK COLLECT INTO rows_out;
        ELSE
            FORALL i IN col_in.first .. col_in.last
                UPDATE epis_hidrics_line
                   SET id_epis_hidrics        = l_id_epis_hidrics(i),
                       id_hidrics             = l_id_hidrics(i),
                       id_way                 = l_id_way(i),
                       id_hidrics_location    = l_id_hidrics_location(i),
                       flg_status             = l_flg_status(i),
                       dt_creation            = l_dt_creation(i),
                       id_prof_cancel         = l_id_prof_cancel(i),
                       id_cancel_reason       = l_id_cancel_reason(i),
                       notes_cancel           = l_notes_cancel(i),
                       dt_cancel              = l_dt_cancel(i),
                       create_user            = l_create_user(i),
                       create_time            = l_create_time(i),
                       create_institution     = l_create_institution(i),
                       update_user            = l_update_user(i),
                       update_time            = l_update_time(i),
                       update_institution     = l_update_institution(i),
                       id_prof_last_change    = l_id_prof_last_change(i),
                       dt_epis_hidrics_line   = l_dt_epis_hidrics_line(i),
                       flg_action             = l_flg_action(i),
                       id_epis_hid_ftxt_way   = l_id_epis_hid_ftxt_way(i),
                       id_epis_hid_ftxt_fluid = l_id_epis_hid_ftxt_fluid(i),
                       id_epis_hid_ftxt_loc   = l_id_epis_hid_ftxt_loc(i)
                 WHERE id_epis_hidrics_line = l_id_epis_hidrics_line(i)
                RETURNING ROWID BULK COLLECT INTO rows_out;
        END IF;
    END upd;

    PROCEDURE upd
    (
        col_in            IN epis_hidrics_line_tc,
        ignore_if_null_in IN BOOLEAN := TRUE,
        handle_error_in   IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        upd(col_in, ignore_if_null_in, handle_error_in, rows_out);
    END upd;

    FUNCTION dynupdstr
    (
        colname_in IN all_tab_columns.column_name%TYPE,
        where_in   IN VARCHAR2 := NULL
    )
    
     RETURN VARCHAR2 IS
    BEGIN
        RETURN 'BEGIN UPDATE EPIS_HIDRICS_LINE
             SET ' || colname_in || ' = :value
           WHERE ' || nvl(where_in, '1=1') || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;';
    END dynupdstr;

    FUNCTION dynupdstr_no_rows_out
    (
        colname_in IN all_tab_columns.column_name%TYPE,
        where_in   IN VARCHAR2 := NULL
    )
    
     RETURN VARCHAR2 IS
    BEGIN
        RETURN 'UPDATE EPIS_HIDRICS_LINE
             SET ' || colname_in || ' = :value
           WHERE ' || nvl(where_in, '1=1');
    END dynupdstr_no_rows_out;

    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    ) IS
    BEGIN
        EXECUTE IMMEDIATE 'BEGIN UPDATE EPIS_HIDRICS_LINE set ' || colname_in || '=' || colname_in || ' + ' ||
                          nvl(increment_value_in, 1) || ' WHERE ' || nvl(where_in, '1=1') ||
                          ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
            USING OUT rows_out;
    EXCEPTION
        WHEN dup_val_on_index THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner           all_constraints.owner%TYPE;
                    l_name            all_constraints.constraint_name%TYPE;
                    l_err_instance_id PLS_INTEGER;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'DUPLICATE-VALUE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'EPIS_HIDRICS_LINE');
                END;
            END IF;
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'EPIS_HIDRICS_LINE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'EPIS_HIDRICS_LINE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
        WHEN e_null_column_value THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    v_errm    VARCHAR2(2000) := dbms_utility.format_error_stack;
                    dot1loc   INTEGER;
                    dot2loc   INTEGER;
                    parenloc  INTEGER;
                    c_owner   all_constraints.owner%TYPE;
                    c_tabname all_tables.table_name%TYPE;
                    c_colname all_tab_columns.column_name%TYPE;
                BEGIN
                    dot1loc   := instr(v_errm, '.', 1, 1);
                    dot2loc   := instr(v_errm, '.', 1, 2);
                    parenloc  := instr(v_errm, '(');
                    c_owner   := substr(v_errm, parenloc + 1, dot1loc - parenloc - 1);
                    c_tabname := substr(v_errm, dot1loc + 1, dot2loc - dot1loc - 1);
                    c_colname := substr(v_errm, dot2loc + 1, instr(v_errm, ')') - dot2loc - 1);
                
                    pk_alert_exceptions.raise_error(error_name_in => 'COLUMN-CANNOT-BE-NULL',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => c_owner,
                                                    name2_in      => 'TABLE_NAME',
                                                    value2_in     => c_tabname,
                                                    name3_in      => 'COLUMN_NAME',
                                                    value3_in     => c_colname);
                END;
            END IF;
    END increment_onecol;

    PROCEDURE increment_onecol
    (
        colname_in         IN all_tab_columns.column_name%TYPE,
        where_in           IN VARCHAR2 := NULL,
        increment_value_in IN NUMBER DEFAULT 1,
        handle_error_in    IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        EXECUTE IMMEDIATE 'UPDATE EPIS_HIDRICS_LINE set ' || colname_in || '=' || colname_in || ' + ' ||
                          nvl(increment_value_in, 1) || ' WHERE ' || nvl(where_in, '1=1');
    EXCEPTION
        WHEN dup_val_on_index THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner           all_constraints.owner%TYPE;
                    l_name            all_constraints.constraint_name%TYPE;
                    l_err_instance_id PLS_INTEGER;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'DUPLICATE-VALUE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'EPIS_HIDRICS_LINE');
                END;
            END IF;
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'EPIS_HIDRICS_LINE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'EPIS_HIDRICS_LINE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
        WHEN e_null_column_value THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    v_errm    VARCHAR2(2000) := dbms_utility.format_error_stack;
                    dot1loc   INTEGER;
                    dot2loc   INTEGER;
                    parenloc  INTEGER;
                    c_owner   all_constraints.owner%TYPE;
                    c_tabname all_tables.table_name%TYPE;
                    c_colname all_tab_columns.column_name%TYPE;
                BEGIN
                    dot1loc   := instr(v_errm, '.', 1, 1);
                    dot2loc   := instr(v_errm, '.', 1, 2);
                    parenloc  := instr(v_errm, '(');
                    c_owner   := substr(v_errm, parenloc + 1, dot1loc - parenloc - 1);
                    c_tabname := substr(v_errm, dot1loc + 1, dot2loc - dot1loc - 1);
                    c_colname := substr(v_errm, dot2loc + 1, instr(v_errm, ')') - dot2loc - 1);
                
                    pk_alert_exceptions.raise_error(error_name_in => 'COLUMN-CANNOT-BE-NULL',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => c_owner,
                                                    name2_in      => 'TABLE_NAME',
                                                    value2_in     => c_tabname,
                                                    name3_in      => 'COLUMN_NAME',
                                                    value3_in     => c_colname);
                END;
            END IF;
    END increment_onecol;

    -- Delete functionality

    PROCEDURE del
    (
        id_epis_hidrics_line_in IN epis_hidrics_line.id_epis_hidrics_line%TYPE,
        handle_error_in         IN BOOLEAN := TRUE,
        rows_out                OUT table_varchar
    ) IS
    BEGIN
        DELETE FROM epis_hidrics_line
         WHERE id_epis_hidrics_line = id_epis_hidrics_line_in
        RETURNING ROWID BULK COLLECT INTO rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'EPIS_HIDRICS_LINE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'EPIS_HIDRICS_LINE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del;

    PROCEDURE del
    (
        id_epis_hidrics_line_in IN epis_hidrics_line.id_epis_hidrics_line%TYPE,
        handle_error_in         IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
    
        del(id_epis_hidrics_line_in => id_epis_hidrics_line_in,
            handle_error_in         => handle_error_in,
            rows_out                => rows_out);
    
    END del;

    -- Delete all rows for primary key column ID_EPIS_HIDRICS_LINE
    PROCEDURE del_id_epis_hidrics_line
    (
        id_epis_hidrics_line_in IN epis_hidrics_line.id_epis_hidrics_line%TYPE,
        handle_error_in         IN BOOLEAN := TRUE,
        rows_out                OUT table_varchar
    ) IS
    BEGIN
        DELETE FROM epis_hidrics_line
         WHERE id_epis_hidrics_line = id_epis_hidrics_line_in
        RETURNING ROWID BULK COLLECT INTO rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'EPIS_HIDRICS_LINE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'EPIS_HIDRICS_LINE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_id_epis_hidrics_line;

    -- Delete all rows for primary key column ID_EPIS_HIDRICS_LINE
    PROCEDURE del_id_epis_hidrics_line
    (
        id_epis_hidrics_line_in IN epis_hidrics_line.id_epis_hidrics_line%TYPE,
        handle_error_in         IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        del_id_epis_hidrics_line(id_epis_hidrics_line_in => id_epis_hidrics_line_in,
                                 handle_error_in         => handle_error_in,
                                 rows_out                => rows_out);
    END del_id_epis_hidrics_line;

    PROCEDURE del_ehl_cr_fk
    (
        id_cancel_reason_in IN epis_hidrics_line.id_cancel_reason%TYPE,
        handle_error_in     IN BOOLEAN := TRUE,
        rows_out            OUT table_varchar
    ) IS
    BEGIN
        DELETE FROM epis_hidrics_line
         WHERE id_cancel_reason = del_ehl_cr_fk.id_cancel_reason_in
        RETURNING ROWID BULK COLLECT INTO rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'EPIS_HIDRICS_LINE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'EPIS_HIDRICS_LINE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_ehl_cr_fk;

    PROCEDURE del_ehl_cr_fk
    (
        id_cancel_reason_in IN epis_hidrics_line.id_cancel_reason%TYPE,
        handle_error_in     IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        del_ehl_cr_fk(id_cancel_reason_in => id_cancel_reason_in,
                      handle_error_in     => handle_error_in,
                      rows_out            => rows_out);
    END del_ehl_cr_fk;

    PROCEDURE del_ehl_ehid_fk
    (
        id_epis_hidrics_in IN epis_hidrics_line.id_epis_hidrics%TYPE,
        handle_error_in    IN BOOLEAN := TRUE,
        rows_out           OUT table_varchar
    ) IS
    BEGIN
        DELETE FROM epis_hidrics_line
         WHERE id_epis_hidrics = del_ehl_ehid_fk.id_epis_hidrics_in
        RETURNING ROWID BULK COLLECT INTO rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'EPIS_HIDRICS_LINE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'EPIS_HIDRICS_LINE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_ehl_ehid_fk;

    PROCEDURE del_ehl_ehid_fk
    (
        id_epis_hidrics_in IN epis_hidrics_line.id_epis_hidrics%TYPE,
        handle_error_in    IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        del_ehl_ehid_fk(id_epis_hidrics_in => id_epis_hidrics_in,
                        handle_error_in    => handle_error_in,
                        rows_out           => rows_out);
    END del_ehl_ehid_fk;

    PROCEDURE del_ehl_ftfluid_fk
    (
        id_epis_hid_ftxt_fluid_in IN epis_hidrics_line.id_epis_hid_ftxt_fluid%TYPE,
        handle_error_in           IN BOOLEAN := TRUE,
        rows_out                  OUT table_varchar
    ) IS
    BEGIN
        DELETE FROM epis_hidrics_line
         WHERE id_epis_hid_ftxt_fluid = del_ehl_ftfluid_fk.id_epis_hid_ftxt_fluid_in
        RETURNING ROWID BULK COLLECT INTO rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'EPIS_HIDRICS_LINE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'EPIS_HIDRICS_LINE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_ehl_ftfluid_fk;

    PROCEDURE del_ehl_ftfluid_fk
    (
        id_epis_hid_ftxt_fluid_in IN epis_hidrics_line.id_epis_hid_ftxt_fluid%TYPE,
        handle_error_in           IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        del_ehl_ftfluid_fk(id_epis_hid_ftxt_fluid_in => id_epis_hid_ftxt_fluid_in,
                           handle_error_in           => handle_error_in,
                           rows_out                  => rows_out);
    END del_ehl_ftfluid_fk;

    PROCEDURE del_ehl_ftloc_fk
    (
        id_epis_hid_ftxt_loc_in IN epis_hidrics_line.id_epis_hid_ftxt_loc%TYPE,
        handle_error_in         IN BOOLEAN := TRUE,
        rows_out                OUT table_varchar
    ) IS
    BEGIN
        DELETE FROM epis_hidrics_line
         WHERE id_epis_hid_ftxt_loc = del_ehl_ftloc_fk.id_epis_hid_ftxt_loc_in
        RETURNING ROWID BULK COLLECT INTO rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'EPIS_HIDRICS_LINE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'EPIS_HIDRICS_LINE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_ehl_ftloc_fk;

    PROCEDURE del_ehl_ftloc_fk
    (
        id_epis_hid_ftxt_loc_in IN epis_hidrics_line.id_epis_hid_ftxt_loc%TYPE,
        handle_error_in         IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        del_ehl_ftloc_fk(id_epis_hid_ftxt_loc_in => id_epis_hid_ftxt_loc_in,
                         handle_error_in         => handle_error_in,
                         rows_out                => rows_out);
    END del_ehl_ftloc_fk;

    PROCEDURE del_ehl_ftway_fk
    (
        id_epis_hid_ftxt_way_in IN epis_hidrics_line.id_epis_hid_ftxt_way%TYPE,
        handle_error_in         IN BOOLEAN := TRUE,
        rows_out                OUT table_varchar
    ) IS
    BEGIN
        DELETE FROM epis_hidrics_line
         WHERE id_epis_hid_ftxt_way = del_ehl_ftway_fk.id_epis_hid_ftxt_way_in
        RETURNING ROWID BULK COLLECT INTO rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'EPIS_HIDRICS_LINE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'EPIS_HIDRICS_LINE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_ehl_ftway_fk;

    PROCEDURE del_ehl_ftway_fk
    (
        id_epis_hid_ftxt_way_in IN epis_hidrics_line.id_epis_hid_ftxt_way%TYPE,
        handle_error_in         IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        del_ehl_ftway_fk(id_epis_hid_ftxt_way_in => id_epis_hid_ftxt_way_in,
                         handle_error_in         => handle_error_in,
                         rows_out                => rows_out);
    END del_ehl_ftway_fk;

    PROCEDURE del_ehl_hid_fk
    (
        id_hidrics_in   IN epis_hidrics_line.id_hidrics%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) IS
    BEGIN
        DELETE FROM epis_hidrics_line
         WHERE id_hidrics = del_ehl_hid_fk.id_hidrics_in
        RETURNING ROWID BULK COLLECT INTO rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'EPIS_HIDRICS_LINE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'EPIS_HIDRICS_LINE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_ehl_hid_fk;

    PROCEDURE del_ehl_hid_fk
    (
        id_hidrics_in   IN epis_hidrics_line.id_hidrics%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        del_ehl_hid_fk(id_hidrics_in => id_hidrics_in, handle_error_in => handle_error_in, rows_out => rows_out);
    END del_ehl_hid_fk;

    PROCEDURE del_ehl_hid_loc_fk
    (
        id_hidrics_location_in IN epis_hidrics_line.id_hidrics_location%TYPE,
        handle_error_in        IN BOOLEAN := TRUE,
        rows_out               OUT table_varchar
    ) IS
    BEGIN
        DELETE FROM epis_hidrics_line
         WHERE id_hidrics_location = del_ehl_hid_loc_fk.id_hidrics_location_in
        RETURNING ROWID BULK COLLECT INTO rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'EPIS_HIDRICS_LINE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'EPIS_HIDRICS_LINE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_ehl_hid_loc_fk;

    PROCEDURE del_ehl_hid_loc_fk
    (
        id_hidrics_location_in IN epis_hidrics_line.id_hidrics_location%TYPE,
        handle_error_in        IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        del_ehl_hid_loc_fk(id_hidrics_location_in => id_hidrics_location_in,
                           handle_error_in        => handle_error_in,
                           rows_out               => rows_out);
    END del_ehl_hid_loc_fk;

    PROCEDURE del_ehl_hid_way_fk
    (
        id_way_in       IN epis_hidrics_line.id_way%TYPE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) IS
    BEGIN
        DELETE FROM epis_hidrics_line
         WHERE id_way = del_ehl_hid_way_fk.id_way_in
        RETURNING ROWID BULK COLLECT INTO rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'EPIS_HIDRICS_LINE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'EPIS_HIDRICS_LINE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_ehl_hid_way_fk;

    PROCEDURE del_ehl_hid_way_fk
    (
        id_way_in       IN epis_hidrics_line.id_way%TYPE,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        del_ehl_hid_way_fk(id_way_in => id_way_in, handle_error_in => handle_error_in, rows_out => rows_out);
    END del_ehl_hid_way_fk;

    PROCEDURE del_ehl_prof_fk
    (
        id_prof_cancel_in IN epis_hidrics_line.id_prof_cancel%TYPE,
        handle_error_in   IN BOOLEAN := TRUE,
        rows_out          OUT table_varchar
    ) IS
    BEGIN
        DELETE FROM epis_hidrics_line
         WHERE id_prof_cancel = del_ehl_prof_fk.id_prof_cancel_in
        RETURNING ROWID BULK COLLECT INTO rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'EPIS_HIDRICS_LINE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'EPIS_HIDRICS_LINE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_ehl_prof_fk;

    PROCEDURE del_ehl_prof_fk
    (
        id_prof_cancel_in IN epis_hidrics_line.id_prof_cancel%TYPE,
        handle_error_in   IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        del_ehl_prof_fk(id_prof_cancel_in => id_prof_cancel_in,
                        handle_error_in   => handle_error_in,
                        rows_out          => rows_out);
    END del_ehl_prof_fk;

    PROCEDURE del_ehl_prof_fk2
    (
        id_prof_last_change_in IN epis_hidrics_line.id_prof_last_change%TYPE,
        handle_error_in        IN BOOLEAN := TRUE,
        rows_out               OUT table_varchar
    ) IS
    BEGIN
        DELETE FROM epis_hidrics_line
         WHERE id_prof_last_change = del_ehl_prof_fk2.id_prof_last_change_in
        RETURNING ROWID BULK COLLECT INTO rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'EPIS_HIDRICS_LINE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'EPIS_HIDRICS_LINE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_ehl_prof_fk2;

    PROCEDURE del_ehl_prof_fk2
    (
        id_prof_last_change_in IN epis_hidrics_line.id_prof_last_change%TYPE,
        handle_error_in        IN BOOLEAN := TRUE
    ) IS
        rows_out table_varchar;
    BEGIN
        del_ehl_prof_fk2(id_prof_last_change_in => id_prof_last_change_in,
                         handle_error_in        => handle_error_in,
                         rows_out               => rows_out);
    END del_ehl_prof_fk2;

    -- Deletions using dynamic SQL
    FUNCTION dyndelstr(where_in IN VARCHAR2) RETURN VARCHAR2 IS
    BEGIN
        IF where_in IS NULL
        THEN
            RETURN 'DELETE FROM EPIS_HIDRICS_LINE';
        ELSE
            RETURN 'DELETE FROM EPIS_HIDRICS_LINE WHERE ' || where_in;
        END IF;
    END dyndelstr;

    FUNCTION dyncoldelstr(colname_in IN all_tab_columns.column_name%TYPE) RETURN VARCHAR2 IS
    BEGIN
        RETURN dyndelstr(colname_in || ' = :value');
    END;

    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
    BEGIN
        EXECUTE IMMEDIATE dyndelstr(where_clause_in);
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'EPIS_HIDRICS_LINE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'EPIS_HIDRICS_LINE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_by;

    PROCEDURE del_by
    (
        where_clause_in IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) IS
    BEGIN
        EXECUTE IMMEDIATE 'BEGIN ' || dyndelstr(where_clause_in) ||
                          ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
            USING OUT rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'EPIS_HIDRICS_LINE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'EPIS_HIDRICS_LINE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_by;

    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
    BEGIN
        EXECUTE IMMEDIATE dyncoldelstr(colname_in)
            USING colvalue_in;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'EPIS_HIDRICS_LINE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'EPIS_HIDRICS_LINE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_by_col;

    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN VARCHAR2,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) IS
    BEGIN
        EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr(colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
            USING IN colvalue_in, OUT rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'EPIS_HIDRICS_LINE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'EPIS_HIDRICS_LINE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_by_col;

    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
    BEGIN
        EXECUTE IMMEDIATE dyncoldelstr(colname_in)
            USING colvalue_in;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'EPIS_HIDRICS_LINE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'EPIS_HIDRICS_LINE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_by_col;

    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN DATE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) IS
    BEGIN
        EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr(colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
            USING IN colvalue_in, OUT rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'EPIS_HIDRICS_LINE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'EPIS_HIDRICS_LINE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_by_col;

    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
    BEGIN
        EXECUTE IMMEDIATE dyncoldelstr(colname_in)
            USING colvalue_in;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'EPIS_HIDRICS_LINE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'EPIS_HIDRICS_LINE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_by_col;

    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN TIMESTAMP WITH LOCAL TIME ZONE,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) IS
    BEGIN
        EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr(colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
            USING IN colvalue_in, OUT rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'EPIS_HIDRICS_LINE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'EPIS_HIDRICS_LINE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_by_col;

    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE
    ) IS
    BEGIN
        EXECUTE IMMEDIATE dyncoldelstr(colname_in)
            USING colvalue_in;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'EPIS_HIDRICS_LINE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'EPIS_HIDRICS_LINE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_by_col;

    PROCEDURE del_by_col
    (
        colname_in      IN VARCHAR2,
        colvalue_in     IN NUMBER,
        handle_error_in IN BOOLEAN := TRUE,
        rows_out        OUT table_varchar
    ) IS
    BEGIN
        EXECUTE IMMEDIATE 'BEGIN ' || dyncoldelstr(colname_in) || ' RETURNING ROWID BULK COLLECT INTO :rows_out; END;'
            USING IN colvalue_in, OUT rows_out;
    EXCEPTION
        WHEN e_check_constraint_failure THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner all_constraints.owner%TYPE;
                    l_name  all_constraints.constraint_name%TYPE;
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    pk_alert_exceptions.raise_error(error_name_in => 'CHECK-CONSTRAINT-FAILURE',
                                                    name1_in      => 'OWNER',
                                                    value1_in     => l_owner,
                                                    name2_in      => 'CONSTRAINT_NAME',
                                                    value2_in     => l_name,
                                                    name3_in      => 'TABLE_NAME',
                                                    value3_in     => 'EPIS_HIDRICS_LINE');
                END;
            END IF;
        WHEN e_integ_constraint_failure
             OR e_no_parent_key
             OR e_child_record_found THEN
            IF NOT handle_error_in
            THEN
                RAISE;
            ELSE
                DECLARE
                    l_owner    all_constraints.owner%TYPE;
                    l_name     all_constraints.constraint_name%TYPE;
                    l_id       PLS_INTEGER;
                    l_err_name VARCHAR2(32767) DEFAULT 'NO-PARENT-KEY-FOUND';
                BEGIN
                    get_constraint_info(l_owner, l_name);
                    IF SQLCODE = -2292 -- Child record found
                    THEN
                        l_err_name := 'CHILD-RECORD-FOUND';
                    END IF;
                    pk_alert_exceptions.register_error(error_name_in       => l_err_name,
                                                       err_instance_id_out => l_id,
                                                       name1_in            => 'OWNER',
                                                       value1_in           => l_owner,
                                                       name2_in            => 'CONSTRAINT_NAME',
                                                       value2_in           => l_name,
                                                       name3_in            => 'TABLE_NAME',
                                                       value3_in           => 'EPIS_HIDRICS_LINE');
                    pk_alert_exceptions.raise_error_instance(err_instance_id_in => l_id);
                END;
            END IF;
    END del_by_col;

    -- Initialize a record with default values for columns in the table.
    PROCEDURE initrec(epis_hidrics_line_inout IN OUT epis_hidrics_line%ROWTYPE) IS
    BEGIN
        epis_hidrics_line_inout.id_epis_hidrics_line   := NULL;
        epis_hidrics_line_inout.id_epis_hidrics        := NULL;
        epis_hidrics_line_inout.id_hidrics             := NULL;
        epis_hidrics_line_inout.id_way                 := NULL;
        epis_hidrics_line_inout.id_hidrics_location    := NULL;
        epis_hidrics_line_inout.flg_status             := NULL;
        epis_hidrics_line_inout.dt_creation            := NULL;
        epis_hidrics_line_inout.id_prof_cancel         := NULL;
        epis_hidrics_line_inout.id_cancel_reason       := NULL;
        epis_hidrics_line_inout.notes_cancel           := NULL;
        epis_hidrics_line_inout.dt_cancel              := NULL;
        epis_hidrics_line_inout.create_user            := NULL;
        epis_hidrics_line_inout.create_time            := NULL;
        epis_hidrics_line_inout.create_institution     := NULL;
        epis_hidrics_line_inout.update_user            := NULL;
        epis_hidrics_line_inout.update_time            := NULL;
        epis_hidrics_line_inout.update_institution     := NULL;
        epis_hidrics_line_inout.id_prof_last_change    := NULL;
        epis_hidrics_line_inout.dt_epis_hidrics_line   := NULL;
        epis_hidrics_line_inout.flg_action             := NULL;
        epis_hidrics_line_inout.id_epis_hid_ftxt_way   := NULL;
        epis_hidrics_line_inout.id_epis_hid_ftxt_fluid := NULL;
        epis_hidrics_line_inout.id_epis_hid_ftxt_loc   := NULL;
    END initrec;

    FUNCTION initrec RETURN epis_hidrics_line%ROWTYPE IS
        l_epis_hidrics_line epis_hidrics_line%ROWTYPE;
    BEGIN
        RETURN l_epis_hidrics_line;
    END initrec;

    FUNCTION get_data_rowid(rows_in IN table_varchar) RETURN epis_hidrics_line_tc IS
        data epis_hidrics_line_tc;
    BEGIN
        SELECT * BULK COLLECT
          INTO data
          FROM epis_hidrics_line
         WHERE ROWID IN (SELECT /*+opt_estimate(table,t,scale_rows=0.0000001))*/
                          *
                           FROM TABLE(rows_in) t);
        RETURN data;
    EXCEPTION
        WHEN OTHERS THEN
            pk_alert_exceptions.raise_error(error_name_in => 'get_data_rowid');
    END get_data_rowid;

    FUNCTION get_data_rowid_pat(rows_in IN table_varchar) RETURN epis_hidrics_line_tc IS
        PRAGMA AUTONOMOUS_TRANSACTION;
        data epis_hidrics_line_tc;
    BEGIN
        data := get_data_rowid(rows_in);
        COMMIT;
        RETURN data;
    EXCEPTION
        WHEN OTHERS THEN
            pk_alert_exceptions.raise_error(error_name_in => 'get_data_rowid');
            ROLLBACK;
    END get_data_rowid_pat;

BEGIN
    NULL;
END ts_epis_hidrics_line;
/
