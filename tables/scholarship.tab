

  CREATE TABLE SCHOLARSHIP
   (	ID_SCHOLARSHIP NUMBER(12,0) NOT NULL ENABLE,
	CODE_SCHOLARSHIP VARCHAR2(200) NOT NULL ENABLE,
	FLG_AVAILABLE VARCHAR2(1) NOT NULL ENABLE,
	RANK NUMBER(3,0) NOT NULL ENABLE,
	ADW_LAST_UPDATE DATE NOT NULL ENABLE
   );


COMMENT ON TABLE scholarship IS 'Habilitações literárias' ;

COMMENT ON COLUMN scholarship.code_scholarship IS 'Código para tradução' ;
COMMENT ON COLUMN scholarship.flg_available IS 'Disponibilidade do registo na instituição onde está instalada a BD' ;
/




