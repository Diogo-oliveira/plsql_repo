

  CREATE TABLE PAT_PROB_VISIT
   (	ID_PAT_PROB_VISIT NUMBER(24,0) NOT NULL ENABLE,
	ID_VISIT NUMBER(24,0) NOT NULL ENABLE,
	ID_PAT_PROBLEM NUMBER(24,0) NOT NULL ENABLE
   );


COMMENT ON TABLE pat_prob_visit IS 'Associação de visitas a problemas e vice-versa.Serve para relacionar visitas.' ;
