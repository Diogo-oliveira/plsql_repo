

  CREATE TABLE DISCH_PREP_MESG
   (	ID_DISCH_PREP_MESG NUMBER(24,0) NOT NULL ENABLE,
	ID_PREP_MESSAGE NUMBER(12,0) NOT NULL ENABLE,
	RANK NUMBER(6,0) NOT NULL ENABLE,
	ID_DISCHARGE NUMBER(24,0) NOT NULL ENABLE
   );


COMMENT ON TABLE disch_prep_mesg IS 'Instruções de tratamento a adicionar às recomendações dadas ao doente ou a outro profissional' ;
