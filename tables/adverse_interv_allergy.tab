

  CREATE TABLE ADVERSE_INTERV_ALLERGY
   (	ID_ADVERSE_INTERV_ALLERGY NUMBER(24,0) NOT NULL ENABLE,
	ID_INTERVENTION NUMBER(24,0) NOT NULL ENABLE,
	ID_ALLERGY NUMBER(12,0) NOT NULL ENABLE,
	RANK NUMBER(24,0) NOT NULL ENABLE,
	ADW_LAST_UPDATE DATE NOT NULL ENABLE
   );


COMMENT ON TABLE adverse_interv_allergy IS 'Associação de intervenções com alergias, de modo a alertar o profissional quando requer para o utente uma intervenção que utilize produtos aos quais é alérgico' ;
