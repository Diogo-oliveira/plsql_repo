

  ALTER TABLE CLI_REC_REQ ADD CONSTRAINT CRR_PK PRIMARY KEY (ID_CLI_REC_REQ) ENABLE;

  ALTER TABLE CLI_REC_REQ MODIFY (ID_CLI_REC_REQ NOT NULL ENABLE);

  ALTER TABLE CLI_REC_REQ MODIFY (DT_CLI_REC_REQ NOT NULL ENABLE);

  ALTER TABLE CLI_REC_REQ MODIFY (ID_PROF_REQ NOT NULL ENABLE);

  ALTER TABLE CLI_REC_REQ MODIFY (ID_EPISODE NOT NULL ENABLE);

  ALTER TABLE CLI_REC_REQ MODIFY (FLG_STATUS NOT NULL ENABLE);


  ALTER TABLE CLI_REC_REQ MODIFY (FLG_TIME NOT NULL ENABLE);


