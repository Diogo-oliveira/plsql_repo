

  ALTER TABLE PAT_PROB_VISIT ADD CONSTRAINT PPV_PK PRIMARY KEY (ID_PAT_PROB_VISIT) ENABLE;

  ALTER TABLE PAT_PROB_VISIT MODIFY (ID_PAT_PROB_VISIT NOT NULL ENABLE);

  ALTER TABLE PAT_PROB_VISIT MODIFY (ID_VISIT NOT NULL ENABLE);

  ALTER TABLE PAT_PROB_VISIT MODIFY (ID_PAT_PROBLEM NOT NULL ENABLE);


