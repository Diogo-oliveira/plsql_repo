
ALTER TABLE PAT_ADVANCE_DIRECTIVE ADD CONSTRAINT PATADVDIR_PK PRIMARY KEY (ID_PAT_ADVANCE_DIRECTIVE) ENABLE;

alter table PAT_ADVANCE_DIRECTIVE
  add constraint PATADVDIR_FLG_CHK
  check (FLG_HAS_ADV_DIRECTIVE IN ('Y','N','U'));
	
alter table PAT_ADVANCE_DIRECTIVE
  add constraint PATADVDIR_FLG_ST_CHK
  check (FLG_STATUS IN ('A','O','C'));	