
  ALTER TABLE PAT_HISTORY_TYPE ADD CONSTRAINT PHE_PK PRIMARY KEY (ID_PAT_HISTORY_TYPE) ENABLE;

  ALTER TABLE PAT_HISTORY_TYPE MODIFY (ID_PAT_HISTORY_TYPE NOT NULL ENABLE);

  ALTER TABLE PAT_HISTORY_TYPE MODIFY (CODE_PAT_HISTORY_TYPE NOT NULL ENABLE);

  ALTER TABLE PAT_HISTORY_TYPE MODIFY (ACRONYM NOT NULL ENABLE);

  ALTER TABLE PAT_HISTORY_TYPE MODIFY (ADW_LAST_UPDATE NOT NULL ENABLE);


