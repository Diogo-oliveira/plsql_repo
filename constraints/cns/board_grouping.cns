

  ALTER TABLE BOARD_GROUPING ADD CONSTRAINT BGG_PK PRIMARY KEY (ID_BOARD_GROUPING) ENABLE;

  ALTER TABLE BOARD_GROUPING MODIFY (ID_BOARD_GROUPING NOT NULL ENABLE);

  ALTER TABLE BOARD_GROUPING MODIFY (ID_BOARD NOT NULL ENABLE);

  ALTER TABLE BOARD_GROUPING MODIFY (ID_BOARD_GROUP NOT NULL ENABLE);

  ALTER TABLE BOARD_GROUPING MODIFY (FLG_AVAILABLE NOT NULL ENABLE);

  ALTER TABLE BOARD_GROUPING MODIFY (ADW_LAST_UPDATE NOT NULL ENABLE);



